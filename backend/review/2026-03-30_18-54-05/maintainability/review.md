### 발견사항

- **[INFO]** SQL 마이그레이션에 롤백 전략 부재
  - 위치: `V003__add_trigger_category.sql`
  - 상세: PostgreSQL에서 `ALTER TYPE ... ADD VALUE`는 트랜잭션 내에서 롤백이 불가능한 DDL 변경입니다. 마이그레이션 실패 시 수동 복구가 필요할 수 있으며, 이 점이 문서화되어 있지 않습니다.
  - 제안: 주석으로 비가역성을 명시하거나, 다운 마이그레이션 스크립트가 있다면 그 한계를 기술

- **[INFO]** `users.controller.ts` — 응답 직렬화 로직의 재사용성
  - 위치: `users.controller.ts:13-23`
  - 상세: 유저 필드를 수동으로 선택하여 반환하는 방식은 필드가 추가될 때 컨트롤러와 관련 DTO/테스트를 함께 수정해야 하는 결합을 만듭니다. 현재 규모에서는 문제없으나, 유저 스펙이 확장되면 중복 수정 포인트가 됩니다.
  - 제안: `UserProfileDto` 혹은 serialization interceptor로 응답 형태를 단일화

- **[INFO]** `users.controller.spec.ts` — `mockUser`의 과도한 필드 포함
  - 위치: `users.controller.spec.ts:8-29`
  - 상세: `passwordHash`, `twoFactorSecret` 등 컨트롤러 테스트와 무관한 내부 필드가 목업에 모두 포함되어 있어 테스트의 의도 파악이 어렵습니다. `as never` 캐스팅도 타입 안전성을 우회합니다.
  - 제안: 컨트롤러가 실제로 사용하는 필드만 포함한 최소 목업을 사용하고, `as Partial<User>` 등 의미 있는 타입 캐스팅 적용

- **[INFO]** `users.controller.ts` — 인증 가드 미선언
  - 위치: `users.controller.ts:8-10`
  - 상세: `@CurrentUser()` 데코레이터가 JWT 페이로드에 의존하나, 컨트롤러 또는 메서드 수준에 `@UseGuards(JwtAuthGuard)` 같은 인증 가드가 명시되어 있지 않습니다. 글로벌 가드로 처리 중이더라도, 코드만 보면 보호 여부를 파악하기 어렵습니다.
  - 제안: 가드를 명시적으로 선언하거나, 글로벌 적용임을 주석으로 기술하여 의도를 명확히

---

### 요약

전반적으로 코드는 간결하고 단일 책임 원칙을 잘 따르고 있습니다. 컨트롤러는 짧고 읽기 쉬우며, 테스트도 시나리오가 명확하게 분리되어 있습니다. 다만 유저 응답 필드를 컨트롤러에서 직접 매핑하는 방식, 과도하게 무거운 테스트 목업, 인증 가드의 암묵적 의존은 장기적으로 변경 비용을 높일 수 있는 소소한 유지보수 리스크입니다. SQL 마이그레이션의 비가역성은 운영 관점에서 인지하고 있어야 합니다.

### 위험도
**LOW**