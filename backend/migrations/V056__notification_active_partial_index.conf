# V056 은 CREATE INDEX CONCURRENTLY / DROP INDEX CONCURRENTLY 를 사용한다.
# 두 명령 모두 transaction block 안에서 실행 불가하므로 Flyway 에 트랜잭션 비활성화를 지시한다.
# 자세한 운영 영향·롤백 절차는 V056__notification_active_partial_index.sql 헤더 주석 참조.
executeInTransaction=false
