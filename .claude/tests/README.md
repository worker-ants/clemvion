# `.claude/tests/` — harness self-tests

Unit tests for the **harness's own Python** (hooks, skill libs, config) — not
the product code under `codebase/`. These guard the automation layer that has
no other coverage: branch guards, branch normalization, and the multi-place
agent registry.

## Run

```bash
python3 -m unittest discover -s .claude/tests -p 'test_*.py'
```

No install step. The suite uses **only the standard library** (`unittest`,
`unittest.mock`), matching the harness convention that its Python carries zero
third-party dependencies — hooks must run on a bare `python3`. Do not introduce
`pytest`/`requirements.txt` here without revisiting that convention.

## What's covered

| File | Guards |
|---|---|
| `test_agent_consistency.py` | Registry drift across the 4 places an agent is declared: `role_instructions.py` (SSOT) ↔ `.claude/agents/<name>.md` ↔ `.claude.project.json` toggles ↔ `README.md` table. Catches add/rename/remove misses; does **not** police prose wording (the `.md` is an intentionally human-edited rendering). |
| `test_branch_guard.py` | `branch_guard.evaluate()` decision table — block only on (main worktree) ∧ (current == origin default); allow otherwise. Default branch is resolved, not hardcoded to `main`. |
| `test_branch_naming.py` | `worktree-*` → `claude/*` normalization: idempotent, skips main worktree / pushed branches / detached HEAD, appends a slug on collision. |
| `test_dependabot_npm_coverage.py` | Every npm tree OUTSIDE the pnpm workspace is registered in `.github/dependabot.yml` (and no entry points at a tree that no longer exists). `pnpm audit` only sees the workspace, so an unregistered independent `package.json` has **no** vulnerability signal at all — that is how `.claude/tools/mermaid-lint` carried an `undici` HIGH unnoticed until 2026-07-18. Workspace membership uses pnpm's glob semantics (a single `*` does not cross `/`), not `fnmatch`, which would classify a nested tree as covered and reproduce the very blind spot this guards. |
| `test_doc_sync_matrix.py` | PROJECT.md's "변경 유형 → 갱신 위치 매핑" matrix + its JSON SSOT (`.claude/config/doc-sync-matrix.json`): JSON shape, row-count 1:1 binding with the prose table, and every referenced `*.test.ts` / `spec/...md` / trigger-glob base path exists. Reaches into `codebase/` and `spec/` on purpose (the matrix is a harness↔product binding). |
| `test_orchestrator_state.py` | The code-review orchestrator's CLI state machine (`--update` / `--apply-routing` / `--summary-state` / `--resume`): bucket transitions, rate-limit episode/reset-hint tracking, routing selection + forced-agent retention, fallback. Driven via subprocess (the real CLI surface; also avoids the two-`_lib` import collision). |
| `test_review_guard.py` | `review_guard.evaluate_review()` block/allow table + the `SUMMARY.md` / `RESOLUTION.md` resolved-state parser, spec `code:` glob → regex, the spec-impl `--impl-done` Gate 2, and the **forced-coverage** gate (`agents_forced` reviewers must each leave a non-empty report, resolved session-relative — never from the manifest's `output_file`, which names a since-deleted worktree). Git/fs helpers are patched (hermetic). |
| `test_consistency_orchestrator_state.py` | The consistency orchestrator's read paths (`--summary-state` / `--resume`) reconciling `_retry_state.json` with reports on disk. Exists because the two orchestrators mirror each other by duplication but only the code-review one had tests — which let a change land where the SKILLs documented self-healing that only half the pair implemented. Subprocess-driven. |
| `test_report_paths_shared.py` | `.claude/_shared/report_paths.py` — the report location/existence rule the push/stop gate and both orchestrator CLIs must answer identically. Pins the two rules (session-relative anchoring; non-empty) plus the directory-basename edge cases (`output_file` ending in `/`, or a bare `.`/`..`) that once let a directory be misjudged a report. `AgreementTest` drives the real gate function and a real `--verify-coverage` subprocess side by side and asserts they agree — the thing that actually matters, since a divergence here was measured in production (2026-07-17) despite a "change both" comment. |
| `test_summary_agent_contract.py` | The three summary agent definitions ↔ what `.claude/workflows/*.js` actually sends them: no file may blame the write block on terminal position (refuted 2026-07-17 — the rule is exact basename), every definition must state the basename rule, take the inlined report bodies as authoritative, persist missing per-agent files, and flag unobtained findings as a false negative. **Prose-checking on purpose** — see the convention note below. |
| `test_workflow_scripts.py` | `.claude/workflows/*.js` syntax (parsed as the harness VM wraps them — `node --check` alone passes even a duplicate `const` on these files) + the `SHARED-BLOCK` mirror of `_lib/agent-return.mjs` staying byte-identical across the three fan-out workflows, since the sandbox forbids `import`. |
| `test_reap_merged_worktrees.py` | `reap-merged-worktrees.sh` end-to-end against a real throwaway git repo with actual worktrees/branches (`gh` stubbed via `REAP_GH_BIN`): removes a worktree only on a `gh`-confirmed merged PR, skips dirty/unmerged/`gh`-unconfirmed ones, and — the incident this covers — never removes a `--keep`-protected worktree even when the shell cwd is a *different* worktree (the state `EnterWorktree` leaves behind), while still reaping everything else. Also exercises `bootstrap-session.sh` driving the reaper end-to-end so a `--keep` the reaper honours but bootstrap never sends cannot slip through unnoticed. |
| `test_bootstrap_mermaid_install.py` | `bootstrap-session.sh`'s mermaid-lint install guard against a real temp repo with a stubbed `npm`: installs once and writes the completion marker, a partial `node_modules` with no marker is reinstalled (not accepted forever — the silent-permanent-disable failure this guard exists to kill), a failed install leaves no marker (retries) and stamps a cooldown (throttles). The guard is **marker-only, not lock-serialised** (the earlier hand-rolled `mkdir` lock was dropped after its stale-lock steal proved a reproducible check-then-act race, review 2026/07/18/02_06_42 C1); `test_concurrent_cold_start_converges_and_then_stops_reinstalling` pins the property that survives — concurrent sessions are **not** serialised, but they converge to a marker and later sessions then skip. Also exercises the **lockfile-hash binding**: the marker records the `package-lock.json` hash so a changed lockfile (a merged Dependabot bump) reinstalls rather than the marker masking a stale tree, the hash is recomputed post-install so an npm-rewritten lockfile still converges, it degrades to presence-only with no hashing tool, a legacy empty marker migrates to a hash once, a failed bump keeps the old marker and recovers after the cooldown, and a lockfile change under concurrent starts converges the marker's content (not mere existence) to the new hash. |
| `test_mermaid_lint_ready.py` | `_lib/mermaid_lint_ready.py`, the single readiness SoT the three consumers share: `is_ready()` requires node_modules **and** the marker (a bare dir is not ready), plus a **cross-language binding** test that fails if `bootstrap-session.sh` (writer), `.githooks/pre-commit` (bash reader), or `lint_mermaid_posttooluse.py` (python reader) drift from `MARKER_NAME` — bash and python can't share a runtime constant, so the agreement is test-guarded (cf. `test_doc_sync_matrix`). |
| `test_review_guard_hardening.py` | The checkout-/rebase-immune freshness rework: porcelain rename parsing, `**/` segment-boundary globbing, the session-dir clock, dirty→mtime vs clean→author-date split, in-flight suppression, and the **rebase author-date** regression (a rebase that only rewrites committer date must not re-arm the gate). Also the **resolution-in-flight** suppression: `_resolution_in_flight` (dispatch marker + applier-started state, both TTL-bound), the `mark_`/`clear_resolution_in_flight` PreToolUse(Agent)/SubagentStop marker hooks, and the Stop guard's suppression + review-done nudge wording. The rebase case uses a **real temp git repo** — see the convention note below. |
| `test_router_safety_policy_doc.py` | `router_safety.py`'s routing-policy table — which calls itself "정책의 단일 진실 원천" — and README's declared mirror, held against the constants they describe: the source-extension count in **each document's own table** plus a phrasing-agnostic sweep, the spelled-out list in both, the source-code row's reviewers vs `_SOURCE_FORCED_REVIEWERS`, one table row per `_RULES` entry with **each row's reviewers compared to the rule it describes** (positional, both documents) and README's rows compared to the docstring's cell by cell, the roster vs `ALL_AGENTS`, and — swept proactively after the miss below — every "디폴트 N개" reviewer-count claim across all four documents that state one. The obligation was documented ("정책 변경 시 두 곳을 같이 갱신하라") and unenforced: both tables read **24 extensions** while the set held **44**, stale since 2026-05-16. Note the guard's own first version checked the docstring count and README's *list* while calling that "both docs", leaving README's table row stale and green — `/ai-review` caught it as CRITICAL on the commit meant to end the drift, which is why the counts are now asserted per document and by shape rather than by one spelling. A second round then found the row checks were union-level — deleting `documentation` from one row passed because another row still named it, and README's reviewer column was unchecked entirely — so the comparison is now row-by-row against `_RULES`. Prose-checking under the convention exception below — this table *is* the policy spec, so its numbers decide which reviewers run. |
| `test_router_decision_trust.py` | Whether a `review-router` decision may be trusted, in **both** routing paths (`--apply-routing` and `.claude/workflows/ai-review.js`). Measured 2026-07-23 (`review/code/2026/07/23/14_47_40`): the router returned `selected=false` for all 14 reviewers — the 7 forced ones included — reasoning "소스 코드 변경 없음(문서만 변경)" over a 19-file changeset holding a brand-new Python module, and `_apply_routing` silently re-added the forced reviewers and trusted the rest, so the run looked like a healthy 7-reviewer review. The rule pinned here is a **contract** check, not a count: forced reviewers are stated to the router as `selected=true` 고정, so returning one false — or omitting it, which used to drop it from `agents_pending` without even a skip record — discards the whole decision. Deliberately **not** a revival of the old "0 또는 1 → 전체 fallback" rule, retired in 6cd7376fc (#244); `test_workflow_has_no_zero_reviewer_fallback` guards against re-adding it by good intentions. The two implementations are run **side by side over a decision matrix** (python via `runpy`, the JS function via `node -e`) because the workflow sandbox forbids imports, so nothing but a test can hold them together. Also pins the prompt's deterministic source/doc composition block — the incident's rationale was a composition claim the prompt itself can refute. |
| `test_line_anchors.py` | The line-number gutter reviewer prompts carry (`code-review-agents/lib/line_anchors.py` + its wiring in `build_files_section`). Reviewers used to receive bare code fences, so a finding's `위치` was whatever the model counted from the top of the assembled `_prompts/<agent>.md` — measured on `review/code/2026/07/17/20_06_14`, all seven cited numbers decoded as offsets into that 1,385-line document ("line 1362" cited for a 99-line file). Pins the property that actually matters: every number emitted points at the right text, and **none is emitted when correctness cannot be established** (unparseable header, body disagreeing with the counts git declared, combined `@@@` diff → verbatim fall-through). `GutterCorrectnessAgainstRealGitTest` replays real history rather than fixtures — unified diff is git's format, not ours, so fixtures would only confirm our model of it (the same exception `test_review_guard_hardening` takes for rebase dates). Also pins the reviewer-definition wording, which is behaviour here (see the convention note below), including a **byte-identical check across all 13 hand-copied `위치` blocks** — substring assertions alone would let one file's wording rot unnoticed. |
| `test_forced_coverage_selection.py` | `_newest_resolved_review_mtime` over **real, non-mocked session dirs** — the load-bearing safety argument for rolling the forced-coverage requirement out to *all* history without a grandfather clause: an under-covered session simply drops out of the "resolved" set, so nothing is retroactively broken, only future reviews are held to the bar. Every prior test of this function mocked it away; real dirs are used because the session clock is the directory name itself, which a mock cannot exercise. |
| `test_plan_guard.py` | `plan_guard` decision logic — which in-progress plan a branch is linked to, and whether that plan was updated (or moved to `plan/complete/`) before the push. This is the PLAN half of the pre-push gate; the REVIEW half is `test_review_guard.py`. |
| `test_guard_review_before_push_main.py` | The pre-push hook's `main()` **entry point**, run as a real process with a stubbed `_lib`: exit 0/2, REVIEW→PLAN gate order (review short-circuits), `BYPASS_*` isolation (one gate's override must not leak into the other), fail-open when a gate module fails to import or its `evaluate_*()` raises, and malformed/empty stdin. `_is_git_push`'s own detection lives in `test_push_guard_allowlist.py`. |
| `test_push_guard_allowlist.py` | Push DETECTION: the blind first-pass regex (frozen byte-for-byte) plus the enumerated allowlist that may only ever release. Differential — the pre-allowlist regex is pinned as a baseline and every command it blocks must still block unless the corpus carries an explicit release reason. Includes the 3-round regression corpus, the release-refusal cases (expansions, script heredocs, owner spoofing), and subprocess-timeout backtracking guards. |
| `test_push_guard_worktree_scope.py` | Push TARGET SELECTION — which *worktree(s)* the gates evaluate. Pins the closed **false-ALLOW hole**: before 2026-07-23 the hook evaluated only its own cwd, so running a push from a clean worktree while naming a different, unreviewed branch skipped the review gate entirely. Runs the real hook against a real git repo + linked worktree with path-keyed stub gates, so a test can prove the hook evaluated a worktree OTHER than cwd. Also covers `_mentions_branch` (bounded substring — `claude/foo` must not match `claude/foo-abc`), cwd still always evaluated, no blanket "any dirty worktree blocks everything", and `BYPASS_*` still suppressing a scoped block. |
| `test_lint_mermaid_exit_codes.py` | `lint-mermaid.mjs`'s exit-code contract against **real node**: a dependency that fails to import exits 3 (tooling broken), distinct from 1 (parse failure) and 2 (usage), so `.githooks/pre-commit` and the PostToolUse hook can fail open instead of blocking every markdown commit. Complements `test_mermaid_lint_ready.py`, which stubs node on purpose. |
| `test_run_test_watchdog.py` | `run-test.sh`'s watchdog: a stage that exceeds its budget is killed by **process group** (so orphaned children die with it) and reported as a timeout rather than hanging the run. |
| `test_report_playwright_flaky.py` | `scripts/report_playwright_flaky.py` — flaky-spec extraction from the Playwright JSON report, the rendered summary/annotations, and the fail-soft paths (missing/garbled report must never affect the build). |
| `test_check_e2e_playwright_config.py` | `scripts/check-e2e-playwright-config.py` — the preflight that binds the Playwright base image tag to the resolved `@playwright/test` version, so a version bump cannot silently run against a mismatched browser image. |
| `test_consistency_impl_done.py` | Regression for the consistency checker's `--impl-done` false positive (a report counted as absent, blocking a clean run). |
| `test_consistency_target_validation.py` | The consistency orchestrator rejects a mode argument that is not a path — a typo'd flag must fail loudly instead of being taken as a target file. |
| `test_tests_readme_catalog.py` | This table itself: every `test_*.py` in this directory has a row, and no row names a file that no longer exists. Without it the catalog drifted to 9 of 27 files unlisted — a test whose purpose nobody recorded, and silently so. |

## Conventions for new tests

- Patch the git-backed helpers (`_repo_root`, `_run_git`, …), not git itself, so
  tests stay hermetic and fast. **Deliberate exception:** when the behaviour
  under test *is* a git semantic — e.g. that `git rebase` rewrites committer date
  but preserves author date, which `review_guard._newest_commit_time` relies on —
  build a real temp repo (`git init` in a `tempfile.mkdtemp()`, isolated via
  `GIT_CONFIG_GLOBAL=/dev/null`) and set `GIT_AUTHOR_DATE`/`GIT_COMMITTER_DATE`
  explicitly. Mocking would assert our model of git, not git. Keep these few and
  fast.
- Load harness modules via `_harness.load_module_by_path` when they would
  collide on the shared `_lib` package name (the two `_lib` packages under
  `hooks/` and `skills/`). See `_harness.py`.
- Assert **structural / behavioral invariants**, not prose. The `.md` agent
  definitions and docs are allowed to read differently from their SSOT.
  **Deliberate exception:** where the document *is* the runtime spec rather than a
  rendering of one — a sub-agent definition is the system prompt the model executes —
  its wording is behaviour, and `test_summary_agent_contract.py` pins the load-bearing
  phrases. That exception earned itself: a refuted explanation ("your write is blocked
  because you are the terminal sub-agent") sat in 7 such files while the workflows had
  begun asking those very agents to write per-agent files, and nothing caught the
  contradiction. Keep such checks to phrases that change what the agent *does*.
