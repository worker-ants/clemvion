---
worktree: guide-identifier-existence
started: 2026-09-13
owner: developer
spec_impact: none
---

# 내가 만든 가드가 **그 가드를 만들게 한 결함**을 못 잡는다

> `spec_impact: none` 은 **이 배치가 `spec/` 을 안 고친다**는 뜻이지 *"spec 갱신이 불필요"*
> 가 아니다. `user-guide-evidence.md §2` 등재는 필요하고 **developer 권한 밖**이라
> planner 항목으로 등재돼 있다(§D). 자기-반증형 소정정 예외에도 해당하지 않는다 —
> 그 예외는 *내가 쓴 예고 문장*에만 열리고, 가드 인벤토리 표는 그게 아니다.

트래커 항목 *"가이드가 적는 식별자(에러 코드·환경변수)가 실재하는지 세는 가드가 없다"*
(2026-09-12 등재)를 닫는다. **`#1330` 이 절반만 닫았고, 남은 절반이 원래 근거였다.**

## A. 재판정 — 이 항목은 왜 아직 열려 있나

항목의 처분 제안은 두 축을 요구한다: *"`content/docs/**` 의 UPPER_SNAKE 토큰이 backend
소스에 실재하는지"* — **에러 코드·환경변수** 둘 다.

`#1330` 은 `guide-error-code-existence` 로 **에러 코드 축**을 닫았다(3축 · 베이스라인 0 ·
허용목록 없음). 환경변수 축은 안 닫았다.

### 그런데 더 나쁘다 — 등재 근거였던 과거 결함을 `#1330` 가드가 못 잡는다

이 항목이 등재된 근거는 **`#1328` 이 손으로 고친 환경변수 오기**다:

```
- description: "… Plain-HTTP URLs are only allowed when `MCP_INSECURE_URL_ALLOWED` is
               enabled — otherwise calls are blocked."
+ description: "… Plain-HTTP URLs are only allowed when `MCP_ALLOW_INSECURE_URL` is
               enabled — otherwise calls are blocked."
```

그 줄에 `#1330` 가드의 술어를 그대로 걸어 **실측**했다:

| 축 | 판정 | 왜 |
|---|---|---|
| 축 1 `<FieldTable>` 의 `name` | ✗ | 토큰이 `name` 이 아니라 **`description` 안**에 있다 |
| 축 2 `code:` 값 | ✗ | `code:` 필드가 아니다 |
| 축 3 실패-문맥 산문 | ✗ | 그 줄에 실패 어휘가 **없다** (KO 쪽은 *"별도 안전장치"*) |

**셋 다 미포착.** `#1330` 이 *"허용목록 없이 오탐 0"* 을 얻으려고 문맥을 좁힌 대가가
**이 결함 클래스 전체**였다. 가드가 자기 존재 이유를 못 잡는 상태다.

> 이건 이 저장소가 반복해 온 형태다 — *"정의를 한 칸 좁게 잡는다"* 의 **거울상**.
> 좁혀서 오탐을 없앴는데 **잡아야 할 것까지 빠졌고**, 그 사실이 베이스라인 0 에 가려졌다.
> `#1330` 의 베이스라인 0 은 참이지만 **아무것도 보장하지 않았다**.

## B. 실측 — 넓은 축은 무엇을 잡고 무엇을 놓치나

기준집합을 **소스 토큰 ∪ env 선언처**(`.env.example` · compose)로 두고, 가이드의
**모든 백틱 UPPER_SNAKE** 를 후보로 잡았다:

| 항목 | 실측 |
|---|---|
| 기준집합 | 1,764종 (소스 1,743 ∪ env 전용 21) |
| 가이드 인용 | **91종** (92 MDX) |
| 기준집합에 없는 것 | **1종** — `MESSAGE_CREATE` (2곳) |
| 과거 결함 `MCP_INSECURE_URL_ALLOWED` | 기준집합에 **없다** → **잡힌다** ✓ |
| 정정된 이름 `MCP_ALLOW_INSECURE_URL` | 기준집합에 **있다** → 통과 ✓ |

즉 넓은 축은 **참 양성을 잡고 오탐이 정확히 1건**이다.

### 기준집합 통합의 위험도 — 실측

env 선언처에만 있는 21종을 전수로 봤다: `POSTGRES_*` · `MINIO_*` · `NEXT_PUBLIC_*` ·
`CHOKIDAR_*` · `PLAYWRIGHT_BASE_URL` 등 **전부 인프라·런타임 설정 이름**이고
`*_FAILED`/`*_ERROR`/`*_NOT_FOUND` 처럼 **에러 코드꼴은 0건**이다. 따라서 기준집합을
통합해도 에러 코드 축이 가려질 위험은 없다.

> **다만 이 병합은 오늘 판정을 지탱하지 않는다 — 뮤턴트가 내 예측을 반증했다.**
> "env 선언처를 빼면 RED" 를 예측했는데 **GREEN** 이었다. 이유를 재니 가이드가 인용하는
> env 변수 8종이 **전부 소스에도** 있었다(`process.env.X`). 소스에 없고 선언처에만 있는
> 21종 중 **가이드 인용은 0종**이다.
>
> 그래도 남긴다 — `#1330` 이 `packages` 를 남긴 것과 같은 이유다. 가이드가 내일
> `POSTGRES_PASSWORD` 를 적으면 기준집합이 좁을 때 **실재하는 변수에 RED** 가 난다.
> 즉 *오늘의 검출*이 아니라 *내일의 오탐*을 막는다. 그 전환 시점을 테스트로 고정했다 —
> env-only 토큰이 인용되기 시작하면 그 단언이 RED 가 되어 신호를 준다.

## C. 처분 — 넓은 축을 추가하고, 허용목록을 **방어적으로** 만든다

`#1330` 의 *"허용목록 없음"* 은 이번 축에서 **유지할 수 없다**. 그 성질을 지키려면 문맥을
좁혀야 하고, 좁히면 §A 가 보인 대로 결함 클래스가 통째로 빠진다. **어느 쪽을 포기할지가
이 항목의 실제 결정이고, 실측이 답을 정했다.**

대신 허용목록이 *은폐 수단*이 되지 않도록 네 가지를 강제한다:

| 강제 | 왜 |
|---|---|
| 각 항목에 **외부 시스템 이름**을 적는다 | *"Planned 니까"* 류의 사유를 쓸 수 없게 |
| **상한**을 둔다 | 넘으면 통과가 아니라 **결정**을 강제 |
| 각 항목이 **여전히 인용되는지** 단언 | 안 쓰이는 항목이 쌓여 규모가 무의미해지는 것 방지 |
| 각 항목이 **기준집합에 없음**을 단언 | 우리 것이 되면 항목이 **RED 로 강제 제거** |

### 왜 별 가드가 아니라 기존 가드의 축으로 넣나

`#1330` 의 축 1·2 는 **따옴표 안** 토큰을 본다(`{ name: "X" }` · `"code": "X"`). 이번 축은
**백틱** 토큰을 본다. 트래커 항목도 *"하나의 가드"* 를 요구한다 — 한 파일에 두면
*"가이드가 적은 식별자는 실재해야 한다"* 가 한 곳에 산다.

### 축 3 은 **교체**된다 — 새 축이 그것을 포함한다

`#1330` 의 축 3 은 *"실패 문맥 줄의 백틱 토큰"* 이고, 새 축은 *"모든 백틱 토큰"* 이다.
**후자가 전자를 포함한다**(기준집합을 통합하면 엄격히 포함). 둘을 함께 두면 같은 토큰이
두 축으로 두 번 보고되고, 축별 floor 가 서로를 가린다. 그래서 **더하는 게 아니라 교체**한다.

최종 축:

| 축 | 대상 | 허용목록 |
|---|---|---|
| `field-table` | `<FieldTable>` 의 `name` (따옴표) | 없음 |
| `code-field` | `code:` 값 (따옴표) | 없음 |
| `backtick` | **모든 백틱 UPPER_SNAKE** | 외부 어휘만 (§C 4강제) |

### 명명 (`--impl-prep` naming_collision INFO#2·#3·#4)

| 대상 | 결정 | 이유 |
|---|---|---|
| 파일명 `guide-error-code-*` | **`guide-identifier-*` 로 리네임** | 스코프가 에러 코드를 넘어 식별자 전반이 된다. 이름이 구현보다 **좁으면** 다음 사람이 env 변수 가드를 찾다가 없다고 판단한다 — `#1330` 이 겪은 *"통과했으니 실재한다"* 오독의 사촌이다 |
| 축 라벨 | `"prose"` **재사용 안 함** → `"backtick"` | `"prose"` 는 *"실패 문맥 게이팅"* 의미를 이미 갖는다. 재사용하면 리포트만 보고 어느 판정인지 구분 못 한다 |
| 허용목록 이름 | `GUIDE_EXTERNAL_VOCABULARY` | 무관 도메인의 `KNOWN_DOCS_ABSENT` 와 `KNOWN_*` 접두를 공유하지 않도록 |

리네임 비용 실측 후 확정한다 — 참조처는 `PROJECT.md` 가드 카탈로그 2행과 트래커의 planner
등재 문구다(둘 다 이 PR 이 고칠 수 있다). 자매 `guide-sanitized-message-parity` 는 스코프가
안 바뀌므로 **리네임 대상이 아니다**.

## D. `--impl-prep` 결과 — **BLOCK: NO** (Critical 0 · WARNING 4 · 위험도 MEDIUM)

`review/consistency/2026/09/13/12_33_41`.

| # | 지적 | 처분 |
|---|---|---|
| 1 | **3 checker 수렴** — `guide-error-code-*` 가 자칭 SoT(`user-guide-evidence.md §2`)에 미등재인데 이번 축 추가가 gap 을 **넓힌다** | `#1330` 이 이미 planner 등재했다. 다만 **리네임**으로 등재 문구가 낡으므로 이 PR 이 그 문구를 갱신한다. 체크리스트에 추가 |
| 2 | *"허용목록 없음"* 원칙 번복의 근거가 plan·코드 주석에만 남고 spec Rationale 로 안 올라간다 | 같은 planner 항목에 **한 턴으로** 묶어 등재 — 두 번 나누면 표가 두 번 미완결이 된다(이 가드 계열의 반복 패턴) |
| 3 | plan frontmatter 에 `spec_impact` 없음 | **고침** — `none` + 의미 구분 주석 |
| 4 | `cafe24-api-metadata.md §4` 가 envelope 정의처를 *"Principle 7"* 로 오인용 (실제 **Principle 0**, `status` 필드도 누락) | **이번 plan 과 무관한 선재 결함**. `spec/**` 이라 planner 등재. 2026-05-16 작성 시점부터 오인용이고 재넘버링 탓이 아님을 checker 가 `git log -S` 로 확인했다 |

> **#1·#2 가 같은 곳을 가리킨다** — `user-guide-evidence.md` 는 이 가드 가족의 SoT 인데
> `#1330` 의 가드 2건도, 이번 축 변경의 근거도 아직 거기 없다. **한 planner 턴**에서
> 표·frontmatter·Rationale 을 함께 처리해야 한다.

INFO 중 #1(원 트래커 처분 제안 번복 표시)·#2~#4(명명)는 §C 와 체크리스트에 반영했다.

## E. 라운드 5 — 인스턴스가 아니라 클래스를 닫는다

`review/code/2026/09/13/16_04_15` (C0 · W4 · LOW) +
`review/consistency/2026/09/13/16_04_45` (**BLOCK: NO** · C0 · W1).

**라운드 4 의 내 주장이 반증됐다.** `CODE_FIELD` 경계를 `(?<![A-Za-z])` 로 고치면서 주석에
*"위험한 형태는 **전부** 소문자로 `code` 로 끝나는 키"* 라고 단정했는데, `_` 는 `[A-Za-z]` 가
아니라 `"error_code":`·`"http_code":` 가 그대로 통과했다. **단정이 고친 범위보다 넓었다.**

| 경계 | 6갈래 대조 불일치 |
|---|---|
| `(?<![A-Za-z])` (라운드 4) | **2건** |
| `(?<!\w)` (지금) | **0건** |

### 방식 전환 — 정규식 5개 전수 감사

라운드 2~4 가 경계 결함을 **한 축씩** 내놓은 것이 패턴으로 보였다. 그래서 이번엔 파일의
정규식 **5개를 한 번에** 점검하고 그 표를 코드 상단에 박았다:

| 정규식 | 경계 | 판정 |
|---|---|---|
| `FIELD_TABLE_NAME` | `{` 앵커 | 안전 |
| `CODE_FIELD` | `(?<!\w)` | 두 번 고침 |
| `BACKTICK` | 백틱 양쪽 | 안전 |
| `collectSourceTokens` | `\b…\b` | 안전하되 **자기검증 부재** → 대조군 추가 |
| `collectEnvDeclarations` ×2 | `^` 행 앵커 | 안전 |

`collectSourceTokens` 는 세 exported 함수 중 **유일하게** 합성 대조군이 없었고(형제 둘은
라운드 2·4 에서 얻었다) `\b` 제거 뮤턴트가 생존했다. 그 방향이 특히 조용한 이유도 적었다 —
경계가 없으면 기준집합이 **부풀어** 가짜 토큰이 우연히 통과한다(거짓 PASS).

| 뮤턴트 | 실측 |
|---|---|
| 경계를 라운드 4 판으로 되돌림 | **RED** |
| 경계 통째 제거 | **RED** |
| `collectSourceTokens` 의 `\b` 제거 | **RED** |

### 나머지 처분

| 지적 | 처분 |
|---|---|
| maintainability W#2 — `lastIndex` 보일러플레이트 4중 복제 | **등재**. 리셋 누락을 겨냥한 테스트가 없다는 것이 요지인데, 공유 헬퍼로 옮기는 리팩터라 이번 배치 범위를 넘는다 |
| documentation W#4 — 라운드 표 stale | **고침**(이 절) |
| SPEC-DRIFT | 등재분 (통산 8회 확인) |

## F. 라운드 6 — 리뷰어가 0을 냈고, 남은 것은 내 것이었다

`review/code/2026/09/13/16_28_47` (**C0 · W0** · INFO 8 · SPEC-DRIFT 1 · LOW) +
`review/consistency/2026/09/13/16_28_53` (**BLOCK: NO** · C0 · W1).

`--impl-done` 의 유일한 WARNING 은 `user-guide-evidence.md §2` 미등재 — **6라운드 연속**
같은 항목이고 planner 몫이다. checker 스스로 *"developer 쪽 조치는 완료 상태"* 라고 적었다.
요약을 믿지 않고 원 리포트를 grep 해 **`[CRITICAL]` 0건**을 확인했다(하향 여부 대조).

### 리뷰어 제안과 **반대 방향**으로 닫은 것 둘

| INFO | 리뷰어 제안 | 실제 처분 | 왜 |
|---|---|---|---|
| #1 `CODE_FIELD` 가 하이픈 키를 통과 | 경계를 `(?<![\w-])` 로 **좁히자** | **좁히지 않고 대조군으로 고정** | 이 가드에서 **과매치는 fail-closed** — 토큰이 `basis` 대조를 더 받을 뿐이다. 좁히면 검사 자체를 안 받아 **거짓 PASS** 방향. `x-code: "INTEGRATION_CALL_FAILED"` 같은 헤더 예시는 진짜 식별자를 담는다 |
| #2 compose 리스트 스타일 미지원 | 정규식을 **넓히자** | **넓히지 않고 한계를 고정** | 리스트 스타일이 생기면 그 변수가 기준집합에서 빠져 RED(거짓 경보). 넓히면 기준집합이 **부풀어 거짓 PASS** 방향 — 라운드 5 가 `collectSourceTokens` 에 적은 것과 같은 논리 |

8갈래로 직접 재현해 두 판본이 갈리는 자리를 특정했다 — `"x-code":`·`"status-code":`
**둘뿐**이고 나머지 6갈래는 동일하다. INFO#2 는 전제를 셌다: 리스트 스타일 **0건**,
매핑 스타일 66건(`docker-compose.yml` 24 · `docker-compose.e2e.yml` 42).

> **둘 다 «넓다/좁다» 를 방향으로 판정했다.** 이 저장소가 반복해 적은 교훈은 *"정의를 한 칸
> 좁게"* 인데, 그것은 **가드가 서술하는 층**에 달린 이야기다. 여기서 축 라벨은 판정에 안
> 쓰이고 판정은 `basis.has` 하나뿐이므로, 좁히기는 정밀도가 아니라 **커버리지**를 깎는다.

### INFO#6 — 우려는 맞다. 이름표 대신 **정본을 실행**했다

회귀 테스트가 `FIELD_TABLE_NAME` 을 손으로 재복제해 단언하고 있었다. 제안은 변수명에
`LEGACY_` 를 붙이자는 것이었는데, 이 축은 **정본이 살아 있으므로 실행하는 쪽이 맞다**.

같은 뮤턴트로 양쪽을 실측해 지적이 실재 위험임을 확인했다:

| 뮤턴트 M3 — 정본의 `UPPER_SNAKE` 완화 | 판정 |
|---|---|
| 옛 **손-사본** 단언 | `exec(BROKEN)?.[1] === undefined` → **GREEN (생존)** |
| 새 **정본-실행** 단언 | 정본이 `"None"` 을 잡아 `field-table` 축 발생 → **RED** |

`#1330` 의 실패-문맥 게이트는 **지금 코드에 없어** 손 사본 외에 재현 수단이 없다. 그쪽은
리터럴로 두되 이름을 `REMOVED_1330_CODE_CONTEXT` 로 바꿨다 — 같은 describe 안에서 **한쪽은
정본 실행, 한쪽은 사본**인 이유가 이름에 드러난다.

### 검증

| 뮤턴트 | 예측 | 실측 |
|---|---|---|
| M1 `CODE_FIELD` 를 하이픈까지 좁힘 | RED | **RED** — 하이픈 대조군 1건 지목 |
| M2 compose 를 리스트 스타일까지 넓힘 | RED | **RED** — 한계 대조군 + 들여쓰기 대조군 2건 |
| M3 `FIELD_TABLE_NAME` 의 `UPPER_SNAKE` 완화 | RED | **RED** — 새 정본-실행 회귀 포함 3건 |

가드 스위트 **31 → 34건** 전부 GREEN.

## 체크리스트

- [x] `/consistency-check --impl-prep spec/conventions/`
      `review/consistency/2026/09/13/12_33_41` — **BLOCK: NO** (Critical 0 · WARNING 4 · MEDIUM)
- [x] 리네임 `guide-error-code-*` → `guide-identifier-*` (+ `PROJECT.md` · 트래커 전방 참조 5곳.
      `#1330` 해소 블록 등 **역사 서술은 이름을 보존**하고 각주만 달았다)
- [x] 기준집합 통합 (소스 ∪ env 선언처) — 베이스라인 0 유지. **단 오늘 지탱하지 않음**(§B)
- [x] 축 3(실패 문맥) → **`backtick` 전수 축으로 교체** + `GUIDE_EXTERNAL_VOCABULARY` 4강제
- [x] **과거 결함 재현 테스트** — 3갈래(오기를 잡는다 / 정정된 이름 통과 / `#1330` 술어면 놓쳤다)
- [x] 뮤테이션 **7건 — RED 6 · GREEN 1**. 생존 1건(env 병합 제거)은 §B 에 반증으로 기록
- [x] `#1330` 의 "허용목록 없음"·"3축" 서술 정정 — 파일은 재작성, 트래커 해소 블록은 각주
- [x] planner 등재 갱신 — 파일명·스코프·**Rationale 요구**를 등재 문구에 반영
- [x] planner 등재 신규 — `cafe24-api-metadata.md §4` Principle 7→0 + `status` (선재, 무관)
- [x] 원 트래커 항목 종결 — 처분 제안에 **취소선 + 번복 근거**(그 제안대로 해서 결함을 놓쳤다)
- [x] `.claude/tools/run-test-all.sh` — **6회** 실행 전부 ALL PASS (lint · unit · build · e2e 307)

      > **타입체크 ratchet 둘이 `build` 안에서 함께 돈다는 것을 로그로 확인했다** (2026-09-08
      > 이후 구조). 이 배치가 만진 것은 `__tests__/*.ts` 인데, `tsconfig.json` 이 테스트 경로를
      > 빼고 `vitest run` 이 타입을 strip 하므로 **그 자리 타입 오류는 lint·unit 이 원리적으로
      > 못 본다** — `#1292` 가 14라운드 통과 후 CI 에서 처음 걸린 자리다.
      > 실측: `build-20260913-164859.log:150-151` 에 backend 197건/36파일 ·
      > frontend 52건/15파일 **baseline 일치**. 직접 재실행해도 같다.
      >
      > (처음엔 `grep -i 'ratchet\|typecheck'` 로 찾다 **0건**이 나와 "안 돈다" 로 오판할 뻔했다.
      > 성공 메시지가 한국어 *"타입 진단"* 이라 패턴이 내 질문보다 좁았다.)
- [ ] **PR 번호 확정 직후 트래커의 `#1331` 7곳을 실제 번호로 치환** — push 전에는
      번호가 확정되지 않아 *예측값*이 들어가 있다. `codebase/` 쪽 1곳은 라운드 6 에서
      **번호 없이** 다시 썼다(바로 옆 리뷰 세션 경로가 이미 확정 앵커다). `plan/**` 은 코드
      시계를 올리지 않으므로 리뷰-후 커밋으로 안전하다.

      **치환 대상은 `plan/in-progress/spec-draft-nullable-notation-followups.md` 의 7곳뿐이다.**
      `grep -rn '#1331' plan/` 은 **10곳**을 낸다 — 이 파일의 3곳은 *치환 항목 자신*과
      *제거된 주석의 역사 인용*이라 **바꾸면 안 된다**. 명령으로 적으면:
      `grep -n '#1331' plan/in-progress/spec-draft-nullable-notation-followups.md`.

      > 이 문구를 처음엔 *"`plan/` 의 7곳"* 이라 썼다가 사후 assert 가 **10곳**을 세어 잡았다.
      > 숫자는 맞았는데 **술어가 틀렸다** — 세어야 할 것은 "plan 안의 등장" 이 아니라
      > "치환해야 할 등장" 이었다. 명령을 그대로 적어 두면 다음 사람이 같은 혼동을 안 한다.

- [ ] `/ai-review` + `--impl-done spec/conventions/` — **라운드 7 대기**

      **완료 기준**: 마지막 라운드가 **`codebase/**` 수정 0 으로 끝날 것**. 라운드를 *처리*한
      것과 *수렴*한 것은 다르다 — 고치면 코드 시계가 올라가 다음 라운드가 필요해진다.

      | R | `/ai-review` | `--impl-done` | 그 라운드 후 `codebase/` 수정 |
      |---|---|---|---|
      | 1 | `14_41_14` · C0 W6 MEDIUM | `14_41_43` · BLOCK:NO · C0 W4 | 있음 → R2 필요 |
      | 2 | `15_03_06` · C0 W1 LOW | `15_03_36` · **BLOCK:YES · C1** | 있음 → R3 필요 |
      | 3 | `15_24_12` · C0 W2 LOW | `15_23_53` · BLOCK:NO · C0 W3 | **있음** → R4 필요 |
      | 4 | `15_42_54` · C0 W2 LOW | `15_43_24` · BLOCK:NO · C0 W2 | **있음** → R5 필요 |
      | 5 | `16_04_15` · C0 W4 LOW | `16_04_45` · BLOCK:NO · C0 W1 | **있음** → R6 필요 |
      | 6 | `16_28_47` · **C0 W0** LOW | `16_28_53` · BLOCK:NO · C0 W1 | **있음** → R7 필요 |
      | 7 | 대기 | 대기 | — |

      **`--impl-done` 은 라운드 4 부터 수렴했다** — checker 가 *"developer 턴에서 추가로 취할
      조치 없음"* 으로 명시했고 라운드 5 도 같다. 잔여는 전부 `spec/**`(planner 몫)이다.
      라운드가 이어지는 이유는 **`/ai-review` 가 매 라운드 가드 자신의 정밀도 갭을 한 개씩
      찾기 때문**이다.

      > **라운드 5 에서 방식을 바꿨다.** 라운드 2~4 는 *"리뷰어가 찾은 것 하나를 고치고 다음
      > 라운드를 기다린다"* 였고, 그래서 경계 결함이 한 축씩 나왔다. 라운드 5 는 **정규식 5개를
      > 한 번에 감사**해 그 표를 코드에 박았다(`guide-identifier-scan.ts` 상단). 클래스를 닫는
      > 시도이지 인스턴스를 닫는 것이 아니다 — 라운드 6 이 그 시도를 검증한다.
      >
      > **검증됐다.** 라운드 6 의 `/ai-review` 는 **Critical 0 · WARNING 0** — 2~5 라운드에서
      > 매번 나오던 경계 WARNING 이 처음으로 0이 됐다. 전수 감사가 인스턴스 추적보다 빨리
      > 클래스를 닫았다.

      > **그런데 라운드 6 도 수렴이 아니다 — 이번엔 «내» 발견 때문이다.** 리뷰어 WARNING 이
      > 0이어도 `codebase/` 를 고치면 코드 시계는 똑같이 올라간다. 고친 것은 두 갈래다:
      >
      > 1. **검증 불가능한 선행 참조** — 스캐너 주석이 *아직 열리지 않은* PR 번호(`#1331`)를
      >    인용하고 있었다. push 전에는 번호가 확정되지 않으므로 병렬 세션이 먼저 PR 을 열면
      >    **남의 PR 을 가리킨다**. 바로 옆에 확정된 리뷰 세션 경로가 이미 있어 번호는
      >    군더더기였다. 변경 파일 3개의 `#NNNN` 을 **전수로 세어** 선행 참조가 이 한 곳뿐임을
      >    확인했다(나머지는 `#1328`·`#1330`, 둘 다 머지분).
      > 2. **INFO 3건의 처분** — 둘은 리뷰어 제안과 **반대 방향**으로 닫았다(아래 §F).
      >
      > **라운드 7 의 정지 규칙을 결과를 보기 전에 선언한다**: `/ai-review` 가 **Critical 0
      > 이고 WARNING 0** 이면 INFO 내용과 무관하게 **거기서 멈춘다** — INFO 는 고치지 않고
      > 트래커에 등재한다. 이 선언이 없으면 *"이 INFO 는 싸니까 하나만 더"* 가 무한 회귀가
      > 된다. 이 저장소가 이미 적어 둔 규칙이기도 하다 — **Critical 0 + 코드 0 이면 doc-루프
      > 금지**.

      > **이 항목을 두 PR 연속 거짓으로 체크했다.** 직전 PR 에서도 라운드 1 세션만 인용한 채
      > `[x]` 였다. 원인이 같다 — **라운드를 하나 «처리»할 때마다 체크하려 든다.** 그런데 이
      > 항목의 완료는 *미래 라운드가 깨끗할 것*에 달려 있어 원리적으로 그 시점에 알 수 없다.
      >
      > 같은 규칙을 이 PR 안에서 **반대 방향으로도** 어겼다 — `run-test-all.sh` 와 이 항목이
      > 수행했는데 미체크였다(라운드 3 INFO#6). 한 규칙을 양방향으로 어긴 것이고, 처방은
      > 하나다: **완료 기준을 항목 본문에 적고 그 기준이 관측될 때만 체크한다.** 위에
      > 적어 두었다.
