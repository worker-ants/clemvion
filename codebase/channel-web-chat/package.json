{
  "name": "channel-web-chat",
  "version": "0.1.0",
  "private": true,
  "description": "임베드형 웹채팅 위젯 SPA (Next.js CSR 전용, output:export). spec/7-channel-web-chat.",
  "scripts": {
    "dev": "source .env 2>/dev/null; next dev --port ${PORT:-3013}",
    "build": "next build",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "//pin": "exact pin 사유 (PROJECT.md §버전 핀 정책): dompurify·marked = sanitize 경로(공급망 무결성), react·react-dom = monorepo 전역 정렬(19.2.4).",
  "dependencies": {
    "dompurify": "3.4.7",
    "marked": "18.0.4",
    "next": "^16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6",
    "@testing-library/react": "^16",
    "@types/dompurify": "^3.0.5",
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^9",
    "eslint-config-next": "^16.2.6",
    "jsdom": "^29.0.1",
    "typescript": "^5",
    "vitest": "^4.1.8"
  },
  "overrides": {
    "next": {
      "postcss": "^8.5.14"
    }
  },
  "engines": {
    "node": ">=24"
  }
}
