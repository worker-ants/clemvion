{
  "name": "@workflow/expression-engine",
  "version": "0.1.0",
  "description": "Expression language parser and evaluator for workflow builder",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "node -e \"const f=require('fs'),c=require('child_process');let ts=true;try{require.resolve('typescript/package.json')}catch{ts=false};if(ts){c.execSync('tsc',{stdio:'inherit'})}else if(!f.existsSync('dist')){throw new Error('prepare: typescript is unavailable and dist/ is missing — cannot produce the build output')}\"",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "dayjs": "^1.11.20"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/jest": "^30.0.0",
    "eslint": "^9.18.0",
    "globals": "^16.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.65.0"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "ts"
    ],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.(t|j)s$": "ts-jest"
    },
    "testEnvironment": "node"
  }
}
