# V132 는 DROP INDEX CONCURRENTLY / CREATE UNIQUE INDEX CONCURRENTLY 를 사용한다.
# 두 명령 모두 transaction block 안에서 실행 불가하므로 Flyway 에 트랜잭션 비활성화를 지시한다.
# 자세한 이유 · 운영 절차 · 롤백은 V132__trigger_endpoint_path_global_unique.sql 헤더 주석 참조.
executeInTransaction=false
