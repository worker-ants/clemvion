# 문서화(Documentation) 리뷰

## 발견사항

- **[WARNING]** 저장 대상 설명 주석이 이번 PR 의 새 코드와 어긋난다 (오래된 주석)
  - 위치: `codebase/backend/src/modules/triggers/triggers.service.ts:658`~`675`
  - 상세: 이 블록은 `**재읽은 행을 저장 대상으로 쓴다.**` 로 시작해 "요청 시작 시점의 `trigger`
    를 그대로 넘기면 …되돌아간다" 는 이전 PR(리뷰 `review/code/2026/09/14/19_07_43` database
    WARNING#2)의 수정을 설명한다. 그런데 바로 아래(679~714줄, 이번 PR 의 diff)에서 저장 대상은
    더 이상 재읽은 행(`target`) 자체가 아니라 `{ id: target.id, ...defined, config: mergedConfig }`
    로 구성한 **부분 객체**다 — `target.id` 만 재읽은 행에서 가져오고 나머지 컬럼은 싣지 않는다.
    658번 줄 제목이 말하는 "재읽은 행을 저장 대상으로 쓴다"는 문자 그대로는 더 이상 사실이 아니다.
    바로 아래(679~706줄)에 정확한 새 설명이 있어 실질적 혼선 위험은 낮지만, 이 함수는 극도로
    주석이 촘촘해 각 블록이 "지금 코드가 왜 이런가"를 규범적으로 설명하는 스타일이다 — 상위
    블록의 전제 문장이 하위 블록에 의해 조용히 뒤집힌 채 남아 있으면, 다음 편집자가 이 블록만
    읽고 "save 에 통째 엔티티가 간다"는 잘못된 멘탈모델을 가질 수 있다.
  - 제안: 658번 줄 머리말을 "재읽은 행의 id 를 저장 대상 키로 쓴다(값은 부분 객체로 좁힌다 — 아래
    679번 줄 참조)"처럼 최소 수정하거나, 두 블록을 병합해 하나의 일관된 서술로 만든다.

- **[WARNING]** 뮤테이션 실측 카운트("53개 케이스")가 이번 PR 의 변경 이후 재측정되지 않았다
  — 파일 자신의 규약을 어겼을 가능성
  - 위치: `codebase/backend/src/modules/triggers/__test-utils__/trigger-transaction-mock.ts:62`~`69`
  - 상세: 이 파일 상단 JSDoc 은 "`transaction` 이 콜백을 실행하지 않게 바꾸면 …53개 케이스가
    RED 다" 라고 실측치를 못박아 두고, 바로 아래 인용구에서 "**이 수는 시점 의존이다** … **이
    파일을 고칠 땐 다시 재라**" 라고 스스로 재측정 의무를 명시한다. 이번 PR 은 정확히 이 파일의
    `save` mock 을 동기 → 비동기로 바꾸고 반환값 처리(`result ?? target`)를 추가했으며
    (diff 114~133줄), 그 변경에 **직접 의존하는 신규 단위 테스트 2건**
    (`triggers.service.spec.ts` `update() — 저장 대상은 이 요청이 바꾸는 필드뿐이다`,
    `update() — save 반환값의 null 이 재읽은 값을 덮지 않는다`)까지 추가했다. 즉 파일 스스로 적어둔
    재측정 트리거 조건("이 파일을 고칠 땐")이 이번 PR 에서 충족됐는데, PR 안 어디에도(plan
    `trigger-save-partial-patch.md`, CHANGELOG) "53"이 갱신됐다는 언급이 없다. 실제로 카운트가
    달라졌는지 여부와 무관하게, 자체 규약이 요구하는 재측정 기록이 diff 에 없다는 사실 자체가
    문서-실측 불일치 위험이다(이 저장소가 반복 지적해 온 "PR 이 닫히는 시점의 값으로 다시 재라"
    패턴).
  - 제안: 뮤턴트(콜백 미실행)를 다시 걸어 `src/modules/triggers` RED 건수를 재측정하고, 숫자가
    바뀌었으면 62~69줄을 갱신한다. 안 바뀌었으면 "재측정함, 여전히 53" 같은 한 줄을 남겨 다음
    편집자가 같은 작업을 반복하지 않게 한다.

- **[INFO]** spec 의 ⚠️ "실측되지 않은 잔여" 문구가 이번 PR 로 반증됐으나 developer 권한 밖이라
  미수정 상태로 남아 있다 — 단, plan 에 이미 명시적으로 추적되어 있어 절차상 결함은 아니다
  - 위치: `spec/2-navigation/2-trigger-list.md:203`~`205`
  - 상세: "⚠️ **실측되지 않은 잔여**: PATCH 의 기본 저장 경로(엔티티 통째 저장)는 ① 재읽기와 저장
    사이의 CASCADE 창에서의 실패 방식, ② 락 밖 컬럼 한정 갱신과의 경합이 확인되지 않았다"는
    이번 PR 이 정확히 측정하고(①②) 고친(② 실결함) 그 자리다. 게다가 "엔티티 통째 저장"이라는
    전제 자체도 이번 PR 로 더 이상 사실이 아니다(부분 객체로 좁혔다). `spec/` 은 developer 쓰기
    권한 밖이라 이 PR 이 직접 고치지 않는 것은 역할 경계상 올바르며, `plan/in-progress/
    trigger-save-partial-patch.md` "이 PR 이 안 하는 것" 절에 planner 후속 작업으로 정확히
    등재되어 있다(§3 ⚠️ 교체 항목 1). 다만 머지 시점부터 그 planner PR 이 열리기 전까지는
    spec 이 코드보다 뒤처진 상태로 남는다 — plan 자신도 이를 INFO#2 로 인지하고 있다.
  - 제안: (조치 불요, 이미 트래킹됨) 머지 직후 planner 후속 PR 을 지체 없이 열 것.

## 요약

이번 PR 은 문서화 품질이 전반적으로 높다. CHANGELOG 항목은 근본 원인·재현 방법·수정 방향·PR
안에서 발견한 회귀까지 구체적 실측과 함께 서술했고, `triggers.service.ts` 의 창 1 로직에는 "왜
이렇게 하는가"를 실측 근거(리뷰 인용, e2e 파일 앵커)와 함께 조밀하게 남겼으며, 새 e2e 특성
테스트 파일은 인접 파일과의 경계를 JSDoc 으로 명시하고 plan 문서는 "내가 틀린 측정"까지 투명하게
기록해 다음 사람이 같은 함정(부분 객체 `save` 반환값을 재조회로 오해)에 빠지지 않도록 했다. 다만
그 조밀함의 대가로 인접한 두 주석 블록 중 상위 블록(저장 대상=재읽은 행)이 하위 블록(저장
대상=부분 객체)에 의해 조용히 낡았고, 별도 파일(`trigger-transaction-mock.ts`)이 스스로 못박은
재측정 의무가 이번 편집에서 지켜졌다는 기록이 없다. 둘 다 CRITICAL 은 아니며 코드 동작 자체에는
영향이 없다.

## 위험도

LOW
