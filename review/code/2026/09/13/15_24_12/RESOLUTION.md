# RESOLUTION — `/ai-review` 라운드 3 (`review/code/2026/09/13/15_24_12`)

Critical 0 · WARNING 2 · SPEC-DRIFT 1 · INFO 7 · 위험도 LOW. 전 항목 처분 완료.

## WARNING #1 — 주석에만 있고 아무도 겨누지 않은 설계 결정 (**고침**)

`UPPER_SNAKE` 가 *"밑줄 최소 1개(약어 `LLM`/`HTTP` 제외)"* 를 요구한다고 주석에 적어 놓고,
그 결정을 겨누는 테스트가 없었다. 리뷰어가 `(?:_[A-Z0-9]+)+` 의 `+`→`*` 뮤테이션을 걸었을 때
스위트가 그대로 GREEN.

**내가 직접 재현했다** — 같은 뮤턴트, 같은 결과(생존). 판별 fixture 를 넣으니 **RED** 로
뒤집혔다:

| | 뮤턴트 `+`→`*` |
|---|---|
| fixture 추가 전 | GREEN (24/24) |
| 추가 후 | **RED** (1 failed / 25) |

fixture 는 두 판정이 갈리는 값으로 짰다 — `` `LLM` ``·`` `HTTP` `` 는 안 집고
`` `LLM_TIMEOUT` `` 은 집는다. 왜 중요한지도 적었다: 약어를 집기 시작하면 가이드의 흔한
낱말이 전부 후보가 되고 **베이스라인이 통제 불능**이 된다.

## WARNING #2 — `.bak` 파일 (**조치 불요 — 존재하지 않는다**)

리뷰어가 `guide-identifier-scan.ts.bak` 이 워크트리에 방치돼 있다고 보고했다.
**실측: 워크트리 전체에 `.bak` 파일 0건**(`find . -name "*.bak"`), git status 도 깨끗하다.

같은 라운드의 **INFO#7 이 원인을 설명한다** — *"리뷰 도중 병렬 세션으로 추정되는 일시적
뮤테이션(+`.bak`/`.bak2`)이 관측됐으나 재확인 시점엔 자체 원복돼 있었고 MD5 도 일치"*.
즉 **다른 세션이 같은 워크트리에서 뮤테이션을 돌리던 순간을 documentation 리뷰어가 본 것**
이고, 그 사이 파일은 사라졌다.

> **이 저장소에 이미 등재된 항목이다** — *"리뷰 in-flight 중에 같은 워크트리에서 뮤테이션을
> 돌리지 않는다"*(프로세스). 이번이 그 항목의 실제 발현 사례다. 내 뮤테이션 스크립트는
> 백업을 **scratchpad**(`mut_bak_*`)에 두고 `finally` 로 원복하므로 소스 옆에 `.bak` 을
> 남기지 않는다 — 이 아티팩트는 내 것이 아니다.
>
> 지우라는 권고는 **지울 대상이 없어** 수행하지 않았다. 없는 파일을 "지웠다" 고 적는 것이
> 더 나쁜 기록이다.

## SPEC-DRIFT — 등재분 (통산 7번째 확인)

`user-guide-evidence.md §2` 가드 가족 미등재. `spec/**` 이라 권한 밖이고 자기-반증형 소정정
예외에도 해당하지 않는다(그 예외는 *내가 쓴 예고 문장*에만 열린다). planner 항목에 파일명·
스코프·Rationale 초안까지 실어 뒀다 — 리뷰어도 *"이번 리뷰가 새로 요구하는 조치는 없음"* 으로
확인.

## INFO — 조치한 것

| # | 처분 |
|---|---|
| 6 | plan 체크리스트 마지막 2항목이 **수행했는데 미체크**였다 → 갱신(라운드별 표 포함). 이 PR 에서 같은 규칙을 두 번째로 어긴 것 — 이전 PR 에서도 `run-test-all.sh` 를 4회 돌려 놓고 미체크였다 |

## INFO — 조치 불요 (사유 기록)

- **1 (architecture)**: `collectEnvDeclarations` 가 선언처별 고정 파라미터 2개 — 3번째
  선언처(k8s·CI)가 실제로 필요해지면 `{label, pattern, texts}[]` 순회로 일반화.
- **2 (architecture)**: 테스트 파일이 통합 가드·데이터 불변식·단위 대조군 세 층위 누적(305행)
  — 축 4개 초과 시 분리 신호로 기록.
- **3·4 (maintainability)**: 정규식 boilerplate 4회 · 설계 근거 3중 복제 — **폴더 전반의
  기존 관례**라 이번 diff 의 신규 결함 아님.
- **5 (testing)**: `readIfPresent` 의 부재 분기가 이 저장소 조건상 도달 불가 — 이식성 방어.
- **7 (교차 관측)**: 위 WARNING#2 의 원인. 조치 불요이나 **다음 라운드에서 유사한 순간적
  diff 를 이 PR 의 결함으로 오인하지 말 것**이라는 리뷰어의 당부를 그대로 남긴다.

## 검증

판별 fixture 뮤턴트 RED 확인 · 가드 스위트 **25/25** GREEN · `run-test-all.sh` 재실행(아래 커밋).
