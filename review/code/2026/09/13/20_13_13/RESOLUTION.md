# RESOLUTION — `/ai-review` 라운드 3 (`review/code/2026/09/13/20_13_13`)

Critical 0 · **WARNING 2** · INFO 12 · 위험도 **LOW**. forced 7명 전원 산출물 확보.

세 라운드의 궤적: **W7 (MEDIUM) → W4 (MEDIUM) → W2 (LOW)**. 남은 둘 중 하나는 `spec/`
권한 밖이고, 하나만 내 것이다.

## WARNING#2 — 1줄 헬퍼만 내 규율을 비켜 갔다 (고침)

`staleEntries` 가 두 호출부 모두 실코퍼스 베이스라인-0(`toEqual([])`)으로만 검증돼,
**필터 방향이 뒤집혀도 목록이 커지기 전까지는 우연히만 잡힌다**는 지적.

정확하다. 이 파일은 신규 함수마다 *"두 판정이 갈리는 값"* 을 고정해 왔는데
(`collectMatches`·`parseWhereRefs`·`isMessagePrefixOnly` 전부) **1줄 함수라는 이유로 이것만
건너뛰었다.** 판별 대조군 3건을 넣었다 — 인용 안 됨 / 인용됨 / 빈 목록.

뮤테이션으로 확인했다:

| 뮤턴트 | 예측 | 실측 |
|---|---|---|
| `staleEntries` 의 필터 방향 반전(`!cited` → `cited`) | RED | **RED** — 새 대조군 2건 + 두 호출부 2건 |

> 리뷰어는 *"우연히만 잡힌다"* 고 했는데 실측하니 **오늘은 호출부도 잡는다**(등록 3종이
> 전부 인용되므로 반전 시 3종이 stale 로 나온다). 다만 리뷰어의 요지는 *"목록이 비거나
> 커지면 그 우연이 사라진다"* 이고 그건 맞다 — 대조군은 그 우연에 의존하지 않는다.

## WARNING#1 — SPEC-DRIFT, 권한 밖 · 등재분 (조치 불요)

spec 6파일이 `CONTAINER_*` 를 코드로 서술한다. **리뷰어가 실측으로 내 쪽이 옳음을 확인**했다
(`execution-engine.service.ts:8016` 의 `nodeExec.error = { message }` — `code` 필드 없음).
라운드 1 에서 근거·선례(`3-loop.md:189-191`)와 함께 트래커에 등재했고, 리뷰어도
*"코드는 유지, planner 가 spec 을 갱신"* 으로 판정했다.

## `--impl-done` 라운드 3 WARNING#2 — bare 인용을 **또** 넣었다 (고침)

라운드 2 에서 이 클래스를 고치는 **그 커밋**이 새 인스턴스를 만들었다(`19_51_33`).
두 PR 통틀어 4번째다. 고치고 **술어를 «내가 추가한 568줄»** 로 좁혀 확인 — 0건.
(라운드 2 에서 이 술어를 «파일 전체» 로 잡아 선재분에 걸렸다.)

### 4번째 재발에 «코드로» 막으려다 규약 위반을 할 뻔했다

*"세 번째 재발이면 산문 말고 코드로"* 를 적용해 폴더 스코프 가드를 세우려 했다. 실측 결과:

| 실측 | 값 |
|---|---|
| 이 규약의 기계 강제 가드 | **이미 있다** — `dto-jsdoc-citation-guard.ts`, 다만 스코프가 **backend DTO JSDoc** |
| 가드 폴더의 bare 인용 | **4건** (내 1 + `spec-links.test.ts` 선재 3) |

작아서 붙일 만해 보였는데 **규약 본문이 그것을 배제하고 있었다** —
`review-citations.md §4`: *"기존 bare 인용(499건)은 그 자리를 **다음에 건드릴 때** 함께
맞춘다. **일괄 치환은 하지 않는다** — 날짜를 코드 컨텍스트로 하나씩 특정해야 하는 일이라
기계적 변환이 안 된다."*

폴더 가드는 `spec-links.test.ts` 의 선재 3건을 강제 정리하게 되므로 §4 위반이다.
**재발 횟수만 보고 처방을 정했는데, 그 처방의 근거 문서에 이미 반대 결정이 있었다.**
이번엔 판단이 «과잉 구축» 쪽으로 기울었고, 문서 읽기가 *"덜 하라"* 로 답을 정했다.

## INFO 12건 — 전부 조치 불요

| # | 사유 |
|---|---|
| 1·2 | security 확인분(ReDoS 없음·traversal 없음) |
| 3 | `user-guide-evidence.md §2` 미등재 — 등재분 (9라운드 확인) |
| 4 | 카탈로그 하드 리드 — 기존 관행, 2라운드 처분 유지 |
| 5·6 | 리뷰 산출물 동반 커밋·라운드 2 스코프 — 정상 |
| 7 | vacuity 임계값 상수화 — 3라운드 연속 이월(낮은 우선순위) |
| 8 | `matchAll` vs 수동 `lastIndex` 공존 — 파일 상단에 명시된 의도적 유예 |
| 9 | 두 목록 이름 인접 — JSDoc 대조표로 완화 |
| 10 | `walkTree` 반복 — 상한 5로 억제 |
| 11 | `where` 검증의 0건·2건 분기 — 순수 함수 분리 시 함께 |
| 12 | 프롬프트 번들이 파일 하나를 누락 표시 — **조립 과정의 누락**이고 리뷰어가 저장소 직접 확인으로 정상임을 확인 |

## 검증

- 가드 스위트 **68 → 71** GREEN
- `staleEntries` 방향 반전 뮤턴트 **RED** (새 대조군이 지목)
- `run-test-all.sh` 4회차 — 별도 기록
