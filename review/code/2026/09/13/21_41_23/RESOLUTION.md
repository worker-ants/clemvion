# RESOLUTION — `/ai-review` 라운드 7 (`review/code/2026/09/13/21_41_23`)

Critical 0 · **WARNING 2** · INFO 9 · 위험도 **LOW**. forced 7명 전원 확보.

## WARNING#2 — 내 뮤턴트가 **분기 하나를 덜 겨눴다** (고침)

라운드 6 이 넣은 `resolveSourceLines` 의 유일성 가드(`found.length === 1`)에 판별
fixture 가 없었다. 리뷰어가 `=== 1` → `>= 1` 뮤테이션으로 **76/76 GREEN 생존**을 관측했다.

**내가 라운드 6 에 돌린 뮤턴트는 «파일명을 없는 것으로»(0건 분기)였고, «2건 이상» 분기는
겨누지 못했다.** 같은 함수를 뮤테이션했는데 **다른 분기**를 봤다.

다중 매치가 가상이 아님을 실측했다 — `backend/src` 에 `index.ts` 가 **46개**. 등록이 늘어
그런 basename 을 가리키면 **조용히 엉뚱한 파일**을 읽는다. 실재하는 그 파일명으로
대조군 3건(0건 / 2건 이상 / 1건)을 짰다.

| 뮤턴트 | 리뷰어 실측 | 지금 |
|---|---|---|
| `found.length === 1` → `>= 1` | GREEN (생존) | **RED** — `[2건 이상]` 단독 |

> 이 파일이 **네 번째로** 밟는 «헬퍼 테스트 ≠ 호출부 테스트» 이고, 이번엔 한 겹 더 얇다 —
> 헬퍼를 테스트하긴 했는데 **분기 하나를 덜 겨눴다.**

## WARNING#1 — 줄 인용이 한 줄 어긋났다 (고침)

`nodeExec.error = { message }` 를 `:8016` 으로 인용했는데 실측 **8017**(8016 은
`nodeExec.status` 줄). 결론(`code` 필드 없음)은 참이라 설계에는 영향이 없다.

2곳을 고치고 `review/` 산출물 밖 **전수 확인 — 잔여 0건**.

## `--impl-done` 라운드 7 WARNING#3 — SoT 인용이 착지하지 않는다 (고침)

세 곳(스캐너 헤더 · 테스트 JSDoc · `PROJECT.md:300`)이
`spec/conventions/user-guide-evidence.md §2` 를 SoT 로 인용하는데 **그 문서에 이 가드가
없다**(실측 `grep -c guide-identifier` → **0**). `#1330` 이 가족을 만든 시점부터 그랬고
9라운드 동안 INFO 로 떠다니다 WARNING 이 됐다.

`spec/` 은 권한 밖이므로 **인용을 사실대로** 고쳤다 — 착지하는 SoT
(`error-codes.md` + `3-error-handling.md §1`)를 적고, *"가족 규약은 그 문서이지만 이 가드의
등재는 planner 트래커 대기"* 를 명시했다. 지금도 참이고 등재 후에도 참이다.

## `--impl-done` INFO#7 — `complete/` 봉인 대비 역참조 (고침)

카탈로그 탈출구의 **조건부 폐기** forward-note 가 내 plan 안에만 있었는데, 그 plan 은
`complete/` 로 봉인된다. 결정이 내려질 트래커 §1.4 항목에 역참조를 넣었다 — (a) 를 택하면
등록 2건이 불필요해지고, won't-do 면 탈출구 분기와 테스트 3건이 제거 대상이라는 **양방향**을
적었다.

## INFO 9건 — 조치 불요

| # | 항목 | 사유 |
|---|---|---|
| 1 | SPEC-DRIFT 6파일 | 7라운드 연속 · 등재분. 리뷰어가 *"코드가 옳고 spec 이 낡음"* 재확인 |
| 2 | `§2` 미등재 | 9라운드 연속 · WARNING#3 처분으로 서술은 정직해졌다 |
| 3 | 서두 주석이 69% | 라운드 8 리뷰어도 *"코드가 명시적으로 요구하는 의도된 설계"* 로 판정. plan 종료 시 정리 제안은 등재분 |
| 4·5·6 | `where` 4단 중첩 · 매직넘버 · `new Set` fixture | 누적 유예 유지 |
| 7 | `sourceLinesCache` 모듈 스코프 | 유일 호출부 — 두 번째가 생기면 재검토 |
| 8 | 카탈로그 하드 리드 | 라운드 1부터 관행 |
| 9 | security·scope 확인분 | 양성 |

## 검증

- 가드 스위트 **76 → 79** GREEN · 폴더 23파일 **3,379건** GREEN
- 유일성 뮤턴트 **생존 → RED**
- `run-test-all.sh` **8회 전부 ALL PASS** · 타입체크 ratchet 둘 baseline 일치
