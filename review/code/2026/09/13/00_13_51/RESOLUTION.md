# RESOLUTION — 라운드 3 (`review/code/2026/09/13/00_13_51`)

**판정**: Critical 0 · WARNING 4. Critical 0.

**해소 커밋**: `b64cd4c57`

| # | 지적 | 처분 |
|---|---|---|
| 1 | `decodeCursor` 가 소유권 검사보다 먼저 돌아 "타 WS + 잘못된 커서" 가 404 → **400** | 되돌리지 않고 **테스트로 고정**(소유권 mock 을 일부러 안 세워 순서 자체가 증거) + 죽은 mock 제거 |
| 2 | 근거 주석 **3중** 복제 — 세 라운드 연속 지적 | JSDoc 한 곳으로 집약, 호출부는 참조로 압축 |
| 3 | `uuid.ts` JSDoc 의 **닫힌 캐너리 목록**이 이 diff 로 낡음 | 목록을 열고 세는 법을 `uuid.spec.ts` SoT 로 |
| 4 | 2라운드에 내가 쓴 **재현 grep 명령이 틀렸다**(정의부까지 잡아 4줄) | 필터 추가 + 사유 기록 |

**4번이 요점이다** — stale 한 개수를 "세는 법" 으로 바꿔 고쳤는데 그 명령이 그 자리에서 또
틀렸다. 2·3번은 2라운드에 **사본만 고치고 SoT 를 안 고친** 결과다.

---

검증: 각 라운드마다 `.claude/tools/run-test-all.sh lint unit build e2e` ALL PASS
(e2e 307 tests — 이 배치가 실 DB 케이스 2건을 더했다). 뮤테이션 6/6 예측 일치.
