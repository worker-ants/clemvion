# RESOLUTION — `/ai-review` 라운드 2 (`review/code/2026/09/13/19_51_33`)

Critical 0 · **WARNING 4** · INFO 9 · 위험도 MEDIUM. forced 7명 전원 산출물 확보.

**이번 라운드의 지적 4건 중 3건이 «라운드 1 의 내 수정» 을 겨눈다.** 고친 자리가 다음
결함이 되는 형태이고, 이 저장소가 이름 붙여 둔 패턴 그대로다.

## WARNING#2 — 내가 «방금 넣은» 검증에 구멍이 있었다 (고침)

라운드 1 에서 `where` 프리텍스트 방지를 넣었는데 `.exec()` **단일 매치**였다.
`where` 가 `…:7121·7125` 처럼 **여러 위치**를 인용하면 **둘째부터 한 번도 검증되지
않는다**. 리뷰어가 **둘째 줄 번호를 없는 값으로 바꾸는 뮤테이션으로 63/63 GREEN 을
관측해 증명**했다 — 추측이 아니라 실측이다.

`parseWhereRefs()` 로 `where` 안의 **모든** `파일:줄` 을 걷도록 고쳤고, 파서 자체에도
대조군을 달았다(`a.ts:10` · `a.ts:10·20` · `a.ts:10 · b.ts:30` · 산문).

같은 뮤턴트를 다시 걸어 확인했다:

| 뮤턴트 | 첫 판 | 지금 |
|---|---|---|
| `where` 의 **둘째** 줄 번호 7125→9999 | **GREEN (생존)** — 리뷰어 실측 | **RED** — `where` 검증이 단독 지목 |

## WARNING#3 — 핵심 술어가 테스트 지역 클로저라 진리표를 못 겨눴다 (고침)

`isMessagePrefixOnly` 가 테스트 파일 안의 화살표 함수였다. AND 항을 지우는 뮤턴트는 RED 를
내지만 *"offender 폭증"* 이라는 **뭉툭한 진단**이고, `(F,T)`·`(F,F)` 조합은 실제 코퍼스에
없어 **어떤 테스트도 관측하지 않았다**.

스캐너로 export 하고(두 집합을 **분리 인자**로 받게 해 조합을 합성할 수 있게) 진리표 4칸을
직접 고정했다. architecture INFO#1(*"판정 로직 소유권이 존재 축 관례와 불일치"*)도 같은
조치로 함께 닫힌다.

## WARNING#4 — 중복을 없앤 **같은 커밋**이 다른 중복을 만들었다 (고침)

라운드 1 에서 수집기 3종을 `collectMatches` 로 합쳤는데, 그 커밋이 테스트 파일에
*"여전히 인용되는가"* 판정을 **두 `it()` 에 복제**했다. `staleEntries(list, cited)` 로 뽑아
두 목록이 같은 판정을 쓰게 했다.

> 라운드 1 RESOLUTION 에 *"중복 제거 주석 바로 옆에서 다른 중복을 만들었다"* 고 적었는데,
> **그 수정 자체가 또 한 번 같은 일을 했다.** 세 번째다.

## WARNING#1 — CHANGELOG 가 **이 PR 이 반증한 주장**을 들고 있었다 (고침)

라운드 1 에서 JSDoc·테스트 제목·plan §E 를 전부 정정했는데 **CHANGELOG 만 빠뜨렸다** —
같은 항목 안에서 앞 문단은 *"카탈로그 덕에 통과"*, 뒷 문단은 *"소비자 인용 때문에 통과"*
로 **자기모순**이었다. 공개 문서라 틀린 근거의 비용이 가장 큰 자리다.

`--impl-done` INFO#1 도 같은 것을 짚었다 — **두 게이트가 독립으로 같은 누락을 봤다.**

> **«자매를 전수로 세라»** 를 또 어겼다. 고칠 때 *"이 주장을 어디에 썼나"* 를 세지 않고
> **보고 있던 파일만** 고쳤다.

## `--impl-done` 라운드 2 WARNING#2 — bare 시각 인용 (고침)

`guide-identifier-existence.test.ts:203` 이 `` `19_23_22` `` 로 날짜 없이 인용했다 —
`review-citations.md §2` 위반이고 **같은 파일의 다른 4곳은 전체 경로로 옳게** 쓰고 있었다.
`#1331` 라운드 2 에서 전수 정정한 것과 **같은 클래스를 한 건 재도입**했다.

전체 경로로 고치고 이 브랜치가 만진 파일 6개를 전수 재확인 — **잔여 0건**.
(`CHANGELOG.md` 에 1건이 남지만 `git diff origin/main` 으로 확인한 결과 **내 추가분이
아니라 선재분**이다. 처음 짠 검사가 «파일 전체» 를 세어 이 구분을 못 했고, 그 술어를
«내가 추가한 줄» 로 좁혀 재확인했다.)

## INFO 처분

| # | 항목 | 처분 |
|---|---|---|
| 1 | 판정 로직 소유권 | **고침** — WARNING#3 과 동일 조치 |
| 2 | 두 목록의 타입 미공유 | 세 번째 목록이 생길 때 |
| 3 | 3-채널 판정 체인 | 축 추가 시 `classifyToken()` 수렴 검토 |
| 4 | `where` 검증이 인라인 30줄 | **부분 고침** — `parseWhereRefs` 로 파싱은 분리, 트리 탐색은 남김 |
| 5 | 항목마다 `walkTree` 반복 | 상한 5건이라 실질 비용 낮음 |
| 6 | vacuity 임계값 근거 미기재 | 낮은 우선순위 (2라운드 연속 지적, 교차 기록) |
| 7 | 카탈로그 하드 리드 | 기존 관행 — 직전 라운드 처분 유지 |
| 8 | `matchAll` 재클론 비용 | 의도적 트레이드오프, 문서화됨 |
| 9 | 리뷰 산출물 27개 동반 커밋 | 정상 게이트 산출물 |

## 검증

- 가드 스위트 **63 → 68** GREEN (진리표 4 + 파서 대조군 1)
- `where` 둘째 위치 뮤턴트 **GREEN → RED** (리뷰어가 증명한 구멍이 닫혔다)
- `run-test-all.sh` 3회차 — 별도 기록
