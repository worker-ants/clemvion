# RESOLUTION — `/ai-review` 라운드 1 (`review/code/2026/09/13/19_23_22`)

Critical 0 · **WARNING 7** · INFO 6 · 위험도 MEDIUM. forced 7명 전원 산출물 확보.

## WARNING#3 — **내 근거가 거짓이었다** (고침) · 그리고 리뷰어보다 나빴다

테스트 제목과 JSDoc 이 *"`MAX_ITERATIONS_EXCEEDED` 는 **카탈로그 덕에** 통과한다"* 라고
적고 있었다. 리뷰어가 *"실은 `isMessagePrefixOnly` 단계에서 이미 걸러져 카탈로그에
도달하지 않는다"* 고 지적했다. 단계별로 직접 재현했다:

| 단계 | 값 |
|---|---|
| 메시지 접두인가 | `true` (`loop-executor.ts:64·85`) |
| 토큰-단독 리터럴이 있나 | **`true`** — `execution-failure-classifier.ts:76` 의 **소비자 Set** |
| ⇒ `isMessagePrefixOnly` | **`false` — 여기서 탈락** |

**리뷰어가 맞다.** 그 토큰은 *이 축이 스스로 «함정» 이라 이름 붙인 소비자-인용 경로*
때문에 통과한다.

전수로 한 칸 더 세니 **리뷰어보다 나빴다**:

| 실측 | 값 |
|---|---|
| 인용된 «접두 전용» 중 카탈로그 등재 | **0종** |
| (인용 무관) 소스 전체 접두 전용 11종 중 카탈로그 등재 | **0종** |

**카탈로그 탈출구는 오늘 한 번도 발화하지 않는다 — 죽은 경로다.**

### 지우지 않고 «사실대로» 적었다

지우면 이 배치가 방금 등재한 planner 항목의 처분안 — *"`CONTAINER_*` 를 §1.4 에
backfill 하면 등록 2종이 불필요해지고 가드가 자동 통과시킨다"* — 이 **거짓이 된다**.

이 파일에 같은 형태의 선례가 있다: `collectEnvDeclarations` 도 *"오늘 판정을 지탱하지
않지만 내일의 오탐을 막는다"* 로 남아 있고 그 문장을 내가 썼다. **차이는 그 사실을
적었느냐다.** 그래서 셋을 했다 —

1. JSDoc·테스트 제목을 **실제 메커니즘**으로 정정,
2. *"탈출구 0회 발화"* 를 **단언으로 고정**(언젠가 1이 되면 RED 로 알린다),
3. **합성 대조군**으로 *"그래도 작동은 한다"* 를 증명 — 0인 이유가 *"필터가 깨져서"* 가
   아니라 *"오늘 해당이 없어서"* 임을 가른다.

## WARNING#4 — 거울상 목록보다 강제가 2개 적었다 (고침)

`GUIDE_EXTERNAL_VOCABULARY` 는 4강제인데 신규 목록은 2개뿐이었다. **상한**
(`NON_EMITTED_VOCABULARY_CAP = 5`, 거울상과 같은 값)과 **"여전히 가이드에 인용되는가"**
(죽은 항목 누적 방지)를 추가해 대칭시켰다.

## WARNING#6 — `where` 가 단언 없는 프리텍스트였다 (고침) · **즉시 한 건 잡았다**

`where` 필드의 `파일:줄` 을 파싱해 그 줄에 토큰이 실제 있는지 검증하는 테스트를 넣었다.
이 저장소는 자매 축(`impl-anchor-existence`)에서 이미 grep 강제를 쓰고 있어 **이 목록만
예외**였다는 지적이 정확했다.

**넣자마자 RED 가 났다** — `MAKESHOP_UNRESOLVED_PATH_PARAM` 의 `where` 에 줄 번호가 아예
없었다("makeshop.handler.ts — 일반 `Error` 메시지 접두"). 실측해 `:436` 을 채웠다.
INFO#5 가 지적한 `CONTAINER_MULTIPLE_EMIT` 의 줄 번호 누락도 `:7130` 으로 채웠다(실측).

## WARNING#7 — 중복을 피했다고 적은 주석 **바로 옆에서** 다른 중복을 만들었다 (고침)

수집기 3종이 정규식과 그룹 번호만 다르고 구조가 같았다. `collectMatches(texts, rx, group)`
공유 헬퍼로 추출했다. **위쪽 세 함수의 수동 `lastIndex` 관용구는 그대로 둔다** — 그
리팩터는 트래커 별건이고 여기서 앞당기면 diff 가 두 가지 일을 하게 된다.

## WARNING#5 — 신규 수집기 3종에 합성 대조군이 없었다 (고침)

형제 함수 수준의 `describe("발행 축 수집기 — 경계 대조군")` 을 추가했다. 각 제약마다
**두 판정이 갈리는 값**을 골랐다 — 여닫이 따옴표 불일치 · `:` 유무 · `:` 뒤 공백 유무 ·
여는 따옴표 직후 여부 · 백틱 vs 따옴표.

## WARNING#1·#2 — 트래커 등재 (고침)

- 이 diff 가 실제로 닫은 트래커 항목(`CONTAINER_*` 도 방출 코드가 아니다)을 `[x]` 로
  체크하고 해소 근거를 적었다 — **실측**: `execution-engine.service.ts:8016` 이
  `nodeExec.error = { message }` 로 기록한다(`code` 필드가 **아예 없다**).
- impl-prep 이 *"별도 plan 항목으로 등재"* 를 명시 권고한 2건을 등재했다:
  spec 6파일의 `CONTAINER_*` 코드 서술(선례 `3-loop.md §6` 로 통일) · §1.4 카탈로그 택일.
  **`review/**` 는 SoT 가 아니므로 옮기지 않으면 소실된다.**

## INFO 처분

| # | 항목 | 처분 |
|---|---|---|
| 1 | 카탈로그 SoT 를 `readIfPresent` 없이 하드 읽기 | 기존 관행 일치 — 조치 불요 |
| 2 | `matchAll` vs 수동 `lastIndex` 공존 | 트래커 교차 기록분 |
| 3 | 두 목록 이름 인접 | JSDoc 대조표로 방어 확인 |
| 4 | vacuity 하한이 이름 없는 리터럴 | 낮은 우선순위 |
| 5 | `CONTAINER_MULTIPLE_EMIT` 의 `where` 줄 번호 누락 | **고침** (`:7130`) |
| 6 | CHANGELOG 정밀 표현 vs 가이드 단순화 | 가이드 수준에선 단순화가 정확 — WARNING#2 등재분과 함께 처리 |

## 검증

| 뮤턴트 | 예측 | 실측 |
|---|---|---|
| 등록 목록 비우기 | RED | **RED** — 베이스라인 0 · vacuity |
| `QUOTED_LITERAL` 역참조 제거 | RED | **RED** — 여닫이 대조군 등 5건 |
| `CATALOG_CODE` 무력화 | RED | **RED** — 수집기 vacuity · 백틱 대조군 등 4건 |
| `MESSAGE_PREFIX` 의 `:` 제거 | RED | **RED** — `:` 대조군 등 5건 |
| `collectMatches` 그룹 2→1 | RED | **RED** — 공용 헬퍼 대조군 등 5건 |
| `where` 줄 번호 436→437 | RED | **RED** — `where` 검증 단독 |

- 가드 스위트 **46 → 63** GREEN · `src/lib/docs/__tests__/` 23파일 **3,363건** GREEN

> **중간에 «가짜 실패» 를 한 번 만났다.** 폴더 전체 실행에서 4파일이 `Cannot find package
> '@/lib/i18n/types'` 로 죽었는데, 원인은 내 코드가 아니라 **워크트리 루트에서 `npx vitest`
> 를 돌린 것**이었다 — 루트엔 로컬 바이너리가 없어 `npx` 가 **다른 버전(4.1.9)** 을 받아
> 별칭 해석이 깨졌다. `codebase/frontend` 에서 다시 돌리니 4.1.11 로 23파일 전부 GREEN.
> 이 저장소가 기록해 둔 *"워크스페이스 루트 npx 는 다른 버전을 잡는다"* 의 이번 판본이다.
