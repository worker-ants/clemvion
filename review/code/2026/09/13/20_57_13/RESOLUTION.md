# RESOLUTION — `/ai-review` 라운드 5 (`review/code/2026/09/13/20_57_13`)

Critical 0 · **WARNING 2** · SPEC-DRIFT 1 · INFO 8 · 위험도 **LOW**. forced 7명 전원 확보.

## WARNING#1 — 리뷰어의 진단은 맞고 **처방은 부족했다** (실측으로 확인)

리뷰어가 **뮤테이션으로** 증명했다: 실제 offender 체인에서 `.filter(t => !catalogCodes.has(t))`
한 줄을 지워도 **71/71 GREEN**. 그리고 원인을 *"`[한계]`·`[대조군]` 이 offender 계산과 분리된
**병렬 구현**이라 실제 코드 경로 변경을 감지하지 못한다"* 로 짚고, `computeNonEmittedOffenders`
정본 추출을 제안했다.

**제안대로 했다.** 판정을 스캐너의 `computeNonEmittedOffenders` 하나로 모으고 베이스라인과
`[한계]` 가 같은 함수를 부르게 했다. 그리고 **같은 뮤턴트를 다시 걸었다**:

| | 실측 |
|---|---|
| 정본 통합 **전** | GREEN (생존) |
| 정본 통합 **후** | **GREEN (여전히 생존)** |

**처방이 듣지 않았다.** 이유는 테스트 구조가 아니라 **데이터**다 — 그 필터는 오늘
실코퍼스에서 **한 번도 발화하지 않는다**(«접두 전용 ∩ 카탈로그» = 공집합, 라운드 1 실측).
필터를 지워도 결과가 같으므로 **실코퍼스 기반 테스트로는 원리적으로 못 잡는다.**

> **리뷰어의 처방을 그대로 적용하고 «됐다» 고 넘어갈 뻔했다.** 뮤턴트를 다시 걸어 본 것이
> 유일한 방어였다 — 이 저장소가 적어 둔 *"GREEN 은 증거가 아니다"* 의 이번 판본이고,
> 이번엔 **리뷰어의 제안을 검증 없이 신뢰하는 형태**로 나타났다.

### 실제로 닫은 방법 — 합성 진리표

정본에 **네 항이 각각 무는지**를 겨눈 합성 케이스 5건을 넣었다. 카탈로그 항은
*"같은 입력에서 카탈로그에만 넣고 빼는"* 두 값이 갈리는 자리다.

| 뮤턴트 | 실측 |
|---|---|
| 카탈로그 항 삭제 | **RED** — `[카탈로그 항]` 단독 |
| 등록 항 삭제 | **RED** — `[등록 항]` + 베이스라인 |
| 접두-전용 항 반전 | **RED** — `[접두-전용 항]` + `[한계]` + 양성 케이스 |

**정본 통합 자체는 유지한다** — 판정이 한 곳에 모인 것은 그 자체로 옳고, 위 세 뮤턴트가
동시에 베이스라인까지 흔드는 것은 통합 덕이다. 다만 **그것만으로는 카탈로그 항을 못 지켰다**는
사실을 함께 적는다.

## WARNING#2 — 일괄 치환이 «옛 이름을 적어야 할 자리» 까지 바꿨다 (고침)

개명 JSDoc 이 *"`staleGuideEntries` 로 바꿨다 — 첫 판은 **`staleGuideEntries`** 였는데"* 로
자기모순이었다. 라운드 4 에서 `s.replace("staleEntries", "staleGuideEntries")` 를 파일 전체에
걸었고, **개명 이력을 서술하는 그 문장의 옛 이름까지** 바꿨다. plan 은 정확히 적고 있어
코드와 plan 이 갈렸다.

> 기계적 일괄 치환의 고전적 함정이고, 이 세션에서 «술어를 좁게/넓게 잡는» 형태가 또 나온
> 것이다 — 치환 대상은 *"이 식별자를 «가리키는» 자리"* 인데 *"이 문자열이 나오는 자리"* 로
> 잡았다.

## `--impl-done` 라운드 5 WARNING#3 — **이 배치의 근거 항목**이 아직 `[ ]` 였다 (고침)

plan 머리말이 *"트래커 항목 … 을 닫는다"* 라고 선언하는데
`spec-draft-nullable-notation-followups.md:3394` 는 여전히 미체크였다.

라운드 1 에서 **자매 항목(3404, `CONTAINER_*`)은 닫았는데 정작 배치의 근거인 3394 를 안
닫았다.** 또 *"자매를 전수로 세라"* 를 어긴 형태다 — 이번엔 **닫는 방향**으로.

## SPEC-DRIFT · INFO — 조치 불요 (하나 제외)

| # | 항목 | 처분 |
|---|---|---|
| SPEC-DRIFT | spec 6파일 | 5라운드 연속 불변 · planner 등재분 |
| INFO#4 (impl-done) | `MAKESHOP_UNRESOLVED_PATH_PARAM` 등록에 3208 forward-note 없음 | **보강** — `CONTAINER_*` 엔 있는데 이것만 없어 절반짜리였다 |
| 1 | `where` 가 산문+구조 참조 혼합 | 등록이 늘면 구조화 필드 분리 |
| 2 | 두 CAP 동일성 미단언 | 낮은 우선순위 |
| 3 | `where` 의 `hits.length !== 1` 분기 미도달 | 라운드 3 유예 유지 |
| 4·5·6 | `§2` 미등재 · 토큰 유일성 · `where` 스코프 | 선재·등재분·fail-safe |
| 7 | 4라운드 이월 저위험 항목 | 변화 없음 |
| 8 | 리뷰 산출물 90여 개 | 정상 게이트 산출물 |

## 검증

- 가드 스위트 **71 → 76** GREEN
- 카탈로그 필터 뮤턴트 **GREEN(생존) → RED** — 리뷰어가 증명한 구멍이 실제로 닫혔다
- `run-test-all.sh` 6회차 — 별도 기록
