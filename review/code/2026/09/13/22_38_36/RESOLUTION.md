# RESOLUTION — `/ai-review` 라운드 9 (`review/code/2026/09/13/22_38_36`)

**Critical 0** · WARNING 2 · INFO 4 · 위험도 **LOW**. forced 7명 전원 확보.
`[CRITICAL]` 마커 실측 **0건**.

라운드 8 의 CRITICAL(예고 미정정) + WARNING 3(orphan 조각 · 루트 둘 · 구분자)이 **전부
해소 확인**됐다. 남은 둘은 **라운드 8 커밋 자신이 만든 것**이다.

## WARNING#1 — 정정 주석이 자기 라운드를 틀리게 적었다 (고침)

라운드 8 커밋이 넣은 주석이 자기를 *"(라운드 9 정정)"* 이라 적었는데, 같은 커밋의 메시지도
plan §L 도 *"라운드 8"* 이다. 이 저장소는 «라운드 N» 을 커밋·리뷰 세션과 대조하는 **감사
라벨**로 쓰므로, 진짜 라운드 9 가 커밋을 만들면 한 PR 안에 «라운드 9» 를 자칭하는 지점이
둘이 된다.

세 자리를 8 로 맞췄다 — 스캐너 주석 · 트래커 실측 표 행 · 라운드 8 consistency RESOLUTION 표 행.

## WARNING#2 — JSDoc 비중 (조치 불요, 등재분)

`collectCatalogCodes`(본문 2줄 / JSDoc 37줄) 등. **형제 트래커 항목**이고 라운드 8 이 그
항목의 stale 실측을 3시점 표(361 → 601 → 622)로 갱신했다. 리뷰어도 *"이번 PR 스코프 밖
별도 항목으로 처리 가능"* 으로 판정했다.

## SPEC-DRIFT — 9라운드 연속, 등재분 (조치 불요)

spec 6파일의 `CONTAINER_*` 서술. 리뷰어가 *"코드가 옳고 spec 이 낡음"* 을 9번째로 재확인했다.

## `--impl-done` 라운드 9 WARNING#3 — 같은 커밋이 자기 인용을 깼다 (고침)

`guide-identifier-scan.ts` 가 트래커를 `:3407` 로 가리켰는데 **같은 커밋**이 그 트래커
위쪽에 20여 줄(형제 항목 실측 표)을 넣어 항목이 **3421** 로 내려갔다. 쓸 때는 참이었고
커밋될 때 거짓이 됐다.

> 이 저장소가 이미 적어 둔 규칙 그대로다 — ***"내가 편집하는 파일을 줄 번호로 인용하지
> 마라."*** 알고 있는 규칙을 **같은 커밋 안에서** 어겼다.

**checker 의 처방(`:3494`)은 틀렸다** — 3494 는 §1.4 를 다루는 **다른 항목**의 표제줄이다.
그리고 번호를 고치면 같은 클래스를 한 번 더 밟는다. **앵커 문구**로 바꿨다.

**클래스로 전수 판정**했다 — `review/` 밖 브랜치 편집 파일 8개에서 5건:

| 인용 | 판정 | 처분 |
|---|---|---|
| `scan.ts` → 트래커 `:3407` | **내가 깼다**(같은 커밋) | 앵커 문구 |
| plan §L → `guide-identifier-scan.ts:77` | **내가 밀었다**(헤더 +21줄) | 번호 제거 |
| 트래커 → `PROJECT.md:40-41` | **선행 브랜치**(이 브랜치 시프트 0). 그 문장은 지금 그 파일에 **없다** — ratchet 은 2026-09-08 에 `build` 안으로 옮겨졌다 | 앵커 문구 |
| 트래커 → `PROJECT.md:315` | **선행 브랜치**(실제 331) | 앵커 문구 |
| plan §A → `PROJECT.md:300` | **유효** — `PROJECT.md` diff 는 300행 제자리 1줄, 시프트 0 | 유지 |

사후 단언으로 «잔여 = `PROJECT.md:300` 1건, 그리고 그것은 유효» 를 고정했다.

## `--impl-done` W2 — 이미 등재돼 있다 (재등재 안 함)

checker 가 *"«허용목록 없음» 원칙의 2차 번복이 spec Rationale 에 미착지"* 를 새 등재
대상으로 제안했다. **실측하니 이미 있다** — 트래커 `user-guide-evidence.md §2.1` 항목이
1차(`GUIDE_EXTERNAL_VOCABULARY`)·2차(`GUIDE_NON_EMITTED_VOCABULARY`)를 **이름으로 지목**하고
*"제약이 정반대라 합칠 수 없다"* 와 *"표·frontmatter·Rationale 을 한 턴에"* 까지 적는다.

## INFO 4건 — 조치 불요

| # | 항목 | 사유 |
|---|---|---|
| 1 | `rest.replace(m[0], "")` 가 위치 아닌 내용으로 지운다 | 매치 스팬의 **다중집합이 제거분과 같아** 오늘 거짓 음성을 못 만든다. 리뷰어도 *"실코퍼스에 없음, 리스크 낮음"* |
| 2 | 캡처 그룹 인덱스가 정규식 순서에 암묵 결속 | named group 전환은 다음 축 추가 시 |
| 3 | 트래커 부수 편집 2건 | 리뷰어가 **규약 준수**로 판정 |
| 4 | 리뷰 산출물 178파일 vs 코드 2파일 | 강제 게이트 규약에 따른 정상 |

## 검증

- 가드 스위트 **82건** GREEN(주석-only 변경이라 개수 불변) · 폴더 23파일 **3,382건** GREEN
