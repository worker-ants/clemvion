# RESOLUTION — `/ai-review` 라운드 6 (`review/code/2026/09/13/21_19_46`)

Critical 0 · **WARNING 2** · INFO 10 · 위험도 **LOW**. forced 7명 전원 확보.

여섯 라운드: **W7 → W4 → W2 → W1 → W2 → W2**, MEDIUM ×2 → LOW ×4.
이번 둘은 **성능 nit 하나, 주석 속 숫자 하나**다.

## WARNING#1 — 같은 파일을 세 번 다시 훑고 있었다 (고침)

`where` 검증이 **참조마다** `codebase/backend/src`(1,304 파일) 전체를 다시 순회했다.
실측: 등록 3항목의 참조 4개 중 **3개가 같은 파일**(`execution-engine.service.ts`).

`basename` 키로 **파일당 한 번만** 순회하는 모듈 스코프 캐시(`resolveSourceLines`)로 바꿨다.
등록이 늘어나는 방향의 설계라 배율이 함께 커지는 자리였다.

**리팩터가 검증을 약화시키지 않았음을 다시 증명했다** — 캐시가 결과를 가릴 수 있으므로
뮤턴트를 재실행했다:

| 뮤턴트 | 실측 |
|---|---|
| `where` **둘째** 줄 번호 7125→9999 | **RED** |
| `where` 파일명을 없는 것으로 | **RED** — 리뷰어가 *"미도달"* 로 본 `hits.length !== 1` 분기까지 관측됐다 |

> 이 저장소 규칙 — *"구조를 바꿨으면 증거를 다시 만들어라. 분리 전 실측은 분리 후를
> 보증하지 않는다."*

## WARNING#2 — «네 항» 과 «filter 호출 횟수» 를 혼동했다 (고침)

라운드 5 주석이 옛 체인을 *"`.filter()` 네 번/네 개"* 로 적었는데 **실측 3회**다.
직접 세었다 — `git show 2931d921f~1` 의 해당 블록에 `.filter(` 가 **3개**.

판정식의 **네 항**(인용·접두전용·카탈로그·등록)과 **filter 호출 횟수**를 혼동했다 —
인용·중복제거 단계는 `filter` 가 아니다. 두 자리 모두 «세 번/세 개» 로 정정하고 혼동의
정체를 주석에 남겼다.

> 라운드 5 가 *"근거를 확인하기 전에 결론"* 을 경계하며 쓴 그 주석 자체에 **미검증 숫자**가
> 들어갔다. 숫자를 쓸 때 세는 습관이 아직 자동이 아니다.

## `--impl-done` 라운드 6 — 둘 다 내 plan 위생 (고침)

| # | 지적 | 처분 |
|---|---|---|
| W#3 | 신규 항목이 겨냥한 spec 6파일 중 **5개가 `spec_impact` 에서 빠짐** | **고침** — 5경로 추가 + 전수 대조 assert |
| W#4 | 신규 §1.4 항목이 기존 *"한 턴에 묶어라"* 합의를 인용 안 함(넷째가 됐다) | **고침** — 상호 링크 |

W#3 은 **그 문서 자신이 같은 실패를 두 번 자백해 둔 자리에서 세 번째**다. 원인도 같다 —
`4-execution-engine.md` 가 이미 목록에 있어서 *"있다"* 로 읽었고, **항목의 대상 파일 전수를
목록과 대조하지 않았다.** 그 절차를 주석으로 박았고, 스크립트에 전수 대조 assert 를 넣었다.

## INFO 10건 — 조치 불요 (하나는 조건부 등재)

| # | 항목 | 처분 |
|---|---|---|
| 5 | 카탈로그 탈출구가 실코퍼스에서 미발화 | **조건부 등재 제안 수용** — backfill 이 won't-do 로 처분되면 이 분기와 대응 테스트 3건도 제거 대상임을 plan 에 적었다 |
| 9 | `new Set` 중복제거를 겨눈 fixture 없음(뮤턴트 생존 실측) | 리뷰어가 *"거짓 PASS 로 이어지지 않음 — 진단 품질만"* 으로 판정. 낮은 우선순위 |
| 1·2 | spec 6파일 · `§2` 미등재 | 6라운드 연속 등재분 |
| 3·4 | capture-group 위치 결합 · 두 관용구 공존 | 다음 축 추가 시 |
| 6 | 테스트 파일이 두 축을 한 파일에 누적 | 세 번째 축에서 분리 검토 |
| 7·8 | 한 글자 상수 `T` · `sets` 스프레드 조합 | 기존 관례 일관 |
| 10 | 수집기 2종의 정규식 스캔 추가 | 모듈 로드 시 1회 고정비 |

## 검증

- 가드 스위트 **76건** GREEN
- `where` 뮤턴트 2종 **RED** (캐시 리팩터 후 재측정)
- `run-test-all.sh` 7회차 — 별도 기록
