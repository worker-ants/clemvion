# RESOLUTION — `/ai-review` 라운드 10 (`review/code/2026/09/13/23_04_01`)

**Critical 0** · WARNING 3 · INFO 10 · 위험도 **LOW**. forced 7명 전원 확보.
`[CRITICAL]` 마커 실측 **0건**.

**이 라운드의 처분은 전부 `plan/**` 이고 `codebase/**` 수정은 0** — 선언한 완료 기준
(*"마지막 라운드가 `codebase/**` 수정 0 으로 끝날 것"*)을 충족한다.

## WARNING#3 — §D-2 표가 두 블록인용에 쪼개져 있었다 (고침)

라운드 5·6 이 표 **안**에 forward-note 를 끼워 넣어, 3행 표가 **1행 표 + 정렬 안 된 평문
2줄**로 렌더링되고 있었다. 같은 문서가 *"등록 대상은 §D-2 의 3종"* 이라 말하는데 렌더링은
1종만 보여 줬다. 3행을 붙이고 인용을 표 뒤로 옮겼다.

**클래스로 훑었다** — 브랜치가 만지는 4개 문서에서 «구분선 없는 `|` 블록» 을 세니 트래커에
3건이 더 나왔는데, 열어 보니 **전부 코드펜스 안의 spec 인용**이었다(거짓 양성 3). 실제로
깨진 표는 §D-2 하나다.

## WARNING#1 — `collectMatches` 캡처그룹 결속 (**등재만**, 고치지 않음)

`collectMatches(texts, rx, group)` 이 그룹 번호를 정수 리터럴로 받는다. 정규식의 그룹
순서가 바뀌면 컴파일 타임에 안 잡히고 `undefined` 가 조용히 `Set` 에 섞인다.

**오늘 결함은 없다** — 세 짝 전부 실측 일치, 한 자리는 주석으로 방어돼 있다. 리뷰어도
*"향후 축 추가 시"* 로 한정했다.

| 라운드 | 같은 코드에 대한 등급 |
|---|---|
| 9 | **INFO** (*"시급도 낮음"*) |
| 10 | **WARNING** |

판정이 갈리므로 한쪽 근거로 닫지 않고 트래커에 등재했다 — 처분안 (a) named capture group
(인덱스 결속 자체를 없앤다) / (b) 이름 있는 상수까지 함께.

> **여기서 고치면 코드 시계가 올라가 라운드 11 을 부른다.** 열 라운드가 매번 직전 라운드의
> 내 작업을 잡았다 — **결함이 아닌 개선**을 위해 그 사이클을 한 번 더 돌리지 않는다.
> 결과를 보기 전에 정지 규칙을 선언해 둔 이유가 이것이다.

## WARNING#2 — 헤더 서사 152줄 (등재분, 조치 불요)

형제 트래커 항목이고 라운드 8 이 그 항목의 stale 실측을 3시점 표로 갱신했다.

## SPEC-DRIFT — 10라운드 연속, 등재분 (조치 불요)

spec 6파일의 `CONTAINER_*` 서술. 리뷰어가 *"developer 스코프가 아니므로 이번 PR 의 누락이
아니라 **의도된 위임**"* 으로 명시했다.

## INFO#10 — «다른 세션의 미커밋 뮤테이션» 은 리뷰어 자신이었다 (실측으로 종결)

documentation 리뷰어가 *"리뷰 시작 시점에 `SOURCE_ROOTS` 가 미커밋 뮤테이션 상태"* 를
관측하고 다른 세션을 의심했다. **같은 세션의 testing 리뷰어**가 INFO#8 에서
*"`SOURCE_ROOTS` 에서 `packages` 제거 뮤테이션으로 판별력 확인"* 이라고 적는다 — **한
사건의 양면**이고, 이 저장소가 이미 이름 붙인 *"병렬 리뷰어가 공유 워크트리를 뮤테이션해
서로를 오염시킨다"* 의 재현이다.

리뷰어가 요청한 종결 전 재확인을 실행했다:

| 검사 | 결과 |
|---|---|
| `git diff HEAD -- codebase/` | **빈 출력** |
| `SOURCE_ROOTS` 현재 값 | `["codebase/backend/src", "codebase/packages"]` — 두 루트 온전 |

워크트리는 커밋 상태와 일치한다.

## 나머지 INFO — 조치 불요

| # | 항목 | 사유 |
|---|---|---|
| 1·2 | ReDoS 전제 불성립 · 시크릿/경로탐색 부재 | **양성 확인** |
| 3 | 발행 축이 선언대로 배선됨(등록 3항목 `where` 재실측 일치) | **양성 확인** |
| 4 | *"`§2` 미등재"* 를 스스로 정확히 자백 — 과거 클래스의 오류 없음 | **양성 확인**(라운드 7 처분의 효과) |
| 5 | 실질 변경 8파일, 나머지는 게이트 산출물 | 정상 |
| 6·7 | 신규 export 는 순수 함수/불변 상수 · `SOURCE_ROOTS` 등 추출이 실제 중복을 합쳤다 | **양성 확인**(라운드 8 처분의 효과) |
| 8 | 리뷰어가 뮤테이션 재현 — 겨눈 1건만 RED, 81건 GREEN | **판별력 독립 확인** |
| 9 | `refs.length === 0` 분기 미도달 · `[2건 이상]` 실코퍼스 결합 | 기존 유예, fail-loud 방향 |

## 검증

- `codebase/**` 수정 **0** — 가드 스위트 **82건** · 폴더 23파일 **3,382건** 불변 GREEN
- `run-test-all.sh` **10회 전부 ALL PASS**(e2e 307) · 타입체크 ratchet 둘 baseline 일치
- 10회차가 현재 `codebase/**` 트리와 1:1 (이후 코드 변경 0)
