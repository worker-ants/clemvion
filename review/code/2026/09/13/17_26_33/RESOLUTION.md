# RESOLUTION — `/ai-review` 라운드 8 (`review/code/2026/09/13/17_26_33`)

**Critical 0 · WARNING 3** · INFO 14 · 위험도 LOW. forced 7명 전원 산출물 확보.
`codebase/**` 수정 **0건** — 이 라운드가 수렴 라운드다(근거는 마지막 절).

## WARNING#1 — SPEC-DRIFT (권한 밖 · 등재분)

`user-guide-evidence.md §2` 가드 인벤토리. **통산 11회** 확인. 리뷰어 자신이 *"코드 변경
불요(유지)"* 로 적었다. `spec/` 쓰기는 planner 전속이고 자기-반증형 소정정 예외에도
해당하지 않는다(그 예외는 *내가 쓴 예고 문장*에만 열린다).

라운드 8 의 `--impl-done` 이 이 항목에 대해 **7라운드 만에 새로운 것을 말했다** — 아래
consistency RESOLUTION 참조.

## WARNING#3 — 지적은 맞고, 범위는 내 파일보다 훨씬 넓었다 (등재)

*"소스 주석이 비영속 `review/**` 세션 경로를 15곳에서 인용한다. 정리·이관되면 죽은 링크"*.

전제를 의심하지 말고 **실측**했다. 그리고 **리뷰어가 맞았다**:

| 실측 | 값 |
|---|---|
| 이력상 `review/**` 삭제·이동 | **3건** — 세션 통째 삭제(`f7c56bf0a`, 2026-05-30) · 세션 리네임(`4b1f899b7`) · `.gitignore` 삭제 |

그런데 같은 명령으로 한 칸 더 셌더니 **범위가 뒤집혔다**:

| 실측 | 값 |
|---|---|
| `review/**` 를 인용하는 `codebase/` 소스 파일 | **75개** (내 두 파일 제외) |
| 그 인용 총 건수 | **225회** |

`response-contract.ts`·`pg-error-fixtures.ts` 같은 이 저장소의 모범 파일이 전부 같은 관례를
쓴다. **15/240 만 바꾸면 근거 없이 규약이 둘로 갈린다.** 규약을 정하는 일이므로 planner
항목으로 등재했다.

> 대안도 각각 값을 치른다 — 커밋 SHA 는 **새 staleness 축**을 만들고(이 PR 이 라운드 4 에서
> 같은 이유로 SHA 핀을 거절했다), PR 번호는 **push 전에 확정되지 않으며**(라운드 6 에서
> 실제로 그 이유로 하나를 뺐다), `plan/` 경로는 완료 시 `in-progress/` → `complete/` 로
> **이동해서 깨진다**. *"더 안정적인 앵커"가 자명하지 않다*는 것이 이 항목의 핵심이다.

## WARNING#2 — 숫자는 리뷰어가 맞다 (등재, 착수 조건 명시)

*"361줄 중 260줄(72%)이 현재 계약이 아니라 라운드 5~7 의 연대기"*. 직접 세어 보니
**361줄 = 주석 260 · 코드 84 · 빈줄 17 → 72%.** 리뷰어의 숫자와 정확히 일치한다.

**«의도된 트레이드오프» 라는 말로 닫지 않는다** — 그 판정은 라운드 7 리뷰어의 INFO#9 이고
라운드 8 리뷰어는 정반대로 봤다. 두 판정이 갈리는 항목을 한쪽 근거로 닫는 것이 이 저장소가
반복해 지적한 형태다. 실제 유예 이유는 둘이고 둘 다 검증 가능하다:

1. **이 파일을 재작성하다 자기 주석을 지운 전력이 있다** — 라운드 1 documentation
   WARNING#3 이 그것을 잡았다(*"이 주석을 지우지 말 것"* 을 스스로 지웠다). 260줄 재배치는
   같은 사고의 표면이 가장 넓은 작업이다.
2. 순수 산문 재배치인데 **리뷰 라운드를 하나 더 소비**한다.

**착수 조건을 «지금은 참이 아닌 것»으로 적었다**: 라운드 3~7 이 주석에 박은 결정들은 이제
대조군 테스트가 고정하고 있다(`[설계] 하이픈 키…` · `[한계] compose 리스트…` ·
`[경계] 스팬 안쪽…`). 산문이 *유일한 운반체* 이던 시기는 끝났다. 다음에 이 파일을 만질 때
**주석 문단마다 "이걸 고정하는 테스트가 있는가"** 를 묻고 있으면 한 줄 참조로 줄인다.

## INFO 14건 — 전부 조치 불요

| # | 사유 |
|---|---|
| 1 `FIELD_TABLE_NAME` 중첩 객체 엣지 | 코퍼스 0건. 다음 축 손볼 때 한계 주석에 |
| 2 `readIfPresent` 부재 분기 미실행 | `.env.example` 이 항상 존재. 낮은 우선순위 |
| 3 `CODE_FIELD` 의 따옴표 없는 값 | 코퍼스 0건 |
| 4 허용목록 항목별 검증이 원소 1개로만 실행 | 두 번째 항목이 생기면 자연 해소 |
| 5 `lastIndex` 보일러플레이트 | 라운드 5 등재분 |
| 6·7·8 스코프 재확인 | 관례 준수·기처분 |
| 9~12 부작용 | 회귀 없음 확인(양성) |
| 13·14 유지보수성 | 기존 결정 유지·양성 |

## 이 라운드가 수렴 라운드인 근거

**선언했던 정지 규칙은 "Critical 0 **이고** WARNING 0"** 이었고, 이번은 C0 · **W3** 이라
그 규칙은 발동하지 않는다. 규칙을 결과에 맞춰 고치지 않는다.

대신 이 저장소가 **이 PR 이전부터 적어 둔** 기준으로 판단한다 — *"수렴은 «발견 0» 이 아니라
**발견의 성격**(동작 → 구조 → 문서)으로 판단한다"*:

| 라운드 | 발견의 성격 |
|---|---|
| 1~5 | 정밀도·경계 (**동작**) |
| 6 | 리뷰어 발견 0 — 내 선행 참조만 |
| 7 | **동작** — 축이 코퍼스 6종을 한 번도 검사 안 함 (CRITICAL) |
| 8 | **문서** — 주석 비중 · 인용 앵커 · spec 표 미갱신 |

라운드 8 의 WARNING 3건 중 **코드 결함은 0건**이다 — 하나는 planner 전속(리뷰어가 *"코드
변경 불요"* 명시), 둘은 산문 형태이고 둘 다 실측과 착수 조건을 달아 등재했다.

그리고 이 PR 이 스스로 정한 완료 기준 — **"마지막 라운드가 `codebase/**` 수정 0 으로 끝날
것"** — 을 이 라운드가 충족한다. 라운드 8 의 처분은 전부 `plan/**` 이고 코드 시계를 올리지
않는다.

## 검증

- 가드 스위트 **39건** GREEN (라운드 7 이후 코드 무변경)
- `run-test-all.sh` 7회 전부 ALL PASS · 타입체크 ratchet 둘 baseline 일치
