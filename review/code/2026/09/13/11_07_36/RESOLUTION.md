# RESOLUTION — 라운드 3 (`review/code/2026/09/13/11_07_36`)

Critical 0 · WARNING 3 · INFO 11 · 위험도 **MEDIUM**. **전 항목 처분 완료.**
처분 커밋: `137784219` (*"지어낸 이름을 없애면서 실재하는 이름 5종을 빠뜨렸다"*).

## 이 라운드가 위험도를 올린 이유

> 이 PR 이 새로 만든 노드-종류별 표가 `spec/5-system/3-error-handling.md §1.4` 대비 **실재
> 코드 5종을 누락**해, 이 PR 자신의 핵심 취지(*"사용자가 마주칠 코드를 어디서도 못 찾는다"*)를
> **한 단계 좁은 스코프에서 재현**한다.

지어낸 이름을 없애면서 실재하는 이름을 빠뜨렸다 — 축을 바꿔 같은 병을 옮긴 것이다. 원인은
spec 표를 **미러링하지 않고 큐레이션**한 것이고, 큐레이션은 조용히 떨어뜨린다.

## WARNING

| # | 카테고리 | 처분 | 근거 |
|---|---|---|---|
| 1 | requirement | **고침 + 등재** | 누락 5종 보강 — `DB_HOST_BLOCKED`·`EMAIL_HOST_BLOCKED`(둘 다 **SSRF 방어**)·`MAX_COLLECTION_RETRIES_EXCEEDED`·`SUB_WORKFLOW_QUEUE_FAILED`·`WORKFLOW_FORBIDDEN_WORKSPACE`. `*_HOST_BLOCKED` 가 무엇인지 한 줄 추가(사용자가 만나면 "왜 막혔지" 를 알아야 하는 코드다). **가드는 못 잡는 클래스**라 역방향 가드를 트래커 등재 |
| 2 | requirement·documentation | **경계 명시** | MakeShop 11종 중 7종만 나열한 것은 의도적 curation 이 맞다 — 나머지 넷은 *"호출이 나가기 전"* 설정 검증 실패다. 목록을 늘리는 대신 **두 갈래라는 사실**을 적었다(사용자가 취할 행동이 다르다: 재시도 vs 노드 설정 수정) |
| 3 | architecture | **등재** | 유령 필드 방향(선언 있고 생산자 0건)은 `assertMatchesContract` 가 원리적으로 못 본다. 리뷰는 *"세 번째 재발 시 검토"* 라 했는데 **이미 세 번째다** — 이 PR 한 건 안에서 `latencyMs`×2 · `meta`. 그 사실과 선실측 항목을 등재 문구에 기록 |

## 내 실측이 리뷰어보다 거칠었다

누락 종수를 직접 재려고 쓴 정규식이 **11종** 차이를 냈는데, 그중 6은 코드가 아니었다 —
env 변수 2종(`ALLOW_PRIVATE_HOST_TARGETS`·`CODE_NODE_MEMORY_LIMIT_MB`) ·
`details.integrationCode` 하위값 3종 · 명시적 **미발행** `HTTP_TIMEOUT`. 리뷰어의 **5종**이
맞았다. 역방향 가드를 만들 때 *"어느 토큰이 대상인가"* 를 먼저 정해야 하는 이유이고, 그
경고를 등재 문구에 남겼다.

## INFO — 조치한 것

| # | 처분 |
|---|---|
| 3 | `run-test-all.sh` 체크박스 — 4회 돌려 놓고 미체크였다. 실행 결과와 함께 갱신 |

## INFO — 조치 불요 (사유 기록)

- **1·2 (maintainability·architecture)**: 두 유사 DTO 공용 베이스 · 반복 서술 정리 — 세 번째
  유사 결함 시 재검토(현재 방침 유지).
- **4 (testing)**: 가드의 "실재" 판정이 원시 텍스트라 주석·죽은 코드의 토큰도 실재로 센다.
  오늘 영향 0이나 **코드 은퇴 시 주석 잔존**이 사각지대 — 파일 상단 트레이드오프 표에 축을
  더할 후보로 남김.
- **5 (performance)**: 중복 스캔 — 라운드 2 와 동일 판단(차단 아님).
- **6 (dependency)**: 텍스트 스캔 soft coupling 은 의도 + vacuity floor 방어.
- **7 (side_effect)**: 저장소 밖 소비자 영향은 CHANGELOG 고지로 처리 완료.
- **8 (testing)**: `message: null` 경계 미커버 — 프로덕션 발행 케이스 없음.
- **9 (scope)**: 스코프 확장이 코드 주석·CHANGELOG·plan 전 층에서 disclosure 됨.
- **10·11**: MCP 3종 · spec §3 갭 — 이미 등재분.

## 정지 규칙에 대한 판단

라운드 2 결과를 보기 전에 선언한 규칙은 *"라운드 3 의 `codebase/**` 발견이 **주석·문서**
성격이면 고치지 말고 등재하고 멈춘다"* 였다. W#1·W#2 는 문자 그대로는 문서인데 **고쳤다.**

규칙이 밝힌 이유가 *"그 지점부터는 수렴이 아니라 무한 루프다"* 이고, 이번 건은 (a) 부수적
정리가 아니라 **이 PR 의 산출물 자체**가 틀린 것이며 (b) spec 표에 5행을 맞추는 **유계·기계적**
작업이라 새 발견을 낳지 않는다. 규칙을 넓힌 게 아니라 이번 건이 규칙의 전제에 해당하지
않는다고 판단했다.

대신 규칙의 목적을 지키려고 **이번 라운드에 새 가드를 만들지 않았다** — 역방향 가드도 유령
필드 스캐너도 등재만 했다. 그게 유계화의 실제 수단이다.

## 검증

`run-test-all.sh` 4회째 ALL PASS (e2e 307) · 타입체크 ratchet 둘 baseline 일치 ·
frontend 문서 가드 23파일 3,308 테스트 GREEN (5종 추가 후에도 베이스라인 0 유지).
