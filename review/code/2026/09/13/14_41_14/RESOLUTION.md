# RESOLUTION — `/ai-review` (`review/code/2026/09/13/14_41_14`)

Critical 0 · WARNING 6 · INFO 12 · 위험도 MEDIUM. **전 항목 처분 완료.**

## 가장 아픈 지적 — 내가 "지우지 말 것" 이라 쓴 주석을 내가 지웠다

`#1330` 에서 이 가드의 한계(*"존재 검사이지 방출 검사가 아니다"*)를 적고 그 절을
**"이 주석을 지우지 말 것"** 으로 끝냈다. 이유까지 적었다 — *"가드가 무엇을 보장하지 않는지가
없으면 다음 사람이 '통과했으니 실재한다' 로 읽는다."*

**그 다음 PR 에서 파일을 재작성하며 그 절을 통째로 삭제했다.** 같은 문장을 쓴 사람이 한 PR
뒤에 지웠고, `documentation` 리뷰어가 잡았다(실측 확인: 원본 1건 → 재작성본 0건).

> **왜 눈에 안 띄었나**: 파일 **재작성**은 리네임·이동과 달리 diff 가 "삭제+생성" 으로 보여
> **무엇이 사라졌는지가 보이지 않는다**. 리네임 스윕은 *참조*를 쫓았지 *내용*을 대조하지
> 않았다. 복원하면서 그 경위와 *"다음에 재작성하면 옛 판본의 주석 절 목록을 먼저 뽑아
> 대조할 것"* 을 함께 못박았다.

## WARNING

| # | 카테고리 | 처분 | 근거 |
|---|---|---|---|
| 1 | 문서 정합성 (**6 리뷰어 수렴**) | **고침** | 리네임이 자매 `guide-sanitized-message-parity.test.ts:16` 의 상호참조를 놓쳤다. 새 이름 + 옛 이름 병기로 정정. **내 스윕이 놓친 이유도 실측했다** — `head -12` 로 자른 출력을 전수로 읽었다 |
| 2 | 유지보수성·부작용 (**3 리뷰어 수렴**) | **고침** | `composeTexts` 가 "compose" 라면서 루트의 **모든** `.yml`/`.yaml`(`pnpm-lock.yaml` 784KB 포함)을 읽었다. `docker-compose*` 로 좁혔다 — 오늘 매치 0건이라 무해했지만 **구현이 이름보다 넓었다** |
| 3 | 문서화 | **고침** | 위 "가장 아픈 지적" — 한계 절 복원 + env 축까지 일반화 + 재발 경위 |
| 4 | 문서화 | **고침** | CHANGELOG 가 옛 파일명·*"허용목록 없음"* 을 그대로 두고 있었다. 리네임·허용목록 4강제·**두 번 바뀐 경위**를 반영 |
| 5 | SPEC-DRIFT | **등재분** | `user-guide-evidence.md §2` 가 "가드 3건" — `#1330` 부터의 선재 갭, planner 항목으로 이미 등재(4번째 독립 확인) |
| 6 | 테스트 | **고침** | 재작성하며 실제 코퍼스를 **이름으로** 고정하던 회귀 단언이 전부 합성 fixture 로 갈렸다. 총량 floor 는 개별 토큰이 사라져도 통과한다 — `discord.en.mdx`/`EXECUTION_TIMEOUT` 과 `mcp-servers.mdx`/`MCP_ALLOW_INSECURE_URL` 둘로 복원 |

## INFO — 조치 불요 (사유 기록)

- **1·2·3 (security)**: ReDoS 형태 아님(`_` 앵커 분리) · env 스캔은 **이름만** 수집하고 값은
  버림 · 존재≠방출은 보안 이슈가 아니라 정합성 문제로 별도 트랙.
- **4 (performance)**: env 병합이 오늘 지탱하지 않는다는 사실이 코드·plan·테스트 **세 곳에
  뮤테이션 실측으로 일관 disclose** 됨 — 리뷰어가 양성으로 확인.
- **5 (performance)**: 가드별 소스 재적재 — 기존 자매와 동일 패턴, 가드가 더 늘면 공유 헬퍼.
- **6 (architecture)**: 과거 결함 재현이 삭제된 구현의 정규식을 손으로 복제 — 연결이 git
  이력뿐. 선택 사항이라 미적용(SHA 를 적으면 그 SHA 가 낡는 축이 새로 생긴다).
- **7 (architecture)**: 한 파일에 세 책임 누적 — 축 4개 초과 또는 허용목록 상한 근접 시 분리.
- **8 (scope)**: 무관 백로그(`cafe24` Principle 오인용) 등재는 관례 준수 + 무관 명시.
- **9 (scope)**: 이력 단절 — **리뷰어가 맞고 내 첫 반박이 틀렸다.** 처음엔 *"실제로는
  `git mv` 를 썼으니 리뷰어가 diff 표면만 본 것"* 이라고 적었는데, 검증하니
  `--find-renames=25%` 로도 git 이 **D + A** 로 기록한다. 명령을 **실행한 것**과 git 이
  **rename 으로 인식하는 것**은 다르다 — 내용을 크게 재작성하면 유사도가 임계 아래로 떨어져
  `git log --follow` 가 끊긴다. **내가 친 명령이 아니라 남은 효과가 판정 기준이다.**
  지금 고치려면 이력 재작성이라 조치하지 않고, 다음에 리네임+재작성이 겹치면 **두 커밋으로
  나눌 것**(먼저 순수 리네임, 그다음 내용 변경)을 여기 기록한다.
- **10 (testing)**: `collectEnvDeclarations` 합성 단위 테스트 부재 — 낮은 우선순위.
- **11 (documentation)**: `\\b` 가 한국어에서 성립하지 않는다는 교훈이 문맥-게이팅 축 삭제와
  함께 소실 — 그 축 자체가 사라졌으므로 교훈의 자리도 사라졌다. 공유 규약 문서 이관은 별건.
- **12 (dependency)**: 신규 외부 의존성 0건.

## 검증

`run-test-all.sh` ALL PASS (e2e 307) · 가드 스위트 19/19 GREEN(회귀 단언 2건 포함).
