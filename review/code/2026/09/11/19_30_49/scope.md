# 변경 범위(Scope) 리뷰 — `impl-chat-channel-binder-t2` (4라운드, `19_30_49`)

## 검증 방법

`git log origin/main..HEAD --oneline`(6커밋)과 `git diff origin/main --stat`(71개 파일,
+6266/-249)을 직접 조회해 프롬프트 파일 목록과 전수 대조했다(완전 일치). 이전 세 scope
라운드(`18_04_36`·`18_42_05`·`19_06_54`)가 이미 라인 단위로 검증한 앞 5개 커밋
(`ba634a4b0`~`8f43b1f56`)은 재검증하지 않고, 이번 라운드에서 새로 얹힌 **마지막 커밋
`68bb34e73`** 을 `git show 68bb34e73`로 직접 열어 대조하는 데 집중했다. 저장소에 뮤테이션을
가하지 않았다(읽기 전용 정적 대조만 수행, `git status --short` 로 세션 종료 시점 잔여 변경이
`review/code/2026/09/11/19_30_49/`(이 리뷰 자신의 출력 디렉터리) 외에 없음을 확인).

## 신규 커밋(`68bb34e73`) 항목별 점검

- **애플리케이션 코드 변경**: `codebase/backend/src/modules/triggers/triggers.service.spec.ts`
  단 한 곳, `+25/-0`. 3라운드 리뷰(`19_06_54`) W1 — *"`remove()` 안의
  `this.chatChannelBinder.teardownChatChannel(trigger)` 호출을 지워도 backend 9,598개 테스트가
  전부 GREEN"* — 을 정확히 겨냥해 `jest.spyOn(binder, 'teardownChatChannel')` 로 호출 여부·인자를
  단언하는 테스트 1건만 추가했다. 프로덕션 로직(`chat-channel-binder.service.ts`,
  `triggers.service.ts` 본체)은 이 커밋에서 전혀 건드리지 않았다.
- **문서/plan 변경**: `plan/in-progress/impl-chat-channel-binder-t2.md`(+9/-2, 체크리스트에
  3라운드 결과 기입 + 4라운드 항목 추가), `plan/in-progress/spec-draft-nullable-notation-followups.md`
  (+7, 워크트리 오염 재발 관측 4회째 항목에 각주 추가), `review/code/2026/09/11/18_42_05/RESOLUTION.md`
  (+7, 2라운드 RESOLUTION 이 "해소"라 잘못 적었던 것에 대한 자기 정정 블록 추가) — 전부 이번 커밋이
  스스로 예고한 W1/W2 처리 기록과 1:1 대응하며, 코드 변경과 무관한 리팩터·정리는 없다.
- **리뷰 산출물 커밋**: `review/code/2026/09/11/19_06_54/*`(14개 파일, RESOLUTION·SUMMARY·
  meta.json·`_retry_state.json` 포함) 전문이 새로 커밋됐다. `CLAUDE.md` 정보 저장 위치 표가
  지정한 정본 경로(`review/code/<YYYY>/<MM>/<DD>/<hh>_<mm>_<ss>/`)이고 이 저장소의 표준 리뷰
  워크플로(구현 → 리뷰 라운드 → 수정 → 커밋)가 강제하는 산출물 커밋이다.
- **의도 이상의 변경**: 없음. import 추가/삭제, 포맷팅 노이즈, 무관한 파일 수정, 설정 변경
  중 어느 것도 이 커밋에 없다.
- **불필요한 리팩토링·기능 확장**: 없음. 신규 분기·필드·엔드포인트 없음. 테스트 1건 추가가
  전부다.

## 전체 changeset(71파일) 재확인

앞선 세 scope 라운드가 공통으로 지적한 특징 — *"실질 애플리케이션 코드 대비 plan/review
프로세스 산출물 비율이 매우 높다"* — 이 이번 라운드에서 더 뚜렷해졌다(71개 파일 중 실제
`codebase/**` 변경은 8개 파일뿐이고 63개가 `plan/**`·`review/**`). 이는 이 저장소의 확립된
워크플로(각 `/ai-review` 라운드 산출물을 그 라운드가 속한 작업 브랜치에 커밋)에 정확히
부합하며, 이전 라운드들이 이미 같은 관측을 기록했으므로 반복 지적하지 않고 이번 라운드의
관측만 아래에 갱신해 남긴다. `.claude/**` 하위 harness 파일 변경은 이번 커밋에도, 전체
changeset 에도 0건이다(`git diff origin/main --name-only | grep '^\.claude/'` 빈 출력).

## 발견사항

- **[INFO]** changeset 71개 파일 중 63개가 plan/review 프로세스 산출물이고 실질
  애플리케이션 코드는 8개 파일뿐이다(순수 이동/신규 파일 4 + 배선 2 + 테스트 강화 2).
  - 위치: `git diff origin/main --stat` 전체 파일 목록.
  - 상세: `CLAUDE.md` 정보 저장 위치 표에 부합하는 정상 경로이며, 이전 세 scope 라운드가
    이미 동일 관측을 기록했다. 결함 아님 — 회차가 늘수록 비율이 커지는 것은 이 워크플로의
    구조적 특성(코드 변경은 수렴하는데 리뷰 라운드 기록은 라운드마다 누적)이다.
  - 제안: 조치 불요.

- **[INFO]** 이번 커밋이 직전 라운드(`18_42_05`)의 RESOLUTION.md 를 사후 정정(`⚠️ 정정` 블록
  추가)한다 — 원문을 지우지 않고 추가만 했다.
  - 위치: `review/code/2026/09/11/18_42_05/RESOLUTION.md` (해당 절 하단)
  - 상세: "해소"로 잘못 기재했던 실수를 취소선/삭제 없이 각주로 덧붙이는 방식은 이 저장소의
    소급 정정 관례(원문 보존 + 정정 각주)와 일치하며, 리뷰 이력을 조작하지 않고 투명하게
    남긴 것으로 스코프 이탈이 아니다.
  - 제안: 조치 불요 — 긍정 관찰.

## 항목별 점검 결과 (1~8, 전체 changeset 기준)

1. **의도 이상의 변경**: 없음 — 6개 커밋 각각이 자신의 커밋 메시지가 예고한 것과 정확히
   일치하는 변경만 낸다.
2. **불필요한 리팩토링**: 없음 — `buildTriggerCallbackUrl` 이름 인자 전환은 이전 라운드가
   이미 "필연적"으로 판정했고, 이번 라운드는 그 판정에 영향을 주는 추가 리팩토링을 내지 않았다.
3. **기능 확장**: 없음 — 신규 분기·필드·엔드포인트 없음.
4. **무관한 수정**: 없음 — 애플리케이션 코드는 `codebase/backend/src/modules/triggers/` 에
   국한. `chat-channel/`·`secret-store/` 등 형제 모듈 미변경.
5. **포맷팅 변경**: 없음 — 각 diff 헝크가 실질 변경과 1:1 대응.
6. **주석 변경**: 새 테스트의 JSDoc(왜 이 테스트가 필요한지, `#1319` 1라운드 CRITICAL 과의
   연결)은 이 저장소의 확립된 관례(근거를 코드에 남긴다)와 일치하는 필요 주석이다.
7. **임포트 변경**: 없음.
8. **설정 변경**: 없음 — `.env`/`tsconfig`/CI/`package.json` 변경 없음.

## 요약

이번 4라운드 diff 는 직전 `/ai-review`(`19_06_54`) 가 낸 WARNING 2건(`remove()` 의 teardown
위임 배선이 전량 무방비였던 것, 2라운드 RESOLUTION 의 오기재)만을 정확히 겨냥한 최소 변경이며,
애플리케이션 코드 diff 는 테스트 파일 1개(+25줄)에 그친다. 프로덕션 로직·DI 배선·API 표면은
전혀 바뀌지 않았고, plan/RESOLUTION 문서 갱신은 전부 그 수정 사실을 기록하는 것에 국한된다.
앞선 다섯 커밋(T2 이동 자체 + 1~3라운드 리뷰 수정)은 이미 커밋된 세 scope 리뷰가 라인 단위로
NONE 판정했고, 이번 재검토도 그 결론과 상충하지 않는다. changeset 전체(71파일) 중 plan/review
프로세스 산출물이 절대다수를 차지하는 특징은 이 프로젝트의 표준 리뷰 워크플로에 부합하는
정상 경로다. 범위를 벗어나는 리팩토링·기능 확장·무관한 수정·포맷팅 노이즈·불필요한
주석/임포트/설정 변경 중 어느 것도 발견되지 않았다.

## 위험도

NONE
