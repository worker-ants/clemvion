# RESOLUTION — `review/code/2026/09/11/12_41_25` (4라운드, `--route=all`) — **리뷰 루프 종결**

**입력**: CRITICAL 0 · WARNING 2 · SPEC-DRIFT 1 · RISK LOW · reviewer 14/14 · forced 누락 0 ·
unfinished 0.

**결론**: `codebase/**` 수정 **0**. 3라운드 WARNING 전부 해소 확인됐고, 신규 2건은 **둘 다 내가
쓴 문장의 정밀도 문제**로 등재 대상이다. 발견의 성격이 *동작 → 구조 → 문서 → 내 산문의
정밀도* 까지 내려왔으므로 **수렴으로 판정하고 루프를 종결한다.**

---

## WARNING 1 — 배열-형태 e2e 케이스가 `code` 를 단언하지 않는다

**분류**: 등재 (커버리지 개선). **내 주장이 거짓인 것은 아니다.**

### 실측 — 주장과 커버리지를 대조했다

내 파일 헤더는 *"`error.details.code` 단언이 이 파일에 **다섯 번** 나온다"* 와 *"**각 `it()` 의
`toEqual`** 은 정확 일치라 `code` 가 빠지면 RED"* 라고 적는다. reviewer 가 지목한 케이스
(*"너무 짧은 plaintext (DTO `@MinLength` 발동) → 400 DTO envelope"*)는 `toEqual` 이 아니라
**`.toBe()` 로 `details[0].field` 만** 본다 — 즉 내 문장이 세는 다섯 곳에 **포함되지 않는다.**
헤더는 자신이 덮는 범위를 정확히 말했다.

다만 reviewer 의 실질 지적은 맞다: **배열 갈래(파이프 층)의 `code` wire 증거가 0건**이다.
그 층의 `code` 는 이 PR 이 배선한 것이 **아니고**(`validation.pipe.ts` 가 이전부터 싣는다)
그래서 이 PR 의 검증 의무 밖이지만, 한 줄이면 그 갈래도 고정된다.

**처분**: 이미 등재된 *"PATCH 경로 e2e wire 증거"* 항목과 **같은 축**이라 그 항목에 합쳐
등재한다 — 두 갈래(배열/객체) × 두 경로(POST/PATCH) 중 지금 덮인 것은 객체×POST 하나다.

## WARNING 2 — 내 두 주석의 확신도가 어긋난다

**분류**: 등재 (주석 정합). **진짜 불일치다.**

| 자리 | 문면 | 작성 라운드 |
|---|---|---|
| `triggers.service.spec.ts` `authConfigId` 테스트 주석 | *"§5.3 의 「둘을 겹쳐 쓰지 않는다」를 **어기지 않는다**"* — 결론을 단정 | 2라운드 |
| `triggers.service.ts` `assertAuthConfigInWorkspace` 소스 주석 | *"§5.3 **판정 미해결**, planner 결정 대기"* | 4라운드 |

**소스 주석이 맞고 테스트 주석이 과했다.** 2라운드에 단언을 넣을 때 나는 그 자리가 §5.3 의
금지에 걸리는지를 **판정한 적이 없는데** 결론처럼 적었다. 4라운드에 소스 주석을 쓰면서
*"판정 자체가 사안"* 임을 알게 됐고, 그때 **옛 주석을 같이 고치지 않은 것이 이 불일치다.**

**처분**: 등재. `codebase/**` 주석-only 수정은 리뷰를 stale 시켜 라운드를 늘리는 지렛대이고,
정합시킬 방향(테스트 주석 → "planner 결정 대기")은 **§5.3 판정이 나오면 어차피 다시 바뀐다** —
그 판정 항목과 함께 처리하는 것이 맞다.

## SPEC-DRIFT — 이미 durable 등재됨

`15-chat-channel.md §5.4.1.2` 의 시한부 문장. reviewer 도 *"이미 두 plan 에 durable 하게
등재돼 있다(신규 미검출 아님)"* 로 확인했다. planner 턴 대상 — developer 는 자기-반증형
소정정 조건 1 불성립(그 문장은 `#1316` planner 턴이 썼다).

## INFO 7 — 내 fixture 주석의 사실 오류

`password.util.spec.ts` 의 `it.each` JSDoc 에 *"`'P@ss1'` 은 3종을 갖췄으나 8자 미만"* 이라
적었는데 실제로는 **4종**이다(`P` 대문자 · `@` 특수 · `ss` 소문자 · `1` 숫자). **결론은
맞다**(종류 분기가 발동하지 않고 길이 분기만 발동) — 개수만 틀렸다. 등재.

---

## 등재한 3건 (durable 트래커)

`plan/in-progress/spec-draft-nullable-notation-followups.md` 의 *"`details[].code` 배선의 잔여
개선"* 항목에 합친다:

- (h 확장) e2e wire 증거를 **두 갈래 × 두 경로** 축으로 — 지금 덮인 것은 객체×POST 하나다.
- (j) `triggers.service.spec.ts` `authConfigId` 테스트 주석의 확신도를 소스와 정합
  (§5.3 판정 항목과 **함께** 처리).
- (k) `password.util.spec.ts` fixture JSDoc 의 *"3종"* → **4종** 정정.

## 왜 5라운드를 돌지 않나

선언한 정지 규칙은 *"종료 조건은 「발견 0」이 아니라 「`codebase/**` 수정 0 으로 끝나는
라운드」"* 이고 **이 라운드가 그것이다.** 4라운드째 발견의 성격은 전부 **내가 쓴 산문의
정밀도**(주장 범위·주석 확신도·개수 오기)이고, 그 셋 중 둘은 §5.3 planner 판정이 나오면
**어차피 다시 손대야 하는 자리**다. 지금 고치면 그 판정 뒤에 또 고친다.

3라운드 상한을 한 번 넘긴 것은 커밋 `9fcce3f47` 본문에 사유를 적었다 — 직전 라운드가 넣은
`review-citations.md` 위반 2곳을 그대로 머지하는 것이 상한 준수보다 나빴기 때문이다.
**그 초과가 값을 했다**: 4라운드가 위 두 자기-과잉주장을 새로 찾았다.

## 검증 상태

4단계 전부 PASS (마지막 코드 커밋 기준) — lint · unit(backend **9,568** / 454 suite) ·
build(**타입체크 ratchet 둘**, baseline 197 / 52 일치) · e2e(supertest 305 + **playwright 51**).

라운드별 수렴: **R1** 5 WARNING(구조) → **R2** 2(문서) → **R3** 2(내 일괄적용 1건 + drift) →
**R4** 2(내 산문 정밀도). CRITICAL 은 네 라운드 전부 **0**.
