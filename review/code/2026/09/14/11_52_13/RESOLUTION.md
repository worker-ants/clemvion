# RESOLUTION — `/ai-review` 라운드 2 (`review/code/2026/09/14/11_52_13`)

**Critical 0** · WARNING 2 · INFO 7 · 위험도 **LOW**. forced 7명 전원 확보.
`[CRITICAL]` 마커 실측 **0건**.

## WARNING#2 — 방어 분기를 «수기 뮤테이션» 으로만 확인하고 묶지 않았다 (고침)

라운드 1 에서 `readStringArrayConst` 에 `existsSync` 방어를 넣고 **그 자리에서 손으로**
경로를 바꿔 RED 를 봤다. 그런데 **커밋된 spec 에는 그 분기를 타는 `it()` 이 0건**이라
CI 가 회귀를 못 잡는다 — 분기를 통째로 지워도 9/9 GREEN 이었다.

> 라운드 1 에서 다른 가드들은 대조군으로 묶어 놓고 **이 분기만 «한 번 봤다» 로 끝냈다.**
> «GREEN 은 증거가 아니다» 를 뮤턴트로는 지켰는데 **영속성** 쪽을 빠뜨렸다.

### `.toThrow()` 로는 안 된다 — 이 저장소가 이미 아는 함정

파일이 없으면 **`readFileSync` 도 던진다.** 그래서 맨 `.toThrow()` 는 방어 분기를 지워도
참이다 — 기록된 *"`.toThrow()` 는 무엇이 던졌는지 안 본다"* 형태 그대로다.
**메시지로 판별**하도록 짰다:

```ts
expect(() => readStringArrayConst(tmp, 'definitely-absent.ts', 'X'))
  .toThrow(/옮겨졌거나 이름이 바뀌었다/);
```

| 뮤턴트 | 예측 | 실측 |
|---|---|---|
| `existsSync` 방어 분기 통째 삭제 | RED, **그 한 건만** | **1 failed / 9 passed** — 정확히 일치 |

그 실행에서 `ENOENT` 가 실제로 관측됐다 — **뮤턴트도 던지긴 했다**는 뜻이고, 맨
`.toThrow()` 였으면 통과했을 자리를 메시지 매칭이 갈랐다는 직접 증거다.

## WARNING#1 — `2-trigger-list.md` 의 `code:` 갭 (조치 불요, 등재분)

라운드 1 과 같은 지적이고 리뷰어가 *"코드가 spec 을 위반하는 방향이 아니라 spec 문서의
메타데이터가 좁아진 것"* · *"트래커에 이미 정당하게 등재됨(우회 아님), 코드 쪽 추가 조치는
불요"* 로 판정했다.

## INFO 처분

| # | 항목 | 처분 |
|---|---|---|
| 1 | 중첩 템플릿 리터럴 `${'…'}` 잔재 | **고침** — 라운드 1 수정의 부스러기 |
| 2 | 단건 조회 커버리지 | 등재분, 리뷰어가 *"은폐 아님"* 확인 |
| 3 | `secret-store.md` §R4 가 `delete()`, §2.1 은 `remove()` | **planner 항목으로 등재**(아래) — 이 배치가 §R4 를 처음 명시 인용해 가시화됐다 |
| 4 | 동명 지역 선언 미구분 | 대상 3파일에 해당 없음. JSDoc 에 «최상위 하나» 가정을 적을지는 다음 접촉 시 |
| 5 | G·H 가 원 회귀(chatChannel 재조회)의 정확한 분기는 아님 | **결함 아님** — schedule 은 PATCH 로 `chatChannel` 을 구조적으로 못 보낸다(`disallowed`). 더 일반적인 회귀를 지킨다 |
| 6 | 라운드 1 커밋이 코드+트래커+리뷰 산출물을 한 커밋에 | **참고** — 기록된 순서(코드 커밋 → 세션 → SUMMARY → 리뷰-only 커밋)와 어긋난다. 다음 배치에서 지킨다 |
| 7 | 보안·부작용 양성 | 조치 불요 |

## 검증

- 가드 스위트 **9 → 10** GREEN · 새 대조군이 **판별함을 뮤턴트로 확인**
