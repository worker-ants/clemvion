# RESOLUTION — `/ai-review` 라운드 3 (`review/code/2026/09/14/12_17_14`)

**Critical 0** · WARNING 2 · INFO 7 · 위험도 **LOW**. forced 7명 전원 확보.
`[CRITICAL]` 마커 실측 **0건**. security·requirement·scope·side_effect 4명 **NONE**.

## WARNING#1 — 약속한 항이 셋인데 잠근 것은 둘이었다 (고침)

가드 JSDoc 이 *"`as`·`satisfies`·괄호를 **루프로** 벗긴다"* 라고 **셋**을 약속하는데,
괄호(`ParenthesizedExpression`) 분기를 지워도 10/10 GREEN 이었다. 리뷰어 주장을 전후로
직접 쟀다:

| | 괄호 분기 삭제 뮤턴트 |
|---|---|
| 대조군 추가 **전** | **GREEN (생존)** — 갭 실재 확인 |
| 대조군 추가 **후** | **RED**, 그 한 건만 (11건 중 1 failed) |

`const X = (['a', 'b'] as const);` fixture 로 닫았다.

> **이 배치에서 세 번째 같은 형태다** — 라운드 1 `existsSync`(수기로만 확인) · 라운드 2 그
> 분기의 영속성 · 라운드 3 괄호 항. 공통점은 **내가 JSDoc 에 N 개를 열거하고 N−1 개를
> 잠근다**는 것이다. 기록된 *"문서한 보장이 구현보다 넓으면 안 된다"* 의 테스트 판(版)이고,
> 판별 질문은 **«열거한 항마다 대조군이 있는가»** 다.

## WARNING#2 — 같은 수치를 두 문서에 쓰고 하나만 고쳤다 (고침)

라운드 2 에서 *"9자리"* 를 13으로 정정했는데 `trigger-canary-hardening.md` 만 고치고
**트래커의 둘째 사본을 놓쳤다.** 한 배치가 쓴 두 문서가 서로 다른 수를 **«(실측)» 표시와
함께** 주장하고 있었다 — 다음 사람이 재검증 없이 오신뢰할 자리다. 트래커 쪽도 정정하고
정정 이력을 남겼다.

## INFO 처분

| # | 항목 | 처분 |
|---|---|---|
| 1 | SPEC-DRIFT — `2-trigger-list.md` `code:` | 등재분. 리뷰어가 *"구현이 계약을 넓혔을 뿐 위반 아님"* 확인 |
| 2 | `secret-store.md §R4` 의 `delete()` | 등재분(라운드 2) |
| 3 | 에러 메시지에 자기 파일명 하드코딩 | **유예** — `__filename` 은 컴파일 산출물 경로를 내 «가드 파일» 로 오인하게 할 수 있다(`dist/…`). 리네임 시 함께 고칠 자리라 다음 접촉 시 |
| 4 | AST 언랩 로직이 3곳 독립 재구현 | 리뷰어가 *"저장소가 «가드별 독립 순수 로직» 관례를 명시 채택"* 확인. 4번째에 재고 |
| 5 | 동명 비-최상위 선언 미구분 | 라운드 2부터 유예. 대상 3파일에 해당 없음 |
| 6 | 단건 `GET /api/triggers/:id` 커버리지 | 등재분. *"은폐 아니라 plan 이 스스로 좁힌 스코프"* |
| 7 | 케이스 헤딩의 콜론/em dash 혼용 | grep 목적에 영향 없음(숫자만 통일하면 된다). 다음 편집 시 |

## 검증

- 가드 스위트 **10 → 11** GREEN
- 갭을 **전후로** 실측: 대조군 전 GREEN(생존) → 후 RED(1건 단독)
