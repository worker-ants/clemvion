# RESOLUTION — `/ai-review` 라운드 5 (`review/code/2026/09/14/13_04_49`)

**Critical 0** · WARNING 1 · INFO 7 · 위험도 **LOW**. forced 7명 전원 확보.
`[CRITICAL]` 마커 실측 **0건**. **6명이 NONE**(security·requirement·scope·side_effect·
maintainability·testing), documentation 만 LOW.

## WARNING#1 — 수를 맞추지 말고 복제를 없앴다 (고침)

캐너리 self-spec 헤더가 *"다섯 자리"* 를 **두 번** 적는데 실측 **6**이고, 형제 e2e 헤더는
*"여섯 형태"* 라 **두 문서가 다른 수를 주장**하고 있었다.

**내가 만든 결함은 아니다** — `origin/main` 에도 있다(실측 2건). 그러나 **항목 3 에서 바로 그
docstring 을 고쳐 쓰면서 두 줄 위의 틀린 수를 못 봤다.** 이 배치 **여섯 번째** 같은 형태다.

처분은 «6으로 고친다» 가 아니라 **«수를 여기서 빼고 e2e 파일을 SoT 로 가리킨다»** 다 —
맞추면 자리가 하나 늘 때 또 갈린다. 항목 4 의 teardown 서술을 한 곳 정본으로 두고 자매가
가리키게 한 것과 같은 처분이다.

## INFO#3 — plan 의 «9건» 이 낡았다 (고침)

항목 1 체크리스트가 착수 시점 값에 멈춰 있었다 → **«착수 9건 → 라운드 1~5 가 대조군 3건을
더해 최종 12건»** 으로 갱신. (이 배치에서 stale 수치는 세 번째다 — 9자리·9건·`delete()` 스코프.)

## INFO#4 — **철회했다. «한 줄» 이 아니었다**

*"`tmp` 를 `string | undefined` 로 바꾸고 `afterAll` 에 가드"* 를 적용했더니 **타입체크
ratchet 이 `0 → 10` 진단**을 냈다 — 사용처 10곳이 `string` 을 요구한다. **jest 는 타입을
strip 해서 못 본다**; `build` 단계의 ratchet 만이 잡았다.

남은 선택지는 둘뿐이고 둘 다 값이 안 맞는다:

| 선택지 | 왜 안 하나 |
|---|---|
| 사용처 10곳 수정 | 얻는 것(이미 실패 중인 환경에서 이중 예외 회피)에 비해 과하다 |
| `let tmp!: string` | «미할당일 수 있다» 를 «확실히 할당된다» 로 단언하는 **타입 거짓말** — 이 저장소의 `nullable-type-lie-cast` 가드가 겨누는 바로 그 형태다 |

원복하고 **그 판단을 코드 주석에 남겼다** — 다음 사람이 같은 «개선» 을 다시 시도하지 않도록.

> 리뷰어가 *"신경 쓰인다면"* · *"차단 사유 아님"* 으로 정확히 낮춰 제안했다. 내가 그것을
> 값싸다고 보고 집어 든 것이 판단 착오였고, **ratchet 이 그 착오를 잡았다.**

## 나머지 INFO — 조치 불요

| # | 항목 | 사유 |
|---|---|---|
| 1 | unwrap 로직이 `user-entity-exposure-guard.ts` 와 두 번째 독립 구현 | 리뷰어가 *"괄호 처리 차이는 실제 제약에서 온 합리적 발산, 규칙-of-3 미달"* 로 판정 |
| 2 | `readAllTriggerSecretColumnLists` 전용 단위 테스트 없음 | 원자 함수가 12케이스로 잠겨 있다. 기존 처분 재확인 |
| 5 | `secret-store.md §R4` 오기 | 등재분. 전수 3곳·Flyway 무조치까지 트래커에 반영됨 |
| 6 | 커밋 4건이 코드+plan+리뷰 산출물을 함께 담음 | 라운드 2 에 *"다음 배치에서 지킨다"* 로 자체 기록. 실질 스코프 이탈 없음 |
| 7 | 보안·부작용·스코프 교차 확인 | **양성** |

## 검증

- 가드 스위트 **12건** · 캐너리 self-spec **12건** GREEN
- `run-test-all.sh` **ALL PASS** — 단, **한 번 `build` 에서 멈췄고**(위 INFO#4) 원복 후 통과
