# RESOLUTION — `/ai-review` 라운드 4 (`review/code/2026/09/14/12_37_01`)

**Critical 0** · WARNING 1 · INFO 11 · 위험도 **LOW**. forced 7명 전원 확보.
`[CRITICAL]` 마커 실측 **0건**. security·scope·side_effect·documentation 4명 **NONE**.

## WARNING#1 — `null`/`[]` 경계의 한쪽 진입로에 대조군이 없었다 (고침)

`readStringArrayConst` 의 JSDoc 이 *"`[]`(목록이 비었다)와 `null`(못 읽었다)을 가른다"* 고
선언하는데, **«이름은 맞는데 초기값이 배열이 아님»** 진입로는 잠겨 있지 않았다. 리뷰어
주장을 전후로 직접 쟀다:

| | 그 분기가 `[]` 를 내도록 하는 뮤턴트 |
|---|---|
| 대조군 **전** | **GREEN (생존)** — 갭 실재 |
| 대조군 **후** | **RED**, 그 한 건만 (12건 중 1 failed) |

`const X = { a: 1 };` fixture 로 닫았다. 리뷰어가 대조 실험(unwrap 단일패스 뮤턴트 → 5/11
RED)으로 **다른 분기는 판별력이 정상**임을 함께 보인 것도 확인했다 — 그래서 이 갭은
«테스트가 전반적으로 무르다» 가 아니라 **딱 그 한 진입로**의 문제였다.

## 같은 형태 다섯 번째 — 산문을 구조로 바꿨다

| R | 열거했는데 덮지 않은 것 |
|---|---|
| 1 | `existsSync` 방어를 수기로만 확인 |
| 2 | 「9자리→13」 정정을 두 문서 중 하나만 |
| 3 | JSDoc 이 래퍼 **셋**을 약속하고 **둘**만 잠금 |
| 4 | `null`/`[]` 경계의 한쪽 진입로 |
| (트래커) | stale `delete()` **세 곳** 중 하나만 등재 |

기록된 규칙이 *"세 번째 재발이면 산문 규율 말고 **코드로** 바꿔라"* 다. 네 번을 산문으로
적었으니 다섯 번째는 **구조**로 바꿨다 — 대조군 블록 머리에 **«분기 ↔ 대조군 대응표»**
(7행)를 뒀다. 형제 캐너리 `trigger-workflow-ref.spec.ts` 의 «가드 실행 순서 목록» 과 같은
장치이고, 그쪽에서 이미 효과가 입증된 형태다. **분기를 더하면 표에 행을 먼저 넣는다** —
행만 있고 대조군이 없으면 그 자리가 눈에 보인다.

## INFO#8 — 리뷰어가 서로의 뮤테이션을 봤다 (재발, 확인 완료)

requirement 리뷰어가 *"공유 워크트리에 uncommitted 뮤테이션 관측 — 내가 만든 것이 아니고
원복하지 않고 보고한다"* 를 절차 노트로 남겼다. testing 리뷰어의 실험이다(같은 가설을 같은
파일에서 검증 중이었다). 리뷰어 권고대로 착수 전 `git status --short` 확인 —
**`codebase/**` 잔재 0**, untracked 는 이번 라운드 산출물 디렉토리 둘뿐이다.

이 배치에서 **두 번째** 관측이다(라운드 1 은 `SOURCE_ROOTS`). 이미 등재된 프로세스 항목의
추가 사례.

## 나머지 INFO — 조치 불요

| # | 항목 | 사유 |
|---|---|---|
| 1 | 에러 메시지의 자기 파일명 하드코딩 | 라운드 3 에 유예 처분(`__filename` 은 `dist/…` 를 가리킬 수 있다) |
| 2 | AST 언랩이 3벌 독립 존재 | *"가드별 독립 순수 로직"* 관례. 4번째에 재고 |
| 3 | `expectTriggerWorkflowRef` 3곳 반복 | 독립 시나리오라 공용화 시 의도가 흐려진다 |
| 4 | `readAllTriggerSecretColumnLists` 전용 단위 테스트 없음 | 배선 함수이고 3개 원자 함수가 각각 잠겨 있다. 참고 기록 |
| 5·6 | 보안 양성 — 경로 하드코딩·AST-only·더미 시크릿 | 확인 |
| 7 | spec 문서 결함 2건 | planner 등재분. **전수로 세니 3곳**이었고 셋째는 무조치가 정답(Flyway) — 트래커에 반영 |
| 9 | 라운드 1~3 지적 전부 해소 확인 | **양성 확인** |
| 10·11 | 스코프 1:1 대응 · 부작용 없음 | 확인 |

## 검증

- 가드 스위트 **11 → 12** GREEN · 대응표 7행 전부 실제 `it()` 과 짝지어짐 확인
- 갭을 **전후로** 실측: 대조군 전 GREEN(생존) → 후 RED(1건 단독)
