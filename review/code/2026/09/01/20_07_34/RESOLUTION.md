# RESOLUTION — 잔여 2건 + lint 글롭 사각지대 리뷰 1라운드

대상 SUMMARY: 위험도 **MEDIUM** · Critical **0** · Warning **4** · INFO 4

WARNING 4건 전부 조치했다. **넷 다 코드가 아니라 내 plan 문서**이고, 성격이 둘로 갈린다 —
(A) 같은 diff 가 이미 한 일을 문서가 안 따라감(W1·W2), (B) 문서가 스스로 고쳤다고 말하는
자리에서 같은 결함을 새로 만듦(W3·W4).

## W1·W2 — 체크리스트가 같은 diff 와 모순됐다

plan 에 두 항목이 열린 채였다:

- *"`parser.ts:317` 은 이 PR 에서 고치지 않는다 — 원인이 환경이면 고칠 대상이 아니다"*
- *"로컬-CI 툴체인 차이 규명 — 재개 신호: 이 PR 의 packages-checks 결과"*

그런데 **같은 diff 가 둘 다 해결했다.** `parser.ts:317` 은 고쳐져 있고, 툴체인 차이의 lint
쪽은 근본 원인(글롭 quoting)을 찾아 6개 패키지를 수정했다. requirement·scope·testing **3명이
독립적으로** W1 을 지적했다.

전제가 바뀐 것을 적었다 — "원인이 환경이면" 이라는 조건이 **거짓으로 판명**됐으므로 그 조건에
매달린 유예도 성립하지 않는다. W2 는 "신호가 둘" 에서 **"하나뿐"** 으로 좁혔다.

## W3 — 죽은 링크를 고쳤다고 적은 배너 바로 아래에서 새로 만들었다

`spec-draft-avatar-storage-key.md` 를 `complete/` 로 옮기며 "인바운드 링크가 깨져 있었는데
이동이 그것을 고친다" 고 적었다. 그런데 **이 문서 자신의 outgoing 링크**가
`./spec-sync-user-profile-gaps.md` 를 가리키고, 그 파일은 여전히 `in-progress/` 다 —
**반대 방향으로 하나를 깼다.**

`../in-progress/` 로 정정하고, 같은 절의 `../complete/`(이동 후 자기 디렉터리를 되짚는 형태)도
`./` 로 정리했다.

**docs 가드는 3119 초록이었다** — `plan/complete/**` 가 상대링크 가드 범위 밖이다. 이동 절차에
"이동 문서 자신의 outgoing 링크 재계산" 이 없는 것과 합쳐져 조용히 통과한다. 별도 항목으로
등재했다(harness 규약 변경이라 이 changeset 과 성격이 달라 미조치).

## W4 — "실측으로 정정한다" 는 배너 안에서 미검증 사실을 강화했다

도입 커밋을 `4afab7ca1` 로 적었다. 실측:

| 확인 | 결과 |
|---|---|
| `git log --diff-filter=A` (파일 신설) | **`8ff827ef6` (#1233)** |
| `4afab7ca1` 이 이 파일에 한 일 | **주석 7줄 추가** — 타입 에러를 만들 수 없다 |
| 두 커밋의 CI `Test (jest)` | **양쪽 다 실행되고 success** |

가장 나쁜 지점은 **위치**다. 그 오귀속이 하필 *"아래 결론의 근거가 틀렸다"* 고 정정하는 배너
안에 있었고, 나는 인접한 미검증 사실을 재검증 없이 옮겨 적어 **강화**했다. 정정문을 쓰면서
그 문단의 다른 사실은 안 재본 것이다.

결론(로컬-CI 차이)은 바뀌지 않는다 — 두 커밋 모두 CI 에서 통과했기 때문이다.

## INFO 4건

- **INFO 1** (lint 스코프 실질 확장) — PR 본문에 명시했다. 향후 새 최상위 파일이 처음 lint 에
  걸릴 때 원인 추적에 필요한 정보다.
- **INFO 2** (`SubclassName` 매핑 타입의 진입장벽) — 근거 주석과 뮤테이션 검증이 딸려 있어
  실질 부담이 낮다는 리뷰어 판단에 동의. 재사용이 생기면 이름 있는 유틸리티로 승격.
- **INFO 3** (hoisting 차이) — plan 에 미해결로 추적 중. 이번 PR 이 CI 를 실제로 돌리므로
  1차 판정이 나온다.
- **INFO 4** (plan 이동이 `git mv` 가 아니라 delete+add 로 보임) — 예방적 권고를 따라
  커밋 후 `git show HEAD:<new-path>` 로 반영을 확인했다.

## 검증

lint **PASS** · unit **PASS** · build **PASS** · e2e **PASS**(backend 50 suites / 291 +
playwright 51 = **342**) · docs 가드 **3119**.
