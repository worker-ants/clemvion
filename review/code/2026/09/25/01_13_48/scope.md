# 변경 범위(Scope) 리뷰 — MinIO 이미지 일치 가드 · 3라운드

## 발견사항

- **[INFO]** 리뷰 대상 3파일 밖에서 `plan/in-progress/spec-draft-nullable-notation-followups.md` 도 이번 라운드(커밋 `749ee1499`→`9d734571e`→`48bab62ca`)에 4줄 추가됐다 (`git diff 749ee1499 HEAD` 로 확인, payload 의 "리뷰 대상 파일" 목록에는 없음).
  - 위치: `plan/in-progress/spec-draft-nullable-notation-followups.md` — k8s 버킷 Job 항목 아래 인용문 블록 (파일 내 line ~5847 부근, 5800줄대 대형 트래커라 전체 파일 컨텍스트는 이 payload 에 없음)
  - 상세: 내용을 확인해 보니 스코프 위반이 아니라 **정정**이다 — `self-hosting-deployment.md` §3 에 잘못 얹었던 "아바타 공개 정책" 체크박스(1·2라운드 리뷰 `00_25_55` W3 · `00_56_30` W2 가 스코프 밖으로 지적)를, 이번 라운드에서 그 plan 에서 **제거**하고 이미 존재하던 백로그 트래커의 k8s 버킷 Job 항목 쪽으로 각주 형태로 옮겼다. 즉 이전 라운드의 스코프 침범을 해소하는 이동이지 새로운 확장이 아니다.
  - 제안: 조치 불요. 다만 향후 orchestrator 가 scope reviewer 페이로드를 구성할 때 이런 "선행 지적에 대한 원복/이동" 커밋은 이동 대상 파일도 함께 포함하면 완결성이 높아진다.

- **[INFO]** `.claude/tests/test_minio_image_parity.py` 의 `_PINNED` 정규식 변경은 직전 라운드 리뷰(`00_56_30` W1 — 레지스트리 포트 뒤 `latest` 를 놓침)에 대한 정확히 그 범위의 수정이다.
  - 위치: `.claude/tests/test_minio_image_parity.py:74-76` (정규식 정의), `:239-257` (`test_pin_violation_edges` 신규 서브케이스)
  - 상세: `^[^:@\s]+:(?P<tag>[^@\s]+)@sha256:[0-9a-f]{64}$` → `^(?:[^/@\s]+/)*[^/:@\s]+:(?P<tag>[^/:@\s]+)@sha256:[0-9a-f]{64}$`. 추가된 주석(74-76행)은 왜 첫 `:` 분할이 틀렸는지 설명하는, 비직관적 정규식에 필요한 최소 설명이다. 새 서브테스트(`registry.example:5000/...`, 빈 태그, 포트+`latest`)는 모두 이 정규식 변경이 만들어 낸 새 분기를 직접 겨냥한다 — 과잉 테스트 아님.
  - 제안: 없음. 스코프 내 정밀 수정.

- **[INFO]** `plan/in-progress/minio-image-parity-guard.md` §D W3 셀 편집은 프로젝트 관례(원문 취소선 보존 + 정정 사유 병기)를 따른 자기 문서 정정이다.
  - 위치: `plan/in-progress/minio-image-parity-guard.md` — §D 처분 표 W3 행 (전체 파일 컨텍스트 122번째 표 행)
  - 상세: "같은 §3 의 «버킷 자동 생성» 줄이 아바타 공개 정책을 빠뜨린 것도 함께 적었다" 문장을 취소선으로 남기고, 그 판단이 스스로 스코프 밖이었음을 인정하며 이동 사실을 적었다. 새 기능·범위 확장이 아니라 스코프 자기 교정 기록.
  - 제안: 없음.

## 요약
이번 라운드(커밋 `9d734571e`, `48bab62ca`)의 실질 diff 는 두 갈래다 — (1) 직전 리뷰가 지적한 정규식 버그(레지스트리 포트가 `latest` 판정을 가림)를 정확히 그 범위에서 고치고 대응 테스트를 추가한 것, (2) 이전 라운드에서 이미 "스코프 밖"으로 지적됐던 아바타 공개 정책 체크박스를 `self-hosting-deployment.md` 에서 제거해 원래 있어야 할 백로그 트래커로 옮긴 것. 두 갈래 모두 의도된 작업 범위(MinIO 이미지 일치 가드) 및 직전 리뷰 피드백에 정확히 대응하며, 리팩토링·기능 확장·포맷팅 뒤섞임·불필요한 임포트/설정 변경은 관찰되지 않았다. 유일한 관찰 사항은 이동 대상 파일(백로그 트래커)이 이번 리뷰 payload 의 "리뷰 대상 파일" 목록에 포함되지 않았다는 점인데, 내용을 직접 대조한 결과 스코프 위반이 아니라 이전 위반의 정정이므로 위험으로 분류하지 않는다.

## 위험도
NONE
