# RESOLUTION — `review/code/2026/09/25/00_56_30` (3라운드, 파일 범위 지정)

**Critical 0 · Warning 2 · INFO 16.** forced 7/7. 정지 규칙 미충족 → 둘 다 조치, 4라운드를 돈다.

> **이 라운드의 범위 — 왜 `--branch` 가 아니라 파일 목록인가.** `--branch origin/main` 으로 준비하니 diff 가
> 1 · 2라운드 리뷰 산출물(`review/**` 40여 파일)에 먹혀 **정작 테스트 파일의 diff 가 «프롬프트 크기 제한으로 생략»**
> 됐다(돌리지 않고 폐기했다). `--branch` 가 있으면 파일 목록은 무시되므로(오케스트레이터 docstring 이 같은 사고를
> 기록하고 있다) 파일 목록만으로 다시 준비했다: 테스트 · 워크플로 · plan 둘(네 파일 전문 적재 확인).
> README(82KB, 파일당 상한 초과) · CHANGELOG · 트래커는 뺐다 — 이 라운드에서 그 셋이 바뀐 것은 분기 전수 문장뿐이고
> 같은 내용이 plan §B-2 에 있다.

## 조치 항목

| SUMMARY # | 지적 | 조치 | 판별 확인 | commit |
| --- | --- | --- | --- | --- |
| Warning 1 (testing, 실측 재현) | `_PINNED` 가 첫 `:` 에서 이름을 끊어 `registry:5000/x:latest@…` 의 태그를 `5000/x:latest` 로 읽는다 → `latest` 를 놓치는 거짓 음성. 가드의 존재 이유를 우회 | 정규식을 참조 문법대로: 태그는 **마지막 경로 구성요소** 뒤에만, 앞 구성요소는 포트의 `:` 허용. 경계 추가: 포트 + 정상 태그(통과) · 포트 + `latest` · 포트 + 다이제스트만 · 빈 태그 | 옛 정규식으로 되돌리면 **포트 관련 경계 둘만** RED(바이트코드 끔), 원복 후 `git status` 빈 것 | `9d734571e` |
| Warning 2 (scope) | `self-hosting-deployment.md` 의 아바타 정책 체크박스가 스코프 밖 — 1라운드 W3 에 이어 **두 번째** | 줄이는 것으로는 안 닫혔다. **그 plan 에서 뺐고**, 범위 밖 발견의 자리인 트래커의 k8s 버킷 Job 항목에 «같은 누락이 self-hosting §3 에도» 한 줄로 옮겼다. 이제 이 PR 의 그 plan 편집은 가드 자리 목록 교차 참조뿐이다(INFO 6 이 스코프 내로 판정) | — | (이 RESOLUTION 커밋) |

## INFO 처분

- **INFO 1**(대문자 hex · 65자 이상 경계) — 소문자는 의도다: OCI 이미지 명세가 sha256 인코딩을 `[a-f0-9]{64}` 로
  정의한다. 주석에 근거를 적었다. 65자는 `{64}` + `$` 로 이미 거부된다.
- **INFO 15**(빈 태그) — 경계 케이스로 추가했다(위 W1 커밋).
- **INFO 10**(파일 이동 시 `FileNotFoundError`) — 경로와 함께 실패하므로 «조용히 통과» 가 아니다. 가드의 약속 밖.
- **INFO 12 · 13 · 14 · 16** — 값 검사 반복 · 시퀀스 가드 대칭 · 헬퍼 docstring · `_render` — reviewer 도 선택으로
  판정. 두 자리 반복에 헬퍼를 들이면 간접성만 는다.
- 나머지 — 긍정 관찰.

## TEST 결과

- lint · unit · build · e2e — **해당 없음**: `codebase/**` 무변경. 검증은 하네스 pytest(CLAUDE.md).
- 하네스 · docs 가드 — 이 커밋 직전 실행 결과를 plan 체크리스트에 적는다.
