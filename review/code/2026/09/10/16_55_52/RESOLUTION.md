# RESOLUTION — `review/code/2026/09/10/16_55_52` (4라운드, 마지막)

Critical 0. Warning 3건 중 **1건 수정 · 2건 후속 등재**, INFO 중 1건 수정.
**`codebase/` 는 이 턴에 한 글자도 바뀌지 않았다** — 그것이 이 라운드가 마지막인 이유다.

## 수정 (전부 `plan/**` — 게이트의 `newest_code` 를 움직이지 않는다)

| # | 파일 | 수정 | 근거 |
|---|---|---|---|
| 1 | `plan/in-progress/harness-review-gate-followups.md` | "열려 있는 것의 성격" 목록에 **§N 불릿 추가** + 재발 사실 기록 + 세 번째 재발 시 코드로 바꿀 후보(문서 끝 `## §X` 절 수 ↔ 목록 불릿 수 대조 docs 가드) | documentation W1 |
| 2 | `plan/complete/trigger-workflow-ref-canary.md` | *"일곱 번째"* 의 단위를 명시 — **항목 7개, 발생 8회**("길이 표" 가 2회) | documentation INFO |

**1번은 아프게 정확한 지적이다.** 그 요약은 *"개수 단정 문장이 새 항목을 반영 못해 두 번 낡았다"*
는 이유로 이번 세션에 성격 목록으로 다시 쓴 것인데, **같은 세션·같은 날 §M 바로 다음에 등재한 §N 이
그 목록에서 빠졌다.** 즉 형태를 바꾼 편집이 그 편집의 목적을 그 자리에서 배신했다. 기록한 교훈은
*"형태를 바꾼 것만으로는 부족하다 — 항목을 추가할 때 이 목록을 함께 보는 습관이 필요하다"* 이고,
세 번째 재발이면 산문 규율 말고 가드로 바꾼다.

## 후속 등재 (`codebase/**` 주석 — 고치면 라운드가 하나 더 필요하다)

`spec-draft-nullable-notation-followups.md` 에 한 항목으로 묶어 등재했다.

| 지적 | 요지 |
|---|---|
| `maintainability` W1 | 케이스 헤딩 8개는 아라비아인데 `## 가드 ③·⑤` 하나만 원문자 — `grep '가드 [0-9]'` 로 "5" 가 안 잡힌다. 헤더가 내세우는 "위에서 아래로 스캔" 목적을 그 케이스에서 깬다 |
| `maintainability` W2 | 헤더가 3라운드 경고에도 23→34줄(+48%). **분량이 아니라 성격이 문제다** — 리뷰 이력은 RESOLUTION·커밋·plan 에 durable 하게 남는데 소스에 또 박으면 자기수정 로그가 매 라운드 쌓인다 |
| `requirement` INFO | 인용 `"keys [] ≠ ['id','name']"` 이 Jest 실제 출력의 리터럴이 아니라 의역 |

**셋 중 W2 만 습관 차원이다.** 나머지 둘은 한 자리를 고치면 끝나지만 W2 는 *"리뷰가 지적하면 그
사실을 소스 주석에 적는다"* 는 내 기본 반응을 지목하고, 그 반응이 네 라운드 동안 두 파일의 주석을
계속 키웠다. 판별 질문을 트래커에 적었다 — **"이 문장이 없으면 다음 사람이 코드를 잘못 쓰는가."**
예면 소스, 아니면 트래커.

## 왜 여기서 멈추는가 — 규칙을 결과 보기 전에 선언했다

3라운드 SUMMARY 에 적었다: *"4라운드를 마지막으로 하고, 그 라운드의 주석-수준 이하 발견은 브랜치에서
고치지 않고 후속으로 등재한다."* 규칙을 만든 뒤 발견이 그럴듯해서 깨는 것이 바로 그 규칙이 막으려는
실패다. 다만 **처분을 파일 위치로 갈랐다** — 게이트의 freshness 는 `codebase/**` 기준이므로 `plan/**`
수정은 라운드를 요구하지 않고, 정지 규칙이 막으려는 것은 *"코드를 고쳐 리뷰를 무효화하는 것"* 이지
기록을 정확히 하는 것이 아니다.

**수렴 근거는 발견의 성격이다** — 1R 동작 → 2R 구조 → 3R 주석의 사실성 → 4R 주석의 표기·성격.
이번 라운드는 **8명 중 6명이 NONE** 이고 남은 둘의 지적도 코드 밖이다.

> `--amend` 로 author date 를 보존하면 4라운드 세션이 새 커밋을 덮어 라운드 없이 고칠 수 있다.
> **쓰지 않았다** — reviewer 가 보지 않은 코드를 타임스탬프로 게이트에 통과시키는 것이고, 이 세션이
> 앞서 거절한 `BYPASS_REVIEW_GUARD` 와 같은 종류다.

## 검증

`codebase/` 무변경이므로 4단계를 다시 돌리지 않았다 — 직전 커밋(`cfd195fb4`) 시점에 전부 PASS 했고
그 이후 코드가 바뀌지 않았다. 이번 턴의 변경은 `plan/**`·`review/**` 뿐이다.

| 단계 | 마지막 실행(`cfd195fb4`) |
|---|---|
| self-spec 단독 | 12 passed / 12 |
| `lint` | PASS (57s) |
| `build` | PASS (163s) |
| `unit` | PASS (87s) — backend 454 suites / 9,521 |
| `e2e` | PASS (237s) — `tests=305` + playwright |
| 두 타입체크 ratchet | backend 197건/36파일 · frontend 52건/15파일 — baseline 일치 |
| docs 가드 | 21파일 / 3,214 tests PASS |

## 처분 요약

- Critical **0**.
- Warning **3** → `plan/**` 1건 **수정**, `codebase/**` 주석 2건 **등재**.
- INFO 중 1건 수정, 나머지는 확인 기록.
- **누적 코드 수정 21건은 이 라운드로 늘지 않았다** — 4라운드가 `codebase/` 를 안 건드린 첫 라운드다.
