# RESOLUTION — `review/code/2026/09/06/16_28_58` (+ consistency `16_29_00`)

**원 결과**: 코드 리뷰 Critical **1**(절차) · WARNING 4 ·
consistency **BLOCK: NO** · Critical **0** · WARNING 5(전부 선행 부채, 기등재)
**처분**: Critical 은 **사용자 결정으로 해소**. WARNING 2건 수정, 2건은 기록 처분.

---

## Critical — 코드 결함이 아니라 **절차 미결**이었다

scope reviewer 가 지적한 것: `.claude/**` harness 수정이 **승인 없이 이미 HEAD 에
반영돼 실효 중**이다. `CLAUDE.md` Skill 표는 developer 쓰기 범위를 `codebase/**`·
`plan/**`·`review/**` 로 적고 `.claude/**` 는 어느 역할에도 배정돼 있지 않다.

**이것은 직전 라운드에 내가 스스로 plan 에 올린 항목이다.** 리뷰어가 그것을 *"미결
상태로 코드가 돌고 있다"* 는 사실로 승격시켰다 — 등재는 처분이 아니라는 지적이고,
맞다.

**나는 이 Critical 을 닫을 수 없었다.** 스스로에게 권한을 부여할 수 없고, `CLAUDE.md`
는 `spec/`·`plan/` 어디에도 속하지 않아 planner 스킬 범위로도 덮이지 않는다.

### 사용자 결정 (2026-09-06)

세 선택지를 제시했고 **"남긴다 + `CLAUDE.md` 명시"** 를 택했다.

| 선택지 | 판정 |
|---|---|
| **남긴다 + `CLAUDE.md` 에 harness 행 명시** | **채택** |
| harness 커밋을 별도 PR 로 분리 | 미채택 — **이 PR 의 `--impl-done` 게이트가 그 파서 수정에 의존한다**(수정 전에는 `workspace-response.dto.ts` 가 spec-linked 로 안 잡혔다). 두 PR 로 가르면 서로를 기다리는 순환이 된다 |
| 되돌리고 spec 7파일 주석 스윕 | 미채택 — 인스턴스를 고치고 **클래스를 남긴다**. 다음에 누가 주석을 넣으면 같은 유실이 재발한다 |

따라서 **코드는 그대로 두고, 남은 것은 `CLAUDE.md` 다.** Skill 표에 harness 행을 넣는
작업을 plan 에 등재했다 — 지금 상태는 *"관행으로는 허용, 문서로는 미기술"* 이고, 그것이
이 브랜치가 계속 지적받은 결함의 형태다.

---

## W4 (maintainability) — orphan JSDoc 세 번째 재발

`it` → `it.each` 파라미터화 과정에서 JSDoc 두 개가 `callFor` 헬퍼 위에 쌓였고, 앞 블록
("반대 방향 대조군")은 헬퍼가 아니라 **테스트를 설명하는 것**이었다.

**이 브랜치가 이미 두 번 스스로 잡아 고친 클래스**다. 각 JSDoc 을 자기 대상 위로 옮겼다.

## W5 (documentation) — 해소된 제약을 여전히 지시하고 있었다

plan 의 열린 항목 캐비아트가 *"`code:` 에 YAML 주석 넣지 마라"* 라고 지시하는데, 같은
문서 뒤쪽 harness 항목은 그 파서 결함이 **해소됐고** `review-citations.md` 가 실제로
인라인 주석을 쓴다고 기록한다. 시점이 다른 두 서술이 공존했다.

취소선 + 해소 표기로 정정했다. **지우지는 않았다** — 왜 한때 금지였는지가 다음 사람에게
필요하다.

## W2·W3 (scope) — 기록 처분

JSDoc 인용 가드와 트리거 409 계약이 원 작업과 별개 관심사라는 지적. 리뷰어가 *"되돌릴
필요 없음"* 으로 판정했고, 연쇄의 인과(파서 수정 → 게이트 스코프 확대 → 기존 갭 발견)는
CHANGELOG·커밋 메시지·plan 에 이미 투명하게 적혀 있다. **PR 설명에 네 관심사를 명시**하는
것으로 처분한다.

## INFO#9 — 의도적 보류를 실측과 함께 등재

`integration-oauth.service.ts` 2곳이 `pgErrorConstraint()` 로 치환 가능하다. 실측: 각 4줄,
`isPostgresUniqueViolation` 을 이미 import 중이라 **import 한 줄 + 표현 2개**.

**하지 않은 이유는 비용이 아니라 범위다.** scope reviewer 가 관심사 확산을 반복 지적한
상황에서 다섯 번째 모듈을 여는 것은 그 지적을 정면으로 무시하는 것이다. 두 자리는 동작이
옳고(같은 두 표면을 본다) 위험이 없다 — 남은 것은 중복뿐이다.

## 조치하지 않은 나머지

| # | 사유 |
|---|---|
| 6·8·10·11·15·16 | 확인 기록(양성) |
| 7 | 구조 가드의 태생적 한계 — 문서화된 잔여 위험, 이름 축으로 보완 |
| 12 | `joinedAt` 이 `null` 에 도달하는 경로 없음 |
| 13·14 | 이미 plan 등재 |
| consistency W1~W5 | 전부 `2-trigger-list.md` 선행 부채, 기등재. W4(Auth Config)는 **유예 근거가 틀렸음을 확인해 정정**했고, W5(동명 타입)는 **프런트 쪽 역참조를 추가**했다 |

## 검증

lint / unit / build / e2e 전부 PASS (커밋 시점 수치는 커밋 메시지 참조).
