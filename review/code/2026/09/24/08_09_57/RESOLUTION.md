# RESOLUTION — `review/code/2026/09/24/08_09_57`

Critical 0 · Warning 6. 여섯 전부 이번 턴에 처리했다(등재로 갈음한 것 하나는 §보류 에 사유).

## 조치 항목

| # | 카테고리 | 조치 | 커밋 |
| --- | --- | --- | --- |
| 1 | 문서화 — CHANGELOG 누락 | 최상단에 항목 추가. 더해 **#1373 항목의 «남는 것» 서술을 정정**했다 — 거기 «이 PR 이 닫지 않는다» 로 적힌 owner 승격 TOCTOU 를 이번 PR 이 닫았으므로 취소선 + 해소 주석을 달았다 | `06aa6d5e1` |
| 2 | 보안 — 권한 순서 오라클의 블라스트 반경 | **지적 대상 문서가 틀렸다.** 트래커 항목은 이미 *"요청자가 그 워크스페이스 멤버가 아니어도"* 로 정확하고 13/17 라우트 분석까지 담고 있다 — 좁게 적힌 것은 내가 이번 턴에 쓴 `plan/in-progress/member-owner-toctou.md` §F 였고 그쪽을 고쳤다. 지적의 **실질은 맞아서** 직접 확인했다: `RolesGuard` 는 `handlerConsumesWorkspaceId` 가 false 면 단락하고(`roles.guard.ts:116`) 이 핸들러는 `@Param('id')` 를 쓴다 → 노출 대상은 **임의 인증 사용자** | `06aa6d5e1` |
| 3 | 동시성 — 제3 상태 | **분기 형태 자체를 바꿨다.** `still?.role === 'owner'` → `if (still)`. DELETE 의 술어가 `role` 하나뿐이라, 행이 남아 있는데 0행이었다면 그 시점에 owner 였다는 뜻 말고 없다 — 이양 연쇄로 강등됐더라도 «막은 것은 owner» 는 그대로다. 현재 role 을 다시 물으면 **실재하는 멤버를 404 로** 보고하게 된다 | `06aa6d5e1` |
| 4 | 테스트 위생 — 공유 워크스페이스 오염 | 그 블록만 **전용 워크스페이스**를 만든다. raw UPDATE 가 남기는 «owner 2명» (애플리케이션 코드로 도달 불가)이 공유 fixture 에 남지 않고, *"이 블록은 파일의 마지막이어야 한다"* 는 **주석 의존 불변식이 사라졌다** | `06aa6d5e1` |
| 5 | 유지보수성 — 가드 대기 시간 3번째 하드코딩 | `VACUITY_GUARD_MS` 를 `test/helpers/concurrency.ts` 에서 **export** 하고 재진입 e2e 두 파일이 쓴다. 그래야 그 값이 `assertGuardBelowKnownTimeouts` 의 검사 범위 안에 있다 | `06aa6d5e1` |
| 6 | 유지보수성 — 재진입 보일러플레이트 2번째 복제 | **등재로 갈음**(§보류) | `06aa6d5e1` (트래커) |

### #3 의 여파 — 내 단위 테스트 하나가 일어날 수 없는 상태를 고정하고 있었다

«진 쪽은 404» 가 `affected: 0` + 재조회 `editor` 를 mock 했는데, 술어가 들어온 뒤로 그 조합은
성립하지 않는다. **술어 이전에 쓰인 단언이 그대로 남아 있었던 것**이다. 재조회를 `null` 로
바꿔 실제 경우(동시 제거)로 만들고, 강등 케이스를 별 블록으로 추가했다.

그래서 **직전 라운드의 뮤턴트 B 판정은 폐기하고 다시 쟀다** — 그 판정이 무효 mock 위에 있었다.

| 뮤턴트 | 예측 | 실측 | 죽은 테스트 |
| --- | --- | --- | --- |
| **A**: `role: Not('owner')` 제거 | unit 1 · e2e 1 | **1 · 1** | «한 행을 지우면 …»(술어 단언) · e2e «owner 로 승격되면 403»(`Expected 403, Received 200`) |
| **B′**: `if (still)` → `if (still?.role === 'owner')` (옛 형태 복귀) | unit 1 | **1** | «재조회가 강등된 행을 봐도 403 이다» |
| **B″**: `if (still)` 분기 제거 | unit 2 | **2** | «owner 로 승격됐으면 403» + «강등된 행을 봐도 403» |
| **C**: `affected === 0` → `!affected` | unit 2 | **2** | 드라이버 미보고 대조군 `undefined`·`null` |

A 의 e2e 다리는 **다시 쟀다** — 테스트 파일이 #4 로 바뀌었으므로 직전 측정을 재사용하지 않았다.

## TEST 결과

| 단계 | 결과 |
| --- | --- |
| lint | PASS (`_test_logs/lint-20260924-082914.log`) |
| unit | PASS — backend **472 스위트 / 9944 테스트** (`_test_logs/unit-20260924-083009.log`) |
| build | PASS — 타입체크 ratchet 포함 (`_test_logs/build-20260924-083125.log`) |
| e2e | **통과** — 379 PASS (`_test_logs/e2e-20260924-083407.log`) |

## 보류·후속 항목

**#6 (재진입 보일러플레이트 2번째 복제)** 만 등재로 갈음했다. developer SKILL §수렴 예외의
네 조건을 모두 인용한다:

- **(a) 동작 결함이 아니다** — 두 파일 다 초록이고 재현되는 오동작이 없다. 구조 수준이다.
- **(b) fix 가 새 라운드를 강제한다** — 헬퍼 추출은 `codebase/**` 변경이라 게이트 freshness 가
  재무장되고, 그 라운드가 또 잔여를 낼 형태다(직전 #1377 이 같은 추출에서 3라운드를 돌았다).
- **(c) 근거** — #1377 이 `raceUnderHeldLock` 을 뽑을 때 **갱신 경합을 의도적으로 제외**했고
  («요청 하나 + 락 안 mutate 는 축이 다르다»), 둘뿐인 지금 뽑으면 파라미터가 붙어 공유하는
  것보다 감추는 것이 많아진다. 비용이 아니라 **수렴**이 사유다. 임계를 «세 번째 자리» 로
  숫자로 고정해 트래커에 적었다 — 판단을 다음 사람에게 미루지 않는다.
- **(d) 그 턴에 등재** — `plan/in-progress/spec-draft-nullable-notation-followups.md` 에
  이름 후보(`reenterUnderHeldLock`)까지 함께 올렸다.

같은 턴에 등재한 **planner 항목 셋**(`--impl-prep` `07_29_15` 유래, 이 리뷰와 별개):
`data-flow/12-workspace.md` 에 owner 보호 메커니즘 명문화 · `CANNOT_REMOVE_OWNER` 외 2개 코드
카탈로그 등재 · `1-auth.md` §3.2 RBAC 표 GFM 깨짐.
