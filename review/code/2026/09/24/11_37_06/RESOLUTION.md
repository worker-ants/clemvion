# RESOLUTION — `review/code/2026/09/24/11_37_06` (2라운드)

Critical 0 · Warning 1 · SPEC-DRIFT 1. 수렴한다.

## 조치 항목

| # | 카테고리 | 조치 | 커밋 |
| --- | --- | --- | --- |
| W1 | Documentation — 존재하지 않는 경로로의 전방 참조 | 트래커 두 자리가 `plan/complete/member-auth-order.md` 를 인용하는데 plan 은 아직 `in-progress/` 에 있다. «이 PR 의 종료 커밋에서 `plan/complete/` 로 이동한다» 를 **괄호로 명시**해 참조를 현재 사실에 맞췄다 | 이 커밋 |
| SPEC-DRIFT 1 | `1-auth.md:551` · `3-error-handling.md:46`·`:49` | **조치 불요 — 이미 이관됨.** 리뷰어도 *"이미 planner 백로그로 정확히 이관됨"* 이라 확인했다. `spec/` 은 developer 권한 밖이고 자기-반증형 소정정 조건 1·2 둘 다 불충족 | (1라운드 `3fcc19e2c`) |

### W1 은 같은 형태를 세 번째 밟은 것이다

`#1377` 이 «해소 각주를 plan 이동 **전에** 써서 존재하지 않는 경로를 가리켰다» 로 지적받았고,
`#1384` 에서도 CHANGELOG 를 사실보다 먼저 썼다. 이번엔 트래커다. 공통 원인은 **«곧 참이 될
것»을 지금 참인 것처럼 쓰는 것**이고, 이번 처방은 문장 안에 **시제를 명시**하는 것이다 —
경로를 지금 사실(`in-progress`)로 바꾸면 이동 뒤 다시 틀리므로, 「어디로 갈 것인지」를 적는 편이
두 시점 모두에서 참이다.

## TEST 결과

| 단계 | 결과 |
| --- | --- |
| lint | PASS — 직전 라운드 산출(`_test_logs/lint-20260924-112419.log`). 이번 조치는 `plan/**` 뿐이라 코드가 바뀌지 않았다 |
| unit | PASS — backend **472 스위트 / 9946 테스트** (`_test_logs/unit-20260924-112514.log`) |
| build | PASS — 타입체크 ratchet 포함 (`_test_logs/build-20260924-112634.log`) |
| e2e | **통과** — 380 PASS (`_test_logs/e2e-20260924-112929.log`) |

> 재실행하지 않은 근거: 이번 라운드의 조치는 `plan/in-progress/**` 두 파일뿐이고
> `codebase/**` 수정이 **0건**이다. 게이트 freshness 기준도 `codebase/**` 다 —
> 단정하지 않고 `review_guard.evaluate_review()` 로 확인한다.

## 보류·후속 항목

**INFO #5·#6(판정 순서 커버리지 비대칭 두 칸)** 을 트래커에 등재했다. 두 라운드 연속
나온 지적이라 잃지 않게 올렸고, 수렴 예외 (a)~(d)를 항목 본문에 인용했다:

- **(a) 동작 결함이 아니다** — 둘 다 커버리지다. 현재 구현은 의도대로 동작한다.
- **(b) fix 가 새 라운드를 강제한다** — `codebase/**` 변경이라 freshness 가 재무장된다.
  이 PR 은 이미 2라운드이고, «그 라운드의 `codebase/**` 수정 0건» 이 **결과를 보기 전에
  선언한** 정지 조건이었다.
- **(c) 근거는 비용이 아니라 수렴** — 둘 다 «`wireFindOne` 조합 하나 추가» 라 다음 근접
  편집에서 싸게 닫힌다.
- **(d) 그 턴에 등재** — 재현 조합(`wireFindOne(null, { role: 'editor' })`)까지 적었다.

나머지 INFO 14건은 «조치 불요» 이거나 이미 별 항목으로 추적 중이다(13-라우트 축 등).

### 수렴 판정

정지 규칙은 **결과를 보기 전에** 선언했다 — «Critical 0 · Warning 0 · 그 라운드의
`codebase/**` 수정 0건». 2라운드는 Warning 1 이지만 그것이 요구한 수정은 `plan/**` 뿐이라
**`codebase/**` 수정 0건**이다. 다음 라운드가 볼 새 코드가 없으므로 여기서 수렴한다.
라운드 추이: **Critical 1 / Warning 5 → Critical 0 / Warning 1**.
