# RESOLUTION — `review/code/2026/09/24/20_19_10` (2라운드)

**Critical 0 · Warning 3 · INFO 9.** reviewer 14명 전원, forced 8/8.
**실질 코드(`isPendingPlanPath` + 테스트 3파일)는 14명 전원 NONE.** Warning 셋은 전부 문서의
수치 정확성이다.

## 조치 항목

| SUMMARY # | 지적 | 조치 | commit |
| --- | --- | --- | --- |
| Warning 1 | `#1387` CHANGELOG 백필이 이 PR 커밋에 동봉 — 별도 커밋이었으면 더 명확 | **이미 커밋된 것을 되돌리거나 쪼개지 않는다**(`--amend`·이력 재작성 금지). reviewer 스스로 «저장소 관행과 RESOLUTION 투명 기록으로 정당화, 머지를 막을 사안 아님» 이라 적었다. 다음부터 백필은 별도 커밋으로 낸다 | — |
| Warning 2 | CHANGELOG 가 판별 부담을 «단위 테스트 15개» 로 서술 — 실제 `isPendingPlanPath` 전담은 8개 | **실측 후 정정**: `isApplicable` 7 · **전담 8** · 파일 전체 15. «전담 8개(나머지 7개는 기존 `isApplicable`)» 로 | `3d55df09d` |
| Warning 3 | plan 의 «단위 13 · 가드 55» 가 stale — 리뷰 1라운드가 단언 2개를 더한 뒤 갱신 안 됨 | 두 자리 모두 **측정 시점을 함께** 적었다: 첫 구현 뒤 13 → 1라운드 뒤 **15(전담 8)**. 숫자만 바꾸면 이전 기록이 틀렸던 것처럼 읽힌다 — 그때는 맞았다 | `3d55df09d` |

**INFO 6**(M5 «단위 5 RED» 가 재현하면 7) 도 같은 형태라 함께 고쳤다. **다시 쟀다** — 지금은
**7 RED**(8개 중 «accepts» 만 통과). «5» 는 전담 단언이 6개였을 때 잰 값이라 당시엔 맞았다.
표에 두 값을 시점과 함께 남겼다.

> 이 라운드의 세 지적은 전부 **내가 수치를 적은 뒤 그 수치의 기반이 바뀌었는데 다시 안 잰**
> 형태다. 1라운드에서 단언을 2개 더하면서 그 수를 인용한 자리 넷(CHANGELOG 1 · plan 3)을
> 훑지 않았다.

## 종결 판정 — 3라운드를 돌리지 않는다

선언한 정지 규칙은 «Critical 0 · Warning 0 · 그 라운드 `codebase/**` 수정 0건» 이다. Warning 3
이라 글자 그대로는 미충족이다. 멈추는 근거:

- 이 라운드 자체의 `codebase/**` 수정은 **0건**이고, 세 fix 는 **`CHANGELOG.md` 와 plan 뿐**이다.
  리뷰 게이트의 freshness 는 `codebase/**` 기준이라 이 세션을 stale 로 만들지 않는다.
- fix 의 정합은 리뷰어 없이 **직접 재서** 검증했다 — 테스트 수는 describe 블록별로 세었고,
  M5 는 뮤턴트를 다시 넣어 7 RED 를 확인했다.
- 발견의 성격이 **1라운드(인용 절 오류 · CHANGELOG 누락) → 2라운드(수치 정확성)** 로 좁혀졌다.
  코드는 두 라운드 연속 전원 NONE 이다.

규칙의 목적(«fix 가 코드를 건드리는 동안 멈추지 않는다»)은 이미 성립했다. 글자와 목적이 갈린
자리라 조용히 넘기지 않고 여기 적는다.

## INFO — 조치하지 않은 것

- **INFO 1** (백슬래시 세그먼트가 `..` 로 인식 안 되는 이론적 틈) — 신뢰 경계가 PR 리뷰를 거쳐
  병합된 spec frontmatter 이고, reviewer 도 «실제 악용 가능성 사실상 없음» 이라 했다. 막으려면
  `codebase/**` 변경이라 라운드가 늘어난다.
- **INFO 2 · 4** (디렉터리 지식·접두 매칭 중복) — 1라운드의 유예 결정 승계(세 번째 소비처 등장 시).
- **INFO 3** (`unknown` 시그니처의 이유가 암묵적) — 함수 첫 줄 주석이 «YAML 이 `- 42` 를 숫자로
  파싱한다» 를 이미 적고 있다.
- **INFO 5 · 9** (리뷰 도중 `isPendingPlanPath` 가 일시적으로 `return true;` 로 관측됨 · 1회 비재현
  flake) — **내가 만든 상태가 아니다.** 병렬 fan-out 중 다른 reviewer 의 판별 뮤테이션 잔여물로
  추정되고, 관측한 둘 다 그 상태로 판단하지 않았다고 밝혔다. 리뷰 시작 전과 지금의 워킹트리는
  깨끗하다.
- **INFO 7 · 8** — 1라운드 조치의 유효성 재확인.

## TEST 결과

이 라운드의 수정은 `CHANGELOG.md` 와 plan 뿐이다. 코드 불변이므로 1라운드 RESOLUTION 의 측정이
그대로 유효하다: lint PASS · unit PASS(frontend 291/6716) · build PASS · e2e 380 PASS.

## 보류·후속 항목

없다.
