# RESOLUTION — `review/code/2026/09/24/19_57_00` (1라운드)

**Critical 0 · Warning 2 · INFO 8.** reviewer 14명 전원 산출물 확보, forced 8/8.

Warning 둘 다 **내 것이고 둘 다 조치했다. 보류 0건.** 둘째는 내 **이전 PR 의 거짓 전제**까지
드러냈다.

## 조치 항목

| SUMMARY # | 지적 | 조치 | commit |
| --- | --- | --- | --- |
| Warning 1 | SoT 를 `spec-impl-evidence.md §3`(«pending_plans row»)으로 인용했는데 경로 제약은 **§2.1 필드 정의 표**에 있다. §3 은 `status` 라이프사이클이다 | **전수로 다시 세어 5곳**(코드 3 · plan 2)을 `§2.1` 로 정정. 리뷰어가 센 수와 같다. 원인: 「§3: implemented ⇒ pending_plans 없음」(상태 규칙)과 경로 정의를 뭉갰다 | `d644263cd` |
| Warning 2 | 실제 사고를 고치는 가드 강화인데 `CHANGELOG.md` 항목이 없다. 이 저장소는 가드 신설·강화를 기록해 온 선례가 다수 있다 | **이 PR 항목 추가** + **`#1387` 백필** + **트래커 항목 정정** — 아래 §W2 | `d644263cd` |

### W2 — 누락이 아니라 **거짓 전제**였다

`#1387`(jest ESM) 에서 CHANGELOG 를 «해당 없음» 으로 판정했고 근거로 **«143개 항목이 전부
제품 동작 변경»** 을 들었다. 확인하니 **틀렸다** — 헤딩에 «가드» 가 든 항목만 **11개**이고,
그중엔 순수 테스트 가드 변경이 있다(«raw UPDATE/DELETE … RETURNING 회귀 가드를 큐레이션에서
발견형으로 확장», «주간 가드가 사흘 전에 이미 빨간불이었다»). 헤딩을 훑고 뭉갰다.

그래서 셋을 했다:

1. **이 PR 의 항목** — 선례의 형태(무엇이 뚫려 있었고 무엇을 새로 강제하는가)를 따랐다.
2. **`#1387` 백필** — 그 PR 은 repo-guard(`esm-native-load.spec.ts`)를 세웠으니 관행상
   항목감이다. 거짓 전제로 건너뛴 것이라 **«왜 백필인가»** 를 항목 안에 적었다(백필 선례:
   `#1373`).
3. **트래커 항목을 다시 썼다** — 원래 이름은 «CHANGELOG 「해당 없음」 판정에 성문 근거가
   없다» 였고 거짓 전제를 그대로 싣고 있었다. 틀렸던 경위를 인용문으로 남기고, **실제
   관행**(가드 신설·강화 = 항목)과 **남은 진짜 질문**(가드도 제품 동작도 아닌 순수 도구
   변경)으로 바꿨다. `#1388`(`@nestjs/typeorm` 12)은 판정한 것이 아니라 **고려하지 않았음**도
   적었다.

## INFO — 조치한 것 (W1 이 어차피 `codebase/**` 를 건드려 라운드가 늘므로 함께)

| # | 지적 | 조치 | 판별 확인 |
| --- | --- | --- | --- |
| INFO 3 | 비-string YAML 값(`- 42`)에서 `path.posix.normalize` 가 `TypeError` — 어느 항목이 틀렸는지 숨긴다 | 시그니처를 `unknown` 으로, 비-string 은 «plan 아님» 으로 보고. 단언 추가 | **M7** typeof 가드 제거 → 그 단언만 RED |
| INFO 4 | look-alike 디렉터리(`plan/in-progress-archive/`) 캐너리 없음 — 정확성이 트레일링 슬래시에만 기댄다 | 캐너리 단언 추가 | **M6** 트레일링 슬래시 제거 → 그 단언만 RED |
| INFO 8 | 가드의 두 단언 역할 분담 설명 없음 | 주석 한 줄 — «실존이 plan 여부를 대신하면 안 된다» | (주석) |

## INFO — 조치하지 않은 것과 그 이유

- **INFO 1 · 5** (plan 디렉터리 지식이 `PENDING_PLAN_DIRS` 와 가드의 `/in-progress/`→`/complete/`
  치환에 중복 · 접두 매칭 패턴이 `isApplicable` 과 중복) — reviewer 스스로 «세 번째가 생기면»
  으로 조건을 걸었다. 지금 합치면 가드의 치환 로직(설계상 의도된 complete/ 허용)까지 건드려
  이 PR 의 스코프를 넘는다.
- **INFO 2** (같은 클래스의 갭이 `spec-status-lifecycle.test.ts` guard (c) 에 남았다) —
  **등재하지 않는다.** guard (c) 는 `pending_plans` 를 입력으로 받는데, 이제 이 가드가
  비-plan 항목에 **RED** 를 내므로 **스위트가 초록이면 guard (c) 가 보는 입력은 plan 뿐**이다.
  두 가드는 같은 스위트에서 함께 돈다. 독립 방어가 필요해지면 그때 `isPendingPlanPath` 를
  재사용하면 된다.
- **INFO 6** (무관한 트래커 항목 동봉) — impl-prep Warning 의 등재 의무를 그 턴에 이행한 것.
- **INFO 7** (보안 — 오히려 개선) — 조치 불요.

## TEST 결과

| 단계 | 결과 | 로그 |
| --- | --- | --- |
| lint | **PASS** | `_test_logs/lint-20260924-200829.log` |
| unit | **PASS** — frontend 291 파일 / 6716(직전 6714 — 새 단언 2개만큼) | `_test_logs/unit-20260924-200924.log` |
| build | **PASS** | `_test_logs/build-20260924-201052.log` |
| e2e | **통과** — 380 PASS | `_test_logs/e2e-20260924-201302.log` |

## 보류·후속 항목

Warning 보류 0건. 트래커 항목 하나를 **정정**했다(«CHANGELOG 에 무엇이 들어가는지 성문 기준이
없다» — 위 §W2).
