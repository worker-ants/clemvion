# RESOLUTION — `review/code/2026/09/24/08_46_47` (2라운드)

Critical 0 · Warning 2. 하나는 조치, 하나는 이 PR 스코프 밖(사유 아래).

## 조치 항목

| # | 카테고리 | 조치 | 커밋 |
| --- | --- | --- | --- |
| 1 | Testing — 단위 테스트 2블록이 완전 중복 | 중복 블록(«DELETE 시점에 행이 사라졌으면 404 다») **제거**. 남긴 «진 쪽은 404» 의 JSDoc 에 그것이 맡는 역할 둘(0-행의 나머지 이유 · «존재하든 말든 403» 편집을 죽임)과 **중복이 생긴 경위**를 적었다 | 이 커밋 |
| 2 | Security — 권한 검사 순서 오라클 | **무조치.** 이 PR 이 만들지도 고치지도 않은 기존 결함이고, 리뷰어 자신이 *"이미 정확한 블라스트 반경(비-멤버 포함 임의 인증 사용자)으로 트래커에 등재돼 있어 이 PR 을 막을 사유는 아니다"* 라고 적었다. 1라운드에서 그 반경을 실측 확인했고 좁게 적혀 있던 내 plan §F 를 정정했다 | — |

### #1 은 내가 1라운드 fix 에서 만든 것이다

1라운드 W3 을 고치며 «진 쪽은 404» 의 재조회를 `editor` → `null` 로 바꿨는데, **같은 라운드에서
«행이 사라졌으면 404» 블록을 따로 추가해 둔 상태**였다. 그 둘이 mock·단언까지 동일해졌다.
정정 하나가 곧바로 다음 중복을 만든 형태라, 남긴 블록의 JSDoc 에 경위를 적어 둔다.

**중복 제거로 커버리지가 줄지 않았음을 뮤턴트로 확인했다:**

| 뮤턴트 | 예측 | 실측 | 죽은 테스트 |
| --- | --- | --- | --- |
| `if (still)` → `if (true)` («존재하든 말든 403») | 1 | **1** | «진 쪽은 404 이고 감사를 남기지 않는다» |

지운 블록이 맡던 유일한 관측을 남은 블록이 그대로 낸다 — 그래서 지운 것이 맞다.

## TEST 결과

| 단계 | 결과 |
| --- | --- |
| lint | PASS (`_test_logs/lint-20260924-085941.log`) |
| unit | PASS — backend **472 스위트 / 9943 테스트** (`_test_logs/unit-20260924-090051.log`, 중복 1건 제거로 9944 → 9943) |
| build | PASS — 타입체크 ratchet 포함 (`_test_logs/build-20260924-090207.log`) |
| e2e | **통과** — 379 PASS (`_test_logs/e2e-20260924-090449.log`) |

## 보류·후속 항목

없다. #2 는 유예가 아니라 **스코프 밖**이다 — 이미 등재된 별 항목이고 계약이 다르다
(이 PR: «owner 를 지우지 않는다» / 그 항목: «대상의 존재·role 을 흘리지 않는다»).
INFO 10건은 전부 «조치 불요» 또는 이미 트래커 등재로 처분됐다.
