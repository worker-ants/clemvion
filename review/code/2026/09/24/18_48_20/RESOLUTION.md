# RESOLUTION — `review/code/2026/09/24/18_48_20` (2라운드)

**Critical 0 · Warning 1 · INFO 13.** reviewer 14명 전원 산출물 확보.
1라운드 Warning 3건의 해소를 전 reviewer 가 독립 재현으로 확인했다.

## 조치 항목

| SUMMARY # | 지적 | 조치 | commit |
| --- | --- | --- | --- |
| Warning 1 | `deps-typeorm12.md` 가 «`1-auth.md` 캐럿 정정은 트래커 §3 재개 조건과 함께 처리한다» 고 적었는데 **트래커에 그 항목이 없다**(0회). 이 plan 은 `complete/` 로 가므로 정정이 유실된다 | 트래커 `nestjs-v12-coordinated-upgrade.md` **§E 종결 조건에 체크박스로 등재**. 처방까지 적었다 — 숫자를 새 값으로 바꾸기보다 빼고 «caret 범위라» 로 일반화(같은 Rationale 이 이미 «수치를 spec 에 미러링하지 말 것» 이라 경고). `deps-typeorm12.md` 의 서술도 정정하고 틀렸던 경위를 남겼다. **검증**: 트래커에서 `캐럿 인용 정정` 1건 | `ed37b9b0b` |

**이 지적의 성격** — 가리킨 대상이 존재하지 않는 문장이었다. impl-prep W3 가 같은 것을
요구했고 내가 «트래커와 함께 처리한다» 고 **적기만** 했다. 할 일은 머지 후에도 **열려
있는 문서에 체크박스로** 있어야 한다.

## 종결 판정 — 3라운드를 돌리지 않는다

결과를 보기 전 선언한 정지 규칙은 «Critical 0 · Warning 0 · 그 라운드 `codebase/**` 수정
0건» 이었다. 이 라운드는 **Warning 1** 이라 글자 그대로는 미충족이다. 그래도 멈추는 근거:

- 이 라운드 자체의 `codebase/**` 수정은 **0건**이고, Warning 의 fix 도 **plan 한 파일**이다.
- 리뷰 게이트의 freshness 는 `codebase/**` 기준이라 plan 편집은 이 세션을 stale 로 만들지
  않는다 — 그러니 3라운드는 **게이트가 요구하지 않는다**.
- fix 의 정합은 리뷰어 14명 없이 직접 검증된다 — 트래커에 항목이 **실재하는가**(grep 1건).
- 선언한 규칙의 목적은 «fix 가 코드를 건드리는 동안 루프를 멈추지 않는 것» 이다. 이 fix 는
  코드를 건드리지 않는다.

즉 규칙을 굽힌 것이 아니라 **규칙이 지키려던 것이 이미 성립했다**. 글자와 목적이 갈린 자리라
조용히 넘기지 않고 여기 적는다.

## INFO 처분

- **INFO 4** (`require(esm)` 결속에 자동 가드 없음 — 산문 경고가 유일한 방어선) —
  **이번엔 넣지 않는다.** 가드는 `codebase/**` 변경이라 새 라운드를 강제하고, reviewer
  스스로 «다음 floor 변경 PR 스코프» 로 범위를 정했다. 산문은 누군가 floor 를 고칠 때 보는
  바로 그 자리(`PROJECT.md` Node 지원 floor 줄)에 있다.
- **INFO 7** (판별자 MB 미코드화) — 1라운드에서 «다음 `@nestjs/*` 착수 PR» 로 처분, 재지적
  아닌 잔존 확인이다.
- **INFO 10** (섹션 번호·기준값 중복) — 1라운드에서 검토·수용한 트레이드오프, 이번 diff 가
  악화시키지 않았다고 reviewer 가 확인.
- 나머지 INFO 는 «없음»·해소 확인·긍정 사례.

## TEST 결과

이 라운드의 수정은 plan 한 파일이다. 코드·lockfile 불변이므로 1라운드 RESOLUTION 의
측정이 그대로 유효하다:

| 단계 | 결과 | 로그 |
| --- | --- | --- |
| lint | **PASS** | `_test_logs/lint-20260924-184526.log` |
| unit | **PASS** — backend 473 스위트 / 9950 | `_test_logs/unit-20260924-184626.log` |
| build | **PASS** — Docker 단계 포함 | `_test_logs/build-20260924-183706.log` |
| e2e | **통과** — 380 PASS | `_test_logs/e2e-20260924-181031.log` |

plan 편집 후 frontend docs 가드를 다시 돌렸다(아래 커밋 본문).

## 보류·후속 항목

Warning 보류 0건. 새 등재 없음 — 이 라운드의 fix **자체가** 누락됐던 후속을 등재한 것이다.
