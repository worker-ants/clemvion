# RESOLUTION — review/code/2026/09/26/00_27_56 (4라운드, 전수 `--route=all`)

SUMMARY: Critical 0 · Warning 4 · INFO 20. 14명 전원 결과 확보(forced 7명 누락 없음). 라운드 전 선언한 판정 규칙: «Critical 0 ·
Warning 0 · 그 라운드 `codebase/**` 수정 0건이면 종결, 2라운드부터 구조 · 문서 잔여만 남으면 수렴 예외». 이 라운드의 Warning 넷은
전부 테스트 커버리지 · 구조 · 성능이고 동작 결함이 아니다 — **수렴 예외로 종결하고 `codebase/**` 는 고치지 않았다.**

## 조치 항목

| # | 발견 | 처분 | 커밋 |
| --- | --- | --- | --- |
| W1 | `WorkspacesService.getMemberRole` 의 `manager` 분기(3라운드 CRITICAL 수정의 일부)를 실제로 타는 테스트가 없다 | **수렴 예외로 등재** — 아래 판정. 트래커 «통합 소유자 강제의 테스트 · 구조 잔여» 1 | — |
| W2 | update · reauthorize · remove 가 대상이 personal 이어도 역할을 먼저 읽는다 | **기존 트래커 항목 갱신** — «워크스페이스 역할을 가드와 핸들러가 두 번 조회한다»(1 · 3 · 4라운드 같은 지적 — 지연 조회 처방을 더했다). 리뷰어도 «API 형태 변경이 필요해 이번 PR 스코프를 넘는다 · 현재 비용(인덱스 조회 1회) 감내 가능» 으로 적었다 | (plan 커밋) |
| W3 | «`judgedRow` 조건부 update + `affected` 판정» 4줄이 세 곳에 반복 | **수렴 예외로 등재** — 같은 항목 3 | — |
| W4 | e2e 가 `beforeAll` 의 행을 마지막 케이스에서 지워 순서에 기대고, Organization rotate e2e 가 없다 | **수렴 예외로 등재** — 같은 항목 2 | — |
| INFO 4 · 13 | 컨트롤러의 타입 import 출처 · mode/action 표기 규약 주석 | 같은 항목 3 에 덧붙였다 | — |
| INFO 16 | 트래커 체크박스 · plan 이동이 아직 | 이 PR 의 마무리 커밋이 한다(리뷰 *뒤*가 정상 순서) | (마무리 커밋) |
| 그 외 INFO | 1 · 2 · 3 · 5~12 · 14 · 15 · 17~20 | 기록만 — 조치 불요 판정에 동의(INFO 3 실행 엔진은 후속 plan, INFO 9 파일 수 서술은 고지 문구의 오기) | — |

## 수렴 판정 — developer SKILL §ISSUE FIX 정책 «수렴 예외» 적용

W1 · W3 · W4 는 `plan/in-progress/spec-draft-nullable-notation-followups.md` 에 **이 턴에** 등재하고 이 PR 에서는 고치지 않는다. 네 조건:

- **(a) 동작 결함이 아니다** — W1 은 커버리지 갭이다. 역할 재조회 자체는 unit 이 지킨다(뮤턴트 R13 — 락 안 재조회를 끄면 강등 케이스가
  RED). `manager` 분기가 깨져도 역할은 여전히 다시 읽히고, 잃는 것은 «락 보유 중 두 번째 커넥션 회피» 라는 자원 최적화다. W3 은
  4줄 반복(구조), W4 는 e2e 의 fixture 공유 · 한 경로 부재(테스트 구조 · 커버리지)다. 재현되는 오동작이 없다. 발견의 성격도 라운드를
  따라 동작(1라운드 동시성 · 2라운드 사용자 노출 문구 · 3라운드 역할 TOCTOU) → 테스트 · 구조(4라운드)로 옮겼다.
- **(b) 고치면 새 라운드를 강제한다** — 셋 다 `codebase/**` 편집이라 리뷰 게이트 freshness 가 다시 무장된다. 이 PR 은 네 라운드 동안
  매 라운드가 직전 수정의 인접 표면에서 같은 크기의 지적을 새로 냈다(3라운드 `rotate` 수정 → 4라운드 그 수정의 테스트 갭).
- **(c) 이 조항을 여기 인용한다** — 등재 사유는 비용이 아니라 수렴이다.
- **(d) 등재는 이 턴에 했다** — 트래커 «통합 소유자 강제의 테스트 · 구조 잔여 — 트랜잭션 매니저 분기 · Organization rotate e2e ·
  조건부 쓰기 헬퍼»(신설), «워크스페이스 역할을 가드와 핸들러가 두 번 조회한다»(갱신).

## TEST 결과

이 라운드는 `codebase/**` 를 고치지 않았다. 마지막 codebase 편집(`a8b5c8b13`) 뒤 돌린 결과가 유효하다
(3라운드 `RESOLUTION.md` 와 같은 런):

- lint: 통과 (`_test_logs/lint-20260926-001803.log`)
- unit: 통과 — backend 10238 · frontend 6781 (`_test_logs/unit-20260926-001856.log`)
- build: 통과 (타입체크 ratchet 포함, `_test_logs/build-20260926-002013.log`)
- e2e: 통과 — 72 스위트 · 405 passed (`_test_logs/e2e-20260926-002255.log`)

## 보류·후속 항목

- W1 · W3 · W4 · INFO 4 · 13 — 트래커 «통합 소유자 강제의 테스트 · 구조 잔여»(수렴 예외).
- W2 — 트래커 «워크스페이스 역할을 가드와 핸들러가 두 번 조회한다»(갱신).
