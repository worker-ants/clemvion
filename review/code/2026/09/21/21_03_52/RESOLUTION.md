# RESOLUTION — 21_03_52 (라운드 3, 수렴)

**Critical 0 · Warning 0.** 착수 전 선언한 정지 규칙(«Critical·Warning 0 인 라운드, 또는
`codebase/**` 수정 0 인 라운드»)의 **첫째 절**로 수렴했다.

Warning 추이: **1 → 1 → 0**. 두 Warning 모두 «내가 방금 넣은 것» 에 관한 것이었다 —
라운드 1 은 헬퍼를 만들어 놓고 `PROJECT.md` 에 안 넣은 것, 라운드 2 는 그 fix 로 넣은 검사의
주장이 실제 범위보다 넓은 것.

## 조치 항목

| SUMMARY # | 분류 | 조치 | 비고 |
|-----------|------|------|------|
| INFO 4 (`PROJECT.md` 볼드 스타일이 같은 목록의 다른 항목과 어긋남) | 문서 | 아래 마무리 커밋 | 레이블만 굵게 하는 목록 관례에 맞췄다. **`PROJECT.md` 는 `codebase/**` 밖이라 리뷰 freshness 를 재무장시키지 않는다** — 그래서 수렴 라운드에서도 무료로 고칠 수 있었다. |
| INFO 2 (수동 레지스트리 · 모듈 로드 결합) | 유예 | 무수정 | 라운드 2 에서 **명시적으로 기각한 트레이드오프**이고 리뷰어도 그렇게 기록했다. 테스트가 프로덕션 상수를 임포트하는 것은 drift 를 막는 정상 수단이고, 상수가 옮겨지면 **컴파일 에러로 즉시 시끄럽게** 깨진다(조용한 실패가 아니다). 라운드 2 에서 바로잡은 것은 결합이 아니라 **문구의 범위**였다. |
| INFO 1·5·8·9·10 | 유예 | 무수정 | `setTimeout` 미정리(리팩터 **전** 9파일에 이미 있던 패턴, 회귀 아님) · 표시 문자열 수동 동기화 · `@throws` 의 `BEGIN`/`lock.sql` 구분 · `locker !== db` 런타임 미강제 · spec 문서 부재(테스트 하네스라 spec 이 관여할 층이 아니다). 전부 비차단이고 문서화된 한계다. |
| INFO 3 (diff 대부분이 리뷰 산출물) | 확인 | — | 게이트가 강제하는 정상 부산물. |
| INFO 7 (N≥3 · `lock.params` 생략 미검증) | 유예 | 무수정 | 라운드 1·2 와 같은 YAGNI 판단. 호출부가 생기는 자리에서 검증된다. |

## 유예 중 하나만 트래커로 올린다 — INFO 6

**«주석 대신 코드로 고정» 한 방어 자체가 미검증이다.** 라운드 2 에서 `KNOWN_LOCK_TIMEOUTS_MS`
순회 검사를 넣었는데, 그 분기도 `fires.length < 2` 가드도 **어떤 테스트도 지나가지 않는다**.
둘 다 실제 Postgres 타이밍과 무관한 **순수 동기 분기**라 DB 없는 Jest unit 으로 행사할 수 있다.

이번 PR 에서 하지 않는 이유는 `developer` SKILL §수렴 예외 (a)(b) 다 — (a) 재현되는 동작
결함이 아니라 방어 코드의 커버리지 문제이고, (b) 고치면 `codebase/**` 가 다시 바뀌어 라운드가
한 번 더 도는데 이 라운드가 **Warning 0 으로 수렴한 직후**다.

**다만 «다음 근접 편집» 에 맡기지 않는다** — 이 저장소에서 그 편집이 오지 않는 것을 이미 겪었다.
트래커 항목으로 올린다(§보류 참고).

## TEST 결과

- lint  : 통과 (`_test_logs/lint-20260921-205435.log`)
- unit  : 통과 (`_test_logs/unit-20260921-205525.log`)
- build : 통과 (`_test_logs/build-20260921-205635.log`, 백엔드 타입체크 ratchet 포함)
- e2e   : 통과 (378/378, `_test_logs/e2e-20260921-205918.log`)

## 이 PR 의 핵심 증거 — 음성 대조군

테스트 리팩터에서 **«전부 GREEN» 은 증거가 아니다.** 헬퍼가 락을 안 잡아도 아홉 파일이 전부
통과할 수 있다. 그래서 헬퍼의 락 쿼리를 `void lock` 으로 바꾼 뮤턴트를 넣었고,
**예측 11 RED / 실측 11 RED**(9 스위트 전부)였다. 실패 사유까지 확인했다 —
`expect(raced).toBe('pending')` 에서 `Received: "settled"`, 즉 **정확히 공허성 가드**다.

## 보류·후속 항목

- **INFO 6** — `raceUnderHeldLock` 의 순수 동기 분기 둘(`fires.length < 2` · 잠금 상한 검사)을
  DB 불필요 `concurrency.spec.ts` unit 으로 행사한다. 트래커 등재.
- INFO 1·5·8·9 — 위 표의 근거로 문서화된 한계로 남긴다.
