# RESOLUTION — `review/code/2026/09/05/10_20_57`

전체 위험도 **LOW** · Critical **0** · WARNING **1**. **조치 완료.** INFO 중 2건도 함께 닫았다.

## 조치 항목

| # | 카테고리 | 지적 | 조치 |
|---|---|---|---|
| 1 | requirement | README §5 의 *"정상 흐름에서는 발생하지 않는다"* 가 **실제 실패 표면보다 좁다** — CREATE 성공 후 `DROP(old)` 이 실패하면 마이그레이션 전체가 실패로 기록되고, **이 문서가 지시하는 `repair` + 재실행** 절차에서 재빌드가 난다 | 그 문단을 **두 경로 표**로 바꿨다. 둘째 경로가 정상 절차 안에 있다는 것을 명시하고, 그럼에도 이 순서를 택하는 이유(재빌드는 스스로 회복하지만 인덱스 0개는 안 낫는다)를 남겼다 |

### 전제를 직접 확인했다

`repair` + 재실행이 정말 이 문서의 절차인지 읽어 봤다 — `README.md:195`(*"마이그 적용 전
**`flyway repair`** 가 필요하다"*), `:201`(`docker compose up migrate-repair`),
`:211`(`clemvion/migrate repair -url=...`). **지적이 맞다.**

### 같은 문구가 남은 자리를 전수로 찾았다

이 세션에서 반복된 실패가 *"한 곳만 고치고 미러를 놓친다"* 라 grep 으로 전수 확인했다 —
**3곳**이었다:

| 위치 | 처분 |
|---|---|
| `codebase/backend/migrations/README.md` | **정정** (본 조치) |
| `plan/complete/spec-draft-migration-rerun-and-citations.md:97` | **정정** |
| `codebase/backend/migrations/V110__…sql:57` | **손대지 않음 — append-only.** 대신 후속 등재 |

**V110 을 왜 안 고쳤나**: 이미 머지된 마이그레이션 파일이고, 이 저장소의 append-only 원칙은
그것을 수정 대상으로 두지 않는다. 다만 그 파일이 규약의 **선례로 지목**되고 있어 읽는 사람이
헤더를 규약으로 오인할 여지가 있다 — 그래서 `plan/in-progress/spec-draft-nullable-notation-followups.md`
에 처분 선택지 (a)/(b) 와 함께 등재했다. 임의로 정하지 않았다.

## INFO 처분

| # | 지적 | 조치 |
|---|---|---|
| 1 | README 규칙 요약 불릿이 설명 문단의 `DO $$ ... $$` 예시를 반영 못 함 | **조치** — `(예: ALTER TABLE, DO $$ ... $$)` 로 동기화 |
| 2 | `migrations.md` 신규 불릿이 §5 절차 목록(1~6)과 마무리 블록쿼트 사이를 끊는다 | **조치** — 블록쿼트 뒤로 옮기고 자체 블록쿼트로 분리 |
| 3 | 정량 주장(107파일/514회)과 V056/V106/V110 서술이 저장소와 전수 일치 | 확인 보고 |
| 4 | 이전 6라운드 지적 전항목이 최종본에 반영됨을 직접 대조 확인 | 확인 보고 |
| 5 | 마이그레이션 실물 SQL 이 README §5 표와 line-level 일치 | 확인 보고 |

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint · build | **해당 없음** — 애플리케이션 코드 변경 0 |
| unit | **PASS** — frontend 스위트 **289 files / 6,328 tests** |
| e2e | **면제 (화이트리스트 부분집합)** — 변경 set 은 `.md` 와 `review/**` 아래 `.json` 뿐. 이번 조치도 그 집합을 벗어나지 않는다 |

## 보류·후속 항목

- **`V110` 헤더의 좁은 서술** — 등재 완료(위 참조). append-only 라 이 PR 에서 고치지 않는다.
