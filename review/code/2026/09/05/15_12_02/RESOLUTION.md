# RESOLUTION — `review/code/2026/09/05/15_12_02`

전체 위험도 **LOW** · Critical **0** · WARNING **0** · INFO **10**.

앞 두 라운드의 Critical/WARNING 이 실측으로 해소됐음을 9명의 reviewer 가 재확인했다.
**차단 사유 없음.** 그럼에도 INFO 2건은 닫았다 — 아래 참조.

## 조치 항목

| # | 카테고리 | 지적 | 조치 | 커밋 |
|---|---|---|---|---|
| I1 | testing | `visitUnion` 의 `allowUndeclared` 면제 분기가 어떤 테스트에도 안 걸린다 — 지워도 스펙 전부 통과 | **고쳤다.** 뮤턴트 생존을 먼저 실측(36/36 통과)한 뒤 캐너리 추가 → 같은 뮤턴트가 그 테스트 하나만 실패시킨다 | `bf02fe328` |
| I2 | maintainability | `visitUnion` 의 `_onPath` 가 본문에서 안 쓰인다 | **고쳤다.** union 아래로는 더 내려가지 않으므로 순환 가드가 필요 없다 — 파라미터를 없애고 그 이유를 docstring 에 적었다 | `bf02fe328` |

**나머지 8건은 조치 불요다.** 성격별로:

| # | 지적 | 처분 |
|---|---|---|
| I3 | `AuditLogDto.user`/`ipAddress` 가 응답 DTO 인데 tri-state 선언 | **다른 트랙.** 선언 층의 §5.4 drift 이고, §5.4 스윕 항목이 이미 트래커에 있다. 이 PR 은 값 검사기를 세웠지 선언을 정리하지 않는다 |
| I4 | `User` 엔티티 컬럼 수준 방어 부재 | **이미 등재.** 직전 라운드 W2 로 유예한 항목의 재확인이다 |
| I5 | `getSortColumn` 폴백·필터 경로 유닛 테스트 부재 | **기존 갭.** 이번 diff 가 만든 것이 아니고, 이 서비스를 다음에 만질 때로 미룬다 |
| I6 | `find → toBeDefined → assert` 3문장 반복 | **이미 등재** (직전 라운드 W6) |
| I7 | `response-contract.ts` 가 어떤 spec `code:` 에도 없다 | **이미 등재** (planner 항목) |
| I8 | `action`/`resource_type`/`user_id` 단독 필터 인덱스 부재 | **기존 상태.** 이번 diff 무관, 트래픽 프로파일 확인이 선행돼야 한다 |
| I9 | 한 브랜치가 네 갈래를 담는다 — PR 설명에 인덱스 필요 | **PR 본문에 반영한다** |
| I10 | `user` 응답 축소가 기술적으로 breaking narrowing | **의도이고 CHANGELOG 에 기록됨.** 원 노출 자체가 계약 위반이었다 |

## 왜 비차단인데 I1 을 고쳤나

이 라운드는 Critical·WARNING 0 이라 게이트를 막지 않는다. 그래도 I1 은 닫았다 —
**검증자 안에 테스트가 못 무는 분기가 있다**는 지적이고, 그것이 이 PR 이 잡으려는 결함과
정확히 같은 종류이기 때문이다. 두 라운드 연속으로 "내 테스트가 vacuous 했다" 는 지적을
받았으니(14_39_31 C1 의 순환 테스트), 같은 형태를 알고도 남기는 것은 앞뒤가 안 맞는다.

지적을 받자마자 고치지 않고 **뮤턴트로 먼저 확인**했다: 그 분기를 통째로 지워도 36개 스펙이
전부 통과했다. 지적이 맞았다.

## 라운드 수렴 판단

| 라운드 | 최상위 발견 | 성격 |
|---|---|---|
| `13_49_54` | 감사 로그 민감정보 유출 (`user` 26키) | **동작** |
| `14_39_31` | 자기참조 거짓 통과 · union 사각지대 | **동작** |
| `15_12_02` | 미검증 면제 분기 · 미사용 파라미터 | **구조** |

발견이 **동작 → 구조**로 내려갔고 Critical·WARNING 이 0 이다. 남은 INFO 8건은 전부
"다른 트랙 / 이미 등재 / 기존 갭 / 의도" 로, 이 PR 이 만든 것이 아니다. **수렴으로 본다.**

"발견 0" 을 기준으로 삼지 않는 이유는 이 저장소의 규약대로다 — 라운드마다 INFO 는 계속
나오고, 그것을 0 으로 만들려는 루프가 앞선 PR 에서 8라운드를 태운 적이 있다.

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint | **PASS** (`15:21:22`) |
| unit | **PASS** — 446 스위트 / 9,379 통과 (`15:22:18`). 스펙 36→37 |
| build | **PASS** (`15:23:42`) |
| e2e | **PASS** — 51 스위트 / 295 통과 (`15:26:12`) |

**e2e 면제 아님** — 코드 변경이므로 수행했다.

## 보류·후속 항목

이 라운드가 새로 만든 후속은 없다. 열려 있는 항목은 전부
`plan/in-progress/spec-draft-nullable-notation-followups.md` 에 있고, **미체크 체크박스
7건이 단일 진실**이다.
