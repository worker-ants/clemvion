# RESOLUTION — `00_06_38`

전체 위험도 **LOW** · Critical **0** · WARNING **2**. **둘 다 조치 완료.**
두 건 모두 **`spec/`·`plan/` 문서 조치**이고 `codebase/**` 는 건드리지 않았다.

## 조치 항목

| # | 카테고리 | 지적 | 조치 |
|---|---|---|---|
| 1 | requirement | `spec/1-data-model.md` 의 새 Rationale 이 출처를 `#1284` 로 인용했는데 그 항목은 다른 PR 에서 등재됐다 | **사실 오류 — 정정.** `git log -S` 로 추적하니 등재는 **`#1277`**(`cce8a188b`), 전제 교체는 **`#1278`**(`e55b3a74a`) 이었다. 인용을 그대로 바꾸고 "그 트래커의 마지막 열린 developer 항목" 이라는 맥락을 덧붙였다 |
| 2 | documentation | 리뷰 세션 ID 인용이 코드·테스트 주석 3파일 6곳으로 확산 — "영구 코드 주석에 일시적 프로세스 식별자" | **실측 후 등재.** 이 PR 의 일탈이 아니라 저장소의 오래된 관례임이 드러났다(아래). 규약 결정 항목으로 plan 에 등재했다 |

### W1 — 내 인용이 틀렸다

원문은 *"`#1284` 후속으로 남아 있던 developer 항목"* 이었다. 문자 그대로는 "#1284 가 머지된
뒤에도 남아 있던" 이라 읽을 여지가 있지만, Rationale 의 `출처:` 줄은 **어디서 왔는지**를
적는 자리다. 그 자리에 후속 관계를 적어 출처처럼 보이게 한 것이 잘못이다.

측정으로 확정했다:

```
git log --oneline -S "idx_schedule_next_run" -- plan/in-progress/spec-draft-nullable-notation-followups.md
  e55b3a74a … (#1278)   ← 전제 교체
  cce8a188b … (#1277)   ← 최초 등재
```

### W2 — 지적의 전제가 저장소 실태와 달랐다

리뷰는 이 인용 방식을 이 PR 이 들여온 것처럼 다뤘다. **실측은 반대였다**:

| | 값 |
|---|---|
| `origin/main` 의 `codebase/` 안 `hh_mm_ss` 인용 | **104개 파일 · 508회** |
| 가장 오래된 예 | `roles.guard.spec.ts` → `review/code/2026/08/08/20_53_48` |

즉 이 PR 이 따른 것은 **저장소가 이미 택한 방식**이다. 한 파일만 다르게 쓰면 일관성이
깨지므로 단독으로 바꾸지 않고, (a) 관례 성문화 / (b) 앞으로 PR 번호·SHA 로 전환 중
어느 쪽인지 정하는 **규약 항목**으로 등재했다.

> **처음 셀 때 "0건" 이라는 거짓 0 을 냈다.** `-E "\b[0-9]{2}_…"` 패턴이 안 물었는데
> 하마터면 그것을 "선례 없음" 으로 읽고 **정반대 결론**을 낼 뻔했다. 존재가 확실한
> 문자열(`20_16_17`)로 명령 자체를 먼저 검증해서 잡았다 — 0 은 언제나 "없다" 와
> "못 찾았다" 두 가지다.

## INFO 처분

전부 **조치 불요** 또는 **기존 처분 유지**다. 새로 판단이 바뀐 것은 없다.

| # | 처분 |
|---|---|
| 1 (DROP-first 비대칭) | 문서화된 트레이드오프 + 후속 등재 완료 — 유지 |
| 2~6 (인젝션 없음 · 더미 비밀번호 · 인가 경계 · line-level 일치 · TODO 없음) | 확인 보고 |
| 7 (`(trigger_id)` 드라이브바이) · 11 (asc/desc 체인 반복) · 16 (규약화 분리 등재) | 기존 처분 유지 |
| 8 (세션 ID 인용) | WARNING #2 로 처리 |
| 9 (e2e cleanup 부재) | 파일 전체 관례와 동일 — 유지 |
| 10 (`J.` 가 73줄로 최장, 정렬·격리 두 관심사) | **미조치.** 두 관심사가 **같은 인덱스의 두 컬럼**(선두 `workspace_id` = 격리, 후행 `next_run_at` = 정렬)에 대응해서, 한 요청 흐름 안에서 보는 것이 이 테스트의 의미다. 리뷰도 "강제 아님" |
| 12 (신규 unit 을 뮤테이션으로 검증) | 리뷰가 직접 `next_run_at`→`last_run_at` 뮤턴트로 RED 를 확인해 줬다 — vacuous 아님이 외부 검증됨 |
| 13 (`last_run_at` 축 무테스트) · 14 (CONCURRENTLY 복구 자동화 없음) | 이 PR 이 만든 갭이 아니다. 13 은 그 함수를 손댈 때, 14 는 비용 대비 실익이 낮아 defer |
| 15 (쓰기 비용 벤치마크) | 기존 처분 유지 — 합성 데이터 수치를 런북 기준선으로 쓰지 않는다 |

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint | **PASS** — 직전 라운드 값 유효 (`_test_logs/lint-20260904-235659.log`) |
| unit | **PASS** — 직전 라운드 값 유효 (`_test_logs/unit-20260904-235759.log`) |
| build | **PASS** — 직전 라운드 값 유효 (`_test_logs/build-20260904-235935.log`) |
| e2e | **통과** — 51 suites / 295 passed, 직전 라운드 값 유효 (`_test_logs/e2e-20260905-000210.log`) |

> **재수행하지 않은 근거**: 이 라운드의 두 조치는 `spec/1-data-model.md` 한 줄과
> `plan/in-progress/**` 한 항목뿐이고 **`codebase/**` 를 전혀 건드리지 않았다.**
> `git status` 로 변경 집합을 확인했고, 게이트 정본(`review_guard.evaluate_review`)에도
> 물어 코드 리뷰 freshness 가 유지되는지 확인했다. 테스트가 관찰할 대상이 바뀌지 않았으므로
> 같은 실행 결과를 인용한다 — 새로 돌려도 같은 값이 나온다는 뜻이 아니라, **돌릴 이유가
> 없다**는 뜻이다.

## 보류·후속 항목

- **`CREATE INDEX CONCURRENTLY` 재실행 규약화** — 등재 유지.
- **코드 주석의 리뷰 세션 ID 인용 규약** — 이번에 신규 등재. 실측(104파일·508회)을 함께 담아
  다음 사람이 (a) 성문화 / (b) 전환 중 고를 수 있게 했다.
