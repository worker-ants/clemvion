# RESOLUTION — `review/code/2026/09/05/10_39_00`

전체 위험도 **LOW** · Critical **0** · WARNING **1**. **조치 완료.** INFO#1 도 함께 닫았다.

## 조치 항목

| # | 카테고리 | 지적 | 조치 |
|---|---|---|---|
| 1 | requirement | `review-citations.md` §3 의 `spec/**` 행이 *"현재 위반 사례 0건"* 이라 단언하는데 **실측하면 이미 bare 인용이 많다** | 실측치로 정정 + §4 grandfather 목록에 `spec/**` 포함 + Rationale 에 왜 틀렸는지 기록 |

### 재지 않고 썼다

이 문서의 다른 범위는 전부 실측해 놓고 **`spec/**` 행만 짐작으로 채웠다.** 리뷰어 수치를
그대로 받지 않고 직접 셌더니 **더 많았다**:

| | 리뷰어 | 내 실측 |
|---|---|---|
| bare 인용이 있는 파일 | 12개 | **18개** |
| bare 인용 건수 | 22곳 이상 | **36건** (총 45건 중) |

`spec/conventions/review-citations.md` 자신도 그 목록에 든다 — §2 표가 **금지 형태를 예시로
인용**하기 때문이라 정당하다. 그 점을 고려해도 나머지가 충분히 많다.

Rationale 에 항목을 남겼다: *"범위를 넓히는 편집은 그 범위를 재는 일까지 포함한다."*
`scripts/**`·`.github/**` 를 넣을 때는 8건 중 6건이 bare 라는 것을 재고 넣었으면서,
바로 다음 라운드에 추가한 `spec/**` 은 재지 않았다.

## INFO 처분

| # | 지적 | 조치 |
|---|---|---|
| 1 | `migrations.md` §3 이 `migrate-repair` 절차를 README **§5** 로 가리키는데 실제는 **§6** — pre-existing, diff 밖이라 강제 아님 | **조치했다.** 직접 확인하니 절차는 `README.md:210`(§6) 안이고 §5 에는 없다. 이 PR 이 §5 에 `migrate-repair` 를 새로 언급하면서 **같은 절차를 가리키는 두 인용이 엇갈린 채 병존**하게 됐다 — 내가 만든 혼동이라 이미 편집 중인 파일에서 한 단어로 닫았다 |
| 2 | `plan/**` 안에 두 형태가 혼재 — §3 이 `plan/**` 을 대상 밖으로 명시했으므로 위반 아님 | 조치 불요 |

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint · build | **해당 없음** — 애플리케이션 코드 변경 0 |
| unit | **PASS** — frontend 스위트 **289 files / 6,328 tests** |
| e2e | **면제 (화이트리스트 부분집합)** — 변경 set 은 `.md` 와 `review/**` 아래 `.json` 뿐 |

## 보류·후속 항목

없음.
