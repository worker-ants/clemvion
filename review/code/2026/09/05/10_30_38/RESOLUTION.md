# RESOLUTION — `review/code/2026/09/05/10_30_38`

**Critical 0 · WARNING 0.** 게이트상 조치 의무는 없지만 **INFO#1 을 고쳤다.**

## INFO#1 을 WARNING 처럼 다룬 이유

리뷰의 지적:

> 직전 라운드(`10_04_12`)가 고친 것과 **같은 유형의 결함이 최신 커밋(`74d405b07`)에서
> 같은 파일 안 다른 자리에 재발** — `"아래 §checksum 보정 절차가 그 경로다"`.
> *"그 자리를 고쳤다"는 안심이 다른 자리의 같은 패턴까지 커버한다는 보장이 아니라는
> 신호로 남긴다.*

**맞다.** 한 라운드 전에 `§인덱스 교체`(앵커 없는 이름 기반 참조)를 고쳤는데, 그 다음 커밋에서
같은 형태를 새로 만들었다. 지적받은 **인스턴스**만 고치고 **클래스**를 안 닫은 것이다.

그래서 이번엔 전수로 셌다:

| | 값 |
|---|---|
| README 의 `§` 참조 전체 | **7개** |
| 그중 이름 기반 (앵커 없음) | **1개** — `§checksum` (line 160, 내 직전 커밋이 만든 것) |
| 조치 후 이름 기반 | **0개** |

## 리뷰의 제안을 그대로 쓰지 않은 이유

리뷰는 `§6` 으로 바꾸라고 했다. 위치상으로는 맞다 — checksum 보정 절차는 `### 6.` 안에 있다.
그런데 **§6 의 제목은 "테이블-rewrite 형 `ALTER COLUMN TYPE`"** 이라, `§6` 만 적으면 읽는
사람이 엉뚱한 절을 찾는다. 직접 열어 확인하고 **구체 명령 + 위치**로 적었다:

```
`repair` + 재실행(`docker compose up migrate-repair` → `migrate`, §6 말미)
```

## 나머지 INFO 처분

| # | 처분 |
|---|---|
| 2 | `sanitize-loader-error.ts` 의 인용이 워킹트리에 없는 디렉터리를 가리킴 — **`review-citations.md` §1 이 이미 실측·명시한 그 1건**이다. 새 결함 아님 |
| 3·5 | 앞선 9라운드 지적 전항목 반영 확인 + 정량 주장 5개 전수 재현 일치 — 확인 기록 |
| 4 | 유보 2건(`V110` 헤더, Flyway `mixed=true`)이 서술과 실제 상태 일치 — 등재 완료 |

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint · build | **해당 없음** — 애플리케이션 코드 변경 0 |
| unit | **PASS** — frontend 스위트 **289 files / 6,328 tests** |
| e2e | **면제 (화이트리스트 부분집합)** — 변경 set 은 `.md` 와 `review/**` 아래 `.json` 뿐 |

## 보류·후속 항목

없음. 이 PR 이 등재한 3건(`mixed=true` 도입 · bare 인용 8건 · `V110` 헤더 서술)은
`plan/in-progress/spec-draft-nullable-notation-followups.md` 에 있다.
