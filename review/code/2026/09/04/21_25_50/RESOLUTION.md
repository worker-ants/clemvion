# RESOLUTION — `21_25_50`

전체 위험도 **LOW** · Critical **0** · WARNING **1**.

## 조치 항목

| # | 카테고리 | 지적 | 처분 | commit |
|---|---|---|---|---|
| 1 | scope | 이 라운드의 프롬프트 페이로드가 실제 HEAD 보다 **1커밋 stale** — `meta.json`(21:25:50) 생성 78초 뒤 `0ac45dfad` 가 커밋됐다 | **코드 결함 아님 — 조치 불요.** 리뷰어 스스로 그 커밋을 직접 대조해 `codebase/**` 무변경(리뷰 산출물 + planner 항목 1건)임을 확인하고 "스코프 판정 자체는 불변" 으로 판정했다 | — |

### WARNING #1 을 조치 없이 닫는 근거

이 지적은 **내 코드가 아니라 리뷰 하네스의 경합 윈도우**에 관한 것이다. 오케스트레이터가
프롬프트를 조립한 뒤 내가 리뷰 산출물 커밋을 올리는 사이의 간격이며, 그 커밋이
`codebase/**` 를 건드리지 않았다는 것은 리뷰어가 `git show` 로 직접 확인했다. 고칠 코드가
없으므로 fix 대상이 아니고, 하네스 개선은 이 PR 의 범위 밖이다.

**stale 여부가 판정을 바꿀 수 있었는지 따로 확인했다**: 그 커밋의 diff 는
`review/code/2026/09/04/21_10_30/**` 와 `plan/in-progress/spec-draft-nullable-notation-followups.md`
뿐이다. 즉 이 라운드가 본 코드는 지금 HEAD 의 코드와 **같다**.

## INFO 중 실제로 고친 것 — #2

| # | 지적 | 조치 | commit |
|---|---|---|---|
| 2 | 신규 e2e 의 `threshold` 예시값이 전부 정수(`10`/`15`)라 `Math.round`/`parseInt` 가 끼어들어도 GREEN — `numeric(12,4)` 정밀도 보존을 직접 검증하지 못한다 | 소수부 4자리를 꽉 채운 값(`12.3456`, `7.0625`)으로 교체. GET 단언을 `Number()` 비교에서 **문자열 정확 일치**로 올리고, PATCH 뒤에도 DB 를 다시 읽는 단언을 추가 | `4e7a52bc9` |

**INFO 인데 왜 고쳤나**: 이 e2e 는 `20_39_25` W4 를 닫으려고 세운 **검증자 그 자체**다.
검증자의 입력이 분기를 못 가르면 그것을 근거로 한 종결 판정도 약해진다. 이 저장소에서
반복된 실패 형태 — **입력 집합 자체가 커버리지이고, 좁히는 편집은 늘 조용히 통과한다** —
가 정확히 이 자리에 해당한다. 지적은 INFO 였지만 내 앞선 RESOLUTION 이 "고정한다" 고
주장한 축이라 그 주장에 맞게 올렸다.

지적된 대로 **새 `it` 을 늘리지 않고 기존 케이스의 값을 교체**했다.

## 나머지 INFO 처분

| # | 처분 |
|---|---|
| 1 (DTO 타입 정정 성격) · 8 (시크릿 없음) · 9 (선행 4라운드 WARNING 전부 해소 재확인) | 조치 불요 — 확인 보고 |
| 3 (`it.each` 두 블록 병합) | **미조치.** 두 블록은 서로 다른 라운드(`20_16_17` 정규식 위음성 / `20_39_25` 포지셔널 인자)의 근거를 각각 인용한다. 합치면 어느 형태가 어느 지적에서 왔는지가 사라진다 — 이 저장소가 반복해 지적받은 "근거를 소급해 뭉개는" 형태다. 가독성 이득보다 이력 손실이 크다 |
| 4 (`21_10_30` RESOLUTION 의 build/e2e placeholder) | **이미 해소.** `f213266a4` 가 실측치(build PASS · e2e 51 suites / 293 passed)를 기입했다. 리뷰 시점에는 그 커밋 전이었다 |
| 5 (리뷰 시점 e2e 완주 미확인) | **이미 해소.** 완주 로그에서 `PASS test/alerts-threshold-wire-type.e2e-spec.ts` 를 확인했다 (`21_10_30` RESOLUTION 에 기록) |
| 6 (`spec/1-data-model.md:873` Float 라벨) · 7 (`swagger.md` 성문화) | `spec/` 쓰기라 developer 권한 밖. plan 에 planner 항목으로 기등재 |

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint | **PASS** (`_test_logs/lint-20260904-213628.log`) |
| unit | **PASS** — backend jest **9,338 passed / 9,339 total** (변동 없음. 이번 편집은 e2e 파일 1개라 unit 수가 늘 이유가 없다) (`_test_logs/unit-20260904-213728.log`) |
| build | **PASS** (`_test_logs/build-20260904-213905.log`) |
| e2e | **통과** — **51 suites / 293 passed**. 값을 소수로 바꾼 그 스펙이 실제로 돌았음을 확인 (`PASS test/alerts-threshold-wire-type.e2e-spec.ts`) — 이 라운드의 요점이 **그 단언이 실제로 실행됐는가**이므로 총계만으로는 부족하다 (`_test_logs/e2e-20260904-214140.log`) |

## 보류·후속 항목

없음.
