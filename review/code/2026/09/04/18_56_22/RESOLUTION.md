# RESOLUTION — `18_56_22`

## 조치 항목

| SUMMARY # | 발견 | 조치 |
|---|---|---|
| W2 (maintainability) | 신규 테스트 2건만 한국어이고 같은 파일의 기존 3건은 영어 — **diff 가 파일 안에 언어 불일치를 새로 만들었다** | 기존 컨벤션(영어)으로 통일 |
| W1 (api_contract/side_effect) | 저장소 밖 클라이언트의 200→400 breaking change | **추가 코드 변경 없음** — 직전 라운드에서 CHANGELOG 문구를 "저장소 안에서 확인한 것" 으로 좁히고 **배포 시 확인** 줄을 넣었다. 리뷰어도 *"이미 병합 가능 수준"* 이며 이 저장소에 API 버전 체계가 없어 **이 diff 만의 신규 갭이 아니라고** 판정했다 |
| INFO#12 | 신규 거절 단언이 응답 바디(`code`)를 안 봐 형제 테스트와 검증 깊이가 다름 | `body.code === 'VALIDATION_ERROR'` 단언 추가 |
| INFO#8 | JSDoc 이 **ephemeral 리뷰 세션 ID** 를 영구 주석의 근거로 인용 — 그 디렉터리가 정리되면 dangling | SoT 인 plan 문서 인용으로 교체 |

## 뮤테이션 재검증 (재작성 후)

단언을 다시 썼으므로 분기가 바뀌었다 — 통과를 그대로 증거로 삼지 않고 다시 심었다.

| | 예측 | 실측 |
|---|---|---|
| `forbidNonWhitelisted: true → false` | 거절 단언만 RED | **RED 1 / 4 pass** |
| 원복 | GREEN | **GREEN 5건** |

## 남기는 것 (INFO, 비차단)

- **`?workflowId=<uuid>` → 400 을 종단으로 찌르는 e2e 는 없다** (INFO#11). 파이프 유닛
  테스트가 전역 파이프를 겨눠 **더 넓게** 덮지만 라우트 종단 보증은 아니다. 리뷰어도
  "필수 아님" 으로 분류했다.
- `plan` §③ 스냅샷 모집단(1,096 → 실제 1,095)이 이번 제거로 한 칸 더 드리프트했다
  (INFO#15). 그 문서가 이미 *"잰 시점의 값"* 원칙을 명시해 수용된 드리프트다.

## TEST 결과

- lint: **PASS**
- unit: **PASS** — backend jest 445스위트 **9,324건**
- build: **PASS**
- e2e: **PASS** — 292건
