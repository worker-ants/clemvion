# RESOLUTION — repo-guard walker 통합 + 낡은 spec 캐스트 가드 리뷰 5R

대상 SUMMARY: 위험도 **MEDIUM** · Critical **0** · Warning **3** · INFO 14
reviewer 9명 결과 확보(forced 7명 전원 포함).

**Warning 3건 전부 내 편집이 만든 것이고, 전부 4R 조치의 부산물이다.** 기능 결함은 없다
(판정 로직 불변, 전 스위트 GREEN).

## W1·W2 — 문장 중간부터 치환해 앞부분이 매달렸다

| 위치 | 결과 |
|---|---|
| `guard.ts:221-222` | `… — 그 근거와` / `근거는 그쪽 docstring 에 있다.` |
| `plan:277-278` | `… 판정 대상` / `판정 대상이 그만큼 줄어든다` |

둘 다 같은 기계적 실패다. 여러 줄 문장의 **중간부터** 치환하면서 앞줄 꼬리를 안 봤다.

**더 나쁜 것은 검증 방식이다.** 나는 *제거한 토큰이 grep 에 안 나오는 것*으로 확인했다 —
**부재로 검증했지 결과를 읽지 않았다.** 지운 문자열이 사라진 것과 남은 문장이 말이 되는 것은
다른 명제다.

## W3 — 재훑기를 방금 편집한 파일 하나로만 돌렸다

4R 에서 세운 절차("문구를 grep 해서 나온 전부를 고친다")를 적용해 2자리를 찾았다고 보고했다.
그런데 **고친 뒤의 재훑기는 `guard.ts` 한 파일에만 돌렸다.** 같은 문장의 사본이 자매 spec
파일에 있었고 그대로 남았다.

> **절차를 세운 그 턴에, 그 절차의 검증 단계에서 같은 병이 났다.** 이 plan 에서 "한 자리만
> 고치기" 는 이제 다섯 번째이고, 이번은 *고치기* 가 아니라 *확인하기* 에서 났다.

## 이번엔 방법을 바꿨다 — 부재 대신 결과

1. **고친 세 자리의 문단을 실제로 출력해 읽었다** (RESOLUTION 작성 전에).
2. **줄바꿈을 사이에 둔 어절 중복을 기계적으로 검출**하는 스크립트를 돌렸다 — 이번 세션에
   내가 쓴 5개 파일 전수. 히트 13건은 **전부 `});` 닫는 괄호**로 산문 중복 0.
3. 재훑기 범위를 편집한 파일이 아니라 **그 문구가 있을 수 있는 파일 전부**로 잡았다.

`오늘 저장소` 히트 중 나머지 2건(`source-scan.ts:137`·`source-scan.spec.ts:158`)은 `#1241`
이 쓴 것이라 손대지 않았다 — 지나가며 남의 문서를 고치는 것은 별건이다(`git log -L` 확인).

## INFO — 조치 없음, 근거만

- **INFO#4** `stripComments`→`stripLiterals` 순서 때문에 URL 이 있는 줄의 캐스트를 놓칠 수
  있다(위음성). 순서를 뒤집으면 원천 차단된다는 제안도 맞다. 다만 **`stripComments` 는 이
  저장소가 이미 "문자열 속 `//` 도 주석으로 본다" 를 의도로 문서화한 함수**라, 순서를 뒤집는
  것은 그 함수의 계약을 새 소비처가 바꾸는 일이 된다. 실사례 0건이므로 미룬다.
- **INFO#5** `SPEC_CAST` 가 `obj.field = null as unknown as X` 대입문 형태를 못 본다.
  맞다 — 실사례는 가드 자신의 픽스처 문자열뿐이다. 위음성 방향.
- **INFO#10·11**(performance) 저장소 전수 블록의 중복 스캔(≈16ms). 사실이다. 다만 이 라운드는
  **주석만 고치기로** 범위를 좁혔다 — 테스트 구조를 바꾸면 또 한 라운드가 돈다.
- 나머지는 앞선 라운드에서 처분된 항목의 재확인이다.

## 검증

eslint **PASS** · 가드+source-scan 스위트 GREEN · 판정 로직 **불변**(주석·문자열만 변경).
