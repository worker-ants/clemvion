# RESOLUTION — repo-guard walker 통합 + 낡은 spec 캐스트 가드 리뷰 7R

대상 SUMMARY: 위험도 **LOW** · Critical **0** · Warning **1** · INFO 15
reviewer 9명 결과 확보(forced 7명 전원 포함).

## W1 — 같은 실패 모드가 **이 changeset 안에서 두 번째**다

6R 에서 `masked-reject-callers.spec.ts` 상단에 새 JSDoc + `describe` 를 끼워 넣으면서,
원래 그 자리에 있던 JSDoc 이 자신이 설명하던 `describe('resolveTriggerParameters 직접 호출부
허용목록')` 로부터 떨어졌다.

**1R 의 W4 가 정확히 이것이었다** — `stripLiterals` 를 `countCalls` 의 JSDoc 과 선언 사이에
끼워 넣어 orphan 을 만들었다. 다른 파일, 같은 기계적 원인: **선언 앞에 문서가 붙어 있는
자리에 블록을 삽입하면서 그 결속을 안 봤다.**

**조치**: 새 블록 전체(자기 JSDoc 포함)를 앞으로 빼고 원본 JSDoc 을 자기 `describe` 바로
위로 되돌렸다. 결과를 출력해 **각 JSDoc 이 자기 대상에 붙었는지 눈으로 확인**했다.

### 두 번 났으니 기계적으로 훑었다

`*/` 다음 줄이 (a) 또 다른 `/**` 이거나 (b) 빈 줄인 자리를 이번 세션에 내가 쓴 5개 파일에서
전수 검출했다.

- (a) **0건** — 진짜 orphan 없음.
- (b) 2건 — 둘 다 **파일 최상단 docstring + 빈 줄 + import** 로, 이 저장소의 표준 관례다.
  **읽어서 확인했다**(검출기가 플래그했다고 고치지 않았다).

## INFO 15건 — 전부 조치 없음

이번 라운드 INFO 는 **하나도 새로운 것이 없다.** 전부 앞선 라운드에서 판단·유예된 항목의
재확인이고, 각각 재개 트리거가 붙어 있다:

| 항목 | 트리거 |
|---|---|
| `includeSpec` JSDoc "실사례 하나" (3R 부터) | 그 파일을 다음에 만질 때 **개수 서술 자체를 뺀다** |
| 저장소 전수 스캔 3중 (5R 부터) | 테스트 구조를 만질 일이 생길 때 |
| `@OneToOne` 분기 미실행 · `isNullableType` 기본값 형태 · `WIDENED_DECL` 2단 데코레이터 | 저장소에 그 형태가 실재할 때 (전부 **위음성** 방향, 현재 0건) |
| 파일 책임 누적 · regex/AST 비일관 · God Module | 네 번째 검사 추가 / 데코레이터 2단 실재 / 다음 프리미티브 |
| 래퍼 이름 4종 | 5개 가드 중 하나를 다시 만질 때 |

**INFO#14** — 리뷰 도중 공유 워크트리에서 `.bak` 을 포함한 일시적 수정 상태가 관측됐다.
이번엔 **내 뮤테이션**이다(W1 배선 검증). 6R 에서는 리뷰어 쪽이었고 1R 에서도 내 쪽이었다.
**공유 워크트리에서 뮤테이션 검증과 리뷰가 겹치는 것은 구조적 성질**이라, 매번 "다음엔
리뷰 전에" 로 적는 대신 여기 한 번 기록한다 — reviewer 가 세 번 모두 "이 diff 소속 아님"
으로 정확히 판정했다.

## 검증

lint **PASS** · 가드+source-scan **184건** GREEN · eslint PASS · JSDoc 결속 전수 확인.
