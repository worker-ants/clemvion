# RESOLUTION — `23_47_43`

전체 위험도 **LOW** · Critical **0** · WARNING **1**. **조치 완료** (등재 유예 없음).

## 조치 항목

| # | 카테고리 | 지적 | 조치 | commit |
|---|---|---|---|---|
| 1 | testing | `J.` 의 워크스페이스 격리 단언이 `for (row of isolated.data) expect(내것아님)` 형태인데, `otherWs` 에는 스케줄이 하나도 없어 **루프 바디가 한 번도 실행되지 않는다** — 정상 경로에서 관측되지 않는 단언 | `expect(isolated.body.data).toEqual([])` 로 교체. 기대값이 "빈 목록" 이면 기대값을 그대로 쓰는 것이 강한 형태다 | `a74704c49` |

### 이건 내가 반복해 온 형태다

이 저장소에 이미 등록된 실패 패턴 그대로였다 — **"부정 단언이 제3상태에서 참"**. `mine.has(id)`
가 거짓임을 확인하는 루프는 `data` 가 비어 있을 때도 통과한다. 통과의 이유가 "격리가 됐다" 가
아니라 "검사할 것이 없었다" 였다.

바꾼 형태는 두 회귀 클래스를 모두 문다:

| 회귀 | 종전 루프 | `toEqual([])` |
|---|---|---|
| 워크스페이스 필터가 통째로 사라짐 (내 스케줄이 샌다) | 잡힘 | 잡힘 |
| 필터는 걸리되 **다른** workspace_id 의 행이 섞임 | **못 잡음** | 잡힘 |

리뷰가 지적한 대로 이 저장소의 다른 격리 테스트(`knowledge-base`·`agent-memory-admin`)도
길이 0 을 직접 단언한다 — 관례와도 맞춘 셈이다.

## INFO 처분

| # | 처분 |
|---|---|
| 1·2 (인젝션 표면 없음 · 인가 경계 유지) · 4 (직전 두 라운드 WARNING 해소 재확인) · 8 (doc-sync-matrix 매칭 0건) | 확인 보고 — 조치 불요 |
| 3 (DROP-first 재실행 비대칭) | **기존 처분 유지.** `DO` 블록이 트랜잭션이라 `CONCURRENTLY` 와 같은 파일에 둘 수 없어 코드로는 없앨 수 없다. 헤더에 트레이드오프 표로 명시했고, 규약 성문화는 후속 등재 완료 |
| 5 (헤더가 리뷰 세션 ID 를 인용) | **미조치 — 다만 지적은 타당하다.** append-only 파일에 일시적 식별자를 남긴 것은 맞다. 지금 고치면 그 자체로 라운드를 한 번 더 도는데, 본문이 자기완결적이라 인용이 무의미해져도 의미 손실이 없다. **향후 지침으로 받는다** (커밋 SHA·PR 번호 사용) |
| 6 (쓰기 비용 벤치마크 부재) | 기존 처분 유지 — 합성 데이터 수치를 런북 기준선으로 쓰지 않는다 |
| 7 (`(trigger_id)` 행 드라이브바이) | 3회째 동일 판정 — 기존 처분 유지 |
| 9 (신규 e2e 의 cleanup 부재) | **미조치.** 이 파일의 `A.`~`I.` 전부가 같은 관례이고, e2e 는 `make e2e-down` 으로 초기화된다. `J.` 만 다르게 하면 오히려 파일이 일관성을 잃는다 |

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint | **PASS** (`_test_logs/lint-20260904-235659.log`) |
| unit | **PASS** (`_test_logs/unit-20260904-235759.log`) |
| build | **PASS** (`_test_logs/build-20260904-235935.log`) |
| e2e | **통과** — **51 suites / 295 passed** (변동 없음 — 기존 `J.` 안의 단언을 교체한 것이라 `it` 수가 늘 이유가 없다) (`_test_logs/e2e-20260905-000210.log`) |

## 보류·후속 항목

- **`CREATE INDEX CONCURRENTLY` 재실행 규약화** — 등재 유지
  (`spec-draft-nullable-notation-followups.md`). DROP-first 유무 각각의 위험을 표로 담아 두었다.
