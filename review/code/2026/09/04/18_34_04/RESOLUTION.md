# RESOLUTION — `18_34_04`

## 조치 항목

| SUMMARY # | 발견 | 조치 |
|---|---|---|
| W2 (testing) | 이 변경의 **요점인 동작 변화(200→400)를 고정하는 테스트가 없다** — `forbidNonWhitelisted` 축을 단언하는 자리가 저장소 어디에도 없었다 | `validation.pipe.spec.ts` 에 거절 단언 + 대조군 추가. **뮤테이션 검증** |
| W3 (requirement) | plan `## 종결 조건` 의 "열려 있는 것은 넷" 이 **이 diff 로 즉시 stale** (실제 2건) | **개수를 아예 뺐다** — 근본 원인이 하드코딩이다 |
| W1 (api_contract/side_effect) | 저장소 밖 클라이언트는 **관측 범위 밖**인데 CHANGELOG 가 "실측으로 확인한 소비자 현황" 으로 읽힌다 | 문구를 "저장소 안에서 확인한 것" 으로 좁히고 **배포 시 확인** 한 줄 추가 |

## W2 — 뮤테이션 검증 (예측 / 실측)

| 뮤턴트 | 예측 | 실측 |
|---|---|---|
| `forbidNonWhitelisted: true → false` | 거절 단언만 RED, 대조군은 GREEN | **RED 1 / 4 pass — 정확히 그 갈림** |
| 원복 | GREEN | **GREEN 5건** |

뮤턴트 삽입 전 앵커 존재를 단언으로 확인했다 — 이 세션에서 치환 실패로 **무효 뮤턴트**가
GREEN 을 낸 적이 있어서다.

## W3 — 같은 자리가 두 번 낡았다

이 문장은 **직전 PR 에서 이미 한 번 고쳤다**(2R W1). 그때 실제 열린 항목 수로 맞췄는데,
이번 diff 가 한 건을 닫으면서 **또** 어긋났다. 원인은 내용이 아니라 형태다 — **개수를
손으로 적으면 항목을 닫을 때마다 따로 갱신해야 한다.**

그래서 이번엔 숫자를 고치지 않고 **없앴다.** 개수의 단일 진실은 `## 후속` 의 미체크
체크박스이고, 표는 열린 것의 성격만 적는다.

## TEST 결과

- lint: **PASS**
- unit: **PASS** — backend jest 445스위트 **9,324건** (직전 9,322 + 새 단언 2)
- build: **PASS**
- e2e: **PASS** — 292건

## 보류·후속 항목

없음. 잔여 2건(§5.4 drift 2단계 · `idx_schedule_next_run`)은 이 PR 범위 밖이며 각각
검증자 도입·`EXPLAIN` 이 선행이다.
