# RESOLUTION — `20_16_17`

위험도 **MEDIUM** · Critical 0 · WARNING 4. W1~W3 조치, W4 는 등재된 후속으로 남긴다.

## 조치 항목

| # | 발견 | 조치 |
|---|---|---|
| W1 | 신규 가드가 **정규식**으로 `@Column` 을 찾는다 — **같은 파일이 "정규식으로 세 번 틀렸다, 그래서 AST 로 갔다" 고 적어 둔 자리에서** 같은 실수를 했다. 리뷰어 3명이 위음성 4형태를 재현 | **AST 로 교체.** `readStringOption` 헬퍼를 형제 `readBooleanOption` 옆에 두고 `callDecorators` 를 재사용했다. **4형태를 대조군으로 고정** |
| W2 | 경로 역할 판별이 원본 절대경로를 그대로 본다 — **이 파일이 세운 `toPosixPath` 관례**를 신규 함수만 안 따랐다. 윈도우에서 가드 전체가 조용히 "위반 0건" 이 된다 | 판별 전 `toPosixPath` 적용 + 디렉터리 표식을 명명 상수로(`ENTITY_DIR`/`RESPONSE_DTO_DIR`) |
| W3 | `<Entity>Dto` 이름 관례 의존이 문서에도 대조군에도 없다. 저장소에 실제로 벗어난 이름(`StatisticsResponseDto`)이 있다 | docstring 에 한계 명시 + **음성 대조군** 추가 |
| W4 | 실제 `/api/alerts` 가 `threshold` 를 문자열로 **직렬화**하는지 보는 런타임 테스트가 없다 | **등재로 갈음.** plan 의 2단계 (b) 항목이 정확히 이것이고 **첫 대표 엔드포인트로 `GET /api/alerts` 를 이미 명시**해 뒀다(§수렴 예외 (a)(b)(c)(d)) |

## W1 — 같은 파일이 적어 둔 원칙을 내가 어겼다

`swagger-dto-contract-guard.ts` 위쪽 docstring 은 *"정규식으로 세 번 틀렸다 … 넓히는 것으로
는 안 되고 도구를 바꿔야 했다"* 라고 적는다. **그 아래에 정규식 술어를 새로 추가했다.**

리뷰어들이 재현한 위음성 넷 — 옵션의 중첩 객체(`transformer: {...}`)가 `[^}]*` 를 끊고,
데코레이터와 선언이 같은 줄이면 개행 강제에 걸리고, `public` 같은 접근 제한자나 사이에 낀
`@Index()` 에도 깨진다. 넷 다 **numeric 컬럼을 "numeric 아님" 으로 조용히 분류**해 가드의
존재 이유를 무력화한다.

### 뮤테이션 검증 (예측 / 실측)

| | 예측 | 실측 |
|---|---|---|
| AST → 초판 정규식으로 되돌림 | 최소 3건 RED | **4건 전부 RED** (25 pass) |
| 원복 | GREEN | **GREEN 29건** |

예측보다 하나 더 물었다. 이제 누군가 정규식으로 되돌리면 스위트가 막는다.

## W4 를 등재로 갈음하는 근거 (§수렴 예외)

- **(a) 동작 결함이 아니다** — 정적 축은 이번에 닫혔고, 남은 것은 직렬화 축의 **커버리지**다.
  재현되는 오동작이 없다(FE·평가 로직 모두 이미 문자열 전제).
- **(b) fix 가 새 라운드를 강제한다** — 런타임 테스트는 `codebase/` 변경이라 게이트
  freshness 가 재무장되고, 이 PR 은 이미 4라운드를 돌았다.
- **(c) 이 조항을 근거와 함께 인용한다** — 지금 이 문단이다.
- **(d) 등재는 이 턴에 했다** — 신규 항목이 아니라 **기존 2단계 (b) 항목의 갱신**이며,
  첫 대표 엔드포인트로 `GET /api/alerts` 를 명시해 두었다.

## TEST 결과

- lint: **PASS**
- unit: **PASS** — backend jest 445스위트 **9,334건**
- build: **PASS**
- e2e: **PASS** — 292건
- 타입체크 ratchet: **baseline 일치** (197/36)

## 보류·후속 항목

- **W4 / 2단계 (b)** — `GET /api/alerts` 런타임 계약 테스트. plan 에 등재됨.
- INFO I1~I3(헬퍼 분리·상수화·Map 키 유일성) 중 **I1·I2 는 이번에 함께 반영**했다
  (`collectNumericFields`/`collectDtoFieldTypes` 분리, 디렉터리 상수화). I3(동명 클래스
  충돌)는 현재 중복 0건이라 남긴다.
