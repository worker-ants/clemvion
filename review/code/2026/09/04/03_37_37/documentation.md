# 문서화(Documentation) 리뷰

## 검증 방법

`origin/main` 대비 이 브랜치의 실제 코드/문서 변경 9개 파일(`source-scan.{ts,spec.ts}`,
repo-guard 5개, `nullable-type-lie-cast-guard.ts`/`.spec.ts`, plan 문서)을 `Read`/`git show`로
저장소 현재 전문을 직접 열어 확인했다. 이 diff 는 이미 5라운드(01_49_18~03_17_44)의 documentation
리뷰를 거쳤으므로, 그 라운드들이 지적한 항목(JSDoc orphan·`stripLiterals` 무테스트·"그 근거와
근거는"/"판정 대상 판정 대상이" 중복 어구·`오늘 저장소` 상대 표현)이 **가장 최근 커밋
(`93cd244af`, "리뷰 5R — 부재로 검증했지 결과를 읽지 않았다")에서 실제로 고쳐졌는지**를 코드로
재확인하는 데 검증을 집중했다(`grep`, `git show 93cd244af`, `git log --oneline`).

- 중복 어구 3건(근거/근거, 판정 대상/판정 대상, 오늘 저장소) — **전부 해당 커밋에서 정정 확인**.
- 재발 방지를 위해 이번 세션도 자체적으로 어절 중복 검출(줄바꿈 인접 어절 일치)을 8개 대상 파일
  전수에 돌렸다 — 프로세 중복 신규 히트 0건(주석 빈 줄 매치 8건은 오탐).
- `plan/in-progress/entity-nullable-column-type-mismatch.md` 는 이 diff 로 **새로 추가된** "## 한
  자리만 고치는 버릇" 절의 표·헤딩·산문을 `sed`/`grep -n` 으로 직접 대조했고, 표가 실제로
  렌더링되는지 `python3 -m markdown`(tables 확장)으로 검증했다.
- 저장소 트리에는 아무것도 쓰지 않았다(`markdown` 렌더링은 파일 내용을 stdin 문자열로만 읽어
  수행, 디스크에 결과를 쓰지 않음). 단, 리뷰 도중 `git status --short` 로 **내가 만들지 않은
  기존 미커밋 변경 1건**을 발견했다 — 아래 INFO 참조. 그 파일은 건드리지 않았고
  `git status --short` 는 리뷰 시작 시점과 동일하게 그 한 줄만 보여준다.

## 발견사항

- **[WARNING]** plan 문서의 새 절 제목이 "네 번" 이라고 하는데 그 절이 방금 다섯 번째 사례를
  추가하며 본문은 "다섯 다" 로 이미 고쳐져 있다 — 헤딩만 안 고쳤다
  - 위치: `plan/in-progress/entity-nullable-column-type-mismatch.md:289`
    (`## 한 자리만 고치는 버릇 — 이 plan 에서 네 번 반복했다`)
    vs 같은 파일 `:298`(표 5번째 행 추가) · `:300`(`다섯 다 같은 형태다 — …`)
  - 상세: 이 절은 최신 커밋(`93cd244af`, "리뷰 5R")에서 표에 5번째 행(`| 5 | 절차를 세운 그
    턴에 **재훑기를 편집한 파일 하나로만** 돌려 자매 파일을 놓쳤다 | 리뷰 |`)을 추가하고, 뒤이은
    산문도 `넷 다 같은 형태다` → `다섯 다 같은 형태다` 로 정확히 고쳤다(`git show 93cd244af --
    plan/…` 로 확인). 그런데 그 절을 여는 헤딩(`## 한 자리만 고치는 버릇 — 이 plan 에서 네 번
    반복했다`)은 이번 커밋의 diff 에 포함되지 않아 "네 번" 그대로 남았다 — 헤딩과 본문이 서로
    다른 개수를 말한다. 이 PR 이 스스로 "한 자리만 고치고 그 클래스를 훑지 않는다" 는 패턴을
    네 번(현재는 다섯 번) 반복했다고 자인하며 절차까지 만든 바로 그 지점에서, **그 헤딩 자체가
    같은 클래스의 여섯 번째 사례**가 됐다 — grep 대상을 "본문 산문(넷 다/다섯 다)" 으로만
    좁히고 헤딩 문구("네 번")까지 훑지 않은 것으로 보인다.
  - 제안: 헤딩을 `## 한 자리만 고치는 버릇 — 이 plan 에서 다섯 번 반복했다` 로 정정.

- **[WARNING]** 같은 표에 삽입된 빈 줄이 GFM 표를 끊어, 5번째 행이 표가 아니라 일반 문단으로
  렌더링된다(실측 확인)
  - 위치: `plan/in-progress/entity-nullable-column-type-mismatch.md:296-298`
    (`:296` 4번째 행 끝, `:297` 빈 줄, `:298` 5번째 행)
  - 상세: 표는 헤더(`:291`)·구분선(`:292`)·데이터 행(`:293`~`:296`)까지는 줄바꿈만 있고 빈 줄이
    없다가, `:296`(4번째 행) 다음에 **빈 줄 하나**(`:297`)가 끼어 있고 그다음 `:298`(5번째 행)이
    이어진다. GFM/CommonMark 표는 헤더+구분선 바로 뒤에 빈 줄 없이 이어지는 데이터 행들의
    연속으로만 인식되므로, 중간의 빈 줄이 표를 종료시킨다. `python3 -m markdown`(tables
    확장)으로 이 절만 떼어 실제 렌더링해 확인했다 — 1~4행은 정상적으로 `<table>`/`<tr>` 로
    렌더링되지만, 5번째 행은 표 밖으로 밀려나 `<p>| 5 | 절차를 세운 그 턴에 <strong>재훑기를
    편집한 파일 하나로만</strong> 돌려 자매 파일을 놓쳤다 | 리뷰 |</p>` 로 — 즉 파이프 문자가
    그대로 노출되는 일반 문단으로 — 렌더링된다. GitHub 등 대부분의 GFM 구현도 표–빈 줄–행 배치를
    동일하게 표 종료로 처리하므로 실제 PR 뷰에서도 같은 깨짐이 재현될 가능성이 높다. 바로 위
    WARNING(헤딩 "네 번")과 마찬가지로, "표에 다섯 번째 행을 추가한다" 는 편집 자체는 정확했지만
    편집 결과를 렌더링해서 읽지 않았다는 같은 성격의 결함이다 — 공교롭게도 이 절이 "검증은
    부재가 아니라 결과로 한다"(`:310`)를 스스로 적어 놓은 바로 그 절이다.
  - 제안: `:297` 의 빈 줄을 제거해 표를 하나로 잇는다.

- **[INFO]** 리뷰 대상 diff **밖**의 우발적 사실: 공유 워크트리에 이 PR 이 만들지 않은 미커밋
  로컬 변경이 관측됨 — 언급된 주석과 코드가 지금 당장은 불일치
  - 위치: `codebase/backend/src/repo-guards/__tests__/masked-reject-callers-guard.ts:48-51`
    (working tree 상의 미커밋 상태, `git diff HEAD -- <파일>` 로 확인 가능)
  - 상세: `git status --short` 에 이 파일이 `M`(수정, 미스테이지)으로 나타난다. 커밋된 HEAD
    (`git show HEAD:<path>`)는 `return collectTsFiles(rootDir, { includeSpec: true });` 이고 바로
    위 주석("`includeSpec` — 테스트 코드가 base 함수를 직접 부르는 것도 잡아야 한다. 그래서 위
    허용목록에 `*.spec.ts` 항목이 실제로 들어 있다.")과 정확히 일치한다. 그런데 지금 디스크에는
    그 옵션 인자가 빠진 `return collectTsFiles(rootDir);` 로 바뀌어 있어, **디스크 상태만 보면**
    주석이 코드와 어긋난다. 이 리뷰의 사전 안내(다른 reviewer 들이 동시에 같은 워크트리를 읽고
    있으며 mutation 검증용으로 파일을 고쳤다가 되돌리는 일이 있다는 경고)와 정확히 들어맞는
    패턴으로 보인다 — `includeSpec: true` 를 빼면 어떤 테스트가 깨지는지 보려는 뮤테이션 검증의
    잔여물일 가능성이 높다. 이 변경은 **이번 diff(≠origin/main)에 속하지 않고**, 내가 만든 것도
    아니다. 원복은 시도하지 않았다 — 다른 세션이 현재 이 뮤테이션을 사용 중일 수 있어 `git
    checkout`/`restore` 는 금지 규약이고, 이 파일을 내가 건드린 적이 없어 `cp` 스냅숏도 갖고
    있지 않다. 조용히 넘어가면 다음 사람이 이 diff 자체의 결함으로 오인할 수 있어 명시한다.
  - 제안: 조치 불필요(이 리뷰의 판정 대상 아님). 다만 병합/커밋 전에 `git status --short` 로
    이 미커밋 변경이 실수로 스테이지되지 않았는지 확인할 것.

## 확인된 정상 항목 (재검증, 조치 불필요)

- 5R(`93cd244af`)이 고쳤다고 주장한 세 곳 — `nullable-type-lie-cast-guard.ts:221-222`("그 근거와
  근거는" → "그 근거는"), `entity-nullable-column-type-mismatch.md:277-278`("판정 대상 판정
  대상이" → "판정 대상이"), `nullable-type-lie-cast.spec.ts:241`("오늘 저장소는" →
  "저장소는…(2026-09-04 실측)") — **전부 코드/문서에 실제로 반영됨을 직접 확인**. 문법적으로도
  자연스럽게 읽힌다.
- `stripComments`/`stripLiterals`/`countCalls` 각자의 JSDoc 이 자기 선언 바로 위에 정확히 붙어
  있다(1R W4 조치 유지).
- `WIDENED_DECL` 의 "추가 데코레이터 1개까지만" 한계가 `## 한계` 절로 명시돼 있고
  (`nullable-type-lie-cast-guard.ts:160-166`), 2026-09-04 실측 근거도 딸려 있다.
- README·API 문서·CHANGELOG: 이 diff 는 내부 test-tooling(`repo-guards` walker 통합 + 신규 정적
  가드) + plan 문서 갱신뿐이다. `codebase/backend/` 하위에는 관련 README 가 없고, 루트
  `CHANGELOG.md` 는 API/DTO/스키마 계약이 실제로 바뀐 변경만 기록하는 관례라 해당 없음. 신규
  환경변수·배포 설정도 없음.

## 요약

이 diff 는 5라운드의 documentation 리뷰를 거치며 지적된 항목(JSDoc 배치·전용 테스트 부재·중복
어구 3건)이 최신 커밋에서 실제로 코드에 반영됐음을 재확인했다 — 새 결함은 없다. 다만 그 최신 커밋
자체(plan 문서에 "다섯 번째 사례"를 추가한 편집)가 **같은 파일 안에서 새 결함 2건을 만들었다**:
①표를 여는 헤딩이 "네 번" 이라고 말하는데 방금 고친 본문은 "다섯 다" 라고 말해 헤딩·본문이
어긋나고, ②표 4번째와 5번째 행 사이에 낀 빈 줄이 GFM 표를 끊어 5번째 행이 표가 아니라 파이프
문자가 노출된 일반 문단으로 렌더링된다(실측: `python-markdown` 확인). 둘 다 기능·게이트에는
영향이 없는 순수 문서 결함이지만, 이 PR 이 정확히 "한 자리만 고치고 그 클래스를 훑지 않는 버릇"을
주제로 다섯 번 자인하고 절차까지 만든 바로 그 절에서, 그 절을 편집한 최신 커밋 자체가 같은 클래스의
여섯 번째 사례를 남겼다는 점에서 기록해 둘 가치가 있다. 별도로, 리뷰 대상 diff 밖에서 공유
워크트리의 미커밋 로컬 변경(`masked-reject-callers-guard.ts`)을 관측했다 — 이 diff 의 결함이
아니며 손대지 않았다.

## 위험도

MEDIUM
