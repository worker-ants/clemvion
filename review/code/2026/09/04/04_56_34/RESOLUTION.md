# RESOLUTION — repo-guard walker 통합 + 낡은 spec 캐스트 가드 리뷰 10R (최종)

대상 SUMMARY: 위험도 **LOW** · Critical **0** · Warning **0** · INFO 15
reviewer 9명 결과 확보(forced 7명 전원).

**코드 조치 없음 — 수렴.** WARNING 이 0이고, INFO 15건은 **전부 앞선 9라운드에서 근거와 함께
유예가 확정된 항목의 재확인**이다(security reviewer 는 발견 0건).

## 라운드 추이 — 발견의 성격으로 본 수렴

| 라운드 | W | 성격 |
|---|---|---|
| 1R | 4 | 거짓 단언("못 잡는다") · 테스트 부재 · 새 사본 · JSDoc orphan |
| 2R | 1 | **오탐** — 앞 PR 에서 반증한 실패 모드를 재도입 |
| 3R | 1 | 문서 — 같은 파일이 금지한 하드코딩 숫자 |
| 4R | 1 | 문서 — 한 자리만 지워 **깨진 참조** |
| 5R | 3 | 문서 — 매달린 어절 2 · 자매 파일 누락 |
| 6R | 3 | **진짜 사각지대**(옵션 배선, 리뷰어 뮤테이션 실증) + 문서 2 |
| 7R | 1 | JSDoc orphan 재발 |
| 8R | 1 | **자매 비대칭** — 부팅 크래시 가드만 안 고쳤다 |
| 9R | 2 | 중복 스캔 · 체크박스 stale (**코드 결함 아님**) |
| 10R | **0** | — |

실질 결함은 **8R 에서 끝났고** 9R 이 그것을 확인했다. 3R~5R·7R 은 전부 **내 문서 편집이 만든
것**이었다 — 그 구간이 이 루프에서 가장 비싼 부분이다.

## 이번에 등재한 것 — INFO#12

`widenedEntityFields` 의 동명 충돌 배제가 `@Column` 조합으로만 캐너리돼 있다. **관계
데코레이터끼리의 충돌이 저장소에 3건 실재**한다 — `integration`·`trigger`·`user`
(직접 실측, 리뷰어 수치와 일치).

**코드는 이미 이 축을 다룬다** — `WIDENED_DECL` 이 세 데코레이터를 모두 잡고 배제 로직은
종류를 구분하지 않는다. 없는 것은 캐너리뿐이다. 지금 넣으면 **Warning 0 인 라운드를 다시
돌아야** 해서 plan 에 등재했다(`grep` 으로 등재 확인).

## 나머지 INFO 14건 — 재개 트리거

| 항목 | 트리거 |
|---|---|
| 파일 내용 이중 read · 정규식 파싱 구조 중복 | 그 파일을 다음에 만질 때 |
| 래퍼 이름 4종 통일 | 5개 가드 중 하나를 개별로 만질 때 |
| build-exclude 비대칭 | `__test-utils__` 가 devDependency 를 import 할 때 |
| 범용/도메인 predicate 분리 | predicate 가 3~4개 넘을 때 |
| `.d.ts` docstring 언급 | `src` 하위에 `.d.ts` 가 생길 때 |
| planner 턴 3건 (`§2.9`·`§2.2`·`§5.4`) | **developer 권한 밖** — 다음 planner 턴 |

`documentation` INFO#13(9R 리포트의 "planner 항목 2건" 이 실측 3건과 다름)은 **이 diff 가
만든 결함이 아니고**, 리뷰 산출물은 시점 스냅샷이라 사후 정정 대상이 아니다.

## 검증

lint **PASS** · unit backend **9,283**(443 suites) · build **PASS** · e2e **292** ·
ratchet **197/36** · 가드 8스위트 **147건** · `tsc` 비-spec **0**.
