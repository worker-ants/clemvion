# RESOLUTION — repo-guard walker 통합 + 낡은 spec 캐스트 가드 리뷰 4R

대상 SUMMARY: 위험도 **LOW** · Critical **0** · Warning **1** · INFO 9
reviewer 8명 결과 확보(forced 7명 전원 포함).

## W1 — 한 자리만 고쳐서 **깨진 참조**를 만들었다

3R 에서 `widenedEntityFields` docstring 의 하드코딩 `"20건"` 을 지웠다. 그런데 같은 커밋
묶음이 심어 둔 **다른 두 자리를 안 고쳤다**. 그중 하나가 하필

> 그 근거와 **실측 20건은 그쪽 docstring 에 있다**

라서, 내가 방금 지운 숫자를 가리키는 **거짓 참조**가 됐다. 고치는 행위가 새 결함을 만든 것이다.

### 이번엔 손으로 찾지 않고 훑었다

```
grep -rn "20건" codebase/backend/src plan/…
```

내 것 **3자리**(guard docstring · spec docstring · plan)가 한 번에 나왔다. 셋 다 고쳤고
재훑기 **0건**. 리뷰어가 제안한 명령이 정확히 이것이었다 — 3R 에서 내가 돌렸어야 했다.

## 이 plan 에서 네 번째다 — 규칙이 아니라 절차로 바꿨다

| # | 무엇 | 어떻게 드러났나 |
|---|---|---|
| 1 | 배치 1 RESOLUTION 이 "plan 이 추적한다" 고 썼는데 그 자리가 없었다 | 리뷰 |
| 2 | 배치 말 캐스트 훑기를 *그 배치가 넓힌 필드*로만 돌렸다 | plan 정리 중 실측 |
| 3 | `ipWhitelist` 를 고치고 그 위 "고치지 않았다" 문단을 뒀다 | 리뷰 |
| 4 | `"20건"` 을 한 자리만 지워 깨진 참조를 만들었다 | 리뷰 |

넷 다 **내가 알아챈 자리만 고치고 그 클래스를 훑지 않은 것**이다. 3번 뒤에 *"조치로 앞
서술이 거짓이 되면 그것도 고치는 것까지가 조치다"* 라고 **규칙을 적었는데 4번이 났다.**

**규칙을 쓰는 것으로는 안 고쳐진다.** plan 에 절차로 적었다 —
**서술을 고칠 때는 그 문구를 grep 해서 나온 전부를 고친다.**

## 숫자를 어디에 쓸 수 있나 — 선을 그었다

`collectScanTargets` 가 세운 "검증되지 않는 숫자는 적지 않는다" 를 코드에 적용하면서,
plan 의 `135 → 115` 도 같은 문제인지 봤다. **다르다**:

| | 규칙 |
|---|---|
| 코드 docstring | 개수를 쓰지 않는다 — 낡지 않는 예시와 세는 방법만 |
| plan·커밋 메시지 | 개수를 쓰되 **날짜를 박는다** (`2026-09-04 실측 135 → 115`) |

plan 은 **시점 기록**이고 코드 docstring 은 **지금 참인 서술**이다. `collectTsFiles`
docstring 이 이미 후자에 날짜 헤딩을 쓰고 있어 그 관례를 따랐다. plan 쪽 수치에 날짜를
박고, 이 선을 plan 에 명시했다.

## INFO 9건 — 전부 조치 없음, 각각 트리거가 있다

이번 라운드 INFO 는 **전부 앞선 라운드에서 검토·유예된 항목의 재확인**이다. 각 항목에
재개 트리거가 붙어 있다: 4번째 검사 추가(파일 분리) · **3번째** 이름-기반 매칭 술어(공유
헬퍼 승격) · `src/` 에 `.d.ts` 실제 발생(`.d.ts` 필터 가정 재검토) · 5개 가드 재수정 시점
(래퍼 이름 통일). 조건이 오기 전에 착수하지 않는다.

## 그 절차를 바로 적용했다 — 리뷰가 아니라 내 훑기가 2자리를 더 찾았다

위 절차("서술을 고칠 때는 그 문구를 grep 해서 나온 전부를 고친다")를 이번 커밋에 바로 돌렸다.
세 축으로 훑었다 — ① 날짜 없는 하드코딩 개수 ② 다른 문서를 가리키는 상호 참조 ③ 날짜 없는
"실측" 주장.

**내 것 2자리가 더 나왔다** (`guard.ts` 의 "저장소 전수에 그런 조합은 없다(실측)" ·
"오늘 저장소는 전부 `T | null`"). 둘 다 **현재 저장소 속성을 주장하는데 날짜가 없어**
조용히 낡을 자리다. 날짜를 박고 `오늘` 같은 상대 표현을 없앴다. 재훑기 0건.

> 나머지 히트는 (a) 과거 사건 서술(`"실측 12건" 을 박았다가 낡았다` — 그 자체가 이력이라
> 낡지 않는다) (b) 이미 날짜 헤딩 아래 있는 표 (c) 내가 쓰지 않은 기존 docstring 이다.
> (c) 는 손대지 않았다 — 남의 문서를 지나가며 고치는 것은 별건이다.

## 검증

lint **PASS** · unit backend **9,278**(443 suites) · build **PASS** · e2e **292** ·
ratchet **197/36** · 가드 8스위트 **142건**.

> 이번 조치는 **주석·plan 문자열만** 바꿨다(판정 로직 불변). 그래도 `codebase/` 가 바뀌었으니
> 게이트가 새 리뷰를 요구하면 돈다.
