# RESOLUTION — `21_10_30`

전체 위험도 **LOW** · Critical **0** · WARNING **2**. 두 건 **전부 조치**했다 (등재 유예 없음).

## 조치 항목

| # | 카테고리 | 지적 | 조치 | commit |
|---|---|---|---|---|
| 1 | testing | `readOption` 의 JSDoc 이 약속한 "리터럴을 만날 때까지 계속 훑는다" 분기를 무는 테스트가 0건. 조기 return 으로 뮤테이션해도 **32/32 GREEN** | `[캐너리]` describe 신설 — 같은 키가 두 번 나오고 앞엣것이 비-리터럴인 픽스처로 뒤엣것을 집는지 단언 | `40005a6e0` |
| 2 | documentation | plan 에 planner 항목 2개를 끼워 넣으면서 "§5.4 2단계" 의 연속 서술 **44줄**이 마크다운 들여쓰기상 무관한 "Float 라벨링" 항목 하위로 재부모화. `(a) … 아래 참조` 가 두 항목 건너뛴 자리를 가리키고 있었다 | 연속 서술 블록을 2단계 항목 바로 아래로 되돌렸다 (두 planner 항목은 그 뒤로) | `40005a6e0` |

### W1 을 "JSDoc 을 낮추는" 쪽으로 처리하지 않은 이유

리뷰는 두 갈래를 줬다 — 캐너리를 세우거나, JSDoc 을 "이론상 방어, 미검증" 으로 낮추거나.
**캐너리를 골랐다**: 이 파일은 이미 `@Transform` 예외에 대해 **실사례 0건인 분기도 캐너리로
고정한다**는 원칙을 세워 두었고, 여기서 문서를 낮추면 같은 파일 안에서 두 분기를 서로 다른
기준으로 다루게 된다. 리뷰가 지적한 "내적 불일치" 가 그것이었다.

다만 캐너리가 쓰는 **중복 키 픽스처는 실제 데코레이터에 나오지 않는 모양**이다 — 그 사실을
테스트 docstring 에 명시했다. 이 분기를 가르는 입력이 원리적으로 그것뿐이기 때문이다
(앞엣것이 비-리터럴이기만 하면 원본과 뮤턴트가 **둘 다 `undefined`** 를 돌려줘 갈리지 않는다).

### 뮤테이션 실측 — 예측 / 실측

| 뮤턴트 | 예측 | 실측 |
|---|---|---|
| (원본, 캐너리 추가 후) | GREEN 33 | **GREEN 33** |
| M5 `if (picked !== undefined) return picked;` → `return picked;` | RED 1 — **새 캐너리** | **RED 1 — 죽은 것이 그 캐너리** |

**리뷰가 이 뮤턴트를 먼저 돌려 32/32 GREEN 을 실측했고**(그래서 WARNING 이 됐다), 캐너리
추가 뒤 같은 뮤턴트가 RED 로 뒤집혔다. 개수만 세지 않고 **실패한 테스트 이름을 확인**해
죽은 것이 의도한 그 캐너리임을 확정했다.

원복은 `cp` + 절대경로, 앵커 존재를 `assert` 로 선검증, 마지막에 `git diff --quiet` 확인.

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint | **PASS** (`_test_logs/lint-20260904-212255.log`) |
| unit | **PASS** — backend jest **9,338 passed / 9,339 total** (직전 9,337 → **+1**, 신규 캐너리 1건) (`_test_logs/unit-20260904-212355.log`) |
| build | (실행 중 — 완료 후 실측 기입) |
| e2e | (실행 중 — 완료 후 실측 기입) |

## 보류·후속 항목

없음. INFO 9건은 전부 **조치 불요** 또는 **planner 트랙 기등재**로 판정됐다:

- INFO#1·#2 (`spec/1-data-model.md:873` 의 `Float` 라벨 · `swagger.md` numeric 불변식) —
  `spec/` 쓰기라 developer 권한 밖. plan 에 planner 항목으로 이미 등재.
- **INFO#3 은 신규 제안**이라 plan 에 등재했다 — "내부 서사는 `//` 주석, 소비자용 설명은
  JSDoc" 분리 패턴의 성문화. 위 두 항목과 같은 planner 세션에 묶으라는 리뷰 권고까지 함께.
- INFO#4·#5 (`judgeNumeric` 헬퍼 · `extends`/`PickType` 합성 캐너리) — `20_39_25` 에서 이미
  조치 불요로 판정된 캐리오버. 검출력에 영향 없음.
- INFO#6~#9 (공개 OpenAPI 타입 변경 고지 · `_retry_state.json` 절대경로 · 리뷰 산출물
  볼륨 · 가드 3단계 왕복) — 전부 정상 워크플로로 판정.
