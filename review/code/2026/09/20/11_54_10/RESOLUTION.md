# RESOLUTION — schedule-trigger 「D. PATCH cron」 flake (1라운드)

SUMMARY: Critical 0 · Warning 3 · INFO 9. 정지 규칙(리뷰 전에 선언): Critical · Warning 0 이거나 `codebase/` 수정 0 인
라운드에서 수렴, 최대 3라운드.

## 조치 항목

| SUMMARY # | 처분 | 커밋 |
|---|---|---|
| W1 «연 1회 cron 은 분 단위 cron 과 겹칠 수 없다» 가 참이 아니다 — 12/31 23:59 KST 의 1분에는 둘 다 1월 1일 00:00 | **고침 — 내 전제가 반증됐다.** 빈도만 «하루 1분» → «연 1분» 으로 줄었을 뿐 같은 메커니즘이 남아 있었고, 그 창에서 실제로 터지는 것은 **그대로 두었던 `not.toBe(originalNext)`** 였다. 그 단언을 **뺐다** — 판정은 «새 cron 이 만드는 값인가» 하나로 하고, 대신 «생성 cron 의 값은 그 창 밖» 을 별도 단언해 «PATCH 가 옛 값을 그대로 두었다» 를 가른다. JSDoc·plan 서술도 «겹칠 수 없다» 를 버리고 «겹치는 순간에도 이 단언은 참이다» 로 고쳤다 | `a8ddcfb32` |
| W2 상한 `patchedAt + 65_000` 의 실질 여유가 5초뿐 | **고침.** 여유를 30초로 (`-30_000` ~ `+90_000`). 판별 대상(생성 cron 값)은 몇 달 뒤라 창을 넓혀도 갈린다 — 플레이크를 고치며 다른 플레이크를 들이지 않는다 | `a8ddcfb32` |
| W3 «실측» 인용이 근거 없는 문서를 가리킨다 | **고침.** JSDoc · plan 양쪽을 실제 근거(`review/code/2026/09/20/09_35_16/RESOLUTION.md` §TEST 결과 · 트래커 항목)로 정정. `plan/complete/ssrf-catch-instanceof.md` 에는 등재 한 줄만 있었다 | `a8ddcfb32` |
| INFO 1 결과가 분 경계(`:00`)인지 안 본다 | 고침 — `getUTCSeconds() === 0` 을 더했다. 분 단위 cron 이 만든 값이 아니면 갈린다 | `a8ddcfb32` |
| INFO 2 cron 재계산 happy-path 의 결정적 **단위** 테스트가 없다 | 조치 없음 — 이 PR 의 범위는 e2e 한 케이스의 거짓 실패다. 단위 테스트 추가는 별 항목이라 트래커에 등재한다 | 마무리 |
| INFO 3 연말 경계를 fake timer 로 재현하지 않았다 | 조치 없음 — W1 조치로 그 경계가 **판정에서 빠졌다**(대리 지표를 제거했으므로 재현 대상이 사라졌다) |
| INFO 5 · 6 · 7 | 조치 없음 — 5: 허용 오차 리터럴은 근거 주석과 함께 두었다(같은 파일 기존 스타일) · 6: 연 1회 cron 리터럴 중복은 `it()` 자기완결 fixture 관례 · 7: 케이스 JSDoc 이 맥락을 담는다 |
| INFO 4 · 8 · 9 | 조치 없음 — 워크플로 산출물(plan · consistency 세션)이고 관례대로다 |

## TEST 결과

- lint: 통과
- unit: 통과
- build: 통과
- e2e: 통과 (366). 조치 후 대상 케이스 단독 실행도 통과, 뮤턴트(서비스의 재계산 블록 삭제) RED 재확인 — 이번엔 «1분 안» 단언이 직접 잡는다
