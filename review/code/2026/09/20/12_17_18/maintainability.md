# 유지보수성(Maintainability) 리뷰

## 리뷰 범위

- `codebase/backend/test/schedule-trigger.e2e-spec.ts` — 「D. PATCH cron → nextRunAt 재계산」 케이스: cron 리터럴 교체, `patchedAt` 캡처, `not.toBe(originalNext)` 제거 후 "새 cron 이 만드는 값인가" 4단언 추가 (실제 코드 변경의 전부, `git diff origin/main` 으로 대조 확인)
- `plan/in-progress/schedule-cron-flake.md` — 신규 plan 문서
- `plan/in-progress/spec-draft-nullable-notation-followups.md` — 트래커 항목 해소 처리 + 신규 백로그 1건 추가 (§4924 부근)
- `review/code/2026/09/20/11_54_10/*`, `review/consistency/2026/09/20/11_21_16/*` — 이전 라운드 워크플로 산출물 (자동 생성)

뮤테이션 없이 읽기 전용으로 분석했다 (`git status --short` 변화 없음).

## 발견사항

- **[INFO]** 판정 창의 상한 `90_000` 이 두 곳에 중복된 매직 넘버로 박혀 있고, 둘이 같은 값이어야 한다는 관계가 이름으로 드러나지 않는다
  - 위치: `codebase/backend/test/schedule-trigger.e2e-spec.ts:318`, `:322`
  - 상세: `318` 은 "새 cron 값이 이 시각 이전에 와야 한다"(상한), `322` 는 "옛(연 1회) cron 값은 이 시각 이후여야 한다"(하한)로 **서로 반대 방향의 경계**지만 개념적으로는 같은 "1분 창의 끝"을 가리킨다. 지금은 두 `expect` 에 리터럴 `90_000` 이 각각 따로 적혀 있어, 나중에 누군가 여유를 조정하며 한쪽만 고치면(예: 318만 `120_000` 으로 넓히고 322를 놓치면) 두 단언이 서로 다른 경계를 보게 되어 "새 값인가 vs 옛 값이 아닌가"를 가르는 판별력이 조용히 무너질 수 있다 — 이 테스트 자체가 이번 수정에서 "대리 지표가 조용히 무너진다"는 것을 정확히 겪은 사례라 더 눈에 띈다.
  - 제안: `const windowMs = 90_000;` 같은 지역 상수로 뽑아 두 `expect` 가 같은 값을 참조하게 하면, 그 관계가 코드로 강제되고 리뷰 시점에도 "이 둘은 반드시 같이 움직인다"가 명시적으로 보인다. 급한 결함은 아니다 — 현재도 주석(320행)이 관계를 설명하고 있다.

- **[INFO]** 허용 오차 상수(`30_000`/`90_000`)가 이름 없는 인라인 리터럴 — 기존 컨벤션과 일치, 새 결함 아님
  - 위치: `codebase/backend/test/schedule-trigger.e2e-spec.ts:317-318`
  - 상세: 1라운드 리뷰에서 이미 지적된 항목(당시 값은 `5_000`/`65_000`)과 같은 패턴이며, 같은 파일의 기존 테스트 A(112행, `Date.now() - 60_000`)도 동일 스타일(인라인 리터럴 + 근거 주석)을 쓴다. 위 313-315행 주석이 근거를 충분히 설명하므로 새로운 유지보수성 결함은 아니다.
  - 제안: 조치 불요. 세 번째 등장 시 `MINUTE_CRON_TOLERANCE_MS` 류 명명 상수화 고려(1라운드 제안과 동일).

- **가독성/일관성 (양호, 발견사항 아님)**: 케이스 앞 JSDoc(283-292행)이 "무엇이 문제였는지 → 왜 「달라졌다」가 대리 지표인지 → 무엇을 대신 보는지 → 왜 흔들리지 않는지"를 실측 인용과 함께 서술하는 방식이 파일 내 다른 `it()` 주석들과 형식이 일치한다. `patchedAt`/`nextRunMs` 네이밍도 기존 변수명 관례(카멜케이스, 의미 전달)와 일관적이다. 함수 길이(`it('D. ...')` 본문 약 34줄)·중첩 깊이(0단, 순차 statement 만)도 파일 내 다른 테스트 대비 과도하지 않다.

- **plan/tracker 문서 (`schedule-cron-flake.md`, `spec-draft-nullable-notation-followups.md`)**: 발견사항 없음. 저장소 plan 템플릿을 그대로 따르고, "해소" 기록이 실제 커밋(`a8ddcfb32`)·근거(RESOLUTION)를 구체적으로 인용해 다음 사람이 재구성할 필요가 없다.

- **워크플로 산출물(`review/**`)**: 사람이 직접 유지보수하지 않는 1회성 리포트/상태 파일이라 유지보수성 리스크가 원천적으로 낮다. 구조적 이상 없음.

## 요약

이번 diff 는 e2e 테스트 한 케이스의 cron 리터럴 교체와 판별 단언 재작성(1라운드 리뷰의 W1/W2/W3 조치 반영)에 국한되며, 범위가 작고 근거 주석이 충실하다. 가독성·네이밍·함수 길이·중첩·복잡도 모두 파일 기존 스타일과 일치해 문제가 없다. 유일하게 새로 눈에 띄는 점은 반대 방향 경계 두 곳에 같은 매직 넘버 `90_000` 이 독립적으로 박혀 있어, 향후 한쪽만 수정되면 판별력이 조용히 깨질 수 있다는 것인데(INFO), 이는 이번 수정이 고친 "대리 지표가 조용히 무너지는" 문제와 결이 닮아 있어 짚어 둘 가치는 있으나 현재 코드에 결함이 있는 것은 아니고 급히 고칠 필요도 없다. CRITICAL/WARNING 급 발견사항은 없다.

## 위험도

NONE
