# RESOLUTION — schedule-trigger 「D. PATCH cron」 flake (2라운드)

SUMMARY: Critical 0 · Warning 2 · INFO 6. 정지 규칙(1라운드 전에 선언): Critical · Warning 0 이거나 `codebase/` 수정 0 인
라운드에서 수렴, 최대 3라운드.

## 조치 항목

| SUMMARY # | 처분 | 커밋 |
|---|---|---|
| W1 1라운드에 내가 **새로 넣은** «옛 값은 창 밖» 단언이 같은 종류의 창(연말 90초)을 다시 만들었다 | **고침 — 같은 실수를 두 번 했다.** 옛 값과의 비교는 형태가 무엇이든(«다르다» 든 «창 밖» 이든) 두 cron 의 다음 실행이 같아지는 순간에 거짓 실패한다. **옛 값 비교를 통째로 뺐다**(`originalNext` 선언도) — 판정은 «지금 시각과 새 cron 의 관계»(1분 안 · 분 경계)만으로 한다. 재계산이 없었으면 값은 몇 달 뒤라 그 창 밖으로 떨어져 갈린다. 뮤턴트(서비스 재계산 블록 삭제) RED 재확인. JSDoc 에 «옛 값과 비교하지 않는다» 와 그 이유를 적었다 | `b40b5b98f` |
| W2 트래커가 아직 없는 `plan/complete/schedule-cron-flake.md` 를 «해소» 로 선인용 | **고침.** 그 문장을 **되돌렸다** — 실제 `--impl-done` + `plan/complete/` 이동이 끝나는 마무리 커밋에서 다시 적는다. 같은 파일의 다른 «해소» 인용 셋이 모두 이동 뒤에 적힌 선례를 따른다 | `b40b5b98f` |
| INFO 1 plan 이 «`NAV-WF-02` 를 planner 항목으로 등재한다» 고만 적고 실제 등재가 없다 | **고침.** 트래커에 planner 항목으로 실제 등재하고(`--impl-prep` cross_spec WARNING 1 근거와 함께), plan 문구를 «등재했다» 로 | `b40b5b98f` |
| INFO 2 상한·하한의 `90_000` 이 따로 박혀 있다 | 조치 없음 — W1 조치로 하한 쪽(옛 값 비교)이 사라져 `90_000` 은 이제 **한 곳**이다 |
| INFO 3 밀리초까지는 안 본다 | 조치 없음 — 분 경계 판정에 초 자리로 충분하고, 값은 ISO 문자열로 온다 |
| INFO 4 · 5 · 6 | 조치 없음 — 4: spec 은 타이밍 세부를 규정하지 않는다(위반 아님) · 5: 매분 트리거 잔존은 이 diff 이전부터의 패턴 · 6: `_retry_state.json` 절대경로는 orchestrator 표준 |

## TEST 결과

- lint: 통과
- unit: 통과
- build: 통과
- e2e: 통과 (366). 조치 후 대상 케이스 단독 통과 + 뮤턴트 RED 재확인
