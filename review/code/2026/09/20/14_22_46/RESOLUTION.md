# RESOLUTION — cron 재계산 happy-path 단위 테스트 (1라운드)

SUMMARY: Critical 0 · Warning 2 · INFO 12. 정지 규칙(리뷰 전에 선언): Critical · Warning 0 이거나 `codebase/` 수정 0 인
라운드에서 수렴, 최대 3라운드.

## 조치 항목

| SUMMARY # | 처분 | 커밋 |
|---|---|---|
| W1 게이트를 통째로 무력화한 뮤턴트(`if (true)`)가 살아남는다 — «둘 다 거짓» 쪽이 빈칸 | **고침 — 내 plan 의 «각 항이 표면» 이 반쪽이었다.** `||` 는 항이 둘이지만 **분기는 셋**이다(cron · timezone · 둘 다 아님). 대조군 테스트를 넣었다 — `{ name }` 만 바꾸는 PATCH 는 `computeNextRuns` 를 **호출하지 않고** `nextRunAt` 이 갱신 전 값 그대로다. 리뷰어가 살아남는다고 실측한 그 뮤턴트를 같은 형태(`|| true`)로 재현해 **RED** 확인 | `ae060b266` |
| W2 신설 `scheduleRow()` 를 바로 위 기존 테스트에 적용하지 않아 같은 리터럴이 두 번 | **고침.** 팩토리를 방어 분기 테스트 **위**로 올리고 그 테스트도 쓰게 했다 — 정의가 하나다 | `ae060b266` |
| INFO 2 리뷰 중 서비스 파일이 순간 뮤테이트된 상태를 둘이 관측 | 조치 없음 — 이 세션의 판별력 검증(`cp` 백업 → 치환 → 원복)이다. 리뷰어들도 «원복 완료, 이번 diff 밖» 으로 적었다. 이 저장소가 아는 형태(병렬 리뷰어와 공유 워크트리) |
| INFO 4 spy 를 `mockRestore` 하지 않는다 | 조치 없음 — 매 테스트가 `beforeEach` 에서 새 `service` 를 받아 실질 누수가 없고, 이 파일의 기존 세 spy 도 같은 형태다 |
| INFO 6 · 7 · 8 spy 캐스트 · `saved` 캡처 · describe 구조 | 조치 없음 — 셋 다 이 파일 **전체**의 패턴이라 이 diff 에서만 바꾸면 오히려 두 양식이 공존한다. 이 파일을 다시 크게 만질 때 함께 |
| INFO 9 JSDoc 위치 | 조치 없음 — 팩토리를 위로 올리며 그 JSDoc 이 이제 두 테스트 바로 위에 온다(구조가 정리됐다) |
| INFO 10 `toHaveBeenCalledTimes(1)` 미포함 | 조치 없음 — 대조군이 «호출되지 않는다» 를 보고, happy-path 가 «무엇으로 불렸나» 를 본다. 호출 지점은 하나뿐이다 |
| INFO 1 · 3 · 5 · 11 · 12 | 확인 기록(시크릿 없음 · spec 일치 · 산출물 관례 · 인용 실재 · 체크리스트 일치) — 조치 없음 |

## TEST 결과

- lint: 통과
- unit: 통과
- build: 통과
- e2e: 통과 (366)
