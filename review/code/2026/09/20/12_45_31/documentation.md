# 문서화(Documentation) 리뷰 — schedule-cron-flake (3라운드 / 누적 diff)

## 발견사항

- **[WARNING]** 클래스 JSDoc 안에서 "생성 cron 값은 창 밖으로 떨어진다"는 주장과 "그 비교는 신뢰할 수 없어서 하지 않는다"는 주장이 같은 커밋(`b40b5b98f`) 안에서 서로 모순된다
  - 위치: `codebase/backend/test/schedule-trigger.e2e-spec.ts:290`(`* 초 자리가 0(분 경계)이다. 재계산이 아예 없었다면 값은 생성 cron 의 것(연 1회, 몇 달 뒤)이라 이 창 밖으로 떨어진다.`) vs `:292-294`(`* **옛 값과 비교하지 않는다.** 「달라졌다」든 「옛 값은 창 밖이다」든, 두 cron 의 다음 실행이 같아지는 순간이 있으면 그 순간에 거짓 실패한다 — 연 1회 cron 도 12/31 23:59 KST 에는 분 단위 cron 과 같은 시각을 가리킨다(1라운드·2라운드 리뷰가 두 번 잡았다).`)
  - 상세: 290행은 "재계산이 없었다면 생성 cron 의 값은 (연 1회이므로) 몇 달 뒤라 이 창 밖으로 떨어진다"고 절대적으로 단언한다. 그런데 바로 두 줄 뒤(292-294행)는 정확히 이 형태의 주장("옛 값은 창 밖이다")을 이름까지 지목해 반증한다 — 매년 12/31 23:59 KST 의 1분 동안은 생성 cron(`0 0 1 1 *`)의 다음 실행도 분 단위 cron 과 같은 시각이 되어 "몇 달 뒤"가 아니라 "수십 초 뒤"가 된다. 이 반증은 실제로 2라운드 리뷰(`review/code/2026/09/20/12_17_18/testing.md` WARNING 1)가 찾아냈고, 그에 따라 2라운드 조치 커밋(`b40b5b98f`)이 코드에서 `originalNext` 비교 단언 자체를 완전히 제거했다. 그런데 그 **같은 커밋**이 새로 써 넣은 290행은 삭제된 그 단언의 전제("생성 cron 값은 몇 달 뒤")를 여전히 사실인 것처럼 서술한다 — 자기모순이다. 다음에 이 케이스를 만질 사람이 290행만 읽으면 "생성 cron 값이 항상 창 밖으로 떨어진다"고 오인해, 그 전제 위에서 다시 비교 단언을 추가하는 (이미 두 번 반복된) 실수를 세 번째로 반복할 위험이 있다.
  - 제안: 290행을 "재계산이 없었다면 값은 생성 cron 의 것(연 1회, 대개 몇 달 뒤)이지만 이 사실에 기대어 옛 값을 비교하지는 않는다 — 그 비교 자체가 292행 이하가 설명하는 이유로 신뢰할 수 없다"는 취지로 정정하거나, 아예 292-294행과 중복되므로 삭제한다.

- **[WARNING]** 위와 같은 반증된 전제("생성 cron 값은 몇 달 뒤라 창 밖")가 삭제된 단언을 정당화하던 인라인 주석에도 그대로 남아 있다
  - 위치: `codebase/backend/test/schedule-trigger.e2e-spec.ts:315-317` (`// 분 단위 cron 의 다음 실행은 늘 다음 분 경계 — 요청 시각부터 60초 안이다. 여유 30초는 e2e 부하(요청 · DB · 트리거 재등록) // 몫이고, 판별 대상인 생성 cron 값은 몇 달 뒤라 여유를 넓혀도 갈린다. 초 자리가 0인지도 본다 — 분 경계가 아닌 값이 오면 // 그것은 분 단위 cron 이 만든 값이 아니다.`)
  - 상세: 이 문장은 1라운드 커밋(`a8ddcfb32`)이 `expect(new Date(originalNext...).getTime()).toBeGreaterThan(patchedAt + 90_000)` 단언을 추가하면서, "여유(하한 30초/상한 90초)를 넉넉히 잡아도 판별 대상인 생성 cron 값(몇 달 뒤)과는 절대 안 겹친다"는 근거로 쓰였다. 그런데 2라운드(`b40b5b98f`)가 바로 그 단언(`originalNext > patchedAt + 90_000`)을 "연말 90초 동안 이 창 밖 주장 자체가 깨진다"는 이유로 완전히 삭제했다(커밋 메시지: "옛 값과의 비교는 형태가 무엇이든 두 cron 이 같은 시각을 가리키는 순간에 거짓 실패한다"). 코드에서 해당 단언은 사라졌는데, 그 단언의 존재 이유를 설명하던 이 주석은 그대로 남아 지금은 **가리키는 코드가 없는 근거**가 됐다 — 현재 두 `expect()`(`:319-320`, 하한/상한)는 `patchedAt` 만 기준으로 하고 생성 cron 값과는 전혀 비교하지 않으므로, "판별 대상인 생성 cron 값은 몇 달 뒤라 여유를 넓혀도 갈린다"는 절은 이 두 단언의 동작을 설명하지 않는다. 이 파일 자신의 JSDoc(292-294행)이 이미 "이 전제(옛 값은 창 밖)에 기대지 않는다"고 명시한 것과도 어긋난다.
  - 제안: "몫이고, 판별 대상인 생성 cron 값은 몇 달 뒤라 여유를 넓혀도 갈린다." 절을 삭제하고 "여유 30초는 e2e 부하(요청 · DB · 트리거 재등록) 몫이다. 초 자리가 0인지도 본다 — 분 경계가 아닌 값이 오면 그것은 분 단위 cron 이 만든 값이 아니다."로 좁힌다 — 실제로 남아 있는 두 단언(범위·분 경계)이 하는 일만 서술하게 된다.

- **[WARNING]** plan 문서의 「할 것」·체크리스트가 1라운드 중간 설계("생성 cron 값과 비교")를 그대로 남겨 두어, 최종 코드("옛 값과 비교하지 않는다")와 정면으로 어긋난다
  - 위치: `plan/in-progress/schedule-cron-flake.md:33-34`(`2. 판정을 «다르다» 에서 «새 cron 이 만드는 값인가» 로 바꾼다 — PATCH 응답의 \`nextRunAt\` 이 요청 시점부터 1분 안(부하 여유 30초) 이고 초 자리가 0(분 경계)이며, 생성 cron 의 값은 그 창 밖임을 함께 단언한다.`), `:52-53`(체크리스트 `(2) 같은 뮤턴트 + **옛 «다르다» 단언 제거** → 새 «1분 안» 단언 홀로 잡는다`)
  - 상세: 이 문장들은 1라운드 조치(`a8ddcfb32`)가 이 plan 파일에 써 넣은 것으로, 그 시점 코드는 실제로 "생성 cron 의 값은 그 창 밖임을 함께 단언"하는 `expect(new Date(originalNext...)).toBeGreaterThan(...)` 을 갖고 있었다. 그런데 2라운드(`b40b5b98f`)가 이 단언을 코드에서 완전히 제거했고(위 첫 번째 발견사항 참고), 그 커밋은 `plan/in-progress/schedule-cron-flake.md` 를 2줄만 고쳤다(체크리스트의 "등재한다"→"등재했다"). "할 것" §2 문장과 체크리스트 (2)항은 갱신되지 않아, 지금 이 plan 파일을 읽으면 "PATCH 뒤 값이 요청 시점부터 1분 안이고 **생성 cron 의 값이 그 창 밖임을 함께 단언한다**"는, 실제로는 존재하지 않는 단언이 최종 설계인 것처럼 서술된다. 바로 그 코드의 JSDoc(`schedule-trigger.e2e-spec.ts:292`)은 "**옛 값과 비교하지 않는다**"고 명시적으로 반대로 말한다 — 같은 작업을 설명하는 두 문서(코드 JSDoc과 plan)가 서로 다른 최종 설계를 주장하는 상태다. 이 plan은 체크리스트 마지막 항목("트래커 해소 · 이 plan `plan/complete/`로")이 완료되면 그 내용 그대로 `plan/complete/`에 영구 보존되므로, 지금 정정하지 않으면 이 모순이 그대로 완료 기록으로 남는다.
  - 제안: `--impl-done` 전에 "할 것" §2의 "생성 cron 의 값은 그 창 밖임을 함께 단언한다" 절과 체크리스트 (2)의 "새 «1분 안» 단언 홀로 잡는다"는 표현을, 2라운드에서 그 단언 자체가 반증되어 완전히 제거됐다는 사실(및 근거: `review/code/2026/09/20/12_17_18/testing.md` WARNING 1 · `review/code/2026/09/20/12_17_18/RESOLUTION.md` W1)까지 반영해 갱신한다.

## 확인했으나 문제 없음

- 「실측」 인용 정확성(1라운드 WARNING 3) — 재확인 결과 **정정 유지**. `schedule-trigger.e2e-spec.ts:287`, `plan/in-progress/schedule-cron-flake.md:21-22` 모두 `plan/in-progress/spec-draft-nullable-notation-followups.md`(해당 항목, 4960-4965행)와 `review/code/2026/09/20/09_35_16/RESOLUTION.md`(27행 — 실제로 `_test_logs/e2e-20260920-095855.log`·09:58/10:02 KST 재실행 366 통과 기록이 있음을 직접 열어 확인)를 정확히 가리킨다.
- 트래커의 조기 「해소」 선인용(2라운드 WARNING 1) — 재확인 결과 **되돌려짐 확인**. `plan/in-progress/spec-draft-nullable-notation-followups.md:4960-4965`의 해당 항목은 여전히 `- [ ]`(미완)이고 "해소"·`plan/complete/schedule-cron-flake.md` 문구가 없다.
- `NAV-WF-02` 플래너 등재(2라운드 INFO 1) — 재확인 결과 **등재 완료**. `plan/in-progress/spec-draft-nullable-notation-followups.md:4927-4931`에 실제 항목이 있고, `plan/in-progress/schedule-cron-flake.md:51`의 문구도 "등재한다"(예정)에서 "등재했다"(완료)로 정정됐다.
- 모듈 최상단 JSDoc 이 D 케이스의 flake 이력을 언급하지 않는 점(1·2라운드 INFO, 조치 불요로 기처분) — 케이스별 JSDoc(283-294행)이 맥락을 충분히 담고 있어 여전히 조치 불요로 판단, 재-flag 하지 않음.
- README·API 문서·CHANGELOG·환경변수 문서·예제 코드 — 이번 누적 diff는 e2e 테스트 비교식·cron 리터럴 + plan/tracker/리뷰 산출물 갱신뿐이라 해당 없음(서비스 코드·API 계약·설정 변경 없음).
- `review/code/2026/09/20/{11_54_10,12_17_18}/*`·`review/consistency/2026/09/20/11_21_16/*` — 워크플로가 그 시점 상태를 기록한 1회성 산출물이며 `spec/conventions/review-citations.md` §3이 인용 규약 적용 대상에서 제외한다. 내용도 대응하는 실제 커밋(`a8ddcfb32`, `b40b5b98f`)의 diff와 일치함을 확인했다.

## 요약

핵심 코드(`schedule-trigger.e2e-spec.ts`)는 1·2라운드에서 지적된 인용 오류·조기 완료 선언을 실제로 정정했지만, 2라운드가 "옛 값 비교를 통째로 뺀다"며 코드에서 `originalNext` 단언을 삭제한 바로 그 커밋(`b40b5b98f`) 안에서, 그 단언을 정당화하던 전제("생성 cron 값은 몇 달 뒤라 창 밖")가 클래스 JSDoc(290행) 두 곳과 인라인 주석(315-317행)에 그대로 남아 같은 문서 블록의 다른 문장(292-294행 "옛 값과 비교하지 않는다")과 정면으로 모순된다 — 삭제된 코드의 근거만 남고 그 코드는 사라진 형태다. 같은 결의 staleness가 `plan/in-progress/schedule-cron-flake.md`의 "할 것"·체크리스트에도 있다 — 1라운드 중간 설계("생성 cron 값과 비교")가 2라운드의 완전 제거를 반영하지 못한 채 남아, plan을 읽는 사람과 코드 JSDoc을 읽는 사람이 서로 다른 최종 설계를 알게 된다. 이 plan은 완료 시 `plan/complete/`에 그대로 보존되므로 지금 정정이 필요하다. 그 외 1·2라운드가 잡았던 인용 오류·조기 "해소" 선언·NAV-WF-02 등재 누락은 모두 실제로 고쳐졌음을 재확인했고, README/API/CHANGELOG/설정 문서 갱신은 이번 범위에 해당하지 않는다.

## 위험도

LOW
