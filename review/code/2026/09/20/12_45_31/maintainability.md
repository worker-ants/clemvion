# 유지보수성(Maintainability) 리뷰

## 리뷰 범위

- `codebase/backend/test/schedule-trigger.e2e-spec.ts` — 「D. PATCH cron → nextRunAt 재계산」 케이스: cron 리터럴 교체(`0 10 * * *` → `0 0 1 1 *`), `originalNext` 선언·`not.toBe(originalNext)` 단언 완전 제거, `patchedAt` 캡처 + "새 cron 이 만드는 값인가"를 보는 3단언(하한/상한/분경계 초=0)으로 대체. JSDoc 신규 추가(283-295행). (`git diff origin/main HEAD` 로 최종 누적 diff 대조 확인 — 1·2라운드 조치가 모두 반영된 상태)
- `plan/in-progress/schedule-cron-flake.md` — plan 문서(3라운드 동안 동일 파일이 세 커밋에 걸쳐 갱신, 최종 diff 는 신규 파일 생성 형태)
- `plan/in-progress/spec-draft-nullable-notation-followups.md` — 트래커 항목 2건 신규 등재(§4924 부근)
- `review/code/2026/09/20/11_54_10/*`, `review/code/2026/09/20/12_17_18/*`, `review/consistency/2026/09/20/11_21_16/*` — 이전 라운드 워크플로 산출물(자동 생성, 유지보수 대상 아님)

뮤테이션 없이 읽기 전용으로 분석했다 (`git status --short` 는 이 세션의 `review/code/2026/09/20/12_45_31/` 신규 생성만 보이며, 대상 코드 파일에 대한 변경 없음).

## 발견사항

- **[INFO]** 2라운드에서 지적됐던 "반대 방향 경계 두 곳에 같은 매직 넘버 `90_000` 이 중복" 문제는 이번 3라운드 조치(하한 비교 자체를 제거)로 **해소됨을 확인** — 현재 `30_000`/`90_000` 은 각각 한 곳에만 등장한다(`grep` 확인). 발견사항 아님, 확인만 기록.
  - 위치: `codebase/backend/test/schedule-trigger.e2e-spec.ts:319`(하한), `:320`(상한)

- **[INFO]** 허용 오차 상수(`30_000`/`90_000`)가 이름 없는 인라인 리터럴로 남아 있다 — 1·2라운드에서 이미 다뤄진 항목과 같은 패턴이며 새 결함은 아니다.
  - 위치: `codebase/backend/test/schedule-trigger.e2e-spec.ts:319-320`
  - 상세: 같은 파일 기존 케이스(A, `Date.now() - 60_000` 스타일)도 인라인 리터럴 + 근거 주석 조합을 쓰고, 316-318행 주석이 두 값의 근거(60초 분 경계 + 30초 e2e 부하 여유)를 충분히 설명한다.
  - 제안: 조치 불요. 세 번째 등장 시 `MINUTE_CRON_TOLERANCE_MS` 류 이름 있는 상수화 고려(기존 제안과 동일, 우선순위 낮음).

- **가독성/구조 (양호, 발견사항 아님)**: `originalNext`/`not.toBe` 제거로 케이스 본문이 "새 cron 관점의 단언 3개"로만 구성돼 오히려 1·2라운드보다 **단순해졌다** — 판정 로직이 두 갈래(새 값인가 / 옛 값이 창 밖인가)에서 한 갈래로 줄며 향후 유지보수자가 "이 창이 왜 있는가"를 추적할 표면이 줄었다. JSDoc(283-295행)이 "왜 옛 값과 비교하지 않는가"를 실측·회귀 이력과 함께 명시해, 코드만 보고는 드러나지 않는 설계 의도를 보완한다. 함수 길이(`it()` 본문 약 30줄), 중첩 깊이(0단, 순차 statement), 네이밍(`patchedAt`/`nextRunMs`) 모두 파일 내 다른 테스트 대비 과도하지 않다.

- **plan/tracker 문서**: 발견사항 없음. `schedule-cron-flake.md` 는 저장소 plan 템플릿을 따르고 3라운드에 걸친 조치 이력(커밋 SHA·근거 문서 인용)이 남아 있어 재구성이 쉽다. `spec-draft-nullable-notation-followups.md` 신규 등재 2건도 트래커의 기존 항목 포맷(담당·우선순위·등재일·근거)과 일관적이다.

## 요약

3라운드에 걸친 누적 diff 는 e2e 테스트 한 케이스의 판정 로직을 "달라졌다"(대리 지표) → "새 cron 이 만드는 값인가"(직접 신호)로 재작성한 것에 국한되며, 각 라운드 조치가 이전 라운드의 지적(연말 겹침 창, 매직 넘버 중복)을 실제로 해소했음을 코드 상태로 확인했다. 최종 상태는 오히려 이전 라운드보다 단언 구조가 단순하고(3단언, 단일 판정 축), 매직 넘버 중복도 사라졌다. 가독성·네이밍·함수 길이·중첩·복잡도·일관성 모두 파일 기존 스타일과 일치하며 CRITICAL/WARNING 급 발견사항은 없다.

## 위험도

NONE
