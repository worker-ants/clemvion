# RESOLUTION — schedule-trigger 「D. PATCH cron」 flake (3라운드)

SUMMARY: Critical 0 · Warning 4 · INFO 4.

## 정지 규칙 조정 (결과를 본 뒤가 아니라, 조치 성격이 정해진 뒤에)

1라운드 전 선언: «Critical · Warning 0 이거나 `codebase/` 수정 0 인 라운드에서 수렴, 최대 3라운드». 이 라운드의 조치는 **주석 ·
plan 서술 정정뿐**인데, 그 주석이 `codebase/` 안에 있어 게이트 freshness 가 재무장된다 — 즉 «문서만 고쳐도 라운드가 하나 더
필요한» 구조다. 그래서 상한을 **4라운드**로 한 번 늘리고, **4라운드는 검증 라운드로 그 결과와 무관하게 수렴**한다(남는 것은
등재). 무한 연장을 막기 위해 지금 못 박는다.

## 조치 항목

| SUMMARY # | 처분 | 커밋 |
|---|---|---|
| W2 JSDoc 이 «재계산이 없었다면 생성 cron 값이라 이 창 밖으로 떨어진다» 고 단정 — 두 줄 뒤 서술과 모순 | **고침.** «거의 언제나 이 창 밖이다» 로 낮추고, 남는 반대 방향 창(연말 ~2분)을 그 자리에 적었다 | `5d551ad73` |
| W3 인라인 주석이 2라운드에 **삭제된** 단언의 근거를 계속 설명 | **고침.** «판별 대상인 생성 cron 값은 몇 달 뒤라…» 절을 지우고, 남은 두 단언(1분 안 · 분 경계)만 설명하게 했다 | `5d551ad73` |
| W4 plan 「할 것」 §2 · 체크리스트가 1라운드 중간 설계를 그대로 담고 있다 | **고침.** §2 에서 «생성 cron 값과 비교» 를 취소선으로 남기고 폐기 근거(2라운드 W1 · 그 RESOLUTION)를 인용. 체크리스트의 뮤턴트 서술도 실제 이력(단언이 줄어드는 동안 매번 재확인)으로 | `5d551ad73` |
| W1 반대 방향 잔여 — 연말 ~2분(12/31 23:58:30 ~ 01/01 00:00:30 KST)에는 재계산이 없어도 통과한다(거짓 **통과**) | **등재(수렴 예외 (a)~(d)).** (a) 동작 결함이 아니라 테스트 판별력의 좁은 구멍이고, 방향이 반대라 CI 를 붉게 만들지 않는다 — 이 PR 이 없앤 «하루 1분 거짓 실패» 와 성격이 다르다. (b) 닫으려면 시각을 고정해야 하는데 e2e 는 실 서버라 불가능하다 — 닫는 자리는 `computeNextRuns` 를 spy 로 보는 **단위 테스트**이고 이미 트래커 항목이다. (c) 이 절이 근거다. (d) 그 트래커 항목에 이번 실측(연말 창)을 **합쳐서** 적었다 — 새 항목을 늘리지 않고 닫을 자리를 하나로 모은다. 코드 주석에도 잔여와 닫는 자리를 남겼다 | 마무리 |
| INFO 1 · 2 · 3 · 4 | 조치 없음 — 1: spec 은 타이밍 허용오차를 규정하지 않는다(위반 아님) · 2: 이미 등재된 그 단위 테스트 항목 · 3 · 4: 앞선 라운드와 같은 처분(기존 패턴) |

## 보류·후속 항목

- `plan/in-progress/spec-draft-nullable-notation-followups.md` «cron 재계산 happy-path 의 결정적 단위 테스트» — 이번 W1 의 연말
  창 실측을 그 항목에 합쳐 적었다. 그 단위 테스트가 들어오면 이 e2e 의 남은 구멍이 의미를 잃는다.

## TEST 결과

- lint: 통과
- unit: 통과
- build: 통과
- e2e: 통과 (366)
