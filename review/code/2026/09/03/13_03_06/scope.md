# 변경 범위(Scope) 리뷰

## 컨텍스트 확인

`git merge-base HEAD origin/main` = `af41a3c6e`. `git diff origin/main...HEAD --stat` 로 실측한
결과 이 리뷰 페이로드(54개 파일)는 4개 커밋의 누적 diff와 정확히 일치한다:

- `69aad5d5d` — plan 이 선언한 이월 INFO 5건(cutoff clamp 주석·`expiryTimers` non-optional화·
  `MSG_AUTH_TOKEN_EXPIRING` 상수 승격·`armExpiryTimers` 선제 해제·`.unref()`) 이행
- `b75e6a76b` — 1R 리뷰(JSDoc 오귀속 2건 · 조기 `return` 순서 결함) 수정
- `80ac92668` — 2R 리뷰("런북에서 추적 중" 거짓 주장) 수정 + 런북 항목 신설
- `a1984f196` — 3R 리뷰(테스트 순서 미검증 · 라벨 재사용) 수정, **이번 라운드에서 처음 검토**

이전 3개 라운드(`11_57_58`·`12_16_24`·`12_40_10`)의 scope 리뷰가 각각 독립적으로 NONE 을 냈고,
직접 소스(`Read`/`grep`)로 대조해 그 결론이 현재도 유효함을 재확인했다:

- `websocket.gateway.ts` — `armExpiryTimers` JSDoc(§1.2 근거)이 그 함수 바로 위에, `clearExpiryTimers`
  자체 JSDoc 이 그 메서드 바로 위에 정상 인접(`:161-175`, `:230-234`). 선제 `clearExpiryTimers(client.id)`
  가 조기 `return` **앞**에 위치(`:184`). `notice.unref(); cutoff.unref();` 두 줄 존재 확인(`:224-225`).
- `websocket-events.types.ts` — `AuthTokenExpiredPayload` JSDoc 과 `MSG_AUTH_TOKEN_EXPIRING` JSDoc 이
  각각 자기 대상 선언 바로 위에 정상 배치(`:274-303`, `:305-311`).
- `websocket.gateway.spec.ts` — 신규 테스트 5건(상수 일치·재무장 exp有·재무장 exp無·cutoff 음수
  clamp+순서·unref) 전부 plan 이 선언한 5개 이월 항목 또는 그 항목에 대한 리뷰 재발 수정에
  1:1 대응. import 추가(`MSG_AUTH_TOKEN_EXPIRING`)는 그 자리에서 즉시 소비됨(미사용 임포트 없음).
- import 재정렬(`websocket.gateway.ts:23-27`)은 신규 심볼 추가에 종속된 최소 변경.
- `codebase/frontend`, 다른 backend 모듈, 설정 파일(`package.json`, CI, lint config 등) 어디에도
  손대지 않음.
- review 산출물 33개 파일(`review/code/**`, `review/consistency/**`)은 CLAUDE.md 가 명시한 저장
  위치·developer SKILL 의 "구현 완료 후 `/ai-review` 상시 강제" 규약에 따른 정상 부산물.

## 발견사항

- **[WARNING]** `a1984f196` 이 plan 문서에 리뷰 링크를 삽입하면서 인용문(blockquote) 한 문장을
  둘로 쪼개 그 사이에 끼워 넣었다 — blockquote 마커·들여쓰기가 빠져 렌더링도 깨지고 문장도
  끊긴다
  - 위치: `plan/in-progress/ws-token-expired-socket-lifetime-impl.md:190-194`
  - 상세: 190~192행과 194행은 `      > ` (6칸 들여쓰기 + `>`)로 시작하는 하나의 인용 문단이고,
    원래 의도된 문장은 "...그 자리의 런북 항목 2건은 **다른 주제**...였다. **추적한다고
    적으면서 추적처를 만들지 않았다.**" 이다. 이번 diff 가 `12_16_24/SUMMARY.md` 링크
    참조를 추가하며 그 문장 한가운데 — "…였다."와 "추적한다고" 사이 — 새 줄을 끼워 넣었는데,
    그 줄(193행 `> 상세: ... 추적한다고`)만 들여쓰기와 `>` 마커가 없다. 실제 파일을 `Read` 로
    직접 열어 확인한 원문:
    ```
    190:      > **이 항목은 리뷰가 내 거짓 주장을 잡아 만들어졌다.** 서브사이클 `11_57_58` RESOLUTION 에서 이 트레이드오프를
    191:      > *"배포 런북에서 별도 추적 중"* 이라고 썼는데, 2R reviewer 가 이 plan 을 실측해 보니
    192:      > 그 자리의 런북 항목 2건은 **다른 주제**(재연결 스파이크·배포 전환 창)였다.
    193:> 상세: [`12_16_24/SUMMARY.md`](../../review/code/2026/09/03/12_16_24/SUMMARY.md) 추적한다고
    194:      > 적으면서 추적처를 만들지 않았다.
    ```
    결과적으로 (1) 193행은 마크다운 blockquote 밖의 별도 단락으로 렌더링되어 190~192행 인용문이
    조기 종료되고, (2) "추적한다고"가 "적으면서 추적처를 만들지 않았다"에서 분리되며 그 사이에
    무관해 보이는 "상세: [링크]"가 끼어 원문 문장을 두 조각으로 갈라놓는다. 링크를 추가하려는
    의도(회귀 이력 상호 참조 보강, 이 라운드의 다른 곳에서는 정상적으로 잘 쓰인 패턴 —
    `:27-30`의 "라운드 라벨 범례" 참고)는 이 diff 의 정당한 목적이지만, 삽입 지점을 문장
    중간이 아니라 문장 끝(194행 뒤)으로 잡았어야 했다. `git blame`/diff 로 확인한 결과 이 변경은
    `a1984f196`(3R, "라벨을 세션 타임스탬프로 바꾸고 SUMMARY 링크를 보강했다")에서 처음
    도입됐고, 그 시점 이후 아직 어느 리뷰 라운드도 이 특정 hunk 를 검토하지 않았다(11_57_58·
    12_16_24·12_40_10 는 모두 이 커밋보다 먼저 실행됨) — 이번 라운드가 처음 포착한 항목이다.
  - 제안: 193행의 링크 참조를 194행("적으면서 추적처를 만들지 않았다.") **뒤**로 옮기고, 옮긴
    줄에도 `      > ` 접두를 붙여 인용 블록 형식을 유지한다. 예:
    `      > 적으면서 추적처를 만들지 않았다. 상세: [\`12_16_24/SUMMARY.md\`](../../review/code/2026/09/03/12_16_24/SUMMARY.md)`

## 그 외 확인한 항목 (문제 없음)

- 3R 커밋(`a1984f196`)의 `codebase/` 변경은 `websocket.gateway.spec.ts` 7줄(`invocationCallOrder`
  단언 추가)뿐이며, 커밋 메시지의 "프로덕션 코드는 이 라운드에서 안 바뀌었다" 서술과 실측이
  일치한다(`git show --stat` 로 `websocket.gateway.ts`/`websocket-events.types.ts` 가 diff 목록에
  없음을 확인).
- `#1270`(존재하지 않는 PR 번호 예측) 참조는 `grep` 결과 저장소 어디에도 남아있지 않다 — 이번
  라운드가 주장하는 정리가 실제로 완료됨을 확인.
- 나머지 review/consistency 산출물(33개 파일)은 애플리케이션 코드가 아니며, 프로젝트가 명시한
  저장 규약(`review/code/**`, `review/consistency/**`)과 정확히 일치하는 경로에 놓여 있다.

## 요약

핵심 코드 변경(3개 소스 파일)은 plan 이 선언한 이월 INFO 5건과 그에 대한 3차례 리뷰의 재발
수정에 정확히 대응하며, 4개 커밋 전부가 단일 작업 범위 안에서만 움직인다 — 프로덕션 코드
스코프 이탈, 불필요한 리팩토링, 기능 확장, 무관한 파일·설정 변경은 발견되지 않았다(3개
선행 라운드의 독립 NONE 판정을 직접 소스 대조로 재확인). 다만 가장 최근 커밋(`a1984f196`)이
plan 트래커 문서에 리뷰 링크를 추가하면서 기존 인용 문장을 형식·의미 양쪽에서 훼손했다 —
아직 어느 리뷰 라운드도 짚지 않은 새 결함이라 WARNING 으로 기록한다. 코드 자체에는 영향이
없고 plan 문서 한 곳의 마크다운 렌더링·가독성 문제에 그친다.

## 위험도

LOW
