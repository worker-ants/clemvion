# RESOLUTION — WS 이월 INFO 정리 서브사이클 `12_40_10`

대상 SUMMARY: 위험도 **LOW** · Critical **0** · Warning **2** · INFO 15

**WARNING 2건 조치.** 둘 다 **reviewer 가 실측으로 뒷받침**했고, 그중 하나는 내가 직전
라운드에 추가한 테스트의 사각이었다.

## W1 (testing) — 존재만 단언하고 **순서**를 안 봤다

직전 라운드(`12_16_24` INFO#4)에서 추가한 *"이미 만료된 exp 로 연결하면 즉시 끊는다"* 테스트가
`emit`·`disconnect` **각각 호출됐다**는 사실만 단언했다.

`exp` 가 완전히 과거면 두 지연이 **모두 0 으로 클램프**되고, 그 tie 상태에서 Node 는
**등록 순서**로 실행 순서를 정한다. 즉 두 `setTimeout` 블록을 뒤바꾸는 리팩터가
**"통지 없이 먼저 끊기는"** 동작을 조용히 만들 수 있는데 테스트가 안 걸린다.

> **reviewer 가 그 뮤턴트를 직접 걸어 생존을 확인했다** — 필터 없는 전체 스위트(72개)를
> 3회 반복해서. 추측이 아니다.

`mock.invocationCallOrder` 로 `emit → disconnect` 순서를 단언하고, **같은 뮤턴트**로 확인했다:

| 뮤턴트 | 조치 전 | 조치 후 |
|---|---|---|
| `notice`/`cutoff` 등록 순서 교환 | **생존**(reviewer 실측) | **RED 1** |

## W2 (documentation) — 같은 라벨이 15줄 간격으로 다른 것을 가리켰다

`리뷰 2R W1` 이 **두 리뷰 사이클**의 무관한 두 발견에 쓰였다:

| 위치 | 실제 대상 | 출처 사이클 |
|---|---|---|
| `:158` | 만료 타이머 **지터**(performance) | 원 PR `#1266` 5라운드 리뷰 |
| `:169` | **셧다운 중 콜백 미실행**(side_effect) | 이월 INFO 정리 서브사이클 |

직전 라운드가 **커밋 메시지 수준**에서 지적한 라벨 재사용과 같은 병인데, 이번엔 **plan 본문
안에서 더 좁은 간격**으로 재발했다. 지적을 받고도 같은 형태를 한 칸 옆에서 다시 만들었다.

서브사이클 라운드를 **세션 타임스탬프**로 식별하도록 바꾸고(`11_57_58`·`12_16_24`·`12_40_10`),
plan 상단에 **두 라벨 체계가 다르다는 범례**를 넣었다. `12_16_24` SUMMARY 링크도 함께 보강해
두 서브라운드의 문서화 수준 비대칭(INFO#14)을 없앴다.

## 부수 — 존재하지 않는 PR 번호를 예측했다

범례를 쓰면서 이 작업을 `#1270` 으로 지칭했는데 **그 PR 은 아직 없다.** 병합 시 번호가 달라지면
문서가 조용히 틀린다. 두 곳 모두 번호 대신 문서 내 참조로 바꿨다.

## 미조치 (판단 유지)

- **INFO#1·#2** `unref` 셧다운 트레이드오프 · `connectionStateRecovery` 시 id 재사용 —
  4명이 독립 확인했고 **둘 다 plan 에 재개 신호와 함께 등재**돼 있다(전자는 이번 사이클에
  내가 추가한 항목).
- **INFO#10** `Math.max(0,…)` 주석이 두 곳에 유사 반복 — 2곳뿐이라 둔다. 3번째가 생기면 통합.
- **INFO#11** `connectWithExp` 헬퍼를 `exp` 생략까지 지원하도록 확장 — 1회성이라 rule-of-three
  미달. reviewer 도 "지금 강제 아님" 으로 적었다.
- **INFO#13** `exp: NaN`/`Infinity` — **3라운드 연속** 같은 판단이다. `undefined` 와 **동일한
  조기 return 경로**라 같은 분기를 세 번째 입력으로 다시 덮는 것뿐이다.
- **INFO#12** 상수 시제(`EXPIRING` vs `EXPIRED`) — 1R 에서 검토·기각했다. 이벤트명은 wire
  계약이라 못 바꾸고, 상수만 맞추면 "통지 시점은 아직 만료 전" 이라는 사실이 흐려진다.

## 검증

lint **PASS** · docs·링크 가드 **3157** · backend ratchet **198/37** ·
websocket gateway **72/72** · 뮤테이션 **5축 RED + 1축 의도적 생존**(clamp).
