# RESOLUTION — `18_45_57` (Critical 0 · WARNING 2 · RISK LOW)

## W1 (maintainability) — 반영

**지적**: 좌표계 표에서 떨어진 산문(§1.1 등)이 행 번호를 **접미사 없는 맨숫자**로 인용해,
*"값 vs 행 번호"* 오독이 남는다 — **이 문서가 스스로 경계하는 바로 그 부류**이고,
consistency `18_14_45` 의 CRITICAL(`= 1` 을 리터럴 1 로 읽음)과 같은 종류다.

**정확한 지적이다.** 같은 모호성을 두 라운드에 걸쳐 두 번 만들었다 — 처음엔 표 안에서
(CRITICAL), 이번엔 표 밖 산문에서. 전수로 고쳤다:

- 행을 지칭하는 모든 산문·표 셀·콜아웃을 **"표 N행"** 으로 통일(9곳)
- §1 표 아래 콜아웃에 **표기 규칙 자체를 명문화** — *"본 문서의 산문은 행을 지칭할 때 항상
  '표 N행' 으로 적는다"*. 규칙을 적어 두지 않으면 다음 편집자가 같은 것을 세 번째로 만든다.

## W2 (documentation) — 반영

**지적**: plan 체크리스트가 `code:` 를 "4파일" 이라 적었는데 바로 다음 줄에 6개가 나열됐다
(consistency W1 반영으로 2개 추가된 뒤 개수 라벨만 안 고쳤다).

**"6파일(정의처 4 + `18_27_11` W1 로 추가한 스캐너 2)"** 로 정정 — 개수만 고치지 않고
**왜 6이 됐는지**를 남겼다. 실제 spec `code:` 도 6개로 일치함을 확인했다.

## INFO 18건 — 조치 안 함

대부분 "조치 불요" 명시이거나 긍정 확인이다. 판단이 필요했던 셋:

| # | 항목 | 처분 |
| --- | --- | --- |
| 12 | §1.1 순서 규율이 표 3행 스캐너만 인용 | **W1 수정에 흡수** — "두 스캐너(표 2행 `hasMaskedLeaf` · 표 3행 `hasMaskedMarkerLeaf`)" 로 함께 인용하도록 확장했다 |
| 13 | 새 소비처 추가 절차 예시 없음 | 안 넣는다. 이 문서는 **좌표계 사실**을 소유하지 절차를 소유하지 않는다 — 예시를 넣으면 코드가 바뀔 때 예시도 stale 해지는 표면이 하나 더 는다 |
| 4 | conventions ↔ 5-system 양방향 참조 누적 | 리뷰어가 `node-cancellation.md`↔`execution-context.md` 선례와 일치한다고 확인했다. 세 번째 유사 문서가 생기면 단방향으로 충분한지 재검토 |

권장 조치사항 4번(`ws-event-types-extract.md` 의 `TerminalErrorPayload` 전수 확인)은 리뷰어도
*"이번 PR 책임 범위 아님"* 이라 적었다. 그 미확인 사실은 신설 문서 §2 에 **범위 caveat 로
명시**돼 있고, 형제 plan 에 추적 항목이 살아 있다.

## 라우팅 메모

`routing_status=fallback-distrusted-decision` — 라우터 결정이 신뢰되지 않아 **14명 전원**이
돌았다. forced 7명 전원 결과 확보. 누락 없음.
