# RESOLUTION — `21_15_53`

**Critical 0 · Warning 2 · INFO 7.** Warning 2건 모두 실측으로 확인하고 수정했다.

## Warning #1 · #2 — plan 의 트래커 줄 번호 인용이 stale (documentation)

### 실측 확인 — 지적이 맞다

리뷰어 주장을 `sed -n` 으로 직접 대조했다(리뷰어 판정도 실측 대상이다):

| 인용 | 최종 파일에서 실제 내용 | 판정 |
| --- | --- | --- |
| `L868` (throwIfAny 항목이라 주장) | *"남은 산문 재기술은 JSDoc 2곳이다"* — 무관한 항목 | ❌ stale |
| `L826-827` (② 유예 근거라 주장) | ③ `reRun` 141줄 실측 노트 | ❌ stale |
| 실제 throwIfAny 항목 | **L888** | 리뷰어 지적대로 |
| 실제 ② 본문 | **L831-832** | 리뷰어 지적대로 |

원인은 리뷰어가 짚은 그대로다 — 이 PR **자신이** 같은 트래커의 **위쪽 항목들에 줄을
삽입**해서(② 유예 근거 12줄 · ③ 실측 노트 4줄) 아래 인용이 전부 밀렸다. base 커밋 기준으로는
맞던 숫자였다.

### 수정 — 숫자 정정이 아니라 **인용 방식 교체**

리뷰어는 *"`L888` 로 정정하거나 앵커 문구로 바꾼다"* 를 제안했다. **후자를 택했다.**

정본 트래커는 앞으로도 계속 편집되는 살아 있는 문서다. 숫자로 고쳐 두면 **다음 PR 이 위쪽에
한 줄만 넣어도 같은 방식으로 또 틀린다** — 이번에 그 일이 실제로 일어난 것이 증거다. 항목
제목을 앵커로 쓰면 줄이 밀려도 유효하고, `grep` 으로 바로 찾힌다.

```diff
-- [x] ① … → **트래커 L868 항목 종결**
+- [x] ① … → 트래커 항목 **`throwIfAny` 의 phase 경계 트레이드오프 미검증** 종결

-- [x] ② 유예 근거 교체 (트래커 L826-827) · …
+- [x] ② 유예 근거 교체 (트래커 **`findMaskedResubmissions` 직접 단위 테스트 부재** 항목) · …
```

**전수 확인**: 두 plan 파일에서 줄 번호 인용을 다시 훑어 남은 것이 없음을 확인했다
(`swagger.md L256/L257` 은 이 PR 이 편집하지 않는 외부 파일 인용이라 밀리지 않는다).

## INFO #2 — `allReasons` 헬퍼 추출: **하지 않는다**

리뷰어가 *"여유가 있을 때 … 향후 3번째 복붙을 방지"* 로 낸 비차단 제안이다. 지금 하지 않는
이유는 **인스턴스가 2개**라 rule of three 미달이고, 이 PR 이 선언한 범위(테스트 1건 추가 +
트래커 재판정) 밖의 리팩터이기 때문이다. 리뷰어 자신도 *"지금 블로킹 사안 아님"* 으로 적었다.

같은 이유로 이 PR 은 `ExecutionsService.reRun` 리팩터도 손대지 않았다 — scope 리뷰어가
*"plan 이 '이 PR 밖' 으로 명시한 리팩터를 실제로 손대지 않았다"* 로 확인했다.

## 나머지 INFO 5건 — 조치 불요

전부 확인성 기록이다. 특히:

- **INFO #1** (security/requirement/testing 합류): 신규 캐너리가 고정한 트레이드오프가 실제
  구현·spec(`1-manual-trigger.md` §6 Rationale) 과 정확히 일치하며 **대조군 포함으로 vacuous
  하지 않음**을 리뷰어들이 독립 확인했다.
- **INFO #4**: `findMaskedResubmissions` 유예의 **교체된 근거**(분기 6종 표)가 실제 함수
  로직과 일치함을 리뷰어가 대조 확인했다 — 근거 교체가 실측에 근거했음을 뒷받침한다.
