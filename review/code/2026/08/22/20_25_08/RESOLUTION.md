# RESOLUTION — `20_25_08` (Critical 0 · WARNING 1 · RISK LOW)

## W1 (requirement) — 반영(정정 기록) · 추가 축약은 안 함

**지적**: description 이 3차 축약(304 → 236 → 129자)을 거쳤어도 적용 규약
(`swagger.md` §3 **"DTO `description`은 10~40자 내외"**)을 여전히 3배 초과한다. 그리고
**내 커밋 메시지의 *"길이 가이드 안(129자)으로 들어갔다"* 는 잘못된 줄을 대입한 결과**다 —
그 150자는 `@ApiOperation` 엔드포인트 레벨 규정이다.

### 지적이 맞다 — 나도 같은 것을 독립적으로 발견했다

`swagger.md` 에는 길이 줄이 **둘** 있다:

| 줄 | 대상 | 상한 |
| --- | --- | --- |
| L256 | **DTO `description`** ← 이 필드에 적용 | 10~40자 |
| L257 | 엔드포인트 `summary`/`description` | 50~150자 |

나는 L257 을 보고 *"150자 안"* 이라 적었다. **문서는 맞게 열었는데 줄을 잘못 봤다.**
정정을 트래커에 명시했다(⚠️ 표시로 무엇을 왜 틀렸는지 포함).

### 그런데도 추가 축약은 하지 않는다 — 실측 근거

`origin/main` 기준(이 PR 변경 **전**) 같은 파일의 실측:

| 필드 | 길이 | 가이드(10~40) 대비 |
| --- | --- | --- |
| `inputOverride` | **98자** | 2.5배 초과 |
| `dryRun` | **174자** | 4.4배 초과 |

**이 파일은 내 변경 전부터 가이드 밖에 있었다.** 40자 안에 넣으려면 캐비엇(마커 정확 일치 →
`400 MASKED_VALUE_RESUBMITTED`)을 지워야 하는데 **그 캐비엇을 넣는 것이 이 항목(785)의
목적**이라 자기모순이다.

옳은 지렛대는 **규약 문면 쪽**이다 — §3 의 길이 예외가 *"**응답** 값이 저장된 값과 다를 수
있는 필드"* 로 한정돼 있어 **요청 필드의 보안·거부 캐비엇**을 포괄하지 못한다. 그 확장은
규약 개정이라 **planner 턴**이고, 트래커에 실측(98/174자)과 함께 등재했다.

> 두 게이트 다 이 항목을 *"실행 계약 불변, 긴급하지 않음, 이번 PR 을 막지 않음"* 으로
> 판정했고 권장 조치 1번이 **"트래커에 보완 기록"** 이다 — 그것을 이행했다.

## INFO 10건 — 조치 안 함

전부 "조치 불요"이거나 이전 라운드 기처분 재확인이다. 값진 확인 둘:

- **INFO 3** — `masked-reject-callers-guard` 가 `ts.isIdentifier` 노드만 보므로 JSDoc trivia 를
  오탐하지 않고, `resolve-trigger-parameters.ts` 의 함수 선언 식별자는 `ALLOWED_DIRECT_CALLERS`
  에 이미 등재돼 있음을 리뷰어가 **가드 소스로 직접** 확인했다. 내 뮤테이션 2종 결과와 일치.
- **INFO 6** — 봉인된 `plan/complete/masked-marker-cosmetic-followups.md` 가 이후 3커밋 이력을
  담지 않는 것은 **lifecycle 관행과 일치하는 의도**(봉인 후 append 금지, 후속은 in-progress
  트래커로)라고 판정됐다 — 직전 라운드의 지적을 그 방식으로 처리한 것이 맞았다는 확인이다.

## 수렴 판정

| 라운드 | WARNING | 성격 |
| --- | --- | --- |
| `19_25_39` | 2 | 내가 만든 회귀(docblock 언어) + 형제 엔드포인트 비대칭 |
| `19_36_12` | 1 | 미머지 PR 을 근거로 삼음 |
| `20_05_07` | **0** | — |
| `20_25_08` | 1 | 규약 문면 vs 실무 괴리 + **내 서술 오류 정정** |

발견이 **코드 → 문서 → 규약 문면**으로 내려왔고, 마지막 것은 이 PR 이 만든 결함이 아니라
**규약이 현실보다 좁다**는 선존 사실이다. 수렴으로 판정한다.
