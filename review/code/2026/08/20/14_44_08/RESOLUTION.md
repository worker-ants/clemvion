# RESOLUTION — 14_44_08

대상 SUMMARY: `review/code/2026/08/20/14_44_08/SUMMARY.md` (위험도 **MEDIUM**, Critical **0**, WARNING **8**, INFO 10)

**처분: WARNING 5건 코드·문서 수정 + 3건 트래커 등재. INFO 2건 추가 반영.**

---

## WARNING 2 — 내 fix 가 새 우회를 열었다 (requirement)

직전 라운드에서 *"값이 비었는가"* → *"사용자가 건드렸는가"* 로 판정을 바꿨는데, 그게 **다른
구멍을 냈다**: 한 번 건드리면 `touchedMaskedKeys` 에 영구 등록돼 **값을 다시 마커로 되돌려도**
제출이 풀린다. 이 PR 이 막으려던 오염이 그 경로로 되살아난다.

**두 판정 중 하나를 고르는 문제가 아니었다 — 각각이 서로 다른 경로로 뚫린다:**

| 조건 | 이것만 쓰면 뚫리는 경로 |
|---|---|
| 값이 비었는가 | 스키마 지연 도착 시 `coerceInput("boolean","")` → `false` 로 조용히 풀림 (`14_08_45` W2) |
| 사용자가 건드렸는가 | 건드린 **뒤** 값이 다시 마커여도 영구 해제 (`14_44_08` W2) |

그래서 **둘 다** 요구한다 — 건드렸고 **그리고** 현재 값에 마커가 없어야 풀린다. 후자는
`hasMaskedMarkerLeaf` 라 스칼라·중첩을 함께 본다.

**재검증(뮤테이션)**: 값 재검증을 빼면 신규 캐너리가 **RED**.

## WARNING 1 — SPEC-DRIFT (requirement)

판정을 두 번 바꾸는 동안 spec 2곳 + plan 체크리스트가 *"값이 비어 있는 동안"* 이라는 **첫
버전**에 머물렀다. 관측 가능한 동작이 다르므로(값 기반은 재차 비면 재차단, 최종본은 마커
잔존 시 재차단) 세 곳을 최종 규칙으로 맞췄다.

- `spec/5-system/13-replay-rerun.md` §10.2 · `spec/5-system/14-external-interaction-api.md` §R17 표
- `plan/in-progress/eia-inputdata-marker-guard.md` 체크리스트 — 두 라운드에 걸쳐 좁힌 경위를 남김

## WARNING 3 · INFO 8 · INFO 9 — 테스트 (testing)

판정 전환이 막으려던 회귀 자체가 고정돼 있지 않았다. 세 건을 추가했다:

- **건드린 뒤 값이 다시 마커면 계속 막는다** (W2 캐너리)
- object 필드도 마커를 지우면 풀린다 — 언블록 경로가 스칼라만 검증돼 있었다 (INFO 8)
- **마스킹 키가 둘이면 하나만 채워도 계속 막힌다** — `some`→`every` 뮤테이션은 이 케이스만 잡는다 (INFO 9)

## WARNING 7 — 테스트 파일 JSDoc 소제목 (documentation)

소제목이 *"`inputData` 는 **의도적으로 대상이 아니다**"* 라는 **구 결론을 현재형으로** 단언하고,
정정은 본문 중간 blockquote 에만 있었다. 위에서 읽는 사람은 옛 결론을 먼저 만난다.
자매 파일(DTO)이 이미 쓰던 패턴대로 **주제문을 현재 진실로 바꾸고 옛 설명은 역사 caveat 으로**
재배치했다.

## WARNING 8 — JSDoc 블록 분리 (maintainability)

같은 판정을 설명하는 두 블록이 연속으로 놓여 한쪽만 갱신될 위험이 있었다. W2 수정과 함께
**하나로 병합**하고 표로 정리했다.

---

## 트래커 등재 (3건) — 이번 PR 을 막을 사안이 아니다

| # | 항목 | 등재 사유 |
|---|------|-----------|
| 4 | 마스킹 게이트 4곳 통합 헬퍼 | fragmentation 이 실제로 직전 라운드 CRITICAL 을 냈다. 근본 해소는 리팩터라 별건 |
| 6 | `inputOverride` 서버측 마커 거부 | **이번 PR 이 만든 결함이 아니다** — security 도 INFO 로 판정했고 §R17 이 가드 범위를 UI 정상 흐름으로 명시했다 |
| 5 | 응답 의미 반전의 외부 소비자 확인 | 스키마로 드러나지 않는 콘텐츠 계약 변경. 저장소 밖 소비자 존재 여부는 diff 범위 밖 |

## 미반영 INFO (8건)

1·2 는 의도된 설계 경계(캐너리로 고정), 3 은 이미 트래커 항목, 4·5·7 은 리뷰어가 조치 불요로
판정, 6(변수명)은 최종 판정이 `maskedKeys` 교집합만 보므로 이름이 오해를 낳지 않는다,
10 은 세는 기준 차이로 실제 모순이 아니다.

## 검증

fix 반영 후 TEST WORKFLOW 4단계 전부 PASS — lint / unit(백엔드 427 suites·8,832 ·
프런트 6,063) / build / e2e 276 + playwright 51. 모달 스위트 단독 **27 passed**.
