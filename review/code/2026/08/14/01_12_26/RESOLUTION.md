# RESOLUTION — `01_12_26` (+ consistency `01_12_33`)

ai-review **CRITICAL 0 / WARNING 5** (forced 7명 전원 — `testing` 은 API 에러로 죽어 재실행해
확보). WARNING 4건 조치, 1건은 근거를 적어 후속으로 넘김.

## 먼저: `testing` 리뷰어가 죽었고, 그걸 clean 으로 넘기지 않았다

1차 실행에서 `testing` 이 `API Error: Server error mid-response` 로 실패해 `testing.md` 가
디스크에 없었다. 요약 에이전트가 이를 스스로 크게 표시했다 — *"이 보고서의 LOW 판정은
testing 관점을 커버하지 못한 상태이며, testing 재실행 전까지 clean 으로 간주하지 말 것."*

`resumeFromRunId` 로 캐시된 12명은 재생하고 죽은 하나만 다시 돌렸다. **복구된 `testing` 이
실제로 새 WARNING(W5)을 냈다** — 넘겼으면 그대로 잃었을 항목이다.

(push 게이트도 같은 것을 본다 — `_summary_is_resolved` 가 forced 커버리지를 요구하므로
`testing.md` 없이는 어차피 통과하지 못한다.)

## W2·W5 — 트레이드오프를 재보지 않고 단언했다

**조치 완료, 그리고 결론이 뒤집혔다.**

`stripComments` 가 줄 끝 `//` 를 남기는 이유를 내가 *"대상 파일에 `https://` 가 있어 URL 을
자르게 된다"* 고 적었다. **재본 적이 없다.** 실측하니:

| | 현행 | 줄 끝까지 제거 |
|---|---|---|
| `execution-engine.service.ts` | 2 | 2 |
| `knowledge-base.service.ts` | 5 | 5 |
| `auth-oauth.service.ts` | 1 | 1 |
| `executions.service.ts` | 1 | 1 |

URL 은 8줄 있지만 **헬퍼 호출과 같은 줄에 있는 것이 하나도 없다.** 걱정한 비용이 0이었다.

그리고 더 중요한 것을 놓쳤다 — **두 방향의 위험이 대칭이 아니다**:

- 남겨두면: 주석 언급이 카운트를 **부풀려 호출 누락을 가린다** → 조용히 통과
- 지우면: URL 이 잘려 카운트가 **줄어** → RED, 시끄럽지만 안전

가드의 존재 이유가 "조용히 통과" 를 막는 것이므로 그쪽을 닫는 게 맞다. 줄 끝 주석도
지우도록 바꾸고, **두 방향을 모두 테스트로 고정**했다(부풀림 차단 + 잘림은 알려진 한계).

뮤테이션으로 확인: 줄-끝 스트리핑을 되돌리면 **2 failed(사살)**.

## W3·W4 — 두 헬퍼가 서로를 안 가리키고 있었다

**조치 완료.** 지적이 정확하다. 이 PR 이 진단한 결함의 재발 경로가 정확히 이것이다 —
새 raw SQL 지점을 추가하는 사람이 먼저 눈에 띄는 `assertRowArray` 를 재사용하는 것.

- `assert-row-array.ts` — **"`UPDATE`/`DELETE … RETURNING` 에는 쓰지 마라"** 경고 + 분담 표.
  핵심은 *"튜플도 배열이라 이 가드를 그대로 통과한다"* — 이게 8곳이 빠진 이유다.
- `update-returning-rows.ts` — **제네릭 `T` 는 검증이 아니라 단언**이고 raw 행의 키는
  **snake_case** 라는 캐비엇. 이 PR 안에서 실제로 그 함정에 빠졌고(`rememberMe`),
  나머지 7개 호출부는 `id` 처럼 **대소문자 차이가 없는 필드만 써서 우연히 피했을 뿐**이다.

## INFO 7 — 5라운드째 반복된 주석 앵커

**조치 완료.** `:727` 이 `① 과 같은 CAS 락` 이라 적었는데 `:335` 의 실제 표기는 `1)` 라
그 앵커가 존재하지 않는다. **이 주석은 이 PR 에서 내가 쓴 것**이고 5라운드 연속 지적됐다 —
넘길 이유가 없다.

## W1 — 후속으로 넘김 (근거)

가드가 **"이 3개 파일" 을 하드코딩**해서, 새 raw UPDATE/DELETE 지점이 그 밖의 파일에 생기면
아무도 RED 를 내지 않는다. 지적이 맞다 — 현재 방식은 "아는 지점이 후퇴하지 않는지" 만 지키고
"새 지점이 생겼는지" 는 못 본다.

리뷰어도 "급하지 않음" 으로 달았고, 처방(래퍼로 구조적 강제)은 기존 호출부 전수 이관이라
이 PR 의 성격(단일 결함 수정)을 벗어난다. **plan 에 착수 전 비용 측정 조건과 함께 등재**했다.

## 검증

- lint `--max-warnings 0` 통과 · **37스위트 457 passed** · ratchet **199/38 일치**
- 뮤테이션 사살(줄-끝 스트리핑 제거 → 2 failed)

## consistency `01_12_33` — BLOCK: YES 이나 이 PR 과 무관하다

**스코프 아티팩트다.** target 이 `spec/5-system/`(EIA r8 cache-scope)인데 이 PR 은 spec 을
1줄도 안 바꾼다. 두 checker 가 스스로 근본 원인을 *"spec 내용 결함이 아니라 오케스트레이터의
워크트리 라우팅 오류"*, *"planner 인계 대상 없음"* 이라고 적었다.

원인이 이번에 특정됐다 — **워크트리 이름(`eia-r8-cache-scope-4ae434`)이 stale** 하다. 이
워크트리는 다른 브랜치로 재사용됐는데 이름이 그대로라, 체커 프롬프트에 "EIA r8 작업" 이라는
잘못된 검토 대상이 주입된다.

push 게이트에는 영향이 없다 — `_newest_resolved_impl_done_mtime` 은 **BLOCK: NO 세션만 세고
최신을 취한다**(코드를 읽어 확인). 채택되는 세션은 `00_54_07`(BLOCK: NO)이고, 그것이
spec-linked 최신 변경(`00:53:54`)보다 뒤다. 나중의 YES 는 거부권을 갖지 않는다.

그 세션의 실질 WARNING(에러 카탈로그 미등재)은 planner 위임으로 등재돼 있다.

## 넘김 (근거 명시)

| # | 처분 |
|---|---|
| INFO 6 (`AuthOAuthStateRow` 이름) | `XRow` 접미사는 저장소 선례(`WaitingNodeRow` 등). 접두어 변경은 다음에 손댈 때 |
| INFO 8 (기존 7개 mock 이 행 배열 형태) | 헬퍼가 양쪽을 다 받으므로 무해. 실 shape 판별은 신규 3건 + e2e 가 담당 |
| INFO 9 (드라이버 내부 동작 의존) | 이 의존은 제거 불가 — 대신 e2e 가 실 드라이버로 고정한다. 버전업 시 그 e2e 가 신호 |
| INFO 10 (`reEmbedAll` 비-트랜잭션) | 기존 구조, plan 후속 등재됨 |
| INFO 11·12 (실동시성 검증) | 단일 SQL statement 원자성은 DB 계층 보장. `Promise.all` 재현은 선택 사항 |
| INFO 13 (user guide) | 셋 다 "문서가 이미 전제한 동작을 복원" — 갱신 갭 없음 |
