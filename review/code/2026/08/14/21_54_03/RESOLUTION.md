# RESOLUTION — `21_54_03` (+ consistency `--impl-done` `21_53_54`)

ai-review **CRITICAL 0 / WARNING 2** · consistency **BLOCK: NO**. 두 게이트 모두 열렸다.

수렴 판정: 이번 라운드 발견은 **전부 문서 계층**이고, 두 게이트가 **독립적으로 같은 항목**
(HANDOFF staleness)에 수렴했다. 코드 로직 델타는 0 — 11개 reviewer 가 직접 대조로 재확인.

## W1 — HANDOFF 가 이미 해소된 차단을 미해결로 서술 (documentation + plan_coherence)

**조치 완료.** 두 게이트가 같은 것을 짚었다(`21_54_03` W1 · `21_53_54` W1). 중단 인계
문서가 재개 후에도 "🚫 차단 1 / ⚠️ 차단 2" 를 그대로 말하고 있었다 — 재개자가 끝난 일을
다시 하거나 상태를 오판할 수 있다.

상단에 종결 배너(각 차단의 처분 + 해소 커밋 `462455a52` + 게이트 결과) 추가,
frontmatter `status: in-progress → complete`.

**삭제하지 않은 이유**: 중단·재개가 있었다는 사실과 그 시점의 측정치가 기록으로서 값을
갖는다. 다만 "재개 절차" 를 남겨 두면 그것이 곧 다음 staleness 이므로 **이미 집행됐음을
배너에서 명시**했다.

> **교훈**: 인계 문서는 **재개되는 순간 stale 이 예약된다.** 쓸 때는 정확했고, 재개하면서
> 그 정확성이 소멸한다. 두 게이트가 같은 라운드에 같은 항목을 짚은 건 그만큼 눈에 띄는
> 패턴이라는 뜻이다.

## W2 — `emitNodeEvent` 무상한 payload 위 strip 오버헤드 (performance)

**조치 없음 — 이미 등재된 별건이고, 리뷰어도 "즉시 조치 불요" 로 판단.**

실측(+2.4~2.6배, 6.5MB 에서 +61ms)과 상한 부재는 이 라운드 이전에 확인·문서화됐다.
**핵심은 이것이 strip 이 만든 문제가 아니라는 점** — strip 을 제거한 뮤턴트에서도 6.5MB
payload 에 39ms 가 든다. 상한 부재 자체가 선존 결함이라 **별건으로 분리 등재**했다.

리뷰어의 후속 제안("직렬화 문자열에 `llmCalls` 부재 시 재귀 스킵")은 유망하지만
**직렬화 비용을 새로 내는 것이라 순 이득인지 A/B 가 필요하다** — 추측으로 넣지 않는다.

## consistency W2 — 체크리스트 (3) 에 소급 정정 이력 없음

**조치 완료.** (3) 이 `--impl-done` BLOCK: NO 로 닫혔는데, 그 근거였던 §6.2 매핑 행이
이후 라운드에서 CRITICAL 로 뒤집혔다. 각주를 달아 **닫힌 체크박스가 영구 보증이 아님**을
그 자리에 남겼다.

## consistency INFO 5·6 — 교차참조 정밀도

**조치 완료(둘 다 실측 후).**

- 앵커 누락 2곳 → `#446-messagessource-마커` 보강. 앵커 형태는 지어내지 않고
  `conversation-thread.md §5.1` 자신이 쓰는 형태를 확인해 맞췄다
- **폴백 SoT 를 WS §4.4.6 단독으로 좁혔다.** `Conversation Thread §5.1` 을 공동 인용하고
  있었는데 **직접 열어 보니 §5.1 에는 폴백 문장이 없다**(매핑 표만). 인용이 실제보다
  넓었던 사례 — §5.1 은 "매핑 표 근거이지 폴백 근거가 아니다" 로 명시

## 넘김 (근거 명시)

| # | 처분 |
|---|---|
| ai INFO 1 (브랜치 1차 요구사항 미진전) | 의도된 pivot. 보안 결함이 발견돼 우선순위를 바꿨고 3개 plan 에 경위 기록됨 |
| ai INFO 2 (`stripDeep` identity 캐시) | 유예 유지. 반복 emit 만 해결하고 worst-case 를 못 푼다 |
| ai INFO 3 (JSDoc 이 리뷰 라운드 `hh_mm_ss` 인용) | 유효한 지적. 핵심 계약은 본문에 서술돼 있어 현재 문제 없음. **다음 각주부터 커밋 SHA 병기** |
| ai INFO 4 (fanout 호출부 2곳 중복) | 현재 2곳. 3번째가 생기면 `stripForFanout` 로 통합 |
| ai INFO 5 (응답 payload 축소, 버전 신호 없음) | 보안 수정의 목적 자체. CHANGELOG 공지로 갈음 |
| ai INFO 6 (REST 성능 회귀 벤치 미커밋) | 타당하다 — 결론("AI 경로가 빨라진다")이 조용히 무효화될 수 있다. **다음에 이 순서를 만질 때** A/B 를 커밋 가능한 형태로 남길 것 |

## 검증

- spec 무결성 가드 **20 files / 2932 tests**
- 백엔드 관련 **24 suites / 393 tests** · `lint --max-warnings 0`
- forced reviewer 7명 전원 결과 확보(`forced_missing: []`)
