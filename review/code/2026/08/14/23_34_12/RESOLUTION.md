# RESOLUTION — `23_34_12`

ai-review **CRITICAL 0 / WARNING 3**. 전부 조치. 발견이 **문서·커버리지 계층**으로 내려왔다
— 동작 결함 0, 구조 결함 0. 수렴으로 판단한다.

## W1 — 교훈을 적어 둔 바로 그 줄이 테스트에 안 걸려 있었다 (testing)

**조치 완료.** `finalizeStalledExhausted` 의 자식 `NodeExecution` cascade 가
`status` 만 단언돼 있어 `error.code` 를 바꿔도 GREEN 이었다(리뷰어 뮤테이션 실측).

**리뷰어가 짚은 아이러니가 정확하다** — 이 줄은 내가 *"손으로 값을 반복하면 갈린다"* 며
부모 상수(`stalledError.code`)를 참조하도록 고친 자리다. 그 교훈을 주석으로 적으면서
**그 주장을 지키는 테스트는 안 붙였다.** 값 단언 추가 후 `code: 'MUTATED'` 뮤턴트로
RED 확인.

## W3 — 소스만 고치고 스펙 파일을 놓쳤다 (requirement)

**조치 완료.** `chat-channel.dispatcher.spec.ts` 의 describe JSDoc 이 **형제 `.ts` 에서
방금 걷어낸 것과 같은 죽은 참조**(존재한 적 없는 plan 이름 · 지금은 다른 코드를 가리키는
줄 번호 · "string 이 기본 경로" 라는 낡은 전제)를 그대로 갖고 있었다.

> **이 브랜치에서 "한 곳만 고쳤다" 가 세 번째다** — §6 표/§6.4 blockquote, 소스/스펙 JSDoc,
> 그리고 §6.4 정정 때 다섯 문서 중 둘을 체커가 놓친 것까지. 매번 형태가 조금씩 달라
> 한 번의 grep 으로 안 잡힌다. **"고쳤다" 를 쓰는 시점에 자매를 전수로 세는 것**이
> 유일하게 작동한 방법이었다.

## W2 — 체크리스트가 커밋 메시지보다 늦다 (documentation)

**조치 완료.** 커밋이 "완료" 를 보고하는데 체크박스는 그대로였다. **이 plan 에서만 세
번째**(`22_55_51` W11 · `23_18_06` W2 · `23_34_12` W2)라 근본 원인을 plan 에 적었다 —
선언 시점과 표시 시점이 다르면 항상 어긋난다. 커밋 직전에 체크박스를 함께 스테이징하는
것이 유일한 해법이었다.

## INFO 넘김 (근거)

| # | 처분 |
|---|---|
| 1·2 (`details`/`message` 값-패턴 마스킹) | 백로그 등재 유지. 리뷰어가 **`details` 는 현재 라이터 4곳이 채우지 않아 도달 불가**임을 확인 — 등재 항목에 `details` 도 포함하도록 문구는 이미 그렇게 적혀 있다 |
| 3 (타입 2중 선언) | 3중 → 2중으로 개선된 상태. 모듈 경계 대가로 유지 |
| 4 (`execution.cancelled` 미통일) | 코드·spec·plan 3계층 일관 서술 — 은폐 아님 |
| 5 (dispatcher 스칼라 message 값 변화) | 실제 emit 경로가 스칼라를 넣지 않는다. 헬퍼 spec 이 스칼라 케이스를 이미 고정 |
| 6 (프런트 fallback 분기 미고정) | 두 라운드째 같은 판단 — 헬퍼가 `message: ''` 로 흡수해 크래시 경로가 아니다 |
| 7·9·10 (방어 범위·장문 주석·`null` vs `""`) | 기결정 |
| 8 (관용구 3중 반복) | 의도적 일관성. **4번째가 생기면** `extractErrorMessage` 추출 |
| 11 (로그 `code` 값 변화) | 저장소 내부 소비자 0건. CHANGELOG 고지됨 |
| 12 (부모/자식 UPDATE 비-트랜잭션) | pre-existing. 함수 JSDoc 이 이미 "알려진 이론적 race(수용)" 로 문서화 |
| 13 (user guide) | 매칭 trigger 0건 |

## 검증

- 백엔드 **424 suites / 8674 passed** · lint `--max-warnings 0` · 타입 199(래칫 동일)
- W1 판별력: `code: 'MUTATED'` 뮤턴트에서 RED 확인
