# RESOLUTION — `12_06_20`

ai-review **CRITICAL 0 / WARNING 2** (forced 7명 전원). 둘 다 조치 완료.
consistency 는 `12_06_21` 진행 중 — 직전 `11_02_18` 은 BLOCK: NO.

## W1 — 같은 커밋이 고친 것을 테스트 JSDoc 이 현재형으로 서술했다

**조치 완료.** `b49ee4310` 이 경계 연산자를 `>=`→`>` 로 통일했는데, 같은 커밋의 테스트
JSDoc 은 *"연산자가 다르다"* 를 **현재형**으로 남겨 뒀다. 프로덕션 인라인 주석은 과거형으로
정확히 적었는데 테스트만 놓쳤다. 리뷰어 셋(maintainability·testing·documentation)이 독립 지적.

**같은 클래스가 직전 라운드에서 이미 잡혔다** — `10_32_27` testing W7 이 정확히 이것이었고
고쳤는데, **바로 다음 커밋에서 재발**했다.

원인이 보인다: 조사하면서 JSDoc 을 쓰면 그 시점의 상태는 **옛 상태**다. 그 뒤 코드를 고치고
JSDoc 을 다시 읽지 않으면 서술이 그대로 남는다. 이번엔 과거형으로 바꾸고 *"같은 커밋에서
통일했다 — 지금 두 함수의 경계는 동일하다"* 를 명시했다.

## W2 — 매직넘버가 자매 관례를 어겼다

**조치 완료.** `it.each([0, 5, 8, 9, 10, 11, 12])` 이 리터럴이었다. 같은 파일 `:203` 의
자매 경계 테스트는 *"상수 변경 시 자동 추적되도록 매직넘버 대신 import"* 라고 **관례를
명시해 뒀는데** 초판이 어겼다.

위험이 조용한 쪽이라 특히 나쁘다 — `MAX_SANITIZE_DEPTH` 가 바뀌면 테스트는 **계속 통과하면서
경계 판별력만 잃는다**. 전부 상수 상대값으로 바꿨다.

### 그리고 표본에 전환점을 넣고 다시 쟀다

리뷰어가 INFO 6 으로 "판별력 전환점(7)이 표본에 없다" 고 짚었다. `MAX-3` 을 넣었는데,
**그건 재본 적 없는 값**이라 JSDoc 테이블의 "MAX-2 이상은 판별력 없음" 이 미측정 주장이
될 뻔했다. 뮤턴트로 다시 쟀다:

| depth | strip 없이 |
|---|---|
| `0` · `MAX-5` · `MAX-3` | **RED** (strip 이 지킨다) |
| `MAX-2` 이상 | 통과 (sanitize 상한이 먼저 막는다) |

전환점은 `MAX-3` ↔ `MAX-2` 사이. 마커가 `llmCalls` 배열 안 두 단계 더 들어가 있어 노드
깊이 `MAX-2` 부터 마커가 상한 밖으로 나간다 — 이 설명까지 JSDoc 에 적었다.

## 검증

- websocket 6스위트 **138 passed** · lint(`--max-warnings 0`) · ratchet 199/38
- 깊이 표본 8개, 그중 3개가 판별력 있음(뮤턴트 실측)

## 넘김 (근거 명시)

| # | 처분 |
|---|---|
| INFO 1 (깊이 방어가 호출 순서 의존) | JSDoc 에 이미 명시. 신규 호출부 추가 시 재검토 — 그 전제를 깨는 호출부가 아직 없다 |
| INFO 2 (유출 사후 대응) | 코드 아님. plan 에 미완료 항목으로 등재됨, 운영 판단 대기 |
| INFO 3 (`Object.entries` 할당) | 정확한 지적. "할당 없음" 은 **반환 identity** 에 대한 주장이고 순회 자체는 배열을 만든다. 다음에 이 함수를 만질 때 문구를 좁힌다 |
| INFO 4 (SPEC-DRIFT) | planner 인계 항목 (7) 로 이미 등재 |
| INFO 5 (이름 기반 strip 의 계약 확장) | 의도된 트레이드오프. collateral 0 확인 완료 |
| INFO 7 (`emitNodeEvent` 쪽 nested 테스트 비대칭) | 두 경로가 같은 `stripDeep` 을 공유해 기능적으로는 덮인다. node 이벤트가 중첩 shape 을 갖게 되면 짝으로 추가 |
| INFO 8 (경계 서사가 세 곳에 중복) | **W1 이 그 실례다.** 다음에 이 로직을 건드릴 때 세 곳을 함께 갱신하는 것이 처방 — 지금 통합하면 서사의 맥락이 흩어진다 |
