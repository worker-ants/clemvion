# 부작용(Side Effect) Review — harness-block-backstop (2026-08-01 01:49:32 라운드)

## 스코프 메모

이번 라운드의 "리뷰 대상 파일" 44건은 전부 `review/code/2026/08/01/{00_03_38,00_33_34,01_17_35,01_17_47}/**` — 즉 **이 브랜치의 이전 4개 리뷰 라운드 산출물**(역할별 리뷰 `.md`, `SUMMARY.md`, `RESOLUTION.md`, `meta.json`, `_retry_state.json`)이 이번에 처음 git 에 커밋되어 diff 로 잡힌 것이다. 실제 하네스 소스(`.claude/_shared/block_integrity.py`, `.claude/_shared/retry_state.py`, 4개 훅, 3개 orchestrator, 2개 신규 테스트 등 15개 파일)는 이번 라운드의 "리뷰 대상 파일"에 없다 — `git diff --stat origin/main...HEAD` 로 직접 대조한 결과 그 15개 파일은 이미 앞선 라운드(00_03_38/00_33_34/01_17_35)에서 리뷰가 끝났고, 이번 라운드는 그 리뷰 결과물 자체를 새로 커밋되는 diff 로 보는 구조다(이 프로젝트의 "직전 검토 코드는 이후 라운드 changeset 에서 제외" 관례). 프롬프트 44건 전부(파일 1~44)를 읽었고, 크기 제한으로 diff 가 생략된 항목(파일 1,3,5,6,9,10,17,20,24,27,28,29,33,37,43)은 `Read`/`git show`/`cat`으로 실제 파일을 직접 열어 대조했다. 추가로 `/tmp/code-review-agents-log.txt`(오케스트레이터 자체 debug 로그) 와 `git log --all`/`git check-ignore`/`git grep --all`로 아래 발견사항을 교차검증했다.

## 발견사항

- **[WARNING]** 유령(orphan) 리뷰 세션 `01_17_47` — 직전 세션의 gitignore 대상 `_prompts/` 스테이징 파일을 "변경된 파일"로 오인해 준비된 뒤 완료되지 못했고, 그 산출물(허위 사유 포함)이 그대로 영구 커밋됨
  - 위치: `review/code/2026/08/01/01_17_47/meta.json:15,20,25,30,35,40,45,50,55,60,65,70,75,80`(신규 파일, `"files"` 배열의 `file_path` 14건 전부가 `_prompts/*.md`) / `review/code/2026/08/01/01_17_47/_retry_state.json:13-16`(`agents_forced_reasons.documentation` — 프롬프트에는 크기 제한으로 diff 가 생략돼 게이트가 없어, 실제 파일을 직접 `Read`/grep 해 확인한 줄 번호. 신규 파일 diff 라 그 번호는 게이트가 있었다면 일치했을 값과 같다)
  - 상세: `meta.json`의 `"files"` 배열 16건 중 14건이 `review/code/2026/08/01/01_17_35/_prompts/*.md` — 즉 **다른 세션(`01_17_35`)의, `.gitignore:38`(`review/**/_prompts/`)로 명시 제외된 서브에이전트 프롬프트 스테이징 파일**이다. `_retry_state.json`의 `agents_forced_reasons.documentation`은 이를 근거로 "문서 파일(.md/.txt/.rst/.adoc/LICENSE/CHANGELOG 등) 변경"이라 기록해 `documentation` 리뷰어를 강제 발동시켰다 — 그러나 이 "문서 파일들"은 다른 세션의 임시 프롬프트 스테이징일 뿐 git 추적 대상이 아니므로, 이 커밋을 체크아웃하는 어느 클론에도 나타나지 않는 경로다. `_retry_state.json` 전체를 대조하면 `routing_status: "pending"`, `agents_success: []`, `agents_fatal: []`, `agent_history: {}`이고 `SUMMARY.md`/개별 리뷰어 `.md` 산출물이 이 세션 디렉터리에 전혀 없다 — 라우터조차 한 번도 실행되지 못한 채 준비만 되고 버려진 세션이다. 오케스트레이터 자체의 debug 로그(`/tmp/code-review-agents-log.txt:1734-1735`)를 대조하면 `01_17_35` 준비(`01:17:35.990`, `route=all`, 15 files — 실제 하네스 diff 와 일치)와 `01_17_47` 준비(`01:17:47.406`, `route=auto`, 16 files)가 **12초 간격**으로 별도 호출됐고, `01_17_47`은 로그 전체에서 이 한 줄 이후 다시는 등장하지 않는다(resume/router 실행 흔적 없음). 그 시점 `01_17_35/` 디렉터리에 실제로 존재하던 항목 수(`_retry_state.json` + `meta.json` + `_prompts/` 하위 14개 = 16)가 로그의 "16 files"와 정확히 일치해, 이 두 번째 호출이 `01_17_35` 세션 디렉터리 자체를 대상으로 (git diff 가 아니라) 재귀적 파일 나열을 수행했음을 강하게 뒷받침한다. 이 저장소에서 그런 방식으로 디렉터리를 나열하는 경로는 `.claude/skills/code-review-agents/scripts/code_review_orchestrator.py:968-975`(`get_directory_files`, `--files <dir>` 인자용)뿐이며, 이 함수는 `.git/`만 걸러낼 뿐 `.gitignore`를 전혀 참조하지 않는다(반면 인자 없는 기본 경로 `get_git_diff_files`, :857-872,는 `git ls-files --others --exclude-standard`로 gitignore 를 올바르게 존중함을 확인했다) — 이 파일은 이번 라운드의 "리뷰 대상 파일"이 아니므로 이번 diff 자체의 결함이라 단정하진 않지만, 이번에 새로 커밋된 두 파일이 바로 그 결함의 실행 증거다. `git grep`으로 저장소 전체(`origin/main` 포함) 히스토리를 대조한 결과 `meta.json`의 `"files"` 배열이 `_prompts/` 경로를 담은 사례는 이번 건이 유일함(다른 곳 0건)을 확인했다 — 알려진/용인된 반복 패턴이 아니라 이번에 처음 발생·발견된 결함이다. 실질 피해는 (1) 14개 리뷰어 프롬프트(각 ~159KB) + 라우터 프롬프트(~160KB), 총 ~2.4MB 분량이 실사용 없이 디스크에 생성됐다가 방치되고, (2) `review/**` 세션 디렉터리는 이 프로젝트 관례상 영구 보존(prune 없음)이므로 이 무의미한 세션의 `meta.json`/`_retry_state.json`이 앞으로도 `review/code/**` 전체 이력을 순회하는 모든 게이트 스캔의 대상에 영원히 남고, (3) `agents_forced_reasons`에 박제된 "문서 파일 변경" 사유 자체가 사실과 다른 채로 git 히스토리에 영구 기록된다는 점이다. 이 세션은 `SUMMARY.md`가 없어 다른 리뷰어들이 이미 확인한 "최신 리졸브 세션" 스캔 요구사항을 만족하지 못해 어떤 게이트 판정에서도 채택되지 않으므로(정상적으로 건너뛰어짐) 실제 차단/허용 판정에 영향은 없다.
  - 제안: (1) `plan/in-progress/harness-review-gate-ci-backstop.md`에 이 결함 클래스("`--files <dir>`가 gitignore 를 무시하고, 리뷰 세션 디렉터리를 대상으로 호출되면 다른 세션의 `_prompts/` 스테이징을 오염된 changeset 으로 흡수한다")를 후속 항목으로 등재. (2) `get_directory_files()`를 `os.walk` 대신 `git ls-files --others --exclude-standard -- <dir>` + `git ls-files -- <dir>`(tracked 포함) 조합으로 바꿔 gitignore 를 존중하도록 하드닝. (3) 지금 당장 `review/code/2026/08/01/01_17_47/{meta.json,_retry_state.json}`을 급히 삭제할 필요는 없으나(무해한 clutter), 원한다면 별도 정리 커밋으로 제거하거나 최소한 plan 문서에 "이 세션은 오탐, 무시할 것"이라는 주석을 남겨 향후 감사자의 혼동을 막을 것.

- **[INFO]** 이번 라운드의 리뷰 대상 자체가 라운드를 거듭할수록 스스로를 삼키며 커지는 구조 — 이번 프롬프트의 광범위한 truncation 이 그 직접 증거
  - 위치: 이번 프롬프트 전체(44개 파일 중 15건이 "프롬프트 크기 제한으로 diff 생략" 스텁만 실림 — 파일 1,3,5,6,9,10,17,20,24,27,28,29,33,37,43), 프롬프트 말미의 경고문("어떤 파일의 전체 내용도 실리지 않았습니다")
  - 상세: `review/`는 `_prompts/` 하위만 gitignore 대상이라(`.gitignore:38`) 각 라운드가 생성하는 실제 리뷰 리포트(`architecture.md`, `security.md`, `side_effect.md` 등)는 전부 git 에 커밋된다. 이 PR 은 지금까지 5개 라운드(00_03_38 → 00_33_34 → 01_17_35 → 01_17_47(무효) → 01_49_32, 이번)를 거쳤고, 매 라운드의 "변경된 파일" 목록에는 그 이전 라운드들이 만든 리뷰 산출물이 전부 새로 커밋되는 diff 로 잡힌다 — 그 결과 이번 라운드의 프롬프트는 실제 하네스 코드 diff 는 0줄이고 순전히 이전 라운드 리뷰 산출물 44개 파일(약 2,161줄/99,000토큰)로만 구성됐다. 이 자체는 프로젝트 관례(review 세션 영구 보존, gitignore 는 `_prompts/`에만 적용)상 예견된 설계이고 이미 컨텍스트 예산 메커니즘(`e7fef2510` 커밋)이 truncation 으로 대응하고 있지만, "리뷰가 리뷰를 낳고 그 리뷰가 다음 리뷰의 diff 가 되는" 자기참조적 성장은 이번 라운드에서 실측 가능한 형태(15/44 파일 생략, 남은 파일 다수도 "전체 파일 컨텍스트 없음")로 나타났다. 위 WARNING 항목(유령 세션)도 이런 "라운드가 라운드를 부르는" 압력(12초 간격의 중복 호출) 속에서 발생한 사고로 보인다.
  - 제안: 이번 diff 자체를 되돌릴 필요는 없다(구조적으로 불가피한 설계 트레이드오프). 다만 이 PR 처럼 라운드 수가 많아지는 장기 fix-review 루프에서는, 리뷰어 프롬프트 조립 시 "이전 라운드의 리뷰 산출물"은 원문 diff 대신 이미 결론이 난 요약/링크만 참조하도록 구성 로직을 분리하는 것을 검토할 가치가 있다 — 지금처럼 매 라운드가 이전 라운드 산출물 전체를 "diff" 취급하면 라운드 수에 비례해 프롬프트가 계속 성장한다.

- **[INFO]** (검증 결과, 문제 없음) 직전 라운드(00_33_34/01_17_35) side_effect 리뷰가 지적한 실질 부작용 항목의 사후 처분을 실제 소스와 직접 대조 — 전부 해소 또는 문서화된 defer 로 확인됨
  - 위치: `.claude/hooks/guard_review_before_stop.py:381-382`(수정 확인용으로 직접 Read), `plan/in-progress/harness-review-gate-ci-backstop.md:84`(§후속 10 등재 확인용으로 직접 Read)
  - 상세: `01_17_35/side_effect.md`와 `00_33_34/side_effect.md`가 각각 지적한 "Stop 훅의 note throttle 마커가 내용이 아니라 인덱스로 키잉되어 '다른 모순이면 통과한다'는 자기 주석을 위반한다"(이후 `01_17_35/RESOLUTION.md`에서 5개 리뷰어의 합의로 CRITICAL 로 격상·수정됨)가, 현재 `guard_review_before_stop.py:381-382`에 `hashlib.sha1(note.encode("utf-8")).hexdigest()[:12]` 기반 키잉으로 실제 대체돼 있음을 직접 확인했다(RESOLUTION.md 의 주장을 그대로 신뢰하지 않고 소스를 열어 검증). 또한 `01_17_35/concurrency.md`가 지적한 "`_retry_state.json`의 lost update, 특히 `agent_history`/rate-limit 필드는 자기치유 경로가 없다"는 항목이 `plan/in-progress/harness-review-gate-ci-backstop.md` §후속 10 에 실제로 등재돼 있음을 확인했다. 이번 라운드가 리뷰하는 44개 산출물 파일 안에서, 이 두 건 외에 새로 미해결로 남아있는 side-effect 성격의 항목은 발견되지 않았다.
  - (조치 불요 — 기록 목적)

## 요약

이번 라운드의 "리뷰 대상 파일" 44건은 실제 하네스 코드(.py/.md 15개, 이미 앞선 3개 라운드에서 리뷰 완료)가 아니라 그 라운드들이 만든 리뷰 산출물(리뷰 `.md`, `SUMMARY.md`, `RESOLUTION.md`, `meta.json`, `_retry_state.json`) 자체다. 이 산출물들의 **내용**은 대부분 이미 처분이 끝난 과거 리뷰 기록이라 새로 재론할 부작용이 없지만, 그 **존재 자체**를 부작용 관점에서 감사한 결과 하나의 구체적 결함을 새로 발견했다 — 직전 세션(`01_17_35`)이 진행되던 도중 12초 만에 별도로 재호출된 오케스트레이터가 `01_17_35`의 gitignore 대상 `_prompts/` 스테이징 파일을 "변경된 문서 파일"로 오인해 documentation 리뷰어를 강제 발동시키는 유령 세션(`01_17_47`)을 준비했고, 라우터조차 실행되지 못한 채 그 잘못된 전제(허위 "문서 파일 변경" 사유 포함)만 `meta.json`/`_retry_state.json` 형태로 영구 커밋됐다. 이 결함은 `.gitignore`를 참조하지 않는 `get_directory_files()`(`--files <dir>` 경로)로 설명 가능하고, 저장소 전체 히스토리에서 유일한 사례임을 확인해 이번에 처음 드러난 것으로 판단한다. 다만 해당 세션에 `SUMMARY.md`가 없어 어떤 게이트의 "최신 리졸브 세션" 판정에도 채택되지 않으므로 실제 차단/허용 판정에 영향은 없다 — WARNING 으로 표기한다. 그 외 눈에 띄는 것은 이 PR 자체의 반복 리뷰 라운드가 이전 라운드 산출물을 다음 라운드의 diff 로 계속 흡수하며 프롬프트가 자기참조적으로 커지는 구조적 특성(INFO, 이번 diff 의 결함이 아니라 설계 트레이드오프)과, 앞선 라운드들이 지적한 CRITICAL/WARNING 이 실제로 해소되었음을 소스 대조로 확인한 결과(INFO, 조치 불요)뿐이다. 전역 변수 신설, 함수 시그니처의 하위호환 파괴, 예기치 못한 환경변수·네트워크 접근, 이벤트/콜백 배선 변경은 이번 44개 파일 어디에도 없다(애초에 실행 코드가 아니므로 해당 범주 대부분은 적용 대상이 아니다).

## 위험도

LOW
