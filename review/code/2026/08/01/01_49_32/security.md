# Security Review — harness-block-backstop (2026-08-01 01:49:32)

## 조사 방법

프롬프트가 지정한 44개 "변경 파일"을 `Read`로 전수 확인했다(그중 다수가 `... 프롬프트 크기 제한으로 diff 생략 ...`로 축약돼 있어 해당 파일은 워크트리에서 직접 열어 확인). 추가로 `git diff --stat origin/main...HEAD`, `git log --oneline origin/main..HEAD`로 이 라운드가 실제로 무엇을 리뷰 대상으로 받았는지 검증했다.

핵심 확인 사항: 이번 프롬프트의 44개 파일은 **전부 `review/code/2026/08/01/{00_03_38,00_33_34,01_17_35,01_17_47}/**` 하위의 리뷰 세션 산출물**(다른 서브에이전트가 작성한 마크다운 리포트, `meta.json`, `_retry_state.json` 스냅샷)이며, 애플리케이션·하네스 실행 코드(`.py`)는 단 한 파일도 포함되어 있지 않다. 실제 하네스 소스 변경(`.claude/_shared/block_integrity.py`, `.claude/_shared/retry_state.py`, `.claude/hooks/**`, `.claude/skills/**/scripts/*.py`, `.claude/tests/*.py`)은 이 브랜치의 이전 커밋들(`30cc0f738`→`7dd4ad8c7`, 1R~5R 리뷰 반영 포함)에서 이미 반영·리뷰돼 있고, 이번 라운드의 diff 베이스는 그 이후에 새로 생성된 리뷰 세션 디렉터리(00_03_38/00_33_34/01_17_35/01_17_47)만을 신규 파일로 잡고 있다. 따라서 이번 보안 리뷰의 실질 대상은 "코드"가 아니라 "코드에 대한 과거 리뷰 기록물"이다.

이 성격을 감안해 다음을 직접 실측했다:

1. 44개 파일 전체에 대해 하드코딩 시크릿·연결 문자열·JWT·인증서·`shell=True`/`eval`/`os.system` 패턴을 grep (결과: 매치 0건 — 유일한 매치는 "grep 결과 없음"이라고 서술하는 리뷰 문장 자체였다).
2. 신규 파일 `review/code/2026/08/01/01_17_35/RESOLUTION.md`(이 라운드에서 처음 리뷰 대상이 됨)가 주장하는 두 건의 보안 관련 수정 — (a) Stop 훅 note 억제 마커를 인덱스 대신 note 텍스트의 SHA1 다이제스트로 키잉, (b) `summary_block_verdict()`가 END-앵커 매치 중 첫 번째가 아니라 **마지막**을 채택 — 을 실제 현재 소스(`​.claude/hooks/guard_review_before_stop.py`, `.claude/_shared/block_integrity.py`)와 직접 대조해 정확히 반영돼 있음을 확인했다(아래 발견사항 참고). 이는 "과거 리뷰 문서가 실제로 고쳐졌다고 주장하는 보안 결함이 실제로 고쳐졌는지"를 검증하는 것이 이런 메타 리뷰 라운드에서 보안 리뷰어가 할 수 있는 유일하게 의미 있는 점검이기 때문이다.

## 발견사항

- **[INFO]** 이번 diff는 실행 코드가 아니라 리뷰 세션 산출물(마크다운 리포트 + JSON 상태 스냅샷) 44건 — 보안 관점에서 평가할 신규 공격 표면이 없음
  - 위치: 44개 파일 전체(예: `review/code/2026/08/01/00_03_38/meta.json`, `review/code/2026/08/01/00_33_34/SUMMARY.md`, `review/code/2026/08/01/01_17_35/RESOLUTION.md` 등). 상세 목록은 `review/code/2026/08/01/01_49_32/meta.json`과 대조 확인.
  - 상세: 44개 파일 모두 `change_type: Review`(신규 추가)이며 언어는 `md`/`json`뿐이다. SQL/커맨드/LDAP 인젝션, XSS, 경로 탐색, 인증/인가, 입력 검증, 암호화 등 점검 관점이 성립하려면 요청을 처리하거나 데이터를 파싱·실행하는 코드 경로가 있어야 하는데, 이 파일들은 전부 다른 리뷰 라운드가 과거에 생성한 산문 보고서와 하네스 오케스트레이터의 내부 상태 스냅샷(세션 디렉터리 경로, sub-agent 호출 기록, 체커별 pending/success/fatal 버킷)일 뿐이다. `session_dir`/`prompt_file`/`output_file` 등의 문자열 필드는 이 워크트리 안에서 오케스트레이터 스스로 생성한 로컬 파일 경로이며 외부(원격) 입력이 개입할 지점이 없다.
  - 제안: 조치 불요.

- **[INFO]** 신규 `RESOLUTION.md`가 인용하는 두 건의 보안 관련 수정(Stop 훅 note 마커 키잉, 하향 판정 파서 우선순위)이 실제 현재 소스에 정확히 반영돼 있음을 직접 대조 확인 — 문서-코드 drift 없음
  - 위치: `review/code/2026/08/01/01_17_35/RESOLUTION.md` (CRITICAL 절 "Stop 훅 스로틀이 텍스트가 아니라 인덱스로 키잉", WARNING 표 W15 행) — 대조한 실제 소스: `.claude/hooks/guard_review_before_stop.py`(`_run` 함수, `hashlib.sha1(note.encode("utf-8")).hexdigest()[:12]`로 마커 키 구성), `.claude/_shared/block_integrity.py`(`summary_block_verdict`, `matches[-1].group(1)`로 최종 매치 채택).
  - 상세: 이 워크트리의 세 라운드(00_33_34/01_17_35 각 security.md·testing.md·side_effect.md)가 독립적으로 지적했던 "Stop 훅 note 중복억제가 인덱스 기반이라 서로 다른 하향 경고를 영구히 삼킨다"(당시 WARNING/MEDIUM~CRITICAL로 보고됨)와 "`summary_block_verdict()`가 END-앵커 매치 중 텍스트상 가장 먼저 나오는 것을 채택해 stale 판정을 고를 수 있다"(WARNING) 두 결함을, `RESOLUTION.md`는 각각 "마커 키를 노트 텍스트의 sha1 앞 12자로" 및 "`finditer`→`[-1]`"로 고쳤다고 서술한다. 실제 파일을 직접 열어 확인한 결과 두 수정 모두 정확히 그 형태로 존재한다 — 즉 새로 추가되는 이 리뷰 기록물이 "이미 고쳐졌다"고 주장하는 두 보안 관련 통제(하향 경고 억제 로직, 차단 판정 파서)가 실제로 그 상태임을 재현 검증했다.
  - 제안: 조치 불요(검증 결과 기록). 참고로 두 결함 모두 이번 diff(리뷰 산출물)가 아니라 이전 커밋(`7dd4ad8c7` "5R 리뷰 반영")에서 이미 고쳐진 것이며, 이 라운드는 그 사실을 문서화한 산출물만 신규로 담고 있다.

- **[INFO]** `_retry_state.json` 스냅샷들이 로컬 개발자 머신의 절대 경로를 그대로 담아 커밋 이력에 영구 기록됨 — 낮은 리스크, 이번 diff가 새로 만든 패턴 아님
  - 위치: `review/code/2026/08/01/00_03_38/_retry_state.json:2`(`"session_dir": "/Volumes/project/private/clemvion/.claude/worktrees/harness-block-backstop-b56163/review/code/2026/08/01/00_03_38"`), 동일 필드가 `:4,7,47,48,...`(summary/router/prompt/output 파일 경로) 및 `00_33_34`/`01_17_35`/`01_17_47` 세션의 동종 파일에도 반복.
  - 상세: 오케스트레이터가 세션 상태를 기록할 때 절대 경로를 그대로 직렬화하며, 이 JSON은 `review/**`가 `.gitignore` 예외(`_prompts/`만 제외) 대상이라 커밋된다. 이로 인해 로컬 사용자명(`clemvion`)과 디렉터리 구조가 저장소 이력에 영구 보존된다. 다만 이는 (a) 자격증명·PII가 아니라 로컬 파일시스템 경로 수준의 정보이고, (b) 이 저장소 전체 `review/code/**`·`review/consistency/**`(수백 개 기존 세션)에 걸쳐 이미 확립된 동일 패턴이라 이번 44개 파일이 새로 만든 노출면이 아니며, (c) 이 도구의 위협 모델(단일 개발자 로컬 하네스)상 실익 있는 공격으로 이어지지 않는다.
  - 제안: 조치 불요(정보성). 이 관례를 바꾸려면 세션 스냅샷 직렬화 시 절대 경로 대신 프로젝트 루트 기준 상대 경로를 쓰는 것을 고려할 수 있으나, 이는 이번 diff의 스코프를 크게 벗어나는 하네스 전역 변경이라 별도 논의가 필요.

이 외 아래 항목은 조사했으나 문제 없음(44개 파일 전수 grep 기준):

- **인젝션**: SQL/커맨드/LDAP/XSS/경로 탐색 어느 것도 성립할 코드 경로 자체가 없음(마크다운/JSON 텍스트만 존재). 문서 안에 인용된 코드 스니펫(`subprocess.run(["git", ...])` 등)도 전부 "이 패턴이 안전하다"를 서술하는 인용문이지 신규 실행 코드가 아님.
- **하드코딩된 시크릿**: `api_key`/`secret`/`password`/`token=`/PEM·SSH 키 헤더/AWS 키 패턴/`postgres://`·`mysql://` 연결 문자열/JWT(`eyJ...`) 형태 전부 44개 파일에서 매치 0건.
- **인증/인가**: 해당 코드 경로 없음(리뷰 산출물에는 세션/실행 개념이 없음).
- **입력 검증**: 해당 없음 — 사용자 입력을 받는 지점 자체가 없음.
- **OWASP Top 10**: 웹 애플리케이션 표면(엔드포인트, 세션 쿠키, CSRF 등) 자체가 존재하지 않음.
- **암호화**: 해시/암호화 관련 코드는 리뷰 산출물이 과거에 검토했던 실제 소스(`hashlib.sha1` 사용처)를 서술만 할 뿐, 이번 44개 파일 자체는 암호화 로직을 포함하지 않음.
- **에러 처리**: 리뷰 문서들이 인용하는 예외 처리 서술(`traceback.print_exc(file=sys.stderr)` 등)은 전부 로컬 stderr 출력이며 네트워크로 전달되지 않는다는 과거 검토 결과의 인용일 뿐, 이번 파일 자체가 에러를 발생시키는 코드는 아님.
- **의존성 보안**: 신규 서드파티 의존성 관련 내용 없음(리뷰 문서들이 "표준 라이브러리만 사용"이라는 과거 검토 결과를 서술).

## 요약

이번 보안 리뷰 라운드(01_49_32)에 실제로 배정된 44개 변경 파일은 애플리케이션·하네스 실행 코드가 아니라 지난 네 차례 리뷰 세션(00_03_38, 00_33_34, 01_17_35, 01_17_47)이 생성한 마크다운 리포트와 JSON 상태 스냅샷(리뷰 산출물)뿐이다. `git diff --stat`/`git log`로 직접 대조한 결과, 실제 하네스 소스(`_shared/block_integrity.py`, `_shared/retry_state.py`, 훅·오케스트레이터·테스트)는 이 브랜치의 이전 커밋들(1R~5R 리뷰 반영, 최종 `7dd4ad8c7`)에서 이미 수정·커밋됐고 이번 라운드의 diff 베이스에는 포함되지 않는다. 그 결과 인젝션·시크릿·인증/인가·입력검증·암호화·에러노출·의존성 등 표준 보안 점검 관점이 적용될 신규 코드 표면이 이번 diff에는 존재하지 않는다(44개 파일 전수 grep으로 시크릿·연결문자열·위험 패턴 0건 확인). 다만 단순히 "해당 없음"으로 넘기지 않고, 이번에 처음 리뷰 대상이 된 `RESOLUTION.md`가 주장하는 두 건의 보안 관련 수정(Stop 훅 note 억제의 콘텐츠 기반 키잉, 하향 판정 파서의 최종-매치 우선순위)을 실제 소스와 대조해 문서와 코드가 어긋나지 않음을 직접 검증했다. 유일하게 기록해 둘 만한 저위험 관찰은 `_retry_state.json` 스냅샷들이 로컬 개발자의 절대 파일 경로를 커밋 이력에 남긴다는 점인데, 이는 이번 diff가 새로 만든 노출면이 아니라 이 저장소 전반에 이미 확립된 기존 관례이며 위협 모델상 실익 있는 공격으로 이어지지 않는다.

## 위험도

NONE
