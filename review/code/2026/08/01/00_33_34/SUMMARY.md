# Code Review 통합 보고서

## 전체 위험도

**MEDIUM** — CRITICAL 은 없으나, 이 PR 의 핵심 목적(Critical 하향을 조용히 놓치지 않는 backstop)을 구현하는 배선 자체에 완결성 gap 이 있고(`evaluate_review()` Gate 2 가 하향-모순 `notes` 를 차단 경로에서 유실), 그 gap 이 테스트로 전혀 보호되지 않는다는 사실이 3개 독립 reviewer(requirement/maintainability/testing) 수렴 + 실제 mutation 실험(738/738 테스트가 배선 제거 후에도 통과)으로 실증됐다. 기능적으로는 이미 차단이 발생하는 상황이라 "잘못된 ALLOW" 는 아니지만, 이 백스톱이 존재하는 이유 자체가 이 경로에서 무력화된다는 점에서 MEDIUM 으로 판정한다.

**라우터 상태**: 이번 세션은 routing 미사용(전체 14개 reviewer 직접 실행)이며, forced(router_safety) 화이트리스트 7명(`documentation, maintainability, requirement, scope, security, side_effect, testing`) 전원 결과가 확보되어 누락이 없다 — 강제 화이트리스트 미이행 리스크는 이번 세션에 해당하지 않는다.

## Critical 발견사항

없음.

## 경고 (WARNING)

| # | 카테고리 | 발견사항 | 위치 | 제안 |
|---|----------|----------|------|------|
| 1 | 로직·테스트 | `evaluate_review()` Gate 2 의 두 번째 차단 분기(`newest_impl_done < newest_spec_code`, stale `--impl-done` 세션 차단)가 직전에 `_newest_resolved_impl_done_mtime` 이 채운 하향-모순 `notes` 를 `ReviewDecision` 생성 시 전달하지 않아 유실시킨다. requirement·maintainability·testing 3개 reviewer 가 독립적으로 지적했으며, testing 은 `evaluate_review()` 최종 반환의 `tuple(notes)` 를 제거해도 `.claude/tests/` 738개 전부 그대로 통과함을 실측(mutation 실험)해, 이 배선이 기존 테스트로 전혀 보호되지 않음을 실증했다. 이 시나리오는 이미 차단이 발생하므로 "잘못된 ALLOW" 는 아니지만, 하향 사실을 "보이게" 만든다는 이 백스톱의 존재 이유가 바로 이 경로에서 무효화된다. | `.claude/hooks/_lib/review_guard.py:964-998` (문제 분기 979-986행; 대조 — 정상 반영 998행, notes 수집원 718행·756-759행) | Gate 2 의 두 차단 반환(969-978, 979-986행) 모두에 `tuple(notes)` 를 전달하거나 `_blocked(reason)` 형태의 공용 헬퍼로 통일. "차단되어도 notes 는 보존된다"를 확인하는 회귀 테스트(mock 없이 실제 하향-모순 `--impl-done` 세션으로 `evaluate_review()` 를 직접 호출하는 통합 테스트) 추가. |
| 2 | 스코프 | `merge_coordinator_orchestrator.py` 의 "Git / gh helpers" 섹션 구분 주석이, 정규식 기반 함수 추출이 함수 사이 주석까지 삼키는 사고를 복구한 `b06982ec4`("3R 부분 반영") 커밋에서 누락됐다. 같은 결함 클래스가 이미 2개 파일(`code_review_orchestrator.py` 27줄, `consistency_orchestrator.py` 3줄)에서는 복원됐는데, 세 번째 발생분(이 파일)만 그 복구 대상 감사에서 빠졌다 — 커밋 메시지의 "실측 28줄"이라는 자체 감사 수치도 이 3줄을 포함하지 않아 감사 자체가 불완전했다는 신호. 기능적 영향은 없는 cosmetic 유실. | `.claude/skills/merge-coordinator/scripts/merge_coordinator_orchestrator.py:100-125` (특히 121-125행, `_apply_status_update` delegate 직후 ~ `_git` 정의 직전) | `origin/main` 의 해당 3줄(`# ---` / `# Git / gh helpers` / `# ---`)을 그대로 복원. 동일 정규식 추출을 거친 다른 위치에도 같은 결함 클래스가 더 있는지 재점검. |
| 3 | 아키텍처·유지보수성 | 신규 `notes` advisory 출력이 push/stop 두 형제 훅에서 서로 다른 방식으로 구현됐다 — push 훅(`_report_notes`)은 `try/except Exception: pass` 로 감싸 "관측이 가드 자체를 깨서는 안 된다"는 이 파일군의 원칙을 지키지만, stop 훅은 동일 책임을 예외 가드 없는 인라인 코드로 작성했다. `main()` 바깥 `try/except` 가 결국 fail-open 시키긴 하지만, 이 경로를 타면 그 아래 **PLAN-COMPLETE 게이트 점검이 이번 실행에서 통째로 스킵**된다 — push 훅에서는 같은 상황이 국소적으로 흡수되는 것과 대조적. 또한 `failopen_state.Outcome`/push `_Outcome` 에는 `.notes` 필드가 추가됐지만 stop 훅의 폴백 `_Fallback` 에는 없어(현재는 죽은 필드) 형태가 어긋난다 — 이 PR 이 다른 곳(`_shared/retry_state.py`, `_shared/block_integrity.py`)에서 없애려는 "Change both" 중복 패턴이 이 지점에서 작게 재현된 것. | push: `.claude/hooks/guard_review_before_push.py:733-750`(`_report_notes`), `:809-866`(`_evaluate_over_targets`) / stop: `.claude/hooks/guard_review_before_stop.py:360-361` / 관련: `.claude/hooks/_lib/failopen_state.py:47-54`(`Outcome.notes`), push `_Outcome:793-799`, stop `_Fallback:103-107`(`.notes` 없음) | 스트림 선택+출력 로직을 `failopen_state.py` 에 공유 헬퍼(예: `emit_notes(notes, stream)`)로 추출해 두 훅이 재사용하도록 통일. 최소한 stop 훅도 `try/except Exception: pass` 로 감싸고, `_Fallback` 에 `.notes` 필드를 추가해 형태를 맞출 것. |
| 4 | 아키텍처 | 하향-모순 advisory 가 stop 훅에서 스로틀 없이 세션의 매 턴 종료 시도마다 반복 출력될 수 있다 — 바로 몇 줄 아래의 nudge 메커니즘(`_nudge_once`, marker 기반 세션/브랜치당 1회 제한)과 비대칭이며, 이 PR 자신이 `review_guard.py`/`block_integrity.py` docstring 에서 세운 "매번 우는 경고는 아무도 안 읽는다" 는 원칙과 반대 방향이다. side_effect reviewer 도 동일 관측("once-per-branch 마커 없음")으로 독립 확인. | `.claude/hooks/guard_review_before_stop.py:360-361` (대조: nudge 스로틀 `:365-370`, `_nudge_once` 정의 `:231-240`) | `_nudge_once` 처럼 세션/브랜치당 1회로 제한하거나, 최소한 동일 세션·동일 사유의 반복 출력을 억제하는 marker 도입. |
| 5 | 테스트 | `retry_state.save_state()` 의 신규 atomic-write(temp file + `os.replace`) 안전성 보장이 전혀 테스트로 검증되지 않는다. 원본 `_save_state` 는 단순 truncating `open(state_file, "w")` 였는데 이번에 "동시 reader 가 반쯤 쓰인 파일을 보는 창을 없앤다"는 의미 있는 안전성 속성이 새로 생겼음에도, `test_retry_state_shared.py` 를 포함해 어떤 테스트도 tmp 파일 생성/정리, 쓰기 실패 시 원본 보존, `os.replace` 실제 호출 여부를 확인하지 않는다 — 조용히 원래의 truncating write 로 퇴행해도 기존 테스트가 잡지 못한다. | `.claude/_shared/retry_state.py:50-75`(`save_state`) | `json.dump` 를 mock 해 쓰기 도중 예외를 강제하고 (a) 원본 보존 (b) `.tmp.*` 정리를 단언하는 테스트, 정상 경로에서 `os.replace` 호출을 pin 하는 테스트 추가. |
| 6 | 테스트 | `merge_coordinator_orchestrator.py` 가 `_load_state`/`_save_state`/`_apply_status_update` 3개 함수를 `_shared/retry_state.py` 로 위임하도록 바뀌었는데, 다른 두 orchestrator(`code_review_orchestrator.py`/`consistency_orchestrator.py`)는 신규 `test_retry_state_shared.py` + 기존 subprocess 기반 테스트로 커버되는 반면 `merge_coordinator_orchestrator.py` 를 다루는 테스트 파일이 `.claude/tests/` 에 전혀 없다 — 세 번째 소비자로의 전환 자체가 어떤 자동 테스트로도 지켜지지 않는다. | `.claude/skills/merge-coordinator/scripts/merge_coordinator_orchestrator.py:113-122` | `test_retry_state_shared.py` 에 `merge_coordinator_orchestrator.py` 의 `--update`(`_apply_status_update`) CLI 경로 케이스를 추가하거나 전용 테스트 파일 신설. |
| 7 | 요구사항(프로세스 정합성) | 이번 PR 이 대폭(19줄) 갱신한 `plan/in-progress/harness-review-gate-ci-backstop.md` 자신의 `worktree:` frontmatter 가 이미 사라진 worktree(`harness-review-gate-fixes-1bd6aa` — `git worktree list`/로컬 브랜치 어디에도 없음, reap 또는 merge 됨)를 그대로 가리킨다. `plan-lifecycle.md` 의 "연결 판정" 로직이 이 필드로 plan-worktree 연결을 확인하므로, 이 plan 이 현재 worktree(`harness-block-backstop-b56163`)에 의해 살아있게 유지되고 있다는 사실을 인식하지 못한다. | `plan/in-progress/harness-review-gate-ci-backstop.md:1-7`(frontmatter 3번째 줄) | `worktree: harness-block-backstop-b56163` 로 갱신(다음 worktree 로 넘어가면 그때 또 갱신). |
| 8 | 문서화 | 신규 "하향-모순 백스톱" 경고를 서술하는 두 문서(`consistency-summary.md`, `SKILL.md`)가 "게이트가 이제 checker `[CRITICAL]` 과 모순되면 경고를 낸다"고 범위 한정 없이 일반화한다. 실제로는 `contradiction_note()` 가 `evaluate_review()` 의 `if spec_linked:` 분기(Gate 2) 안에서 **`--impl-done` 모드 세션만** 대조할 때 단 한 곳에서만 호출된다 — spec-linked 변경이 없거나 세션이 `--spec`/`--plan`/`--impl-prep` 모드면 이 백스톱은 발화하지 않는다. 두 문서 모두 "금지 조항이 여전히 1차 방어"라고 명시해 완전한 안전 오신은 아니지만, 실제 적용 범위가 문구보다 좁다. | `.claude/agents/consistency-summary.md:49-53`(§요약 지침 3), `.claude/skills/consistency-checker/SKILL.md:113-116`(§4 BLOCK 처리) | 범위 한정 절 추가. 예: "(현재는 `--impl-done` 세션이 push/turn-end 게이트의 Gate 2 로 채택될 때만 발화하며, 다른 모드 세션이나 spec-linked 변경이 없는 경우는 본 금지 조항에만 의존합니다.)" |
| 9 | 동시성 | `apply_status_update()` 의 `agent_history`/`rate_limit_episodes`/`last_reset_hint_sec` 세 필드는 동시 writer 에 대해 원자적이지 않다(lost update). `agents_success/pending/fatal` 버킷은 `reconcile_state_with_disk()` 가 디스크 기준으로 매번 재계산해 유실이 있어도 다음 reconcile 에서 수렴하지만, 이 세 필드는 그런 수렴 경로가 없다(코드 주석 자신이 "tracked, not solved here" 로 인지). CLAUDE.md 가 "독립적인 도구 호출은 병렬로" 지시하므로, 여러 sub-agent 의 `--update` 가 같은 턴에 병렬 Bash 호출로 실행되는 경로가 실제로 존재해 이론적 시나리오가 아니다. 유실 시 `last_reset_hint_sec` 이 과소값으로 남아 `/loop` 가 rate-limit 해제 전에 재시도하거나, `agent_history` 일부가 감사 근거에서 조용히 빠질 수 있다. | `.claude/_shared/retry_state.py:158-188`(특히 171-172, 174-175, 180) | 이미 인지된 트레이드오프이므로 즉시 수정 요구보다는, (1) `--update` 호출부에 "동일 세션 병렬 배치 금지" caveat 추가 또는 (2) 경량 `fcntl.flock` 고려. 후자를 택하지 않는다면 `failopen_state.py:111-115` 의 유사 근거와 상호 참조를 남길 것. |

## 참고 (INFO)

| # | 카테고리 | 발견사항 | 위치 | 제안 |
|---|----------|----------|------|------|
| 1 | 보안·부작용 | `retry_state.save_state()` 의 atomic write(temp file + `os.replace`) 도입에 따른 부수 효과 2가지: (a) `open(tmp, "w")` 이 `O_EXCL`/`O_NOFOLLOW` 없이 열려 이론적으로 심볼릭 링크 선점에 취약하나, 단일 사용자 로컬 개발 하네스라는 위협 모델상 실익 없는 공격(공격자가 이미 같은 디렉터리 쓰기 권한을 가져야 함); (b) 프로세스가 cleanup 전에 크래시하면 `_retry_state.json.tmp.<pid>` 잔여 파일이 세션 디렉터리에 남아 `git add -A` 등으로 커밋될 수 있음(원본 JSON 자체는 훼손되지 않음). 둘 다 낮은 실익/영향. | `.claude/_shared/retry_state.py:65-69`(tmp 경로 생성~`os.replace`) / 호출부 `code_review_orchestrator.py:194`, `consistency_orchestrator.py:95`, `merge_coordinator_orchestrator.py:118` | 조치 불요(정보성). 필요 시 `tempfile.mkstemp(dir=...)` 로 교체(배타적 생성 보장) 또는 `.gitignore` 에 `review/**/*.tmp.*` 패턴 추가. |
| 2 | 요구사항 | `block_integrity.summary_block_verdict()` 의 END-우선 판정 규칙에 이론적 잔여 엣지케이스가 있다 — 맨몸 stale 템플릿 줄이 override 배너보다 텍스트상 먼저 나오면 `.search()` 가 override 이전 판정을 채택해 "배너가 이긴다"는 설계 의도와 반대 결과가 날 수 있다. 실측 732개 세션 표본에서는 관찰되지 않음(자동 생성 줄은 항상 검증문구가 뒤따르는 문서 관례 덕분). | `.claude/_shared/block_integrity.py:60-65,96-107` | 낮은 우선순위. 필요 시 override 배너 전용 마커를 앵커에 포함해 "맨몸 템플릿" 과 "배너" 를 구조적으로 구분. |
| 3 | 요구사항 | `retry_state.load_state()` 는 파일이 없을 때는 우아하게 처리(stderr + `sys.exit(1)`)하지만, 파일은 있으나 JSON 이 손상된 경우 raw traceback 과 함께 크래시한다 — 원본에도 있던 비대칭이며 이번 PR 이 새로 만든 결함은 아님(atomic write 가 가장 흔한 손상 원인은 이미 제거). | `.claude/_shared/retry_state.py:41-47`(`load_state`) | 선택 사항: `json.JSONDecodeError` 를 잡아 "파일 없음" 과 대칭적인 메시지로 변환. |
| 4 | 테스트 | 동일 사실("게이트가 이제 checker `[CRITICAL]` 과 모순되면 경고")을 서술하는 두 문서(`consistency-summary.md`/`SKILL.md`) 간 drift 를 잡는 테스트가 없다 — 이 프로젝트는 다른 곳(`test_router_safety_policy_doc.py` 등)에서 이런 drift 를 테스트로 고정하는 관행이 있는데 이번 두 문장에는 적용되지 않음. 현재는 표현만 다르고 내용은 일치. | `.claude/agents/consistency-summary.md`§요약지침3, `.claude/skills/consistency-checker/SKILL.md`§4 | 우선순위 낮음 — 3번째 문서가 같은 사실을 언급하게 되면 재고. |
| 5 | 문서화 | `_shared/retry_state.py` 의 형제 함수 4개(`save_state`/`reconcile_state_with_disk`/`emit_summary_state`/`apply_status_update`)는 모두 docstring 을 갖췄는데 `load_state` 만 없다 — 특히 파일 없으면 `sys.exit(1)` 하는 호출자 계약이 이름만으로 드러나지 않는다. | `.claude/_shared/retry_state.py:41`(`load_state`) | 한 줄 docstring 추가(예: 파일 없으면 stderr 메시지와 함께 exit(1) 한다는 계약 명시). |
| 6 | 문서화 | `.claude/tests/README.md` 의 `test_block_integrity.py` 행이 판정 predicate(`CountCriticalTagsTest`/`VerdictIsAnchoredTest`/`DowngradedCriticalsTest`) 설명에는 충실하나, 같은 파일의 "배선(wiring)" 검증 클래스(`GateSurfacesTheContradictionTest`/`NotesReachBothHooksTest` — 게이트가 predicate 를 실제 호출하는지, 경고가 올바른 스트림에 도달하는지)는 언급하지 않는다. 다른 행들은 배선 검증도 명시하는 관례가 있어 이 행만 예외. | `.claude/tests/README.md`(`test_block_integrity.py` 행) | "게이트가 이 predicate 를 실제로 호출하는지, 경고가 훅별 올바른 스트림에 도달하는지" 한 문장 추가. |
| 7 | 유지보수성 | 신규 테스트가 이미 있는 공용 헬퍼를 재사용하지 않았다 — `test_orchestrator_derives_its_list_from_here` 는 `_lib` 네임스페이스 충돌 회피용 fresh-interpreter 패턴을 인라인으로 다시 짜 이미 4개 파일이 공유하는 보일러플레이트의 5번째 사본을 만들었고(plan 후속 #10 에 `_harness.py` 추출이 이미 등재돼 있음), `test_role_instructions_registers_the_same_checkers` 는 같은 파일 안에서 이미 쓰는 `_harness.load_module_by_path` 를 두고 `importlib` 시퀀스를 손으로 재작성했다. | `.claude/tests/test_block_integrity.py:66-86,88-107` | 지금 당장 통합 불필요(범위 밖 후속). `role_instructions` 케이스는 `_harness.load_module_by_path` 호출로 즉시 교체 가능. 향후 `_harness.py` 추출 시 이 파일도 이관 대상에 포함. |
| 8 | 유지보수성 | `_evaluate_over_targets` 의 `if notes is None: notes = []` 방어 분기가 이번 diff 자신으로 인해 죽은 코드가 됐다 — 근거 주석은 "`_Outcome` 이 `failopen_state`(이 필드 이전 정의) 또는 로컬 fallback 에서 올 수 있다"고 설명하지만, 이 diff 가 두 `Outcome` 정의(`failopen_state.Outcome`, 로컬 `_Outcome`) 모두에 `self.notes: list = []` 를 추가해 도달 불가능한 분기가 됐다. | `.claude/hooks/guard_review_before_push.py:850-856` | 방어 코드 자체는 무해하니 유지 가능. 주석만 "두 정의 모두 이제 `.notes` 를 가지므로 이 분기는 향후 세 번째 구현 대비" 로 정정. |
| 9 | 유지보수성 | `for note in (getattr(decision, "notes", ()) or ()) if decision else ():` 형태의 중첩 삼항식이 `for` 의 iterable 자리에 들어가 있어 가독성이 떨어진다(동작은 올바름). | `.claude/hooks/guard_review_before_stop.py:360` | `notes = getattr(decision, "notes", ()) if decision else ()` 로 변수 분리 후 `for note in notes or ():` 형태로 단순화. |
| 10 | 유지보수성 | `_shared/retry_state.py` 추출 배경("AST 비교 결과 4/5 함수 동일")을 설명하는 거의 동일한 산문이 4개 파일(모듈 docstring + 3개 orchestrator)에 반복된다 — 코드 중복은 사라졌지만 설명 주석 차원에서 "Change both" 형태가 남음. | `.claude/_shared/retry_state.py:1-29` + 3개 orchestrator 파일 각 근거 주석 | 시급하지 않음. 향후 근거 갱신 시 `_shared/retry_state.py` 한 곳만 갱신하고 호출부는 링크로 참조. |
| 11 | 아키텍처 | 5-checker 레지스트리(`ALL_CHECKERS`)의 SSOT 가 "SUMMARY 판정 검증"이 책임인 `block_integrity.py` 에 얹혀 SRP 경계가 약간 흐려진다. `test_block_integrity.py::CheckerListIsCanonicalTest` 가 `consistency_orchestrator.ALL_CHECKERS`/`role_instructions.CHECKER_INSTRUCTIONS` 와의 일치를 교차검증해 drift 리스크는 낮지만, 개념적으로 "체커 레지스트리"가 "그 산출물을 사후 검증하는 모듈"의 부속물이 됨. | `.claude/_shared/block_integrity.py:67-78` | 시급하지 않음. 4번째 소비자가 생기면 중립적인 `_shared/checkers.py` 로 레지스트리만 분리 고려. |
| 12 | 아키텍처 | `_shared/retry_state.py` 모듈 docstring 이 여전히 "shared by both orchestrators"(2개)로 서술하지만 실제로는 `merge_coordinator_orchestrator.py` 까지 3번째 소비자로 (부분) 합류했다 — 이 브랜치 자체의 커밋 이력(`7b54b088a`→`a0dcebea2`)이 이 서술을 stale 하게 만듦. | `.claude/_shared/retry_state.py:1` | docstring 을 "세 orchestrator, 단 merge-coordinator 는 3/5 함수만 위임" 정도로 정정. |
| 13 | 성능 | 현재는 무해하나 향후 스케일링 시 고려할 성능 관찰 3가지(모두 실측, 조치 불요): (a) 이 백스톱이 얹히는 기반 — `review/code`·`review/consistency` 전체를 캐시 없이 매번 `os.walk` 하는 기존 패턴 — 은 세션 수(현재 732/769개)에 비례해 단조 증가하며 이 훅은 매 push·매 턴 종료마다 동기 실행됨(`evaluate_review()` 실측 약 95~110ms, hang 없음); (b) `contradiction_note()` 가 채택된 세션 1건에 한해 이미 읽은 `SUMMARY.md` 를 다시 읽음("1+1"이지 N+1 아님); (c) `_evaluate_over_targets` 의 advisory 중복 제거가 형태상 O(targets×notes) — 둘 다 실사용 범위(한 자릿수)에서 무영향. 이번 diff 자체(신규 backstop)는 채택된 세션 1개에만 적용되도록 이미 O(1) 로 유계 설계돼 있어 문제 없음(전용 회귀 테스트로 고정됨). | `.claude/hooks/_lib/review_guard.py:389-397,678-686,524-554,718-760`, `.claude/_shared/block_integrity.py:110-143`, `.claude/hooks/guard_review_before_push.py:847-859` | 지금 조치 불요. 세션 수가 한 자릿수 더 늘면 세션별 판정을 1회만 계산해두는 인덱스/캐시, 또는 디렉터리명 내림차순 정렬 후 조기 종료 방식을 고려. |
| 14 | 동시성 | `failopen_state.py` 의 연속 fail-open 카운터도 동일 클래스의 미잠금 read-increment-write 이나, 코드 주석이 이미 "관측용 카운터에 `fcntl.flock` 은 과함" 이라는 근거를 명시하고 blast radius 가 배너 지연으로 국한돼 판단 자체가 타당함. | `.claude/hooks/_lib/failopen_state.py:63-84,97-116` | 조치 불요(이미 근거 있는 수용된 트레이드오프). |
| 15 | 동시성 | Stop 훅의 nudge 마커(`_already_nudged`→`_mark_nudged`)가 check-then-act(TOCTOU) 이나, 현재 아키텍처에서 동일 세션의 Stop 훅이 진짜로 동시에 두 번 실행되는 경로가 없어 실질적으로 트리거되지 않음. | `.claude/hooks/guard_review_before_stop.py:208-240` | 조치 불요. 향후 훅 호출 모델이 비동기/다중 세션 동시실행으로 바뀌면 `os.open(marker, os.O_CREAT|os.O_EXCL)` 로 원자화 고려. |
| 16 | 동시성·의존성·아키텍처 | `merge_coordinator_orchestrator.py` 에는 `reconcile_state_with_disk` 자기치유가 없다 — `Agent` tool 로 직접 fan-out 된 merge-coordinator 세션은 `_retry_state.json` 이 prepare 시점 스냅샷에 멈춘 채 SUMMARY 가 실제 성공을 보고하는 모순을 겪을 수 있다(다른 두 orchestrator 는 이미 고침). concurrency·architecture·dependency 3개 reviewer 가 독립적으로 확인. 코드 주석과 plan 문서 §9 양쪽에 "별도 skill 의 동작 변경이라 별도 PR 로 분리"라고 명시돼 있어 이번 diff 가 은폐한 회귀가 아니라 의도된 defer. | `.claude/skills/merge-coordinator/scripts/merge_coordinator_orchestrator.py:100-112`, `plan/in-progress/harness-review-gate-ci-backstop.md`§9 | 이번 PR 범위 조치 불요(이미 추적됨). 후속 PR 에서 `_shared/retry_state.py` 에 `reconcile_state_with_disk` 를 두고 세 orchestrator 가 공유하도록 완결. |
| 17 | 의존성 | 하네스 Python 최소 버전이 명문화된 문서/설정이 없다 — 이번 PR 은 `str.removesuffix()`(3.9+) 대신 수동 슬라이싱을 써서 정확히 회피했지만(주석에 근거 명시, grep 으로 저장소 전체에 `removesuffix`/`removeprefix` 미사용 확인), CI(`python-version: '3.x'`)는 최신 버전으로 뜨기 때문에 로컬 구버전 `python3` 전용 회귀를 애초에 잡을 수 없는 구조적 공백이 남아있다(이번 PR 이 만든 문제는 아님). | `.claude/_shared/block_integrity.py:133-136` | 이번 PR 조치 불요. 여유 있을 때 `.claude/tests/README.md` 또는 convention 문서에 최소 Python 버전 명문화 고려. |
| 18 | 보안 | 신설된 하향-금지 backstop 은 설계상 "경고 전용"이다 — `[CRITICAL]` 하향이 재발해도 push/Stop 훅 출력에 경고 문구만 얹을 뿐 `blocked` 값 자체는 바꾸지 않아, 경고를 무시하면 하향된 세션이 여전히 게이트를 통과한다. 정당한 중복 병합·등급 상향 가능성 때문에 하드 블록은 과잉 차단이라는 근거가 docstring 에 명시된 의도된 트레이드오프이며, 오히려 기존에 전혀 없던 가시성을 추가한 보안-긍정적 변경이다. | `.claude/_shared/block_integrity.py:22-26`, `.claude/hooks/_lib/review_guard.py:756-759,964-998` | 조치 불요(의도된 설계). 이 경고가 실제로 몇 번 무시되는지 계측되면 하드 블록 승격 여부를 재검토할 근거가 됨. |

## 에이전트별 위험도 요약

| 에이전트 | 위험도 | 핵심 발견 |
|----------|--------|-----------|
| security | LOW | atomic write 심볼릭 링크 이론적 위험(낮음), 백스톱은 설계상 경고 전용(의도됨) |
| requirement | LOW | Gate2 notes 유실(WARNING #1 기여), plan frontmatter stale worktree(WARNING #7), 그 외 이전 라운드(1R~3R) 반영 사항 대부분 재확인 |
| scope | LOW | merge_coordinator 구분 주석 유실(3R 복구 커밋에서 누락, WARNING #2), retry_state 추출 자체는 재확인 결과 새 이슈 없음 |
| side_effect | LOW | atomic write 임시파일 + ALLOW 경로 신규 출력(둘 다 의도된 설계), 신규/확장 시그니처 하위호환 확인 |
| testing | MEDIUM | 백스톱 핵심 배선(notes 전파) 무테스트 — mutation 실험(738/738 통과)으로 실증(WARNING #1 기여). `save_state` atomic-write(WARNING #5), `merge_coordinator` 위임(WARNING #6) 도 무테스트 |
| documentation | LOW | 백스톱 경고 문서 서술이 실제 적용 범위(`--impl-done`+spec-linked 한정)보다 넓게 일반화됨(WARNING #8). 그 외 문서 품질 전반 양호 |
| maintainability | LOW | Gate2 notes 유실(WARNING #1 기여), 두 훅 예외처리 비대칭(WARNING #3 기여), 테스트 보일러플레이트 중복(INFO #7) |
| architecture | LOW | notes 배선이 두 훅에서 상이하게 중복 구현(WARNING #3 기여), 하향 경고 스로틀 부재(WARNING #4), 순환의존/레이어 위반 없음 |
| performance | NONE | 신규 backstop 자체는 O(1) 유계 설계로 확인(회귀 테스트 존재), 기반 전체이력 재스캔은 이번 diff 무관하게 점진 성장(현재 무해) |
| concurrency | LOW | `agent_history`/`rate_limit` 필드 lost-update 가능(WARNING #9, 문서화된 잔여 리스크). thread/asyncio 직접 사용 없음 |
| dependency | NONE | 신규 외부 의존성 0건, 내부 의존성 이관 방향·중복제거 모두 정확 |
| database | NONE | 해당 없음 — DB 관련 코드 경로 자체가 없음 |
| api_contract | NONE | 해당 없음 — 제품 REST API 변경 없음, 내부 CLI/hook 계약은 하위호환 확인 |
| user_guide_sync | NONE | 해당 없음 — `codebase/`·`spec/` 변경 0건, 매트릭스 21개 trigger 매칭 0건 |

## 발견 없는 에이전트

- **database** — 리뷰 대상 15개 파일 전부 `.claude/` 하네스 코드/문서로, SQL·ORM·스키마·트랜잭션 등 데이터베이스 관련 코드 경로 자체가 없음을 확인.
- **api_contract** — `codebase/` 제품 코드 변경 0건. 내부 CLI/hook 프로토콜 확장(`notes` 필드, `in_flight_ok` 파라미터 등)은 모두 하위호환 기본값을 갖춰 계약 위반 없음.
- **user_guide_sync** — `.claude/config/doc-sync-matrix.json` 21개 trigger 행 중 매칭 0건(`codebase/`·`spec/` 변경 없음, 전량 `.claude/`+`plan/in-progress/`).

## 권장 조치사항

1. `evaluate_review()` Gate 2 의 두 차단 반환 경로에 `tuple(notes)` 를 전달(또는 공용 헬퍼로 통일)하고, "차단되어도 notes 는 보존된다"를 확인하는 회귀 테스트를 추가한다 — 이 PR 의 핵심 목적(하향 가시화)이 실제로 완결되는지를 보호하는 가장 중요한 조치이며, 3개 reviewer 수렴 + mutation 실증으로 뒷받침된다. (WARNING #1)
2. `retry_state.save_state()` 의 atomic-write 안전성(원본 보존, tmp 정리, `os.replace` 호출)을 검증하는 테스트를 추가한다. (WARNING #5)
3. `merge_coordinator_orchestrator.py` 의 `_shared/retry_state.py` 위임 경로에 대한 회귀 테스트를 신설해 다른 두 orchestrator 와 동등한 커버리지를 갖춘다. (WARNING #6)
4. push/stop 두 훅의 `notes` 출력 로직을 공유 헬퍼로 통합하고 stop 훅에 예외 처리를 추가하며(현재 예외 시 PLAN-COMPLETE 게이트 점검이 통째로 스킵될 수 있음), 하향-모순 경고에 세션/브랜치당 1회 스로틀을 도입한다. (WARNING #3, #4)
5. `merge_coordinator_orchestrator.py` 의 "Git / gh helpers" 구분 주석을 복원한다(3R 복구 커밋에서 누락된 세 번째 인스턴스). (WARNING #2)
6. `plan/in-progress/harness-review-gate-ci-backstop.md` 의 `worktree:` frontmatter 를 현재 worktree(`harness-block-backstop-b56163`)로 갱신한다. (WARNING #7)
7. `consistency-summary.md`/`SKILL.md` 의 백스톱 서술에 "현재는 `--impl-done` 세션이 Gate 2 로 채택될 때만 발화" 라는 범위 한정 문구를 추가한다. (WARNING #8)
8. (낮은 우선순위) `retry_state.load_state()` docstring 추가, `README.md` 의 배선-검증 언급 보강, 테스트 보일러플레이트 중복을 향후 `_harness.py` 추출 후속 항목(#10)에 포함한다. (INFO #5, #6, #7)

## 라우터 결정

- `routing_status=skipped`: 라우터 미사용 — 전체 14개 reviewer 직접 실행(`security, requirement, scope, side_effect, testing, documentation, maintainability, architecture, performance, concurrency, dependency, database, api_contract, user_guide_sync`).
- **강제 포함(router_safety)**: `documentation, maintainability, requirement, scope, security, side_effect, testing` (7명) — 전원 결과 확보 확인됨(누락 없음).
- **제외**: 없음(routing 미사용이므로 router 에 의한 제외 자체가 발생하지 않음).