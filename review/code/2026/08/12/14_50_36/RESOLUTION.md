# RESOLUTION — `14_50_36`

리뷰 결과: **CRITICAL 0 / WARNING 2 / RISK LOW**. reviewer 8명 실행, 강제 7명 전원 결과 확보
(`forced_missing: []`, `unfinished: []`).

직전 라운드 CRITICAL(오탐)이 이번 라운드에서도 **독립 재확인**됐다 — 8명 전원이 `catchError`
위치를 검증했고 소스 기준 `catchError`(107행) < `switchMap`(113행). 워크트리 뮤테이션
아티팩트였다는 판정이 굳어졌다.

---

## WARNING #1 (concurrency) — 조치 불요, 이미 문서화·추적됨

fail-open 이 GET→SET 비원자 구조와 결합해 **Redis 장애 지속 구간 전체**로 중복 실행 창이
넓어진다는 지적. 리뷰어 스스로 "spec 이 명시적으로 승인한 가용성 우선 트레이드오프이며 이미
클래스 docstring·`CHANGELOG.md`·plan 백로그로 문서화·추적됨. **코드 되돌릴 필요 없음**" 으로
판정했다. 직전 라운드에서 한 조치가 그대로 유효하다.

남는 리스크는 "관측 지표가 실제 구현되기 전까지 운영이 이 구간을 인지할 수단이 없다" 인데,
그 항목은 plan `§후속` 에 이미 등재돼 있다. 이 PR 에서 관측 인프라까지 넣는 것은 스코프
확장이라 하지 않는다.

## WARNING #2 (documentation) — **내 RESOLUTION 기록이 거짓이었다** → 정정 + 실제 조치

직전 `14_27_02/RESOLUTION.md` 의 INFO 6 처분에 **"헤더는 갱신함"** 이라고 적었는데 거짓이었다.
그 라운드에서 실제로 갱신한 것은 `idempotency.interceptor.ts` 의 **클래스 docstring** 이고,
지적 대상이던 `idempotency.interceptor.spec.ts` 의 **파일 최상단 헤더**(신규 3번째 describe
미언급)는 손대지 않았다. **직전 PR 라운드에서 헤더에 "두 번째 describe" 를 추가했던 일을
이번 것으로 착각**해 쓴 기록이다.

조치 둘 다 했다:

1. **실제 갱신** — 헤더에 세 번째 describe(Redis 런타임 장애 fail-open: 조회 실패 강등 ·
   적재 실패 · 비-`Error` reject · 409 캐너리) 한 줄 추가.
2. **기록 정정** — `14_27_02/RESOLUTION.md` 의 해당 셀을 사실대로 바꾸고, 문서 끝에 왜
   지우지 않고 정정으로 남기는지를 적었다: `review/**` 는 SoT 가 아니지만 후속 세션이
   처분 기록으로 "이미 처리됨" 을 판단하므로, **거짓 완료 기록은 그 판단을 오염시킨다.**

> 이 세션에서 **"내가 쓴 기록이 실제와 다르다"** 가 세 번째다 — §R8 오인용(테스트 이름) ·
> "캐시 히트 경로 **전체**를 메웠다" 과장 · 본 건. 셋 다 리뷰어가 잡았고 셋 다 코드가 아니라
> **내 서술**의 문제였다. 코드는 매번 맞았는데 그 코드에 대해 내가 쓴 문장이 틀렸다.

## INFO 처분

| # | 항목 | 처분 |
|---|---|---|
| 1·2·3 | fail-open 트레이드오프 · 로그 미노출 · R8 초과 | 조치 불요 (문서화/추적 완료 또는 비결함) |
| 4·5·6 | `bodyHashOf` 이동 · plan 백로그 추가 · 리뷰 산출물 누적 | 조치 불요 (정상 review-fix 산출물) |
| 7 | GET/SET 로그 조립 중복 2곳 | **보류 유지** — 3번째 실패 경로가 생기면 `warnCacheFailure` 추출 재검토 |
| 8 | 헤더·블록 docstring 미반영 | **조치** — WARNING #2 와 동일 근본 원인, 헤더 갱신으로 함께 해소 |
| 9·10 | 전 라운드 CRITICAL 오탐 재확인 · "세 경로" 주장이 이제 전부 테스트로 뒷받침 | 조치 불요 (확인 목적) |
| 11 | GET→SET 비원자(선재) | 백로그 유지 (`SET NX EX` 검토) |

## 검증

- eslint **0 errors / 0 warnings**
- backend unit — idempotency spec **16/16** (변경은 주석 1줄이라 테스트 수 불변)

## 수렴 판단

2라운드 발견 성격: **구현 결함(라운드 0) → 트레이드오프 문서화·테스트 보강 → 내 기록 정확성.**
코드 결함은 이번 라운드 **0** 이고, WARNING 2건 중 하나는 리뷰어가 스스로 "코드 변경 불필요"
로 닫았으며 다른 하나는 주석 한 줄과 기록 정정이다. 이 축에서 더 나올 것은 없다고 본다.
