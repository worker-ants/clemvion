# 변경 범위(Scope) 리뷰 — 세션 `12_40_58` (누적 diff `origin/main...HEAD`, 50파일, 4번째 라운드)

## 컨텍스트 — 이 리뷰는 4번째 scope 라운드다

`git log --oneline origin/main..HEAD` 확인 결과 이 브랜치는 8개 커밋으로 구성된다:

```
b0b57366f fix(backend): 내가 붙인 테스트 이름이 spec 이 반대로 말하는 동작을 spec 으로 정당화했다   (round3 WARNING fix)
7c7aee1c4 fix(backend): 방어를 새로 만들어 놓고 그걸 검증하는 테스트는 안 만들었다 (리뷰 WARNING 2건) (round2 WARNING fix)
67b7d7d77 chore(plan): lint warning 처분 기록을 실제로 한 것에 맞춘다 + 백로그 파일명 정정
ee8e44e8f fix(backend): 남은 lint warning 21건 처분 + `--max-warnings 0` 으로 바닥을 건다
ba93680ab revert(backend): 리뷰어가 워크트리에 남긴 8파일을 내가 `git add -A` 로 커밋했다 — 되돌린다
9add2eba7 chore(plan): backend lint warning 전량 처분 확정 — ratchet 이 아니라 0 을 바닥으로 건다
e95201932 test(backend): 방금 타입을 붙인 콜백이 한 번도 실행된 적이 없었다 + 리뷰 산출물
17221ecb9 wip(backend): lint warning 46→21 — 세 파일의 `any` 경계에 타입을 붙인다
```

이 diff는 이미 세 차례(`11_06_12`, `12_05_39`, `12_24_14`) scope 리뷰를 거쳤고 세 번 다
CRITICAL/WARNING 없이 NONE/LOW 로 수렴했다(각 라운드의 `scope.md` 자체가 이번 diff 에
파일 22, 35, 47 로 포함되어 있어 직접 대조 가능). `git show b0b57366f`(HEAD, 가장 최신
커밋)를 직접 열어 커밋 메시지와 diff 를 대조한 결과, 커밋 본문이 예고한 변경(테스트 이름
축소 + `409` 캐너리 테스트 신설 + `catch` 분기 테스트 신설 + docstring 1문단 + plan
백로그 항목 추가)과 실제 diff 가 정확히 일치한다 — 이번 라운드에서 처음 노출되는 실질
변경분은 없다.

## 발견사항

- **[INFO]** "타입 주석만" 이라는 원 선언 범위를 신규 테스트가 다시 넘어섰다 — 3번째 반복,
  이번에도 disclosure 됨
  - 위치: `codebase/backend/src/modules/external-interaction/idempotency.interceptor.spec.ts`
    (신규 테스트 `409 도 캐시되지 않는다 — R8 위반 상태를 고정하는 캐너리`,
    `손상된 캐시 JSON → 무시하고 신규 처리 + 정상 적재`) — `git show b0b57366f` 로 직접 대조
  - 상세: 이 브랜치 원 커밋(`17221ecb9`)의 선언 범위는 "lint `no-unsafe-*` 경고를 타입
    주석으로 처분, 런타임 미접촉"이었다. 그런데 4개 커밋(`e95201932`, `7c7aee1c4`,
    `b0b57366f`)에 걸쳐 누적 7건의 신규 테스트(`migrate-node-output-refs.spec.ts` Pass 2
    1건 + `idempotency.interceptor.spec.ts` 6건)가 추가됐다 — 순수 타입 주석이 아니라
    행위 검증 코드다. 다만 매 라운드 은폐 없이 커밋 메시지·`RESOLUTION.md`·plan 문서에
    근거(뮤테이션 판별력 실측 포함)를 남기며 자기-발견했고, 전부 이 PR 이 직접 건드린
    자리(방금 타입을 붙인 콜백, 방금 도입한 `HttpResponseLike` 방어)의 커버리지를 메우는
    것이지 무관한 기능을 확장하는 것이 아니다. 세 번째 반복이라는 점은 패턴으로 주목할
    만하지만, 매번 실측·문서화가 동반돼 CRITICAL/WARNING 사유는 아니다.
  - 제안: 조치 불요. 다음에 유사 케이스가 또 나오면 원 커밋 메시지의 "타입 전용" 선언
    자체를 "타입 전용 + 그 타입이 지탱하는 방어의 회귀 테스트"로 처음부터 넓혀 적는 편이
    반복 disclosure 보다 정직하다.

- **[INFO]** 리뷰 산출물 누적 비율이 라운드마다 계속 커진다 (4x → 4.9x → 이번 **6.15x**)
  - 위치: `review/code/2026/08/12/{11_06_12,12_05_39,12_24_14}/*` (35개 파일, 3043줄
    추가) vs `codebase/`+`plan/` (15개 파일, 466 삽입/29 삭제 = 495줄) — `git diff --stat
    origin/main...HEAD -- codebase/ plan/` 및 `-- review/` 로 각각 직접 재계산
  - 상세: 실질 코드+plan 변경은 495줄인데 review 산출물은 3043줄로 약 **6.15배**다.
    이전 라운드들이 각각 이 패턴을 INFO 로 지적·조치불요 처리했고(`11_06_12/scope.md`:
    "10파일", `12_05_39/scope.md`: "4배 이상", `12_24_14/scope.md`: "4.9배"), CLAUDE.md 가
    "구현 완료 후 자동 review/fix 는 상시 승인된 강제 의무"로 산출물 커밋을 표준 워크플로로
    명시하므로 규약 위반은 아니다. 다만 라운드가 거듭될수록 **이전 라운드 자신의 산출물이
    다음 라운드 diff 에 재노출**되는 구조라 비율이 산술적으로 계속 커진다 — 실제로 이번
    프롬프트 자체가 97,592 토큰으로 단일 Read 호출 상한(25,000)을 넘어 여러 차례로 나눠
    읽어야 했다(관찰, 코드 결함 아님). 코드 스코프 자체의 문제는 아니지만, 병합 전에 이
    브랜치를 한 번 정리(스쿼시 또는 `review/**` diff base 갱신)하지 않으면 다음 라운드가
    또 이 위에 쌓여 비율이 더 커진다.
  - 제안: 코드 조치 불요. PR 설명에 "review/** 는 산출물, 기능 diff 아님"을 명시하는
    기존 권고를 유지하되, 이번엔 병합 전 정리(스쿼시 등)를 한 번 고려할 시점이라는 관찰을
    남긴다.

- **[INFO]** 마지막 커밋(`b0b57366f`)의 실제 diff가 자신의 커밋 메시지·`RESOLUTION.md`
  예고와 정확히 일치함을 직접 확인 — 새로운 스코프 이탈 없음
  - 위치: `git show b0b57366f` 전체 (`idempotency.interceptor.spec.ts` +64/-4,
    `plan/in-progress/backend-lint-gate-broken-on-main.md` 백로그 3항목 추가분)
  - 상세: `review/code/2026/08/12/12_24_14/RESOLUTION.md`(이번 diff 파일 39)가 예고한
    조치("테스트 이름 축소", "409 캐너리 신설", "동작 수정 안 함", "plan `§후속` 등재")와
    실제 diff hunk 가 한 글자 단위로 일치한다. 캐시 제외 조건(`statusCode >= 400`) 자체는
    이 커밋에서 전혀 수정되지 않았다 — 캐너리 테스트만 추가돼 기존 동작을 고정할 뿐이다.
  - 판정: 문제 없음(발견 아님, 확인 목적).

그 외 CRITICAL/WARNING 급 스코프 이탈은 **발견되지 않았다.**

## 점검 관점별 확인 내역

1. **의도 이상의 변경** — 코드 12파일(1~14 중 소스) 전부 "라이브러리 경계 `any` 에 타입
   주석/제네릭/단언 추가 + `--max-warnings 0` 게이트"라는 단일 목적에 부합. 유일한 예외는
   위 INFO 1(신규 테스트, 3번째 반복이지만 매번 disclosure).
2. **불필요한 리팩토링** — 없음. 이전 3라운드의 side_effect 리뷰가 emit md5 비교로 로직
   무변경을 실증했고, 이번 라운드에 새로 추가된 hunk(캐너리 테스트 2건)도 기존 코드를
   전혀 건드리지 않는다.
3. **기능 확장** — 없음. 신규 테스트는 이미 존재하는 인터셉터 동작을 검증할 뿐 프로덕션
   코드에 새 분기·기능을 추가하지 않는다.
4. **무관한 수정** — 없음. `git diff --stat origin/main...HEAD` 결과 50개 파일 전부
   backend lint 처분 + 그 처분에 대한 리뷰 대응 + 리뷰 산출물이며, 다른 모듈·다른 기능
   영역은 등장하지 않는다.
5. **포맷팅 변경** — 이번 라운드 신규분(캐너리 테스트 2건, docstring 1문단, plan 백로그
   3항목)에 순수 포맷팅 변경은 없다.
6. **주석 변경** — 신규 주석(캐너리 테스트 앞 6줄, spec 파일 상단 docstring 확장)은 전부
   그 자리의 새 테스트가 왜 필요한지·무엇을 고정하는지를 설명하며 drive-by 가 아니다.
7. **임포트 변경** — 이번 라운드 신규분에 import 변경 없음.
8. **설정 변경** — 이번 라운드 신규분에 설정 파일 변경 없음
   (`package.json` `--max-warnings 0` 은 2라운드 전(`ee8e44e8f`)부터 이미 존재하던 변경이
   누적 diff 에 재노출된 것).

## 요약

이번 세션(`12_40_58`)이 처음 마주하는 실질 신규 변경은 `b0b57366f` 커밋 하나(캐너리
테스트 2건 + docstring 1문단 + plan 백로그 3항목 추가)뿐이며, 직접 `git show` 로 대조한
결과 그 커밋 메시지와 `RESOLUTION.md`(파일 39)가 예고한 내용과 diff 가 정확히 일치한다 —
캐시 제외 조건 등 실제 동작은 전혀 바뀌지 않았고 새로운 스코프 이탈도 없다. 나머지 코드
12파일과 plan 문서, 그리고 3개 라운드의 리뷰 산출물(35파일)은 이전 세 차례 scope
라운드가 이미 라인 단위로 검증한 동일 hunk 다. 두 가지 관찰이 반복·누적되고 있다: (1)
"타입 주석만"이라는 최초 선언 범위를 테스트 보강이 이번까지 3번째로 넘어섰지만 매번 실측
근거와 함께 정직하게 disclosure 됐고, 넘어선 범위 자체도 이 PR 이 직접 만든 방어/콜백의
커버리지를 메우는 것이라 무관한 확장이 아니다. (2) 리뷰 산출물 대 실질 코드 변경 비율이
라운드를 거듭할수록 산술적으로 커지고 있다(4x → 4.9x → 6.15x) — CLAUDE.md 워크플로에
부합해 코드 결함은 아니지만, 병합 전 정리를 한 번 고려할 시점이라는 관찰을 남긴다. 두 관찰
모두 INFO 수준이며 CRITICAL/WARNING 대상은 없다.

## 위험도

LOW

STATUS: OK
