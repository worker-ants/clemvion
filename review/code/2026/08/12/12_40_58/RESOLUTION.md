# RESOLUTION — `12_40_58`

리뷰 결과: **CRITICAL 0 / WARNING 1 / RISK LOW**. reviewer 8명 실행, 강제 8명 전원 결과 확보
(`forced_missing: []`, `unfinished: []`).

---

## WARNING #1 (documentation/requirement) — 소스 주석이 §R8 을 반대로 말한다 → **수정**

**지적의 핵심은 정확하다.** 직전 라운드에 테스트 **이름**의 §R8 오귀속은 고쳤으면서 **소스
주석은 그대로 뒀다.** 그래서 같은 모듈의 spec 파일과 소스 파일이 서로 다른 이야기를 하는
상태가 됐다. 고친 자리 옆에 같은 오류가 남아 있으면 다음 사람은 **안 고쳐진 쪽을 믿는다.**

### 다만 지목한 3곳 중 하나는 무고했다 — 한 줄씩 판정했다

리뷰어는 `:42` · `:54-55` · `:118` 을 지목했다. 원문을 열어 각각 판정한 결과:

| 위치 | 원문 요지 | 판정 |
|---|---|---|
| `:118` `4xx 는 캐시 제외 (Spec EIA §R8)` | R8 을 근거로 4xx 전체 제외 주장 | **틀림 — 정정** |
| `:54-55` `400 VALIDATION_ERROR 응답은 캐시 제외` | 400 VALIDATION_ERROR 한정 | **무고 — R8 원문과 정합, 손대지 않음** |
| `:42` `캐시된 정상 응답 JSON 문자열 (2xx). 4xx 중 VALIDATION_ERROR 는 캐시 제외` | R8 과 반대는 아니나 `(2xx)` 가 409·410 캐시 대상을 배제하는 뉘앙스 | **보강** |
| `:145` `4xx/5xx 모두 캐시 제외` | **리뷰어가 안 짚은 자리.** 구현 사실로는 맞지만 R8 문맥에서 오해 소지 | **손질** |

즉 지적 3곳 중 **1곳은 무고**하고, 대신 **리뷰어가 안 짚은 `:145`** 가 손질 대상이었다.
"3곳" 을 그대로 받아 일괄 수정했으면 **맞는 주석을 틀리게 바꾸고** 진짜 자리는 놓쳤다.

### 조치

`:118` 에 선재 결함의 전모(R8 원문 · `>= 400` 이 409·410 을 함께 떨군다는 사실 ·
`EIA-RL-02` 영향 · 409 캐너리 · 백로그 경로)를 적고, `:42` 는 "R8 상 대상은 2xx·409·410 이나
현 구현은 2xx~3xx 만 적재" 로 보강, `:145` 는 R8 정합 부분과 선재 결함 부분을 갈라 적었다.

**emit 불변 실측**: 주석만 바뀌었으므로 `removeComments: true` 하에서 emit 이 같아야 한다 —
`ts.Program` md5 비교 결과 **`5a37c1258b12efb99321c4990ea42f6f` 로 HEAD 와 동일**. 덧붙여 이
해시는 **최초 origin/main 측정값과도 같다** — 이 파일은 브랜치 전체에 걸쳐 emit 이 바이트
동일하다. "타입 전용" 정체성이 이 자리에서 유지됨을 수치로 확인했다.

---

## INFO 처분 — 4건 수용, 4건 유예

| # | 항목 | 처분 |
|---|---|---|
| 6 | 테스트 제목의 마크다운 `**...**` 가 Jest 리포터에 별표로 출력 | **수정** — 별표 제거. 파일 내 유일 예외였다 |
| 7 | 손상 JSON 테스트가 호출 **횟수**만 보고 저장값은 미단언 | **수정** — 지적대로 `bodyHash` 가 빈 문자열이어도 그린이었다. `bodyHash`/`statusCode`/`responseJson` 단언 추가 |
| 5 | 생성자 인라인 호출 7곳 | **수정 — 직전 라운드의 내 기각 근거가 틀렸다** (아래) |
| 3 | "타입 전용" 범위를 테스트가 3번째 초과 | **수용(문구)** — 커밋 메시지를 처음부터 "타입 전용 + 그 타입이 지탱하는 회귀 테스트" 로 넓혀 적으라는 권고를 따른다 |
| 1 | 캐시 제외 범위 선재 결함 | **유예 유지** — plan 등재 + 409 캐너리 |
| 2 | admission SQL shape 미검증 | **유예 유지** — plan 등재(4라운드 연속 동일 판정) |
| 4 | 리뷰 산출물 비율 6.15x, 스쿼시 고려 | **사용자 결정 영역** — 병합 전략이라 여기서 정하지 않는다. 아래 보고에 올린다 |
| 8 | dispatcher/executions 커버리지 갭 | **유예 유지** — plan 등재 |

> **INFO 5 — 내 기각 근거가 틀렸다.** 직전 RESOLUTION 에 "각 테스트가 서로 다른 redis
> 스텁을 쥐어야 해서 헬퍼로 묶으면 인자만 늘어난다" 고 적고 기각했는데, 실제로 7곳의
> 생성자 호출은 **다른 인자가 `redis` 하나뿐**이다(`undefined, redis as never, undefined`).
> 열어 세어 보지 않고 "인자가 늘어난다" 고 단정한 것이다. `makeInterceptor(redis)` 로
> 추출해 7곳 → 1곳. 위 W-4 블록(4건)은 `redisConn` **주입 우선순위 자체가 검증 대상**이라
> 생성자를 그대로 노출해 뒀고, 그 이유를 헬퍼 docstring 에 적었다.

## 검증 (조치 후)

- eslint **0 errors / 0 warnings**
- ratchet **199건 / 38파일 baseline 일치**
- backend unit **418 suites / 8519 passed / 1 skipped** (테스트 수 불변 — 이번 라운드는
  주석·단언 보강·헬퍼 추출이라 신규 케이스가 없다)
- `idempotency.interceptor.ts` emit md5 **HEAD 및 origin/main 과 동일**

## 수렴 판단

4라운드 발견 성격: **동작·타입 → 문서·커버리지 → 내 서술 정확도 → 주석 문구·테스트 스타일.**
이번 유일 WARNING 은 런타임 무영향 주석이었고, 코드 동작 변경은 3라운드 연속 **0** 이다.
남은 INFO 는 전부 선재 항목(plan 등재 + 캐너리 고정)이거나 사용자 결정 영역(병합 전략)이다.
이 축에서 더 나올 것은 없다고 본다 — 다음 라운드는 이 커밋을 덮는 확인용이다.
