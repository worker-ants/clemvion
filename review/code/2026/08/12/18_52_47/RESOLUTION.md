# RESOLUTION — `18_52_47`

리뷰 결과: **CRITICAL 0 / WARNING 1 / RISK LOW**. reviewer 9명 실행, 강제 7명 전원 결과 확보
(`forced_missing: []`, `unfinished: []`). **WARNING 1건 조치.**

---

## WARNING #1 (documentation) — **내가 직전 커밋에 쓴 문장이 과장이었다** → 조치

직전 라운드에서 모듈 docstring 에 "이 블록의 테스트는 **전부** `Logger.prototype.warn` 을
함께 단언한다" 고 적었는데, 실제로는 **7건 중 4건**만 그렇다. 리뷰어가 각 `it` 라인을 세어
지적했다.

**이것이 이 저장소가 반복 경계하는 "문서한 보장이 구현보다 넓다" 의 재발**이고, 하필
그 결함 클래스를 막으려고 쓴 문장에서 났다.

**조치**: 정확한 부분집합으로 좁혔다 — "적재·직렬화가 조용히 실패할 수 있는 **4건**
(GET 실패 · SET 실패 · 직렬화 실패 양 채널)" 이 warn 을 단언하고, **나머지 3건은 각각 다른
것을 보므로**(캐시 미스 강등 후 재적재 · `catchError` 위치 캐너리 · 비-`Error` reject 에서
로그 조립 생존) 붙이지 않았다는 사실까지 적었다.

> **3건에 warn 을 억지로 붙이지 않은 이유**: 그 테스트들은 warn 이 목적이 아니다. 붙이면
> 각 테스트가 무엇을 지키는지가 흐려지고, "모든 테스트에 warn 단언" 이라는 형태만 맞추는
> 껍데기가 된다. 문장을 사실에 맞추는 쪽이 옳다.

## INFO 처분 — 전부 유예 또는 확인용

| # | 항목 | 처분 |
|---|---|---|
| 1 | 캐시 키 미스코프 | **유예** — 선재, plan 등재, 우선순위 상향 근거 기록됨 |
| 2 | 캐시 엔트리 내부 `responseJson` 손상 | **기록 완료 확인** — 직전 라운드의 불이행을 이번에 실제로 적었고 리뷰어가 확인했다 |
| 7 | `data-flow/15` mermaid 의 적재 주체 misattribution | **유예** — 선재 서술, planner 항목 |
| 11·12·13 | `JSON.parse` 중복 · 판정 팩터링 비대칭 · `intercept()` 길이 | **유예 유지** (6라운드 연속) |
| 14 | e2e 가 `REDIS_KEY_PREFIX` 를 리터럴로 3곳 하드코딩 | **유예** — 타당한 지적이나 상수 export 는 프로덕션 표면을 테스트 편의로 넓히는 변경이라 별도 판단이 필요하다. plan 후속 항목 성격 |
| 3~6·8~10·15~18 | 확인·정합성 기록 | 조치 불요 |

## 검증

- eslint **0 errors / 0 warnings**
- backend unit — idempotency spec **25/25** (문장 1개 수정이라 테스트 불변)

## 수렴 판단

| 라운드 | CRITICAL | WARNING | 성격 |
|---|---|---|---|
| `16_29_45` | **1** | 5 | dead code · vacuous test |
| `16_53_26` | 0 | 1 | 자매 자리(400) |
| `17_07_45` | 0 | 4 | 불변식 방어 · 가드 우회 · replay |
| `18_07_36` | 0 | 4 | 방어 미검증 · e2e 자매 · ID 충돌 |
| `18_37_45` | 0 | 4 | warn 패턴 · **기록 불이행** |
| `18_52_47` | 0 | **1** | **문장 과장 1건** |

CRITICAL 은 1라운드 이후 0 이고 WARNING 이 5→1→4→4→4→1 로 줄었다. 마지막 라운드의 유일
항목은 **코드가 아니라 주석 한 문장**이며, 남은 INFO 는 전부 선재이거나 6라운드 연속 유예된
선택 사항이다. 이 축에서 더 나올 것은 없다고 본다.
