# 아키텍처(Architecture) 리뷰 — masked-marker-contract-7d2e14 (누적 diff, 12_50_37)

## 발견사항

- **[WARNING]** "탐지 로직 중복은 안전하다"는 이 PR의 핵심 전제가 이미 깨졌다 — backend/frontend 두 미러 가드 사본이 `SOT_DIR` 경계 판정에서 **서로 다른 로직**을 쓰고 있고, 커밋 메시지는 양쪽 다 고쳤다고 주장하지만 실제로는 한쪽만 고쳐졌다
  - 위치: `codebase/frontend/src/lib/repo-guards/__tests__/masked-marker-mirror-guard.ts:143` (`findMirrorRedeclarations` 내부, `if (relPath.startsWith(SOT_DIR.split(path.sep).join("/"))) continue;`) vs `codebase/backend/src/repo-guards/__tests__/masked-marker-mirror-guard.ts:141` (동일 위치, `if (relPath === SOT_DIR || relPath.startsWith(\`${SOT_DIR}/\`)) continue;`). 둘 다 diff 안의 신규 파일 전체(310줄 순수 추가)라 게이트 숫자가 곧 실제 소스 줄 번호다.
  - 상세: 커밋 `811a40f48`("파생이 '전수처럼 보이지만 아닌' 목록을 만들었다 — 라운드3 처분")의 메시지는 "`startsWith(SOT_DIR)` 만 쓰면 `masked-markers-extra` 같은 형제를 오배제하므로 **경계를 명시했다**. 이 시리즈가 접두 겹침으로 반복해 당한 자리다"라고 적는다. 그런데 `git show 811a40f48 -- .../frontend/.../masked-marker-mirror-guard.ts` 로 그 커밋의 실제 diff 를 직접 열어 확인한 결과, frontend 파일에서 바뀐 것은 `resolveScanDirs` 의 2단계 확장뿐이고 `SOT_DIR` 제외 판정 줄(`relPath.startsWith(SOT_DIR...)`)은 **손대지 않은 채 그대로**다. 경계 명시(`=== SOT_DIR || startsWith(SOT_DIR + '/')`) 는 backend 사본에만 적용됐다(같은 `git show` 로 backend diff 에서 `-      if (relPath.startsWith(SOT_DIR)) continue;` / `+      if (relPath === SOT_DIR || relPath.startsWith(\`${SOT_DIR}/\`)) continue;` 를 직접 확인). 즉 이 PR 자신이 "값의 미러는 없앴지만 **탐지 로직의 중복은 구멍을 만들지 않는다** — 한 사본이 낡아도 다른 사본이 같은 불변식을 지킨다"(두 파일 헤더 주석, 동일 문구)고 세 라운드에 걸쳐 정당화해 온 바로 그 전제가, 이번엔 한쪽만 고쳐지는 실제 드리프트로 반증됐다. `12_25_15` architecture 리뷰가 이미 "탐지 로직 자체의 결함은 두 사본에 동시에 존재하므로 중복이 결함 전파의 방어가 되지 못한다"고 경고했는데, 여기서는 그보다 한 단계 더 나쁜 형태(수정 자체가 비대칭적으로 적용)로 발현했다.
    실질 위험은 낮다 — 현재 `codebase/packages/` 아래 `masked-markers` 와 접두가 겹치는 형제 패키지(예: `masked-markers-extra`, `masked-markers-legacy`)가 없어(직접 확인) frontend 쪽의 미수정 prefix 비교가 지금 당장 파일을 잘못 배제하지는 않는다. 그러나 (1) 두 스택 어디에도 이 정확한 시나리오("SoT 접두가 겹치는 형제 디렉터리가 스캔에서 잘못 제외되는가")를 직접 단언하는 캐너리가 없다 — `masked-marker-mirror.test.ts`/`.spec.ts` 의 "접두가 겹치는 다른 식별자" 캐너리는 **식별자** 접두 겹침(`MAX_MASK_DEPTH_OLD`)만 다루고 **경로** 접두 겹침은 다루지 않는다. (2) RESOLUTION.md 류 산출물이 "두 사본 다 고쳤다"는 근거 없는 확신을 다시 한 번 코드에 남긴 것이라, 다음 세션이 이 문구를 신뢰하고 형제 패키지를 추가하는 순간 frontend 사본만 조용히 그 형제를 스캔에서 빼먹는다(fail-open) — 이 시리즈가 이미 두 번(`11_27_29`, `11_53_49`) 겪은 "자매를 전수로 못 세는" 실패 클래스의 세 번째 변주다.
  - 제안: frontend 파일의 143행을 backend 141행과 동일하게 `relPath === SOT_DIR || relPath.startsWith(\`${SOT_DIR}/\`)` (또는 `path.sep` 정규화 유지한 동등 형태)로 맞춘다. 동시에 두 테스트 파일에 `codebase/packages/masked-markers-extra/src/foo.ts` 형태의 합성 fixture로 "SoT 와 접두가 겹치는 형제 디렉터리도 스캔 대상에 포함된다"를 직접 단언하는 캐너리를 추가해, 이 경계가 다음에도 조용히 재발하지 않도록 한다(뮤테이션 실증: 현재 frontend 로직을 backend 로직으로 바꿔도 기존 스위트가 전부 GREEN 으로 유지된다는 사실 자체가 이 경계를 못 잡는 캐너리 공백의 증거다).

- **[INFO]** 탐지 로직(~150줄, `resolveScanDirs`/`listSourceFiles`/`findRedeclaredSymbols`/`findMirrorRedeclarations`)이 backend/frontend 두 파일에 문자 그대로 복제돼 있고, 이번 WARNING 이 그 트레이드오프의 실제 비용(두 사본이 안 맞을 수 있다)을 처음으로 실증했다
  - 위치: `codebase/backend/src/repo-guards/__tests__/masked-marker-mirror-guard.ts` 전체 vs `codebase/frontend/src/lib/repo-guards/__tests__/masked-marker-mirror-guard.ts` 전체.
  - 상세: `11_53_49`/`12_25_15` 두 라운드가 이미 이 중복을 "CI 트리거 대칭을 위해 의도적으로 선택된 것"이라 인정했고, 그 판단 자체는 여전히 타당하다(서로 다른 런타임·workspace 경계라 파일을 직접 공유할 수 없음). 다만 이번 WARNING 은 "탐지 로직이 서로 달라지면 두 사본이 같은 불변식을 지킨다는 전제가 깨진다"는 그때의 경고가 추상적 우려가 아니라 실제로 일어난 사건임을 보여준다. `@workflow/masked-markers`(또는 신설 `@workflow/repo-guards`)의 devDependency-only export 로 탐지 로직 자체를 재추출하면 "각 워크플로에서 독립 실행된다"는 속성은 유지하면서 이런 종류의 사본 간 드리프트를 원천 차단할 수 있다는 제안이 이미 두 라운드 전에 나와 있었다.
  - 제안: 즉시 조치는 불필요(위 WARNING 을 두 사본 동기화로 해소하면 당장의 불일치는 사라짐). 다만 이번 사건 자체가 "다음에 이 로직을 또 고칠 일이 생기면 그것이 재추출 신호"라는 `12_25_15` 리뷰의 조건을 이미 충족했다고 볼 수 있다 — 재추출을 진지하게 검토할 시점이다.

## 요약

이 PR 의 핵심 설계(backend/frontend 에 손으로 복제되던 마스킹 마커 상수·판정 로직·깊이 상한을 `@workflow/masked-markers` 공유 패키지로 추출하고, 두 스택은 재export shim 으로 소비처 import 경로를 보존)는 SOLID·결합도·레이어 책임 어느 축에서도 견고하며, 순환 의존 없는 단방향 "공유 커널" 구조와 `@workflow/ai-end-reason` 선례를 정확히 따르는 등록 표면(8곳)은 세 차례의 이전 라운드(`11_27_29`, `11_53_49`, `12_25_15`)를 거치며 이미 충분히 검증됐다. 다만 이번 최종 누적 diff를 다시 실측한 결과, 이 PR이 반복해서 정당화해 온 "값의 미러는 위험하지만 탐지 로직의 중복은 각 사본이 독립적으로 같은 불변식을 지키므로 안전하다"는 전제 자체가, `SOT_DIR` 경계 판정 로직에서 이미 깨져 있다 — 최근 커밋(`811a40f48`)이 "양쪽 다 경계를 명시했다"고 주장하지만 실제로는 backend 사본만 고쳐지고 frontend 사본은 옛 무경계 prefix 비교 그대로 남아 있다. 현재는 이름이 겹치는 형제 패키지가 없어 라이브 영향은 없지만, 이는 이 시리즈가 세 라운드 연속 겪어 온 "자매를 전수로 못 세는" 실패 클래스가 이번엔 "스캔 대상"이 아니라 "수정 적용 범위" 자체에서 재발한 것이며, 이 경계를 직접 단언하는 캐너리도 양쪽 어디에도 없어 재발을 막을 안전망이 없다. 그 외 나머지 아키텍처 축(패키지 경계, 의존 방향, 추상화 수준, 확장성)은 이전 라운드들의 판정대로 견고하다.

## 위험도
MEDIUM
