# RESOLUTION — 05_08_35

대상 SUMMARY: `review/code/2026/08/21/05_08_35/SUMMARY.md` (위험도 **LOW**, Critical **0**, WARNING **2**, INFO 10)

**처분: W2 수정(breaking 명시), W1 무조치(리뷰어 판정 수용) + 이월 항목 트래커 등재.**
`codebase/` 변경 없음 — 문서(CHANGELOG)와 plan 만 바뀐다.

---

## WARNING 2 — breaking 을 서술만 하고 **라벨하지 않았다** (side_effect / api_contract) — **수정**

CHANGELOG 는 이미 *"직접 입력한 마커도 거부된다 — 마커 세 문자열은 Manual 파라미터의
예약어다"* 라고 **서술**하고 있었다. 그런데 두 라운드에 걸쳐 서로 다른 reviewer 가 같은
요청을 했다 — **breaking 으로 명시하라.**

서술과 라벨은 다르다. 릴리스 노트를 뽑는 사람은 저장소의 기존 관행대로
`**Behavior change (breaking): …**` 형태를 찾는다(다른 항목들이 그렇게 돼 있다). 그 형태가
없으면 이 변경은 릴리스 노트에서 조용히 빠진다.

기존 표기 관행을 실측해 맞춘 문단을 항목 맨 앞에 넣었다 — 영향 범위(UI 우회 직접 호출
클라이언트만), 정확 일치 조건(`a***b` 무영향), 외부 소비자 부재 확인 근거를 함께 적었다.

## WARNING 1 — 저장소 전역 가드 번들 (scope) — **무조치**

리뷰어 본문 그대로다: *"기능적으로 무해하고 CHANGELOG 에 '범위를 넘는다'고 투명히 자백돼
있어 **병합 차단 사유 아님**"* — 직전 라운드(`04_46_40` W1)에서 공시를 추가해 닫은 항목이고,
이번 지적은 **향후 관행 권고**다.

분리하지 않은 이유는 직전 RESOLUTION 과 동일하다 — 두 가드는 이 PR 의 wrapper 와
`typescript` import 를 각각 전제하므로, 분리하면 그 사이 커밋 구간에 **가드 없이 코드만 있는
상태**가 생긴다. 권고 자체는 트래커에 인용해 남겼다.

## 이월 항목을 트래커에 등재했다 — **"이미 기록됨" 이 거짓이었다**

여러 라운드의 RESOLUTION 에 "INFO 는 트래커로 넘긴다" 고 적어 왔다. push 직전에 실측했더니
`plan/in-progress/` 어디에도 **한 건도 없었다.**

> `review/**` 는 SoT 가 아니다. 리뷰 산출물에만 적힌 항목은 PR 이 닫히는 순간 사실상
> 사라진다 — 이 저장소에서 같은 방식으로 항목을 잃을 뻔한 전례가 있다.

`plan/in-progress/spec-sync-external-interaction-api-gaps.md` 에 9건 + 관행 권고 1건을
기존 등재 형식으로 적었다 — `error.code` drift · Swagger 예약어 · **cross-stack 마커 계약
테스트** · base JSDoc 역참조 · `REASON_TO_DETAIL` 밀도 · 주석 언어 혼재 · `reRun` 분해 ·
`findMaskedResubmissions` 직접 테스트 · `throwIfAny` phase 경계 미검증.

## 미조치 INFO (10건)

전부 이월 확인 또는 리뷰어 스스로 "조치 불요·강제 아님" 판정. 위 트래커 등재로 SoT 가
`review/**` 밖으로 옮겨졌다.

## 수렴 판정

| 라운드 | Critical | Warning | 발견의 성격 |
|---|---|---|---|
| `00_03_57` | **1** | 9 | **동작** — boolean 완전 우회 |
| `00_39_27` | 0 | 5 | 절차 · 폐기된 설계를 지시하던 spec |
| `01_15_47` | 0 | 0 | — |
| `01_38_26` | 0 | 3 | **구조** — 가드 부재 |
| `02_04_38` | 0 | 3 | 가드 자신의 결함 |
| `02_29_01` | 0 | 0 | — |
| `02_49_22` | 0 | 1 | 가드 우회 형태 |
| `03_14_16` | 0 | 3 | 가드 우회 형태(4번째) |
| `04_20_10` | 0 | 1 | 가드의 **검증** 부재 |
| `04_46_40` | 0 | 1 | **공시** |
| `05_08_35` | 0 | 2 | **이미 처분된 트레이드오프의 재진술** (둘 다 리뷰어가 비차단 명시) |

수렴은 "발견 0" 이 아니라 **발견의 성격**으로 판단한다. 이번 라운드의 WARNING 둘은 새 결함이
아니라 **이전 라운드에서 근거와 함께 수용된 트레이드오프를 다시 진술**한 것이고, 두 reviewer
모두 본문에 "병합 차단 사유 아님" / "되돌릴 필요 없음" 을 적었다. 그중 실행 가능한 요구
하나(breaking 라벨)를 집행했다.

**런타임 방어**(거부 로직·두 호출부·에러 봉투)는 `01_15_47` 이후 **열 라운드 연속**
CRITICAL 0 / WARNING 0 이다.

## 검증

이번 변경은 `CHANGELOG.md` 와 `plan/` 뿐 — **`codebase/` 무변경**이라 직전 라운드의 4단계
결과(`04_46_40` RESOLUTION)가 그대로 유효하다.

| 단계 | 결과 (직전 실행, 코드 동일) |
| --- | --- |
| lint | PASS (49s) |
| unit | PASS — backend jest **430 suites / 8,896 passed** (1 skipped, 8,897 total) |
| build | PASS (138s) |
| 타입체크 ratchet | **199건 / 38파일 baseline 일치** |
| e2e | PASS (207s) — backend supertest **276** · playwright **51 passed (55.8s)** |
