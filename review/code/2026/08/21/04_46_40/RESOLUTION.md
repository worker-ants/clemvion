# RESOLUTION — 04_46_40

대상 SUMMARY: `review/code/2026/08/21/04_46_40/SUMMARY.md` (위험도 **LOW**, Critical **0**, WARNING **1**, INFO 19)

**처분: WARNING 1건 수정(공시), INFO 1건 동반 조치.** 코드 동작 변경 없음.

---

## WARNING 1 — 저장소 전역 가드를 **공시 없이** 끼워 넣었다 (scope) — **수정**

리뷰어 지적: `production-build-devdep-guard` 는 "마스킹 재제출 거부" 라는 선언 범위를 넘어
**저장소 전역 CI 불변식**을 신설하는데 `CHANGELOG.md` 에 한 줄도 없다.

맞는 지적이다. 두 가드가 들어온 경위는 각 단계가 리뷰 WARNING 에 떠밀린 연쇄였지만 —

> 가드 필요(`01_38_26`) → 정규식 가드 → 우회 4회(`~03_14_16`) → **AST 전환** → `src/` 에서
> devDependency `typescript` import → dist 누출 위험 → 빌드 제외 → 그 보장의 검증
> 부재(`04_20_10`) → **devdep 가드**

— 경위가 정당하다는 것과 **읽는 사람이 알 수 있다는 것은 다른 문제**다. CHANGELOG 만 보는
사람은 저장소 전역 규칙이 둘 생긴 걸 모른다.

`CHANGELOG.md` 의 해당 항목에 "부산물로 저장소 전역 가드 두 개가 생겼다 — 이 기능의 범위를
넘으므로 따로 적어 둔다" 문단을 추가했다. 각 가드가 무엇을 막는지, 왜 정규식에서 AST 로
갔는지, `dist` 누출이 **선존**이었다는 사실까지 적었다.

**별도 PR 로 분리하지 않은 이유**: 두 가드는 이 PR 의 코드가 없으면 지킬 대상이 없다
(`masked-reject-callers` 는 이 PR 이 만든 wrapper 를, `production-build-devdep` 은 이 PR 이
만든 `typescript` import 를 전제한다). 분리하면 그 사이 커밋 구간에서 가드 없이 코드만 있는
상태가 생긴다 — 리뷰어도 "즉시 조치 불요, 병합 차단 사유 아님" 으로 판정했다.

## INFO 11 — 매직넘버 `500` 의 근거 미기록 (maintainability) — **동반 조치**

vacuous 방지 캐너리의 하한이 근거 없는 숫자로 남아 있었다. 그 캐너리는 **다른 두 단언이
조용히 통과하는 것을 막는 유일한 장치**라(뮤테이션으로 실증됨), 다음 사람이 근거 없이
낮추면 보호가 사라진다. 도입 시점 실측 **805 파일**과 "이 아래로 떨어지면 파일이 준 게
아니라 설정 해석이 고장난 것을 의심하라" 는 판단 기준을 주석에 남겼다.

## 미조치 INFO (18건)

전부 이월 확인 또는 리뷰어 스스로 "조치 불요·강제 아님·스코프 밖" 판정. 대표 —
controller/service 두 레이어 배선(선존 관행) · `reRun` 길이 · `error.code` 선존 drift ·
Swagger description(5라운드째 이월) · `reRun`/`toTriggerParameterErrorDetails` JSDoc 요약 ·
크로스런타임 마커 미러 테스트(별도 트래커) · `executeNode` 스코프 밖.

## 수렴 판정

| 라운드 | Critical | Warning | 발견의 성격 |
|---|---|---|---|
| `00_03_57` | **1** | 9 | **동작** — boolean 완전 우회 |
| `00_39_27` | 0 | 5 | 절차 · 폐기된 설계를 지시하던 spec |
| `01_15_47` | 0 | 0 | — |
| `01_38_26` | 0 | 3 | **구조** — 가드 부재 |
| `02_04_38` | 0 | 3 | 가드 자신의 결함 |
| `02_29_01` | 0 | 0 | — |
| `02_49_22` | 0 | 1 | 가드 우회 형태 |
| `03_14_16` | 0 | 3 | 가드 우회 형태(4번째) |
| `04_20_10` | 0 | 1 | **가드의 검증** 부재 |
| `04_46_40` | 0 | 1 | **공시** — 코드 아님 |

수렴은 "발견 0" 이 아니라 **발견의 성격**으로 판단한다. 동작 → 구조 → 가드 → 가드의 검증 →
공시로 한 칸씩 멀어졌고, 이번 라운드의 유일한 WARNING 은 **코드가 아니라 문서**이며 리뷰어
스스로 병합 차단 사유가 아니라고 적었다.

**런타임 방어**(거부 로직·두 호출부·에러 봉투)는 `01_15_47` 이후 **아홉 라운드 연속**
CRITICAL 0 / WARNING 0 이다.

## 검증

TEST WORKFLOW 4단계 PASS + ratchet — 이번 변경은 주석·마크다운뿐이라 동작에 영향이 없지만,
이 저장소는 **영향 추정으로 e2e 를 면제하지 않으므로** 전 단계를 다시 돌렸다.

| 단계 | 결과 |
| --- | --- |
| lint | PASS (49s) |
| unit | PASS — backend jest **430 suites / 8,896 passed** (1 skipped, 8,897 total) |
| build | PASS (138s) |
| 타입체크 ratchet | **199건 / 38파일 baseline 일치** |
| e2e | PASS (207s) — backend supertest **276** · playwright **51 passed (55.8s)** 실측 |
