# RESOLUTION — `10_41_55` (branch `claude/eia-terminal-error-sanitize-audit`)

Critical **0** · Warning **2** · SPEC-DRIFT 1 · INFO 14. **수렴.**

3라운드 만에 발견의 성격이 **실제 결함 → 주석/문서 미세 비대칭 + spec 미러(planner 영역)** 로
내려왔다. 여기서 `codebase/**` 편집을 멈춘다.

---

## W2 (maintainability) — 주석을 한쪽에만 달았다 · **반영**

전 라운드가 "완전 동일 중복 단언 2건" 을 지적했는데 **내가 한쪽에만 의도 주석을 달았다.**
남은 `details` 키-생략 테스트에도 같은 취지 주석을 붙였다. 지적이 정확하다 — 의도가 코드에
없으면 다음 사람에겐 그냥 중복이고, 절반만 설명하면 오히려 "이쪽은 진짜 중복" 으로 읽힌다.

## W1 / SPEC-DRIFT — **planner 영역, 이미 등재**

§6.4 페이로드 절과 R17 마스킹 카탈로그가 이번 egress 마스킹을 아직 열거하지 않는다. 리뷰도
*"코드 변경 불요, plan 후속을 그대로 집행하면 됨"* 으로 결론냈다. `spec/` 은 developer 권한
밖이고 `plan/in-progress/eia-terminal-error-sanitize.md` §후속에 **R17 5번째 항목 + §6.4
캐비엇**으로 구체 등재돼 있다.

## INFO 반영

| # | 조치 |
|---|---|
| 13 트래커 문장이 술어 없이 끊김 | **반영** — 완결 + 승격 시 회귀 테스트 선행 조건 명시 |
| 6 fanout 이 재조회 없이 payload 미러링 | **긍정 확인으로 수용** — 리뷰어가 `notification-fanout:132` · `sse-adapter:162` 를 추적해 **마스킹 우회 경로가 없음**을 독립 검증했다. 내 초크포인트 논거를 밖에서 확인해 준 것이라 조치 없음 |

나머지 INFO(연결 문자열 잔여 갭 · 길이 상한 · 취소 경로 비대칭 · 이중 마스킹 idempotent ·
타입 단언 · 관용구 혼재 · 스칼라 분기 판별 불가)는 전부 **기결정 재확인**이거나 이미 등재된
후속이다.

---

## 3라운드 수렴 판정

| 라운드 | 발견 | 성격 |
|---|---|---|
| `09_51_00` | 마스킹 범위 주장 과장 · 공허한 테스트 | **실제 결함** |
| `10_19_30` | 검증 범위 주장 과장 · 오표기 전파 · "5곳" 중의성 | 문서/검증 주장 |
| `10_41_55` | 주석 비대칭 · spec 미러(planner) | **주석·미러** |

세 라운드 모두 **내 주장이 실제보다 넓다**는 같은 뿌리였다 — 마스킹 범위 → 검증 범위 → 주석
범위 순으로 좁아졌다. 코드 결함은 1라운드가 마지막이다.

---

## 검증

| 스테이지 | 결과 |
|---|---|
| lint | PASS (`--max-warnings 0`) |
| unit | PASS — backend **426 suites / 8754 tests** |
| build | PASS |
| e2e | PASS — 276 |

`toTerminalErrorPayload` 스위트 **26/26**. 뮤테이션 누계: 마스킹 제거 5/5 RED ·
`code`/`nodeId` 마스킹 2/2 RED · 무효 뮤턴트 2건은 재작성 후 재판정, 생존 0.
