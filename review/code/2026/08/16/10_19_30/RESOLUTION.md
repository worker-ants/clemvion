# RESOLUTION — `10_19_30` (branch `claude/eia-terminal-error-sanitize-audit`)

Critical **0** · Warning **6** · INFO 14. Warning 4건 반영, 2건은 planner 후속(권한 밖).

---

## W3 (testing) — **또 같은 형태였다.** 검증 범위 주장이 실제보다 넓었다 · **반영**

지적이 정확하다. JSDoc 이 *"호출부 5곳 전부 구조적으로 마스킹된다"* 고 쓰는데, 내 뮤테이션이
실제로 판별한 것은 **문자열·객체 두 분기**뿐이다.

그리고 나머지 둘은 **테스트로 판별할 수 없다** — 스칼라(`number`/`boolean`/`bigint`)와
non-object 분기는 값 공간에 secret 이 존재할 수 없어 마스킹이 **정의상 no-op** 이다. 그 둘을
래핑하는 이유는 보안이 아니라 **균일성**(새 분기가 생겼을 때 한 갈래만 빠지는 걸 막는다)이다.

→ 테스트를 억지로 만들지 않고 **JSDoc 에 검증 범위를 정확히 적었다**. "전부 검증됐다" 로
읽히지 않게 하는 것이 조치다.

## W5 (문서) — 내가 오표기를 **전파**했다 · **반영 + 출처까지 정정**

`§3.3` 은 실제로 "인증" 이고 outbound webhook 은 `§3.1`(EIA-NX-02)이다. `--impl-done`
(`10_19_31`)에서 내 신규 2곳은 이미 고쳤는데, 리뷰가 **내가 복사해 온 출처**(`CHANGELOG.md:45`,
#1174 항목)를 짚었다. 거기서 검증 없이 옮겨 적은 것이라 **출처도 함께 정정**했다 — 안 고치면
다음 사람이 또 복사한다.

## W4 (문서) — "5곳" 두 개가 다른 집합 · **반영**

같은 파일 인접 JSDoc 에서 "5곳" 이 각각 **취소 이벤트 호출부**와 **`toTerminalErrorPayload`
호출부**를 가리켜 오독 위험이 있었다. 핵심 안전성 논거와 직결되는 대목이라 분해해 적었다 —
`EXECUTION_FAILED` 4 + `chat-channel.dispatcher` 1, 그리고 "취소 5곳과는 다른 집합" 명시.

## W6 (유지보수성) — 중복 단언 · **주석으로 의도 명시**

전 라운드에 "의도가 다르다" 고만 답했는데, 그 의도가 코드에 없으면 다음 사람에겐 그냥 중복이다.
주석으로 적었다.

## W1·W2 (SPEC-DRIFT) — **planner 후속** (권한 밖)

§6.4 가 이번 wire 값 변화를 문서화하지 않는다. 지적이 맞고, **외부 통합사가 보는 정본은
CHANGELOG 가 아니라 §6.4** 라는 W2 의 논지가 특히 옳다. `spec/` 은 developer 권한 밖이라
plan 후속에 등재했다 — `10_19_31` 이 요구한 R17 카탈로그 5번째 항목과 **같은 planner 턴**에서
§6.4 캐비엇까지 함께 처리한다.

## INFO 반영 2건

| # | 조치 |
|---|---|
| 6 `details: null` 미테스트 | **반영** — `undefined` 와 다르게 **키를 보존**함을 고정 |
| 7 잔여 갭 캐너리 없음 | **반영** — JSDoc 표가 "못 잡는다" 고 선언한 것을 테스트로 고정. `SECRET_LEAK_PATTERNS` 가 넓어지면 이 테스트가 깨져 **표·CHANGELOG 도 같이 고치라는 신호**가 된다 |

나머지 INFO(길이 상한 · 이중 마스킹 idempotent · 취소 경로 비대칭 · `details` 키-이름 마스킹
표면 · 캐스트 주석 · basename 중복)는 무조치 또는 기존 등재. INFO4 는 리뷰어가 프런트 소스까지
추적해 내 W3 답변을 **독립 확인**해 준 것이라 조치 없음.

---

## 검증

`toTerminalErrorPayload` 스위트 **26/26**. TEST WORKFLOW 는 아래 재수행 결과 기재.
