# RESOLUTION — `18_16_05` (2라운드, forced 7/7) — **수렴**

RISK=LOW · **CRITICAL 0 · WARNING 0**. 7명 전원이 직전 라운드 WARNING 3건의 해소를
독립 검증했다(소스 대조 + vitest 19/19 + unittest 63/63 + **뮤테이션 재현**).

## 프로세스 경고 — 즉시 확인했다. **유실 없음**

리뷰어가 스스로 보고했다:

> *"testing reviewer 가 뮤테이션 검증 과정에서 `cp` 백업 목적지 경로를 실수로 worktree
> 루트의 untracked 파일 `scratchpad_idem_backup.ts` 로 지정해 내용을 덮어썼다가 삭제로
> 정리했다 … git 비추적 파일이라 커밋 이력으로 복구 불가능"*

**세션 시작 시 main checkout 에 그 이름의 파일이 있었으므로** 바로 쟀다:

| 위치 | 상태 |
| --- | --- |
| main checkout | **무손상** — 23,798 bytes, mtime **8/13**(오늘 건드려진 흔적 없음) |
| 이 worktree | 없음 |
| 다른 worktree | 없음 |

이 worktree 는 `origin/main` 에서 새로 만들었고 그 파일은 untracked 라 체크아웃되지
않는다 — 즉 **리뷰어가 만든 파일을 리뷰어가 지운 것**이고 잃은 것이 없다.

**그래도 트래커 등급을 올렸다.** 기존 항목(`14_10_42` INFO 9)은 *"관측이 오염될 수 있다"*
였는데 이건 *"복구 불가능한 파일을 지울 수 있다"* 이고, 이번에 안 터진 이유는 **설계가
아니라 우연**(그 경로 이름이 이 worktree 에 없었다)이다. 같은 실수가 main checkout 을
향했으면 8/13 부터 있던 남의 파일이 사라졌다.

처분 후보에 **(d) 백업 목적지를 세션 scratch 로 강제**를 추가했다. 기존 (a)*"뮤테이션은
scratch 사본에서"* 만으로는 **이 사례를 못 막는다** — 그 규칙은 뮤테이션 **대상**을 말하지
**백업 목적지**를 말하지 않기 때문이다.

## INFO 3 — 반영

`PROJECT.md` 요약이 `node_modules` 제외를 빠뜨려 SoT(`spec-impl-evidence.md §4.2`)·구현
(`GOVERNANCE_SKIP_DIRS`)과 어긋나 있었다. **세 곳이 같은 규칙을 서술하는데 하나가
달랐다** — 이 PR 이 다루는 바로 그 클래스라 고쳤다. 문서 한 줄이라 게이트에 영향 없다.

## INFO 5·6 — 의도적으로 미룬다 (사유)

- **#5** fixture 헬퍼 `w` → `writeFixture` 개명
- **#6** `.md` 판별 predicate 를 두 `walkTree` 호출에서 추출

둘 다 타당하고 사소하다. 다만 **`codebase/**` 를 건드리므로 push 게이트의 코드 신선도가
깨져 7명 전원 리뷰를 한 라운드 더 태워야 한다.** Critical 0 · Warning 0 인 상태에서
순수 네이밍·중복 정리를 위해 그 비용을 치르지 않는다 — 이 저장소 규약도 *"Critical·Warning
0 이면 INFO 비차단 수렴"* 이다.

**다음에 이 파일을 다른 이유로 여는 작업이 함께 처리한다** (`redact-stored-error.ts` 위생
4건과 같은 처분 형태).

## 나머지 INFO — 조치 불요

- **#1** CI 트리거 `.claude/**` 확장자 무관 — 의도적. 좁히면 가드의 **구현·fixture**(비-md)
  가 바뀔 때 안 돌아 이 워크플로가 막으려는 갭이 다른 형태로 재발한다.
- **#2** `path.resolve` 미confine — 리뷰어도 *"이 PR 이 표면을 넓히지 않음"* 판정.
- **#4** `node_modules` 제외 근거 JSDoc 미서술 — 리뷰어도 *"자명"* 판정.
- **#7** 매직 넘버 3곳 중복 — 직전 라운드에서 **의도적 유지**로 결정, 재확인만.
- **#8** 신규 테스트가 `:(glob)*.md` 를 하드코딩 — YAML↔실행 연결은
  `DeadFilterTest` 가 실 YAML 을 읽어 이미 커버. 리뷰어도 *"실질 공백 아님"*.
- **#9** `mkdtemp` 잔존 가능성 — 표준 격리 패턴.
- **#10·#11·#12** 공유 함수 확장·신규 export·스크립트 삭제 — 회귀 없음 확인.

## 수렴 판정

| 라운드 | 성격 |
| --- | --- |
| `17_52_44` | **테스트 부재** — 핵심 주장을 상시 가드가 안 지킴 (W2·W3) + SPEC-DRIFT (W1) |
| `18_16_05` | **없음** — Critical 0 · Warning 0, 전원이 해소를 독립 검증 |

TEST WORKFLOW 4단계 PASS — frontend 6,174 passed(가드 13→19) · e2e 285.
