# RESOLUTION — `12_00_05` (3라운드, 강제 7명 전원)

RISK=MEDIUM · **CRITICAL 0** · WARNING 6 → **6건 전부 처리** (W5·W6 은 기지/절차 사안).
INFO 6·11 도 반영.

## 이 라운드가 말한 것 — 같은 클래스가 **세 번째**

> *"'미러 스윕이 몇 곳을 놓친다'는 동일 클래스의 결함이 세 번째로 재발했고, 그중 하나는
> `RESOLUTION.md` 가 실제로는 고쳐지지 않은 것을 '처리했다'고 명시적으로 잘못 기록한
> 사례라 문서 신뢰성 측면에서 낮게만 볼 수 없다."*

전부 받아들인다. 라운드별로 **내가 닫았다고 선언한 뒤** 새로 나온 자리가 3 → 3 이다.
수가 안 줄었다는 건 **닫는 방법이 틀렸다**는 뜻이다.

## W1 — **허위 완료 선언이 두 번 연속**

직전 RESOLUTION 에 *"유일한 잔존 소비처를 주어로 재연결해 처리했다"* 고 썼는데,
`git show 23e1c91a0` 대조 결과 **새 문장 하나를 앞에 끼워 넣었을 뿐** 뒤에 붕 뜬 절은
그대로였다. 리뷰어가 diff 로 반증했다.

원문을 `origin/main` 에서 꺼내 보고서야 원인을 알았다:

```
// … 이 상수는
// `handler-output.adapter.ts` 도 쓰고, 그쪽은 노드 `config` echo 를 DB·WS·표현식으로
// 내보낸다 — 비-자격증명 config 필드가 이 이름들과 겹치면 멀쩡한 값이 가려진다.
```

**`내보낸다` 는 제거된 소비처(어댑터)의 서술어**였다. 나는 그걸 살아 있는 문장으로 보고
앞에 주어를 하나 더 얹었고, 그래서 주어와 서술어가 **더 벌어졌다**.

이번엔 문장 **전체**를 취소선으로 넘기고, 잔존 소비처가 실제로 뭘 하는지 **읽고** 썼다 —
`explore-tools.service.ts:98` 의 `deepRedactSecrets(maskSensitiveFields(v))` 가 실행 행의
`inputData`/`outputData`/`error` 를 겹쳐 가려 LLM 컨텍스트로 내보낸다. 과잉 마스킹 성질은
그 소비처에도 그대로라 그 경고도 살렸다.

## W2 — 신설한 캐너리가 **vacuous** 였다

`expect(typeof out.apiKey).toBe('string')` 는 **마스킹돼도 참**이라 분기를 못 가른다.
리뷰어가 `deepRedactObject` 의 `v !== ''` 가드를 빼는 뮤턴트로 42/42 GREEN 을 실증했다.

`expect(out.apiKey).toBe('')` 로 교체하고 **같은 뮤턴트를 직접 재현**했다:

| | 예측 | 실측 |
| --- | --- | --- |
| M5 (`v !== '' &&` 제거) | 그 대조군 **1건만** RED | **1 failed / 41 passed** — 실패 케이스가 정확히 그 대조군 |

원복은 `cp` 백업으로 했고 `git status` 로 잔여 0 확인.

**한 라운드 전 CRITICAL 이 "캐너리가 아무것도 안 본다" 였는데, 그걸 고치는 라운드에서 낸
새 캐너리가 같은 병을 앓았다.** 타입 단언은 값 단언이 아니다.

## W3 — 내가 **보고도 정상이라고 판단한** 자리

`1-ai-agent.md:480` 은 직전 라운드 grep 출력에 **떠 있었다**. 나는 *"config echo 는 egress
를 지나니까 이 문장은 맞다"* 고 판단하고 넘겼다. 틀렸다 — 물었어야 할 질문은
**"그 값이 echo 에 실리기는 하는가"** 였다.

실측: `assembleSingleTurnConfigEcho`(1428~) · `buildMultiTurnConfigEcho`(3532~) **양쪽 모두
`llmConfigId` 0건**. 게다가 그 문장 자신이 나열하는 필드 목록이 곧 allow-list 이고 거기
`llmConfigId` 가 없다. **없는 값에 마스킹을 귀속**시키는 W3(2라운드)와 같은 결함이다.

## W4 — `4-execution-engine.md:1510`

같은 파일 `:193`/`:203` 은 고치면서 `:1510` 은 놔뒀다. 이건 이중으로 틀렸다 —
*"`maskSensitiveFields` 와 동일 **allow-list** 정책"* 이라 부르는데 그건 allow-list 가
아니라 **키 이름 목록 마스커**다. 실제 allow-list 는 `buildRetryState`/`buildResumeState`
가 옮길 키를 열거하는 쪽이다. 둘 다 정정.

## 이번엔 **닫는 방법**을 바꿨다

패턴 치환을 버리고 **두 축의 주장 기반 스윕**을 스크립트로 짜서 전수 판정했다:

- **[A]** `maskSensitiveFields` 를 언급 + 취소선 없음 + 미래형 아님 + 잔존 소비처 아님
  → **35건 적출, 전건 육안 판정**. 전부 정당했다 — 함수 자신의 정의/테스트, 잔존 소비처
  (`explore-tools`), 과거형 서술(`종전엔 …`), 그리고 내가 쓴 정정문.
  살아 있는 미래형 1건(`7-llm-client.md:325` — *"향후 body logging 이 도입될 경우"*)은
  `maskSensitiveFields` 가 아직 존재하므로 **유효**하다.
- **[B]** 부재(`미동봉`/`미포함`/`포함하지 않`)를 말하면서 그 부재를 마스킹에 귀속
  → **1건**, `1-data-model.md:564` 인데 이건 부재 귀속이 아니라 *"열거된 경로에서만 마스킹,
  DB 는 원문 보존"* 이라 해당 없음.

핵심은 **35 를 다 봤다**는 것이다. 앞선 두 라운드는 매번 *"고칠 자리 N곳"* 이라는 좁은
후보집합을 만들고 그 안에서만 움직였다 — 그러니 후보에 안 든 자매는 구조적으로 못 찾았다.

## W5 · W6 — 처분

- **W5** safe-by-convention 이동과 크로스-노드 릴레이는 R-5 정정 블록 + 트래커에 기등재.
  리뷰어도 *"이 PR 을 막을 신규 미문서화 결함은 아니다"* 로 판정. 우선순위만 유지.
- **W6** doc-link 곁다리 실측이 마스킹 스윕 커밋에 섞였다 — 맞다. 내용은 정확하지만 커밋을
  갈랐어야 했다. 리뷰어 판단대로 소급 분리는 하지 않고, 이번 커밋은 마스킹 범위로 한정했다.

## INFO

- **#6** `setStructuredOutput` 이 참조 저장인데 자매 `setEngineResolvedConfig` 는
  shallow-copy 하고 **그 이유까지 JSDoc 에 적어 뒀다** — 비대칭 지적이 정확하다. 게다가 이
  PR 로 **의미가 생긴** 비대칭이다(종전엔 `maskSensitiveFields` 가 만든 fresh 객체라
  aliasing 이 우연히 가려져 있었다). 그 사실을 `setStructuredOutput` JSDoc 에 대칭으로 명시.
- **#11** plan 의 테스트 카운트가 또 어긋났다(9,018 vs 9,020). 갱신하고, **"PR 이 닫히는
  시점의 값"** 이라는 단서를 그 줄에 붙였다.
- **#2**(WS 정규식 비대칭) · **#10**(egress 진입점 통합 테스트) — 기지 별건, 트래커 유지.
- **#7**(`DEFAULT_SENSITIVE_KEYS` export 범위) · **#9**(설명 근접중복) — 강제 아님. 이 PR 이
  이미 넓으므로 더 넓히지 않는다.

TEST WORKFLOW 4단계 PASS — backend **9,020 passed** / 433 suites · e2e 285 · ratchet 199/38.
