# RESOLUTION — `14_10_42` (6라운드, **forced 7/7 전원 처음부터**)

RISK=LOW · **CRITICAL 0** · WARNING 3 — **셋 다 기지·기처분 사안**이고 신규 결함이 아니다.
INFO 중 신규 관찰 2건은 트래커 등재, 1건은 즉시 확인해 무해 판정.

## 이번엔 forced 를 안 좁혔다

`12_52_43` W2 가 잡은 그 실수(내가 `agents_forced` 를 좁혀 넘겨 커버리지 자기검사를
공허하게 만든 것)를 반복하지 않으려고, orchestrator 가 `_retry_state.json` 에 기록한
**진짜 forced 목록을 그대로 읽어** 7명 전원을 돌렸다:

```python
forced = set(d["agents_forced"])   # ← 내가 정하지 않는다
inv = [i for i in d["subagent_invocations"] if ... in forced]
assert len(inv) == len(forced)
```

SUMMARY 도 *"forced 7개 전원 실행 및 전문 확보, 미이행 없음"* 으로 나왔고 이번엔 그 문장이
`meta.json` 기준으로 참이다.

## WARNING 1·2·3 — 전부 기지, 조치 불요

- **W1** DB 원문 저장으로 노출 표면이 두 egress 밖으로 넓어짐. R-5 정정 블록 기등재.
  다만 리뷰어 권고 중 **한 조각은 새것**이라 받았다 — *"DB 백업/리포팅·ETL 파이프라인이
  `config` 원문을 재유출하지 않는지"* 는 **아직 안 쟀다**. 트래커에 동반 점검으로 등재.
- **W2** safe-by-convention 전환 + 브랜디드 타입 부재. `10_53_52` 이래 매 라운드 비차단
  재확인. 트래커 우선순위 유지.
- **W3** doc-link 곁다리 혼입. `12_00_05` RESOLUTION W6 에서 *"소급 분리 안 함"* 으로 명시
  처분 완료. 세 번째 재지적이지만 처분이 바뀔 이유가 없다.

## INFO 9 — **즉시 확인했다**. 잔여물은 없었고, 원인 귀속이 틀렸다

requirement 리뷰어가 *"이 diff 와 무관한 미커밋 뮤테이션 잔여물(`DEFAULT_SENSITIVE_KEYS` 에
`oauthCred` 삽입) — **다른 병렬 세션**의 산물로 추정"* 이라 보고했다. 이 저장소가 겪은 적
있는 클래스라 바로 쟀다:

```
grep oauthCred …util.ts      → 없음
git status --short            → review/ 신규 디렉토리뿐
git show HEAD:…util.ts | grep -c oauthCred → 0
```

**작업트리·HEAD 모두 clean.** 실질 오염은 남지 않았다.

원인도 *"다른 세션"* 이 아니다 — **같은 라운드의 testing 리뷰어**가 캐너리를 재현하려 넣은
것이다(그 리뷰어가 뮤테이션 재현 성공을 보고했다). 즉 **형제 리뷰어가 서로의 관측을
오염시켰다**.

이번엔 무해했지만 **타이밍이 조금만 달랐으면** 형제가 뮤턴트 상태의 소스를 읽고 없는 결함을
보고하거나 있는 결함을 놓쳤을 것이다. 그리고 실측해 보니 **완화책이 프롬프트에 없다** —
`_prompts/testing.md` 에 `scratch`·`git restore`·`병렬` 언급이 **각 0건**이다. 종전 교훈이
reviewer 프롬프트 생성 경로에 반영된 적이 없다는 뜻이라 트래커에 처분 후보와 함께 등재했다.

## INFO 7 — 신규 관찰. 등재

이 PR **이전에 저장된** 실행을 단일 노드 디버그 재실행의 predecessor 로 시딩하면,
그 `config` 는 옛 boundary 가 남긴 `****abcd` 를 그대로 캐리한다. 표현식이 그 값을 읽으면
**마스킹 문자열을 읽는다** — 이 PR 이 고치려던 증상이 과거 실행에는 남는다.

일반적 마이그레이션 한계라 이 PR 을 막지 않는다. 처분 후보 두 개와 **먼저 잴 것**(그 경로의
실사용 빈도 · `****` 마커가 정상 값과 구별 가능한지)을 함께 등재했다.

## 나머지 INFO — 조치 불요

- **#8** `DEEP_REDACT_CACHE` staleness 가 `12_52_43` 의 *"오늘 도달 불가"* 처분과 일치함을
  독립 재확인. 내 실측(REST 는 조회마다 새 엔티티 · WS 는 이 캐시 미사용)과 같다.
- **#6** aliasing 계약은 JSDoc 2-hop 분리 + 캐너리 2건으로 이미 고정.
- **#14** `node-output.md` mutation-보호 절 미갱신 — `13_47_15` INFO 6 으로 이미 등재.
- **#10·#11·#12** 주석 길이 · 근접중복 · 캐스트 반복(임계선 3회 미달) — 여러 라운드째 형태
  변경 없이 유지. **#11 은 지적이 날카롭다**(실제로 mirror-sweep drift 가 여기서 났다)
  — 다만 그 다섯은 **정정문**이라 접으면 취소선의 대상이 사라진다는 사유를 `13_25_45`
  RESOLUTION 에 이미 적었고, **정정문이 수명을 다할 때** 축약 대상이다.
- **#13** egress 게이트웨이 e2e 갭 — 기존부터 있던 갭, 별건 추적 중.
