# RESOLUTION — `17_36_15`

- 결과: **RISK=LOW · Critical 0 · Warning 2 · SPEC-DRIFT 1 · INFO 10**, reviewer **9/9**
  (`concurrency`·`database` 를 강제 포함시켰다 — 트랜잭션 경계 변경이라 기본 7명으론 부족)
- 처리: **수동**. 셋 다 작고 main 이 전체 맥락을 갖고 있었다.

## WARNING 2건

| # | 지적 | 조치 |
| --- | --- | --- |
| 1 | CHANGELOG 미갱신 — 이 저장소는 유사 급 수정마다 dated addendum 을 남긴다 | 기존 "8곳이 행 배열로 오인" 절에 소급 정정 블록 추가 |
| 2 | 두 분기가 epilogue 4줄을 손으로 복제 | `finishStatusTransition` 으로 추출 |

W2 를 받아들인 이유가 중요하다. **이번 변경이 그 위험을 새로 만들었다** — else 분기가
트랜잭션을 쓰게 되면서 두 분기가 완전 대칭이 됐고, 그 순간 복제가 "다음에 한쪽만 고치는"
구조가 됐다. 이 파일은 형제 분기 drift 를 이미 여러 번 겪었고 **`WARNING #9` 주석 자체가
그 사례**다. 헬퍼 docstring 에 그 경위를 실었다.

## SPEC-DRIFT — 내가 `#1242` 에 쓴 각주가 불완전했다

리뷰어가 짚은 대상이 **바로 전 PR 에서 내가 쓴 소급 각주**다. 나는 `8332d9a20` 이 고쳤다고
적고 *"DB 는 안 깨졌다"* 로 마무리했는데, 그 수정이 얹은 throw 는 **트랜잭션 밖 단발 UPDATE**
위에 있어 롤백을 부르지 못했다. 그래서 각주는 "닫혔다" 로 읽히는데 실제로는 **관측만** 했고
롤백은 이 PR 이 한다.

**내가 반복 지적받아 온 "문서한 보장이 구현보다 넓다" 를, 하필 그 결함을 문서화하는
각주에서 또 했다.**

처리: `developer` 의 자기-반증형 소정정 예외는 **예고·트리거 문장**에만 열린다. 대상이
소급 각주(이력 서술)라 조건 2를 충족하지 못하므로 우회하지 않고 **planner 턴**을 밟았다 —
draft(`spec-draft-else-branch-transaction.md`) → `--spec` → 반영.

각주에는 **"창이 있었다" 와 "발동했다" 를 구분**해 적는다. 노출 창은 `8332d9a20`
(2026-08-13) ~ 이 PR 로 약 17일이지만, shape 위반은 드라이버 계약이 바뀌어야 나는
이벤트라 실제 발동 여부는 확인되지 않았다.

## INFO 처분 — 2건은 실행, 8건은 기록

| # | 처분 | 사유 |
| --- | --- | --- |
| 2 중첩 트랜잭션 self-deadlock | **실행** | 이번 변경이 새로 만든 제약이다. 두 분기 모두 내부에서 트랜잭션을 여니, 호출자가 자기 트랜잭션 안에서 부르면 self-deadlock. 현재 11곳은 전부 top-level(리뷰어 전수 대조). JSDoc 에 못박았다 |
| 3 테스트 이름이 mock 경계보다 넓다 | **실행** | `"UPDATE 가 롤백된다"` 는 결론까지 증명한 것처럼 읽힌다. 실 DB `ROLLBACK` 은 TypeORM 계약이라 mock 으로 증명 불가 — 이름을 `"…트랜잭션 manager 를 경유해 밖으로 전파된다 (롤백 전제조건)"` 으로 좁히고 경계를 주석에 적었다 |
| 1 커넥션 풀 점유 증가 | 조치 불요 | 의도된 트레이드오프(롤백 보장이 목적). 짝 전이 분기가 이미 같은 비용을 치른다 |
| 4 실 DB 롤백 e2e 부재 | **조치 불요 + 사유 기록** | shape 위반은 **드라이버 계약이 바뀌어야** 나는 이벤트라 e2e 로 재현할 방법이 없다(드라이버 mock 이 필요하다). INFO 3 의 주석에 이 한계를 함께 적어 "e2e 급 검증됨" 오인을 막았다 |
| 5 신규 테스트의 describe 배치 | won't-do | 기존 패턴 계승이고 이번 diff 가 만든 문제가 아니다 |
| 6 `execution.status` 대입이 트랜잭션 전 | 조치 불요 | 기존 코드, 이번 회귀 아님. shape 위반은 드라이버 버그급이라 실제 발생 가능성이 낮다 |
| 7 17일 노출 창 | 기록 | spec 각주에 실었다 |
| 8 무관 stale 체크박스 동반 처리 | 기록 | 리뷰어도 "커밋 메시지가 사유를 투명하게 설명해 실질 위험 낮음" 으로 판정. 다만 지적대로 **다음엔 별도 커밋**으로 가른다 |
| 9 함수 길이 | 조치 불요 | W2 헬퍼 추출로 완화됨 |
| 10 `/UPDATE execution/` 정규식 22곳 산재 | 조치 불요 | 이번 diff 범위 밖(기존 관용구) |

## planner 턴 결과 — `--spec` BLOCK: NO, WARNING 3건 전량 반영

| # | 지적 | 조치 |
| --- | --- | --- |
| 1 | `backend-lint-gate-broken-on-main.md` 의 `spec_impact: none` 이 방금 받은 완료 항목의 spec 영향을 못 담는다 | 두 파일로 정정 + 사유 배너. **`complete/` 이동 시 Gate C 가 그 값을 그대로 믿어 "영향 없음" 을 오확정**할 뻔했다 |
| 2 | 자매 티켓 `#12` 완료 블록이 이번 보강을 역참조하지 않는다 | "✅ 완료" 블록에 후속 보강 문단 추가 — 내가 닫은 산출물이 같은 날 보강되는데 그 사실이 닫힌 블록에 없었다 |
| 3 | `data-flow/3-execution.md` §2.1 `상태 전이` 행이 트랜잭션 소속 미표기 — 바로 아래 `park 진입` 행과 비대칭 | 행에 guarded UPDATE·트랜잭션·롤백 표기 + §1.1 상호링크 |

INFO 4건 중 **조건 1 보강**(자기-반증형 소정정에서 각주는 developer 저작도 아니다)은
사실이라 여기 기록한다 — 조건 1·2 **둘 다** 실패하므로 planner 턴이 옳았다. 나머지 셋
(EIA §9.3 상호참조 · `node-cancellation.md` §6 병행 각주 · draft 스타일)은 선택 사항이고
이 PR 범위 밖이라 조치하지 않는다.

## 테스트

TEST WORKFLOW 1단계부터 재실행:

| 단계 | 결과 |
| --- | --- |
| lint | PASS (48s) |
| unit | PASS (71s) — backend **9,095** |
| build | PASS (139s) |
| e2e | PASS (222s, backend 285) |

문서 가드 20파일 **3,090건** 통과. 엔진 스펙 단독 **456/456**.
