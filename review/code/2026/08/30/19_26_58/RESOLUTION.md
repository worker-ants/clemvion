# RESOLUTION — `19_26_58` (3라운드)

- 결과: **RISK=LOW · Critical 0 · Warning 1 · INFO 15**, reviewer **9/9**
- 이번 라운드의 코드 델타는 **JSDoc 13줄**뿐이다(동작 변경 0). 산출물 헤더 2줄 제거와
  plan 등재가 나머지다.

> **내 중간 판정이 틀렸다.** 리포트 7개가 도착한 시점에 "Critical 0 · WARNING 0" 이라
> 보고했는데, 유일한 WARNING 이 아직 안 온 `requirement.md` 에 있었다. Monitor 조건을
> `maintainability.md` 에 걸어 두고 그게 마지막인 줄 알았다 — **관측 도구가 부분집합만
> 보고 있었다.** 이 PR 이 세 라운드에 걸쳐 잡아 온 "확인한 범위보다 넓게 주장" 과 같은
> 형태라, 여기 남긴다. (7/9 라는 사실 자체는 그 턴에 정정해 두었다.)

## W1 — `--spec` 근거 draft 를 커밋 전에 지웠다

`plan/in-progress/spec-draft-else-branch-transaction.md` 가 **git 이력에 0건**인데, 커밋
메시지·RESOLUTION·자매 티켓 등 6곳 이상이 그 파일을 근거로 인용한다.

**실측하니 리뷰어 지적보다 나쁘다** — 직전 planner 턴(`#1242`)의
`spec-draft-raw-query-results.md` 도 이력 0건이고, **그건 이미 main 에 머지됐다.** 즉 지금
main 이 존재하지 않는 파일을 인용한다. 저장소엔 `spec-draft-*` 가 69개 보존돼 있어 관례도
명확하다(66개가 `plan/complete/`).

**원인은 실수가 아니라 습관이다.** planner 턴 끝에 draft 를 `rm` 했다. 두 턴 연속 같은
행동을 했으므로 우연이 아니다. draft 는 임시 파일이 아니라 **산출물**이다 — `developer` 가
`spec/` 을 직접 못 고치는 경계를 정당화하는 유일한 증거다.

### 복원 — 기억이 아니라 번들에서

원문이 `--spec` 프롬프트 번들의 코드펜스 안에 그대로 살아 있다(그 번들은 커밋됐다).
**기억으로 재작성하지 않고** `17_49_59/_prompts/cross_spec.md` 24~89행을 그대로 떠서
복원하고, 관례대로 `plan/complete/` 로 옮겼다(66/69 전례). Gate C 포함 문서 가드
20파일 **3,093건** 통과.

`#1242` 것은 **별도 커밋으로 가른다** — 한 라운드 전에 "무관한 정리를 같은 커밋에 섞지
말라"(`17_36_15` scope INFO 8)를 수용했고, 바로 다음 기회에 그 약속을 깨지 않는다.
복원 경로(번들 위치·행 범위)와 **발생원**까지 plan 에 등재했다.

## INFO 15건 — 신규 실행 항목 0건

15건을 성격별로 갈랐다:

| 성격 | 건수 | 내용 |
| --- | --- | --- |
| **내 2라운드 fix 가 옳다는 확인** | 3 | 호출부 수 정정(11→20) 정확 · "어휘적 범위" 한계 명시가 적절 · 프로토콜 헤더 누출 제거 확인 |
| 이전 라운드 기 처분의 재확인 | 6 | 커넥션 풀 점유(×3, 세 reviewer 가 각자) · self-deadlock 이 JSDoc 시행뿐(×2) · 함수 길이 169줄 |
| 이번 델타가 문서뿐이라는 관찰 | 3 | 런타임/시그니처/전역 상태 변경 없음 · 핵심 배선은 앞 두 라운드에서 검증 완료 |
| 스코프 관찰 | 2 | stale 체크박스 동반(반복 관측, 기 수용) · `finishStatusTransition` 추출이 요청 범위 밖으로 보이나 정당 |
| 문서 동기화 확인 | 1 | CHANGELOG·spec 2건·plan 2건이 코드와 일치 |

## 실체를 따로 확인한 것 — SQL 문자열 보간

`security` 가 유일한 보간부 `AND status IN (${elseStatusesSql})` 를 지목했다. 액면으로
넘기지 않고 읽었다: `NON_TERMINAL_STATUSES_SQL` / `NON_TERMINAL_OR_FAILED_STATUSES_SQL` 는
`Object.values(ExecutionStatus)` 로 조립되는 **정적 클래스 상수**라 사용자 입력이 닿지
않고, 이번 diff 는 그 조립 방식을 바꾸지 않고 쿼리를 트랜잭션 콜백 안으로 옮겼을 뿐이다.
인젝션 표면 변화 없음 — reviewer 결론과 일치.

## 수렴 판정

(a) **Critical 0 이 3라운드 연속**이고, 유일한 WARNING 을 이 턴에 닫았다.
(b) 앞 두 라운드의 실질 지적이 전부 닫혔다 — epilogue 중복(헬퍼 추출) · CHANGELOG ·
    SPEC-DRIFT(각주 보강 + 「원자성 보장」 문장) · JSDoc 호출부 수 · 산출물 헤더.
(c) INFO 15건에 **신규 실행 항목이 0건**이다. 절반이 "내 fix 가 옳다" 는 확인이고,
    나머지는 기 처분 재확인이거나 이번 델타가 문서뿐이라는 관찰이다.
(d) 세 reviewer 가 독립적으로 같은 것(커넥션 풀 점유)을 INFO 로 재확인했는데, 이는
    새 발견이 아니라 **1라운드에 이미 "의도된 트레이드오프" 로 처분**한 항목이다.

이 PR 의 목표선(가드 throw 시 UPDATE 롤백 보장)은 1라운드에 닫혔고, 2·3라운드는 그
주변의 **문서 정확성과 거버넌스 추적**을 좁히는 데 쓰였다.

**정직한 단서**: W1 의 복원은 `codebase/**` 를 건드리지 않으므로 리뷰 게이트를 다시
태우지 않는다. 그러나 라운드마다 새 WARNING 이 하나씩 나왔다는 사실(2 → 2 → 1)은
"다음 라운드도 뭔가 찾는다" 를 시사한다. 다만 그 층위가 코드 → 문서 → **산출물 위생**
으로 계속 멀어지고 있고, 이번 W1 은 이 PR 의 코드와 무관한 내 작업 습관 문제였다.
여기서 닫고, 습관 쪽은 plan 의 항목으로 남긴다.

## 남기는 후속 (plan 에 등재됨)

- **harness: sub-agent 프로토콜 헤더 누출 824건** — 처방은 산출물 일괄 수정이 아니라
  **발생원**(orchestrator 집계 또는 sub-agent 계약)이다. 바닥을 안 고치면 다음 라운드가
  또 825+n 을 만든다.
- **self-deadlock 확인의 호출 스택 축** — 이번엔 어휘적 범위까지만 봤다. JSDoc 에 그
  한계를 명시해 뒀다.
- **간헐 테스트 실패 1회 관측** — "harness 잡음" 으로 단정하지 않는다. 반복되면 이
  기록을 근거로 기각하지 말고 재조사할 것.

## 테스트

직전 커밋(`9d5e001bf`) 시점에 1단계부터 재실행: lint PASS(49s) · unit PASS(84s, backend
**9,095**) · build PASS(147s) · e2e PASS(223s, backend 285). 이번 라운드는 코드 델타가
없어 그 결과가 그대로 유효하다.
