# 유지보수성(Maintainability) 리뷰

## 발견사항

- **[WARNING]** `updateExecutionStatus` JSDoc이 "다음에 손댈 때 plan으로 이관"하기로 유예됐던 바로 그 시점에, 이관 대신 서사를 한 겹 더 쌓았다
  - 위치: `codebase/backend/src/modules/execution-engine/execution-engine.service.ts:8577`-`8600` (JSDoc 전체 범위는 `8553`-`8601`, 총 49줄)
  - 상세: 직전 리뷰 라운드(`20_21_06`)의 RESOLUTION은 이 JSDoc의 개정 이력 누적 문제(INFO #2)를 "유예 — **다음에 이 영역을 손댈 때** 세대별 서사를 plan으로 이관. 지금 옮기면 또 코드가 바뀌어 라운드가 붙는다"고 명시적으로 처분했다. 그런데 이번 diff가 정확히 그 "다음에 이 영역을 손댄" 순간이다 — `8577`-`8600`에 "호출 스택 축도 확인했다 (2026-08-31)" 단락(약 15줄)이 새로 추가되며 JSDoc은 4세대째(`17_36_15` → `18_10_28` → `20_21_06` → 이번) 개정 서사를 이어 붙이는 형태가 됐다. 유예 조건이 충족됐는데도 이관은 일어나지 않았고, 오히려 같은 수치(`.transaction(` 총 36개·모듈 안 9개·모듈 밖 27개)가 `plan/in-progress/backend-lint-gate-broken-on-main.md:290`-`298`에도 거의 동일한 문장으로 재기재돼, 두 SoT가 함께 자라는 패턴이 반복됐다. 코드 자체 로직 변경은 0(순수 주석)이라 기능 위험은 없지만, "현재 유효한 제약이 무엇인가"를 파악하려는 다음 개발자가 지나간 세 라운드의 리뷰 서사를 함께 읽어야 하는 부담은 이번에도 줄지 않고 늘었다.
  - 제안: 이번 라운드에서 실행하기 부담스럽다면(코드가 다시 바뀌어 라운드가 또 붙는다는 기존 우려는 이해되나), 최소한 plan 문서 쪽에 "다음 접촉 시 이관" 항목을 명시적으로 재등록해 유예가 무기한으로 늘어지지 않도록 한다. 혹은 JSDoc은 "현재 유효한 제약 + 남는 한계 + 최신 스냅샷 수치"만 남기고, `17_36_15`/`18_10_28`/`20_21_06` 세대별 괄호 각주는 plan 링크 하나로 축약하는 것을 고려.

- **[INFO]** 신규 회귀 테스트 2건이 계약 문구의 번호 매김(`'1)'`, `'2)'`, `'3)'`) 문자열에 구조적으로 결합돼 있다
  - 위치: `.claude/tests/test_agent_return.mjs:109`-`125` (특히 `113`-`114`의 `REPORT_RETURN_CONTRACT.indexOf(step1)` / `fileClause.indexOf('2)')` 슬라이스), `127`-`138`(`lines.find(l => l.trim().startsWith(n))`, `n`이 `'2)'`·`'3)'`)
  - 상세: 두 테스트 모두 `REPORT_RETURN_CONTRACT`을 파싱할 전용 헬퍼 없이, 문자열 리터럴 `'1)'`/`'2)'`/`'3)'`을 찾아 그 지점으로 슬라이스하는 방식이다. 계약 문구가 향후 번호 체계를 바꾸거나(예: "①" 같은 다른 표기), 본문 산문 중 우연히 `'2)'`라는 부분 문자열이 더 일찍 등장하면(현재는 없음) `indexOf`가 의도치 않은 위치에서 잘라 테스트가 조용히 통과하거나 실패 사유가 불분명해질 수 있다. 이번 라운드에서 뮤테이션 검증(계약 문구를 구버전으로 되돌려 RED 확인)은 이미 수행돼 현재는 vacuous가 아님이 확인됐지만, 결합 자체의 취약성은 남아 있다.
  - 제안: 당장 조치는 불요. 향후 `REPORT_RETURN_CONTRACT`에 손댈 일이 생기면, 번호 리터럴 대신 각 스텝을 배열 인덱스나 명명된 상수로 분리해 두는 리팩터를 함께 고려할 것.

- **[INFO, 확인]** 직전 라운드(`20_21_06`)의 핵심 WARNING("가드 테스트 파일명 리네임이 3개 워크플로의 로컬 헤더 주석에는 반영되지 않음")은 이번 diff에서 완전히 해소됐다
  - 위치: `.claude/workflows/ai-review.js:109,113`, `.claude/workflows/consistency-check.js:48,52`, `.claude/workflows/merge-coordinate.js:58,62`, `.claude/workflows/_lib/agent-return.mjs:15,48`
  - 상세: 저장소 소스 전수(`grep -rl "test_workflow_shared_block" --include="*.js" --include="*.mjs" --include="*.py" --include="*.ts" .`)를 직접 재확인했다 — 잔여 0건. 남은 참조는 전부 `review/code/2026/08/30/20_21_06/*.md`(직전 라운드 산출물, 시점 기록이라 편집 대상 아님) 안에서만 존재한다. 같은 파일 안에서 두 곳이 서로 다른 파일명을 가리키던 자기모순은 더 이상 없다.
  - 제안: 조치 불요, 참고 기록.

## 요약

이번 diff는 이전 리뷰 라운드(`20_21_06`)가 지적한 네 건의 WARNING 중 정정 가능한 항목(가드 파일명 절반 리네임, `.transaction(` 수치 재검증·측정 방법 명시, forward-looking 지시문 복원, 커밋 분리 판단 기록)을 성실히 반영했고, 소스 전수 재확인으로 잔여 드리프트가 없음도 확인된다. `REPORT_RETURN_CONTRACT`을 파일/반환 메시지 두 sink로 명확히 가른 계약 정정과 이를 뒷받침하는 신규 회귀 테스트(뮤테이션으로 vacuous 아님 확인됨)는 견고하다. 다만 `updateExecutionStatus` JSDoc은 "다음 접촉 시 plan으로 이관"이라는 스스로의 유예 조건이 이번에 충족됐음에도 이관 대신 서사를 한 겹 더 쌓아 49줄로 늘었고, 같은 수치가 plan 문서와 이중으로 유지되는 패턴이 반복·심화됐다 — 기능 위험은 없으나(순수 주석) 다음 독자의 인지 부담은 계속 누적되고 있다.

## 위험도

LOW
