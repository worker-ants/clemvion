# RESOLUTION — `20_21_06`

- 결과: **RISK=MEDIUM · Critical 0 · Warning 4 · INFO 6**, reviewer **7/7**
- 처리: **수동**.

## W2 가 이번 라운드의 핵심 — 리뷰어 둘이 서로 다른 수를 냈다

`requirement` 는 **35/26**, `documentation` 은 **36/27** 을 주장했다. 어느 쪽도 액면으로
받지 않고 직접 셌다. 쟁점이 둘이라 **둘 다** 처리하는 패턴으로 측정했다:

- 제네릭 인자 `.transaction<T>(` 포함
- **주석 줄 제외** — 내 JSDoc 프로즈 자체가 그 문자열을 담고 있어 자기참조로 부푼다

실측: **총 36 = 엔진 모듈 9 + 모듈 밖 27**. 주석 인용 2건(둘 다 내 JSDoc)은 정확히
배제됐다. 즉 **내 수치가 맞고**, `requirement` 의 35 는 `webauthn.service.ts:338` 의
`this.dataSource.transaction<Outcome>(` 를 단순 패턴이 놓친 값이다.

### 그런데 같은 함정에 **나도 걸려 있었다**

`#1243` 의 호출 스택 대조에서 나는 엔진-참조 여부를 확인할 파일 목록을 만들 때 같은
단순 패턴을 썼고, **그래서 `webauthn.service.ts` 가 그 목록에서 빠져 있었다.** 27개 중
26개만 본 셈이다. 이번에 그 하나를 닫았다 — 엔진 서비스 참조 0건, 그 트랜잭션 블록 안
`this.*` 호출 0건. 이제 27 전수가 실제로 확인됐다.

**리뷰어의 결론(35)은 틀렸지만 그 지적이 내 실제 갭을 찾아냈다.** 수치를 그대로 받았으면
잘못 고쳤을 것이고, 무시했으면 갭을 못 봤을 것이다.

## W1 — 미러링을 보장한다는 PR 이 바로 옆줄에서 미러링을 깼다

가드 파일명 정정이 `SHARED-BLOCK` 마커 줄에만 적용되고, 그 4~5줄 위 로컬 헤더 주석은
옛 이름을 그대로 가리켰다(3개 워크플로 전부). 그 위치는 마커 **밖**이라
`test_workflow_scripts.py` 의 `_extract_block()` 이 구조적으로 못 본다.

내가 문자열 치환을 `guard: …` 가 붙은 형태로만 걸어서 다른 문장 형태를 놓쳤다. 세 곳을
고쳤고, **저장소 전수로 재확인**해 소스 쪽 잔여 0건을 확인했다(남은 17건은 이번 리뷰
리포트가 그 이름을 *인용*한 것 — 산출물은 그 시점 기록이라 고치지 않는다).

## W3 — 내가 지운 forward-looking 지시문을 복원했다

"새 호출부를 추가할 때는 그 축도 함께 볼 것" 을 개정하면서 대체 없이 지웠다. 복원하되
**세는 방법까지** 적었다 — 제네릭 포함·주석 제외. 리뷰어 둘이 각각 그 두 함정에 걸려
35 와 36 으로 갈렸다는 사실 자체가 다음 사람에게 가장 쓸모 있는 정보다.

자동 가드는 여전히 없다. JSDoc 이 유일한 방어라는 점을 명시했다.

## W4 — 커밋 분리, 세 번째 지적

무관한 두 주제를 한 커밋에 넣었다. 사용자가 두 건을 함께 요청해 한 PR 로 묶었고 두 번째는
순수 주석이라 기능 위험이 없다고 봤다. **되돌리지 않되 판단을 plan 에 기록**했다 — PR 이
하나여도 커밋은 나눌 수 있었다. 이 세션에서 같은 지적을 세 번 받았으므로 습관 쪽이다.

## INFO 처분

| # | 처분 |
| --- | --- |
| 3 같은 블록의 "9" 가 두 집합을 가리킴 | **실행** — "위 driver 경유 9곳과는 다른 집합" 명시 |
| 1 이 리뷰 세션이 구버전 계약으로 기동됨 | 조치 불요 — 저장소에 5번째 사본 없음(전수 확인). 머지 후 새 세션이 신 문구를 붙이는지 사후 확인 |
| 2 JSDoc 개정 서사 누적 / 수치가 plan 과 중복 | 유예 — 다음에 이 영역을 손댈 때 세대별 서사를 plan 으로 이관. 지금 옮기면 또 코드가 바뀌어 라운드가 붙는다 |
| 4·5 미러링·파일명 정정은 scope 위반 아님 | 확인 기록 |
| 6 신규 테스트가 vacuous 아님(뮤테이션 확인) | 확인 기록 |

## 테스트

lint PASS(47s) · unit PASS(68s) · build PASS(136s) · e2e PASS(192s, backend 285).
harness 가드: `test_workflow_scripts.py` 5 passed/9 subtests · `test_agent_return.mjs` 13/13.

## 수렴 판정

네 WARNING 전부 닫았고 **코드 동작 변경은 0**이다(전부 주석·계약 문구·plan). 남은 INFO 는
확인성 3건, 조치 불요 1건, 유예 1건(JSDoc 서사 이관 — 지금 옮기면 코드가 또 바뀌어
라운드가 붙는다)이다.

**이 라운드가 값어치한 이유**는 W2 다. 리뷰어 둘이 갈린 덕에 내가 직접 셌고, 그 과정에서
**내 앞 라운드 대조가 `webauthn.service.ts` 하나를 빠뜨렸다**는 걸 찾았다. 리뷰어의 결론
(35)은 틀렸지만 그 불일치가 진짜 갭을 드러냈다 — 액면으로 받았어도, 무시했어도 놓쳤다.
