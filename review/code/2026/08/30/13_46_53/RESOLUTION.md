# RESOLUTION — `13_46_53` (3라운드)

- 결과: **RISK=MEDIUM · Critical 0 · Warning 4 · INFO 8**, reviewer 7/7
  (`forced_missing=[]`, `unfinished=[]`, router 제외 0명)
- 처리: **수동** (SKILL §5) — 네 건 모두 작고 main 이 전체 맥락을 갖고 있었다.

## 먼저 — 내 수렴 예측이 틀렸다

2라운드 뒤 나는 *"WARNING 추이 6 → 3 이고 이번 fix 는 고리를 끊은 것이라 여기서 닫힐 것"*
이라고 적었다. **틀렸다** — 3라운드는 4건이다. 단조 감소가 아니었고, 추세로 수렴을
예측한 것 자체가 근거가 약했다.

## WARNING 4건 — 전부 반영

| # | 지적 | 조치 |
| --- | --- | --- |
| 1 | `CHANGELOG.md` 가 1라운드 시점에 고정 — "음성 5" 인데 실제 **7** | 7 로 정정 + 허용목록이 개수 단위 면제로 바뀐 사실 + 순수 함수 추출 반영 — **이 "7" 도 틀렸다**: 같은 커밋이 CTE 캐너리를 8번째로 추가했다. 4라운드에서 **8** 로 재정정 |
| 2 | plan 완료 배너가 1라운드 뮤테이션 표만 실어, 허용목록이 아직 전면 면제로 읽힘 | `### 후속 하드닝` 절 신설 — 3라운드 표 + 최종 상태 + 실패의 구조적 원인 |
| 3 | `findUnguarded` 합성 테스트 5개가 전부 `discovered` 원소 **1개** — 다중 보고를 못 가름 | 다중 unguarded 케이스 + "통과 항목이 순회를 끊지 않는다" 역방향 케이스 추가 |
| 4 | CTE 접두(`WITH … UPDATE … RETURNING`) blind spot 미공개 | docstring `안 보는 것` 절에 명시 + 음성 캐너리 추가 |

### W4 가 특별한 이유 — 발견이 요약을 거치며 사라졌다

이 항목은 **1라운드 리뷰(`12_41_15` requirement)가 이미 짚었는데 SUMMARY 합성 과정에서
누락돼** 2·3라운드를 그냥 지나갔다. 3라운드가 재발견해서 살았다.

**개별 리포트에 있던 발견이 요약에서 사라질 수 있다.** 나는 매 라운드 SUMMARY 만 읽고
처분했으므로, 요약이 떨어뜨린 것은 내게 도달하지 않았다. 이건 리뷰 파이프라인의 성질이지
누구의 부주의가 아니다 — 그래서 docstring 과 캐너리에 경위를 함께 적었다.

### W3 은 또 같은 병의 다음 겹

합성 테스트를 만들었는데 **원소를 하나만** 썼다. 그래서 루프가 첫 unguarded 에서 `break`
해도 5개 전부 GREEN 이었다(리뷰어가 그 뮤턴트로 실증). 판별 입력의 **차원**을 한 칸 좁게
잡은 것 — 이 PR 전체를 관통한 형태가 합성 입력 안에서 한 번 더 나왔다.

## 독립 재검증 — 커밋된 테스트가 잡는다

| 뮤턴트 | 예측 | 실측 | 잡은 테스트 |
| --- | --- | --- | --- |
| 첫 unguarded 에서 `break` (리뷰어가 쓴 그 뮤턴트) | RED | **RED 1** | `unguarded 가 여럿이면 전부 보고한다` |

원복 후 `src/common/**` **35 suites / 444 tests** GREEN.

## INFO 8건 처분

| # | 처분 | 사유 |
| --- | --- | --- |
| 1 `findUnguarded` 를 `source-scan.ts` 로 미이관 | **조건부 유예** | 두 번째 소비자(`assert-row-array.spec.ts` 가 발견형으로 확장)가 생기는 시점이 트리거. 지금 옮기면 소비자 하나짜리 공유 모듈이 된다 |
| 2 `[string, number]` 튜플 무명 | won't-do | 라벨 튜플 별칭은 이 파일 규모에 과하다 |
| 3~8 | 조치 불요 | `SRC` hoist·`kb-stats` 타입 정정·스캐너 보안·규약 미문서화(planner 추적 중)·JSDoc 정확성·워크플로 산출물 — 전부 확인성 |

## 테스트

lint PASS(50s) · unit PASS(73s, backend **9,091**) · build PASS(143s) ·
e2e PASS(263s, backend 285 + playwright 51).

## 수렴 판정

이 라운드의 4건 중 **둘은 내 문서의 사실 오류**(CHANGELOG 수치·plan 배너 낡음)이고 둘은
가드 커버리지다. 넷 다 닫았고, 특히 W3·W4 는 **영속 테스트**로 고정했다(프로브 아님).

남은 INFO 는 트리거가 있는 유예 1건과 확인성 7건뿐이다. 다음 라운드가 또 다음 겹을 찾을
가능성은 있으나, 그 지점은 이제 **가드의 가드**(합성 입력의 차원, 요약 누락) 층위라 이
PR 의 목표(목록 밖 지점을 잡는다)와의 거리가 멀다 — developer SKILL §수렴 예외로 닫는다.
