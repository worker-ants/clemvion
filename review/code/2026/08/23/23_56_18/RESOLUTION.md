# RESOLUTION — `23_56_18` (3라운드)

RISK=LOW · CRITICAL=0 · WARNING=4. **W3·W4 가 진짜 신규 지적이고 둘 다 고쳤다.**
W1·W2 는 리뷰어가 직접 *"조치 불요"* 로 판정한 기존 항목의 재확인이다.

## W3·W4 — 같은 주장을 다섯 곳에 썼는데 셋만 고쳤다

`23_29_27` CRITICAL 을 받고 *"REST 와 SSE 는 같은 강도다"* 를 §R17 · WS §4.4 · CHANGELOG
**셋**에서 좁혔다. 그런데 같은 주장이 **JSDoc 두 곳에도** 있었다:

| 자리 | 상태 |
|---|---|
| `spec/5-system/14-external-interaction-api.md` §R17 | ✅ 앞 커밋에서 정정 |
| `spec/5-system/6-websocket-protocol.md` §4.4 | ✅ 앞 커밋에서 정정 |
| `CHANGELOG.md` | ✅ 앞 커밋에서 정정 |
| **`websocket.service.ts` `toFanoutEnvelope` JSDoc** | ❌ → 이번에 정정 (W3) |
| **`interaction.service.ts` `getStatus` JSDoc** | ❌ → 이번에 정정 (W4) |

**이것이 이 저장소가 반복해 겪는 그 형태다** — *"고쳤다" 를 쓰는 시점에 자매를 전수로 세라.*
나는 **문서 세 곳**을 세고 **코드 주석 두 곳**을 안 셌다. W4 의 `interaction.service.ts` 는
이번 diff 에 `.spec.ts` 만 들어 있어 더 놓치기 쉬웠다 — 리뷰어가 그 점을 정확히 짚었다.

- **W3**: `toFanoutEnvelope` JSDoc 의 *"여기까지 닫아야 REST 와 SSE 의 방어 강도가 같아진다"*
  를 **`waiting_for_input` 표면** 한정으로 좁히고, `envelope.output` 이 이종 payload 라 같은
  목록을 걸 수 없다는 실측(`{}`)과 캐너리 위치를 함께 적었다.
- **W4**: `getStatus` JSDoc 의 *"SSE·fanout 은 잔여"* 를 취소선 + 정정으로 바꿨다 —
  waiting 표면은 닫혔고 잔여는 `execution.node.*` 의 `envelope.output` 하나다.

**전수 스윕도 돌렸다**: `REST 와 SSE` · `REST·SSE` · `강도가 같` · `강도가 다르` ·
`SSE·fanout 은 잔여` 를 `codebase/**` · `spec/**` · `CHANGELOG.md` · `plan/**` 전체에서
grep 했고, 남은 hit 은 (a) 이미 취소선 처리된 자리, (b) 무관한 문장(`terminal-error-payload.ts`
등), (c) plan 의 이력 서술(상단에 정정 배너가 서 있다) 뿐이다. plan 체크리스트의
*"'강도가 다르다' 서술 제거"* 한 줄은 실제 결과(*제거*가 아니라 *범위 축소*)에 맞춰 고쳤다.

## W1 — 하위 호환 소급 축소

리뷰어 판정 그대로 **조치 불요**. 1·2라운드에서 처분·문서화 완료했고 이번 라운드에 코드
변경 없음. 운영 로그 감사는 여전히 세션 범위 밖.

## W2 — `envelope.output` 잔여

리뷰어가 *"이미 올바른 스코프 판단 + 캐너리 고정"* 으로 **조치 불요** 판정했다. 직전 커밋의
처분과 같은 결론이며, 후속 착수 지침(`outputData` shape 전수 판별 선행)도 리뷰어가 트래커
항목과 같은 내용을 적었다.

## INFO

- **#9 (testing)** — *"`{}` 붕괴 실측 자체를 고정하는 회귀 테스트가 없다"*. 타당한 지적이라
  **트래커 항목의 착수 지침에 이미 넣어 뒀다**(*"버튼 재개 record fixture 로 `{}` 붕괴를
  먼저 캐너리로 고정"*). 이번 라운드에 넣지 않은 이유: 그 캐너리는 **닫는 작업의 첫 걸음**
  이지 잔여를 기술하는 이 PR 의 일부가 아니다. 리뷰어가 나이브 fix 를 직접 넣어 RED 전환을
  재현해 캐너리가 vacuous 하지 않음을 확인했다.
- **#7 (describe 블록명 불일치가 4→5건)** — 기존 defer 결정 유지. 트래커 항목에 이미 서 있고,
  이번에 늘어난 것도 같은 항목이 덮는다.
- **#5 (spec 편집이 코드 커밋에 동봉)** — 타당하다. 다만 이번 건은 **spec 을 따로 머지하면
  live drift 가 생기는** 상황이라 같은 PR 안에 둔 것이고(#1204 에서 내린 것과 같은 판단),
  리뷰어도 *"범위 이탈로 단정할 근거는 아니다"* 로 판정했다. 향후 별도 커밋 분리 권고는 수용.
- **#10 (뮤테이션 표에 잔여 캐너리 미반영)** — 그 캐너리는 **뮤턴트로 죽이는 가드가 아니라
  현 상태 기술**이라 M 표의 대상이 아니다. 대신 캐너리 JSDoc 이 *"닫히면 RED 가 되고 그
  단언을 뒤집는 것이 작업의 일부"* 라고 명시한다.
- 나머지 INFO(#1·#2·#3·#4·#6·#8·#11·#12) — 조치 불요 또는 앞 라운드에서 처분됨.
