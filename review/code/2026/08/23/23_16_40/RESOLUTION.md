# RESOLUTION — `23_16_40` (2라운드, `22_51_46` fix 후 fresh review)

RISK=LOW · CRITICAL=0 · **WARNING=1**. 1라운드 4건 → 1건으로 줄었고, 남은 1건은
**새 지적이 아니라 1라운드 W4 의 재확인**이다.

## WARNING 1 (side_effect / api_contract) — 하위 호환 소급 축소

리뷰어 자신이 *"조치 불요(코드 변경 막을 사유 아님) — 이미 CHANGELOG 자기반증형 정정 +
spec §R17 flip 으로 문서화 완료"* 로 판정했다. 1라운드 W4 에 대해 이 세션이 한 처분과
**같은 결론**이다:

- **한 것**: CHANGELOG 정정 블록이 외부 수신자 **동작 변경**임을 명시하고, 알려진 두
  소비처(위젯·chat-channel)는 실측 무영향, 제3자 webhook 구독자는 **확인 범위 밖**임을
  갈라 적었다. spec §R17 표의 SSE 행을 flip 했다.
- **못 한 것**: 배포 전/후 webhook payload 키 분포 표본 감사 — **운영 로그 접근이 없다.**
  재개 조건이 명확하므로(감사 수단이 생기면) 여기 남기고 넘어간다.

**수렴 판정**: Critical 0 + 새 Warning 0. 남은 Warning 1 은 이미 처분·문서화된 수용
항목이라 `feedback_review_fix_stale_loop` 의 수렴 기준(발견의 **성격**이 동작→구조→문서로
이동)에 부합한다.

## INFO — 처리

- **#8 (requirement)** — **내 서식 오류**였다. 트래커 항목 제목에서 볼드가 중첩돼
  (`**wire-only ~~4키~~ **8키**가 …**`) 렌더가 깨졌다. 중첩을 풀어
  `**wire-only 키가 … 밖이다** (~~4키~~ → **8키**)` 로 고쳤다.

## INFO — 트래커에 등재하고 넘김

`review/**` 는 SoT 가 아니므로, 미룬 항목은 **이 턴에 정본 트래커**
(`spec-sync-external-interaction-api-gaps.md`)에 적었다.

- **#6 (architecture) + #1 (security)** — **한 항목으로 묶어 등재.** fanout chokepoint 가
  타입이 아니라 **JSDoc 으로만** 강제되고(`broadcastToChannel` 은 여전히 public), allowlist 는
  **이름 기반**이라 같은 이름의 내부 필드가 나중에 붙으면 통과한다. 두 지적의 처방이 같은
  자리(신규 emit·신규 top-level 필드 리뷰 체크리스트)라 갈라 적을 이유가 없다.
  이번 diff 가 만든 문제는 아니지만, **이 PR 이 그 문에 보안 책임을 하나 더 얹어** 우회
  비용이 전보다 커졌다는 점을 함께 기록했다.
- **#14 (testing) + #12 (testing)** — 캐너리 4건이 `describe('llmCalls strip …')` 안에 있어
  블록명이 대상과 어긋난다. **이번에 안 옮긴 이유는 "위험" 이 아니라 "값"** 이다: 이동은
  `codebase/**` 변경이라 방금 끝난 리뷰가 다시 stale 이 되는데, 순수 이동에 리뷰 한 바퀴를
  쓸 값어치는 없다. `emitNodeEvent` 미검증도 같은 자리에 적었고, 재개 신호를
  *"`emitNodeEvent` 가 `nodeOutput` 을 싣는 첫 케이스"* 로 명시했다.

## INFO — 조치 불요 (사유 확인만)

- **#3 (`.includes()`→`Set`)**: 1라운드에서 거절한 근거(타입 결속 상실 + 두 번째 동기화
  지점)를 리뷰어가 **그대로 인용하며 보류 유지**로 판정했다. 재개 조건은 *"원소 수가 수십
  단위"* — 현재 13.
- **#5 (shared 계층 도메인 결속)**: `import type` 이라 런타임 순환이 없고, 이번 라운드에
  재배치를 재검토해 무변경 결론과 사유를 이미 기록했다.
- **#7 (공유 목록이 두 표면 계약을 동시 결정)**: 1라운드 W1 로 처리 — REST 캐너리가 그
  확장을 의도로 고정한다.
- **#9·#10·#11 (maintainability)**: 전부 재개 조건 명시(3번째 소비처 / 그룹 4개 이상).
- **#13 (명시 `null` 분기)**, **#15 (`{@link}` 렌더)**, **#16·#17 (리뷰 산출물 성격)**,
  **#2·#4·#18·#19**: 조치 불요 또는 확인용.
