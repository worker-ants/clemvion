# RESOLUTION — `01_44_22` (2라운드, forced 7/7)

RISK=LOW · **CRITICAL 0** · WARNING 3 → **전부 반영**. INFO 7·8·9 도 반영.

## W2 — **이 PR 이 세운 원칙을 형제 가드에 안 적용했다**

지적이 가장 날카롭다. 1라운드에서 `direct` 분기를 *"커버리지 0인 방어는 위험하다"* 는
이유로 지웠는데, **같은 함수의 `!code || !message` 가드도 커버리지 0** 이었다. 리뷰어가
`if (false)` 뮤테이션으로 87/87 GREEN 을 실증했다.

**이쪽은 지우지 않고 테스트를 붙였다** — `direct` 와 달리 **도달하기 때문**이다. 백엔드가
`error: {}` 나 코드 없는 객체를 싣는 것은 타입상 막히지 않고, 그때 가드가 없으면
`code`/`message` 가 `undefined` 인 배너가 사용자에게 빈 칸으로 보인다.

| | 예측 | 실측 |
| --- | --- | --- |
| M4 `if (!code \|\| !message)` → `if (false)` | 1 failed (새 가드 테스트만) | **1 failed** — 일치 |

리뷰어가 87/87 GREEN 을 본 그 뮤턴트를 이제 문다.

## W1 — **내 JSDoc 이 같은 PR 안에서 낡았다**

캐너리 위 주석에 *"CT-S9/S10 은 `error` 를 객체로 보내 `direct` 분기로 통과한다"* 고 썼는데,
같은 diff 의 1라운드 fix 에서 **CT-S9/S10 도 문자열로 정정했고 `direct` 를 지웠다**. 내가
그 JSDoc 을 라운드 1 **이전에** 쓰고 갱신하지 않은 것이다.

이 PR 에서만 *"주석이 사라진 상태를 서술한다"* 가 세 번째다(1라운드 W1·W2, 지금 W1).

원문을 취소선으로 남기고, **이 캐너리를 따로 두는 진짜 이유**로 재서술했다 — CT-S9/S10 은
`retryable` 유무라는 다른 축을 보느라 payload 에 필드가 섞여 있고, 이 캐너리는 결함의
**최소 조합**만 남겨 회귀 시 원인이 한 줄로 읽히게 한다.

## W3 — CHANGELOG

이 저장소는 *"조용히 죽어 있던 경로를 고쳐 사용자 관측 동작이 바뀌는"* 변경을 운영 영향과
함께 기록해 왔다(리뷰어가 `CHANGELOG.md:485` 선례 제시). 이번 건이 정확히 그 계급인데
누락했다.

Unreleased 에 항목을 넣고 **"이 배포 이후 사용자가 처음 배너를 본다 — 회귀가 아니라 원래
의도된 동작의 복구"** 를 명시했다. 백엔드 payload·API 계약은 안 바뀌었다는 것도 함께 적었다.

## INFO — 반영

- **#7** `details` 키 부재 조합 미검증 → `retryable` 이 `false` 로 떨어지는지 고정.
- **#8** *"does NOT APPEND"* 테스트가 옛 object shape 을 쓰고 있었다 — 이 PR 이 세운
  *"fixture = production shape"* 원칙을 **음성 테스트에도** 적용했다.
- **#9** 상위 describe 주석이 한 겹 얕았다(`output.error`) → `output.output.error`.

## 넘김 (사유)

- **#3** 두 핸들러의 ~20줄 중복 — 1라운드에서도 범위 밖으로 판정했고 리뷰어도 같은 판정.
  추출하려면 두 핸들러의 **다른 차이**(completed 는 `duration`·`status` 처리가 다르다)까지
  건드린다.
- **#4** `asRecord(asRecord(domain)?.error)` 밀도 — JSDoc 이 보완한다는 리뷰어 판단에 동의.
  중간 이름을 주면 `domain` 이 하나 더 늘어 오히려 읽기 부담이 는다.
- **#5** `payload.output` 타입 표기 불일치 — 공유 `NodeHandlerOutput` 타입 부재가 근본
  원인이고, 그 타입을 프런트에 들이는 것은 이 결함과 직교한 별건이다.
- **#6** JSDoc 이 함수보다 길다 — 컨벤션과 일치한다는 리뷰어 판단에 동의. 이 결함이 **문서
  문구에서 왔으므로** 포스트모템을 그 자리에 두는 값이 크다.
- **#10** 범위 밖 인접 주석(`payload.output` 필드 위) — diff 밖이고 기술적으로 틀리지 않다.
- **#11** PR 설명에 배너 최초 노출 명시 — PR 본문에 반영한다.

TEST WORKFLOW 4단계 PASS — frontend **6,181 passed**(이 스위트 87→89) · e2e 285.
