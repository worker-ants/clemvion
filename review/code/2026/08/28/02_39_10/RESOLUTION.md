# RESOLUTION — `02_39_10` (5라운드, forced 7/7)

RISK=LOW · **CRITICAL 0** · WARNING 1 → 반영. INFO 12 도 함께.
7명 중 **6명 NONE**, testing 만 LOW.

## W1 — 가드 축이 한 칸 더 바깥으로 갔다

`details.retryable` / `details.retryAfterSec` 의 `typeof` 좁히기가 무테스트였다. 리뷰어가
타입 체크 제거 뮤테이션으로 **92/92 GREEN** 을 실증했다.

**실패 모드가 구체적이다**: 백엔드가 `retryable: "true"`(문자열)를 보내면 그 가드가 없을 때
`"true"` 가 truthy 라 **`[다시 시도]` 가 잘못 노출**된다 — 사용자가 가망 없는 재시도를
누른다(CT-S10 이 막으려던 바로 그 상황).

**두 핸들러 모두에 넣었다.** 그 블록은 완전히 복제돼 있어 한쪽만 테스트하면 나머지가
무방비다 — 이 PR 이 세 번 겪은 *"형제 중 하나만"* 을 반복하지 않으려는 것이다.

| | 예측 | 실측 | 의미 |
| --- | --- | --- | --- |
| M9 `failed` 쪽 가드 무력화 | 1 failed | **3 failed** | 나머지 둘도 같은 블록을 지난다 — 설명됨. **`completed` 는 안 죽었다** |
| M10 `completed` 쪽 가드 무력화 | 1 failed | **1 failed** — 일치 | `completed` 케이스만 죽었다 |

**두 뮤턴트가 서로 다른 테스트를 죽인 것**이 대칭 커버의 증거다. 한쪽만 테스트했으면
M10 이 살아남았을 것이다.

## INFO 12 — `details` 비-object

`details: "n/a"` 케이스도 같은 자리에서 함께 가른다(M9 가 무는 3건 중 하나).

## 수렴 판정 — 성격으로 본다

| 라운드 | 발견의 성격 |
| --- | --- |
| `01_26_11` W4 | **실질 결함** — JSDoc 분리 · 죽은 분기 · fixture 복제 |
| `01_44_22` W3 | **실질** — 자기모순 주석 · 도달하는 가드의 커버리지 0 · CHANGELOG |
| `02_02_18` W1 | **실질** — 단락 평가가 분기를 가림(직전 판정 반증) |
| `02_21_19` W1 | **테스트 정밀도** — 분기를 못 가르는 fixture |
| `02_39_10` W1 | **테스트 정밀도** — 선택적 메타데이터 필드의 타입 가드 |

CRITICAL 0 이 5라운드 연속이고, **코드 동작 결함은 3라운드째 없다**. 남은 것은 매번
*"바로 옆 가드도 테스트가 없다"* 이고 축이 바깥으로 한 칸씩 이동한다.

이번엔 지적받은 자리만 고치지 않고 **두 핸들러 대칭 + `details` 비-object 까지 한 번에**
닫았다. 이 파일의 다항 술어·타입 가드는 이제 전수로 fixture 를 갖는다(4라운드 자가 점검
+ 이번 라운드). **수렴으로 판정한다.**

## 넘김 (사유)

- **#5** `details` 캐스트가 `asRecord` 와 달리 배열을 배제 안 함 — 소비부가 named-property
  접근만 하고 wire 상 배열 경로가 없다(리뷰어도 *"실질 영향 없음"*). 4라운드에서 확인한
  **등가 뮤턴트** 클래스와 같은 자리다.
- **#9** 좌/우항 가드 2건의 준비 코드 반복 — `it.each` 는 세 번째 조합이 생기면.
- **#10·#11·#13** 핸들러 중복 · 언래핑 밀도 · 타입 표기 · 도메인/wire 주석 — **5라운드
  연속 유예**, 사유 유효. 리뷰어도 *"현 판정 유지"*.
- **#6** `handleNodeCompleted` 도달 가능성 — 백엔드 조사(planner 턴), 이 PR 과 직교.
- **#3** 프로바이더 원문 메시지 노출 — spec 이 승인한 계약.
- **#14** PR 본문 문구 — PR 생성 시 반영.

TEST WORKFLOW 4단계 PASS — 이 스위트 **95/95**(92→95) · e2e 285.
