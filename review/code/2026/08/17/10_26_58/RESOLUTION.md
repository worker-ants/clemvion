# RESOLUTION — `10_26_58` ai-review 5라운드 후속 조치

**CRITICAL 0 · WARNING 6 · 위험도 MEDIUM.** forced 7명 전원 결과 확보.
**6건 전부 조치**(이연 0). 전부 직전 커밋(`83436ed45`)이 formal spec 만 갱신하고 남긴
**문서 stale** 이었다 — 코드·보안·spec 은 클린이었다.

| # | 조치 | 내용 |
|---|---|---|
| 1 | **반영** | 트래커가 `⑥-b`·background-runs 캐너리를 여전히 *"비대상 고정"* 으로 서술 — **정반대를 고정**한다. 방치하면 후속 작업이 방금 고친 flip-flop CRITICAL 을 되돌린다. 캐너리를 **방향별 표**로 갈라 적었다 |
| 2 | **반영** | CHANGELOG 의 블랭킷 *"`inputData` 는 마스킹하지 않는다"* 에 레벨 한정 캐비엇 추가 |
| 3 | **반영** | plan 요약 표 B행 + §철회에 "범위 정정" 절 신설(레벨 표 + 캐너리 방향) |
| 4 | **반영** | 유저 가이드 Input 행에 마스킹 캐비엇(KO/EN) — 이제 노드 레벨이 마스킹되므로 Output 행과 대칭이 맞다 |
| 5 | **반영** | 테스트 JSDoc 이 `⑥-b`·background-runs 를 "비대상 고정" 으로 **오분류**했다. 개수·목록 대신 **방향별 표**로 재작성 |
| 6 | **반영** | `NodeExecutionSummaryDto` 에 `inputData` 선언 추가. 런타임에는 늘 있었는데 스키마에만 없던 선존 갭인데, 마스킹 정책이 얹히며 *"문서화되지 않은 필드에 문서화되지 않은 정책"* 이 되어 닫았다 |

## 이 주석이 두 번 틀렸다는 사실을 주석에 남겼다

`executions.service.spec.ts` 의 캐너리 JSDoc 은 초판이 *"네 표면"* 이라 적고 다섯을
나열했고(`00_23_57` W1), 그 정정판은 `⑥-b`·background-runs 를 반대 방향으로 오분류했다
(`10_26_58` W5). 두 실패 모두 **개수·목록으로 적었기 때문**이라, 이제 **방향별 표**로
적고 그 이력을 주석 안에 남겼다 — 같은 형태가 세 번째로 재발하지 않게.

## INFO 19건 — 조치 불요

전부 선행 라운드가 평가·수용·등재를 마친 항목이거나 양호 사례 기록이다. 대표:
`SECRET_LEAK_PATTERNS` bare `token=`(등재됨) · WS 채널 role 미검사(선존, 이번 diff 는 완화
방향) · `maskIfPresent` 방어 분기 도달 불가(기존 처분 유지) · 깊이 상한 상수 중복(후속).
requirement 리뷰어는 **위험도 NONE**, 카브아웃이 코드·테스트·spec 3층에서 대칭임을 실측 확인.

## 검증

TEST WORKFLOW 4단계 PASS — lint / unit(백엔드 427 suites) / build / e2e **276**
