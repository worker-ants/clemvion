# RESOLUTION — `11_44_10`

ai-review **CRITICAL 0 / WARNING 7**. 4건 조치, 3건은 트래커 등재분 재확인.

## W1 — 뮤테이션으로 실증된 마지막 무방비 경로

**조치 완료.** `markQueueWaitTimeout` 의 `durationMs` threading 이 **추출부를 통째로 깨는
뮤테이션(`?.duration_ms` → `undefined`)에도 GREEN** 이었다. mock 의 `raw` 가 비어 있어
RETURNING 경로가 애초에 안 돌았기 때문이다 — **직전 라운드에 `markWebChatIdleTimeout` 에서
겪은 것과 완전히 같은 형태**다.

mock 에 `duration_ms: 600000` 을 주고 정확 매칭으로 고정. 이 경로만 값의 의미가 **"큐 대기
시간"** 이라 형제 4경로로 대체 증명되지 않는다는 지적이 정확했다.

> **같은 형태를 두 라운드 연속 겪었다.** 첫 번째는 리뷰어가 잡아 줬고, 두 번째는 리뷰어가
> **뮤테이션으로 직접 실증**해서 왔다. "mock 이 그 경로를 태우는지" 를 단언 추가와 함께
> 확인하는 것이 절차로 자리잡지 않았다는 뜻이다.

## W3 — 자매 문서 미적용, 3번째

**조치 완료.** 직전 라운드에서 `execution-engine.service.ts` 의 "호출부 4곳" 을 5곳으로
정정했는데, **같은 사실을 서술하는 `terminal-duration.ts` 는 4곳인 채로 뒀다.**
"한 곳만 고쳤다" 가 이 브랜치에서 세 번째다.

## W4 · INFO 2 — 조치 완료

- **W4** `[x]` 로 닫은 plan 항목의 본문 **절반만 취소선**이라 미완료로 오독될 상태였다.
  전체를 인용 블록 + 취소선으로 닫고, **등재 시점 추정(4곳)이 실측(6곳)보다 작았다**는
  사실도 적었다
- **INFO 2** 테스트가 int4 상한을 상수 참조와 **리터럴 하드코딩** 두 경로로 검증하고
  있었다 — 상수가 바뀌면 리터럴 쪽만 거짓 GREEN 을 낸다. 보간으로 통일

## 넘김 — 트래커 등재분

| # | 처분 |
|---|---|
| W2 (emit 초크포인트 부재) | **6라운드 반복 재발의 구조적 원인**이라는 진단이 맞다. 별건으로 등재돼 있다(emit 경계 타입화) |
| W5 (REST 비대칭) | `09_58_24` W4 |
| W6·W7 (retry-turn·집계 오염) | `10_34_51` W1·W3 |

## 검증

- 백엔드 **425 suites / 8707 passed** · lint `--max-warnings 0` · 타입 **199**(래칫 동일)
- spec 가드 **2931**
