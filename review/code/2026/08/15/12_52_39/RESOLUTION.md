# RESOLUTION — `12_52_39`

ai-review **CRITICAL 0 / WARNING 6**. **코드 변경 없음** — 3건 신규 등재, 3건 기등재 확인.

리뷰어의 결론을 그대로 인용한다: *"이번 PR 자체는 위 조치들 없이도 merge 가능한 상태"*.

## 수렴 판정

이 브랜치는 10라운드를 돌았다. 수렴은 "발견 0" 이 아니라 **발견의 성격**으로 판단한다:

| 라운드 | 성격 |
|---|---|
| `09_58_24` · `11_09_44` | **CRITICAL** — 실행이 영구 고착 (int4 오버플로, SQL·JS 각각) |
| `10_34_51` ~ `11_44_10` | 과잉 스코프 1건, vacuous mock 2건 — **동작** |
| `11_59_09` | AVG 집계 오염 1건 — **동작** (내 변경이 유발) |
| `12_26_36` | vacuous mock 1건 + 자매 오버플로 1건 — **동작** (둘 다 내가 만듦) |
| `12_52_39` | **선존 구조 1건 + 리팩터 2건** — 전부 이 PR 이 유발하지 않음 |

동작 결함이 마지막 라운드에서 처음으로 **0** 이 됐다. 신규 지적 3건은 전부 이 PR 밖의
선존 항목이고, 나머지 3건은 이미 CHANGELOG·spec·트래커에 고지된 기지 항목이다.

## W1 — 세 자매 중 하나만 트랜잭션 밖 (database) → **등재**

**리뷰어가 직전 라운드의 자기 판정을 실측으로 정정한 건이다.** 확인했고, 사실이다:

| 함수 | 트랜잭션 | NodeExecution 쓰기 |
|---|---|---|
| `cancelParkedExecution` | **있음** | 있음 |
| `markWebChatIdleTimeout` | **있음** | 있음 |
| `finalizeStalledExhausted` | **없음** | 있음 |

첫 UPDATE 커밋 후 둘째가 실패하면 자식 NodeExecution 이 **영구 `RUNNING`** 으로 잔류한다 —
자매 두 함수의 docstring 이 경고하는 그 실패 모드다.

**유예 근거 (실측)**: `git diff origin/main...HEAD` 에서 이 함수의 NodeExecution cascade 도
트랜잭션 경계도 건드린 라인이 **0건**이다. 선존이고 이 PR 이 위험을 바꾸지 않았다.
`spec-sync-external-interaction-api-gaps.md` 에 표와 함께 등재했다.

> 이 저장소의 반복 형태(*"하드닝을 자매 함수 미적용"*)의 교과서적 사례라 그 표현으로 적어
> 뒀다 — 셋 중 둘만 닫혀 있다.

## W5·W6 — 관용구 16곳 반복 (maintainability) → **등재, 이번 PR 에서 안 함**

`RETURNING` 추출 5곳 + 재계산 대입 11곳. 헬퍼로 축소하라는 지적은 타당하다.

**유예 근거**: 이 브랜치에서 **넓은 일괄 편집이 대상 밖 8곳을 조용히 바꿔 전량 되돌린
전례**가 있다. 16곳을 한 번에 건드리는 리팩터를 10라운드째 PR 의 끝에 넣는 것은 그 사고를
재현하기 딱 좋은 자리다. 별도 PR 에서 리뷰와 함께 한다 — 트래커 등재했다.

## W2·W3·W4 — 기등재 확인

| # | 항목 | 상태 |
|---|---|---|
| 2 | 프런트엔드 Duration 컬럼 | 트래커 등재 + **왜 status 필터가 오답인지** 실측 기재. CHANGELOG·유저가이드 KO/EN 고지 |
| 3 | REST/push `durationMs` 비대칭 | CHANGELOG·spec·트래커 3중 고지 |
| 4 | retry-turn DB≠emit | spec §6.5 "알려진 예외" + 트래커 |

리뷰어가 셋 다 *"차단 사유 아님"* 으로 명시했다.

## INFO 처분

| # | 처분 |
|---|---|
| 1 (이중 호출) | 무조치 — O(1) 순수함수, 4라운드 공통 확인 |
| 2 (dashboard enum vs statistics 리터럴) | **타당한 지적**. W5·W6 리팩터와 같은 파일군이라 함께 등재된 후속에서 |
| 3 (`expect.any(Number)`) | 무조치 — 헬퍼 자체가 NaN 을 전수 커버 |
| 4 (plan 현재형 서술) | 무조치, 비차단 (10라운드째 확인) |
| 5 (주석 3중 복제) | 무조치 — 타입 분리는 의도적 |
| 6 (avg 지표 정의 변경) | **CHANGELOG 에 고지 완료** — 직전 라운드에 추가했다 |
| 7 (SQL 인젝션 표면 없음) | positive finding |
| 8 (타입 초크포인트 등재 확인) | 직전 라운드 지적이 닫혔음을 리뷰어가 실측 확인 |

## 검증

이 라운드는 **코드를 바꾸지 않았다** — 직전 라운드의 검증이 그대로 유효하다.

- 백엔드 **425 suites / 8710 passed** · lint `--max-warnings 0` **0 errors**
- 타입 **199** (래칫 일치) · frontend docs 가드 **20 files / 2931 passed**
- W1 판정: 세 자매의 트랜잭션 유무를 스크립트로 전수 확인 (위 표)
- W1 선존 판정: `git diff origin/main...HEAD` 해당 함수 cascade/트랜잭션 **0줄**
