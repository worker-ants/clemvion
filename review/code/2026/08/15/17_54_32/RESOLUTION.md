# RESOLUTION — `17_54_32`

ai-review **CRITICAL 0 / WARNING 7**. 전부 조치 + INFO 3건.

**이 라운드가 잡은 둘은 내가 이 PR 에서 만든 것이고, 하나는 내가 쓴 판별력 주장이 거짓이라는 것이다.**

## W4 — 내가 클래스 JSDoc 을 지웠다 (scope)

**조치 완료. 가장 아픈 지적이다.**

`TerminalEventPayload` 의 JSDoc 을 클래스 선언 위에 넣으면서 **기존 클래스 docstring 을
밀어냈다** — "C-6 strangle step 1" · 24곳 직접호출 이력 · 향후 비-WS 채널 확장 노트가
저장소 어디에도 남지 않았다(리뷰어가 `grep` 전수로 확인). `@Injectable() export class` 에
docstring 이 아예 없는 상태였다.

`git show origin/main:` 으로 원문을 복구해 클래스 위에 되돌리고, 타입 JSDoc 은 타입 위에
분리했다. **파사드로 달라진 점("종결 3종은 thin wrapper 가 아니다")** 한 문단만 덧붙였다.

> 문서를 **추가**하려다 **삭제**했다. 삽입 위치가 기존 블록을 밀어내는지 보지 않았다.

## W6 — 판별력 주장이 거짓이었다 (testing)

**조치 완료. 그리고 이 지적을 처리하다 내 주석이 틀렸다는 걸 뮤테이션이 반증했다.**

리뷰어: *"TS2345 검증이 수작업으로 보이며 저장소에 박제되지 않았다 — 누군가 필드를
optional 로 완화해도 기존 테스트는 GREEN"*. 맞다.

`@ts-expect-error` 5건을 넣고 주석에 *"ts-jest 가 타입체크하므로"* 라고 적었는데 —
**뮤테이션이 반증했다.** `cancelledBy` 를 optional 로 완화해도 **jest 는 9/9 GREEN** 이다.
이 저장소의 jest 는 타입을 strip 한다.

실제 강제 주체는 **타입 래칫 게이트**(`tsc`)다 — 같은 뮤턴트에서 진단이 **199 → 200**.
주석을 그 사실로 정정했다.

**지시문 위치도 틀렸었다.** 여러 줄 객체 리터럴은 에러가 **속성 줄**에 보고되는데 객체 위에
둔 2건이 `Unused '@ts-expect-error'` 로 잡혀 래칫이 **203** 이 됐다. 속성 바로 위로 옮겨
199 복귀.

> 이 저장소의 기록된 교훈 그대로다 — *"타입 가드·새 테스트가 실제로 실행·타입체크되는지
> 확인"*. 나는 그 교훈을 알면서 **주석에 반대로 적었다.**

## W1 — CHANGELOG 누락, 그리고 실제 wire 변화가 있다 (documentation)

**조치 완료.** 이 PR 은 순수 리팩터가 아니다 — `retry-turn` 재진입 취소 경로가 종전엔
`result` 키를 안 실었는데 이제 **`result.cancelledBy: "user"` 가 실린다.** 수신자가 보는
payload 가 바뀐다. 그 사실과 영향(“`result` 부재를 신호로 쓰던 코드”)을 명시했다.

## W2·W3 — 자매 plan 의 stale (documentation · requirement)

| # | 항목 | 조치 |
|---|---|---|
| 2 | 취소선이 **라벨(`~~원문:~~`)에만** 걸려 아래 문단이 stale | 문단 **전체**를 인용+취소선으로. 같은 PR 계열의 `durationMs` 항목이 이미 겪고 고친 "절반만 취소선" 이 인접 파일에서 재발했다 |
| 3 | 그 plan 이 스스로 "단일 진실" 로 지정한 **SoT 표 #2 행** 미갱신 | **P2 완료** + 근거 링크. `--impl-prep` 이 이 리스크를 사전에 예견했었다 |

## W5·W7 — 중복과 부채

| # | 조치 |
|---|---|
| 5 | `TYPE_TO_EVENT` 가 두 describe 에 복제 → **모듈 스코프 단일 선언** |
| 7 | 순환 근본 미해소 → **트래커 등재**. `websocket.service` 가 런타임 값(enum)과 서비스를 함께 export 하는 것이 원인. 72 suites 사고가 이론이 아님을 보여준 사례로 함께 적었다 |

## INFO 처분

| # | 처분 |
|---|---|
| 9 (`failed` + `error: null` 미검증) | **조치** — 키 유지를 `'error' in wire` 로 단언 |
| 7·10 (plan 문서가 초안 메서드명·타입 사용) | **조치** — 실제 구현과 통일 |
| 1 (`cancelledBy: 'user'` 정확도) | 무조치 — plan 에 기지 한계로 명시됨. `error.code` 파생은 후속 |
| 2·4 (파사드가 도메인 파생 흡수 · refactor+fix 혼합) | 무조치 — JSDoc·plan·커밋에 정직하게 문서화됨 |
| 3 (`message` 가 spec 보다 엄격) | 무조치 — 3 호출부 전부 채운다. 안전한 방향 |
| 5 (wire 계약 변화) | **W1 에서 CHANGELOG 로 고지** |
| 6·8 (값 import 확대 · `wire` 가 `Record`) | 무조치 — 후자는 테스트가 방어. 급하지 않음 |

## 검증

- 백엔드 **425 suites / 8737 passed** · lint **0** · 타입 **199**(래칫)
- 판별력(실제 강제 주체 = `tsc` 래칫): `cancelledBy` optional 완화 → **199 → 200**
- 직접 호출 잔여 **0곳** (스크립트 실측)
