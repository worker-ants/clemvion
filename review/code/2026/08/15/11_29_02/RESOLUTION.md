# RESOLUTION — `11_29_02`

ai-review **CRITICAL 0 / WARNING 8**. 4건 조치 — **전부 내가 이번 PR 에서 쓴 것의 결함**이다.
나머지 4건은 트래커 등재분.

## W3 — 내가 상수를 끼워 넣으면서 JSDoc 을 고아로 만들었다

**조치 완료.** 직전 커밋에서 `PG_INT4_MAX` 를 `resolveTerminalDurationMs` **바로 앞**에
삽입했더니, 기존 함수 JSDoc 이 상수에 자리를 뺏겨 **어느 선언에도 붙지 않게** 됐다.
IDE hover·TypeDoc 에서 문서가 사라진다.

**이 세션에서 두 번째 orphan JSDoc 이다**(직전 PR 에서도 함수를 옮긴 자리에 같은 일이
있었다). 하필 이 PR 이 두 차례 CRITICAL 을 겪고 남긴 **안전 근거 문서**라 유실 비용이 컸다.
상수 블록을 위로, 함수 JSDoc 을 함수 바로 위로 재배치.

## W4 — 내가 센 "호출부 4곳" 이 실제로는 5곳

**조치 완료.** 직전 라운드에서 JSDoc 을 정정하면서 *"호출부 4곳 모두 값을 넘긴다"* 고
썼는데 `grep -c` 실측은 **5**다. **정정하는 문장에서 또 잘못 셌다.**

## W5 — 2라운드째 미이행 + mock 이 threading 을 안 태우고 있었다

**조치 완료.** `markWebChatIdleTimeout` emit 에 `durationMs` 단언을 넣었더니 `null` 이
나왔다 — **mock 이 `raw` 를 안 줘서** RETURNING 경로가 애초에 안 돌고 있었다.
`expect.any(Number)` 로 뒀으면 vacuous 했을 자리다. mock 에 `duration_ms: 3600000` 을 주고
**정확 매칭**으로 고정했다.

> 이 라운드에서 가장 값진 발견이다. "단언을 추가했다" 가 "검증했다" 를 뜻하지 않는다 —
> **mock 이 그 경로를 태우는지**를 함께 봐야 한다. 이 세션이 뮤테이션으로 네 번 배운 것과
> 같은 교훈이 mock 축에서 재현됐다.

## W2 — CHANGELOG 가 영향 범위를 좁게 적었다

**조치 완료.** *"취소 UPDATE 가 실패"* 라고만 적었는데 실제 도달 경로는 **정상 완료**도
포함한다(폼·버튼·AI 대기에 시간 기반 강제취소가 없다). "JS·SQL 두 경로 모두" 로 확장.

## 넘김 — 트래커 등재분

| # | 처분 |
|---|---|
| W1 (종결 payload 조립 초크포인트 부재) | **가장 근본적인 지적이다.** `emitExecution(payload: unknown)` 때문에 필드 하나를 16곳에 손으로 스레딩했고, 이 PR 이 두 라운드에 걸쳐 같은 클래스의 누락을 냈다. 별건 등재됨(emit 경계 타입화) |
| W6 (retry-turn DB≠emit) | `10_34_51` W1 |
| W7 (AVG 집계 오염) | `10_34_51` W3 |
| W8 (REST 비대칭) | `09_58_24` W4 |

## 검증

- 백엔드 **425 suites / 8707 passed** · lint `--max-warnings 0` · 타입 **199**(래칫 동일)
