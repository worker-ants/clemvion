# RESOLUTION — `15_23_10` (+ consistency `15_23_42`)

ai-review **CRITICAL 0 / WARNING 3** · 위험도 **LOW**.
`--impl-done` **BLOCK: NO** — 직전 라운드의 CRITICAL 이 해소됐다.

2건 조치, 1건 등재. **코드는 바꾸지 않았다** — 전부 spec/plan 문서다.

## 수렴 판정

| 라운드 | 성격 |
|---|---|
| `13_58_27` | WARNING 10 — **동작 2건**(내가 만든 vacuous mock·자매 오버플로) |
| `14_47_14` | WARNING 2 — 재발 방지(극성 캐비엇·가드 목록) |
| `15_00_41` + consistency **CRITICAL** | WARNING 4 — **내 수정의 잔여**(try/catch·되쓰기 단언) + 규약문서가 되돌려진 동작 서술 |
| `15_23_10` + consistency **BLOCK: NO** | WARNING 3 — **선존 구조 1 · 테스트 위생 1 · 내 문서 stale 1** |

동작 결함이 0 이고, 신규 지적 중 이 PR 이 유발한 것은 **문서 stale 1건**뿐이다.

## W3 — "체크박스는 갱신, 옆 산문은 stale" 세 번째 (documentation)

**조치 완료. 같은 세션에서 세 번째다.**

정본 트래커의 결론 문단이 *"이 PR 이 세운 불변식의 **유일한 잔여 위반**이다. 즉시 고치지
않은 이유는…"* 이라는 **미완료 전제**를 유지했다 — 바로 위 체크박스 3개가 이번 diff 에서
전부 `[x]` 로 바뀌었는데도.

같은 클래스를 이 세션에서 이미 두 번 겪었다:

1. §6.5 를 **보존하려던 편집**에서 근거 세 줄을 취소선 없이 삭제
2. `node-cancellation.md` 가 **되돌려진 중간 동작**을 계속 서술 (consistency CRITICAL)
3. 이 문단

취소선 + 해소 노트로 정정하고, **그 사실 자체를 문단에 적었다** — *"체크박스를 옮길 때 그
옆 산문을 같이 읽어라."*

## W1 — 동일 전이에 독립 emit 이 여러 번 (concurrency) → **캐비엇 + 등재**

**조치: spec 캐비엇 추가 + 트래커 등재.**

0행 후 재조회 방식으로 **"DB 와 모순되는" 중복은 닫혔지만**, 여러 finalizer 가 같은
CANCELLED 전이를 각자 관측하면 각자 발행한다. payload 는 같으므로(모두 DB 정본을 읽는다)
**모순이 아니라 중복**이다.

리뷰어가 *"이번 diff 가 새로 만든 문제는 아니고… 긴급도는 낮으나"* 로 명시했다. 수신자가
알아야 할 계약이라 **§6.5 에 캐비엇**을 넣고(멱등 처리 안내), lock 보장 실측은 트래커에
등재했다.

## W2 — 테스트 `(d)` 가 공유 `arrange()` 를 우회 (maintainability) → **등재**

**유예 근거 (실측)**:

- 테스트 **자체는 판별력이 있다** — `try/catch` 를 뺀 뮤턴트에서 `(d)` 가 **RED**
- 지적은 **미래 stale 위험**이지 현재 동작·커버리지 결함이 아니다
- 정정은 codebase 편집이라 리뷰 신선도 게이트가 다시 열린다. 이 브랜치는 이미 **5라운드**

동작 위험이 아니므로 별도 PR 로 등재했다.

## consistency `15_23_42` WARNING 2건 — 둘 다 조치

| # | 항목 | 처분 |
|---|---|---|
| 1 | §6.5 "해소" 가 검증 범위보다 넓다 | **조치** — *"배선의 해소이지 실 DB 값 검증의 해소가 아니다"* 캐비엇 + 자매 plan #4 참조. **문서한 보장이 구현보다 넓으면 안 된다** |
| 2 | `§5.4` 앵커가 **같은 문서의 엉뚱한 절**을 가리킴 | **조치** — `2-api-convention.md#54-부재-표현--null-vs-키-생략` 로 정정. INFO 3(캐비엇 표기)도 함께 |

## INFO 처분

| # | 처분 |
|---|---|
| consistency INFO1 (`eia-types.ts` 미러) | **등재** — spec 지정 코드 SoT 4면 중 1면. 체커가 *"기능 파급 없음(현재 아무도 안 읽음)"* 으로 판정. codebase 편집이라 별도 PR |
| ai-review INFO 1·5·6 | 기결정 / 이론적 / 구조적 한계(자체 주석에 명시됨) |
| ai-review INFO 3·7·8 | in-place mutation 계약 · 레이블 순서 · positive finding — 비긴급 |

## 검증

- 백엔드 **425 suites / 8730 passed** · lint **0** · 타입 **199**(래칫) · spec 가드 **2935**
- **TEST WORKFLOW 최종 커밋 기준 재실측** — build PASS · **e2e 276 passed**
  (직전 기록은 3커밋 전 값이었다)
- 이 라운드의 편집은 **spec/plan 문서뿐** — 코드 변경 0
