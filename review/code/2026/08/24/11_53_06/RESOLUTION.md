# RESOLUTION — `11_53_06` (3라운드, 타겟 2명)

RISK=LOW · CRITICAL=0 · **WARNING=1**. 그 1건과 INFO 1건을 처리했고, 둘 다 **코드가 아니라
내 기록**의 결함이었다. 나머지 INFO 2건은 사유를 적고 넘긴다.

## WARNING 1 (documentation) — 커밋 본문은 "PASS" 라 적고 체크박스는 비어 있었다

직전 커밋(`225936105`) 본문이 *"TEST WORKFLOW 4단계 PASS — backend 8,997 passed / 433
suites · e2e 285 passed · ratchet 199/38 일치"* 라고 **수치까지** 적었는데, **같은 커밋이
실어 보낸 plan 의 그 체크박스는 `[ ]` 였다.**

plan 은 종결 게이트·lifecycle 이동이 읽는 SoT 라, 그 상태로 두면 다음 판단이 *"아직 안
돌았다"* 로 읽는다. 이 저장소가 반복해서 기록한 **"plan 체크박스 = 실제 상태"** 형태이고,
*"체크와 `complete/` 이동은 한 동작"* 이라는 규칙도 같은 자리에 있다.

동기화하면서 `/ai-review` 3라운드 결과도 함께 적었고, 미체크 **0건**을 확인한 뒤
`in-progress/` → `complete/` 로 옮겼다 — 두 동작을 갈라 두지 않았다.

## INFO 1 (testing) — 뮤테이션 카운트가 stale

M1 을 돌린 시점의 spec baseline 은 **58건**인데, 이후 리뷰가 캐너리를 늘려 지금은 **63건**
이다. 표의 `2 failed / 56 passed` 가 그래서 낡았다.

**숫자를 덮어쓰지 않고 두 시점을 나란히 적었다** — 이 저장소의 규칙이 *"PR 안의 정량 기록은
PR 이 닫히는 시점의 값"* 이므로 종결 시점 재측정(**M5**: `3 failed / 60 passed`)을 행으로
추가하고, 왜 baseline 이 둘인지를 표 아래에 적었다. 결론(그 뮤턴트가 잡힌다)은 두 시점 모두
같고, **늘어난 RED 1건이 `.failed` 캐너리**다 — 즉 stale 이 오히려 그 캐너리가 실제로
일하고 있다는 증거가 됐다.

## 넘김 (사유)

- **INFO 2** (`narrowTopLevelNodeOutput` 의 `null`·스칼라 분기 미pin) — 리뷰어가 직접
  짚었듯 자매 `allowlistNodeOutputKeys` 가 **같은 가드를 한 번 더** 갖고 있어 지금은
  관측 가능한 동작이 불변인 **동치 뮤턴트**다. 다만 리뷰어가 단 조건(*"sister 가드를
  제거하는 리팩터 시 조용히 무너진다"*)이 정확해서, 그 재개 신호를 남기는 편이 낫다 —
  다음에 이 헬퍼를 만질 때 `it.each([null, 'str', 42])` 로 못박는다.
- **INFO 3** (신규 캐너리 3건의 `describe` 배치) — 기존 트래커 항목의 연장이고, 늘어난
  3건도 그 항목이 덮는다. 순수 이동에 리뷰 한 바퀴를 더 쓸 값어치는 없다(`#1208` 에서 같은
  판단을 했고 리뷰어도 매 라운드 *"조치 불요"* 로 판정했다).
