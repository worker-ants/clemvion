# RESOLUTION — `17_15_29` (`/ai-review`, 타겟 3명)

CRITICAL 0 · WARNING 3 · INFO 3. **3건 전부 처리**했고, 리뷰어가 세 건의 **공통 원인**까지
짚어 그것도 등재했다.

## 공통 원인 (INFO 3) — 트래커는 **어느 라운드에도 검토 대상이 아니었다**

W2·W3 이 둘 다 *"정본 트래커에 반증된 근거가 취소선 없이 남아 있다"* 인데, 리뷰어가 왜
세 라운드를 돌고도 안 잡혔는지를 실측으로 설명했다:

> `review/consistency/2026/08/24/*/meta.json` 의 `target_path` 가 세 라운드 모두
> **draft plan 한 개**다. 자매 트래커는 **스코프 밖**이었다.

**게이트를 여러 번 도는 것으로는 못 메운다 — 스코프 밖은 몇 번을 돌아도 스코프 밖이다.**
`16_41_05` 가 draft 의 B3 근거를 CRITICAL 로 반증했는데 **같은 근거를 담은 트래커 줄은
그대로 남았고**, 결국 `/ai-review` 가 뒤늦게 잡았다.

**이건 이미 등재한 "후보 미도달" 과 다른 고장**이라 별 항목으로 등재했다 — 그쪽은
*"관련 문서를 번들에 못 싣는다"*, 이쪽은 *"target 자체가 한 개로 고정"* 이고 처방이 다르다.

## WARNING 2·3 (requirement) — 트래커 두 줄 정정

- **B3 줄**: *"동일 이름·다른 계층 … 판별자 금지와 무관"* → 취소선 + **`16_41_05` 이
  반증했다**는 경위와 **재작성된 논지**(엔진은 안 넣는다 · 실 DB 84행 0건 · allowlist 는
  예방적 허용 · 새 코드가 쓰는 건 여전히 C3 위반)를 실었다.
- **B1 줄**: *"라벨은 `NODE_OUTPUT_ALLOWED_KEYS` 주석과 같은 문구"* → *"EIA §R17 과 같은
  문구(코드 JSDoc 은 축약형이라 문자 그대로 같지는 않다)"*. **키 배열 자체는 정확히 일치**
  하므로 기능 위험 없음을 함께 적었다.

draft 는 이미 같은 관례로 고쳐져 있었는데 **트래커만 낡아 한 작업이 두 말**을 하고 있었다.
다음 planner 세션이 폐기된 논리로 B3 를 오독할 자리였다.

## WARNING 1 (documentation) — 3회차 체크박스

`17_04_25` 가 BLOCK: NO 로 끝났는데 체크박스가 `[ ]` 였다. `[x]` + 결과 요약(WARNING 4건도
같은 커밋에서 정정)으로 갱신. **"체크박스 = 실제 상태"** 를 이 세션에서 또 놓쳤다.

## INFO

- **#1** (중복 빈 줄) — drive-by 로 정리했다(1 → 0).
- **#2** (provider 3문서 각주 3벌 복제) — 리뷰어가 *"이 저장소의 provider-미러 컨벤션
  (cafe24/makeshop 선례)과 일치해 스코프 위반 아님"* 으로 판정했다. **동의한다.** 그 선례는
  이 저장소가 DRY 통합을 **의도적으로 철회**한 자리이고, provider 문서는 각자 독립 진화하는
  것이 설계다. B6(하나의 계약을 여러 문서가 재진술)과는 성격이 반대다 — 그 구분은
  `13_30_49` W2 처리 때 이미 세워 뒀다. 조치 불요.
- **#3** — 위 공통 원인. 등재 완료.
