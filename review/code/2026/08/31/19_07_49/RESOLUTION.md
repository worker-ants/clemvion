# RESOLUTION — `19_07_49` (3라운드, 종결)

- 결과: **Critical 0 · Warning 3 · INFO 11**, reviewer **8/8**(forced 7명 전원 확보)
- Warning 추이: 5 → 2 → **3**
- 조치 커밋: `fe9ba9752` (**`codebase/**` 무변경** — spec 인용 1곳 + plan 등재뿐)

Warning 이 2 → 3 으로 늘었지만 **성격은 수렴 중**이다. 이 저장소의 수렴 기준은 "발견 0" 이
아니라 발견의 성격(동작 → 구조 → 문서)이고, 3라운드 전체에서 **동작 결함은 0** 이다.
이번 3건은 프로세스 판정 1 · 문서 인용 1 · 등재 누락 1 로 전부 마지막 축이다.

## W1 (프로세스/scope) — 조치: 판정을 뒤집어 기록

3라운드 연속 INFO 였던 항목을 리뷰어가 `git log -S` 근거와 함께 격상했다. **재확인 결과
리뷰어가 맞다.**

| CLAUDE.md 소정정 조건 | 판정 | 근거 |
|---|---|---|
| (1) 대상 문장을 developer 자신이 썼는가 | **아니오** | §8.2 화이트리스트 문장 출처 = `9ed6e6305` (2026-05-21, `spec:` prefix, PR #228) |
| (2) 예고·트리거인가 | **아니오** | HMAC 알고리즘 화이트리스트 = 예외가 **명시 배제**하는 *"API 계약"* |
| (4) 원문을 취소선으로 남겼는가 | **아니오** | 전면 치환 |

따라서 `d743251b0`·`50caf1a85` 는 **자기-반증형 소정정이 아니다.** 정확한 성격은 **planner
트랙 작업** — 두 항목 모두 `owner: project-planner` plan 의 미체크 항목이었고, 사용자가 그
목록을 순서대로 처리하라 지시한 턴에서 수행됐다.

빠진 것은 "예외 조건" 이 아니라 **planner 게이트**다. 쓰기 직전 `--spec` 을 돌리지 못했고
(세션 시작 시 사용자에게 고지·승인), 남은 의무인 `--impl-done`(해당 spec 파일 포함 scope)은
이 RESOLUTION 다음 단계로 실행한다.

**취소선 미보존은 되돌리지 않았다.** §8.2 원문은 폐기된 *결정*이 아니라 **한 번도 참이
아니었던 상태 서술**(구현은 처음부터 `hmac-sha512` 를 지원했다)이다. 남기면 "한때 그랬다"
로 읽혀 이력이 오히려 왜곡된다. 선례: 같은 문서 §R8 `statusCode` 항목.

## W2 (문서정합) — 조치: `L979` 정정 + 스윕 절차 교체

`spec/5-system/6-websocket-protocol.md:979` 의 `§4.6` → `§4.7`. 주어가 **SSE 어댑터 /
EIA** 라 "외부 표면 매핑" 소관인데 "시스템 이벤트" 를 가리키고 있었다.

**네 번째 재발이다.** 매 라운드 내 스윕 정의가 한 칸씩 좁았다:

| 라운드 | 훑은 정의 | 놓친 것 |
|---|---|---|
| 1R | 마크다운 **앵커 링크**만 | bare 프로즈 인용 12곳 |
| 2R | + 프로즈, 주어를 **같은 줄**에서 | 주어가 인접 줄인 4곳 |
| 3R | + **블록 문맥(±5줄)** 키워드 | 키워드 목록 밖 1곳 (`Last-Event-Id`/`SSE`) |

**키워드를 넓히는 처방은 버렸다** — 다음 키워드를 미리 알 수 없으므로 같은 실패를 반복한다.
정본 절차로 교체: 그 파일의 `§4.x` 를 **전수 열거**해 헤딩 시퀀스와 1:1 대조.

**단 존재 검사만으로는 부족하다** — `L979` 의 `§4.6` 은 *존재하는* 헤딩이었고 **주제만**
틀렸다. 각 건에 *"이 인용의 주어가 그 절 제목과 맞는가"* 를 물어야 하고, 그건 자동화가
아니라 사람이 읽는 단계다. 전수 대조 결과 이 1건 외 **잔여 0**.

## W3 (문서화) — 조치: 미조치 2건을 plan 에 등재

직전 커밋이 INFO 2건을 미조치로 판단하고 **사유를 커밋 메시지에만** 남겼다. 이 저장소
관례상 `review/**`·커밋 메시지는 SoT 가 아니다. 둘 다 체크박스로
`plan/in-progress/spec-sync-external-interaction-api-gaps.md` 에 등재했다:

- `8-notifications.md:349` 의 죽은 §4.6 follow-up 참조 — 2026-05 기원 선재 결함이고
  *"번호 이동 오인용"* 과 **다른 클래스**(애초에 없는 대상)라 이번 스윕의 사각지대였다.
- `.claude/skills/consistency-checker/SKILL.md` 의 `--impl-done` 설명이 신설
  `_scope_delta_census` HEAD 블록을 언급하지 않는다.

## INFO 11건 — 신규 실행 항목 2건(위 W3 로 등재), 나머지 미조치

| 성격 | 건수 | 처분 |
|---|---:|---|
| 이번 PR 변경이 정상이라는 확인 | 4 | 조치 불요 (401 additive · HMAC spec 이 구현을 따라잡음 · plan 이 착수항목을 "결정 필요"로 **좁힌** 방향 · census 프롬프트 크기 변화는 의도) |
| 기 등재·기 결정 재확인 | 2 | 조치 불요 |
| 테스트 저우선 갭 | 2 | **미조치** — `diff_lines` 단언(표시값, 판정 로직 무관) · `_count_diff_files` rename fixture(docstring 동기 축). 다음에 이 파일을 만질 때. |
| 저장소 전역 리팩터 | 2 | **미조치** — `@Auth()` 합성 데코레이터 · import 정렬. 별 PR 스코프. |
| 문서 갭 | 1 | W3 으로 등재 |

INFO 5 는 리뷰어가 `diff_lines` 를 `999999` 로 뮤테이션해도 14/14 GREEN 임을 직접 확인해
줬다 — 유효한 지적이되, 판정 로직에 영향이 없는 표시값이라 이번 라운드를 더 열지 않는다.

## 검증

- harness `.claude/tests` **전수 1098 tests** OK (`test_*.py` — 2R 에서 이 범위를 좁게 잡은
  것이 W1 이었으므로 이후 항상 전수로 돈다)
- docs 가드 20파일 **3100 tests** OK
- backend 변경 모듈 49 suites **1101 tests** OK · prettier OK

## 다음 단계

`/consistency-check --impl-done` — W1 이 지목한 planner 게이트의 사후 그물. scope 는
`spec/5-system/`(§4 재배치 · §8.2 정정 두 파일을 포함).
