# RESOLUTION — `16_26_09` 라운드

forced 7명 전원. **Critical 0 · WARNING 7 전부 반영.**

## 1. 내가 흘린 지적이 실제 결함이었다 (requirement, 2라운드 연속)

`recoverFromExpiredToken` 의 `catch` 가 **모든 실패**를 종료로 확정했다. §R4 는 "재차
실패(`401`/`410`)면 종료" 이고, 순수 네트워크 오류는 거기 없다.

**이 변경 자신의 CHANGELOG 가 "그 외 오류는 여전히 soft-fail" 을 원칙으로 적어 뒀는데
refresh 왕복만 그 원칙 밖이었다.** `webchat-boot-single-flight` 이 정확히 그 형태로 살아있는
대화를 영구 유실시킨 사고가 있다.

`EiaError` 이면서 `401`/`410` 일 때만 종료. 회귀 1건 + **뮤테이션 RED**(조건을 `true` 로).

## 2. 내 검증 주장이 절반만 참이었다 (testing)

"세대 재검사 **2곳** 뮤테이션 RED" 라고 적었는데 실측하니 **성공 분기만** 갈린다.
`catch` 분기는 제거해도 초록이다.

재현을 시도했고 실패했다 — `newChat()` 으로 세대를 올린 뒤 늦은 실패를 착지시켜도 `ended`
로 갔다. **재현 실패를 "결함 없음" 으로 읽지 않는다**(인터리빙 지점이 가설의 일부다).
가드는 남기고 **미검증 사실을 그 자리 주석과 plan 양쪽에** 못박았다.

> 시도 중 `startNewConversation?.()` 을 썼는데 그런 액션이 없었다(실제 `newChat`).
> **옵셔널 체이닝이 조용히 no-op** 시켜 엉뚱한 이유로 실패했다 — 이 세션에서 경계한
> vacuous 형태 그대로다. 체이닝을 걷어 없으면 즉시 터지게 했다.

## 3. 내 보류 근거가 반증됐다 (maintainability)

"의존 넷을 주입해야 해 시그니처가 본문보다 길어진다" 로 보류했는데, reviewer 가 **형제
파일**을 들어 반박했다 — `use-token-refresh` 의 `scheduleRefresh` 는 `useCallback` 클로저로
refs 를 직접 캡처한다. 그러면 인자는 셋뿐이다.

**검토한 설계 대안 하나에만 근거한 보류**였다. 지적을 반영하는 것보다 거절하는 쪽이 더
촘촘한 근거를 요구하는데 반대로 했다. `recoverFromExpiredToken` 분리, 순수 이동을 뮤테이션
으로 확인.

## 4. 흘린 항목 명시 처분 (side_effect, 2라운드 연속)

refresh 동시 발화 경합. 첫 라운드 RESOLUTION 에 다른 두 항목은 근거와 함께 명시 보류했는데
이것만 빠졌다. plan 등재 + **왜 지금 안 고치는지**(세 조건 겹침 미재현, 처방이 설계 선택).

## 5~6. 문서 (documentation)

- **"몇 명 수렴" 이 세 곳에서 다 달랐다**(4/3/2). 실측값 4명으로 통일하되 **이름을 나열**했다 —
  숫자만 맞추면 다음에 또 어긋나고, 이름이면 누가 빠졌는지 보인다.
- JSDoc 이 **새 단락만 얹혀** 기존 서술 넷(요약·실패정책·`@returns`·타입 독스트링)과 배치됐다.
  전부 현행화하고, `@returns` 에 **"`continue` 는 아무것도 안 바뀌었다는 뜻이 아니다"**
  (401 복구 성공도 포함 → 호출부는 ref 를 읽어야 한다)를 박았다 — 그게 직전 CRITICAL 의 뿌리다.

## 7. plan 완료 조건 (scope)

완료 조건이 독립인 두 항목이 한 문서에 있다. 지금 가르지 않고 **조건 표**로 명시 — 하나가
닫혀도 `in-progress` 에 남는 것이 정상임을 적어, 나중에 "왜 안 닫히지" 를 다시 조사하지 않게 했다.

## 검증

- 위젯 **415 passed** · `tsc` **0 errors** · 문서 가드 **2876 passed**
- 뮤테이션 누적 9종
