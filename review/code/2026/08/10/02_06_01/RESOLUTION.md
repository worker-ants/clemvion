# RESOLUTION — 2026-08-10 02:06:01

SUMMARY: Critical 0 · **WARNING 1** · risk LOW (reviewer 7/7, forced 전원).

## W1 — 헤더 주석이 자기 PR 의 spec 갱신과 정면으로 어긋났다 (documentation)

**지적**: `plan-frontmatter.test.ts:19-20` 이 "그 규칙의 **단일 구현**은 `spec-links.ts` 의
`collectLivePlanMarkdown`" 이라고 하는데, `ebb6f9598`(plan 스캔·status 판정을 `plan-scan.ts`
로 추출) 이후 정본은 `plan-scan.ts` 이고 `spec-links.ts` 는 **하위호환 re-export** 다.
같은 PR 이 `spec-impl-evidence.md §4.2` 를 "판정 로직은 `plan-scan.ts` 소관" 으로 갱신했으니
**문서끼리 정면으로 어긋난 상태**였다.

**조치**: 정본을 `plan-scan.ts` 로 정정하고, `spec-links.ts` 가 re-export 라는 사실과
왜 그렇게 뒀는지(링크 모듈이 plan 트리 규칙을 갖고 있으면 규칙이 두 곳으로 갈린다)를 함께
적었다. 어긋나 있었다는 사실 자체도 각주로 남겼다.

> **이 PR 에서 같은 클래스의 다섯 번째다.** 내가 쓴 문서가 내 코드보다 넓거나 틀린 경우:
> (1) "액션 버전 핀 고정" 인데 존재만 확인 · (2) "같은 top-level 스코프" 인데 필터가 달랐음 ·
> (3) "8건 전부 이동형" 인데 9건 중 2건이 다른 범주 · (4) "가드가 9건 측정" 인데 그중 1건은
> 가드가 볼 수 없는 위치 · (5) 본 건. 전부 **리뷰나 뮤테이션이 잡았고 내가 먼저 못 봤다.**
>
> 공통 원인은 하나로 보인다 — **작성 시점에 참이던 서술을 이후 편집에 맞춰 갱신하지 않는
> 것**이다. 특히 (2)(5)는 같은 PR 안에서 내가 직접 만든 불일치다.

## INFO — 미조치 (사유)

| # | 내용 | 판단 |
|---|---|---|
| 1 | `rationale_continuity.md:31` 인용 위치가 heading 과 어긋남 | **시점 기록 문서**다(`plan-lifecycle.md §3`). 결론은 사실과 일치하고, 과거 라운드 산출물을 사후 편집하지 않는다 |
| 2 | 가드 설명 행이 3책임을 한 셀에 나열 | 책임이 하나 더 붙으면 분리 검토 — 트리거 조건부 |
| 3 | 리뷰 아티팩트 문장 밀도 | 일회성 산출물, 필수 아님 |
| 4 | §2.2 bullet 정보 밀도 | 선택 |
| 5 | `plan-scan.test.ts` 가 `code:` 에 미등재 | **기존 비대칭 패턴 연장** — `spec-links.ts`/`spec-links.test.ts` 도 같다. 이번 PR 이 만든 불일치가 아니고, 바꾸려면 `code:` 포함 기준(구현만 vs 구현+테스트)을 먼저 정해야 한다. 그건 planner 결정 |

## 검증

- 문서 가드 **19파일 / 2845 tests PASS**
- e2e — 아래 줄
- **후속 게이트 재실행**: 이 fix 가 spec-linked 파일을 건드리므로 코드 리뷰·정합 검토 둘 다
  신선도가 깨진다. 둘을 **동시에** 재실행한다(서로 파일을 건드리지 않으므로 직렬화 불요).
