# RESOLUTION — 2026-08-10 00:53:35 (5라운드, 수렴 확인)

SUMMARY: **Critical 0 · WARNING 1** · risk MEDIUM (reviewer 6/6, forced 전원).
6명 중 5명이 NONE/LOW 이고, 유일한 WARNING 은 코드 결함이 아니라 **재현 미확정 관찰**이다.

## W1 — flaky 관찰: 재현 실패했으나 기각하지 않는다

**지적**: `plan-scan.test.ts` 독립 실행 시 최초 2회 연속으로 4~5개가 FAIL 했고, 그 실패
집합이 `no-status.md`·`broken.md`·`status-empty.md`·`status-num.md`·`status-list.md` —
즉 **`typeof status !== "string"` skip 과 `catch { continue }` 가 없는 것처럼** 동작했다.
이후 8회 이상은 계속 PASS.

### 실측 — 12회 연속 실행

| 실행 | 결과 |
|---|---|
| 1–12 | **전부 PASS** (FAIL 0) |

재현되지 않았다. **그러나 "재현 실패는 부재의 증거가 아니다"** 는 것이 이 저장소의 규칙이라
기각하지 않고 기전을 따진다.

### 유력 기전 — 이 세션의 뮤테이션 하네스가 **그 파일을** 반복해서 변조했다

지적된 실패 형태는 "그 두 분기가 없는 것처럼" 이다. 그리고 나는 이 브랜치에서
`plan-scan.ts` 를 **정확히 그렇게 만드는** 뮤턴트를 반복 적용했다 — `run8.py` 6종 +
단독 non-string 뮤턴트 1종. 각 뮤턴트는 `write → vitest 실행 → 복원` 이라, 그 초 단위
창에서 파일을 읽은 프로세스는 **분기가 실제로 없는 코드**를 본다.

- `N1` 은 위반 수집을 통째로 죽이고, `N6`/단독 뮤턴트는 non-string 을 위반으로 밀어 올린다.
  후자가 만드는 실패 집합이 지적된 목록과 겹친다.
- 즉 이것은 **제품 코드의 flakiness 가 아니라 검증 하네스와 리뷰어의 동시 실행**일 가능성이
  높다. 코드에는 공유 가변 상태가 없다 — 각 `it` 이 `findNonTerminalCompletedPlans(root)` 를
  새로 부르고, fixture 트리는 `beforeAll` 에서 `mkdtemp` 로 격리되며, vitest 는 파일 내
  `it` 을 순차 실행한다.

### 그래서 무엇을 했나 — **코드 변경 없음**, 대신 관찰 기록

원인이 하네스 쪽일 가능성이 높고 12/12 로 재현되지 않으므로 추측에 기대 코드를 바꾸지
않는다. 대신 (a) 이 문단으로 관찰과 기전 가설을 남기고 (b) CI 로그에서 같은 형태가
재발하는지 지켜보기로 한다. **재발하면 이 문단이 첫 데이터 포인트다** — 그때는
"하네스 동시 실행" 가설이 반증된 것이므로 vitest 워커/트랜스폼 캐시 쪽을 봐야 한다.

> 이 판단을 뒤집을 관측: 뮤테이션을 돌리지 않는 CI 에서 같은 실패 집합이 나오면
> 가설이 틀린 것이다. 그 경우 **가설이 틀렸다는 사실 자체가 더 큰 단서**다.

## INFO — 미조치 (사유)

| # | 내용 | 판단 |
|---|---|---|
| 1 | `status: ""` fixture 부재 | 로직은 정확(위반으로 잡힘). 커버리지 완결성 항목 |
| 2 | `collectCompletePlanMarkdown` 빈 트리 직접 단언 부재 | 형제 둘은 단언됨 |
| 3 | `sort()` 순서 미관측 | **정렬은 계약이 아니다** — 호출부가 전부 `.sort()` 또는 `toContain` 으로 순서 비의존이다. 계약이 아닌 것을 테스트로 굳히면 리팩터만 막는다 |
| 4 | Gate C 가 공유 walker 미사용 | **이미 이관됨** — `harness-env-value-subpattern-dedup.md` (4R 이관분과 같은 자리) |
| 5 | `status: false` fixture | `status: no` 가 문자열로 파싱되는 것을 이미 고정했고, `false` 리터럴은 같은 비-문자열 분기라 중복 |
| 6 | `TERMINAL_STATUSES` 런타임 freeze | 타입만으로 충분. 다른 모듈로 더 퍼지면 재검토 |
| 7–12 | 확인성 INFO (재배선 무영향 · 임시 디렉터리 격리 · 중첩 깊이 · silent catch · 의존성) | 전부 "문제 없음" 판정 |

1·2·5 는 **한 줄짜리 fixture 추가**지만, 그걸 넣으려면 codebase 파일을 또 건드려 리뷰
라운드가 하나 더 돈다. 지금 남은 것이 커버리지 완결성뿐이고 Critical 0 · 코드 결함 0 이라
**여기서 수렴으로 읽는다** (이 저장소가 7라운드 stale 루프를 겪고 세운 기준: 수렴은 "발견 0"
이 아니라 **발견의 성격**으로 판단한다).

## 라운드 궤적 — 수렴 근거

| R | 발견 | 성격 |
|---|---|---|
| 1 | 링크 스캐너 공유 구현 미사용 | **동작** — 교체 즉시 stale 앵커 1건 발각 |
| 2 | 스캔 소스 이중화 · 픽스처 부재 | 구조 (링크 쪽) |
| 3 | 같은 둘이 status 쪽에 그대로 | 구조 (반쪽만 적용한 교훈) |
| 4 | 무관측 분기 1 + 인접 코드 리팩터 2 | 경계 — 1건만 조치, 2건 이관 |
| 5 | 재현 미확정 관찰 1 + 커버리지 완결성 INFO | **문서·관찰** |

동작 → 구조 → 경계 → 관찰. 코드 결함은 R1 에서 끝났다.

## 검증

- 문서 가드 **19파일 / 2841 tests PASS**
- `plan-scan.test.ts` **12/12 PASS** (flaky 재현 시도)
- 뮤테이션 누적 **13/13 RED** (그중 1건은 내 테스트가 vacuous 함을 자가 발각)
- e2e **PASS** (260s, tests=264) — 마지막 코드 commit `d1b622084` 다음
- harness **995 tests OK**
- **코드 변경 없음** — 본 RESOLUTION 은 문서만 추가한다
