# RESOLUTION — `13_21_24` 라운드

forced 7명 전원 리포트 확보. **Critical 0**, WARNING 3건(중복 지적 포함) 전부 같은 턴에
조치했다(커밋 `edebb1cc1`).

## 1. JSDoc 이 같은 블록 안에서 자기와 모순 (maintainability · documentation 독립 지적)

**지적**: `use-widget.ts:457` 이 "이중 스트림은 **호출부의 짝 가드**가 막는다" 를 유지한 채,
같은 JSDoc 블록 `:461-462`("**`openStream` 자신이** 진입에서 소유권을 재확인")·`:466-468`
("게이트를 `openStream` 안으로 옮겨 구조적으로 강제한다")과 충돌한다. `:463` 의 "그 짝 가드"
도 같은 잔재.

**판정: 유효.** 그리고 **직전 커밋 `bf8d71802` 의 제목이 "JSDoc·spec 의 옛 아키텍처 서술
정정"** 이다 — 그 정정에서 이 자리만 빠졌다. documentation reviewer 가 `git blame` 으로
2026-07-18(`2d9d202188`) 작성분임을 확인했다.

**조치**: `:457` → "`openStream` 진입 가드가 막는다", `:463` → "그 진입 가드로".

**왜 놓쳤나**: 한 파일 안에 같은 서술이 여러 군데 있는데 "고쳤다" 를 선언하는 시점에
자매를 **전수로 세지 않았다**. 이 티켓의 다섯 번째 주석 drift 이고, 형태가 매번 같다.

## 2. 워크스트림 A/B 혼재 (scope WARNING, MEDIUM)

**지적**: 이 브랜치가 (A) `openStream`/`StreamClaim` 리팩터와 (B) `3-auth-session.md`
frontmatter status 정합성 결함(한 달 전부터 존속) 정정을 한 changeset 에 묶었다. 신설 plan
자신이 "**그 PR 범위 밖**" 이라 적고도 "## 본 PR 에서 한 것" 절에서 그 정정을 수행했다고
적어 **문서가 자기모순**이다.

**판정: 유효 — 다만 틀린 것은 행위가 아니라 서술이다.**

분리가 더 나았을지 따져 봤다. 세 가지가 분리를 막는다:

1. CRITICAL 대상이 워크스트림 A 가 **편집 중인 바로 그 파일**(`3-auth-session.md`)이다 —
   분리하면 두 PR 이 같은 파일에서 충돌한다.
2. `--spec` 게이트는 Critical 을 차단한다. 남겨 두면 이 PR 이 **살아 있는 CRITICAL 위에서**
   머지된다.
3. 실제 변경은 frontmatter 한 줄 + Rationale 캐비엇이고, **세 REST 분기의 구현은 신설
   plan 으로 온전히 이연**됐다. reviewer 자신도 이 셋을 "완화 요인" 으로 인정했다.

**조치**: plan 의 provenance 문단을 위 세 이유로 다시 썼다. "범위 밖" 이라는 주장을 지우고
**"범위를 넓힌 게 아니라 게이트가 요구한 최소치"** 로 정정했다. reviewer 의 최소 조치 제안
(PR 이 두 묶음을 포함함을 명시)은 PR 본문에서도 이행한다.

## 3. 반영하지 않은 것 — 리뷰어의 호평이 틀렸다

maintainability 가 이번 변경의 이점으로 "union 덕에 세 번째 분기가 생겨도 **컴파일러가**
미처리 케이스를 잡아 준다" 를 적었다. **사실이 아니다.**

직전 라운드(`12_48_08`)에서 이미 프로브로 실측했다 — 호출부는 단순 문자열 비교라
exhaustiveness 검사가 아니고, 3번째 variant 를 추가해도 `tsc` 는 잡지 못한다. 실제로 지켜
주는 것은 **부정 비교(fail-closed)** 이며 그래서 `bf8d71802` 에서 두 호출부를 그 관용구로
통일했다.

**같은 주장을 내가 먼저 했다가 리뷰에 반증당했고**(12_48_08 maintainability INFO), 이번엔
리뷰어가 그 반증된 주장을 호평으로 되풀이했다. 칭찬이라도 근거가 틀렸으면 채택하지 않는다 —
받아들이면 다음 사람이 없는 보호막을 믿는다.

## 검증 (조치 후)

- `pnpm --filter channel-web-chat test` — 23 files / **409 passed**
- `pnpm --filter channel-web-chat exec tsc --noEmit` — **0 errors**
- 문서 가드 19파일 **2878 passed**
- 이번 조치는 주석·문서 텍스트 한정이라 동작 무변경 (뮤테이션 재실행 대상 없음).
