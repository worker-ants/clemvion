# RESOLUTION — `14_32_02` 라운드

forced 7명 전원 리포트 확보. **Critical 1 · WARNING 5** — 전부 처분(`2b71fd9f7`).

## 1. `08_32_48` 의 대상 3개가 도달 불가 커밋 산출물 (scope CRITICAL)

**지적**: 회수한 `review/code/2026/08/10/08_32_48` 의 대상 9개 중 6개는 #1125 squash 안에
있으나, `plan-link-integrity.test.ts`·`spec-links.ts`·`spec-plan-completion.test.ts` 3개는
`origin/main` 어디서도 도달 불가한 커밋 `62084e807` 의 산출물이다.

**판정: 사실은 맞다.** 액면가로 받지 않고 실측했다:

| 검사 | 결과 |
|---|---|
| `git merge-base --is-ancestor 62084e807 origin/main` | **NOT reachable** |
| `plan-link-integrity.test.ts` on `origin/main` | **부재** |
| `spec-links.ts` on `origin/main` | 존재 |

`62084e807` 은 이 워크트리에 남아 있던 로컬 커밋이고, 그 plan-link 가드는 **#1123 으로
대체되며 폐기**됐다(그 파일이 main 에 없다).

**처분: 산출물은 유지하고 서술을 정정한다.** `12_48_08` 과 성격이 다르다 —

| | `12_48_08` | `08_32_48` |
|---|---|---|
| 귀속 | **열린 PR #1130** | 폐기된 로컬 커밋 + #1125 |
| 담을 다른 PR | **있다** (그쪽이 커밋했어야 함) | **없다** |
| 남겨 두면 | 두 브랜치에 중복 각인 → 오염 | 갈 곳 없는 기록이 소실 |

전자는 옮기는 것이 정답이었고(옮겼다), 후자는 지우면 **실재했던 리뷰 라운드의 기록이
사라진다.** 그 라운드는 실제로 돌았고 그 결과가 #1125 의 일부를 낳았다.

**틀린 것은 산출물의 위치가 아니라 내 커밋 메시지다** — `b35bd23ca` 가 "전부 이미 머지된 PR
의 라운드 산출물" 이라 단정했는데, `08_32_48` 은 **부분만** 머지됐다(6/9). 그 문장을
본 RESOLUTION 이 정정한다.

> **같은 실수가 두 검증 단계를 "등" 으로 축약된 채 통과했다.** 직전 라운드 scope 도, 내
> RESOLUTION 도 `08_32_48` 을 "#1125 것" 으로 한 줄 처리했다. 9개 중 6개만 확인하고
> 나머지를 "등" 으로 덮은 것이다 — **부분 확인을 전체 확인으로 적는** 형태이고, 이 브랜치
> 에서만 세 번째다.

## 2. 삭제-전용 커밋 가드에 결정적 재현이 없었다 (testing WARNING)

**지적**: `test_line_anchors.py` 는 "가드 하나당 purpose-built 저장소 하나" 관례를 스스로
세워 뒀는데(merge 케이스) 이번 가드만 예외다. 내 검증은 1회성 관찰이라, 그 커밋이
`FIXTURE_SEARCH_DEPTH` 창 밖으로 밀려나면 회귀가 조용해진다.

**판정: 유효.** 그 클래스 docstring 이 "아무도 초록으로 유지할 수 없는 가드는 가드가
아니다" 라고 적어 뒀고, 내 것은 반대로 **"어쩌다 초록"** 이 될 가드였다.

**조치**: `_make_deletion_only_repo` + 3 테스트. 비-vacuity 확인이 "HEAD 가 임계값을 넘으면서
동시에 내용이 없는가" 를 단언한다 — 그게 없으면 조용히 파일을 남긴 픽스처가 엉뚱한 이유로
통과한다. 뮤테이션 RED 2건.

## 3~5. 닫은 결함을 아직 "열려 있다" 고 적는 자리 넷

| 위치 | 내용 | 조치 |
|---|---|---|
| `lib/session.py` ×2 | 배치 분할을 현재 일어나는 일로 서술 | 기전은 "이제 없다" 로 정정하되 **측정 기록은 보존** — 충돌은 배치가 아니라 이름의 성질이고 병렬 세션에서 여전히 발생한다 |
| `pick_commit_fixture` docstring | 실패 2종만 기술 | 세 번째 변종 등재 |
| `tests/README.md` | 같은 행 | 동반 갱신 |
| plan 상위 체크박스 | 하위 3건 완료인데 미해결 | 종결 + 원문 `[~]` 보존 |
| plan 테스트 개수 | "17건" (실제 19) | **내역**(10+7+2)까지 적어 다음 드리프트를 보이게 함. 뮤테이션 5→7 |

## 검증

- harness **1054 tests / OK**
- 문서 가드 19파일 **2872 passed**
- 뮤테이션 누적 7종 RED
