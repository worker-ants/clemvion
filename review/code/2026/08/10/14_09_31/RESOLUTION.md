# RESOLUTION — `14_09_31` 라운드

forced 7명 전원 리포트 확보. **Critical 1 + WARNING 4 전부 조치** (`e4ce8adf8`, `ffb2cfbe5`,
그리고 `bb2bca24e` — 마지막은 다른 브랜치).

## 1. 미머지 PR 의 리뷰 기록을 여기 심었다 (scope CRITICAL, HIGH)

**지적**: 커밋 `b35bd23ca` 가 회수 산출물을 "전부 이미 머지된 PR(#1125~#1129) 것" 이라
단정했는데, `12_48_08` 은 **열려 있는 #1130** 의 라운드다.

**판정: 유효.** 액면가로 받지 않고 각 세션 `meta.json` 을 직접 읽어 대조했다:

| 세션 | 대상 | 귀속 |
|---|---|---|
| `08_32_48` | `git_probe.py`·`session.py`·`consistency_orchestrator.py` | #1125 (머지) ✅ |
| `10_54_59`·`11_08_01`·`11_15_05`·`11_44_32` | `repo-guards/__tests__/**` | #1126 (머지) ✅ |
| **`12_48_08`** | **`use-widget.ts`·`use-widget-eager-start.test.ts`** | **#1130 (열림)** ❌ |
| `10_35_05`·`10_36_44` | `spec/conventions/spec-impl-evidence.md` | #1123 (머지) ✅ |
| `12_06_35` | `spec/7-channel-web-chat/2-sdk.md` | #1128 (머지) ✅ |

**조치**: `12_48_08` 을 이 브랜치에서 제거(`e4ce8adf8`)하고 #1130 브랜치에 커밋·push
(`bb2bca24e`). 그 PR 이 애초에 그 라운드만 커밋에서 빠뜨린 것이었고, 그 라운드가 낳은
후속 커밋들의 근거가 사라진 상태였다.

**reviewer 지적의 절반은 틀렸다**: `10_35_05`·`10_36_44` 도 같은 오귀속으로 지목됐으나
#1123 것이다. 내 오류는 "머지됐다" 가 아니라 **PR 번호 범위**였고, reviewer 가 그 좁은
범위를 근거로 판정해 정상 회수분까지 끌어들였다.

**교훈**: "전부 머지됐다" 는 검증 가능한 주장인데 **세지 않고 썼다.** 세었으면 하나가 열린
PR 것임을 그 자리에서 알았을 것이다 — 그 PR 을 30분 전에 내가 직접 올렸는데도.

## 2. 호출부가 관측되지 않았다 (testing WARNING)

**지적**: `_warn_large_changeset` 을 겨냥하는 테스트는 `main()` 을 거치지 않고, `main()` 을
구동하는 테스트는 stderr 를 통째로 버려 그 호출을 못 본다.

**판정: 유효.** 헬퍼가 맞는 것과 그게 불리는 것은 다르고, 이 저장소는 정확히 그 형태로
세 번 데였다(P1·V5·T6 뮤턴트가 호출부 테스트 이전까지 전부 생존).

**조치**: stderr 포획 + call-site 단언 2건. **뮤테이션 RED** — 헬퍼를 그대로 두고 호출부만
지워도 잡힌다.

## 3~4. 중복 (maintainability WARNING ×2)

- 테스트가 `ALL_AGENTS` 14개를 손 나열 → `orch.ALL_AGENTS` 로 교체. reviewer 가 추가되면
  테스트가 자동으로 따라간다.
- "20개까지 나열 + 나머지 개수" 블록이 매직넘버째로 복제 → `_bulleted_path_sample` 공유.
  router 가 한 프롬프트에서 서로 다른 형식의 두 목록을 읽으면 **그 차이가 의미인지 추측**하게
  되므로, 형식 통일이 미관 문제가 아니다.

## 5. 닫은 결함을 두 문서가 아직 "열려 있다" 고 서술 (documentation WARNING)

`tests/README.md` 는 **인접 두 행이 서로 모순**했다(위 줄: 고쳤다 / 아래 줄: 아직 open item).
skill `README.md` 는 이제 발생하지 않는 로그 이벤트("batch 분할")를 나열했다.

**둘 다 이번 diff 가 건드린 파일**이다 — 계약 드리프트를 고치면서 같은 클래스를 재생산했다.

## 6. 조치 중 노출된 기존 결함 (내 커밋이 드러냈다)

`test_line_anchors` RED. **orchestrator 변경 탓이 아니다** — `pick_commit_fixture` 가 내
삭제-전용 커밋을 골랐고, 소비자가 `git show <sha>:<path>` 로 원본을 얻으므로 삭제된 경로는
전부 빈 문자열이라 대조가 0건이 됐다.

그 docstring 이 이미 같은 클래스 실패 2종(문서-전용·merge 커밋)을 기록하고 있어 세 번째
변종을 같은 자리에 닫았다. 검증: 수정 후 픽커가 `e4ce8adf8` 을 건너뛰고 `b35bd23ca` 를
고른다(실측).

## 검증 (조치 후)

- harness **1051 tests / OK**
- 뮤테이션 6종 RED (SUMMARY 참조)
