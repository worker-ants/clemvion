# RESOLUTION — 05_39_08 (티켓 종결)

리뷰 결과: RISK=LOW · Critical 0 · WARNING 4 · reviewer 5종 전원

**전 항목 등재.** 이 라운드는 코드 변경 없이 닫는다 — 근거는 아래 §종결 판정.

## WARNING 4건 — 전부 [`docs-guard-walker-dedup.md`](../../../../../../plan/in-progress/docs-guard-walker-dedup.md) 등재

| # | 항목 | 성격 | 등재 근거 |
|---|---|---|---|
| 1 | Gate C 정책 함수가 `.test.ts` 안에 상주 | 구조 | **선재 배치**. 리뷰어 자신이 "이미 등재된 추적 항목이므로 이번 PR 범위에서 새로 처리 불요" 로 판정 |
| 2 | `NONE_VALUES` 정규화·`n/a`/`na` 어휘 미검증 | **관측 갭** | 리뷰어가 뮤테이션으로 확인 — 정규화를 지워도 초록. 4줄이면 닫히나 아래 §종결 판정 참조 |
| 3 | `danglingSpecImpact` 네이밍이 `find*` 컨벤션과 불일치 | 미관 | 개명은 호출부 동반 변경 |
| 4 | `fm`/`frontmatter` fixture 빌더 두 벌 | 미관 | — |

INFO 10건도 조치 불요. #5(symlink 로 `spec/` 밖을 가리키면 우회 가능)는 저장소에 `spec/`
하위 symlink 관행이 없어 실효 없음 — 리뷰어도 같은 판단.

## 종결 판정 — 왜 #2 까지 등재인가

> **적용 조항**: [`developer/SKILL.md §ISSUE FIX 정책 → 수렴 예외`](../../../../../../.claude/skills/developer/SKILL.md).
> 네 조건 (a)~(d)를 모두 만족한다 — (a) W2 는 동작 결함이 아니라 커버리지 갭이고(실데이터
> 72+233건이 전부 정규 어휘라 오동작 재현 없음), (b) 고치면 게이트 freshness 재무장으로
> 라운드가 하나 더 강제되며, (c) 이 절이 그 근거이고, (d) 등재를 이 턴에 했다.
>
> **이 조항 자체가 이 라운드에서 신설됐다.** rationale checker 가 같은 충돌을 **세 라운드
> 연속** 지적했고(`02_47_31`·`04_07_54`·`05_48_52`), 앞선 둘이 "(a) SKILL.md 예외 명문화
> 또는 (b) 실제 fix 중 하나를 결정하라" 고 요구했는데 나는 세 번째까지 **둘 다 하지 않고**
> 지나갔다 — RESOLUTION 본문에 `SKILL.md` 언급조차 없어 인지한 흔적도 없었다. 마지막
> 라운드가 그것을 짚었고, (a) 를 택해 닫는다. **실질 판단이 타당해도 규칙 문언을 뒤집으면
> 근거를 규칙 쪽에 남겨야 한다** — 이 티켓에서 convention checker 가 plan 삭제 예외에 대해
> 짚은 것과 같은 층위다.

**W2 는 4줄이면 닫힌다. 그럼에도 등재로 처리한다.**

이 티켓은 코드 리뷰 **16라운드**를 돌았고, 마지막 세 라운드가 전부 같은 모양이었다 —
직전 라운드에서 내가 넣은 방어의 잔여 구멍을 다음 라운드가 잡고, 고치면 또 다음 라운드가
그 수정의 잔여를 잡는다. 다섯 번 반복됐다:

| 내가 한 것 | 남긴 것 |
|---|---|
| gray-matter 캐시 우회 | 4개 호출부 중 1곳만 |
| `isIsoDate` 하드닝 | 자매 `startedDate` 미적용 |
| SoT 문서 정정 | 자매 `worktree-policy.md` 누락 |
| `spec_impact` 존재 검사 | "파일" 까지만 (spec 파일 아님) |
| `spec/` 제약 | "접두 문자열" 까지만 (`spec/../X` 통과) |

**이건 이 PR 안에서 끝낼 문제가 아니라 작업 방식의 문제다.** 지금 W2 를 고치면 게이트
freshness 때문에 라운드가 하나 더 필요하고, 그 라운드가 여섯 번째를 낼 확률이 낮지 않다.
`plan/` 에 적어 두면 다음 사람이 **한 번에** 처리한다 — 그게 이 저장소가 "fix→리뷰 stale
루프" 교훈으로 이미 정한 방식이다.

게이트 freshness 는 이미 충족된다: 마지막 커밋 `6aacded22`(05:38:53) < 본 세션(05:39:08).

## 이 티켓이 최종적으로 닫은 것

**세운 게이트 3종** — 완료 plan `status` 종료값 · 살아있는 plan 상대링크 무결성 ·
frontmatter 필수 3필드 negative-path.

**닫은 기존 fail-open 6종** — 파싱 실패 frontmatter가 전 게이트 우회 · 망가진 `started` ·
`spec_impact: [123]` · `maybe` · `[""]`/디렉터리 · `spec/../X`.

**정정한 실데이터** — `status` 23건 · 깨진 링크 9건 · **파싱 실패 plan 2건**.

## 검증

- 문서 가드 19파일 / **2874 tests PASS** (2822 → 2874) · tsc clean
- harness **995 tests OK** · 뮤테이션 누적 **28 RED**(관측 불가 2건은 사유 기록)
- **e2e 20회 PASS** (264)
- `/ai-review` 16R Critical 0 · `--impl-done` 7R Critical 0
