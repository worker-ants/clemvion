# RESOLUTION — `13_29_35` 라운드

forced 7명 전원 리포트 확보. **Critical 0**, WARNING 2건 전부 같은 턴에 조치했다
(커밋 `13f2accb4`). 둘 다 **문서 텍스트 전용**이라 동작 무변경이다.

## 1. `3-auth-session.md:180` — "짝 가드" stale 용어 (requirement WARNING)

**지적**: 같은 문서 `:166`·`:172` 는 현행 아키텍처("스트림 열기 진입에서 재확인")로
정정됐는데 `:180` 의 "**짝 가드**의 필요성은…" 만 남아 문서 안에서 자기모순.

**판정: 유효.** `git blame` 상 `c9e0e2ff7b`(리팩터 이전) 작성분이고, 이후 **정정을 표방한
커밋 두 개(`bf8d71802` "JSDoc·spec 의 옛 아키텍처 서술 정정" · `edebb1cc1`)가 모두 놓쳤다.**

**조치**: "스트림 열기 재확인의 필요성은…" 으로 정정.

## 2. `webchat-usewidget-extraction.md:69` — fail-open 예시 (documentation WARNING)

**지적**: plan 이 호출부 형태를 `if (openStream(...) === "already_owned") return;` **긍정
비교**로 제시한다. 코드는 `bf8d71802` 에서 부정 비교(fail-closed)로 고쳤으나 그 커밋이
plan 문서를 대상에서 뺐다. **바로 옆 소스 주석(`use-widget.ts:620`)이 정확히 "그렇게 쓰지
말라" 고 경계하는 자리다.**

**판정: 유효.** 그리고 이건 단순 stale 이 아니라 **틀린 것을 정답으로 보여 주는** 형태라
더 나쁘다 — 다음 사람이 이 plan 을 참조해 3번째 호출부를 만들면 fail-open 을 재도입한다.

**조치**: 부정 비교로 교체하고, 왜 긍정 비교가 틀렸는지(형제 `SeedOutcome` 의 관용구와
반대 방향)를 인용구로 고정했다. 직전 라운드 testing 이 INFO 로 지목했으나 미채택으로
남아 있던 항목이기도 하다 — **INFO 를 흘린 것이 다음 라운드 WARNING 이 됐다.**

## 3. 내가 지시한 "전수" 의 범위가 틀렸다

직전 라운드를 닫으며 두 reviewer 에게 "같은 클래스의 잔재를 **전수로** 확인하라" 를
명시했다. 둘 다 성실히 수행했고 `use-widget.ts` 는 실제로 깨끗했다 — maintainability 가
grep 전수 스캔으로 확인해 줬다.

**문제는 내가 정한 집합이 소스 파일이었다는 것이다.** 남은 두 자리는 spec Rationale 과
plan 문서에 있었다. 지시는 옳았고 수행도 옳았는데 **범위가 틀려서 통과했다.**

이번엔 파일이 아니라 **저장소 전체 × 용어**로 셌다:

| 검사 | 결과 |
|---|---|
| `grep -rn "짝 가드" spec/ plan/ codebase/channel-web-chat/` | **0건** |
| `grep -rn '=== "already_owned"' spec/ plan/ codebase/` | 3건 — **전부 정당** |

정당한 3건: (a) 그 관용구가 왜 틀렸는지 설명하는 새 인용구, (b) 소스의 경고 주석,
(c) `review/**` 감사 기록. (c) 는 **고치면 안 된다** — 과거 라운드가 그때 무엇을 적었는지가
사실이고, 나중 RESOLUTION 이 그것을 정정한 이력 자체가 audit trail 이다.

## 검증 (조치 후)

- 문서 가드 19파일 **2878 passed**
- 이번 조치는 `.md` 텍스트 전용 — 소스·테스트 무변경이라 위젯 테스트·`tsc` 재실행 대상 없음
  (직전 라운드에서 409 passed / 0 errors 확인).
