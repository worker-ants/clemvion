# RESOLUTION — `15_23_40` 라운드

forced 7명 전원 리포트 확보. **Critical 0 · WARNING 5 전부 반영.**

## 1. 5곳 중 4곳이 무가드 (requirement · maintainability 수렴)

**지적**: `--strict-peer-dependencies` 가 5곳에 들어갔지만 `action.yml` 한 곳만 실행 검증
테스트로 고정돼 있다. 나머지 4곳은 누가 플래그를 지워도 어떤 테스트도 못 잡는다 —
이 티켓이 막으려는 "조용한 통과" 와 **같은 클래스**다.

**판정: 유효.** 그리고 이 갭은 가설이 아니라 **직전 CRITICAL 이 난 자리**다. action 을
고쳤을 때는 `InstallCommandTest` 가 즉시 RED 를 냈고, 나머지 4곳은 손도 안 댔는데 아무
신호가 없었다.

**조치**: `test_install_gate_flags.py` 신설. 런타임 추출은 **채택하지 않았다** — 매체가 셋
(composite action / bash / Dockerfile)이라 실행 시점도 격리 경계도 다르다. 정적 대조로
"다섯 곳이 일치한다" 만 고정했다.

테스트를 둘로 나눈 이유: 하나는 **아는 곳**이 플래그를 달았는지, 다른 하나는 **모르는 곳이
생겼는지**. 앞의 것만 있으면 목록 밖에 새 install 지점이 자라고, 그게 이번 결함의 형태다.

**가드가 자기 사각지대를 즉시 드러냈다**: 첫 판은 경로 필터만 써서 주석 속 인용까지 실행
지점으로 셌다(4건 오탐). 실행 줄 판정으로 바꿨고, 남은 `.py` 2건은 **확인 가능한 근거**로
제외했다 — 파이썬 가드는 그 명령을 문자열로 인용할 뿐이고, 실제로 돌린다면
`subprocess.run(["pnpm","install",…])` 라 이 연속 문자열과 애초에 매치되지 않는다.

**뮤테이션 3/3 RED**: Dockerfile 플래그 제거(이전엔 아무도 못 잡던 자리) · 등재 목록에서
한 곳 삭제(미등재 탐지 + 집합 불일치 2건) · 주석 필터 vacuity 전제 고정.

## 2. "저장소에서 유일한 소재지" 가 세 파일에서 살아남았다 (documentation ×3)

**지적**: 게이트를 5곳으로 넓히면서 옛 프레이밍을 그대로 뒀다 —

- `action.yml`: 같은 주석 블록 안에서 자기모순. **내가 정정문을 뒤에 덧붙이면서 앞의 단정은
  오히려 강화했다**(두 플래그 모두에 대한 서술이라고).
- 새 docstring: 그 단어만 가져와 caveat 없이 더 절대적으로 읽힌다. **형제 파일이 스스로
  부정하는 주장을 다른 파일이 무비판적으로 반복.**
- `tests/README.md`: 직전 RESOLUTION 이 "동반 갱신" 이라 적었으나 실제로는 뒤에 문장을
  덧붙였을 뿐 "the only copy in the repository" 는 손대지 않았다.

**판정: 유효, 그리고 이건 문구 위생이 아니라 결함의 뿌리다.** "유일한 소재지" 를 믿었기
때문에 한 곳만 고치고 전부 덮었다고 적었다.

**조치**: 범위를 **"CI 워크플로가 공유하는 한 줄"** 로 좁히고, 세 곳 모두에 **그렇게 읽는
것이 이번 사고를 낳았다**를 명시했다. 좁힌 주장은 참이고 여전히 유용하다 — 거기가 깨지면
required check 후보 전부가 한 번에 깨진다.

전수 확인: 정정 후 남은 것은 "유일한 소재지가 **아니다**" 라는 부정문 2건뿐.

## 3. 기존 가드 둘이 나를 또 잡았다

- `TempRepoFixturesGoThroughTheSharedHelperTest` — 실 저장소에 git 을 부르는 새 파일은 그
  **이유를 레지스트리에 등재**해야 한다. 등재했다.
- `CatalogCoverageTest` — `tests/README.md` 행 추가.

둘 다 이 세션에서 이미 한 번씩 데인 형태라, 가드가 있는 쪽은 반복 비용이 0 이다.

## 검증

- harness **1036 tests / OK**
- side_effect 가 Docker COPY 패턴을 repo 밖 scratch 에 재현해 실행 — exit 0
- requirement 가 저장소 전수 재검색으로 호출부 5곳 확정(6번째 없음)
