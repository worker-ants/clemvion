# 변경 범위(Scope) 리뷰

## 발견사항

- **[INFO]** 계획서에 등재된 항목은 "한 문장" 이었으나 실제로는 4줄 문단이 추가됐다
  - 위치: `codebase/backend/src/modules/secret-store/secret-resolver.service.ts:95-99`
  - 상세: `plan/in-progress/deps-peer-gating-and-eslint10.md` 의 해당 백로그 항목(게이트 452-457행,
    "`secret-resolver.service.ts` 의 비부착 주석에서 … 한 문장")은 "한 문장" 추가를 예고했는데,
    실제 diff 는 "아래 '서버 로그에만 남는 것도 아니다' 는 **C1 판정의 보조 근거**일 뿐 판정축이
    아니다 — §6.3.1 은 '소비처가 직렬화하는가' 를 기준으로 삼는 안을 **명시적으로 기각**했다…"
    로 시작하는 4줄 문단이다. 내용은 예고된 취지(보조 근거 vs 판정축 구분)와 정확히 같은 주제이고
    새로운 논지나 무관한 서술을 끌어들이지 않았다 — 근거를 한 문장에서 한 단락으로 넓힌 수준의
    사소한 확장이라 실질적 스코프 이탈로 보기는 어렵다.
  - 제안: 조치 불요. 다음에 "N줄/N문장" 식으로 계획서에 예고할 때는 실제 커밋 산출물과의 오차를
    감안해 "간단한 보강" 정도로 느슨하게 적는 편이 계획-실행 괴리를 줄인다.

## 점검한 나머지 관점 (이상 없음)

- **의도 이상의 변경 / 무관한 수정**: 이 브랜치의 전체 diff(`git diff origin/main...HEAD --stat`)
  는 정확히 프롬프트에 제시된 4개 파일뿐이다. 두 커밋(`3e2360db0` 코드/테스트, `58806c380`
  plan-only)으로 깔끔히 분리돼 있고, plan 이 등재한 "C2 를 단언으로 잠그기" + "secret-resolver
  주석 한 문장" 두 항목 외에 다른 실장·구조 변경이 없다.
- **불필요한 리팩토링**: 없음. `expression-resolver.service.spec.ts`(게이트 164-200) ·
  `code.handler.spec.ts`(게이트 237-261) 는 기존 코드를 건드리지 않고 새 `it(...)` 블록만
  순수 삽입했다. `secret-resolver.service.ts` 는 주석 5줄만 삽입, 로직 변경 0.
  실제 구현부(`expression-resolver.service.ts`, `code.handler.ts`)는 이번 diff 에 아예
  포함되지 않았다 — 이전 라운드(`#1230`)에서 이미 편집된 파일이라 이번 PR 범위 밖이다.
- **기능 확장**: 없음. 새 캐너리 테스트 2건은 §6.3.1 C2 를 "주석 대신 단언"으로 잠그는 것이
  plan 에 명시적으로 등재된 목표이고, 화이트리스트·모양(shape) 단언까지 plan 본문(게이트
  423-429행)이 그대로 예고한 범위다.
- **포맷팅 변경**: diff 3건(스펙 2 + 서비스 1)이 전부 순수 추가(context 줄 재배치·개행 변경
  없음)이며, plan.md 의 삭제 2줄은 `[ ]` → `[x]` 체크박스 전환에 대응하는 정상적인 편집이다.
- **주석 변경**: 추가된 주석은 전부 이번에 신설한 테스트/코드 바로 위에 붙는 신규 설명이고,
  기존 주석을 삭제·수정한 곳은 없다(plan.md 의 취소선 처리 제외 — 이는 규약(§자기 정정)이
  요구하는 "원문을 취소선으로 남기고 정정" 형식).
- **임포트 변경**: 4개 파일 어디에도 import 추가/삭제 없음.
- **설정 변경**: 설정 파일(`eslint.config.mjs`, `package.json`, workflow yml 등) 변경 없음.
  plan.md 는 추적 문서이지 설정이 아니다.

## 요약

이번 diff 는 `plan/in-progress/deps-peer-gating-and-eslint10.md` 가 스스로 등재해 둔 두 개의
좁은 후속 항목(§6.3.1 C2 를 캐너리 단언으로 잠그기, `secret-resolver.service.ts` 판정 근거
보강 주석)만을 정확히 구현한다. 실장 코드 변경은 0건이고, 테스트 2건 삽입 + 주석 삽입 +
plan 체크박스/뮤테이션 기록 갱신으로 diff 가 구성돼 완전히 순수 추가(additive)다. 유일하게
지적할 만한 것은 "한 문장" 계획 대비 실제로는 짧은 문단이 붙었다는 점인데 같은 주제 내의
사소한 분량 확장이라 위험도에 영향을 주지 않는다.

## 위험도

NONE
