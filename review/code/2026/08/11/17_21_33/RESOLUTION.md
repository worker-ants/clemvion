# RESOLUTION — `17_21_33` (+ consistency `17_21_43`)

CRITICAL 0 / WARNING 6 **전부 처분** (커밋 `165960a92`).

## 이 PR 이 배운 것 — 수치는 술어 없이는 참이 아니다

나는 §2 를 끝내고 **"재스캔 잔여 0건"** 이라 썼다. 참이다 — **티켓이 쓴 술어로는.**
규약(`§5-4`)의 술어로 세면 **13건이 남아 있었다.**

같은 문장이 술어에 따라 참이 되기도 거짓이 되기도 한다. 그리고 나는 **더 좁은 쪽**을 골라
"완료" 라고 적었다. 이 저장소가 이미 등재한 "프록시로 실측했다가 틀린다" 의 변종이다 —
이번엔 프록시가 문자열이 아니라 **술어**였다.

흥미로운 것은 리뷰어들도 서로 달랐다는 점이다: security 6 · api_contract 3 · convention 12 ·
내 실측 13. **"남았다" 는 수렴했지만 "얼마나" 는 아무도 같지 않았다.** 수렴은 존재의 증거이지
수량의 증거가 아니다.

## 두 번째 — 검증 자체에서 자매를 묶어 뮤테이트했다

"가짜 앵커 → RED 확인" 은 **두 앵커를 동시에** 바꾼 결과였다. 하나만 잡혀도 RED 가 나온다.
격리해 보니 `swagger.md:350` 은 **멀티라인 링크**라 가드가 원천적으로 못 본다:

| 뮤테이션 | 결과 |
|---|---|
| 350 단독 (멀티라인 상태) | **GREEN — 생존** |
| 350 단독 (한 줄로 편 뒤) | **RED** |
| 398 단독 | **RED** |

링크를 한 줄로 펴서 둘 다 독립 RED 를 확인했다. **주장을 고친 게 아니라 주장이 참이 되도록
코드를 고쳤다** — 그게 옳은 방향이다.

그리고 이건 내 실수인 동시에 **가드의 결함**이다. 저장소 전수로 재니 멀티라인 링크가
**6건/6파일** 있고, 그 전부가 링크·앵커 검증에서 조용히 빠진다. 별도 후속으로 등재했다 —
`review/` 는 SoT 가 아니므로 plan 에 적었다.

## 세 번째 — codemod 가 코드를 깼고, diff 를 읽어서 잡았다

시그니처 정규식이 `@UseInterceptors(\n  FileInterceptor('file', {` 를 메서드로 오인했다.
`tsc` 도 잡았을 결함이지만 **diff 를 눈으로 읽은 것이 더 빨랐다.** 괄호 깊이 0 조건으로 수정.

## 리뷰어들이 나에게 유리한 답을 주지 않았다

- **maintainability** 는 "51곳 복제 대신 공용 데코레이터로 추출하라" 는 유혹적인 지적을
  **기각했다** — 기존 인라인 63+156건, `applyDecorators` 사용처 1곳이라는 실측을 근거로.
  다만 `workspaces.controller.ts` 의 `'해당 워크스페이스 멤버가 아님'` drift 1건을 함께 찾아
  "언젠가 래퍼가 필요한 이유" 도 남겼다.
- **api_contract** 는 `RolesGuard` 소스를 읽어 51건이 **진짜 403 을 낸다**는 것부터 확인했다.
  이게 틀렸다면 이 PR 전체가 "안 나는 에러를 문서화" 였다.
- **rationale** 은 EIA `R14`(403 미사용)와 충돌한다는 가설을 세우고 **실측으로 반증**했다.

## 검증

컨트롤러 타입 오류 **0** · 변경 파일 lint **0** · 문서 가드 **2890 passed** ·
§5-4 술어 기준 잔여 **0건** · 앵커 2곳 **각각 독립 RED**.

## 수렴

발견의 성격이 "대상 집합 정의" → "검증 방법의 타당성" → "문서 표기" 로 얕아졌다. 남은
3건은 전부 이 티켓 범위 밖으로 명시 등재했고, 그중 가드 사각지대는 **측정치와 함께** 넘겼다.
