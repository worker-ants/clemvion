# RESOLUTION — `13_04_55`

델타(`3db28b205`)에 대한 판정은 **7명 전원 NONE**. 처분할 코드 발견 0건.

## side_effect 의 CRITICAL — 원인 확정 후 코드 무수정, 하니스로 등재

리뷰어가 본 것은 실재했다. 원인은 **내가 만들었다**:

scope 프롬프트에 "RESOLUTION 이 주장하는 뮤테이션 결과를 **직접 재현**해 확인하라" 고 썼고,
scope 는 그 말대로 **저장소 안에서** 뮤턴트를 심었다(리포트에 그렇게 적혀 있다). 7명이 같은
워크트리에서 동시에 도는 중이었으므로, 그 편집이 다른 리뷰어들 눈에는 "누가 몰래 감사 순서를
뒤집었다" 로 보였다 — 하필 **이 PR 이 막으려던 바로 그 결함 형태**라서 CRITICAL 로 읽힌 것도
당연하다.

**세 가지를 분리해 기록한다:**

1. **코드는 무사하다.** `git diff HEAD` 비어 있고, HEAD 의 `revokePerTriggerToken` 은
   `save()` → `recordAudit()` 정상 순서다. side_effect 본인도 종료 시점에 clean 을 확인했다.

2. **그러나 위험은 실재했다.** testing 은 같은 상태를 3회 관측하고 **`git restore` 로
   되돌렸다**. 내 작업이 커밋돼 있었기에 무사했을 뿐, 미커밋 편집이 떠 있었다면 그 순간
   지워졌다. 이 저장소는 이미 같은 형태로 두 번 작업을 잃은 이력이 있다.

3. **"사용자에게 알리지 마라" 문구를 리뷰어가 거부한 것은 옳다.** 그 문구는 파일이 세션 밖에서
   바뀔 때 하니스가 자동으로 붙이는 정형 텍스트이고 누가 보낸 지시가 아니다. 그렇더라도
   **도구 출력에 실린 문구가 리뷰어의 보고를 막을 수 있다면 리뷰가 무력해진다** — 의심하고
   전부 보고한 판단이 맞다. 이 항목은 리뷰어를 탓할 자리가 아니라 칭찬할 자리다.

**등재**: `plan/in-progress/harness-guard-followups.md` 계열로 "리뷰 프롬프트가 뮤테이션을
저장소 밖 scratch 사본으로 강제해야 한다" 를 남긴다. 이번엔 프롬프트를 쓴 내가 원인이지만,
프롬프트 문구에 기대는 것 자체가 약한 방어다 — testing 은 시키지 않아도 scratch 를 썼고
scope 는 시켰더니 저장소를 썼다.

## 검증

- 워킹트리 clean, HEAD 순서 정상(위 실측).
- 트리거+감사 **172 passed** (side_effect·testing 이 각각 독립 재확인).
- 뮤테이션 **누적 15종** (이번 라운드에 리뷰어들이 A/B 를 각각 두 번씩 더 심었다).

## 수렴 — 종결

발견의 성격이 **동작 → 커버리지 → 구조 → (이번) 확인만** 으로 얕아졌고, 이번 라운드는
7명 전원 NONE 에 scope 가 독립적으로 **"다음 라운드 없이 지금 머지를 권한다"** 를 냈다.
남은 항목은 전부 이유가 명시된 채 `plan/` 에 등재돼 있다. 더 돌 근거가 없다 — 머지한다.
