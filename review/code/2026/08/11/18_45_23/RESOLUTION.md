# RESOLUTION — `18_45_23`

CRITICAL 0 / WARNING 3 **전부 처분** (커밋 `f410fb1b1`). `codebase/**` 변경 0.

## 내 증거가 세 겹으로 얕았다

이 PR 은 "증거를 제대로 댄다" 를 자부하며 시작했다 — 양방향 뮤테이션, 실제 관측 케이스 재현,
성능 벤치마크. 리뷰가 그 셋을 각각 한 칸씩 파고들었고, 셋 다 **한 칸 얕았다**:

| 내가 한 것 | 실제로 필요했던 것 |
|---|---|
| 클래스를 **통째로** 뮤테이트(제거/과잉조임) | **문자 하나씩** — `.`·`_` 는 아무 테스트도 안 지키고 있었다 |
| 뒤 경계에 `.` 을 **안 넣는다** 고 결정 | 맨 뒤 `.` 은 통과, **뒤에 문자가 오면 거부** — `store.md.bak` 이 남아 있었다 |
| 성능을 **쟀다** | 잰 것을 **문서에 실어야** 한다 — plan 엔 수치가 없었다 |

세 번째가 특히 이 저장소의 등재 교훈 그대로다: **`review/` 는 SoT 가 아니다.** 대화에서
측정한 것은 남지 않는다. 문서에 없으면 안 잰 것과 같다.

## 그리고 사실 하나를 틀렸다

"라운드 사이 편집은 하니스 plan 하나" 라고 plan 과 커밋 메시지 **두 곳에** 적었는데 거짓이다.
`165960a92`(R1→R2 사이)는 **이 PR 자신의 plan 하나만** 건드렸고, harness plan 은 R2 가 끝난
뒤(`fdcb2a61a`)에야 그 목록을 받았다.

documentation 이 git 이력으로 잡았고 **나도 `git show --stat` 으로 재확인**했다 — 오늘 이미
"리뷰어 말을 검증 없이 옮겨 적었다가 반증된" 적이 있어서 이번엔 먼저 확인했다.

**정정이 논지를 강화한다.** 방아쇠가 남의 문서였다면 "재수 없었다" 지만, **내가 라운드 사이에
쓴 회고 절**이라면 이건 구조다 — 리뷰가 자기 산출물로 자기 입력을 잠식한다. 그 문장을 plan 에
그렇게 고쳐 썼다.

## side_effect 가 이 수정의 진짜 위험을 닫았다

경계를 조이면 **정당한 언급까지 떨어질** 수 있다 — 그러면 관련 spec 이 번들에서 빠져
checker 가 못 본다(이 PR 이 고치려는 결함의 **반대 방향**). 그걸 실측하라고 요구했고, 실제로
저장소 전체로 돌렸다:

- `plan/in-progress/**` 32파일 409,815자 × `spec/**` 383파일
- 판정이 갈린 파일 **3건**, 전부 `old=True → new=False`
- 셋 다 원문을 grep 해 **오매치임을 확인** — `5-makeshop.md`→`shop.md`,
  `node-cancellation.md`→`cancellation.md`, 리뷰 리포트 `concurrency.md`→`currency.md`
- **정당한 언급이 함께 떨어진 사례 0**

세 번째가 재미있다 — `review/**` 산출물 파일명이 `spec/**` 파일을 승격시키고 있었다.

## 안 고친 것과 그 근거 (scope 가 엄격히 따졌다)

- **카탈로그 tier-4 강등을 안 넓혔다** — `cafe24-api-catalog/store.md` 는 top-level 인덱스라
  강등 대상이 아닌 것이 **의도된 설계**다. 넓혔다면 정당한 인덱스가 함께 죽는다.
- **diff 예산 바닥을 안 깔았다** — 원인(오매치)을 먼저 닫는다. 바닥만 깔면 무관한 파일이
  여전히 **진짜 관련 spec** 을 밀어낸다. 별 건으로 등재.

scope 판정: "안 고친 것이 아니라 **잘못된 방향으로 안 넓힌 것**", "절반짜리 수정이 아니라
정확한 스코프 판단". 동의한다.

## 검증

- 문자별 뮤테이션 **6종 전부 RED**(기준선 41 passed).
- 하니스 전체 **1073 passed / 1137 subtests**.
- security 가 적대적 입력 4형태로 **~2.0배 선형** 독립 확인.

## 수렴

발견이 "결함 존재"(내가 찾음) → "증거의 깊이"(리뷰가 셋을 파고듦) → "사실 귀속" 으로
옮겨갔고, 이번 fix 는 `codebase/**` 를 한 줄도 안 건드렸다. 더 돌 근거가 없다.
