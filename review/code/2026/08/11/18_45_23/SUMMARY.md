# ai-review SUMMARY — `18_45_23` (forced 7)

델타 = 커밋 `dfe4f4319` (`_named_in` 경계 고정 + 테스트 4 + 완료 plan). **`codebase/**` 변경 0.**

## 집계 — 7/7 착지, **CRITICAL 0**

| reviewer | 위험도 |
|---|---|
| security | **NONE** |
| testing · scope · maintainability · documentation · requirement · side_effect | LOW |

## 이 라운드는 내 증거를 검증하러 왔고, 셋을 뒤집었다

### 1. testing — 클래스는 한 덩어리로 테스트되지 않는다 (WARNING)

내 양방향 뮤테이션(경계 제거 2건 / 과잉 조임 4건)은 **정확히 재현**됐다. 그런데 testing 이
한 걸음 더 갔다 — **경계 클래스의 문자를 하나씩** 빼 보니 `.` 과 `_` 는 **12개 테스트가 전부
초록**이었다. `-` 만 우연히 고정돼 있었다(원 버그가 하필 `secret-store.md` 였으므로).

> **"클래스를 테스트했다" 는 문자를 테스트했다는 뜻이 아니다.** 내 양방향 뮤테이션은 클래스를
> 통째로 없애거나 통째로 바꿨을 뿐이다.

처분하며 문자별 매트릭스를 만들었고, 거기서 **숫자 클래스**가 미고정임을 발견했다 —
이 저장소는 `1-auth.md`·`11-mcp-client.md` 로 이름 짓기 때문에 **가장 현실적인 케이스**다.
그리고 그 테스트는 **처음에 vacuous 했다**(`1-auth.md` 가 자연순 1등이라 승격과 무관하게
`out[0]`). 기준선이 실패하는 것을 보고 알았다.

### 2. requirement — 경계가 한 칸 느슨했다 (WARNING)

`_NAME_END` 가 `.` 을 안 막아 `store.md.bak` 이 여전히 오매치. 내 주석은 "뒤엔 `.` 을 안
넣는다(문장 끝 산문 때문)" 였는데, 정확한 규칙은 **"맨 뒤 `.` 은 통과, 뒤에 파일명 문자가 더
오면 거부"** 다. 이 저장소가 등재한 **"방어의 정의를 한 칸 좁게 잡는다"** 의 재발이다.

### 3. documentation — 방아쇠를 잘못 지목했다 (WARNING)

내가 plan·커밋 메시지에 적은 "라운드 사이 편집은 `harness-review-gate-followups.md` 하나" 가
**거짓**이다. git 타임라인(나도 `git show --stat` 으로 재확인):

| 시각 | 사건 |
|---|---|
| 17:21:43 | R1 — diff 실림 |
| **17:40:35** | `165960a92` — **이 PR 자신의 plan 하나만** 편집 |
| 17:42:52 | R2 — diff 탈락 |
| 17:58:34 | `fdcb2a61a` — 그 목록을 harness plan 으로 이관(**R2 이후**) |

**정정된 사실이 논지를 더 강하게 만든다** — 방아쇠는 남의 문서가 아니라 **내가 라운드 사이에
쓴 회고 절**이었다. 리뷰가 자기 산출물로 자기 입력을 잠식한다.

## 리뷰어들이 스스로를 검증했다

- **security** — 적대적 입력 **4형태**(평문·near-miss 반복·lookbehind 후보 밀집·메타문자
  needle)로 재측정해 전부 **~2.0배 선형**. `re.escape` 를 특수문자 파일명으로 왕복 검증.
- **side_effect** — **실 저장소 before/after 전수 대조**: `plan/in-progress/**` 32파일
  409,815자를 `branch_plan_text` 로, `spec/**` 383파일을 대상으로 옛/새 술어를 돌려 판정이
  갈린 **3건**을 찾고 **각각 원문을 grep 해** 전부 오매치였음을 확인
  (`5-makeshop.md`→`shop.md` · `node-cancellation.md`→`cancellation.md` ·
  리뷰 리포트 `concurrency.md`→`currency.md`). **정당한 언급이 함께 떨어진 사례 0.**
  이게 이 수정의 진짜 위험이었고, 실측으로 닫혔다.
- **maintainability** — 내가 "성능 실측" 을 주장했는데 **plan 에 수치가 없다**는 것을 짚었다.
  재긴 했으나 문서에 안 실었다 — 이 저장소의 "review/ 는 SoT 가 아니다" 와 같은 형태.
  저장소 다른 곳에 같은 결함 클래스가 있는지 **뜻으로** 훑어 없음을 확인.
- **scope** — 완료 plan 의 미체크 `[ ]` 가 lifecycle 문언과 어긋남을 짚으면서, **전례 27건**과
  가드 부재를 세어 "이 PR 만의 일탈은 아니다" 로 균형 잡았다. 그리고 내가 **안 고친 두 가지**
  (카탈로그 강등 확대 · diff 예산 바닥)의 절제가 옳은지 엄격히 따져 **옳다**고 판정했다.

## 처분

| # | 출처 | 처분 |
|---|---|---|
| 1 | testing | 문자별 테스트 6건 추가 — `.`·`_`·`-`·`0-9`·`.bak`·문장끝 캐너리. 매트릭스 6/6 RED |
| 2 | requirement | `_NAME_END` 를 `(?![A-Za-z0-9_\-]\|\.[A-Za-z0-9])` 로 정밀화 |
| 3 | documentation | 방아쇠 귀속 정정 + 타임라인 표 + 헤드 ±23 각주 |
| 4 | maintainability | 성능 실측 4단계 표를 plan 에 실음 |
| 5 | scope | 후속 체크박스 → 산문 등재 |

## RISK: LOW
## CRITICAL_COUNT: 0
## WARNING_COUNT: 3 (전부 처분)
