# RESOLUTION — `16_06_02`

CRITICAL 0 / WARNING 0. 처분한 것은 **security 의 비-blocking INFO 하나**(커밋 `df1375208`).

## 이 PR 이 네 라운드 내내 반복한 하나 — 마지막 사례

**한 사실이 여러 곳에 복제돼 있고, 나는 한 곳만 고친다.**

| 라운드 | 복제된 사실 | 놓친 곳 |
|---|---|---|
| 1 | boot 검증 배선 | 호출부(헬퍼만 테스트) |
| 2 | "applyConfig 가 진단한다"(거짓) | `safeApiBase` JSDoc |
| 3 | `§R0` → `§R7` 번호 | 같은 커밋이 새로 쓴 JSDoc |
| **4** | **"쿼리 경로는 샘플 전용이 아니다"** | **코드 주석 2곳** |

4라운드가 특히 선명하다. 나는 3라운드에서 spec §1 에 이렇게 썼다 —

> 쿼리 경로를 "host 없는 직접 로드/샘플 전용" 으로 읽으면 안 된다 — 그렇게 읽고 제거하면
> 모든 정상 임베드의 부트스트랩이 깨진다.

**그 문장을 쓰는 동안 코드 주석 두 곳이 정확히 그렇게 부르고 있었다.** "읽으면 안 된다" 를
경고로 적으면서, 그 오독의 실제 출처는 손대지 않은 것이다.

`git log -S` 로 추적하니 두 주석 모두 최초 위젯 PR(#384)이다 — **spec 의 오서술이 여기서
왔을 가능성이 크다.** 원본을 남긴 채 파생본만 고치면 오해는 그대로 되돌아온다. 그래서 이번엔
주석을 고치고 SoT 를 §1 로 못박았다.

## 이 라운드의 값 — 리뷰어들이 "처분됐다는 주장" 을 검증했다

새 결함이 없는 델타(주석뿐)에서 7명이 한 일은 **내 처분 주장의 반증 시도**였고, 그게 값을 냈다:

- **documentation** 은 내가 새로 쓴 §1 서술을 믿지 않고 `bridge.ts`·`index.ts`·`use-widget.ts`
  까지 소스로 추적해 "host 유무를 검사하지 않는다" 를 코드로 확인했다. 이 PR 은 내가 쓴 spec
  문장이 이미 한 번 거짓으로 판명된 이력이 있다.
- **maintainability** 는 "축약했다" 를 "부피를 되돌렸다" 로 읽지 않도록 커밋별로 세어
  **순증 +7줄**임을 기록했다. 내 표현이 실제보다 크게 들리는 것을 막은 정정이다.
- **testing** 은 `href` 수정이 **오늘 관측되는 버그를 고친 게 아니라 선제 하드닝**임을 jsdom
  프로브로 규정했다. 과대 서술을 막는 같은 종류의 교정이다.
- **requirement** 는 이번에야 실행 가능해진 테스트를 **실제로 돌려** 451 을 확인했다 —
  앞선 라운드들이 못 한 검증이다.

## 검증

- channel-web-chat **451 passed**, `tsc --noEmit` 0, eslint 0(변경 파일).
- 뮤테이션 누적: 내 6종 + 리뷰어 독립 9종. 이번 라운드에 4종 재실측(8/6/2/4 RED).
- `--sequence.shuffle` 4시드 통과.

## 수렴

7명 전원 NONE. 이번에 처분한 것도 blocking 이 아니었다. 발견의 성격이 배선 → 정정 누락 →
참조 정합 → 처분 확인으로 끝까지 얕아졌고, 마지막 항목은 이 PR 이 만든 결함이 아니라 **이 PR
때문에 드러난 선재 오서술**이다. 더 돌 근거가 없다.
