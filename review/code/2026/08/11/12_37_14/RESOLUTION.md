# RESOLUTION — `12_37_14`

Critical 0 / Warning 3 **전부 처분**.

## W1 (testing) — 자매 둘의 실패경로 테스트가 가짜였다 → **고침**

기존 테스트 `rotateNotificationSecret 가 던지면 감사를 남기지 않는다` 는 `config: {}` 로
**사전 validation** 예외를 만든다. 그 지점은 `save()` **앞**이라, `recordAudit` 를 검증 뒤·저장
앞으로 옮기는 뮤턴트가 그대로 GREEN 으로 산다. `revokePerTriggerToken` 은 실패 테스트가
아예 없었다. 그 뮤턴트가 곧 **원래 잡으려던 결함** — 상태는 안 바뀌었는데 감사에는 회전됐다고
남는 것 — 이다.

실패 지점을 **`save()` 자체**로 옮긴 회귀 1건을 추가했다(`create`/`update` 가 이미 쓰는 패턴).

**뮤테이션 — 자매를 따로 세웠다:**

| 뮤턴트 | 결과 |
|---|---|
| 둘 다 audit→save 순서 반전 | RED (`:2444`, notification 쪽에서 정지) |
| **`revokePerTriggerToken` 만** 반전 | RED (`:2453`) |

두 번째를 따로 돌린 이유: 첫 실행은 전반부 단언에서 멈춰 **후반부가 실행조차 안 됐다**. 한
테스트에 자매 둘을 담으면 앞의 것이 뒤의 것을 가린다 — "둘 다 봤다" 가 되려면 각각을 단독으로
살려야 한다. 이 PR 이 세 번째로 낸 같은 형태다.

원복은 `cp`(백업 → 원본), `git status` 로 잔여 0 확인.

## W2 (requirement · documentation 독립 수렴) — 위치 수식어 → **고침**

**"앞의 둘"** 이라고 썼는데 나열 순서는 `notification_secret_rotated` →
`chat_channel_bot_token_rotated` → `interaction_token_revoked` 이고 평문을 반환하는 것은
**1·3번째**다. 액션명을 직접 쓰는 형태로 두 곳(`audit-action.const.ts`·`1-auth.md §4.1`)을
고쳤다. 저장소에 잔존 0건.

> **정정문 자체가 새 부정확을 들여온 경우다.** 원문("셋 다 평문 반환")을 고치면서 위치
> 수식어를 썼고 그게 또 틀렸다. 가리키는 대상은 **위치가 아니라 이름**으로 쓴다 — 순서는
> 나중에 바뀐다. (`CHANGELOG.md:67` 의 같은 표현은 실제로 1·2번째를 가리켜 참이므로 둔다.)

## 등재 처분 (코드 무수정) — 2건

`plan/in-progress/spec-sync-auth-gaps.md` 의 감사 계열 트랙에 남긴다.

| 출처 | 내용 | 왜 지금 안 고치나 |
|---|---|---|
| testing INFO | `rotateBotToken` 의 5→6 구간 발화 뮤턴트 생존 | 실패 주입점을 6단계 직전으로 옮기려면 secret store mock 을 한 겹 더 세워야 한다. 현 테스트의 docstring 이 스스로를 4단계로 한정하고 있어 **거짓 서술은 아니다** |
| documentation INFO | plan 의 "착수 시점 서술" 에 캐비엇 없음 | 그 파일의 기존 관례(완료 항목의 원 서술 보존)에 부합. 캐비엇을 달면 관례가 갈린다 |

## 검증

- 트리거+감사 **171 passed**(신규 회귀 포함, 직전 170 → +1).
- 내가 건드린 파일 타입 오류 **0**.
- 뮤테이션 **누적 9종** — 감사 호출 제거 ×2 / 액션 스왑 / 조기 발화 ×2 / 필드 손상 3종 /
  자매 순서 반전 ×2(단독 2회).

## 남은 절차

W1 이 `codebase/**` 를 건드렸으므로 이 라운드는 **자기 자신을 무효화한다**. 확인 라운드를
한 번 더 돌린다 — 게이트가 요구하는 대로다.
