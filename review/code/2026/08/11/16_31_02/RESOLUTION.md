# RESOLUTION — `16_31_02` (+ consistency `16_31_08`)

CRITICAL 0 / WARNING 1 + INFO 3. **처분 2 · 유예 1 · 확인기록 1** (커밋 `7e9c75b22`).
`codebase/**` 변경 0줄 — 전부 plan 문서다.

## 여섯 번째 사례가 회고 절 안에서 났다

절 제목은 **"라운드 2~5"(넷)**, 표는 **1~5(다섯 행)**, 본문은 **"다섯 번"**.
**개수를 정확히 세라는 절이 개수를 틀렸다.**

그리고 같은 절에서 두 번째로, **"출처" 라는 말을 두 뜻으로 썼다** — 개념의 뿌리(#384)와
문자열의 저작 시점(#761)을 뭉뚱그렸다. 세 번째 복제본은 #384 에서 복사된 게 아니라
`aba46cc90`(#761)이 **같은 오해를 독립적으로 다시 쓴 것**이다.

이 구분이 장식이 아니다. **원본을 고쳐도 독립 재저술은 막히지 않는다** — 라운드 4 에서
원본 2건을 고쳤는데도 다섯 번째 사례가 나온 이유가 정확히 이것이다. 회고에 그렇게 적었다.

## 유예 1건 — 근거를 실측으로 적는다

`api-base.ts:5` 의 옛 별칭 `direct-load 쿼리 하드닝`. 고치지 않았다:

- **배타성을 주장하지 않는다** — "`apiBase` 는 경로 포함이 정상" 의 근거로 기능을 **참조만**
  한다. 제기자 본인이 "기능적 위험 없음, INFO 로 유지" 로 판정했다.
- 파일도 관심사도 다르다(trailing-slash 정규화). 이 PR 이 닫는 오독 경로가 아니다.
- 고치면 `codebase/**` 가 바뀌어 두 게이트가 재무장된다 — 위험 0인 별칭에 라운드 하나.
  maintainability 도 주석 총량을 더 늘리지 말 것을 같은 라운드에 권고했다.

**plan 에 사유와 함께 등재했다.** `review/` 는 SoT 가 아니다 — 이 저장소가 미룬 항목 5건을
잃을 뻔한 이력이 있다.

## 리뷰어들이 나에게 유리한 답을 주지 않았다

- **maintainability** — "네 곳이 선을 넘었나" 에 "정정이지 새 중복이 아니다" 라고 답하면서도
  **"순수 새 중복이었다면 4곳은 과했을 것"** 을 명시하고, 누적 주석량(로직 ~30줄 : 주석
  ~12줄)이 **"다소 높다"** 며 향후 억제를 권고했다.
- **rationale_continuity** — 내 "#384 유래" 의 **맞는 부분과 틀린 부분을 갈라** 제시했다.
  통째로 인정하거나 통째로 기각하지 않았다.
- **documentation** — 회고 절이 자기 주제를 어긴 것을 잡았다. 내가 쓴 교훈 문장 자체를 감사한 것.

## 검증

`codebase/**` 무변경(451 passed 유지). 회고 표의 5행·"6명 수렴"·"451" 을 plan_coherence 와
requirement 가 각각 **독립 재현**했다. naming 이 인용 해시·앵커·타임스탬프를 전수 실재 확인.

## 수렴

12명 중 11명 NONE, 나머지 하나가 낸 WARNING 은 **문서 내부 산술**이었다. 이번 fix 는
`codebase/**` 를 한 줄도 건드리지 않았다. 발견의 성격이 배선 → 정정 누락 → 참조 정합 →
처분 확인 → 세는 방법 → **세는 말의 정의**까지 좁아졌다. 더 돌 근거가 없다.
