# 유지보수성(Maintainability) Review — 델타 `9416da806`

대상: `codebase/channel-web-chat/src/widget/use-widget.test.ts:15-16` (주석 정정),
`plan/complete/webchat-boot-apibase-scheme-validation.md` (라운드 2~5 회고 절 신설).

## 확인 절차

네 곳의 실제 문구를 직접 열어 대조했다.

- `spec/7-channel-web-chat/4-security.md:39` — 표 한 행, "SoT" 로 명시. 전체 서술의 정본.
- `codebase/channel-web-chat/src/widget/use-widget.ts:222-224` — `configFromQuery` JSDoc.
  본문 + `SoT: 4-security.md §1` 포인터.
- `codebase/channel-web-chat/src/widget/use-widget.ts:1382-1384` — 폴백 호출부(`fallback = configFromQuery()`)
  바로 위 인라인 주석. 3줄, 짧은 경고형.
- `codebase/channel-web-chat/src/widget/use-widget.test.ts:15-16` (이번 델타) — `describe("safeApiBase — 쿼리 경로")`
  바로 위 주석. 2줄, 짧은 경고형 + `4-security.md §1` 포인터.

`git show 9416da806` 로 확인: 실행 코드 변경은 0줄이고, 정정 전 문구(`direct-load 외부 입력 방어`)는
`resolveIframeTarget` 과 **같은 커밋(#384, `a652f8733`)**에서 함께 태어난, 처음부터 틀린 주석이었다
(plan 회고 절의 주장과 `git log -S` 결과가 일치).

## 발견사항

- **[INFO]** 같은 사실이 이제 네 곳(spec 표 1행 + JSDoc 6줄 + 호출부 3줄 + 테스트 2줄, 총 ~12줄)에
  중복 서술된다.
  - 위치: `codebase/channel-web-chat/src/widget/use-widget.ts:222`, `:1384`,
    `codebase/channel-web-chat/src/widget/use-widget.test.ts:15`
  - 상세: 다만 이번 4번째는 **새로 추가된 중복이 아니라 5라운드째 남아 있던 오류 사본을 정정**한
    것이다 — 정정 전 문구가 실행 코드 삭제로 이어질 수 있는 틀린 주장(`direct-load 외부 입력 방어`)
    이었고, 실제로 지난 라운드들에서 같은 오해가 반복 재발했다(리뷰어 스스로도 4라운드에서 "정확히
    2곳"이라 오판). spec/JSDoc/호출부 3곳을 "서로 다른 진입점을 지키는 정당한 트레이드오프"로 판정한
    직전 라운드 판정과 같은 논리가 테스트 파일에도 적용된다 — 테스트를 읽고 병합 로직을 손대려는
    사람이 있을 수 있는 별도 진입점이고, 짧은 경고+포인터(`4-security.md §1`) 형태라 본문을 통째로
    반복하지 않는다. 과잉으로 보기는 어렵다.
  - 제안: 조치 불요. 굳이 더 줄이려면 호출부/테스트 주석을 "짧은 경고 + 포인터"에서 포인터만 남기는
    방향도 있으나, 5라운드에 걸쳐 실제로 반복 재발한 오해를 막는 안전장치이므로 지금 형태를 유지하는
    편이 낫다.

## 판정

1. **네 번째가 선을 넘었는가 — 넘지 않았다.** 앞의 세 곳(spec/JSDoc/호출부)과 이번 테스트 주석은
   성격이 다르다: 이번 커밋은 *새 중복을 추가*한 게 아니라 *5라운드 동안 방치된 유일한 오류 사본을
   원본까지 정정*한 것이다. 정정 전 문구는 실제로 존재하는(그리고 실제로 여러 리뷰 라운드에서
   사람/리뷰어를 오도한) 틀린 서술이었다. "고치지 않고 둔다"는 선택지가 오히려 유지보수성 관점에서
   더 나쁘다 — 다음 편집자가 테스트 주석만 보고 "이 경로는 direct-load 전용"이라 오판해 정상 임베드
   경로를 지울 위험이 그대로 남기 때문이다. 다만 순수 새 중복이었다면 4곳은 과했을 것이라는 점은
   동의한다 — 이번 경우는 "중복 추가"가 아니라 "정정"이라는 점이 판정을 가른다.
2. **6라운드 누적 주석 총량 vs 코드 — 다소 높지만 코드 위험 대비 과하지 않다.** `safeApiBase` /
   `configFromQuery` / 호출부 폴백을 합쳐도 실제 로직은 30줄 안팎인데 그 위에 붙은 방어 주석이
   ~12줄(+ spec 표 1행)이다. 비율만 보면 높지만, 그 주석들이 막으려는 실패("이 경로를 죽은 코드로
   오인해 삭제")가 이 저장소에서 **실제로 5라운드 연속 재발**했다는 실측이 있고(plan 회고 절이 표로
   고정), 대부분 짧은 경고+포인터 형태라 본문 전체를 반복하지 않는다. 순수 볼륨 관점에서 향후 더
   늘리지 않는 것을 권장하되, 이번 정정 자체를 되돌릴 이유는 없다.

## 요약

`9416da806` 은 실행 코드를 건드리지 않는 1줄 주석 정정 + plan 회고 절 추가다. "쿼리 경로는
직접로드/샘플 전용이 아니다"라는 사실이 이제 네 곳에 서술되지만, 이번 델타는 새 중복을 만든 게
아니라 5라운드째 남아 있던 **원본(처음부터 틀렸던) 오류 사본**을 바로잡은 것이므로 직전 라운드가
인정한 "서로 다른 진입점을 지키는 정당한 트레이드오프" 논리가 그대로 이어진다. 누적 주석량은
코드 대비 다소 높은 편이나, 같은 오해가 실측으로 5회 재발한 이력을 감안하면 정당화된다. plan
회고 절(29줄)도 완료 plan 아카이브에 남기는 프로세스 교훈으로 범위에 맞는 분량이다.

## 위험도

NONE

STATUS: OK
