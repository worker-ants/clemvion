# Maintainability Review — `10_41_08`

focus: 직전 라운드(`10_24_54`) WARNING 2건 반영 확인 — (a) `sseErrorDetail` module-scope 이동 위치, (b) `sessionRef` 포인터 축약이 실제 설명을 가리키는가, (c) "가드 한쪽만"(6회)·"뮤테이션 생존 메모"(4회) 모티프가 이번 라운드에 늘었는가(`runApplyConfig` 신규 문단 포함).

## 발견사항

- **[INFO]** `sseErrorDetail` 위치 이동 — 이 파일의 순수-헬퍼 컨벤션과 일치, 확인 완료
  - 위치: `codebase/channel-web-chat/src/widget/use-widget.ts:192-211` (JSDoc 192-202 + 함수 204-211)
  - 상세: 훅(`useWidget()`, 213행) 정의 직전으로 옮겨져, 같은 그룹(클로저를 캡처하지 않는 순수 함수를 module scope 에 두는 자리)에 있는 `fetchEmbedConfig`(:34)·`isEmbedAllowed`(:56)·`safeApiBaseFromQuery`(:170)·`configFromQuery`(:183)·`shouldAbortAfterSeed`(:137)와 동일 블록에 속한다. `openStream` JSDoc(구 437-458)과 정의(구 479) 사이에 끼어 있던 문제(`10_24_54` WARNING)는 구조적으로 해소됐다 — 직접 대조 확인.

- **[WARNING]** `sseErrorDetail` 의 `@internal` 태그가 별도 JSDoc 블록으로 분리돼, 이 저장소가 이미 세운 `@internal` 표기 컨벤션과 어긋난다
  - 위치: `codebase/channel-web-chat/src/widget/use-widget.ts:192-203` (본 설명 JSDoc 192-198, 별도 `@internal` 블록 203)
  - 상세: RESOLUTION(`10_24_54` W1)은 "`@internal — unit-test seam only` 를 달았다(이 저장소의 기존 관례)"라고 적었는데, 그 기존 관례(`codebase/channel-web-chat/src/lib/eia-client.ts:25-32`, `unwrapEnvelope`)는 `@internal` 을 **하나의 JSDoc 블록 안**에 다른 설명과 함께 한 줄로 넣는 형태다. `sseErrorDetail` 은 그 대신 `/** ... */` 두 개를 겹쳐 쌓았다(192-198 하나, 203 또 하나) — 파일 전체에서 이 함수만 유일하게 이중 JSDoc 블록을 쓴다(`grep '^/\*\*'` 로 확인, 다른 함수는 전부 단일 블록). 기능에는 영향 없지만 "관례를 따랐다"는 근거가 실제로는 관례와 형태가 다르다.
  - 제안: `@internal — unit-test seam only. ...` 줄을 192-198 블록 안으로 옮겨 하나의 JSDoc 으로 합칠 것(`eia-client.ts` 의 `unwrapEnvelope` 형태 참고).

- **[INFO]** 축약된 두 포인터는 실제로 유효한 목적지를 가리킨다 — hollow pointer 아님, 확인 완료
  - 위치: `:616`(`seedWaitingFromStatus` JSDoc "§REST 오류 분기" 불릿) → `:632-634`(같은 JSDoc `@returns` 절). `:863-864`(`start()` 호출부) → `seedWaitingFromStatus` 의 `@returns`(`:624-636`).
  - 상세: `:616` 은 "근거는 아래 `@returns` 참조" 로 **같은 JSDoc 블록 안에서 아래**를 가리키므로 스크롤 한 번이면 도달한다. `:634` 를 직접 읽으면 "401 복구가 성공한 경우도 `continue` 에 포함되고 그때 세션 토큰이 교체돼 있다. 그래서 지역 변수가 아니라 `sessionRef.current` 를 읽어야 한다"는 완결된 근거가 실제로 존재한다. `:863-864` 도 "`seedWaitingFromStatus` 의 `@returns`" 로 함수명+태그명을 명시해 검색으로 정확히 도달 가능하다. 둘 다 원문 복제보다 낫다는 판정에 동의.

- **[WARNING]** 포인터 축약 과정에서 `ai-review 16_09_40 CRITICAL — 4명 독립 수렴` provenance 가 소스 어디에도 남지 않게 됐다
  - 위치: `:611-622`(REST 오류 분기), `:624-636`(`@returns`), `:863-865`(`start()` 호출부) — 이 세 곳 전부.
  - 상세: 축약 전 원문(`git show 8eb223c19^:...use-widget.ts`)은 두 자리(구 `:616-617`, 구 `:864-868`) 모두에 `(ai-review 16_09_40 CRITICAL — security·side_effect·requirement·testing 4명 독립 수렴)` 을 명시했다. 이 파일의 다른 모든 breadcrumb 관례(`SeedOutcome`·`StreamClaim`·`shouldAbortAfterSeed`·`applyRefreshedToken` 등)는 "왜 이 코드가 있는가"의 근거로 라운드ID·심각도·수렴 인원을 남겨 향후 독자가 "이건 과한 방어"라고 오판해 지우는 것을 막는 패턴을 일관되게 쓴다. 지금 남은 canonical 자리(`@returns`, `:635-636`)에는 대신 **다른** 사건(`02_04_13 CRITICAL#1`, "복원 경로가 teardown 직후 openStream" — 다른 결함)만 인용돼 있어, `sessionRef.current` 규칙 자체의 심각도·재발 이력("이 파일이 이미 두 번 밟은 CRITICAL")을 뒷받침하는 근거가 소스에서 사라졌다. `grep 16_09_40 use-widget.ts` 결과 세 곳(:122, :521, :556)은 전부 다른 규칙(`shouldAbortAfterSeed`·`recoverFromExpiredToken` 의 401/410 판정)에 관한 것이다.
  - 제안: `@returns`(`:632-634`) 문장 끝에 `(ai-review 16_09_40 CRITICAL — 4명 독립 수렴)` 을 되살릴 것. 포인터로 축약하더라도 "왜 이게 중요한가"의 근거는 canonical 위치엔 남아야, 다음 사람이 규칙을 완화하려 할 때 막는 힘이 유지된다.

- **[INFO]** `applyConfig` 호출부의 기존 포인터(`:1231` "위 주석 참조")는 상대 위치어라 목적지가 모호하다 — 이번 라운드가 만든 문제는 아니나 같은 계열 포인터가 늘어난 지금 재확인할 가치가 있음
  - 위치: `codebase/channel-web-chat/src/widget/use-widget.ts:1231`
  - 상세: "`start()` 와 같은 이유로 ref 를 읽는다 — 위 주석 참조"에서 "위 주석"을 문자 그대로 따라가면 바로 위(`:1227-1230`)는 "스트림 게이트는 `openStream` 안"이라는 **다른 주제**(checkpoint 2/boot 축)다. 실제 의도는 "`start()` 함수 안의 대응 주석(`:863-864`)을 보라"인데, `start()` 는 파일에서 더 위에 정의돼 있을 뿐 "바로 위 문단"은 아니다. 이 라운드가 추가한 두 포인터(`:616`, `:863-864`)는 함수명·태그명을 명시해 이 모호함이 없지만, `:1231` 은 그대로다. 세 포인터가 서로 다른 정밀도로 공존하는 셈이라, 다음에 `:1231` 을 만지게 되면 같은 명시적 anchor 스타일로 맞출 것.
  - 제안: "위 주석 참조" → "`start()` 의 `sessionRef.current` 관련 주석 참조" 처럼 named anchor 로 교체(지금 당장 급한 것은 아님).

- **[WARNING]** "주석 밀도 정리"를 표방한 라운드인데 파일 총 라인 수는 순증가했다 — 추적 중인 두 모티프는 안 늘었지만 새 변종이 그 자리를 메웠다
  - 위치: 파일 전체 (`git show 8eb223c19^:...use-widget.ts | wc -l` = **1358**, `git show 8eb223c19:...` = **1364**, 순증 +6줄). 신규 문단: `:1268-1277`(`runApplyConfig` catch, "stale 가드가 없다 — 그리고 그건 구조적 제약이다" + "오늘 무해한 근거(실측)").
  - 상세: `10_24_54` 리뷰가 지목한 두 모티프를 재계산하면 정확히 이전 개수와 같다 — "가드를 한쪽에만 적용" 계열 6회(`:121`, `:462`, `:558`, `:664`, `:778`, `:1256`), "뮤테이션 생존" 계열 4회(`:125-128`, `:371`, `:574-578`, `:775-780`). 즉 리뷰어가 건 조건("다음 라운드가 늘리면 통합 검토")을 **문자열 카운트 기준으로는** 아직 충족하지 않는다. 그런데 같은 라운드에서 `side_effect` W5 대응으로 추가된 `:1268-1277` 문단은 두 모티프와 **같은 수사 구조** — "여기 방어가 없어 보이지만 오늘은 안전하다(구조적 이유) + 트리거 조건을 등재한다" — 를 쓰는 세 번째 변종이다. 결과적으로 이번 라운드는 중복을 줄이는 fix(포인터 축약, -6줄)와 새 정당화 문단을 추가하는 fix(+10줄)가 같은 커밋에 같이 들어가 **순 라인 수가 오히려 늘었다**(1358→1364). "다음 라운드가 늘리면 통합 검토"라는 조건이 두 모티프의 리터럴 문자열만 세도록 좁게 읽히면, 이런 식으로 매 라운드 새 변종이 그 감시망을 피해가며 파일이 계속 자랄 수 있다.
  - 제안: 지금 당장 통합을 요구하지는 않는다(단일 인스턴스이고 개별 근거가 있다는 점은 기존 판정과 동일). 다만 "통합 검토" 트리거 조건을 다음 라운드부터는 "가드 한쪽만/뮤테이션 생존 두 문구가 늘었는가"가 아니라 "이 파일의 총 라인 수 또는 이런 형태(가드 부재·중복 정당화)의 새 사이트가 늘었는가"로 넓혀 판정할 것을 권한다. 그래야 실제로 관찰된 성장(순증 +6줄)이 다음 라운드 판정에 반영된다.

## 요약

이번 라운드가 반영했다고 주장한 두 WARNING 중, `sseErrorDetail` 위치 이동은 이 파일의 순수-헬퍼 컨벤션과 정확히 일치하고, 두 포인터(`:616`, `:863-864`) 모두 hollow reference 가 아니라 실제로 완결된 설명(`@returns` 절)을 가리켜 원문 복제보다 낫다는 판단은 유효하다. 다만 그 과정에서 `@internal` 태그가 이 저장소의 기존 단일-JSDoc-블록 관례와 다른 이중 블록으로 붙었고, 축약된 두 자리 모두에서 "왜 이 규칙이 중요한가"를 뒷받침하던 `16_09_40 CRITICAL 4명 독립 수렴` provenance 가 소스에서 완전히 사라졌다(남은 canonical 자리는 다른 사건을 인용한다) — 포인터를 따라가면 규칙 자체는 알 수 있어도 "왜 이게 방어적으로 과해 보여도 지우면 안 되는지"의 무게는 옅어졌다. 세 번째 요청(모티프 6회/4회가 늘었는가)에는 리터럴 카운트로는 "안 늘었다"가 정답이지만, 실측(`git show` 라인 수 대조, 1358→1364)은 파일이 여전히 순증가하고 있음을 보여준다 — `runApplyConfig` 에 새로 붙은 "가드 부재 정당화" 문단이 두 모티프와 같은 수사 구조를 반복하는 세 번째 변종이라, 다음 라운드의 "통합 검토" 문턱을 두 문구의 문자열 카운트가 아니라 파일 전체의 성장 추세로 넓혀 잡을 필요가 있다. 기능적 결함은 없다.

## 위험도

LOW
