# RESOLUTION — `10_41_08`

Critical 0 / Warning 8 **전부 처분**. 처분 커밋: `37b38cf31`.

## W7 (testing) — 통합 테스트가 못 가르는 자리 하나 더 → **고침**

`shouldAbortAfterSeed` 는 module-private 라 직접 테스트가 불가했고, `"stale"` 을 `"continue"` 로
오판하는 뮤턴트가 **위젯 418건을 전부 통과**했다(리뷰어 실측). `start()` 호출부는 후행
`isStale(gen)` 재검사가 우연히 덮지만 `applyConfig` 는 다른 축(`isAttemptStale`)이라 같은 동치가
서지 않는다 — `sseErrorDetail` 이 겪은 것과 **같은 형태의 다음 자리**다.

`@internal — unit-test seam only` 로 노출하고 **4-way 진리표 + fail-closed 축**(모르는 갈래는
중단으로 떨어지는가)을 직접 단언한다. 뮤테이션 RED 확인.

함께 `sseErrorDetail` 의 미검증 축(INFO)도 닫았다 — "키는 있는데 값이 `undefined`". `in` 존재검사를
`?? null` 로 바꾸는 리팩터가 조용히 통과하던 자리이고, 그 뮤턴트로 RED 확인.

## W1 — 인용 정정이 **세 곳 중 두 곳**이었다 → **고침**

`4-security §5` → `§1`(표 "에러 메시지 노출") 정정을 `use-widget.ts` 두 곳에만 하고 테스트 파일의
세 번째 사본을 빠뜨렸다. 저장소 잔존 **0건** 확인.

**아홉 번째다.** 그리고 이번엔 하필 *그 패턴을 고치는 작업 안에서* 났다.

## W2 — **"(실측)" 라벨이 틀렸다** → **고침**

이 파일에서 "(실측)" 은 **코드를 바꿔 돌려본 결과**를 뜻한다(다른 5곳이 전부 그렇다). 그런데
`runApplyConfig` 불변식의 근거는 소스를 눈으로 따라간 **정적 논증**이다. 코드 주석과 plan 사본
양쪽을 "정적 추적 — 실행·뮤테이션이 아니다" 로 고쳤다.

내 메모리가 경고하는 축(*"실측했다"가 세 번 틀렸다*)에 정확히 걸린 경우다. 라벨을 아끼는 것이
라벨을 남발하는 것보다 낫다 — 남발하면 진짜 실측도 같이 못 믿게 된다.

## W3·W4 — 축약이 잃은 것 / 태그 배치 → **고침**

- 포인터 축약이 `16_09_40` 의 **"4명 독립 수렴"** provenance 를 잃었다(그 문구가 축약한 사본에만
  있었다). 앵커(`@returns`)에 복원하고 **그곳이 단일 소재지임**을 명시했다 — 포인터 패턴은 앵커가
  실제로 그 내용을 담을 때만 성립한다.
- `@internal` 을 별도 블록으로 분리했던 것을 함수 JSDoc 안으로 합쳤다(`eia-client.ts` 선례).

## W6 — 근거를 다른 항목에서 빌려 왔다 → **고침**

"4곳 중 2곳만 축약, 나머지는 근거 있어 유지" 라고 썼는데 그 "근거" 는 별개의 두 주석 모티프에 대한
것이었고, 4번째 잔존 지점은 근거 없이 방치됐다.

그 지점은 **성격이 다르다** — 프로덕션 코드의 설명 복제가 아니라 **테스트가 자기 단언의 존재
이유**를 적은 것이다. 지우면 다음 사람이 `getUrl()` 단언을 장식으로 읽는다. 그 구분을 그 자리에
적었다.

## W5 — "줄였다" 는 라운드에서 파일이 늘었다 → **인정, 재서술**

지적이 맞다(1358 → 1364). 추적 중인 두 모티프는 안 늘었지만 새 변종이 그 자리를 메웠다.
**"주석 밀도 정리" 라는 프레이밍이 실제보다 넓었다** — 이번 라운드도 순증이므로 그렇게 쓰지
않는다. 중복 제거는 했고 총량은 안 줄었다, 가 정확한 서술이다.

## W8 — "기록이므로 편집 금지" 논리 → **재서술**

같은 티켓에서 `16_42_07` 산출물을 사후 정정한 선례(`3d0cec69b`)가 있으므로 그 논리는 일관되지
않는다. 실제 기준은 **"안전에 영향을 주는 허위 주장은 정정하고, 집계 오차 같은 북키핑은 남긴다"**
이다. 전자는 다음 사람의 판단을 바꾸고 후자는 안 바꾼다. 그 기준으로 다시 적는다.

## 검증

- 위젯 vitest **442 passed** (23 files, +3).
- harness/doc guards **1032 passed / 1128 subtests**.
- `tsc --noEmit` **0 errors**.
- 뮤테이션 **누적 18종** — 이번 2종 추가, 둘 다 RED.

## 수렴 판정 — 이 라운드로 종결

**두 라운드 연속 CRITICAL 0**, security·side_effect 가 NONE, requirement 가 `status: implemented`
를 **최종 참**으로 판정했다. 남은 발견의 성격도 동작이 아니라 서술 정확도와 커버리지 seam 이다.

이 티켓은 12라운드를 돌았고, 라운드 7~10 의 CRITICAL 은 전부 **직전 라운드 fix 가 만든 것**이었다.
그 사슬은 라운드 11 에서 끊겼고 12 에서도 재발하지 않았다. 더 도는 것이 낫다는 근거가 없다.
