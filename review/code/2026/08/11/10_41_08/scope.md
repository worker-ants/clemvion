# 변경 범위(Scope) Review

대상: `claude/webchat-reload-rest-branches` vs `origin/main`(141 files, +16518/-76, 단일 세션
누적 — 4+ 회 선행 scope 라운드가 이미 대부분을 NONE/LOW 로 판정한 diff). 이번 호출은 신규 코드
변경이 아니라(HEAD 는 `c22670c3b`, `10_24_54` 산출물 커밋 이후 워킹트리 변경 없음 — `git status`
clean 확인) **`10_24_54` 라운드의 처분 주장 재검증**이 실질 과제다. 아래 순서로 보고한다: (1)
지시받은 두 재검증 항목, (2) 나머지 diff 에 대한 표준 8관점 점검.

## 1. `10_24_54/RESOLUTION.md` 처분 주장 vs 커밋 `8eb223c19` 재검증

`git show 8eb223c19 --stat` 로 확인한 실제 변경 파일은 3개뿐이다: `use-widget.test.ts`(+32),
`use-widget.ts`(66 changed), `plan/in-progress/webchat-auth-session-status-reconcile.md`(+18).

### (a) "동작 변경 0" — **확인됨, 사실**

RESOLUTION.md 원문에는 이 정확한 문구가 없다("동작 CRITICAL 이 0", 커밋 메시지는 "동작 결함
0")이지만, 실제 diff 를 코드 레벨로 대조한 결과 **런타임 로직 변경은 없다**:

- `use-widget.ts`: `sseErrorDetail` 함수 본문 **1바이트도 안 바뀜** — `useWidget()` 안에서
  module-level(다른 순수 헬퍼 옆, 현재 위치 `:204`, `fetchEmbedConfig`(`:34`)·`isEmbedAllowed`
  (`:56`)·`shouldAbortAfterSeed`(`:137`)와 같은 구역)로 **이동 + `export` 부여**뿐이었다.
  함수 선언이라 호이스팅에 위치 무관 — 동작 차이 없음. `export` 부여는 모듈 표면을 넓히지만
  `@internal — unit-test seam only` 주석(`eia-client.ts:30` 에 이미 있는 저장소 기존 관례,
  `grep` 대조 확인)을 그대로 따른 것이라 신규 패턴 아님.
  - JSDoc·주석 5곳 수정 — 전부 텍스트(스펙 인용 `§5`→`§1`, 중복 설명 → 포인터, 새 불변식 설명
    추가). 코드 분기·조건 변경 없음.
- `use-widget.test.ts`: `sseErrorDetail` 단위 회귀 3건 추가(테스트 코드 — 프로덕션 동작 아님).
- `plan/.../webchat-auth-session-status-reconcile.md`: 신규 추적 항목 18줄 — 문서.

`pnpm vitest run`(23 files) **439 passed** 직접 재실행 확인, `pnpm exec tsc --noEmit` **0
errors** 직접 재실행 확인 — RESOLUTION 의 "검증" 절 수치(439 passed, 0 errors)와 **정확히 일치**.
claim 이 정확하다.

### (b) "뮤테이션 RED 확인"(W1) — **구조적으로 타당, 같은 라운드 `testing.md` 방법론과 일치**

`sseErrorDetail` 을 `return "error"` 로 뭉개는 뮤턴트를 가정하면, 신규 회귀
(`use-widget.test.ts` 신규 `describe("sseErrorDetail...")`)의 첫 `it` 이
`expect(...).toContain("readyState=0")` / `"readyState=2"` 를 단언하므로 **필연적으로 실패한다**
(코드 읽기로 직접 증명 — 워킹트리 변경 금지 지침에 따라 실제 뮤테이션은 실행하지 않았다).
같은 라운드 `review/code/2026/08/11/10_24_54/testing.md` 가 바로 이 뮤턴트(`return "error"`
전체 치환)를 **repo 밖 scratch 사본**에서 실제로 걸어 "75 passed, 0 failed"(수정 전 갭)를
실측했다고 기록돼 있어, RESOLUTION 의 "그 뮤턴트로 RED 확인" 이 가리키는 대상과 내가 코드로
증명한 뮤턴트가 **동일**하다. claim 신뢰할 만하다.

### (c) "주석 4곳 중 2곳만 축약(나머지는 근거 있어 유지)" — **부분 부정확, WARNING**

실제 파일을 대조해 "지역 변수 대신 `sessionRef.current`" rationale 이 나타나는 4곳을 전부
찾았다(`git show 8eb223c19^` 로 수정 전 상태도 대조):

| # | 위치 | 수정 전 | 수정 후 |
|---|---|---|---|
| 1 | `use-widget.ts:1236`(`applyConfig` 스트림 오픈 직전) | 이미 `"위 주석 참조"` 포인터 | 무변경(이미 압축) |
| 2 | `use-widget.ts:616`(`seedWaitingFromStatus` JSDoc, REST 분기 bullet) | 전문(3줄) | `@returns` 참조 포인터로 축약 |
| 3 | `use-widget.ts:863`(seed 호출부 코드 주석) | 전문(4줄, `16_09_40` CRITICAL 인용 포함) | `@returns` 참조 포인터로 축약 |
| 4 | `use-widget-eager-start.test.ts:366`(회귀 테스트 인라인 주석) | 전문(3줄) | **무변경 — 여전히 전문** |

RESOLUTION W3 은 "그중 한 곳은 이미 포인터를 쓰는데 나머지가 안 따랐다. 두 곳을 포인터로
축약했다" 라고만 적어, 이 서술 자체는 위 표(#1 기존 포인터, #2·#3 신규 축약)와 **정확히
일치**한다. 문제는 이어지는 문단이다 — "가드를 한쪽에만' 모티프(6회)·뮤테이션 생존 메모(4회)는
지우지 않았다 — 리뷰어도 지점별 근거가 있다고 판단했고..." 라는 **명시적 근거**는 이 4곳
dedup 주제가 아니라 **다른 두 개의 주석 모티프**에 대한 것이다. `#4`(테스트 파일의 남은 전문
중복)에 대해서는 RESOLUTION 어디에도 "왜 안 건드렸는지" 근거가 없다 — 단지 W3 전체가
"부분 고침" 으로 정직하게 라벨링돼 있을 뿐, "나머지는 근거 있어 유지" 라는 서술은 **이
특정 4곳-dedup 주제에는 적용되지 않는 문구를 가져다 붙인 것**이다.

- 실질 리스크는 낮다 — `#4` 는 프로덕션 코드가 아니라 테스트 파일의 인라인 설명이고, 테스트
  주석이 자기완결적인 것은 일반적으로 합리적 관례다. 하지만 그 합리성은 RESOLUTION 이
  **말하지 않은** 내 추론이지, 문서가 실제로 근거를 댄 것이 아니다.
- 제안: RESOLUTION W3 에 "#4(테스트 파일 사본)는 미착수, 별도 사유 없음 — 다음 라운드가 이
  파일을 또 건드리면 함께 정리" 정도로 명시하거나(사실 그대로 인정), "나머지는 근거 있어
  유지" 프레이밍을 이번 4곳-dedup 항목에서 분리해 재작성 권장. 코드 수정은 불요.

## 2. 직전 라운드 자기 WARNING(W6, SUMMARY 집계표 불일치) 처분 타당성

`10_02_22/SUMMARY.md` 집계표(합계 Warning=9)를 실측 재계산: security 2·scope 1·testing 1·
maintainability 1·side_effect 2·documentation 1·requirement 1 = **9**(표와 일치). 반면 본문
W1~W7 나열에서 각 항목이 credit 하는 reviewer 를 센 값은 scope=2(W2 공동+W6)·side_effect=1
(W1 만)·requirement=2(W1+W7) 로 표(각각 1·2·1)와 **어긋난다** — 지적된 불일치는 실재한다.
`## WARNING_COUNT: 7`(항목 개수, 표의 열 합 9 와는 애초에 다른 지표)도 세 번째 다른 숫자라
혼란을 가중한다.

`10_24_54/SUMMARY.md` 자체의 표(합계 6)와 본문 W1~W6 나열을 같은 방식으로 재검산하면
testing1·maintainability2·requirement1·side_effect1·scope1 = 6, 표와 **완전히 일치**한다 —
"이번 SUMMARY 에서 집계-나열 일치를 확인한 뒤 썼다" 는 claim 은 사실이다.

**"과거 산출물을 정정하지 않는다" 는 판단 자체는 이 경우 타당하다고 본다** — 다만 그 근거로
든 "기록이므로(사후 편집 금지)" 라는 절대 규칙은 **이 티켓 자신의 선례와 모순**된다:
`16_42_07/RESOLUTION.md` 는 `16_56_39` 라운드 이후 커밋 `3d0cec69b` 로 **실제로 사후 편집**됐다
(원문 보존 + `_(원 서술)_` 라벨 + 정정 문단 삽입). 즉 이 저장소는 "review 산출물은 절대
불변" 이 아니라 **"안전에 영향을 주는 허위 완료 주장은 정정하되 원문은 보존한다"** 는 더
좁은 규칙을 이미 실천 중이다. 두 케이스의 실질적 차이는 다음과 같다:

- `16_42_07` 케이스: "코드에 반영됐다(뮤테이션 RED 2건)" 는 **커밋 상태에 대한 사실 주장**이
  틀렸다 — 방치하면 이후 라운드가 "이미 고쳐졌다" 고 믿고 재검증을 건너뛸 수 있어 실제로
  살아있는 CRITICAL(스피너 영구 고착)이 한 라운드 더 노출될 뻔했다. **안전-영향(safety-impacting)**.
- `10_02_22` 케이스: 집계표 열 합이 본문 credit 합과 어긋나는 것은 **리뷰 프로세스 자체의
  북키핑 오차**다. 본문 W1~W7 은 내용이 온전히 남아 있어(항목이 사라진 게 아니다 — 카운트만
  어긋난다) 코드 상태에 대한 오판을 유발하지 않는다. **비-안전영향**.

이 구분(안전-영향 여부)이 실제 판단 기준이라면 이번 처분(미정정)은 옳다. 다만 W6 의 문구
"지난 산출물을 사후 편집하지 않고(기록이므로)" 를 **일반 원칙처럼** 적어 두면, 다음에 진짜
안전-영향 오류가 과거 산출물에서 발견됐을 때 "이 저장소는 절대 안 고친다" 는 잘못된
선례로 오독될 위험이 있다 — `16_42_07` 사례와 함께 "심각도 기준으로 가른다" 로 재서술 권장.
과거 산출물(`10_02_22/SUMMARY.md`)을 지금 정정할 필요는 없다고 판단한다.

## 3. 나머지 diff — 표준 8관점 점검

`review/code/2026/08/10/16_09_40`·`16_26_09`(자체 scope.md 가 이미 상세 판정, `git log`/
`git show --stat` 로 후속 3커밋을 원 티켓 CRITICAL/WARNING 반영으로 추적)·`16_42_07`·
`16_56_39`·`18_23_54`~`18_51_07`·`10_02_22` 라운드는 선행 scope 리뷰가 이미 커버했고, 이번
재조사에서 그 판정을 뒤집을 새 근거를 찾지 못했다. 프롬프트에서 diff 가 생략됐던 프로덕션
파일들을 직접 `git diff origin/main...HEAD` 로 열람해 스팟체크했다:

- **CHANGELOG.md**: 신규 두 항목("재로드 REST 오류 분기", "단명 토큰 redaction + 부팅 실패
  전이") 모두 이 티켓의 실제 코드 변경만 서술 — 관례 포맷 준수, 범위 밖 서술 없음.
- **`use-token-refresh.ts`**: 지수 백오프(`retryDelayMs`)·`onRefreshed` 콜백·`isTerminalAuthError`
  분기 전부 §R4/재로드 REST 오류 처리의 직접 종속 표면(주기 갱신이 `refresh_deferred` 복구를
  떠맡는 구조) — 기능 확장이 아니라 이 티켓이 만든 요구사항의 충족.
- **`spec/7-channel-web-chat/3-auth-session.md`**: `status: partial → implemented` 전환 +
  §3.1/§R4 본문 갱신은 구현 완료 사실과 1:1 대응, 새 결정 없음.
- **`spec/0-overview.md`**: 웹채팅 영역 6문서 전부 `implemented` 로 갱신하는 표 1행 — 위와 동일.

`review/code/2026/08/10/16_09_40`~`18_51_07`·`review/code/2026/08/11/10_02_22`·`10_24_54`
디렉터리(리뷰 산출물 자체)가 코드와 함께 커밋되는 것은 `CLAUDE.md` 정보 저장 위치 표 및 선행
scope 라운드(`16_26_09`)가 이미 "스코프 이탈 아님" 으로 판정한 저장소 관례다 — 재론하지 않는다.

무관한 리팩토링·요청 밖 기능 확장·포맷팅과 실질 변경의 혼입·불필요한 주석/임포트·의도치
않은 설정 변경은 이번 재조사에서 발견되지 않았다.

## 발견사항

- **[WARNING]** `10_24_54/RESOLUTION.md` W3 의 "주석 4곳 중 2곳 축약, 나머지는 근거 있어 유지"
  characterization 이 실제 문서 내용과 부분적으로 어긋난다
  - 위치: `review/code/2026/08/11/10_24_54/RESOLUTION.md` (W3 절), 실제 4번째 잔존 지점은
    `codebase/channel-web-chat/src/widget/use-widget-eager-start.test.ts:366`
  - 상세: 본문 §1(c) 참조 — RESOLUTION 이 명시한 "근거"는 이 4곳-dedup 항목이 아니라 별개의
    두 주석 모티프("가드를 한쪽에만"·뮤테이션 생존 메모)에 대한 것이다. 4번째 sessionRef 중복
    지점(테스트 파일)은 근거 없이 방치됐다.
  - 제안: RESOLUTION 문구를 "#4 는 미착수, 별도 근거 없음" 으로 정정(사실 인정)하거나, 근거
    문단을 이 항목에서 명확히 분리해 재작성. 코드 변경 불요 — 문서 정확성 문제.

- **[INFO]** W6("과거 산출물 미정정") 의 rationale 이 이 티켓 자신의 선례(`16_42_07`
  `3d0cec69b` 사후 편집)와 표면적으로 모순돼 보인다
  - 위치: `review/code/2026/08/11/10_24_54/RESOLUTION.md` (W6 절)
  - 상세: 본문 §2 참조 — 실제 구분 기준은 "안전-영향 허위 주장 vs 비-안전영향 북키핑 오차"로
    보이나 W6 문구는 "기록이므로 사후 편집 금지" 라는 절대 규칙처럼 읽힌다.
  - 제안: 문구에 심각도 기준을 명시해 향후 오독 방지. `10_02_22/SUMMARY.md` 자체를 지금
    정정할 필요는 없다(비-안전영향, 본문 내용 자체는 온전).

- **[INFO]** "동작 변경 0"·"뮤테이션 RED 확인"(W1) — 커밋 `8eb223c19` 대조 결과 **정확함**
  확인. 별도 조치 불요(참고용).

## 요약

`10_24_54/RESOLUTION.md` 의 세 처분 주장 중 "동작 변경 0"(실측: 순수 함수 이동+export+주석
편집+테스트 추가뿐, 런타임 로직 무변경, `439 passed`/`0 errors` 직접 재실행 일치)과 "뮤테이션
RED 확인"(신규 회귀가 지목된 뮤턴트를 구조적으로 반드시 잡음, 같은 라운드 `testing.md` 의
scratch 재현과 정합)은 커밋과 대조해 **정확함**을 확인했다. "주석 4곳 중 2곳만 축약(나머지는
근거 있어 유지)" 는 4곳 dedup 자체의 카운트(1 기존 포인터+2 신규 축약+1 잔존)는 맞지만, "근거
있어 유지" 라는 정당화는 실제로는 **다른 주제**(가드 모티프·뮤테이션 메모)에 대한 것이라
이 항목에 오적용됐다 — 4번째 잔존 지점(`use-widget-eager-start.test.ts:366`)은 방치됐을 뿐
근거가 문서화돼 있지 않다. 자기 WARNING(W6, SUMMARY 집계표 불일치) 처분 판단 — 과거 산출물
미정정, 신규 SUMMARY 만 자기정합성 확인 — 은 결과적으로 타당하다(재계산 결과 `10_24_54/
SUMMARY.md` 는 실제로 표-본문 일치, 과거 오차는 비-안전영향 북키핑 문제로 코드 상태 오판을
유발하지 않는다). 다만 그 rationale 을 "기록이므로 절대 불변" 으로 일반화하면 이 티켓 자신의
`16_42_07` 사후정정 선례와 충돌하므로 "심각도 기준" 으로 재서술을 권장한다. 나머지 diff(141
파일, CHANGELOG·production 코드·spec·plan·리뷰 산출물)는 선행 4+회 scope 라운드의 NONE/LOW
판정과 이번 스팟체크 결과가 일치해 새로운 범위 이탈을 발견하지 못했다.

## 위험도

LOW
