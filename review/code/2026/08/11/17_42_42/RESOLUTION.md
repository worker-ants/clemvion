# RESOLUTION — `17_42_42` (+ consistency `17_42_52`)

CRITICAL 0 / WARNING 2 **전부 처분**. `codebase/**` 변경 0줄 — 전부 plan 문서다.

## 같은 실패가 plan 문서 안에서 났다

maintainability 가 잡은 것: `## 후속` 절이 **두 번** 있고, 그 안에 §5-4(오류)와 §2-4(정정)
버전이 **나란히** 살아 있었다. 정정을 새 절로 append 하면서 **원본을 안 지운 것**이다.

고치면서 전수로 세니 **두 곳이 더** 나왔다:

| 위치 | 남아 있던 것 |
|---|---|
| `## 후속` (첫 번째 절) | §5-4 오기 버전 |
| 106행 | "§5-4 는 401 도 요구한다" |
| 122행 | 정정 전 뮤테이션 주장("가짜 앵커 주입 시 RED") |

**세 곳이다.** 리뷰어는 한 곳을 짚었고, 나머지 둘은 그 지적을 받고 세어서 찾았다.
이 PR 이 다루는 주제(자매 자리를 전수로 세기)가 **이 PR 의 회고 문서 안에서** 재발했다.

## 가드 사각지대를 올바른 plan 으로 옮겼다

plan_coherence 가 정확히 짚었다 — `spec-link-integrity` 의 멀티라인 링크 사각지대는
**하니스 결함**이지 이 P3 spec-doc 티켓의 소관이 아니다. 이 티켓이 `complete/` 로 가면
docs-guard 작업자가 못 찾는다.

`harness-review-gate-followups.md`(이미 같은 가드의 다른 결함을 추적 중)로 이관하면서
**실측·뮤테이션 근거·주의사항**을 함께 옮겼다:

> 남은 5건을 "한 줄로 펴서" 해소하는 것은 **증상 처리**다. 근본 해법은 `extractLinks()` 가
> 멀티라인 링크를 보게 하는 것이고, 그때 **지금 침묵으로 통과하던 기존 링크들이 새로 RED 가
> 될 수 있다** — 고치는 PR 은 그 전수를 감당해야 한다.

이 경고를 붙이지 않으면 다음 사람이 "6건 고치면 끝" 으로 읽는다.

## 이번 라운드가 앞 라운드와 달랐던 점 — 리뷰어가 도구를 바꿨다

직전 라운드에서 셋이 잔여를 6/3/12 로 다르게 셌다. 이번엔:

- api_contract 가 **TypeScript AST** 로 새 스캐너를 짰고,
- security 가 **별도 파서**로 독립 재스캔했고,
- convention 이 자기 스캐너의 **방향 버그(오탐 145건)** 를 발견해 고치고 재실행했고,
- testing 이 실제 `spec-links.ts` 를 **번들해 실행**하고 멀티라인 전수는 **CommonMark 파서**로
  다시 셌다.

**세 독립 구현이 전부 잔여 0으로 수렴했다.** 앞 라운드의 수량 불일치는 술어가 아니라
**도구의 정밀도** 차이였음이 드러났다 — 그래서 이번 수렴은 앞 라운드의 "존재는 수렴,
수량은 불일치" 와 성격이 다르다.

## 검증

- testing 이 내 뮤테이션 3줄 표를 **3/3 재현**, 멀티라인 전수 **6/6 일치**,
  backend 전체 **418 suites / 8511 passed / 0 failed**, 변경 19파일 타입·lint **0**.
- rationale 이 `llm-model-config` 주석 반전을 `git merge-base --is-ancestor` 로 검증 —
  **무근거 번복이 아님** 확정(옛 주석은 작성 시점엔 참, P0 fix 가 전제를 깼다).

## 수렴

발견의 성격이 "대상 집합 정의"(라운드 1) → "검증 도구의 정밀도 + 문서 내부 중복"(라운드 2)
으로 옮겨갔고, 이번 fix 는 `codebase/**` 를 한 줄도 건드리지 않았다. 남은 항목은 전부
**올바른 소유자에게** 근거와 함께 등재했다.
