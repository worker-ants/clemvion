# RESOLUTION — `16_19_38` (+ consistency `16_21_15`)

CRITICAL 0 / WARNING 0. 처분한 것은 **INFO 2건**(커밋 `9416da806`).

## 다섯 번째 사례 — 그리고 이번엔 세는 방법이 틀렸다

| 라운드 | 복제된 사실 | 놓친 곳 |
|---|---|---|
| 1 | boot 검증 배선 | 호출부 |
| 2 | "applyConfig 가 진단한다"(거짓) | `safeApiBase` JSDoc |
| 3 | `§R0` → `§R7` 번호 | 같은 커밋이 새로 쓴 JSDoc |
| 4 | "쿼리 경로는 샘플 전용이 아니다" | 코드 주석 2곳 |
| **5** | 위와 같음 | **테스트 주석 1곳** |

4라운드 처분 커밋에 나는 이렇게 썼다 — **"grep 으로 복제본이 정확히 2곳임을 확인했다."**

**그 실측이 틀렸다.** `샘플` 이라는 **문자열**을 셌기 때문이다. 세 번째 복제본은 같은 주장을
다른 말(`direct-load 외부 입력 방어`)로 적어 두었고, 문자열 grep 은 그것을 통과한다.

즉 이번 실패는 "자매를 안 셌다" 가 아니라 **"세었는데 세는 자가 틀렸다"** 다. 문자열은 뜻의
프록시이고, 이 저장소는 프록시로 실측했다가 틀린 이력을 이미 등재해 두었다. 하필 **복제본을
세는 일** 자체에서 같은 오류가 났다.

**주목할 점**: `documentation` 리뷰어도 전수 grep 으로 "복제본 2곳" 을 독립 검증했고 **역시
세 번째를 못 봤다.** 같은 프록시를 썼기 때문이다. 이를 잡은 것은 문자열이 아니라 **뜻**으로
훑은 `rationale_continuity` 였다. 검증자를 늘리는 것이 아니라 **검증 축을 바꾸는 것**이
답이었다.

## 출처가 확정됐다 — 이 정정이 번복이 아닌 이유

`rationale_continuity` 가 `git log -S` 로 판정했다:

- 주석과 그것을 반증하는 SDK `resolveIframeTarget` 이 **같은 커밋(`a652f8733`, #384)** 에서
  함께 태어났고, 그 사이 서술을 바꾼 커밋은 **0건**.
- `git show a652f8733` 로 당시 폴백 코드를 열어 보니 **host 유무를 검사하지 않았다**.

⇒ "당시엔 맞았는데 낡았다" 가 아니라 **처음부터 코드와 어긋난 주석**이었다. #384 는 그런
설계 결정을 내린 적이 없다 — 주석이 있지도 않은 결정을 서술하고 있었고, 한 달 뒤 그것이
spec 오서술이 됐다. 파생본만 고쳤다면 되돌아왔을 것이다.

## 처분

| 출처 | 처분 |
|---|---|
| rationale_continuity INFO | `use-widget.test.ts:15` 정정 + 커밋에 **내 실측 오류를 명시** |
| plan_coherence INFO | plan 에 **라운드 2~5 회고 절** 추가 — 다섯 사례를 표로 고정하고 "문자열 말고 뜻으로 세라" 를 남김 |

`review/` 는 SoT 가 아니다. 회고를 plan 본문에 옮긴 것이 이 처분의 핵심이다.

## 검증

451 passed. 실행 코드 0줄 변경.

## 수렴

코드 7 전원 NONE, consistency 5 전원 BLOCK:NO. 남은 것은 INFO 2건이었고 둘 다 처분했다.
발견의 성격이 배선 → 정정 누락 → 참조 정합 → 처분 확인 → **세는 방법**으로 끝까지 얕아졌다.
