# RESOLUTION — `01_10_52`

리뷰 결과: **CRITICAL 0 / WARNING 1 / RISK LOW**. reviewer 7명 실행, 강제 7명 전원 결과 확보
(`forced_missing: []`, `unfinished: []`). **WARNING 1건 + INFO 1건 조치.**

---

## WARNING #1 (documentation) — plan 노트가 같은 문단 안에서 자기모순 → 조치

"경계 테스트 13건" 이라 적어 놓고, **그 바로 다음 문단들이** 두 건의 후속 추가(중복 헤더 조인
문자열 · `99` 인접 경계)를 서술한다. 같은 diff 안에서 앞 숫자와 뒤 설명이 어긋났다.

**개수를 직접 실측했다** — 선언은 9개지만 `it.each` 전개 후 실행 단위는 15건이다:

```
$ jest -t "readKey / hashBody 경계값"
Tests: 41 skipped, 15 passed, 56 total
```

**조치**: 15건으로 정정하고 **무엇을 세는 숫자인지**(`it.each` 전개 기준, 선언은 9개)와
**실측 방법**을 함께 적었다. 초안 13건에서 리뷰 라운드에 2건이 늘어난 경위도 남겼다.

> 이 세션에서 "수량을 프록시로 셌다" 는 실패가 반복됐다. `it` 선언을 세면 9, 실행 단위로 세면
> 15 — 어느 쪽도 틀린 게 아니라 **무엇을 세는지 안 밝히면 다음 사람이 검증할 수 없다.**

## INFO #11 (testing) — `hashOf` 가 사전 단언 없이 `calls[0]` 인덱싱 → 조치

회귀 시 `TypeError` 스택트레이스만 남아 "적재가 안 됐다" 인지 "hash 가 다르다" 인지 구분되지
않는다. 파일 내 유사 자리는 `toHaveBeenCalledTimes(1)` 을 먼저 단언하는데 이 자리만 빠졌다.

**조치**: 두 stub 각각에 `toHaveBeenCalledTimes(1)` 선단언 추가.

---

## 나머지 INFO 처분

| # | 항목 | 처분 |
|---|---|---|
| 1 | `isHttpStatusCode` 범위(100~599)가 실제 캐시 대상보다 넓음 | **유지** — 닫힌 목록은 `isErrorStatusCacheable` 담당. 여기서 좁히면 같은 규칙을 두 곳이 반씩 알게 된다 |
| 4 | 호출부가 `readKey` 의 "빈 문자열 미반환" 불변식에 의존하게 됨 | **수용** — 그것이 이번에 의도한 책임 분리다. JSDoc 에 계약을 명시해 뒀고, 그 불변식이 깨지면 테스트가 죽는다(뮤테이션으로 확인) |
| 12 | `makeContext` 가 "헤더 빈 문자열" 과 "헤더 부재" 를 구분 못 함 | **유예** — 현재 `readKey` 가 둘 다 `null` 로 처리해 관측 가능한 차이가 없다. 구분이 필요한 로직이 생기면 `'idempotencyKey' in opts` 로 전환 |
| 9·10 | spec 파일 길이 · `warnSpy` 보일러플레이트 | **유예 유지** — 직전 라운드에서 유예했고 이번 diff 가 규모를 늘리지 않았다 |
| 2·3·5·6·7·8·13 | 확인 기록 | 조치 불요 |

## 검증

- eslint **0/0** · 인터셉터 **56/56** · backend unit **418 suites / 8564 passed**
- e2e **47 suites / 269 passed**
