# RESOLUTION — 세션 `12_37_46` (백로그 잔여 3건)

CRITICAL 0 / WARNING 1. **조치 완료** — 그리고 그 WARNING 이 이 PR 의 요지를 그대로 되돌려
가리켰다.

## WARNING 1 — SoT 를 옮기면서 자매 크로스레퍼런스를 놓쳤다

**조치 완료.** 이 PR 은 "`cafe24:install:*` 키의 normative 정의가 Rationale 절에 있다" 를
고치려고 본문 §4.4 를 신설하고 `redis-keys.md` 인벤토리 포인터를 `§9.8 → §4.4` 로 옮겼다.
그런데 **같은 사실을 가리키는 다른 문서**를 갱신하지 않았다 —
`spec/2-navigation/4-integration.md:1294` 가 여전히 *"키 구성·degradation 의 SoT 는 §9.8"* 이라
적고 있었다.

즉 **두 문서가 서로 다른 절을 SoT 로 지목하는 상태**를 내가 만들었다. 이 PR 이 없애려던 문제
클래스(포인터가 엉뚱한 곳을 가리킴 / 이중 SoT)의 새 인스턴스다. requirement·documentation
두 reviewer 가 독립적으로 같은 지점을 짚었다.

1294행을 **역할별로 갈라** 고쳤다:

- **키 구성·TTL·degradation** → §4.4 (normative)
- **상수 값·설계 근거** → §9.8 (Rate limiting note + "관련 코드 상수" 표)

같은 파일의 `:664`·`:808`·`:858`·`:1442` 인용은 알고리즘·rate-limiting 서술 자체를 가리키므로
§9.8 이 그대로 유효하다(reviewer 확인). 전수로 보고 이 한 줄만 고쳤다.

## 조치하면서 내가 만든 것을 하나 더 발견했다

리뷰가 짚지 않은 것인데, §4.4 표에 `INSTALL_FAIL_THRESHOLD`**(10)** · `INSTALL_FAIL_WINDOW_SEC`
**(600초)** 처럼 **값을 적었다.** 그 값들은 §9.8 "관련 코드 상수" 표가 SoT 다 —
**이 PR 의 3번 항목(`cc:dedup` 이중 SoT 제거)에서 방금 없앤 형태를 같은 PR 에서 다시 만든
것이다.**

§4.4 에서 값을 빼고 **이름으로만** 참조하도록 고쳤고, 왜 그러는지도 적었다:
*"값을 양쪽에 적으면 그 둘이 어긋날 자리가 생긴다."*

> 같은 PR 안에서 같은 결함을 없애고 다시 만들었다. 원인은 "새 절을 쓰면서 읽는 사람이 편하도록
> 값을 인라인" 한 것 — 편의가 곧 중복의 동기다. **normative 절을 새로 만들 때는 "이 절이
> 소유하는 사실" 과 "다른 절에서 빌려온 사실" 을 갈라야 한다.**

## 조치하지 않은 INFO

| INFO | 처분 |
|---|---|
| 1 CCH-SE-02 에 fail-open 함의 문구가 새로 붙음 | 무조치 — 요구사항 행에서 메커니즘을 빼면서 "fail-open" 만 남으면 그 뜻이 사라진다. "그 구간엔 중복 처리가 가능하다" 는 **요구사항의 함의**이지 구현 세부가 아니다 |
| 2 §4.4 가 "§4 실행 로직"(노드 handler) 아래라 관심사가 섞임 | 유예 — 지적이 맞다. install endpoint 는 노드 실행이 아니라 OAuth 설치 플로우다. 다만 새 최상위 절을 만들면 §5~§9 번호가 전부 밀리고 그 앵커를 인용하는 문서가 여럿이다(§9.8 만 4곳). **앵커를 세 번 깨뜨린 브랜치**라 번호 이동은 별건으로 둔다 |
| 3 코드 변경이 docstring 2줄뿐이라 회귀 위험 없음 | 확인 — 조치 불요 |

INFO 2 는 plan 에 후속으로 등재했다.

## 검증

- eslint **0/0** · hooks **5 suites / 97 passed**
- `spec-link-integrity` **13/13** (SoT 재배치 2회 모두 재실행)
- `plan-frontmatter` **137/137**
