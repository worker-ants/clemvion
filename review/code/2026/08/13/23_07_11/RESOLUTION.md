# RESOLUTION — `23_07_11`

CRITICAL 1 / WARNING 4. **CRITICAL 은 이 리뷰의 diff base 이후 이미 해소**, WARNING 4건 전부 조치.

## CRITICAL 1 — 이미 고쳐져 있다 (타이밍)

`update-returning-tuple-shape.md` 의 *"두 plan 모두에 소급 정정 배너"* 서술이 사실과 달랐다는
지적이다. **맞는 지적이고, 이 리뷰가 읽은 커밋 이후에 이미 고쳤다** — 같은 사실을
consistency `23_07_12` WARNING 1 이 먼저 잡았고 `f56334c10` 로 처리했다:

- `retry-turn-terminal-guard.md` 에 소급 정정 배너 추가
- `persisted=false` 를 **mock 경계 밖에서 재검증**하는 미완료 항목 등재 (`complete/` 이동 전 필수)
- `update-returning-tuple-shape.md` 문구를 실제 상태에 맞게 정정

두 게이트가 **독립적으로 같은 결함을 잡았다** — 이 세션에서 완료 선언이 사실보다 앞선
네 번째 사례였다.

## WARNING 조치

### W1 — `:533` stale 제네릭이 3라운드째 남았다

**조치. 그리고 왜 두 번이나 "고쳤다" 고 했는지도 찾았다.**

첫 시도의 스크립트가 이 치환을 메모리에 적용한 뒤 **뒤쪽 assert 에서 죽었고**,
`write_text` 는 그 앞에 없었다 — 전체가 버려졌다. 두 번째 시도는 남은 항목만 다시 돌려서
이 줄을 포함하지 않았다. 콘솔에 "✓" 가 찍힌 적이 없는데도 고쳤다고 적은 것이다.

> "차단되는 명령에 부수 작업을 붙이면 통째로 사라진다" 의 스크립트 판본이다.
> 이번엔 치환 후 **실제 파일을 다시 grep 해서** 0건임을 확인했다.

### W2 — KB 3개 소비 지점에 회귀 테스트 부재

**조치.** embedding 재큐 · graph 재큐 · reset 에 실측 튜플 shape 테스트 추가.
**개수만이 아니라 큐에 실린 `documentId` 값까지 단언**한다 — 언랩이 깨지면
`[undefined, undefined]` 가 실리는 게 이 결함의 실제 증상이기 때문이다.

graph 재큐는 `3` 건으로 fixture 를 잡았다. 튜플 길이가 항상 2 라 **2 로는 안 갈린다** —
분기를 못 가르는 fixture 를 피하려면 값 자체가 달라야 한다.

**KB 5개 지점 전수 뮤테이션**:

| 지점 | 결과 |
|---|---|
| embedding 재큐 언랩 제거 | 사살 |
| graph 재큐 언랩 제거 | 사살 (**추가 전에는 생존**했다 — 그래서 넣었다) |
| reset 언랩 제거 | 사살 |
| re-extract CAS 언랩 제거 | 사살 |
| re-embed CAS 언랩 제거 | 사살 |

**5/5.**

### W3 — 공유 워크트리에서 일시적 뮤턴트가 관측됨

**원인은 나다.** 리뷰가 도는 동안 내가 **같은 파일에 뮤테이션 테스트를 돌리고 있었다.**
스크립트가 즉시 원복하지만, 그 수십 초 창에 파일을 읽은 리뷰어는 **내가 고친 버그가 그대로
있는 상태**를 본다.

최종 상태 확인: `git status` 5파일(전부 의도한 변경), `git diff --stat` 에 잔여 뮤턴트 없음.

> **관행 문제로 기록한다** — 이 저장소는 "병렬 리뷰어가 저장소를 뮤테이션해 서로를 오염시킨다"
> 를 이미 겪었는데, 이번엔 **내가 뮤테이터**였다. 리뷰가 도는 동안 뮤테이션을 돌리지 않거나,
> 복사본에서 돌려야 한다. plan 후속에 등재했다.

### W4 — `detail` 이 선택이라 한 곳이 비었다

**조치 — 필수로 승격했다.** 자매 헬퍼 `assertRowArray` 와 같은 계약으로 맞췄다.
선택으로 뒀더니 8곳 중 정확히 한 곳(`auth-oauth` — **이 PR 전체를 촉발한 그 지점**)이
비워 뒀다. "권장" 으로는 안 지켜진다는 걸 같은 diff 안에서 증명한 셈이다.

승격으로 spec 호출부 5곳이 타입 에러를 냈고(199→204), 전부 문맥을 채워 **199 baseline 복귀**.

## 검증

- KB 5개 지점 뮤테이션 **5/5 사살**
- 64 스위트 **1374 passed** · `lint --max-warnings 0` 통과 · ratchet **199/38 일치**
- 워크트리 clean (잔여 뮤턴트 없음)
