# RESOLUTION — `18_38_10`

CRITICAL 0 / WARNING 1. **WARNING 의 실질을 조치했고, 여기서 리뷰 루프를 닫는다.**

## WARNING 1 — 무조치 결정이 SoT 에서 추적 불가능 (documentation)

**지적이 맞다.** 이 WARNING 은 두 부분인데, 무게가 다르다:

**(a) 절차 결함 — 조치 완료.** 나는 `chat-channel.dispatcher.spec.ts` 스타일 4건을
"실동작 0 이니 넘긴다" 고 판단해 놓고 **plan 에는 캐스트 1건만 적었다.** 나머지 3건의
무조치 결정이 `review/**` 안에만 남았고, `review/**` 는 SoT 가 아니다.

→ `backend-lint-gate-broken-on-main.md` 에 **4건 전부 + 공통 무조치 사유 + 라운드 이력**을
등재했다. 왜 4라운드나 살아남았는지도 함께 적었다: 매 라운드 판단은 옳았는데 **기록을
안 해서 다음 라운드가 새 발견처럼 다시 올렸다.** 무조치도 결정이고, 결정은 기록돼야
반복 비용이 0 이 된다.

**(b) JSDoc 위치 이동 — 등재 후 유예.** 리뷰어의 지적 자체가 *"3라운드째 유예됐다"* 이지
*"유예가 틀렸다"* 가 아니다. 실동작 영향은 0 이고, 손대면 `codebase/**` 가 바뀌어 리뷰가
다시 stale 해진다(가드는 "리뷰 세션 시각 > 최신 코드 변경" 을 요구). **주석 한 블록의
위치 때문에 6라운드째를 도는 것은 비용이 편익을 넘는다.** 다음에 이 파일을 실질 변경할 때
4건을 한 번에 정리한다 — 그 결정이 이제 plan 에 있다.

> 이번 조치가 `plan/` 만 건드리고 `codebase/**` 를 안 건드린 것은 **의도적**이다.
> WARNING 의 실질(추적 불가능)은 plan 등재로 해소되고, 코드를 건드리면 그 순간
> 이 리뷰가 stale 해져 또 한 라운드가 강제된다.

## 수렴 판정 — 루프를 닫는다

| 라운드 | Critical | Warning | 발견의 성격 |
|---|---|---|---|
| `17_15_21` | 0 | 2 | **동작** — 자매 미적용(1곳 fail-open), routing 미해제 |
| `18_00_11` | 0 | 1 | **구조** — 가드 boilerplate 중복 |
| `18_19_33` | 0 | 1 | **문서** — 주석의 세션 인용 오류 |
| `18_38_10` | 0 | 1 | **절차** — 유예 결정의 미등재 |

동작 → 구조 → 문서 → 절차로 단조 하강했고, **프로덕션 코드 발견은 3라운드 연속 0** 이다.
남은 것은 4라운드 내내 같은 판단으로 유예해 온 스타일 4건뿐이고, 그 판단은 이제 SoT 에
기록됐다. 이 저장소가 남긴 기준("Critical 0 + 코드 발견 0 이면 doc-루프 금지")에 해당한다.

## 검증

이번 turn 의 변경은 `plan/` 1파일 + `review/` 산출물뿐이다 — `codebase/**` 무변경이라
`18_38_10` 이 검증한 코드가 그대로다. 직전까지의 실측(뮤테이션 6/6 사살,
`tsc --noEmit` 199 = baseline, `lint --max-warnings 0` 통과, 관련 스위트 497 passed)이
그대로 유효하다.

## INFO 처분

| # | 처분 |
|---|---|
| 3, 4, 12 | 리뷰어가 "이미 정확히 문서화됨 / 조치 불요" 로 결론 — 동의 |
| 1, 2, 6, 10, 11 | 조치 불요. #1(테스트-모듈 결합)·#10(array-like 미커버)은 이미 주석에 트레이드오프로 명시 |
| 7, 8, 9 | **plan 등재 완료**(위 (a)). 무조치 유지 |
| 5 | `reRun` 이 이 조건에서 500 을 낸다 — 그게 **의도**다. 종전엔 조용히 제한을 우회했다. 더 진단 가능한 코드로 노출할지는 별건(§후속 등재됨) |
| 13 | worktree 슬러그 불일치 — 값은 참이고 "고치면" 거짓이 된다. EIA draft 에 같은 메모를 이미 남겼다 |
