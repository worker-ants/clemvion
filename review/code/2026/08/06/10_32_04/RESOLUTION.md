# RESOLUTION — 4R (harness-review-ci-backstop)

리뷰어 14/14 성공. **CRITICAL 4 / WARNING 4.** RISK=CRITICAL.
**9명 이상이 독립적으로 같은 클래스의 우회를 실측 재현**했다.

> 첫 4R 시도는 15개 에이전트 전원이 사용량 한도로 죽어 리포트가 0건이었다. 남은 것은 리뷰어가
> 워크트리에 흘린 mutation 잔여 한 줄(`continue-on-error: true`) 뿐이었고, 되돌리기 전에 그
> 상태로 스위트를 돌려보니 **GREEN** 이었다 — 그 한 줄이 그 라운드의 유일한 산출물이 됐고,
> 곧바로 닫았다(`864b71a7b`). 이번 라운드는 그걸 알려진 구멍으로 명시해 넣고 재실행한 것이다.

## 한 문장으로: 부분집합에 대한 정확 일치는 여전히 부분 일치였다

3R 에서 "패턴 매칭을 버리고 정확 일치로" 반전했는데, **내가 고정한 필드만** 안전했다.
리뷰어들이 옆의 열린 필드로 네 갈래로 뚫었다.

| # | 우회 | 왜 통과했나 | 결과 |
|---|---|---|---|
| C1 | `jobs.gate.continue-on-error: true` | step 에서만 막고 **job 은 `if` 하나만** 봤다 | 게이트가 exit 1 을 내도 job 이 성공 보고 |
| C2 | `on.pull_request.branches: ['없는-브랜치']` | `paths` 만 고정하고 **형제 키는 열어뒀다** | **enforce 무관, 지금 당장** 영원히 트리거 안 됨. Actions 탭에 기록조차 안 남아 감지 불가 |
| C2 | `types: [closed]` | 동상 | PR 이 닫힐 때만 돌아 리뷰 시점엔 개입 못함 |
| C3 | step 목록에 스텁 덮어쓰기 / `$GITHUB_PATH` 하이재킹 삽입, 기존 step `run` 에 `&& …` | **개별 step 만** 보고 목록(개수·순서)은 안 봤다 | `run` 문자열은 그대로인데 스크립트가 안 돎 |

매 라운드 "발견된 그 필드" 를 추가로 고정하는 것으로 대응했고, 매번 옆에서 다시 뚫렸다.
**열거로는 끝나지 않는다.**

**처분: 파싱된 워크플로 문서 전체를 하나의 기대값과 비교.** 어디에 무엇을 더하든 빼든 바꾸든
실패하고, 남는 열린 필드가 없다. `WorkflowWiringTest` 8개 테스트를 2개로 접었다 — 문서 전체
일치 1건 + **기대값 자체가 만족해야 할 성질** 1건(기대값을 게이트가 꺼진 모양으로 함께
고쳐버리는 것을 사람이 마주치게 하는 목록).

## C4 — 이 클래스 전체가 옵션 의존성 하나로 무력화됐다

`setUpClass` 가 `import yaml` 실패 시 `SkipTest` 로 조용히 스킵했다. 이 저장소가 실제로 쓰는
관행(**실패 reviewer 만 타겟 재실행**)에서 PyYAML 이 없으면 위 배선 불변식 전부가 무음 `OK` 로
통과한다. 전체 스위트가 안전했던 건 이 클래스의 설계가 아니라 **무관한 옆 파일**이 우연히
fail-closed 인 덕이었고, 문서화도 테스트도 안 된 결합이다.

**처분: fail-closed.** `try/except` 를 제거해 부재 시 ImportError 로 클래스가 죽는다.
실측 확인 — `yaml` 을 못 찾게 만들고 이 클래스만 돌리면 `exit 1`, skip 아님.

## WARNING

| # | 내용 | 처분 |
|---|---|---|
| W1 | `Fetch base ref` step 을 통째로 지워도 GREEN 이었다 | **문서 전체 고정으로 닫힘**(삭제가 실패). 다만 그 step 이 `fetch-depth: 0` 위에서 실제로 필요한지는 **GH Actions 러너 없이 실측 불가** — 판정하지 않고 plan 에 열린 질문으로 남겼다 |
| W2 | `ReviewDecision` 계약이 세 소비자·두 스텁 사이 duck-typing | 미처분 — `Protocol` 도입은 세 훅에 걸친 별도 범위. 행위 테스트가 이미 커버 |
| W3 | plan 이 "2026-08-01 구현 완료" 에서 멈춰 1R~4R 경화 이력이 없다 | 라운드별 표 + 4R 결론 + 열린 질문 추가 |
| W4 | `pyyaml>=6,<7` pin 이 세 곳에 손으로 중복, 묶는 테스트 없음 | 세 워크플로의 pin 이 하나로 수렴하는지 보는 가드 추가 |

## 검증

- harness 스위트 **826 tests OK** (830 → `WorkflowWiringTest` 8개를 2개로 접고 pin 가드 +1).
- mutation **5종 전부 RED** — 전부 리뷰어가 GREEN 을 실증했던 형태다:
  `job continue-on-error` · `branches` 형제 키 · `types: [closed]` ·
  게이트 앞 스텁 덮어쓰기 step 삽입 · 기존 step `run` 에 `&& hijack` 추가.
- C4 는 별도 실측: `yaml` 부재 시 `Ran 0 tests / FAILED (errors=1)`, skip 아님.

## 남은 판단

문서 전체 고정은 "어디를 바꿔도 실패한다" 를 보장하지만, **기대값과 실제를 함께 고치는 것**은
막을 수 없다(어떤 테스트도 못 막는다). 그건 리뷰가 볼 자리이고, 그래서 기대값 옆에
"이게 게이트를 끄는 변경인가" 를 묻는 성질 목록을 같이 뒀다.
