해당 없음

변경된 50개 파일은 모두 프론트엔드 i18n(국제화) 구현에 해당합니다. 하드코딩된 문자열을 `t()` 번역 함수 호출로 교체하고, 로케일 스토어를 추가하며, 테스트에 로케일 초기화를 추가하는 내용입니다. 백엔드 API 엔드포인트, 요청/응답 구조, HTTP 상태 코드, 인증/인가, URL 설계 등 API 계약과 관련된 변경사항은 없습니다.

README에 `GET /users/me` → `user.locale` 필드 사용에 대한 설명이 추가되었으나, 이는 기존 API 동작에 대한 문서화일 뿐 계약 변경이 아닙니다.

### 요약
이번 변경은 순수 프론트엔드 UI 텍스트의 i18n 처리로, API 계약과 무관합니다. 백엔드와의 인터페이스에는 어떠한 영향도 없습니다.

### 위험도
NONE