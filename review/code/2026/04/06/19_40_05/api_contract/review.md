해당 없음

이 코드는 `SwitchHandler`의 내부 실행 로직과 설정 유효성 검사를 다루는 단위 테스트 파일입니다. HTTP API 엔드포인트, 요청/응답 계약, 인증/인가, URL 설계 등 API 계약과 관련된 요소가 전혀 포함되어 있지 않습니다.

### 요약
워크플로우 실행 엔진 내부의 Switch 노드 핸들러 동작을 검증하는 순수 단위 테스트로, 외부 API 인터페이스와 무관합니다.

### 위험도
NONE