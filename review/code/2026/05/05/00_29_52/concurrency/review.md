### 발견사항

해당 없음

이 변경사항은 순수 UI 렌더링 로직입니다. `summarizeToolResult` 헬퍼 함수, `ToolStatusIcon` 컴포넌트, 그리고 `SummaryView` 내 tool 아이템의 컴팩트 라인 렌더링이 추가되었으며, 모두 단방향 데이터 흐름 위에서 동작하는 동기적 렌더 로직입니다. 공유 가변 상태 접근, 비동기 처리, 타이머, 외부 리소스 접근이 없습니다.

### 요약

변경된 코드는 React의 단방향 데이터 흐름 모델을 따르는 순수 UI 컴포넌트로, 동시성 이슈가 발생할 수 있는 공유 상태 변이, 비동기 사이드 이펙트, 타이머, 이벤트 루프 블로킹 요소가 없습니다. `baseProps`의 `vi.fn()` mock 재사용은 테스트 격리 관점의 주의사항이나 동시성 문제는 아닙니다.

### 위험도
NONE