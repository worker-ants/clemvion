해당 없음

이 변경사항은 전적으로 **프론트엔드 UI 레이어**에 해당합니다. 데이터베이스와 직접적으로 관련된 코드가 존재하지 않습니다.

- 변경 내용: `/profile` 페이지의 readonly + 위험 비례 편집 패턴 구현 (React 컴포넌트, RTL 테스트, i18n 사전, spec/plan 문서)
- 백엔드 API 호출(`PATCH /users/me`, `POST /users/me/change-password`)은 기존 엔드포인트를 그대로 사용하며, 본 PR에서 백엔드 코드·스키마·마이그레이션은 변경 없음

### 요약

모든 변경 파일이 프론트엔드 컴포넌트(TSX), 테스트 파일, 국제화 사전(TS), 명세 문서(MD)에 국한되어 있어 데이터베이스 관련 검토 대상이 없습니다.

### 위험도
NONE