해당 없음

이번 변경사항은 LLM 서비스 호출 핸들러의 에러 케이스 메타 필드 보강 (텍스트 분류 노드), 관련 단위 테스트 추가, plan 문서 상태 갱신, spec 문서 정정으로 구성되어 있습니다. 데이터베이스 쿼리, 스키마 마이그레이션, ORM, 커넥션 풀 등 데이터베이스와 관련된 코드는 포함되지 않습니다.

### 요약
변경된 코드는 전적으로 LLM API 호출 에러 경로에서 `meta.durationMs`, `meta.model`, `meta.llmCalls` 필드를 채우는 로직과 이를 검증하는 테스트, 문서 업데이트에 한정되며, 데이터베이스 레이어와 접점이 없습니다.

### 위험도
NONE