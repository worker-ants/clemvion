### 발견사항

- **[INFO]** 이번 변경(§F 잔여 W3 — e2e paths-ignore ↔ PROJECT.md 화이트리스트 동기 가드 구현)과 무관한 별개 백로그 항목(§E — fail-open 정책 관측성)의 사용자 결정 기록이 같은 diff 에 섞여 있음
  - 위치: `plan/in-progress/harness-guard-followups.md:220`~`225` (§E 섹션, "사용자 결정 (2026-07-23): '관측 가능하게'" 및 신규 체크박스 "구현 — 3개 fail-open 경로…"), 그리고 `plan/in-progress/harness-guard-followups.md:333` (체크리스트의 "E — fail-open 관측 가능하게 (정책 결정 완료 2026-07-23, 구현 대기)")
  - 상세: 실제 코드 산출물(`test_e2e_exemption_paths_sync.py`)은 §F 잔여의 **W3** 항목(e2e 면제 화이트리스트 동기화 가드)을 구현한 것이고, 같은 plan 파일에서 그 항목(라인 276~287, "W3(16_02_39, 신규) — ✅ 완료")을 갱신한 것은 이 diff 의 코드와 직접 대응해 스코프 내다. 반면 §E 섹션(220~225 행)과 하단 체크리스트(333 행)는 **완전히 다른 백로그 항목**(REVIEW/PLAN 게이트의 fail-open 정책 관측성)에 대한 사용자 결정 기록이며, 아직 구현도 되지 않은 상태(체크박스 `- [ ]`)로 이번 코드 변경과 아무 관련이 없다. 두 개의 독립적인 백로그 항목에 대한 plan 갱신이 한 changeset 에 뒤섞여 있다.
  - 제안: 실질 위험은 낮음(코드가 아닌 plan 문서 갱신이라 실행에 영향 없음)이나, 리뷰 대상 changeset 의 범위를 "이 PR 이 실제로 구현한 것"으로 좁히려면 §E 관련 결정 기록은 별도 커밋/PR(또는 그 항목을 실제로 착수하는 PR)로 분리하는 것이 changeset 추적성 면에서 낫다. 다만 같은 세션에서 여러 백로그 항목의 결정을 한 번에 plan 에 반영하는 것이 이 저장소의 기존 관행(다른 완료 이력에서도 여러 항목을 한 plan 파일에 모아 갱신)과 부합한다면 허용 가능한 수준.

### 요약

핵심 코드 산출물은 `.claude/tests/test_e2e_exemption_paths_sync.py`(신규 파서+가드 테스트, W3 항목 구현) 하나이며, `.claude/tests/README.md` 카탈로그 행 추가와 plan 파일의 W3 완료 표시는 그 산출물과 정확히 대응해 스코프 내다. 새 테스트 파일 자체는 (긴 docstring·양방향 검사·파서 경계 fixture 테스트 등) plan 이 미리 명시한 설계를 그대로 구현한 것으로 과설계나 요청 밖 기능 확장으로 보기 어렵고, import 패턴(`import _harness  # noqa: F401`)도 기존 테스트 파일들과 동일한 관례를 따른다. 포맷팅·불필요한 리팩토링·무관한 코드 영역 수정은 발견되지 않았다. 유일한 스코프 이탈 소지는 plan 파일에서 이번 코드와 무관한 §E(fail-open 관측성 정책) 결정 기록이 같은 diff 에 함께 커밋된 점으로, 코드에는 영향이 없는 문서 수준의 부수 변경이라 위험도는 낮다.

### 위험도
LOW
