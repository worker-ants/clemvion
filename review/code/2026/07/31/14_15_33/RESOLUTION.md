# RESOLUTION — 14_15_33 (5차 라운드) — **수렴 종료**

**CRITICAL 0 / 전체 MEDIUM** — 전 시리즈 최초의 CRITICAL 0. 14개 reviewer 전원 결과 확보,
forced 화이트리스트 7명 미이행 없음, skip·미완 0건.

## 수렴 판정 — 발견의 성격으로

| 라운드 | CRITICAL | 성격 |
|---|---|---|
| 1R | 2 | 동작 — 크래시 위험(`_default_branch_ref` 예외 누출) + 프롬프트 조립 무표시 누락 |
| 2R | 1 | 동작 — 생략 안내가 자기 예산을 안 씀(상한 위반) |
| 3R | 1 | 동작 — 그 예약 전략이 규모에서 무너짐 |
| 4R | 3 | **위생/적용 누락/테스트 인프라** — 핵심 로직 결함 0 |
| **5R** | **0** | 구조·문서·성능 미세 회귀만 |

1R~3R 의 CRITICAL 은 전부 같은 수정의 **규모·경계 정교화**였고, 4R 에서 로직 결함이 사라진 뒤
5R 에서 CRITICAL 이 0이 됐다. 남은 WARNING 11건은 구조(함수 분해·wrapper 분리·중복 4곳),
추적 중인 defer, 문서 drift, 그리고 내가 만든 I/O 미세 회귀 1건이다.

**여기서 코드를 더 건드리지 않는 이유**: 이 저장소가 이미 겪은 treadmill이다. 문서 결함을
고치면 다음 라운드가 다른 문서 결함을 낸다. 남은 것 중 **기능에 영향하는 항목은 없고**, 전부
근거째 plan 에 등재돼 다음 착수 때 전제로 쓸 수 있다.

## 이번 라운드 조치 — 문서·주석 전용 3건

| # | 처분 | 내용 |
|---|---|---|
| W11 | **수정** | 거짓 불변식("suppresses the gate")의 **네 번째 자리**가 `test_review_guard_hardening.py` 모듈 docstring 에 남아 있었다. 저장소 전체 grep 으로 이 한 곳뿐임을 확인하고 정정 — 이 PR 이 존재하는 이유가 바로 그 문구가 거짓이었다는 것이라, 읽는 사람이 방금 고친 오류를 다시 배우게 둘 수 없다 |
| W10 | **수정** | plan 의 테스트 건수가 **또** 어긋났다(13 vs 실측 18, 11 vs 실측 12). 3회째 재발이라 이번엔 수치와 함께 "라운드마다 증가 — 파일이 SoT" 를 병기해 다음 갱신 누락이 거짓이 되지 않게 했다 |
| W1 | **defer + 등재** | `_rank_plan_text` 이중 read — **내가 만든 I/O 회귀**. 실측 30개 430,929 bytes(≈3.5ms)라 무해하지만 내 것이므로 #7 로 등재 |

## defer (전부 plan 등재, 근거 포함)

`build_files_section` 예산 전략 4갈래 재구현(3R CRITICAL 의 근본 구조, **2라운드 연속 지적 —
우선순위 상향 권고**) · `evaluate_review` boolean flag 구조 · 기본 브랜치 해석 4곳 중복 ·
git 브랜치-diff 헬퍼 중복 · 하향 금지 정책의 기계적 backstop 부재 · `_default_branch_ref`
성공 경로 미검증 · fresh-interpreter 보일러플레이트 4중 복제 · diff-only 분기 상한 초과(기존) ·
헤더만으로 상한 초과하는 구조적 한계(P3).

## 리뷰어가 확인해 준 것

핵심 결함(`evaluate_review` 의 in-flight 억제가 무조건 적용돼 push 하드게이트가 최대 30분
무리뷰로 열리던 접근통제 결함)이 **3중으로 정상 반영 확인**됐다 — security 의 코드 추적,
testing 의 **실제 뮤테이션**(가드를 되돌려 회귀 테스트가 RED 로 전환됨을 직접 확인),
concurrency 의 구조 분석.

## 내가 이 시리즈에서 틀린 것 (기록용)

1. **1R 프롬프트 누락 판정** — `in_flight_ok` 를 grep 해 "14개 전원에 있으니 실렸다" 고 했는데,
   그 토큰은 **다른 파일들**에 있었다. 파일 경계를 넘는 토큰을 센 프록시 측정. 섹션 단위로
   다시 재고서야 확정됐다.
2. **2R 커밋** — 테스트 1건이 실패했는데 커밋이 그대로 진행됐다(명령을 줄바꿈으로 이어 붙여
   실패가 커밋을 막지 못했다). 원인은 코드가 아니라 stale `__pycache__` 였다.
3. **3R mutation 판정** — `if True:` 치환이 `except` 를 고아로 만들어 SyntaxError 를 냈는데
   그걸 RED 로 셀 뻔했다. 형태 보존 뮤턴트로 다시 돌려 확정.
4. **4R 커밋 오염** — 프로브 파일이 커밋됐다. 정리 `rm` 이 2분 타임아웃에 잘렸고 이어서
   `git add -A` 로 쓸어 담았다. 메모리에 이미 적혀 있던 패턴을 그대로 반복했다.
5. **5R 준비** — `origin/main` 이 움직인 걸 모르고 two-dot diff 를 써서 남의 변경 15개가
   내 리뷰 대상에 섞였다. three-dot 으로 정정.

## 검증

- **harness 스위트**: 702 tests OK (문서 전용 수정 후 재확인).
- **mutation**: 라운드 누적 16종 전부 RED. 호출부 pass-through·형태 보존·예산 예약 제거·
  `--staged` 면제 해제 등 각 가드마다 개별 확인.
