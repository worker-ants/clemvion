### 발견사항

- **[WARNING]** `9-rag-search.md` — 자체 consistency-check(`convention_compliance`, `23_36_57`)가 지적한 "D1 `status: partial` 유지 판단의 근거 미기록" 이 최종 diff에도 미반영
  - 위치: `spec/5-system/9-rag-search.md` (frontmatter `pending_plans` 만 갱신, `## Rationale` 절 무변경) / 비교 대상 `review/consistency/2026/07/16/23_36_57/convention_compliance.md` WARNING#2
  - 상세: 이번 grooming PR에 포함된 자체 consistency-check 산출물(`review/consistency/2026/07/16/23_36_57/`)이 "D1(rag-dynamic-cut 종결 후에도 `9-rag-search.md` `status: partial` 을 유지하기로 한 판단)의 근거가 spec 문서 자체의 `## Rationale` 에는 기록되지 않았다"를 명시적 WARNING으로 지적하고 "`## Rationale` 에 '왜 rag-dynamic-cut 종결 후에도 status: partial 을 유지했나 (2026-07-16)' bullet 추가 권장"이라는 구체적 수정을 제안했다. 그런데 실제 병합된 `spec/5-system/9-rag-search.md` diff(파일 25)는 `pending_plans` 필드 갱신과 두 곳의 인라인 plan 링크 교체만 수행했을 뿐, `## Rationale` 에 해당 bullet을 추가하지 않았다(`grep "status.*partial\|왜.*partial"` 로 확인 — 해당 절 없음). 대비되는 D2(`11-mcp-client.md`, 파일 24)는 같은 리뷰가 지적한 동일 종류의 요구(won't-do 선례 인용)를 실제로 반영했다(`R-wontdo-cached-capabilities` 절에 `6-websocket-protocol.md` 선례 인용 추가). 즉 PR이 스스로 생성한 리뷰 산출물의 액션아이템 중 1건만 disposition 없이 누락된 상태로 커밋에 포함됐다 — CLAUDE.md "정보 저장 위치" 표의 "결정의 배경·근거 → 해당 spec 문서 끝의 `## Rationale`" 원칙과도 어긋난다. 빌드 가드(`spec-status-lifecycle.test.ts`)는 통과하므로 기능적으로 차단되지는 않으나, 자체 지적한 개선사항을 같은 PR에서 처분(반영 또는 명시적 보류 사유 기록) 없이 흘려보낸 것은 요구사항 완결성 관점의 결함이다.
  - 제안: `spec/5-system/9-rag-search.md` `## Rationale` 에 "왜 `rag-dynamic-cut` 종결 후에도 `status: partial` 을 유지했나 (2026-07-16)" bullet을 추가(멀티-KB 리랭크·재임베딩 트리거 등 잔여 미구현 표면과 `rag-quality-improvement.md` 로의 `pending_plans` 재배선 근거를 spec 문서 자체에 남긴다). 후속 커밋으로도 무방.

### 요약
본 PR은 plan/spec 라이프사이클 정리(3건의 plan `in-progress→complete` 이동, 1건의 신규 `plan/research/` 카테고리 도입 및 리서치 문서 이관, `sqitch-poc.md` 흡수, `11-mcp-client.md`/`9-rag-search.md` frontmatter 상태 전이, `spec-link-integrity.test.ts` 주석 및 `spec-impl-evidence.md §4.2` 서술 정정)로 구성된 대규모 하우스키핑 변경이다. 검증 결과 핵심 주장들이 실측과 정확히 일치한다: (1) `plan-frontmatter.test.ts`(`plan/in-progress/*.md` 한정)·`spec-plan-completion.test.ts`(`plan/complete/` 한정)·`plan-stale-audit.sh`(`plan/in-progress` 한정) 코드를 직접 읽어 `plan/research/` 가 이 세 가드의 스캔 범위 밖이라는 문서 주장을 확인했고, 프론트엔드 docs 가드 전체(18 파일 2590 테스트, `spec-link-integrity`/`plan-frontmatter`/`spec-plan-completion`/`spec-status-lifecycle`/`spec-pending-plan-existence`/`no-internal-refs` 포함)가 그린으로 통과한다. (2) `spec-links.ts` `findBrokenLinks()` 구현을 직접 대조해 `spec-link-integrity.test.ts`(파일3)와 `spec-impl-evidence.md §4.2`(파일29)의 정정된 서술("spec 본문 스캔에는 target filter 가 없어 `plan/**` 링크도 검사 대상")이 실제 코드 동작과 정확히 일치함을 확인했다 — 종전의 "plan/ 링크는 예외" 서술이 실제로는 틀렸던 것을 두 위치에서 일관되게 바로잡았다. (3) 새로 `plan/complete/` 로 이동한 5개 plan(`ai-agent-tool-payload-budget-guardrail`·`-followups`·`parallel-p2-followups`·`rag-dynamic-cut`·`spec-sync-mcp-client-gaps`·`trigger-param-output-enricher`)의 `spec_impact` 목록 파일이 전부 실존하고, 인용된 구현 상태(예: `TOOL_DEFINITION_PAYLOAD_EXCEEDED` §4.2/§10, `ai_agent:tool-payload-budget` cross-node-warning-rules §8, `evaluateAiAgentToolPayloadWarnings`/`getGraphWarnings` 배선, `enrichManualTriggerOutputSchema`/`OUTPUT_SCHEMA_ENRICHERS` 디스패치)가 실제 코드베이스와 정확히 대응함을 grep으로 확인했다. (4) `git diff origin/main -M` 으로 실제 저장소 상태를 대조한 결과 review payload 상 "new file"로 표시된 plan 파일들은 실제로는 rename(R062~R094) 이며 review payload 생성 툴의 diff 방식(-M 미적용) 아티팩트일 뿐 기능적 문제는 아니다. (5) 이번 PR에 포함된 자체 consistency-check(`23_36_57`) 가 지적한 WARNING 4건 중 3건(D3 §2-E 유실 커밋먼트 처분, rag-quality-improvement §7 체크박스 명문화, D2 won't-do 선례 인용)은 최종 diff에 정확히 반영됐으나, 나머지 1건(D1 `status: partial` 유지 근거를 `9-rag-search.md` 자체 Rationale에 기록)은 위 발견사항에 적었듯 누락됐다 — 유일한 실질 결함이다.

### 위험도
LOW
