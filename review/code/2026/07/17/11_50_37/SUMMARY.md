# Code Review 통합 보고서

## 전체 위험도

**HIGH** — P0 핵심 수정(`ai-review.js`/`consistency-check.js`의 STATUS/딜리미터 파싱 및 인라인 전달) 자체는 견고하지만, ①이 PR이 스스로 규정한 것과 동일한 결함 클래스가 3번째 형제 파이프라인(`merge-coordinate.js`, 통합 게이트)에 그대로 남아 있고, ②그 근본원인에 대해 상충하는 "확정 진단" 문서 2종이 조정 없이 공존하며(과거 한 번 기각됐던 설계를 다시 되살린 셈), ③정작 고친 핵심 로직 자체에는 자동 회귀 테스트가 전무하다(CI도 미연결). 셋 다 "안전 게이트가 조용히 약해지는데 아무도 모른다"는 이 PR 자신의 문제의식을 PR 스스로 재현하는 지점이다.

강제 포함(router_safety) 대상 7개 reviewer(`documentation, maintainability, requirement, scope, security, side_effect, testing`) 전원 결과 확보 확인됨 — 강제 화이트리스트 커버리지 갭 없음.

## Critical 발견사항

| # | 카테고리 | 발견사항 | 위치 | 제안 |
|---|----------|----------|------|------|
| 1 | 요구사항 | `merge-coordinate.js`가 이번 PR이 고친 P0 결함(STATUS 없으면 `'success'`로 오판 + analyzer 반환 텍스트 유실)을 구조적으로 동일하게 그대로 보유. `parseStatus`는 여전히 `return m ? m[1] : 'success'`(54-56행)이고, 반환값도 `{name, status}`만 보존해 findings 본문을 버리며(66행), summary agent 프롬프트도 여전히 "output_file을 Read해 통합"이라는 디스크-의존 구절(106행)을 유지한다. `/merge-coordinate`는 실제 branch 통합을 게이팅하는 명령으로 `/ai-review`·`/consistency-check`와 동급 이상의 안전-critical 경로인데, analyzer가 harness 지시대로 Write를 건너뛰면 `BLOCK: NO`가 나오면서 미확인 Critical이 그대로 통과할 수 있다 — 이 PR이 2026-07-10 실측 3회로 증명하고 고치려 한 바로 그 실패 모드가 merge 게이트에는 방치돼 있다. plan(`harness-workflow-contract-fix.md`)은 "`.claude/workflows/*.js` 만 고친다"고 선언했음에도 이 누락에 대한 명시적 사유·후속 추적이 어디에도 없다. (architecture·requirement·maintainability·documentation 4개 reviewer 공통 지적, requirement·documentation은 CRITICAL로 직접 평가) | `.claude/workflows/merge-coordinate.js:54-68,76-85,106,134-135`(미변경), `.claude/agents/integration-risk-summary.md:17,20`(미변경), `.claude/skills/merge-coordinator/SKILL.md:63`(미변경) | `merge-coordinate.js`에 `parseAgentReturn`/ANALYZER_CONTRACT(STATUS+delimiter+전문 반환 지시)/inline 전달/누락 파일 persist 패턴을 동일 적용. 이번 스코프에서 의도적으로 제외한 것이면 최소한 plan에 후속 이슈로 명시하고 `integration-risk-summary.md`·`merge-coordinator/SKILL.md`에 known-gap으로 기록. |
| 2 | 문서정합성 | "terminal sub-agent라서 write가 차단된다"는 오진 서술이 8개 파일(그중 3개는 summary sub-agent 자신의 system prompt: `code-review-summary.md`/`consistency-summary.md`/`integration-risk-summary.md`)에 grep으로 확인된 채 남아, 이번 diff가 신설한 `subagent-call-contract.md §7`의 "차단은 basename 정확 일치이며 terminal 여부와 무관"이라는 2026-07-17 실측(probe `wf_61290a15-aec`/`wf_45d76e40-507`)과 정면 모순된다. 더 심각한 것은 `.claude/docs/orchestrator-workflow-migration.md`(미변경)가 이보다 더 정교하고 확신에 찬 **정반대 진단** — "Verified mechanism (5 live probes): the block is `worktree.bgIsolation` guard, NOT a report-file guard, filename is irrelevant" — 을 "CORRECTED diagnosis(2026-05-30)"로 못박아 놓았고, §7은 이 문서와 상호 링크·조정 없이 공존한다는 점이다. 이는 2026-05-30에 이미 한 번 반증됐던 "report-file basename 규칙" 가설이 조정 없이 재등장한 것이며, §7 재실측이 `EnterWorktree`로 제대로 격리된 세션에서 수행됐는지(bgIsolation 오탐 가능성 통제 여부) 기록도 없다. 이 미검증 이론 위에 5월에 명시적으로 기각됐던 설계(리포트 전문을 summary sub-agent 프롬프트에 인라인 전달)가 다시 올라가 있다. 향후 독자·에이전트가 두 "확정 진단" 중 잘못된 쪽을 신뢰해 이번 fix를 되돌릴 위험(진단 뒤집기 3회차 재발 소지). (architecture·documentation 공통 CRITICAL, requirement는 요약 agent 정의 관점에서 WARNING으로 동일 사안 지적) | `.claude/docs/orchestrator-workflow-migration.md`(미변경) vs `.claude/docs/subagent-call-contract.md §7`(신설); `.claude/agents/{code-review-summary,consistency-summary,integration-risk-summary}.md`, `.claude/commands/{ai-review,consistency-check,merge-coordinate}.md`, `.claude/skills/merge-coordinator/SKILL.md:63` | `orchestrator-workflow-migration.md`의 "CORRECTED diagnosis" 절 상단에 "2026-07-17 §7로 재정정됨" 배너+상호링크 추가. 8개 파일의 "terminal이라서 차단" 문구를 basename 기반 서술로 일괄 교체(3개 summary agent system prompt 최우선). §7 재실측 시 EnterWorktree 격리 여부를 기록해 bgIsolation 오탐 가능성 배제. |
| 3 | 테스트 | 이 PR의 실질 핵심 수정 — P0(BLOCK/위험도 거짓음성의 근본원인이던 STATUS·딜리미터 파싱, `usable`/`recovered`/`forcedMissing` 집계) — 코드는 전부 `ai-review.js`/`consistency-check.js`에 있는데 자동 테스트가 0건이고 CI(`harness-checks.yml`)의 `paths:`에도 `.claude/workflows/**`가 없어 트리거되지 않는다(`.claude/skills/**`만 트리거). 실측 결과 `node .claude/workflows/ai-review.js`를 직접 실행하면 `SyntaxError: Illegal return statement`로 하네스 Workflow VM 밖에서는 애초에 구동조차 안 되는 구조라, 순수 로직을 별도 모듈로 추출하지 않는 한 지금 형태로는 유닛테스트가 불가능하다. 검증 수단은 probe workflow 1회성 라이브 실행(`wf_61290a15-aec`, `wf_45d76e40-507`)뿐, 재실행 가능한 회귀 테스트가 아니다. `parseAgentReturn`은 두 파일에 바이트 단위로 동일 복제돼 있어 한쪽만 고치고 다른 쪽을 놓쳐도 테스트가 잡아내지 못한다 — 이 PR이 고치는 "거짓 음성" 버그와 같은 계열의 drift 위험을 스스로 재생산하는 구조. | `.claude/workflows/ai-review.js:89-105,164-174`(`parseAgentReturn`/`usable`/`recovered`/`unfinished`/`forcedMissing`), `.claude/workflows/consistency-check.js:62-78,113-121`(동일 로직 중복), `.github/workflows/harness-checks.yml`(paths 미포함) | `parseAgentReturn`+집계 로직을 `.claude/workflows/_lib/agent-return.mjs` 같은 `export function` 모듈로 추출해 두 워크플로가 import(중복 제거 겸 테스트 가능화)하고, Node 24 내장 `node:test`(`node --test`)로 DELIM 유/무 × STATUS 유/무 × salvage 분기 등 단위테스트 추가. `harness-checks.yml` paths에 `.claude/workflows/**` 추가. |

## 경고 (WARNING)

| # | 카테고리 | 발견사항 | 위치 | 제안 |
|---|----------|----------|------|------|
| 1 | 아키텍처(결합도) | 신규 상태-동기화 CLI(`--sync-from-disk`/`--verify-coverage`)가 `code_review_orchestrator.py`에만 구현됐는데, `consistency-checker/SKILL.md`의 fallback 절은 자기 스크립트가 아니라 이 이웃 스킬의 스크립트 경로를 그대로 호출하라고 안내한다. 두 orchestrator의 `_retry_state.json` 스키마가 우연히 호환되어 오늘은 동작(직접 실측 확인)하지만 이를 보장하는 import·타입·테스트가 전혀 없다. 기존 컨벤션(두 orchestrator가 `_load_state`/`_save_state`/`_apply_status_update`를 완전 복제/미러링 유지, `consistency_orchestrator.py` 상단 주석이 이 원칙을 명시)과도 상충하는 제3의(더 약한) 결합 방식이다. 재사용되는 함수의 docstring/argparse help도 "reviewer"·"8/14"처럼 code-review-agents 전용 어휘를 그대로 써서 consistency-checker(checker 5개) 컨텍스트에서 문맥이 어긋난다. (architecture·requirement·side_effect·documentation 4개 reviewer, scope는 INFO로 관찰) | `.claude/skills/consistency-checker/SKILL.md` fallback 절, `.claude/skills/code-review-agents/scripts/code_review_orchestrator.py:174-247` | `_sync_from_disk`/`_verify_coverage`(스키마 무관 순수 부분)를 `.claude/skills/_lib/`로 추출해 두 orchestrator가 import하거나, 기존 컨벤션대로 `consistency_orchestrator.py`에도 동일 기능을 구현하고 SKILL.md 참조를 자기 스킬로 수정. 유지할 경우 consistency-checker 세션 모양 기준 회귀 테스트 추가 + docstring/help 문구를 skill-불특정 어휘로 완화. |
| 2 | 거버넌스(강제력) | 신규 안전장치(`--verify-coverage`: `agents_forced` 미이행 검증, `--sync-from-disk`: 상태파일 stale 감지)가 hook에 연결되지 않은 순수 CLI이며 호출 의무는 오직 SKILL.md 산문("SUMMARY 확정 전 반드시…")으로만 부과된다. `Workflow` 경로는 `forced_missing[]`을 자동 계산해 반환값에 싣는 구조적 개선이 있지만, 그 값을 보고 행동하는 것 역시 산문(§3)일 뿐 hook이 차단하지 않는다. Fallback(수동 Agent) 경로는 자동 계산조차 없다. 이 프로젝트가 이미 별도 사안(review/fix 후속)에서 "hook 강제 없이 SKILL 산문뿐이면 압력이 커질 때 의무가 먼저 무너진다"고 진단했던 패턴과 동형이며, 애초에 이 PR을 촉발한 사고(2026-07-17, security reviewer가 자가판단으로 누락)도 정확히 "정책을 스스로 판단해 건너뛴" 실패 모드였다 — "이번엔 `--verify-coverage` 호출을 건너뛴다"는 형태로 재발할 여지가 남는다. | `.claude/skills/code-review-agents/SKILL.md` fallback 절, `.claude/hooks/**`(verify-coverage/sync-from-disk 참조 0건) | fallback 경로에 한해 review_guard류 훅에 `--verify-coverage`/`--sync-from-disk` 실행 여부(또는 결과)를 검사하는 게이트 추가 검토, 또는 resolution-applier 호출 전 단계에서 `forced_missing`/상태-디스크 불일치를 기계적으로 재확인. |
| 3 | 범위/Plan위생 | `plan/in-progress/harness-workflow-contract-fix.md`의 P0~P2 세부 체크리스트 항목들(`parseStatus` 기본값 변경, contract 주입, 인라인 전달, 주석 정정, `--verify-coverage`/`--sync-from-disk` 신설 등)이 전부 `[ ]`(미체크)로 커밋됐지만, 실제로는 diff 안에서 전부 구현·문서화가 완료된 상태다(직접 대조 확인). 하단 "작업 체크리스트"(상위 요약)만 `[x]`라 전체적으로는 완료로 읽히지만, P0~P2 세부 항목만 보면 "아직 안 한 일"처럼 보여 plan 문서 내부적으로 모순 — 이 plan은 `plan/in-progress/`의 SoT로 향후 세션·감사가 참조하므로 "무엇이 끝났는지"에 대한 단일 진실을 흐린다. | `plan/in-progress/harness-workflow-contract-fix.md` P0/P1a/P1b/P2 섹션 | 세부 `- [ ]` 항목을 실제 구현 완료 상태(`- [x]`)로 갱신하거나, 세부 체크리스트를 상위 요약 체크리스트로 대체해 중복·불일치 가능성 제거. |
| 4 | 유지보수성 | "하네스가 특정 basename Write를 차단한다"는 ~20줄 설명 코멘트가 이번에 신설된 SoT 문서(`subagent-call-contract.md §7`)를 두고도 `ai-review.js`/`consistency-check.js` 두 파일에 거의 그대로(주석 어휘만 다름) 중복 복제되어 있다(`parseAgentReturn` 함수 자체도 100% 동일 로직으로 diff 대조 확인). 이 코드베이스는 정확히 이런 인라인 중복 서술이 drift를 일으킨 전례를 plan 자체에 기록해 두었다("76행 주석의 정반대로 틀린 전제 정정") — 지금 새로 작성된 장문 코멘트도 향후 하네스 동작이 재실측될 때 SoT 문서만 갱신되고 코드 코멘트는 안 갱신되는 동일 drift 위험을 재현한다. | `.claude/workflows/ai-review.js`, `.claude/workflows/consistency-check.js` (`DELIM`/`parseAgentReturn` 상단 설명 코멘트) | 설명 코멘트를 1~2줄 요약 + `subagent-call-contract.md §7` 링크로 축약. `parseAgentReturn` 자체의 로직 중복은 Workflow 스크립트가 모듈 import를 지원하지 않는 하네스 제약상 불가피할 수 있으나(`.claude/workflows/*.js`에 `require`/`import` 0건), "왜 이런 모양인지"에 대한 서사만큼은 SoT 링크로 대체 가능. |
| 5 | 상태무결성 | `_sync_from_disk`가 `agents_pending`/`agents_success`는 디스크 기준으로 전체 재작성하지만 `agents_fatal`은 "missing이면 유지"로 부분 필터링만 해, 이전에 `agents_fatal`에 있던 agent가 sync 후에도 여전히 파일이 없으면 `agents_pending`과 `agents_fatal` 양쪽에 동시 소속되는 상호배타 위반이 발생한다. testing reviewer가 실제로 재현: `agents_fatal=['security']` 상태에서 sync 후 `agents_pending=['security']`, `agents_fatal=['security']`로 중복 집계됨. `_apply_status_update`는 3버킷 상호배타를 명시적으로 유지하는데(갱신 전 모든 버킷에서 제거 후 정확히 하나에만 추가) 이 함수만 그 불변식을 깬다. `agent_history` 필드도 sync 시 갱신되지 않아 `agents_success`엔 있지만 `agent_history`엔 success 전이가 없는 모순도 가능. `--summary-state`(`/loop --resume`이 참조)가 각 버킷 길이를 단순 합산하므로 이 상태에서 카운트가 중복 집계된다. 신규 테스트 4건 모두 `agents_fatal: []` 고정이라 이 조합을 커버하지 않는다. (architecture는 더 일반적 형태로, testing은 실제 재현으로 각각 지적) | `.claude/skills/code-review-agents/scripts/code_review_orchestrator.py:200-202`(`_sync_from_disk`) | `agents_fatal`이 미리 채워진 상태에서 sync하는 회귀 테스트 추가. 원하는 동작(sync 후 `agents_fatal`은 항상 비우거나 `missing` 계산에서 기존 `agents_fatal` 멤버 제외)을 코드·테스트 양쪽에 확정. `agent_history` 동기화 여부도 docstring에 명시. |
| 6 | 테스트위생 | `test_consistency_target_validation.py::test_valid_target_still_prepares_a_session`이 `cwd=REPO_ROOT` + `CONSISTENCY_OUTPUT_DIR` 미오버라이드로 실제 `<repo>/review/consistency/<날짜>/<시간>/`에 진짜 세션(`_retry_state.json`·`meta.json`·`_prompts/*.md`, 이 중 `_retry_state.json`/`meta.json`은 git 추적 대상)을 생성한다. cleanup(`shutil.rmtree(..., ignore_errors=True)`)이 두 개의 assertion **뒤**에 위치해 `try/finally`나 `addCleanup`으로 감싸여 있지 않아, 그 assertion 중 하나라도 실패하면(향후 회귀 시) 세션 디렉토리가 정리되지 않고 워킹트리에 추적 가능한 상태로 잔류할 수 있다. 같은 스위트의 기존 관례(`test_consistency_impl_done.py`는 격리된 fake git repo, `test_review_guard.py`는 `tempfile.mkdtemp()`)는 모두 이 문제를 원천 차단하는데 이 신규 테스트만 벗어났다. 현재는 assertion이 항상 통과해 로컬 재현 결과 정상 정리됨(즉시 위험은 낮음). | `.claude/tests/test_consistency_target_validation.py:75-93` | `self.addCleanup(lambda: shutil.rmtree(..., ignore_errors=True))`를 세션 경로 확보 직후(assertion 이전)에 등록하거나, `_run`에 `env` 파라미터를 추가해 `CONSISTENCY_OUTPUT_DIR`를 tempdir로 오버라이드. |
| 7 | 계약문서화 | 이번 diff가 두 워크플로에 주입하는 `REVIEWER_CONTRACT`/`CHECKER_CONTRACT` 출력 규약이 `STATUS=<success\|fatal>`만 유효값으로 안내하는데, `subagent-call-contract.md §3`의 원래 enum은 `STATUS=<success|rate_limit|network|fatal>`(4값)이다. 파싱 자체(`/STATUS\s*[=:]\s*([A-Za-z_]+)/`)는 임의 토큰을 허용해 기능적으로 깨지진 않지만, 실제로 rate_limit/network를 겪은 reviewer/checker가 좁혀진 안내를 따라 `fatal`로 자기보고하면 원인 정보가 뭉개진다. §7도 이 의도적 축소를 언급하지 않는다. | `.claude/workflows/ai-review.js:111`, `.claude/workflows/consistency-check.js:86` vs `.claude/docs/subagent-call-contract.md:34` | 축소 이유(Workflow 경로엔 cross-turn 재시도가 없어 rate_limit/network를 별도로 다룰 필요가 없다는 기존 서술과 일치)를 §7에 한 줄 추가하거나, contract 문구에 4값을 그대로 노출. |
| 8 | 보안 | reviewer/checker sub-agent의 자유형식 텍스트 반환을 authoritative로 취급해 구조적 검증 없이 다음 agent(summary) 프롬프트에 그대로 inline하고 디스크에도 그대로 Write시킨다. 경계는 순수 텍스트 구분자(`===REPORT_MARKDOWN_BELOW===`, `----- BEGIN {name}/END {name} -----`)뿐이라, 검토 대상 diff 안에 reviewer LLM을 조작하려는 프롬프트 인젝션 페이로드(예: 가짜 BEGIN/END 블록을 자기 출력에 끼워넣기)가 있으면 구조적 검증 없이 요약 단계로 전파·영속화될 수 있다. router 단계는 이미 `ROUTING_SCHEMA`로 구조화 출력을 쓰는데 review/summary 단계만 텍스트 규약에 의존해 동일 파일 내 신뢰 경계 처리 방식이 일관되지 않는다. 현재는 로컬 신뢰된 세션 자신의 diff를 검토하는 용도라 즉각적 익스플로잇 경로는 없으나(권한상승·데이터유출 아님, 리뷰 품질 저하로 제한적), 외부 기여자 PR을 이 파이프라인으로 자동 리뷰하는 시나리오가 생기면 실질 위협이 될 수 있다. | `.claude/workflows/ai-review.js:92-105,196-243`, `.claude/workflows/consistency-check.js:65-80,122-168` | review/summary 단계도 router처럼 structured-output schema(JSON)로 전환해 헤더/본문 경계를 모델 차원에서 강제하거나, 최소한 조립 시 reviewer의 markdown 안에 `----- BEGIN `/`----- END `/딜리미터 패턴이 포함되면 escape/reject. 외부 기여자 PR 자동 리뷰 사용처가 생기면 우선순위 상향. |
| 9 | 문서화 | `code-review-agents/README.md`가 "본문은 sub-agent가 `output_file`에 Write하고 main에게 반환하지 않는다(context window 부담 최소화)"고 단정하는데, 이번 diff의 Workflow **기본** 경로는 정반대로 모든 reviewer/checker가 항상 STATUS+delimiter+전문을 반환하도록 강제한다(`REVIEWER_CONTRACT`/`CHECKER_CONTRACT`). README는 이번 diff 대상 파일이 아니어서 갱신되지 않았고, 범위 제한("direct-Agent fallback 경로에만 해당")이 명시돼 있지 않아 README만 읽은 독자는 최신 계약과 반대로 이해하게 된다. | `.claude/skills/code-review-agents/README.md:175-183` | 해당 절 앞에 "(direct Agent fan-out 경로 한정 — Workflow 기본 경로는 `subagent-call-contract.md §7` 참고)" 스코프 한정 문구 추가. |
| 10 | 유지보수성 | `output_file` 기본경로 계산 dict-comprehension(`i.get("output_file") or os.path.join(sd, f"{i['name']}.md")`)이 `_sync_from_disk`(191-193행)와 `_verify_coverage`(231-233행) 두 함수에 글자 그대로 중복된다. 같은 파일이 `_load_state`/`_save_state`처럼 반복 로직을 이미 헬퍼로 추출해 쓰는 관례를 갖고 있어 이 부분만 그 관례를 벗어났다. | `.claude/skills/code-review-agents/scripts/code_review_orchestrator.py:191-193,231-233` | `_output_paths(state, sd)` 같은 private 헬퍼로 추출해 두 호출부에서 재사용. |

## 참고 (INFO)

| # | 카테고리 | 발견사항 | 위치 | 제안 |
|---|----------|----------|------|------|
| 1 | 확장성 | reviewer/checker 반환 markdown 전문을 크기 상한 없이 summary sub-agent 프롬프트에 인라인 — reviewer 최대치 기준 개별 보고서가 커지면 프롬프트가 선형 팽창. `orchestrator-workflow-migration.md`가 "풀 리포트가 컨텍스트를 왕복하는 것은 legacy 경로엔 없던 회귀"라 경계했던 것과 같은 종류의 비용이 main 대신 summary sub-agent 프롬프트 쪽에서 무제한 재발할 수 있음. 의도된 트레이드오프(정확성 우선)로 보이나 상한이 전혀 없다는 점은 기록해 둘 가치가 있음. | `.claude/workflows/ai-review.js`/`consistency-check.js` (`inlined`/`needPersist` 구성부) | reviewer당 인라인 markdown에 상한(초과 시 상위 N KB + "전문은 output_file 참고")을 두는 것을 고려. |
| 2 | 검증 완결성 | `_sync_from_disk`/`_verify_coverage`가 파일 "존재"만 `os.path.isfile()`로 확인하고 내용은 검증하지 않아, Write가 중간에 실패해 0바이트/손상 파일을 남겨도 "성공"으로 분류될 수 있음. 실무 영향은 낮음(authoritative 소스는 파일 재Read가 아니라 워크플로가 인라인으로 넘긴 텍스트). | `code_review_orchestrator.py:174-208,211-247` | 필요시 최소 크기(`> 0 bytes`) 체크 추가. 우선순위 낮음. |
| 3 | 코드 구조 | `_require_target`(consistency-checker, ~35줄)이 형제 함수(`_load_state`/`_save_state`/`_apply_status_update`, 모두 module-level)와 달리 `collect_context` 내부의 nested function으로 정의돼 재사용·단위테스트 대상에서 배제됨. 4개 모드(`--spec`/`--plan`/`--impl-prep`/`--impl-done`) 모두에 재사용되므로 module-level이 기존 스타일과 일관적. | `.claude/skills/consistency-checker/scripts/consistency_orchestrator.py:290-326` | 모듈 최상위로 hoist(동작 변경 없는 순수 리팩터). |
| 4 | 정규식 견고성 | `parseAgentReturn`의 STATUS 정규식(`/STATUS\s*[=:]\s*([A-Za-z_]+)/`)이 라인 앵커(`^`/`m` 플래그) 없이 delimiter 이전 텍스트 어디서든 첫 매치를 채택 — reviewer가 STATUS 줄 앞에 서두를 덧붙이고 그 서두에 우연히 `status:`/`STATUS=` 유사 문자열이 섞이면 실제 값과 다른 문자열을 채택해 `recovered[]` 오분류·로그 부정확 가능(finding 유실까지는 아님, markdown은 delimiter 기준 별도 추출). | `.claude/workflows/ai-review.js:96`, `.claude/workflows/consistency-check.js:69` | 정규식에 라인 시작 앵커 추가 검토(단위테스트 부재와 동일 원인, Critical #3에 종속). |
| 5 | 반환계약 의미변화 | `unfinished[]`의 판정 기준이 "status !== 'success'"(구)에서 "!usable(r)"(신, 파일도 본문도 전혀 없는 경우)로 좁아짐. 이전엔 재시도 대상이던 "status=fatal + 본문 있음" 케이스가 이제 `unfinished`에서 빠지고 `recovered`로만 나타남. 필드 자체는 additive이나 의미가 변경됨. SKILL.md §3가 이 변화를 문서화했고 소비자가 main뿐이라 즉각적 breaking 위험은 낮음. | `.claude/workflows/ai-review.js`/`consistency-check.js` 반환 객체 | 별도 조치 불요(문서화 확인됨). 향후 이 반환값을 파싱하는 스크립트가 추가되면 구 의미를 가정하지 않도록 주의. |
| 6 | 설계 위임 | summary sub-agent가 자기 책임(SUMMARY 통합) 밖의 파일(다른 reviewer의 `output_file`)을 쓰도록 위임됨(`needPersist`가 "파일 없는 reviewer"가 아니라 "markdown 반환한 모든 reviewer"를 포함 — 스크립트가 FS 접근 없어 실제 누락 여부를 스스로 계산 못함). "없으면 Write"라는 배타조건은 자연어 지시 해석에만 의존. 최악의 경우 이미 정상 Write된 reviewer 파일을 동일 내용으로 재Write할 수 있으나 데이터 교차오염은 아님. plan에 의도된 설계로 기록됨. | `.claude/workflows/ai-review.js`/`consistency-check.js` `needPersist` 계산부 | 별도 조치 불요(의도된 트레이드오프). 향후 회귀 시 정상 파일이 실제로 덮어써지는지 실측 확인 가치는 있음. |
| 7 | 방어 코드 검증 | `agents_forced`에 `subagent_invocations`에 없는 이름(오타/stale 데이터)이 섞인 경우, Python(`_verify_coverage`)·JS(`forcedMissing`) 양쪽 모두 "파일 없음→missing"으로 안전하게(fail-closed) 처리하는 것으로 보이나 이 동작이 테스트로 고정돼 있지 않음. | `code_review_orchestrator.py:227-235`, `.claude/workflows/ai-review.js:170` | 우선순위 낮음(현재도 안전한 방향의 기본값). 회귀 테스트 추가 고려. |
| 8 | 코드 가독성 | `state["agents_fatal"] = [n for n in state.get("agents_fatal", []) if n in missing]` 한 줄이 "이전 fatal이었지만 지금 디스크에 파일이 생겨 success로 재분류된 항목은 fatal에서도 제거"라는 은근히 중요한 재조정을 인라인 코멘트 없이 압축 표현. | `code_review_orchestrator.py:202` | 한 줄 인라인 코멘트 추가(예: `# previously-fatal agent that now has a file on disk is no longer fatal`). |
| 9 | 코드 주석 | `ai-review.js`의 "Return contract:" 요약 주석이 `summary_output`/`summary_markdown`/`summary_written` 3필드만 나열하고 신규 안전 필드(`recovered[]`/`forced_missing[]`/`reviewers[].has_report`)는 요약 블록에서 누락(개별 선언부 인라인 주석은 있어 완전 미문서화는 아님). `consistency-check.js`는 이런 요약 블록 자체가 없음. | `.claude/workflows/ai-review.js` Summary phase 진입 직전 "Return contract:" 블록 | 필수는 아니나 신규 필드 3-4개를 요약 블록에 한 줄씩 추가하면 코드-주석 동기화가 더 견고해짐. |
| 10 | 오탈자 | `_require_target`의 에러 메시지 "파일 가 아닙니다"(불필요한 공백+조사 오류, "파일이"가 맞음). 기능에는 무관(테스트는 부분 문자열만 확인). | `.claude/skills/consistency-checker/scripts/consistency_orchestrator.py:308` | 조사·공백 수정. |
| 11 | 보안(이론적) | `_retry_state.json`의 `output_file` 값을 `session_dir` 하위 경로인지 재검증하지 않고 `os.path.isfile()`에 그대로 사용. `_retry_state.json` 자체가 로컬에서 이미 신뢰된 `--prepare` 산출물이라 실질 공격 표면은 아님(내용을 읽거나 실행하지 않고 존재 여부만 확인). | `code_review_orchestrator.py:191-194,231-234` | 별도 조치 불요(방어적 기록용). 필요시 `os.path.commonpath` 경계 검증 추가 가능. |

## 확인된 사항 (문제 없음)

- 하드코딩된 시크릿·SQL/XSS/커맨드 인젝션·인증/세션 문제: 미발견(신규 Python 코드는 표준 라이브러리만 사용, `subprocess`/`shell=True` 신규 추가 없음, DB·HTML 렌더링·인증 로직이 diff에 없어 해당 항목 N/A).
- `_require_target()` 입력 검증(경로 실존성, `--spec/--plan/--impl-prep/--impl-done`)이 견고하며, 5개 checker 전원이 손상된 payload를 CRITICAL로 잘못 올려 `BLOCK: YES` 거짓양성을 냈던 실제 사고(2026-07-17)를 구조적으로 막음.
- 변경 범위가 선언된 10개 파일(`.claude/**`+`plan/**`)과 정확히 1:1 매핑되며 무관한 리팩토링·포맷팅 잡음·불필요한 임포트 없음(`git diff --check` clean). `spec/**` 무변경이며 어떤 spec의 `code:` glob에도 매칭되지 않음 — spec fidelity 해당없음 판단 타당.
- 기존 테스트 회귀 없음: `python3 -m unittest discover -s .claude/tests` **215 tests, OK**(복수 reviewer 독립 실행 확인). 신규 14건(target-validation 6 + sync-from-disk 4 + verify-coverage 4) 전부 PASS. `node --check`로 `ai-review.js`/`consistency-check.js` 구문 검증 이상 없음.
- `compute_forced_agents`가 `available_agents`와 이미 교집합을 취해 `agents_forced`가 `subagent_invocations`에 없는 이름을 담아 오탐하는 경로는 없음을 소스 추적으로 확인.

## 에이전트별 위험도 요약

| 에이전트 | 위험도 | 핵심 발견 |
|----------|--------|-----------|
| architecture | HIGH | 상충하는 진단 문서 3종 공존 + 과거 기각된 설계 되살림(CRITICAL), merge-coordinate.js 미적용, cross-skill coupling, hook 미강제(거버넌스), `_sync_from_disk` 불변식 리스크 |
| requirement | HIGH | merge-coordinate.js가 동일 P0 결함 보유(CRITICAL), 요약 sub-agent 정의(3종)가 정정된 오진·구 절차 여전히 보유, consistency-checker fallback 경로 불일치 |
| testing | HIGH | 핵심 P0 수정 로직(.js)에 자동테스트 0건+CI 미연결(CRITICAL), `agents_fatal` 상호배타 불변식 실측 위반, 테스트 cleanup 예외 미안전 |
| documentation | HIGH | terminal-write 오진 8개 파일 잔존+모순 문서 방치(CRITICAL), merge-coordinate.js 결함 미문서화(CRITICAL), README 구식 서술, consistency-checker 재사용 문서 어휘 불일치 |
| side_effect | MEDIUM | cross-skill coupling(공유 계약이 코드 아닌 산문), 테스트 cleanup 예외 미안전, merge-coordinate.js 잔존 결함(INFO) |
| maintainability | MEDIUM | write-차단 서사 중복(drift 재발 위험), merge-coordinate.js 결함 패턴 잔존, output_file 계산 로직 중복 |
| security | LOW | 프롬프트 인젝션/구분자 위조 표면(WARNING), 나머지는 방어적 관찰(INFO) — 하드코딩 시크릿·전통적 인젝션 없음 확인 |
| scope | LOW | plan 세부 체크리스트 상태 불일치(WARNING), cross-skill coupling 관찰(INFO) — 변경 범위 자체는 plan 선언과 1:1 매핑 확인 |

## 발견 없는 에이전트

없음 — 강제 포함된 7개 reviewer를 포함해 실행된 8개 reviewer 전원이 최소 1건 이상의 WARNING 이상 발견을 보고함.

## 권장 조치사항

1. **[최우선]** `merge-coordinate.js`에 이번 PR과 동일한 P0 fix(`parseAgentReturn`/ANALYZER_CONTRACT/inline 전달/`recovered[]`)를 적용한다. 즉시 불가능하면 plan에 명시적 후속 이슈로 등록하고 `merge-coordinator/SKILL.md`·`integration-risk-summary.md`·관련 커맨드 문서에 known-gap으로 기록한다.
2. "terminal write라서 차단된다"는 오진 서술이 남은 8개 파일(특히 3개 summary sub-agent system prompt)을 `subagent-call-contract.md §7`의 basename 모델로 일괄 정정하고, `orchestrator-workflow-migration.md`의 "CORRECTED diagnosis" 절에 §7 상호링크/배너를 추가해 두 "확정 진단" 문서 간 SoT 충돌을 해소한다. §7 재실측 시 `EnterWorktree` 격리 여부를 기록해 bgIsolation 오탐 가능성을 배제한다.
3. `ai-review.js`/`consistency-check.js`의 핵심 파싱/집계 로직(`parseAgentReturn`, `usable`/`recovered`/`forcedMissing`)을 공용 모듈로 추출해 `node --test` 단위테스트를 추가하고, `harness-checks.yml` paths에 `.claude/workflows/**`를 포함시켜 CI 사각지대를 없앤다.
4. `--sync-from-disk`/`--verify-coverage`를 두 orchestrator가 공유하는 `lib/`로 승격하거나 `consistency_orchestrator.py`에도 동일 기능을 구현하고, consistency-checker 세션 모양 기준 회귀 테스트를 추가한다.
5. 신규 안전장치(`--verify-coverage`/`--sync-from-disk`) 호출 의무를 SKILL 산문뿐 아니라 최소 한 단계(hook 또는 resolution-applier 사전 재확인)로 기술적으로 강제하는 방안을 검토한다.
6. `_sync_from_disk`의 `agents_fatal` 재조정 로직이 상호배타 불변식을 유지하도록 수정하고, testing reviewer가 실측 재현한 케이스를 회귀 테스트로 고정한다.
7. `test_consistency_target_validation.py`의 실 저장소 오염 가능성을 제거한다(`addCleanup`을 assertion 이전에 등록하거나 `CONSISTENCY_OUTPUT_DIR`를 tempdir로 오버라이드).
8. plan(`harness-workflow-contract-fix.md`)의 P0~P2 세부 체크리스트를 실제 구현 완료 상태로 갱신한다.
9. (낮은 우선순위) review/summary 단계의 구분자 위조 방어(structured schema 전환 검토), `README.md` return-contract 서술에 스코프 한정 문구 추가, `REVIEWER_CONTRACT`/`CHECKER_CONTRACT`의 STATUS enum 축소 사유 문서화, 기타 INFO 항목 정리.

## 라우터 결정

- `routing_status=done` (router 가 선별):
  - **실행**: `security, architecture, requirement, scope, side_effect, maintainability, testing, documentation` (8명)
  - **제외**: 아래 표 (6명)
  - **강제 포함(router_safety)**: `documentation, maintainability, requirement, scope, security, side_effect, testing` (7명) — 전원 결과 확보 확인됨(커버리지 갭 없음)

  | 제외된 reviewer | 이유 |
  |------------------|------|
  | performance | 라우터 판단(세부 사유는 입력에 미포함). diff가 `.claude/**`+`plan/**` 오케스트레이션 스크립트/문서로 한정되어 런타임 성능 특성 변경 없음으로 추정 |
  | dependency | 라우터 판단(세부 사유 미포함). 신규 외부 의존성 추가 없음(표준 라이브러리만 사용)으로 추정 |
  | database | 라우터 판단(세부 사유 미포함). DB 스키마/쿼리 변경 없음으로 추정 |
  | concurrency | 라우터 판단(세부 사유 미포함). 동시성/락 로직 변경 없음으로 추정 |
  | api_contract | 라우터 판단(세부 사유 미포함). 외부 공개 API 계약 변경 없음으로 추정 |
  | user_guide_sync | 라우터 판단(세부 사유 미포함). 최종 사용자 가이드 문서 변경 없음(하네스 내부 문서만)으로 추정 |