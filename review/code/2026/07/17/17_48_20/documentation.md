# 문서화(Documentation) 리뷰 — webchat-boot-single-flight

## 검증 방법 (요약)

- 리뷰 payload(37개 파일) 를 그대로 신뢰하지 않고 `git diff --stat 14bc86a53(merge-base)..HEAD` 로 이 브랜치의 **실제** 변경분(6개 파일)을 재확인.
- `codebase/channel-web-chat/src/widget/use-widget.ts`·`use-widget-eager-start.test.ts`·`codebase/channel-web-chat/src/lib/widget-state.ts`(+test)·`plan/in-progress/webchat-boot-single-flight.md`·`spec/7-channel-web-chat/2-sdk.md` 를 리뷰 대상 commit(`215cd1c3f`, 이 브랜치 tip)에서 `git show 215cd1c3f:<path>` 로 **클린 스냅샷**을 떠서 정독(공유 워크트리의 live 편집과 분리 — 아래 "진행 상황" 참고).
- `isStale(gen)` 컴파일 여부·`await`-in-non-async 컴파일 여부는 실제 `tsc --noEmit --strict` 로 최소 재현 스니펫을 만들어 **실증**(추론이 아님).
- `pendingResetRef`/`bootGenRef` 등 6개 심볼의 JSDoc-선언 결합 여부는 TypeScript 컴파일러 API(`ts.getJSDocCommentsAndTags` / `stmt.jsDoc`)로 클린 스냅샷에 대해 직접 파싱해 실측.
- `spec/7-channel-web-chat/2-sdk.md` §106 인용의 실제 라인 위치는 `git show 14bc86a53:...`(diff 이전) vs 현재(diff 이후) 를 라인 단위로 대조.
- `plan/in-progress/webchat-boot-single-flight.md` 가 인용하는 테스트 카운트는 `npx vitest run` 실행 결과와 대조, 인용된 과거 리뷰 세션(`02_04_13`·`08_29_33`·`09_36_01`·`11_38_14`·`12_04_49`·`12_34_03`·`13_03_59`·`14_30_15`·`14_56_27`) 은 `review/code/2026/07/17/<id>/RESOLUTION.md` 존재 여부로 실재 확인.
- CHANGELOG.md 컨벤션은 저장소 루트 `CHANGELOG.md` 전체를 읽고 `git diff --stat 14bc86a53..HEAD -- CHANGELOG.md`(무변경 확인) + `git log 14bc86a53..HEAD -- CHANGELOG.md`(빈 결과) 로 교차검증.

## 발견사항

- **[CRITICAL]** `WAITING` case 의 "가드 범위는 WAITING 뿐이다" 주석이 **바로 위 코드**(이 diff 자신이 신설한 `RESTORED`/`BOOTED` 가드)에 의해 그 자리에서 즉시 반증됨
  - 위치: `codebase/channel-web-chat/src/lib/widget-state.ts:155-157`(주석, 이번 diff에서 미수정) vs `:136`(`RESTORED` 신규 가드), `:142`(`BOOTED` 신규 가드) — 커밋 `215cd1c3f` 클린 스냅샷 기준 라인.
  - 상세: `WAITING` case 의 (이번 diff가 손대지 않은) 기존 주석은 이렇게 단언한다 — "**가드 범위는 WAITING 뿐이다** — `RESTORED`/`BOOTED`/`USER_MESSAGE` 도 `state.phase` 를 검사하지 않고 무조건 전이하므로, 'ended 를 벗어나는 액션'의 리듀서 레벨 불변식은 아직 없다." 그런데 바로 위 `case "RESTORED":`(125행)와 `case "BOOTED":`(138행)는 **같은 커밋**에서 각각 `if (state.phase === "ended") return state;` 가드(136·142행)를 새로 얻었다 — 이제 둘 다 `state.phase` 를 검사한다. 즉 이 주석의 핵심 주장은 같은 diff가 만든 코드에 의해 작성되는 순간 이미 거짓이 된다. `USER_MESSAGE` 에 대한 부분만 여전히 참(무조건 전이 유지, `grep -n 'case "USER_MESSAGE"' -A3` 로 확인)이다. 뒤이은 "확대는 후속 — … 실패 사례 없이 넓히지 않는다" 문단도 시제가 안 맞다 — 그 "후속 확대"가 정확히 이 diff의 A-6이기 때문이다(plan §A-6 진행 기록이 스스로 그렇게 서술함). 이 파일은 정확히 "가드 상태를 설명하는 주석이 실제 가드 상태와 어긋나서" 과거 버그가 났다고 스스로 기록해 둔 파일(WAITING 주석 자체가 그 사례를 설명 중)이라, 같은 클래스의 드리프트가 이번 diff로 재도입됐다.
  - 제안: `WAITING` case 주석의 "가드 범위는 WAITING 뿐이다" 문단을 갱신 — 예: "가드 범위는 이제 `WAITING`·`RESTORED`·`BOOTED` 세 곳이다(A-6, `webchat-boot-single-flight.md`). `USER_MESSAGE` 는 아직 무조건 전이한다(오늘은 호출부가 `ended` 에서 디스패치하지 않아 활성 버그 아님)." 로 최신화. "확대는 후속" 문단은 과거형으로 정리하거나 A-6 결과 언급으로 대체.

- **[CRITICAL]** `pendingResetRef` JSDoc — 이번 diff가 갱신한 바로 그 계약 문단을 포함해 — 가 `bootGenRef` JSDoc·선언 삽입으로 **자기 선언과의 인접성을 잃어 tooling 에서 완전히 유실**됨 (TS 컴파일러 API로 실증)
  - 위치: `codebase/channel-web-chat/src/widget/use-widget.ts` (커밋 `215cd1c3f` 기준) — `pendingResetRef` 의도된 JSDoc `:162-197`(이번 diff가 "성공하는 부팅의 정의에 supersede 포함" 문단을 추가한 바로 그 블록) → `:198-213`(이번 diff가 신설한 `bootGenRef` JSDoc, **사이에 끼어듦**) → `:214`(`const bootGenRef = useRef(0);`) → `:215`(`const pendingResetRef = useRef(false);`, 바로 위에 JSDoc 없음).
  - 상세: `ts.createSourceFile` 로 클린 스냅샷을 파싱해 `bootGenRef`/`pendingResetRef` 각 `VariableStatement.jsDoc` 배열을 직접 읽은 결과 — `bootGenRef`(선언 214행): `jsDoc.length === 2`(162-197 블록 **+** 198-213 블록 **둘 다** 흡수), `pendingResetRef`(선언 215행): `jsDoc.length === 0`. 같은 파일의 `worldGenRef`(1개)·`beginBootAttempt`(1개)·`isAttemptStale`(1개)·`establishConfig`(1개)는 전부 정상적으로 자기 JSDoc 1개씩만 붙는 것과 대조적이다. TypeScript 는 코드 없이 연달아 쌓인 `/** */` 블록들을 **바로 다음에 오는 선언 하나**에 전부 붙이므로(중간에 `const bootGenRef = ...;` 라는 실제 코드가 끼어 있었다면 갈렸겠지만, 이번 diff는 JSDoc-A(pendingResetRef 내용) 바로 뒤에 JSDoc-B(bootGenRef 내용)를 놓고 그 뒤에야 `const bootGenRef` 를 둬 두 블록이 물리적으로 붙어버렸다), 결과적으로 "부팅 시도가 대체되면 리셋도 대체된다" 는 이번 diff의 핵심 갱신 내용을 포함해 `pendingResetRef` 전체 JSDoc(4번의 재현된 회귀 경고 + "폐기 로직을 다시 넣지 말 것" + 이번에 추가된 supersede 문단)이 `bootGenRef` 아래에 잘못 귀속되고, `pendingResetRef` 자신은 VSCode hover·TypeDoc 등 어떤 JSDoc 소비 툴에서도 **문서 없음**으로 보인다. 내용 자체(아래 별도 항목 참고)는 정확하고 기존 계약과 모순도 없다 — 문제는 순수하게 **배치**다. 이 ref 는 "같은 결함 클래스에 fix 를 네 번 냈다" 고 스스로 기록한, 이 파일에서 가장 사고 이력이 많은 심볼인데, 그 경고문이 정확히 이번 diff로 인해 도구상 안 보이게 된 것은 아이러니가 크다. lint 설정에 jsdoc 플러그인이 없어 이 클래스의 결함은 CI 로 잡히지 않는다.
  - 제안: `bootGenRef` 의 {JSDoc, `const`} 쌍을 `pendingResetRef` 의 {JSDoc, `const`} 쌍보다 **앞으로** 재배치 — `worldGenRef` 선언 → `bootGenRef` JSDoc → `const bootGenRef` → `pendingResetRef` JSDoc → `const pendingResetRef` 순서로 바꾸면 두 쌍 모두 인접성이 복원된다(TS API로 재확인 권장).

- **[CRITICAL]** 사용자 가시 버그 수정(A-6)인데 `CHANGELOG.md` 미갱신 — 확립된 저장소 관례·직전 동일 클래스 선례와 불일치
  - 위치: `CHANGELOG.md`(저장소 루트) — 이번 브랜치 4개 커밋 어디에도 포함되지 않음(`git diff --stat 14bc86a53..HEAD -- CHANGELOG.md` 빈 결과, `git log 14bc86a53..HEAD -- CHANGELOG.md` 빈 결과로 확인).
  - 상세: A-6 커밋(`215cd1c3f`) 메시지 자체가 `fix(web-chat): ERROR 로 종료된 대화가 wc:boot 재전송으로 부활하던 문제 (사용자 가시)` — 작성자 스스로 "사용자 가시" 로 태깅했고, 커밋 본문에 구체적 재현 로그(`[1] ERROR 후 phase=ended storage잔존=true` → `[2] 재부팅 후 phase=streaming ← 부활`)까지 남겼다. `CHANGELOG.md` 최상단 항목("## Unreleased — 웹채팅 위젯: 버퍼 만료 재동기화 + 종료 처리 일원화")은 바로 이 작업의 **선행 PR(#964, `5de44d4d6`)** 이고, 그 항목의 5번이 정확히 같은 버그 클래스("**종료된 위젯 부활 버그 수정 (사용자 가시 버그 수정)**")를 changelog 에 남긴 선례다. `CHANGELOG.md` 전체를 훑어봐도(총 8개 이상의 "## Unreleased — …" 섹션) CHANGELOG 항목이 별도 후속 커밋으로 붙는 패턴이 아니라 **해당 기능/수정 PR 자체가 자신의 섹션을 최상단에 prepend** 하는 일관된 패턴이며, 예외가 없다. `.claude/agents/documentation-reviewer.md`(본 역할의 시스템 프롬프트) 외에는 CHANGELOG 를 강제하는 CI 게이트가 없어(spec/conventions·PROJECT.md·developer/SKILL.md 어디에도 "CHANGELOG" 언급 없음) 빌드가 막히진 않지만, 사용자가 이미 "이 저장소는 사용자 가시 변경의 CHANGELOG 누락을 반복 지적한 이력이 있다" 고 명시했고, 실측한 관례도 그 지적과 정확히 일치한다.
  - 제안: `CHANGELOG.md` 최상단에 `## Unreleased — …` 섹션 신설. A-6("`ERROR` 로 종료된 대화가 `wc:boot` 재전송으로 부활" — **(사용자 가시 버그 수정)** 태그 필수)을 주 항목으로, §106 준수(supersede/`bootGenRef`)는 plan 자체 평가("오늘 실사용 재전송 경로는 관리자 라이브 미리보기 하나뿐" — 낮은 사용자 영향)를 반영해 부가 항목으로 함께 기술 권장. 선행 PR(#964) 항목의 문체(재현 시나리오 요약 + "(사용자 가시 버그 수정)" 태그)를 그대로 따르면 일관성이 유지된다.

- **[WARNING]** `spec/7-channel-web-chat/2-sdk.md` 신규 `code:` 주석 자신의 "§106" 자기 참조가, 같은 diff 의 프론트매터 편집으로 **즉시 4줄 밀려 stale 화**되고, 이 라인 번호 인용 관행이 이번 diff가 새로 작성한 6곳 이상의 주석에 반복됨
  - 위치: `spec/7-channel-web-chat/2-sdk.md:6`("§106 `wc:boot` 재전송 계약…") vs 실제 "`wc:boot` 재전송(멱등 재설정)" 불릿의 현재 위치(`:110`). diff 이전(`14bc86a53`) 기준으로는 그 불릿이 정확히 106행이었음을 `git show 14bc86a53:spec/7-channel-web-chat/2-sdk.md | grep -n` 로 확인.
  - 상세: 이번 diff 는 `2-sdk.md` frontmatter `code:` 목록에 4줄(주석 2줄 + `host-bridge.ts`/`use-widget.ts` 경로 2줄, `:6-9`)을 **본문보다 앞에** 추가했다 — 그 결과 diff 이전엔 106행이던 "`wc:boot` 재전송(멱등 재설정)" 불릿이 diff 이후엔 110행으로 밀렸다. 그런데 정확히 같은 diff 가 새로 작성한 다음 위치들 전부가 "§106" 을 라인 번호로 인용한다(그 문서에 실제 "§106" 이라는 헤딩은 존재하지 않음 — 헤딩은 `## 1.`~`## 5.`/`R2`~`R6` 까지뿐, `grep -n "^##"` 로 확인): `use-widget.ts:179`(`bootGenRef` JSDoc, `pendingResetRef` 신규 supersede 문단), `use-widget.ts:201`·`204`(`bootGenRef` JSDoc 본문), `use-widget.ts:842`(applyConfig 인라인 주석), `use-widget-eager-start.test.ts` 4곳(신규 테스트 주석·제목), `plan/in-progress/webchat-boot-single-flight.md` 6곳. 즉 이 diff는 "§106" 을 **처음 도입하거나 재사용**하면서 정확히 같은 손길로 그 참조 대상을 어긋나게 만들었다 — 자가 유발(self-inflicted) drift다. 저장소에 이미 이 문제를 인지하고 더 견고하게 처리한 선례가 있다(`spec/conventions/chat-channel-adapter.md`: `EIA §6.5 line 536` — 안정적인 실제 헤딩 번호(§6.5)와 라인 번호(정밀 보조)를 **함께** 표기해 헤딩이 라인 드리프트에도 살아남게 함). "§106" 은 그 안정적 헤딩 짝 없이 라인 번호 단독으로 쓰여 이번처럼 취약하다. 실사용 영향은 낮다 — 사람 독자는 "wc:boot 재전송" 텍스트 검색으로 쉽게 도달하고, 라인 참조가 CI로 검증되지도 않는다. 이번 diff 밖의 기존 코드(`use-widget.ts:602`, `review/code/2026/07/17/12_04_49/security.md` 등)도 "§106" 을 인용하는데, 그쪽은 이번 diff가 만든 라인 이동의 **부수 피해**를 함께 입는다(직접 원인은 아님).
  - 제안: 이번 diff가 새로 쓴 6곳 이상의 "§106" → "§110" 로 일괄 정정하거나(가장 간단), 향후 재드리프트를 막으려면 라인 번호 단독 대신 `chat-channel-adapter.md` 선례처럼 안정적 텍스트 앵커(예: "`wc:boot` 재전송(멱등 재설정)" 불릿, 또는 `2-sdk.md#3-host--iframe-postmessage-프로토콜`)를 함께 쓰는 편이 이 문서를 다음에 만질 때 또 어긋나지 않는다.

- **[INFO]** 리뷰 payload(`_prompts/documentation.md`) 자체의 diff 범위가 크게 오염돼 있음 — 37개 파일 중 **31개가 이 브랜치와 무관**
  - 위치: payload "파일 1"~"파일 12"(`.claude/_shared/**` 삭제·`review_guard.py`/`code_review_orchestrator.py`/`consistency_orchestrator.py` 되돌리기·`harness-checks.yml`), "파일 17"~"파일 21"(`sidebar-*.test.tsx`·`harness-report-contract-followups.md` 이동), "파일 23"~"파일 36"(`review/code/2026/07/17/15_48_02/**` 전체 삭제) — 실제로는 이번 브랜치가 건드린 파일이 아님.
  - 상세: `git merge-base HEAD origin/main` = `14bc86a53`(이 브랜치의 실제 분기점, PR #965), `git rev-list --count HEAD..origin/main` = `1`(그 1개 커밋이 `67871ffbd` "fix(harness): report-path 규칙을 공유 모듈로 — … (#966)"). 즉 이 브랜치가 `14bc86a53` 에서 분기된 **뒤에** `origin/main` 에 PR #966(harness report-paths-shared)이 별도로 머지됐고, payload 생성 시 `origin/main`(현재 tip, #966 포함) 을 base 로 diff 를 떴기 때문에 — 이 브랜치엔 애초에 존재한 적 없는 #966 의 전체 작업이 "삭제"로 표시됐다. `git diff --stat 14bc86a53..HEAD`(진짜 merge-base 기준) 로 재확인한 이 브랜치의 실제 변경분은 **6개 파일**뿐이다: `widget-state.ts`·`widget-state.test.ts`·`use-widget.ts`·`use-widget-eager-start.test.ts`·`plan/in-progress/webchat-boot-single-flight.md`·`spec/7-channel-web-chat/2-sdk.md`. 이 리뷰는 그 6개 파일만을 실제 변경분으로 취급해 작성했다. (같은 근본 원인의 더 작은 사례가 `review/code/2026/07/17/17_36_57/documentation.md` INFO#2 에도 이미 기록돼 있다 — 그 세션은 2-dot/3-dot diff 선택 문제로 추정했는데, 이번 측정으로 원인이 "`origin/main` 이 이 브랜치 분기 후 전진했다" 는 쪽으로 더 구체화됐다.)
  - 제안: 조치 불요(이 PR의 코드/문서 문제 아님). 다만 이번 라운드(17_48_20)의 다른 13개 reviewer 도 동일하게 오염된 payload 를 받았을 가능성이 높다 — SUMMARY 통합 시 `.claude/_shared/**`·`review/code/2026/07/17/15_48_02/**` 등 31개 무관 파일에 대한 다른 reviewer 의 발견사항(있다면)은 이 브랜치의 실제 산출물이 아니므로 걸러낼 것을 권장. 향후 payload 생성은 `git diff <merge-base>..HEAD` 또는 `--range` 로 고정해 이 클래스의 오탐을 근본적으로 막을 수 있다(프로젝트 메모리에 이미 기록된 "diff-scope 오염" 패턴과 동일 근본원인).

- **[INFO]** 검증 완료 — `bootGenRef`/`beginBootAttempt`/`isAttemptStale`/`establishConfig` JSDoc 의 검증 가능한 3개 핵심 주장은 **전부 사실로 확인**됨 (요청 항목 1)
  - 위치: `codebase/channel-web-chat/src/widget/use-widget.ts:201-213`(`bootGenRef` JSDoc) / `:254-256`(`beginBootAttempt` JSDoc) / `:844-845`(applyConfig 인라인 주석) / `:802-836`(`establishConfig` 및 그 JSDoc, `:810`).
  - 상세: (a) "`applyConfig` 는 `gen`(world 단독)을 스코프에 두지 않으므로 거기서 `isStale(gen)` 은 **컴파일되지 않는다**" — `applyConfig`(마운트 `useEffect` 내부에서 정의)와 `gen` 이 선언되는 세 함수(`seedWaitingFromStatus`·`start`·`sendCommand`)는 서로 형제 클로저라 `gen` 이 `applyConfig` 스코프에 전혀 안 보임을 확인했고, 동일 구조의 최소 재현 스니펫을 `tsc --noEmit --strict` 로 실제 컴파일해 `error TS2304: Cannot find name 'gen'.` 를 실측했다. (b) "**비-async 함수 안에는 `await` 을 쓸 수 없다**"(`establishConfig` 가 `async` 를 붙이지 말아야 하는 근거) — 동형 재현 스니펫이 `error TS1308: 'await' expressions are only allowed within async functions…` 로 실제 컴파일 실패함을 확인(plan 문서 §B 완료 기록의 동일 에러코드 인용과 일치). (c) "`!cfg.apiBase` 조기 return 은 세대를 올리지 않는다" — `applyConfig` 본문에서 `if (!cfg.apiBase || !cfg.triggerEndpointPath) return;` 이 `beginBootAttempt()` 호출보다 **먼저** 옴을 코드로 직접 확인. 세 주장 모두 "과대 주장" 이 아니라 정확한 서술이다. 이 파일이 "주석 과대 주장이 반복 적발된 곳" 이라는 전제로 특별히 요청된 항목이라 명시적으로 기록한다.
  - 제안: 없음 — 우수 사례로 기록. (단, 위 CRITICAL#2 가 지적하듯 `pendingResetRef` 는 내용이 아니라 **JSDoc-선언 결합 자체**가 깨져 있어 별도 조치 필요.)

- **[INFO]** `pendingResetRef` 계약 갱신("성공하는 부팅"의 정의에 supersede 포함)은 **내용상** 기존 계약 문장과 모순되지 않음 (요청 항목 2 — 단, 배치 문제는 위 CRITICAL#2로 별도 취급)
  - 위치: `codebase/channel-web-chat/src/widget/use-widget.ts:162-197`(`pendingResetRef` JSDoc 전체, 클린 스냅샷 라인).
  - 상세: 기존 "접수된 리셋은 다음 성공하는 부팅이 이행한다"·"BLOCKED/config 없이 끝나면 의도는 남는다"·"불변식 의존 주의"(endpoint 미구분, 재전송 호출부가 리마운트로 안전성을 보장) 세 문단과, 이번에 추가된 "'성공하는 부팅'의 정의에 supersede 포함" 문단을 대조했다. "성공" = `establishConfig` 도달(=리셋 소비 지점 도달)로 일관되게 정의하면 네 문단 모두 정합한다. 코드로도 확인: `isAttemptStale(attempt)` 검사를 통과한 뒤 **동기적으로**(중간에 `await` 없이) `establishConfig(cfg)` 가 호출되므로(establishConfig 자체가 비-async — 위 항목 참고), "대체된 시도는 리셋을 이행하지 않고 플래그를 남긴 채 물러난다" 는 새 주장에 TOCTOU 여지가 없다. `use-widget-eager-start.test.ts` 의 갱신된 두 기존 테스트("겹친 부팅의 결과가 갈릴 때…" · "겹친 부팅에서 나중 진입이 차단으로 먼저…")도 `hookPosts` 기대값 변경 + "이어서 성공 boot 을 보내면 그때 이행된다" 는 이월 단언을 함께 추가해, 이 계약 변경을 "소실이 아니라 이월" 로 정확히 증명한다.
  - 제안: 없음(내용은 정확) — 다만 위 CRITICAL#2(구조적 유실)를 반드시 함께 조치할 것. 내용이 맞아도 도구가 못 찾으면 다음 유지보수자에게 도달하지 않는다.

- **[INFO]** `plan/in-progress/webchat-boot-single-flight.md` 의 진행 기록은 실측과 정확히 일치함 (요청 항목 5)
  - 위치: `plan/in-progress/webchat-boot-single-flight.md` 전체(특히 §"진행 기록 — A 완료"·"B 완료"·"A-6 완료"·"spec code: 증거").
  - 상세: `npx vitest run` 실행 결과 "Test Files 22 passed / Tests 382 passed"(1) 는 최종 커밋(`215cd1c3f`) 시점 plan의 "channel-web-chat **382 passed**(22 파일, 신규 6건)" 주장과 정확히 일치(2)(주: 현재 워크트리는 아래 "진행 상황" 참고 이유로 383/1개 신규 테스트가 더 있음 — commit `215cd1c3f` 이후 별도 커밋). 단계별 "신규 3건"(A) + "무변경"(B) + "신규 3건"(A-6, 리듀서 `it.each` 2케이스 + 통합 1케이스) = 누적 "신규 6건" 서술도 diff의 실제 신규 `it`/`it.each` 블록 수와 정확히 일치한다. 실행 계획 체크리스트(A-0~A-6, B-1, spec `code:` 확인)는 전 항목 `[x]` 이며 각 항목이 가리키는 실제 코드(예: A-0 "토큰 캡슐화 채택" → `beginBootAttempt`/`isAttemptStale` 신설, A-6 "RESTORED/BOOTED 가드 확대" → widget-state.ts 신규 가드)와 1:1 대응한다. 인용된 과거 리뷰 세션 11개(`02_04_13` 등) 전부 `review/code/2026/07/17/<id>/RESOLUTION.md` 로 실재를 확인했고, "출처: PR #964" 인용도 `git log` 커밋 제목(`5de44d4d6 … ⑨-4 … (#964)`)과 정확히 일치한다.
  - 제안: 없음. 다만 이 문서도 "§106" 을 6곳에서 인용하므로 위 WARNING(§106 라인 드리프트) 정정 시 함께 갱신 대상.

## 요약

핵심 로직(`bootGenRef`/`beginBootAttempt`/`isAttemptStale`/`establishConfig`)의 JSDoc 내용과 plan 문서의 진행 기록은 이례적으로 정확하다 — 사용자가 특히 의심한 "컴파일되지 않는다"류 주장 3건을 `tsc` 로 직접 재현해 전부 사실로 확인했고, `pendingResetRef` 계약 갱신 문구도 기존 계약과 논리적으로 모순이 없으며, plan의 테스트 카운트·체크리스트·과거 세션 인용은 실측과 한 글자도 어긋나지 않았다. 그럼에도 **CRITICAL 3건**을 발견했다 — (1) 이 diff 자신이 신설한 `RESTORED`/`BOOTED` 가드가 바로 옆 `WAITING` case의 기존 주석("가드 범위는 WAITING 뿐")을 그 자리에서 반증시켰고, (2) `pendingResetRef` 의 갱신된 계약 JSDoc(이번 리뷰가 특히 확인을 요청한 supersede 문단 포함)이 `bootGenRef` JSDoc 삽입으로 선언과의 인접성을 잃어 TypeScript API 실측 결과 IDE·TypeDoc 등 모든 JSDoc 소비 툴에서 완전히 유실되며(반대로 `bootGenRef` 가 두 블록을 다 흡수), (3) 커밋 메시지 스스로 "(사용자 가시)" 로 태깅한 A-6 버그 수정이 이 저장소의 명확한 선례(직전 CHANGELOG 항목이 정확히 같은 버그 클래스를 "사용자 가시 버그 수정"으로 기재)에도 불구하고 `CHANGELOG.md` 에 반영되지 않았다. 셋 다 내용이 아니라 **전달·배치·누락**의 문제이며 수정 비용은 낮다. 부가로 spec `code:` 신규 주석의 자기참조 "§106" 이 같은 diff의 frontmatter 편집으로 즉시 4줄 밀려 stale 화되는 것(WARNING, 이번 diff가 새로 쓴 6곳 이상에 영향)과, 리뷰 payload 자체에 이 브랜치와 무관한 31개 파일(별도로 이미 머지된 PR #966 의 되돌리기 아티팩트)이 섞여 들어온 것(INFO, 조치 불요이나 SUMMARY 통합 시 유의 필요)을 기록한다. 참고로 이 리뷰 작성 도중 공유 워크트리에서 `215cd1c3f` 이후 새 커밋(`b1bef8633`, "리뷰가 찾은 CRITICAL 3건 — supersede 설계 결함 + 부작용 누출 + JSDoc 유실")이 이미 랜딩된 것을 관측했다 — 커밋 메시지가 "ai-review 17_36_57/17_48_20 반영" 을 명시하고 "JSDoc 유실" 을 특정하는 것으로 보아 본 리뷰의 CRITICAL#2 와 동일한 결함을 이미 겨냥한 후속 수정으로 보인다. 다만 이는 이번에 리뷰 대상으로 주어진 diff(`215cd1c3f`) 밖의 별도 커밋이므로, 최종 판단·중복 여부 확인은 오케스트레이터가 현재 HEAD 기준으로 재조회할 것을 권장한다.

## 위험도

HIGH
