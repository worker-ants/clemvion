# Code Review 통합 보고서

*검토 범위: 폐기 로직 + `bootGenRef` 제거(**-52/+30 순삭제**). 이 결함 클래스를 끝까지 추적한 2개 reviewer(concurrency·side_effect) 실행 — 상세는 하단 "라우터 결정".*

## 전체 위험도

**LOW** — **결함 클래스가 닫혔음이 두 reviewer 의 실측으로 확인됐다**. 네 방향 수렴·플래그 잔존 무해성이 각각 증명됐고, 첫 라운드의 "유령 리셋" 프레이밍이 과했다는 재평가도 **그 지적을 냈던 reviewer 본인**이 확인했다. WARNING 3건은 전부 **테스트 사각지대·문서 정확성**이며 코드 동작 결함은 0건이다.

**워크트리 위생**: 2인 모두 격리 worktree 에서 mutation/PROBE 를 수행하고 제거했다. 오염 없음.

## Critical 발견사항

없음.

## 경고 (WARNING)

| # | 카테고리 | 발견사항 | 제안 |
|---|----------|----------|------|
| 1 | 테스트 — concurrency, **실측** | **회귀 테스트 4건의 사각지대**: `bootGenRef` 소유권 설계를 **원본 그대로(3줄) 재도입**해도 "혼합 순서" 테스트가 **통과**한다 — 그 테스트는 옛 설계의 **안전한 절반**만 고정하고, 실제 결함을 냈던 반대 절반(소유권자=최신 진입이 차단으로 먼저 끝나며 폐기 → 살아있는 이전 진입의 리셋 소실)은 못 잡는다. 네 번째 잘못된 설계가 무방비. | ④ PROBE 를 5번째 정식 회귀 테스트로 승격. **→ 조치됨**(RESOLUTION §W1): 재도입 시 2건 실패(전엔 0건). |
| 2 | 부작용 — side_effect | **`pendingResetRef` 가 `triggerEndpointPath` 를 구분하지 않음** — 리셋이 접수된 endpoint 와 소비하는 부팅의 endpoint 가 같은지 검사하지 않는다. 폐기 제거로 노출 창이 마운트 생존 기간 전체로 넓어졌다(단 `09_36_01` 도입 시점부터 있던 설계 공백이지 이번 diff 가 만든 건 아님). **오늘 기준 도달 불가**: 유일한 재전송 경로인 `live-preview.tsx` 는 endpoint 가 바뀌면 `iframe` 을 리마운트해 훅 상태를 초기화한다. | JSDoc 에 불변식 의존 명문화(코드 조치 불요). **→ 조치됨**(§W2). |
| 3 | 문서 — side_effect | **죽은 참조**: 테스트 주석이 제거된 `bootGenRef` 소유권 메커니즘을 현재형으로 서술 — `grep -rn bootGenRef codebase/` 결과 **코드베이스 전체 중 유일한 잔존 참조**. 이 파일은 그 메커니즘에서 4라운드 연속 회귀가 난 hotspot 이라 오인 재도입 위험이 일반적 stale comment 보다 실질적으로 크다. | "폐기 로직의 완전한 부재를 지킨다"는 언어로 교체. **→ 조치됨**(§W3). |

## 참고 (INFO)

| # | 발견사항 |
| --- | --- |
| 1 | **네 방향 수렴 확인**(concurrency): `pendingResetRef` 참조가 파일 전체 **정확히 2곳**(set·consume)뿐이고 BLOCKED 분기는 둘 다 안 건드린다. `bootGenRef` 가 사라져 "진입 순서" 개념 자체가 코드에서 없어졌다. ④ PROBE 로 **현재 HEAD PASS / 옛 커밋 FAIL** 대조 확인. |
| 2 | **플래그 영구 잔존은 구조적으로 무해**(concurrency, 증명): `configRef.current` 가 null 로 안 돌아가므로 지연 이행은 **그 마운트의 최초 성공 부팅에서만** 가능 — 그 이전엔 파괴할 활성 대화가 없다. |
| 3 | **"유령 리셋" 재프레이밍 타당**(side_effect Q1): origin 핀 + `useRef` 마운트 스코프로 "다른 host·다른 인스턴스로 새는" 경로가 없다. **자신이 `11_38_14` 에서 낸 프레이밍이 과했음을 스스로 확인**. |
| 4 | **`wc:boot` 재전송 경로는 오늘 단 하나**(side_effect): 공개 SDK·스니펫 로더는 전부 `shutdown()` 후 새 iframe 을 만든다. 기존 iframe 재사용 재전송은 관리자 미리보기뿐. |
| 5 | **소비 시점 UX 불투명성**(side_effect): `blocked` 는 런처조차 없는 완전 `null` 렌더라, 리셋 클릭이 차단된 부팅에서 실패해도 신호가 없고 이후 무관해 보이는 조작으로 뒤늦게 이행된다. **영향 반경은 관리자 1인의 미리보기 세션**으로 국한(실 고객 세션 미도달). 별개 UX 개선 트랙. |
| 6 | 전체 스위트 **375 passed 독립 재현**(side_effect) — 자기보고 수치와 일치. |

## 에이전트별 위험도 요약

| 에이전트 | 위험도 | 핵심 발견 |
|----------|--------|-----------|
| concurrency | MEDIUM | 네 방향 수렴·플래그 무해성을 각각 실측/증명. **WARNING#1**(테스트 사각지대)을 원본 재도입 실험으로 발견 — 이번 라운드 최대 성과. |
| side_effect | LOW | 0 CRITICAL. Q1 재프레이밍 타당성 확인(자기 정정), 재전송 경로 전수 조사, WARNING#2·#3 발견. |

## 라우터 결정

- router 미실행. 델타가 **순삭제**라, 이 결함 클래스를 끝까지 추적해 온 두 관점을 main 이 선별 실행했다.
  - **실행(2명)**: `concurrency`, `side_effect`
  - **미실행(12명)**: 이 코드는 직전 네 라운드가 누적 검토했고 이번 델타는 코드를 **지우는** 방향이라 판단.
  - **한계 명시**: router 의 의미 기반 판단이 아니라 main 의 수동 선별이다. 미실행이 "그 관점에서 깨끗함"을 뜻하지 않는다.
