# RESOLUTION — ai-review 14_30_15 (폐기 로직 제거 재검토)

대상 범위: 폐기 로직 + `bootGenRef` 제거(-52/+30 순삭제). Critical 0 / Warning 3. 2인 reviewer(concurrency·side_effect).

**요약**: **결함 클래스가 닫혔음이 두 reviewer 의 실측으로 확인됐다** — 네 방향 수렴·플래그 잔존 무해성이 각각 증명됐고, 첫 라운드의 "유령 리셋" 재프레이밍도 타당하다고 확인됐다. WARNING 3건은 전부 **테스트 사각지대·문서 정확성**이며 조치 완료. 코드 동작 변경은 없다.

## 확인된 것 (두 reviewer 실측)

- **네 방향 수렴** (concurrency): `pendingResetRef` 참조 지점이 파일 전체 **정확히 2곳**(set·consume)뿐이고 BLOCKED 분기는 둘 다 안 건드린다. `bootGenRef` 가 사라져 "진입 순서" 개념 자체가 코드에서 없어졌으므로 네 방향이 동일 경로로 수렴한다. ④ 시퀀스 PROBE 를 새로 작성해 **현재 HEAD PASS / 옛 `bootGenRef` 커밋 FAIL** 로 대조 확인.
- **플래그 영구 잔존은 구조적으로 무해** (concurrency, 증명): `configRef.current` 는 null 로 되돌아가지 않으므로(대입 2곳·해제 0곳) 지연 이행은 **그 마운트의 최초 성공 부팅에서만** 가능하다 — 그 이전엔 활성 대화 자체가 없으므로 "한참 뒤 엉뚱한 시점에 기존 대화가 사라지는" 피해는 **발생할 수 없다**.
- **"유령 리셋" 재프레이밍 타당** (side_effect, Q1): origin 은 첫 `wc:boot` 에서 핀되고 재전송도 동일 origin 만 허용되며, `pendingResetRef` 는 `useRef` 라 해당 마운트(=해당 iframe 인스턴스)에 스코프된다 → "다른 host·다른 인스턴스로 새는" 경로가 없다. **자신이 `11_38_14` 에서 낸 "2차 boot 의 host 는 요청한 적 없다" 프레이밍이 과했음을 스스로 확인**했다.
- **`wc:boot` 재전송 경로는 오늘 코드베이스에 단 하나** (side_effect): 공개 SDK 의 `boot()` 재호출·스니펫 로더는 전부 기존 인스턴스를 `shutdown()` 후 **새 iframe** 을 만든다. 기존 iframe 재사용 재전송은 관리자 미리보기(`live-preview.tsx`)뿐.
- **전체 스위트 375 passed 독립 재현** (side_effect) — 내 자기보고 수치와 일치.

## Warning

### W1 (concurrency) — 회귀 테스트 4건의 사각지대. **5번째 테스트 추가**

`bootGenRef` 소유권 설계를 **원본 그대로(3줄) 재도입**하는 실험에서, 커밋된 "혼합 순서" 테스트가 **그대로 통과**했다 — 그 테스트는 옛 설계의 **안전한 절반**(먼저 진입한 쪽이 차단으로 먼저 끝남)만 고정하고, 실제로 결함을 냈던 **반대 절반**(소유권자=최신 진입이 차단으로 먼저 끝나며 폐기 → 아직 살아있는 이전 진입의 리셋 소실)은 잡지 못한다.

- **내 mutation 매트릭스의 공백이었다**: 나는 "BLOCKED 폐기 재도입"·"entry-clear 재도입"·"소비 제거"만 검증했고 **`bootGenRef` 설계 자체의 재도입은 시험하지 않았다**. 네 번째 잘못된 설계가 무방비였다.
- **수정**: 리뷰어의 PROBE 를 5번째 정식 회귀 테스트로 승격 — "겹친 부팅에서 나중 진입이 차단으로 먼저 끝나도 먼저 진입한 쪽이 리셋을 이행한다".
- **검증**: `bootGenRef` 3줄 재도입 → **신규 테스트 포함 2건 실패**(재도입 전엔 0건). 이제 **내가 시도한 네 가지 잘못된 설계가 전부 테스트로 막힌다**.

### W2 (side_effect) — `pendingResetRef` 가 `triggerEndpointPath` 를 구분하지 않음. **불변식 명문화**

리셋이 접수됐던 endpoint 와 나중에 소비하는 부팅의 endpoint 가 같은지 검사하지 않는다. 이론상 `X` 로 부팅 중 접수된 리셋이 `Y` 의 세션을 초기화할 수 있다.

- **오늘 기준 도달 불가**(리뷰어 확인): 유일한 재전송 경로인 `live-preview.tsx` 는 `endpointPath` 가 바뀌면 `<iframe key={iframeSrc}>` 가 **리마운트**되어 훅 상태(이 ref 포함)가 통째로 초기화된다. 한 마운트 안에서 서로 다른 endpoint 로 `applyConfig` 가 두 번 불리는 경로가 없다.
- **이번 diff 가 만든 갭은 아니나**(`09_36_01` 도입 시점부터 있던 설계 공백) 폐기 제거로 **노출 창이 마운트 생존 기간 전체로 넓어진 것은 사실**이다.
- **수정**: 리뷰어 제안대로 JSDoc 에 **불변식 의존 주의**를 `teardownSession` 의 기존 동형 주석과 같은 형태로 명문화 — "재전송 호출부가 마운트를 유지한 채 endpoint 를 바꾸지 않는다는 전제에 의존한다. 그 전제가 깨지면 이 조건도 재검토할 것." 코드 조치는 불요(도달 불가).

### W3 (side_effect) — 죽은 참조: 테스트 주석이 제거된 `bootGenRef` 를 현재형으로 서술. **수정**

`grep -rn bootGenRef codebase/` 결과 **코드베이스 전체 중 유일한 잔존 참조**였다. 이 파일은 정확히 그 메커니즘에서 **4라운드 연속 회귀**가 난 hotspot 이라, 유지보수자가 이 주석만 보고 "세대 소유권이 아직 존재한다"고 오인해 재도입을 시도할 위험이 일반적인 stale comment 보다 실질적으로 크다는 지적 — 타당하다.

- **수정**: "이 테스트가 지키는 것은 어떤 가드 조건이 아니라 **폐기 로직의 완전한 부재**다 — 폐기를 어떤 형태로든 재도입하면 이 테스트가 깨진다. 그게 의도다" 로 교체.

## INFO

- **소비 시점 UX 불투명성** (side_effect Q1): `blocked` 는 런처조차 없이 완전 `null` 렌더라, 관리자가 리셋을 눌렀는데 그 부팅이 차단되면 실패 신호가 없고, 이후 무관해 보이는 조작(외형 폼 수정 등)으로 부팅이 성공하는 순간 잊었을 과거 클릭이 뒤늦게 이행된다 — "결과적으로 정답이지만 타이밍이 놀라운" 케이스. **영향 반경은 운영 콘솔 관리자 1인의 미리보기 세션**으로 국한되며 실 고객 세션에 닿지 않는다. 이번 diff 범위 밖의 별개 UX 개선이라 이월.
- **cross-endpoint 리셋 번짐** — W2 와 동일 건. 두 reviewer 가 독립 지적했고 둘 다 "이번 diff 로 악화되지 않음 / 도달 불가"로 판정.

## 검증

- **TEST WORKFLOW**: lint PASS(74s) · unit PASS(72s, `tests=14 passed`) · build PASS(120s).
- **channel-web-chat**: 22 파일 **376 passed**(vitest 실측).
- **mutation — 네 가지 잘못된 설계가 전부 잡힌다**:

  | 재도입한 설계 | 실패 테스트 수 |
  | --- | --- |
  | 진입 시 일괄 폐기 (2번째 시도) | 3건 |
  | BLOCKED 한정 폐기 (3번째 시도) | 2건 |
  | **`bootGenRef` 소유권 (4번째 시도)** | **2건** ← 본 라운드에 닫음 |
  | 소비 자체 제거 | 4건 |

## 이월

- **`applyConfig` single-flight/supersede** (spec `2-sdk.md:106` "마지막 `wc:boot` 적용" 갭) — 두 reviewer 모두 이 이월 판단이 타당하다고 확인.
- **cross-endpoint 리셋 번짐** — 도달 불가. 불변식이 깨질 때 재검토(JSDoc 에 고정).
- **미리보기 BLOCKED 상태 안내 문구** (INFO) — UX 개선, 별개 트랙.
- `state.phase` flicker(이전부터 존재) · 이전 라운드들의 이월 승계.

**교훈**: **"잘못된 설계를 재도입하면 잡히는가"를 mutation 으로 시험할 때, 내가 실제로 시도했던 설계를 전부 넣어봐야 한다.** 나는 세 가지만 시험하고 네 번째(`bootGenRef`)를 빠뜨렸는데, 하필 그게 가장 최근까지 코드에 있던 설계라 재도입 유혹이 가장 큰 것이었다. 리뷰어가 그 공백을 실측으로 메웠다.
