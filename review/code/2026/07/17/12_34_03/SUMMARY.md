# Code Review 통합 보고서

*검토 범위: `b7a2cc063..HEAD` — 직전 라운드 `12_04_49` 의 조치 3건(리셋 폐기 위치 되돌림 + 테스트 견고성 2건). 델타 2파일. 직전 라운드에서 이 결함들을 발견·재현한 2개 reviewer 를 실행 — 상세는 하단 "라우터 결정".*

## 전체 위험도

**MEDIUM** — 직전 라운드의 조치 3건은 **두 reviewer 가 각자 실측으로 유효함을 확인**했다(거짓 음성 해소·전역 상태 누수 해소·양방향 상호보호). 그러나 인접 경로를 조사하던 중 **두 reviewer 가 독립적으로 같은 신규 활성 결함을 순서-대조로 재현**했다 — 겹친 두 부팅의 결과가 갈리고 차단된 쪽이 먼저 resolve 하면 리셋이 소실된다. 같은 계열의 **세 번째 거울상**이며, 두 reviewer 모두 "국소 패치로는 이 클래스를 소거할 수 없고 세대 카운터 같은 **구조적 해법**이 필요하다"는 동일 결론에 도달했다.

**워크트리 위생**: 2인 모두 격리 worktree 에서 mutation/probe 를 수행하고 제거했다(side_effect 1개·testing 2개 순차). 오염 재발 없음.

## Critical 발견사항

없음.

## 경고 (WARNING)

| # | 카테고리 | 발견사항 | 위치 | 제안 |
|---|----------|----------|------|------|
| 1 | 부작용/테스트 — **2인 독립 발견**, 양쪽 다 **순서-대조 실측 재현** | 겹친 두 `applyConfig` 의 결과가 갈리고(하나 BLOCKED, 하나 ALLOWED) **차단된 쪽이 먼저 resolve** 하면, BLOCKED 분기의 무조건 폐기가 **아직 살아있는 ALLOWED 시도가 소비했어야 할 정당한 리셋**을 지운다. **원인**: BLOCKED 분기는 `worldGenRef` 를 올리지 않는다(세계가 바뀐 게 아니라 이 시도가 실패했을 뿐) → 코드 주석이 근거로 삼는 "자기치유"(먼저 소비한 쪽이 나머지를 stale 화)는 **ALLOWED 경로에만 성립**한다. 그 비대칭이 구멍. **실측**: BLOCKED 가 먼저 resolve → `hookPosts=0`/구 세션 잔존; **순서만 뒤집으면**(ALLOWED 먼저) `hookPosts=1`/정상 — 순서-대조로 원인 확정. **실사용 경로**: 관리자 라이브 미리보기에서 다른 `triggerEndpointPath`(다른 임베드 정책)로 전환하는 도중 리셋 버튼을 누르는 조합. 커밋된 두 회귀 테스트 어느 쪽도 이 조합을 커버하지 않음. | `use-widget.ts` — BLOCKED 분기의 무조건 폐기 · 소비 지점 · `teardownSession` 부팅-전 분기 | 국소 패치를 반복하지 말고 **세대 카운터 등 구조적 해법**으로 전환할 것(양 reviewer 공통 결론). side_effect 는 "이번 라운드에서 되돌리거나 더 손대는 것은 권고하지 않는다 — 그것이 바로 `12_04_49` 회귀의 원인이었다"고 명시. **→ 조치됨**(RESOLUTION §W1): 사용자 결정에 따라 `bootGenRef`(부팅 시도 세대) 도입, `pendingResetRef` 의 **소유권**을 "살아있는 최신 시도"로 명시. 세 방향이 동시에 닫혔고 mutation 매트릭스 3종으로 각 요소 독립 고정. |
| 2 | 문서 — testing | 코드 주석의 리뷰 라벨 인용이 `RESOLUTION.md` 번호와 하나씩 밀려 있다. | `use-widget.ts` 주석 | 정정. **→ 조치됨**(구조 전환에 맞춰 주석 전면 재작성하며 라운드 인용을 실제 발견 순서로 정렬). |

## 참고 (INFO)

| # | 발견사항 |
| --- | --- |
| 1 | (side_effect) **남은 미소비 체크포인트는 무해** — `!cfg.apiBase` 조기 return 과 첫 `isStale` 은 `pendingResetRef` 를 아예 참조하지 않는다. grep 전수 확인 결과 이 ref 를 건드리는 곳은 set·BLOCKED 폐기·소비 3곳뿐. |
| 2 | (side_effect·testing) **직전 라운드 조치 3건 전부 유효 확인** — 거짓 음성 fix(2차 boot 을 차단 설정으로 치환하는 probe → 이제 실제 실패) · 전역 상태 누수 fix(고의 실패 + 더미 테스트 → 누수 사라짐) · 양방향 상호보호(두 방향 mutation → 두 테스트가 서로 다른 축을 지키는 상보적 쌍). |
| 3 | (testing) resolve 순서 역전(둘 다 ALLOWED)은 **안전함**을 확인 — 어느 쪽이 먼저 resolve 하든 소비한 쪽이 `worldGen` 을 올려 나머지를 stale 화한다. |
| 4 | (side_effect) **INFO#3(single-flight 부재) 이월 판단은 타당** — 이번 발견이 오히려 그 판단을 강화한다(국소 패치로 소거 불가). 다만 이 구체적 경로를 backlog 에 명시 append 하고 "자기치유" 서술에 성공-경로 한정 단서를 달 것을 권고. |

## 에이전트별 위험도 요약

| 에이전트 | 위험도 | 핵심 발견 |
|----------|--------|-----------|
| side_effect | MEDIUM | 질문 1(양방향 폐쇄)·2(남은 체크포인트 무해)를 독립 검증. WARNING#1 을 순서-대조로 실측 재현하고 **원인(자기치유의 성공-경로 한정)**까지 진단. INFO#3 이월 판단이 타당함을 확인하며 "더 손대지 말 것" 권고. |
| testing | LOW | 직전 라운드 조치 3건을 격리 worktree 6종 mutation/probe 로 전수 검증. WARNING#1 을 독립 재현(side_effect 와 동일 결론). 양방향 상호보호 확인. |

## 라우터 결정

- router 미실행. 델타가 **직전 라운드 지적 3건에 대한 국소 조치**라, 그 지적을 낸 두 관점을 main 이 선별 실행했다.
  - **실행(2명)**: `side_effect`(플래그 수명·소유권), `testing`(회귀 검출력·mutation)
  - **미실행(12명)**: 직전 세 라운드(`09_36_01` 8인 · `11_38_14` 3인 · `12_04_49` 2인)가 이 코드를 누적 검토했고 이번 델타는 그 지적에 대한 조치라 판단.
  - **한계 명시**: router 의 의미 기반 판단이 아니라 main 의 수동 선별이다. 미실행이 "그 관점에서 깨끗함"을 뜻하지 않는다. 다만 이번 신규 발견이 실행된 2인 **양쪽에서** 독립적으로 나왔다는 점은 선별 관점이 이 결함 클래스에 정확히 맞았음을 시사한다.
