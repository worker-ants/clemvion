# RESOLUTION — ai-review 12_34_03 (`12_04_49` 조치 재검토)

대상 범위: `b7a2cc063..HEAD`. RISK MEDIUM / Critical 0 / Warning 2 / INFO 다수. 2인 reviewer.

**요약**: 직전 라운드의 조치 3건은 두 reviewer 가 각자 실측으로 유효함을 확인했다. 그러나 **두 reviewer 가 독립적으로 같은 신규 결함을 순서-대조로 재현**했다 — 같은 계열의 **세 번째 거울상**이다. 사용자 결정("부팅 시도별 스코프로 근본 해결")에 따라 국소 패치를 그만두고 **소유권을 명시**하는 구조로 전환했고, 그제서야 세 방향이 동시에 닫혔다.

## Warning

### W1 (side_effect·testing **독립 발견**, 양쪽 다 순서-대조 재현) — 혼합 순서에서 리셋 소실. **확인·근본 수정**

겹친 두 부팅의 결과가 갈리고(**하나는 BLOCKED, 하나는 ALLOWED**) **차단된 쪽이 먼저 resolve** 하면, BLOCKED 분기의 무조건 폐기가 **아직 살아있는 ALLOWED 시도가 소비했어야 할 정당한 리셋**을 지운다.

- **원인 (side_effect 진단)**: BLOCKED 분기는 **`worldGenRef` 를 올리지 않는다** — 세계가 바뀐 게 아니라 이 시도가 실패했을 뿐이다. 그래서 성공 경로의 자기치유("먼저 소비한 쪽이 `newChat`→세대 증가로 나머지를 stale 화")가 **이 분기엔 성립하지 않는다**. 그 비대칭이 구멍이었다.
- **실측**: BLOCKED 가 먼저 resolve → `hookPosts=0`/구 세션 잔존. **순서만 뒤집으면**(ALLOWED 먼저) `hookPosts=1`/정상. 순서-대조로 원인 확정.
- **실사용 경로**: 관리자 라이브 미리보기에서 **다른 `triggerEndpointPath`(다른 임베드 정책)로 전환**하는 도중 리셋 버튼을 누르는 조합.

**이 시점에 국소 패치를 중단했다.** 같은 결함 클래스에 낸 fix 가 세 번 연속 반대편 구멍을 만들었고(유령 리셋 → 진입-시 일괄 폐기로 막자 리셋 소실 → BLOCKED 한정 폐기로 막자 혼합 순서에서 소실), 매번 "작고 안전하다"는 내 판단이 틀렸다. 두 reviewer 모두 "국소 패치로는 이 클래스를 소거할 수 없고 세대 카운터 같은 **구조적 해법**이 필요하다"고 결론냈고, 사용자에게 판단을 구해 **근본 해결**을 지시받았다.

- **수정**: `bootGenRef`(부팅 시도 세대) 도입 — `applyConfig` 호출 1건 = 1세대. 이 세대는 오직 **`pendingResetRef` 의 소유권**을 정한다: **"살아있는 최신 시도만 그 의도를 폐기할 수 있다"**(`if (bootGenRef.current === bootGen) pendingResetRef.current = false;`).
  - `worldGenRef` 와 축이 다르다 — 부팅 시도는 세계를 바꾸지 않는다(그래서 `teardownSession` 이 부팅 중엔 세대를 안 올린다 = C1 fix). 축을 합치지 않고 나란히 둔 이유를 JSDoc 에 명시했다.
  - **성공 경로엔 세대 검사를 걸지 않았다** — 거긴 `worldGen` 자기치유가 이미 성립한다(먼저 소비한 쪽이 나머지를 stale 화). 필요 없는 곳에 가드를 넣지 않는 게 이 라운드들의 교훈이다.
  - `!cfg.apiBase` 조기 return 은 세대를 올리지 않는다 — 시도로 치지 않는다.
- **회귀 테스트 3건이 서로를 지킨다**(mutation 매트릭스로 각 요소가 독립 고정됨을 확인):

  | mutation | 실패하는 테스트 |
  | --- | --- |
  | BLOCKED 폐기 자체 제거 | ①유령 리셋 **만** |
  | **세대 조건 제거**(무조건 폐기) | ③혼합 순서 **만** ← 본 W1 |
  | entry-clear 재도입(`12_04_49` 의 잘못된 fix) | ②리셋 소실 · ③혼합 순서 |

  어느 방향으로 "고쳐도" 반대편이 잡힌다.

### W2 (testing) — 코드 주석의 리뷰 라벨이 RESOLUTION 번호와 밀림. **수정**

주석을 이번 구조 전환에 맞춰 전면 재작성하며 라운드 인용(`11_38_14` · `12_04_49` · `12_34_03`)을 실제 발견 순서에 맞췄다.

## 직전 라운드 조치의 검증 (두 reviewer 독립 확인)

- **거짓 음성 fix**: adversarial probe(2차 boot 도 차단 설정으로 치환) 재현 → 이제 `waitFor(streaming)` 에서 **실제로 실패**. 해소 확인.
- **전역 상태 누수 fix**: 고의 실패 + 더미 테스트 관측 → `document.referrer` 가 전역 `afterEach` 에서 정상 복원, **누수 사라짐**.
- **양방향 상호보호**: 두 방향 mutation 으로 두 테스트가 서로 다른 축을 지키는 상보적 쌍임을 확인.
- **남은 미소비 체크포인트**: `!cfg.apiBase` 조기 return·첫 `isStale` 은 `pendingResetRef` 를 아예 참조하지 않으며 **무해함**을 grep 전수 + 추적으로 확인(side_effect).

## 검증

- **TEST WORKFLOW**: lint PASS(57s) · unit PASS(72s, `tests=14 passed`) · build PASS(120s).
- **channel-web-chat**: 22 파일 **375 passed**(vitest 실측).
- 회귀 테스트 3건, **mutation 매트릭스 3종**으로 각 가드 요소를 독립 검증(위 표).

## 이월

- **`applyConfig` single-flight/supersede** — 겹친 시도 중 **어느 config 가 최종 적용되는가** 는 여전히 resolve 순서가 정한다. spec `2-sdk.md:106` 은 "마지막 `wc:boot` 의 config 를 적용"이라 하므로 갭이다. `bootGenRef` 가 이미 있으니 확장 지점은 마련됐으나, **이 fix 와 독립 축**이라 손대지 않았다 — side_effect 도 이 이월 판단이 타당하다고 확인했다("국소 패치로는 소거할 수 없다"는 근거가 오히려 강화됨). JSDoc 에 이 갭을 명시했다.
- 이전 라운드들의 이월(`RESTORED`/`BOOTED` 가드 확대 · `guardedAwait` · `useTokenRefresh` 종단 테스트 · 리뷰 파이프라인 정책)을 승계한다.

**교훈 (이 결함 클래스 전체)**: **플래그의 수명(scope)과 소유권(누가 지울 수 있는가)을 명시하지 않으면, 한 방향을 막을 때마다 반대편이 열린다.** 세 라운드에 걸쳐 국소 패치를 세 번 시도했고 세 번 다 거울상을 만들었다. "이 fix 는 작고 안전하다"는 판단이 3연속 틀렸다는 사실 자체가 **구조로 올려야 한다는 신호**였는데, 그 신호를 두 reviewer 가 먼저 읽었다. 경합 상태를 다룰 땐 **반대 방향 테스트를 함께 두는 것**이 fix 의 일부다 — 이번엔 세 방향이 서로를 지킨다.
