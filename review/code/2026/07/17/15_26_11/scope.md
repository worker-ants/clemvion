# 변경 범위(Scope) 리뷰 — 2026-07-17 15_26_11

**대상**: `codebase/channel-web-chat/src/widget/use-widget-eager-start.test.ts` (직전 라운드 `14_56_27` W1·W2 조치, 프로덕션 코드 0줄). 페이로드에 함께 포함된 나머지 6개 파일(`review/code/2026/07/17/{14_30_15,14_56_27}/*`)은 별도 절에서 다룬다.

이 델타의 실제 커밋을 `git log`/`git show`로 직접 확인했다(추정 아님) — 실 fix 는 `e9dcb27c9`(`test(web-chat): 회귀 테스트 2건의 BLOCKED 전제 고정 + "유일한 가드" 문구 정정`) 단일 커밋이며 `git show --stat e9dcb27c9` 결과 `use-widget-eager-start.test.ts` 1개 파일, 18 insertions(+)/4 deletions(-)만 변경했다. 이는 오케스트레이터가 서술한 "프로덕션 코드 0줄, 회귀 테스트 2건 전제 단언 추가 + 주석 정정"과 정확히 일치한다.

## 발견사항

- **[INFO]** 직전 라운드 리뷰어가 "이번 diff 범위는 아님"으로 명시했던 인접 기존 테스트까지 같은 패턴으로 함께 수정
  - 위치: `codebase/channel-web-chat/src/widget/use-widget-eager-start.test.ts:2275`("겹친 부팅의 결과가 갈릴 때, 차단된 쪽이 살아있는 쪽의 리셋을 지우지 않는다" — 이번 커밋이 만든 게 아닌 기존 테스트, hunk 1) vs `:2359`("겹친 부팅에서 나중 진입이 차단으로 먼저 끝나도 먼저 진입한 쪽이 리셋을 이행한다" — 신규 거울상 테스트, hunk 2·3, `14_56_27` W1/W2 가 지목한 필수 대상).
  - 상세: `14_56_27` 라운드의 `testing.md`(이번 페이로드 파일 7)를 원문 대조한 결과, W2 제안은 "`const { result } = renderHook(...)`로 변경하고 ... `:2103`과 동일한 전제-고정 패턴을 적용할 것을 권고. **여유가 되면 `:2275`도 같은 패턴으로 후속 정리(이번 diff 범위는 아님)**"라고 `:2275`(당시 번호)를 명시적으로 선택적(optional)·차후(follow-up) 항목으로 스코핑했다. 그런데 실제 커밋 `e9dcb27c9`는 `:2350`(신규 테스트, 현 `:2359`)뿐 아니라 `:2275`도 **같은 커밋에서** 함께 고쳤다(`result` 캡처 + `expect(result.current.state.phase).toBe("blocked")` 동일 2줄 패턴). "요청된 변경 외 추가 수정 포함 여부"라는 리뷰 기준 1번 항목에 문자 그대로는 해당한다.
  - 다만 다음 근거로 통상적 의미의 스코프 크립(무관한 리팩터링·기능 확장·타 파일 수정)과는 성격이 다르다고 판단한다: (a) `:2275`의 결함이 별개 발견이 아니라 **같은 리뷰어가 같은 조사에서 대조실험(`document.referrer` 제거)으로 직접 증명한 동일 결함 클래스**다(`testing.md`: "동일한 실험을 이번 diff 와 무관한 기존 `:2275`에도 적용했더니 동일하게 PASS했다"). (b) 두 테스트는 같은 파일·같은 `describe` 블록(`"useWidget — 종료/staleness 가드 (ai-review 2026-07-17 02_31_18 W5·W6·W7)"`, `:1388`)·84줄 이내에 있는 자매 테스트다. (c) 적용한 수정은 이미 신규 테스트에 적용하기로 확정된 것과 **완전히 동일한 2줄 패턴** 재적용이며 새로운 로직·헬퍼·구조 변경이 없다. (d) 커밋 메시지가 "리뷰어가 '이번 diff 가 만든 게 아니다' 라고 밝힌 기존 '혼합 순서' 테스트도 같은 패턴이라 함께 정정 — 물려받은 결함이라도 이번에 알았으므로"라고 사유를 투명하게 공개했다. (e) "검증: 둘 다 referrer 제거 시 이제 실패(퇴화 탐지). bootGenRef 재도입 탐지도 약해지지 않음(여전히 2건)"이라고 부작용 없음까지 커밋 메시지에서 직접 검증했다. 프로덕션 코드·무관 파일·import·설정 변경은 전혀 수반되지 않았다.
  - 제안: 조치 불요(정당한 확장으로 판단). 다만 향후 "이번 diff 범위는 아님"으로 명시된 항목을 같은 커밋에 포함시킬 때는, 지금처럼 커밋 메시지에 그 사실과 근거·검증을 명시하는 관행을 계속 유지할 것.

- **[INFO]** 리뷰 payload 에 이번 라운드의 실제 fix 커밋과 무관한 이전 라운드 산출물 6건이 "리뷰 대상 파일"로 함께 포함됨
  - 위치: 파일 2~7 — `review/code/2026/07/17/14_30_15/SUMMARY.md`, `review/code/2026/07/17/14_56_27/{RESOLUTION.md, SUMMARY.md, _retry_state.json, meta.json, testing.md}`.
  - 상세: `git log`로 확인한 결과 이 6개 파일은 실 fix 커밋(`e9dcb27c9`)과는 별개의, 서로 다른 두 "docs-only" 커밋에서 왔다 — `33dbc0777`("docs(review): ai-review 14_56_27 RESOLUTION", `RESOLUTION.md`+`_retry_state.json`+`meta.json`+`testing.md` 4파일)와 `cfe2f5630`("docs(review): 14_30_15 · 14_56_27 SUMMARY 추가", 두 라운드의 `SUMMARY.md` 2파일). 두 커밋 모두 fix 커밋과 섞이지 않은 독립 커밋이며, 내용은 순수하게 과거 라운드(`14_30_15`, `14_56_27`)의 리뷰 산출물을 정식 위치(`review/code/<timestamp>/`, CLAUDE.md 규약)에 사후 기록하는 문서 작업이다(`meta.json`/`_retry_state.json` 은 오케스트레이터 내부 상태, `testing.md`/`RESOLUTION.md`/`SUMMARY.md` 는 그 라운드 자신의 리뷰·해결 기록 — 전부 내용 대조 완료, 이상 없음). 즉 개발자의 fix 작업이 스코프를 넘은 것이 아니라, 이 스코프 리뷰 payload 의 diff 베이스(`e9dcb27c9`의 부모 커밋 `2a9d8e2db`)가 그 사이에 커밋된 문서화 작업까지 함께 포함한 것 — payload 구성(diff-range 선택)의 부수 효과다.
  - 제안: 코드 스코프 관점에서 조치 불요. 참고로 `git diff e9dcb27c9^..HEAD --stat`을 직접 실행해보면 이 페이로드에 없는 파일(이번 라운드 자신의 `review/code/2026/07/17/15_26_11/*`, 이후 진행된 `review/consistency/2026/07/17/15_37_22/*`, `spec/7-channel-web-chat/3-auth-session.md`)까지 더 있어 — payload 는 그보다 이른 시점의 스냅샷으로 생성된 것으로 보인다. 다음 라운드부터 "직전 라운드 대비" 델타를 fix 커밋 단위로 좁히면(예: 리뷰 대상에서 `review/**` 자체 산출물은 애초에 제외) payload 노이즈를 줄일 수 있으나 선택적 개선이며 이번 건의 문제는 아니다.

## 요약

이번 라운드의 실제 코드 변경(`e9dcb27c9`, `use-widget-eager-start.test.ts` 단일 파일 22줄)은 오케스트레이터가 서술한 "프로덕션 코드 0줄, 회귀 테스트 2건 전제 단언 추가 + 주석 정정" 범위와 `git show --stat`으로 정확히 일치함을 확인했다 — production 파일·다른 소스 파일·import·설정 파일 변경은 전혀 없고 포맷팅 변경도 실질 변경과 섞이지 않았다. 유일하게 따져볼 지점은 직전 라운드 리뷰어(`14_56_27` testing)가 "이번 diff 범위는 아님"으로 명시했던 인접 기존 테스트(`:2275`)까지 같은 fix 패턴을 확장 적용한 것인데, 이는 같은 리뷰어가 같은 조사에서 대조실험으로 직접 증명한 동일 결함의 자매 인스턴스를 같은 파일·같은 `describe` 블록 안에서 이미 확정된 2줄 패턴 그대로 즉시 봉합한 것으로, 커밋 메시지에 사유·회귀 없음 검증까지 투명하게 남겼다 — "요청 이상의 변경"이라는 기준에 문자 그대로는 걸리지만, 무관한 리팩토링·기능 확장·타 영역 수정에 해당하지 않는 정당한 확장으로 판단한다. 페이로드에 포함된 나머지 6개 파일(`review/code/2026/07/17/{14_30_15,14_56_27}/*`)은 fix 커밋과 분리된 별도의 docs-only 커밋(`33dbc0777`, `cfe2f5630`)에서 온 과거 라운드 산출물 백필이며, 개발자 스코프 위반이 아니라 이 payload 의 diff 범위 구성에 따른 부수 포함임을 `git log` 로 확인했다.

## 위험도

LOW
