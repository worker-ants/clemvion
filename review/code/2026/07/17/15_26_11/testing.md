# 테스트(Testing) 리뷰 — `pendingResetRef` 회귀 테스트 2건 정정(전제 단언 + 주석 스코프) (2026-07-17 15_26_11)

**대상**: `codebase/channel-web-chat/src/widget/use-widget-eager-start.test.ts` 단독. `git diff-tree --no-commit-id --name-only -r be0b85895`로 직접 확인 — 이 라운드의 커밋은 이 파일 1개만 건드리며(+18/-4), **프로덕션 코드(`use-widget.ts`) 0줄 변경**이다. orchestrator 의 4개 질의 전부를 격리 워크트리에서 독립 mutation 으로 재현했다(주장 인용이 아니라 실측).

## 검증 방법론

- `git worktree add --detach <scratchpad>/mutation-wt-testing-15_26_11 HEAD` 로 공유 워크트리 밖에서 작업. `node_modules`(root·`channel-web-chat`)는 공유 워크트리 실디렉터리 symlink 로 부트스트랩(vitest 전용).
- 베이스라인(HEAD, mutation 없음) 먼저 확인: `use-widget-eager-start.test.ts` 44/44, `channel-web-chat` 전체 22 files/376 tests — 직전 라운드(14_56_27) 자기보고 수치와 정확히 일치.
- 질의 1(퇴화 탐지): 각 테스트에서 `Object.defineProperty(window.document, "referrer", ...)` 블록(4줄)만 Python 스크립트로 정밀 제거(주변 텍스트 사전 assert 로 대상 확인) → 개별 실행 + 전체 스위트 실행 양쪽으로 확인.
- 질의 2(탐지력 유지): 네 가지 잘못된 설계를 원본 커밋(`e195a448c`/`0d6128c74`/`f4785a953`)에서 정확한 코드를 `git show` 로 확보해 정밀 재도입하는 Python 패처를 작성, `use-widget.ts` 에 매 설계마다 적용 → 전체 스위트 실행 → 실패 수·실패 라인 기록 → 원본으로 복원 후 diff 로 클린 확인.
- 종료 후 두 워크트리 모두 `git worktree remove --force`, 공유 워크트리는 `git status --short`(review 산출물 1건 외 무변경)·`git diff --stat -- codebase/`(빈 결과)로 무오염 재확인.

## 발견사항

- **[INFO]** 질의 1 — 두 테스트가 퇴화를 실제로 탐지하는가: **확인(독립 재현, orchestrator 서술과 정확히 일치)**
  - 위치: `:2275`("겹친 부팅의 결과가 갈릴 때…") 의 `referrer` 블록(`:2276-2279`), `:2359`("겹친 부팅에서 나중 진입이…") 의 `referrer` 블록(`:2360-2363`)
  - 상세: 각 테스트에서 `referrer` 설정 4줄만 제거하고 단독 실행한 결과, 둘 다 **`AssertionError: expected 'streaming' to be 'blocked'`**(신규 전제 단언 라인, `:2332`/`:2417`)로 실패했다 — orchestrator 가 보고한 문구와 완전히 동일하다. 이어서 두 테스트의 `referrer` 를 **동시에** 제거하고 파일 전체(44건, `-t` 필터 없이)를 실행해도 정확히 이 2건만 실패하고 나머지 42건(이미 같은 시나리오를 덮는 `:2199` "겹친 부팅…삼키지 않는다" 포함)은 전부 green — 전제 단언이 인접 테스트로 새지 않고 정확히 자기 테스트 안에서만 작동함을 추가로 확인했다.
  - 제안: 없음(주장 확인됨).

- **[INFO]** 질의 2 — 전제 단언 추가가 기존 탐지력을 약화시키지 않았는가: **확인(4종 전부 재현, 실패 수·실패 지점 모두 이전 라운드와 정확히 일치)**

    | # | 재도입한 설계 | 실패 테스트 | 실패 수(이전 라운드 14_56_27 대비) |
    |---|---|---|---|
    | (a) | 진입 시 일괄 폐기 | `:2103, :2199, :2275, :2359` | **4**(동일) |
    | (b) | BLOCKED 한정 폐기 | `:2103, :2275, :2359` | **3**(동일) |
    | (c) | `bootGenRef` 소유권 | `:2103, :2359`(`:2275` 는 여전히 **통과**) | **2**(동일) |
    | (d) | 소비 자체 제거 | `:2022, :2103, :2199, :2275, :2359` | **5**(전건, 동일) |
    | — | 베이스라인 | 없음 | 0 |

    - 상세: 실패 **수**뿐 아니라 실패 **지점**까지 대조했다 — `:2275`/`:2359` 가 실패하는 4개 mutation((a)(b)(d)) 전부에서 스택트레이스가 가리키는 줄은 신규 전제 단언(`:2332`/`:2417`, `toBe("blocked")`)이 아니라 **원래부터 있던 최종 단언** `await waitFor(() => expect(hookPosts).toBe(1))`(`:2343`/`:2428`) 이었다. 즉 이번에 추가한 전제 단언은 이 네 mutation 에서 단 한 번도 발동하지 않는다(전제가 참이므로 통과) — 탐지는 여전히 기존 방식(리셋 미이행) 그대로 이루어진다. 전제 단언은 오직 질의 1 의 시나리오(referrer 부재로 인한 fail-open 퇴화)에서만 발동하도록 정확히 국한되어 있어, 걱정했던 "새 단언이 기존 실패 원인을 가려버리거나 우연히 먼저 걸려 진단을 흐리는" 부작용이 없다.
    - 설계 (c)에서 `:2275`(혼합 순서 원본)가 여전히 통과하는 것도 재확인했다 — 이 mutation 은 BLOCKED 도달 자체는 정상이므로 신규 전제 단언을 통과시키고, 그 설계의 "안전한 절반"이라 최종 리셋도 정상 이행되어 테스트 전체가 green 이다. 전제 단언 추가가 이 사각지대(의도된 것, 아래 질의 3 참조)를 새로 메우거나 깨지 않았다는 뜻이기도 하다.
  - 제안: 없음(탐지력 유지 확인됨).

- **[INFO]** 질의 3 — 정정된 주석이 이제 정확한가: **확인**
  - 위치: `:2349-2358`
  - 상세: 주석은 "`bootGenRef` 재도입 시 (i) `:2275`(혼합 순서)는 여전히 통과하고 (ii) **겹침 케이스 중에선** `:2359` 만 잡으며, (iii) 스위트 전체로는 순차 케이스 `:2103` 도 같은 mutation 을 독립적으로 잡는다"고 서술한다. 위 표의 설계 (c) 행이 정확히 이 세 주장을 그대로 재현한다 — `:2275` PASS, `:2359` FAIL, `:2103` FAIL. 스코프 한정 문구("겹침 케이스 중에선")와 "의도치 않은 이중 방어선이니 어느 쪽도 중복으로 지우지 말 것"이라는 경고 모두 실측과 부합한다.
  - 제안: 없음.

- **[INFO]** 질의 4 — 남은 사각지대: **이번 delta 가 새로 만든 사각지대는 없음. 기존에 이미 식별·이월된 갭 1건만 잔존(범위 밖)**
  - 14_56_27 RESOLUTION.md 가 이미 "이월" 로 기록한 **"config 확립~`pendingResetRef` 소비 사이는 동기 구간이어야 한다"는 세 번째 불변식**은 이번 delta 로도 여전히 테스트/JSDoc 어디에도 명문화되지 않았다(그 구간에 우발적 `await` 를 끼워 넣는 mutation 이 44/44 를 통과시킨다는 이전 라운드의 실측이 이번 diff 로 달라질 이유가 없음 — 코드 자체가 무변경이므로 재검증하지 않았다). 이번 delta 의 범위(테스트 파일의 주석·단언 2곳)가 아니므로 새 CRITICAL/WARNING 으로 올리지 않고 참고로만 남긴다.
  - 그 외 새 사각지대는 발견하지 못했다 — 두 테스트의 전제 단언 위치(1차/2차 중 먼저 resolve 하는 쪽 바로 뒤)는 각 테스트의 시나리오 서술과 정확히 대응하고(`:2275` 는 `embedResolvers[0]`=1차가 먼저 BLOCKED, `:2359` 는 `embedResolvers[1]`=2차가 먼저 BLOCKED), `:2103` 의 첫 번째 전제(`:2163`, 동일하게 `act`+`flushAsync` 직후 `waitFor` 없는 동기 `expect`)와 동형의 관용구를 그대로 따른다 — 새로 도입한 패턴이 아니라 이 파일의 기존 컨벤션을 재사용한 것이라 별도 검증 부담이 낮다.
  - 제안: 없음(범위 밖 항목은 다음 라운드에서 별도로 다룰 사안).

- **[INFO]** 체크리스트 — 이번 delta 한정 결과 요약
  - **테스트 존재 여부**: 프로덕션 코드 변경 없음 — 신규 테스트 불요. 이번 delta 자체가 "기존 회귀 테스트 2건의 견고성 보강"이라 대상 정확.
  - **회귀 테스트 유효성**: 베이스라인 44/44, 376/376 직접 재현. 기존 42건은 이번 diff 로 영향받지 않음(동시 mutation 실행에서도 정확히 2건만 실패).
  - **테스트 격리**: 전역 `afterEach` 의 `referrer` 복원·`vi.unstubAllGlobals()` 가 이번 delta 와 무관하게 그대로 유지되고, 두 테스트를 동시에 mutate 해도 서로·인접 테스트로 새지 않음을 실측 확인.
  - **가독성**: 전제 단언 옆 주석이 "왜 필요한지"(퇴화 시나리오)와 "실측 근거"(referrer 제거 실험)를 명시해 의도가 분명하다. `:2103` 컨벤션과의 관계(동일 관용구 재사용)도 알아보기 쉽다.
  - **Mock 적절성 / 테스트 용이성**: 이번 delta 로 변경 없음 — 14_56_27 리뷰의 평가(적절/2-접점 구조로 테스트 용이)가 그대로 유효.

## 요약

orchestrator 의 4개 질의를 전부 격리 워크트리에서 독립 재현했다. (1) 두 테스트 각각(및 동시 적용) `referrer` 설정 제거 시 신규 전제 단언(`toBe("blocked")`)에서 정확히 `expected 'streaming' to be 'blocked'` 로 실패해 fail-open 퇴화를 탐지한다 — orchestrator 서술과 완전 일치. (2) 네 가지 잘못된 설계(진입 시 일괄 폐기/BLOCKED 한정 폐기/`bootGenRef` 소유권/소비 자체 제거)를 원본 커밋 코드 그대로 재도입한 결과 4/3/2/5건으로 실패해 직전 라운드(전제 단언 추가 전)와 실패 **수**는 물론 실패 **지점**(스택트레이스 라인)까지 동일함을 확인했다 — 신규 전제 단언은 이 네 mutation 에서 단 한 번도 발동하지 않고(전제가 항상 참), 탐지는 여전히 기존 최종 단언(`hookPosts`)이 담당한다. 즉 전제 단언은 순수하게 새 실패 모드(질의 1)만 추가로 커버할 뿐 기존 탐지 경로를 가리거나 대체하지 않는다. (3) 정정된 주석의 세 주장(혼합 순서 테스트는 `bootGenRef` 재도입에 통과·겹침 케이스 중엔 신규 거울상 테스트만 단독 탐지·스위트 전체로는 순차 테스트가 독립적으로 같은 결함을 잡음)이 위 표와 정확히 대응해 이제 정확하다. (4) 이번 delta 가 새로 만든 사각지대는 없으며, 유일한 잔존 갭(config 확립~리셋 소비 사이 동기 구간 불변식 미테스트)은 이미 14_56_27 에서 식별·이월된 것으로 이번 범위 밖이다. 프로덕션 코드 0줄 변경을 `git diff-tree` 로 직접 확인했고, 채널 전체 스위트(376건)도 재실행해 회귀 없음을 재확인했다.

## 위험도

NONE
