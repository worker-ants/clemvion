# RESOLUTION — ai-review 14_56_27 (회귀 테스트 + 주석 재검토)

대상 범위: 테스트 1건 추가 + 주석 2건(프로덕션 코드 **0줄** 변경). RISK LOW / Critical 0 / Warning 2 / INFO 5. 1인 reviewer(testing).

**요약**: 리뷰어가 **네 가지 잘못된 설계가 전부 탐지됨을 독립 재현**했고(4/3/2/5건), 프로덕션 코드가 순수 JSDoc 변경임을 diff 대조로 확인했다. WARNING 2건은 **내가 새로 넣은 테스트/주석 자체의 결함**이며 둘 다 실측 근거가 있어 조치했다.

## 확인된 것 (리뷰어 독립 재현)

- **신규 테스트가 사각지대를 메운다**: `bootGenRef` 3줄 원본 재도입 → 정확히 2건 실패(순차 테스트 + 신규 거울상), 기존 "혼합 순서" 테스트는 통과 — 내 주장·`14_30_15` concurrency 결과와 정확히 일치.
- **네 설계 전부 탐지**(mutation 4종 독립 재현):

  | 재도입한 설계 | 실패 수 |
  | --- | --- |
  | 진입 시 일괄 폐기 | 4 |
  | BLOCKED 한정 폐기 | 3 |
  | `bootGenRef` 소유권 | 2 |
  | 소비 자체 제거 | 5(전건) |
  | (베이스라인) | 0 |

- **프로덕션 무변경**: `use-widget.ts` diff 직접 대조로 코드 0줄 확인.

## Warning

### W1 — 신규 테스트 주석의 "유일한 가드" 주장이 부정확. **정정**

`bootGenRef` 재도입 시 실패하는 테스트는 **둘**이다(순차 테스트도 독립적으로 잡는다 — 겹침이 없어 소유권 조건이 참이 되고 그래서 그쪽도 폐기가 발동한다). "이 테스트만 잡는다"는 **겹침 케이스 계열 안에서만** 참이고 스위트 전체 기준으로는 틀렸다.

- **왜 문제인가(리뷰어 지적, 타당)**: 이 파일은 4라운드 연속 회귀 hotspot 이고 **코드 주석이 RESOLUTION 류 문서에 "증거"로 인용되는 문화**가 있다. 향후 누군가 "순차 테스트는 겹침 테스트와 중복돼 보인다"며 정리할 때 이 문구를 신뢰해 **의도치 않은 이중 방어선의 한쪽을 지울 위험**이 있다.
- **수정**: 스코프를 명시("겹침 케이스 중에선")하고, **"의도치 않은 이중 방어선이니 어느 쪽도 중복으로 지우지 말 것"** 을 명문화 — 둘은 순차/겹침이라는 서로 다른 시나리오로 같은 결함을 잡는다.

### W2 — 신규 테스트의 거짓 음성 여지(2차 BLOCKED 도달 전제 미고정). **수정**

- **리뷰어 실측**: 신규 테스트에서 `document.referrer` 설정 줄만 제거하면(→ `detectHostOrigin` null → `isEmbedAllowed` fail-open → 2차가 BLOCKED 가 아니라 ALLOWED) **그래도 PASS 한다**. `hookPosts===1` 이 "의도한 혼합 순서"와 "실수로 둘 다 ALLOWED(=이미 다른 테스트가 덮는 시나리오로 퇴화)"를 구분하지 못하기 때문이다.
- **이 파일의 확립된 컨벤션을 내가 따르지 않았다** — 순차 테스트는 정확히 이 이유로 1차·2차 전제를 **둘 다** 명시 단언한다(`12_04_49` 에서 같은 유형을 지적받아 넣은 것이다). 같은 지적을 세 번째 받은 셈이다.
- **수정**: `result` 를 캡처하고 2차 resolve 직후 `expect(phase).toBe("blocked")` 로 전제 고정.
  - **리뷰어가 "이번 diff 가 만든 게 아니다"라고 밝힌 기존 "혼합 순서" 테스트(`:2275`)도 같은 패턴**이라 함께 정정했다 — 물려받은 결함이라도 이번에 알았으므로 남겨둘 이유가 없다.
- **검증(리뷰어 실험 재현)**: 두 테스트 각각 `referrer` 설정 제거 → **이제 `expected 'streaming' to be 'blocked'` 로 실패**(퇴화 탐지). 전제 추가로 `bootGenRef` 재도입 탐지가 약해지지 않음도 확인(여전히 2건 실패).

## INFO

- **미문서화된 세 번째 불변식**(리뷰어 신규 mutation): config 확립~소비 사이에 우발적 `await` 를 삽입하는 mutation 이 44/44 전부 통과한다 — 그 구간이 **동기여야 한다**는 불변식이 테스트로 고정돼 있지 않다. **오늘은 도달 불가**(그 구간에 await 이 없다). `13_03_59` concurrency 가 "await 이 하나도 없는 단일 동기 구간"을 안전성 근거로 삼았으므로, 그 전제가 깨지면 재검토가 필요하다 → **이월**(이 파일의 다른 두 "불변식 의존 주의" 주석과 같은 축).
- 나머지 INFO 는 확인 항목 요약이라 조치 없음.

## 검증

- **TEST WORKFLOW**: lint PASS(58s) · unit PASS(70s, `tests=14 passed`) · build PASS(121s).
- **channel-web-chat**: 22 파일 **376 passed**(vitest 실측).
- 두 테스트의 퇴화 탐지 + `bootGenRef` 재도입 탐지를 각각 실측 재확인.

## 이월

- **동기 구간 불변식**(INFO) — config 확립~소비 사이에 await 이 들어가면 깨진다. 오늘 도달 불가.
- `applyConfig` single-flight/supersede · cross-endpoint 리셋 번짐 · 미리보기 BLOCKED 안내 문구 · `state.phase` flicker — 이전 라운드들에서 승계.

**교훈**: **"전제를 고정하라"는 지적을 이 파일에서 세 번째로 받았다.** 매번 다른 테스트에서, 매번 같은 이유로 — 내가 새 테스트를 쓸 때 그 파일이 이미 확립한 컨벤션(`:2103` 이 두 전제를 명시 단언하는 이유가 주석에 적혀 있다)을 읽지 않고 새로 짜기 때문이다. 이 hotspot 에서 테스트를 추가할 땐 **바로 옆 테스트가 왜 그렇게 생겼는지부터** 읽어야 한다.
