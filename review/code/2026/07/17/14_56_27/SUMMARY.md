# Code Review 통합 보고서

*검토 범위: 회귀 테스트 1건 추가 + 주석 2건. **프로덕션 코드 0줄 변경**(diff 대조 확인). 1개 reviewer(testing) 실행 — 상세는 하단 "라우터 결정".*

## 전체 위험도

**LOW** — 리뷰어가 **네 가지 잘못된 설계가 전부 탐지됨을 독립 mutation 으로 재현**했고(4/3/2/5건, 베이스라인 0), 프로덕션 코드가 순수 JSDoc 변경임을 diff 직접 대조로 확인했다. WARNING 2건은 **이번 diff 가 새로 넣은 테스트/주석 자체의 품질** 문제로, 코드 동작 결함은 0건이다.

**워크트리 위생**: 격리 worktree 에서 mutation 수행 후 제거. 오염 없음.

## Critical 발견사항

없음.

## 경고 (WARNING)

| # | 카테고리 | 발견사항 | 제안 |
|---|----------|----------|------|
| 1 | 테스트 — **실측** | **신규 테스트의 거짓 음성 여지**: `document.referrer` 설정 줄만 제거하면(→ `detectHostOrigin` null → `isEmbedAllowed` fail-open → 2차가 BLOCKED 아닌 ALLOWED) **그래도 PASS 한다**. `hookPosts===1` 이 "의도한 혼합 순서"와 "실수로 둘 다 ALLOWED(=이미 다른 테스트가 덮는 시나리오로 퇴화)"를 구분하지 못하기 때문. 이 파일의 확립된 컨벤션(순차 테스트는 1·2차 전제를 **둘 다** 명시 단언 — `12_04_49` 지적으로 넣은 것)을 따르지 않았다. **대조 실험**: 기존 "혼합 순서" 테스트도 동일하게 PASS → 이번 diff 가 만든 게 아니라 물려받은 패턴. **완화**: 순차 테스트가 같은 경로를 독립 단언해 "카나리아" 역할을 하므로 즉각 위험도는 낮다. | `result` 캡처 + 2차 resolve 직후 BLOCKED 전제 단언. **→ 조치됨**(RESOLUTION §W2): 신규 + 물려받은 기존 테스트 **둘 다** 정정. 검증 — 각각 `referrer` 제거 시 이제 `expected 'streaming' to be 'blocked'` 로 실패(퇴화 탐지). |
| 2 | 문서 — **실측** | **신규 테스트 주석의 "유일한 가드" 주장이 부정확**: `bootGenRef` 재도입 시 실패하는 건 **둘**이다(순차 테스트도 독립 탐지 — 겹침이 없어 소유권 조건이 참이 되고 그쪽도 폐기가 발동). "겹침 케이스 계열 안에서만" 참. **왜 문제인가**: 이 파일은 4라운드 연속 회귀 hotspot 이고 **주석이 RESOLUTION 류 문서에 "증거"로 인용되는 문화**가 있어, 향후 "순차 테스트는 중복돼 보인다"며 정리할 때 이 문구를 믿고 **의도치 않은 이중 방어선의 한쪽을 지울 위험**이 있다. | 스코프 명시 + 이중 방어선임을 명문화. **→ 조치됨**(§W1). |

## 참고 (INFO)

| # | 발견사항 |
| --- | --- |
| 1 | **신규 테스트가 사각지대를 메운다 확인**: `bootGenRef` 3줄 원본 재도입 → 정확히 2건 실패(순차 + 신규 거울상), 기존 "혼합 순서"는 통과 — `14_30_15` concurrency 결과와 일치. |
| 2 | **네 설계 전부 탐지**(독립 재현): 진입-시 일괄 폐기 **4**건 / BLOCKED 한정 폐기 **3** / `bootGenRef` 소유권 **2** / 소비 제거 **5**(전건) / 베이스라인 **0**. |
| 3 | **이중 방어선 관찰**: 순차 테스트와 신규 거울상 테스트 둘이 **네 mutation 전부에서 공통 실패**하는 유일한 쌍 — 이 결함 클래스에 대한 이중 방어. |
| 4 | **미문서화된 세 번째 불변식**(신규 mutation): config 확립~소비 사이에 우발적 `await` 를 삽입해도 44/44 전부 통과 — 그 구간이 **동기여야 한다**는 불변식이 테스트로 고정돼 있지 않다. **오늘 도달 불가**(그 구간에 await 없음). `13_03_59` concurrency 가 그 동기성을 안전성 근거로 삼았으므로 전제가 깨지면 재검토 필요 → **이월**. |
| 5 | 프로덕션 코드(`use-widget.ts`) 순수 JSDoc 변경(코드 0줄) diff 대조 확인. |

## 에이전트별 위험도 요약

| 에이전트 | 위험도 | 핵심 발견 |
|----------|--------|-----------|
| testing | LOW | 4개 검증 요청 전부 격리 worktree 독립 mutation 으로 실측. 네 설계 탐지 매트릭스 재현, 신규 테스트의 거짓 음성(W1)·주석 부정확(W2) 발견, 미문서화 불변식(INFO#4) 신규 발견. |

## 라우터 결정

- router 미실행. 델타가 **테스트 1건 + 주석 2건(프로덕션 0줄)** 이라 `testing` 단독 실행이 적합하다고 main 이 판단했다.
  - **실행(1명)**: `testing`
  - **미실행(13명)**: 프로덕션 동작이 바뀌지 않아 동작 축(concurrency·side_effect 등) 재검토 실익이 낮다고 봤다. 직전 라운드(`14_30_15`)가 그 축을 2인으로 검토했다.
  - **한계 명시**: router 의 의미 기반 판단이 아니라 main 의 수동 선별이다. 다만 이번 델타의 성격(테스트·주석)과 실행 관점이 정확히 대응하며, 발견 2건 모두 그 관점에서 나왔다.
