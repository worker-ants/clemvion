# 유지보수성(Maintainability) Review — Fresh Re-review (commit bd15f63f6)

## 리뷰 범위

이전 세션(`review/code/2026/07/10/23_20_30`)에서 이미 LOW 로 판정된 변경 위에, `--impl-done` consistency
check 가 지적한 3건(rationale-continuity·convention-compliance·plan-coherence)을 반영한 커밋
`bd15f63f6` 이 델타다. 코드에 닿는 부분은 오직 하나:

- `codebase/backend/src/nodes/ai/ai-agent/ai-turn-executor.ts` — resume 경로 `llmContext: LlmCallContext`
  선언 바로 위 인라인 주석이 4줄 → 6줄로 확장(문구 정정). 코드 자체(`const llmContext: LlmCallContext = {`)는
  이전 세션에서 이미 검토됐고 이번엔 변경 없음.
- 나머지(RESOLUTION.md 3-헤더 스키마 재작성, 신규 consistency 산출물 6개, plan 종결조건 문구)는 전부 `review/**`
  프로세스 아티팩트로, 소스 코드 가독성·복잡도 관점의 대상이 아니다.

핵심 판단 대상은 orchestrator 가 명시한 질문 하나 — **6줄로 늘어난 이 주석이 1줄짜리 코드 변경 대비 과도한가,
아니면 비직관적인 TS 규칙을 인코딩한 정당한 길이인가**.

## 발견사항

- **[INFO]** 6줄 주석은 과잉이 아니라 정확성을 위해 필요한 길이 — 조치 불요
  - 위치: `ai-turn-executor.ts:2604-2609` (`const llmContext: LlmCallContext = {` 직전)
  - 상세: 이 주석이 방어하는 대상은 컴파일 타임에도 런타임에도 흔적을 남기지 않는 삭제다 — RESOLUTION.md 의
    mutation 검증(§TEST 결과 2번 항목)이 실증하듯, `: LlmCallContext` 주석을 떼면 `tsc`도 통과하고 오탈자
    필드(`nodeExecutionID`)도 조용히 흡수돼 프로덕션에서만 `llm_usage_log` 해당 컬럼이 NULL 로 적재된다
    (#501 재발). 즉 "나중에 누군가 지워도 아무 신호가 없는" 코드이므로, 텍스트 설명이 유일한 방어선이다.
    6줄 각각이 서로 다른 정보를 담아 반복이 없다: ①TS 규칙 자체("object literal 이 타입이 알려진
    대상 — 함수 인자 또는 주석 붙은 변수 — 에 직접 assign 될 때만 걸린다"), ②자매 경로
    (`executeSingleTurn`)가 왜 이 주석 없이도 안전한지, ③주석을 빼면 정확히 무엇이 깨지는지("freshness 가
    사라져 검사되지 않는다"), ④그 결과가 실제로 어떤 버그로 나타나는지(#501 과 동일 실패 모드). 이 중 어느
    한 줄도 앞 문장의 재진술이 아니라 인과 사슬의 다음 고리를 설명한다.
    또한 이 주석이 정확히 방어하려는 시나리오("`: LlmCallContext` 는 리터럴 shape 만 보면 중복 타입 주석처럼
    보여 미래의 'redundant annotation 정리' 리팩터/lint-fix 로 삭제되기 쉽다")는 orchestrator 가 제시한
    가설과 일치하며, 그 삭제가 컴파일 에러도 테스트 실패도 유발하지 않는다는 점(위 mutation 근거)이 바로
    "짧은 주석으로는 안 된다"는 근거다. 짧게 줄이면(예: "타입 주석 지우지 말 것 — TS 검사 우회됨"만 남기면)
    *왜* 우회되는지, *왜 이 파일의 다른 경로는* 괜찮은지가 사라져 다음 편집자가 "그럼 다른 경로처럼 인자로
    바로 넘기게 리팩터해도 되겠네" 같은 잘못된 결론에 이를 위험이 생긴다 — 그 경우 실제로 안전성이 깨진다.
  - 참고 (verbosity 를 지지하는 근거): 이 6줄 확장 자체가 "우회 함정"의 실례다 — 4줄이던 이전 버전은
    "리터럴을 인자로 **직접** 넘길 때만 걸린다"고만 서술했는데, 이는 부정확했다(주석 붙은 변수 선언에도
    같은 검사가 적용된다는 점을 누락 — 바로 이 코드가 그 케이스). `--impl-done` 의 rationale-continuity
    checker 가 이를 사실관계 오류로 잡아 이번 커밋에서 정정했다. 즉 "더 짧게" 시도했던 이전 버전이 실제로
    부정확한 설명을 낳았다는 것이 실증됐으므로, 지금의 6줄이 적정선에 더 가깝다.
  - 제안: 조치 불요. 굳이 더 다듬는다면 ①TS 규칙(1문장) + ②실패 모드(1문장) 로 2문장 압축도 가능하나,
    현재도 문장당 정보 밀도가 높고 필러가 없어 강제할 사안은 아니다. 같은 함수 바로 위에 이미 4줄짜리
    spec-근거 주석(§1.3 인용 + 대칭성 설명)이 있어, 이 파일의 기존 주석 밀도 관례와도 어긋나지 않는다.

- **[INFO]** (전 세션에서 이미 확인, 이번 델타에 재발 없음) 신규 회귀 테스트의 mock 셋업 반복
  - 위치: `information-extractor.handler.spec.ts` — `passes the same llmContext attribution to the retried
    (2nd) chat call` (이번 델타에서 코드 변경 없음, 이전 세션 코드 그대로)
  - 상세: 이전 리뷰(`23_20_30/maintainability.md`)에서 이미 INFO 로 기록된 사안이며 이번 커밋은 이 파일을
    건드리지 않았다. 재확인 목적으로만 언급 — 등급·판단 변경 없음.
  - 제안: 조치 불요(이전 리뷰 참조).

- **[INFO]** RESOLUTION.md 3-헤더 스키마 재작성 — 문서 품질 개선, 코드 유지보수성 영향 없음
  - 위치: `review/code/2026/07/10/23_20_30/RESOLUTION.md` (`## 조치 항목` / `## TEST 결과` / `## 보류·후속 항목`)
  - 상세: developer SKILL 이 규정한 헤더를 리터럴로 채택해 향후 동일 문서를 파싱/검색하는 도구·사람 모두에게
    일관된 구조를 제공한다. 소스 코드가 아니므로 가독성·네이밍·중첩 등 본 리뷰의 8개 관점이 직접 적용되지는
    않지만, 규약 준수(일관성) 관점에서는 개선이다.
  - 제안: 조치 불요.

## 요약

이번 델타는 사실상 주석 문구 정정 1건(4줄→6줄, 런타임 무영향)과 프로세스 문서 정리뿐이다. 핵심 질문—6줄
주석이 과도한가—에 대한 결론은 **아니다**: 이 주석이 지키는 대상은 컴파일러도 테스트도 잡아내지 못하는
삭제(annotation 제거)이므로 텍스트 설명이 유일한 방어선이고, 각 문장이 서로 다른 인과 고리(규칙 → 왜 이
경로만 필요한가 → 무엇이 깨지는가 → 어떤 버그로 나타나는가)를 담아 필러 없이 정보 밀도가 높다. 오히려
이전(4줄) 버전은 "리터럴을 인자로 직접 넘길 때만 걸린다"는 표현이 사실과 어긋나 이 코드 자체(주석 붙은
변수 선언 케이스)를 설명하지 못했고, 이번 확장이 그 오류를 바로잡은 것이므로 길이 증가는 압축의 실패가
아니라 정확성 확보의 대가다. "미래의 누군가 리팩터 중 이 타입 주석을 군더더기로 보고 지울 위험"에 대한
방어로서 6줄은 근거 있는 길이이며, 같은 함수 바로 위 기존 4줄 spec-근거 주석과도 밀도가 일관된다. 굳이
더 줄일 수는 있으나(2문장 압축) 그럴 필요를 강제할 만한 결함은 없다. 나머지 변경(RESOLUTION.md 스키마,
consistency 산출물)은 소스 코드가 아니어서 본 관점의 대상 밖이며 문제 없음.

## 위험도

NONE
