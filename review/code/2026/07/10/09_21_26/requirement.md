# 요구사항(Requirement) Review

대상 커밋: `c417bd299`(merge-base, #880) → `101328117`(C1 초안) → `eaa11a466`(HEAD, ai-review Critical#1 fix)
브랜치: `claude/ai-usage-attribution-hardening-358929` (plan: `plan/in-progress/ai-usage-attribution-hardening.md`)
요구사항 SoT: `spec/data-flow/7-llm-usage.md` §1.3

## 발견사항

- **[CRITICAL]** 리뷰 payload(diff) 가 이 브랜치 고유 변경이 아니라, origin/main 대비 **stale base** 로 인해 이미 머지된 별개 보안/스펙 작업 두 건을 되돌리는 것처럼 보임 — merge 시 silent revert 위험
  - 위치: `codebase/backend/src/modules/external-interaction/interaction.service.ts`(+`.spec.ts`), `codebase/backend/src/shared/utils/sanitize-error-message.ts`(+`.spec.ts`), `codebase/backend/test/external-interaction.e2e-spec.ts`, `plan/complete/eia-secret-masking-residuals.md`(삭제), `plan/in-progress/error-codes-catalog-sot.md`(삭제), `spec/5-system/14-external-interaction-api.md` §R17, `spec/5-system/3-error-handling.md` §1.2.1/§1.8, `review/code/**`·`review/consistency/**` 산출물 다수 삭제 (파일 2·3·9·10·11·12·14·16-37·38·39)
  - 상세: `git merge-base HEAD origin/main` = `c417bd299`(#880). `git diff c417bd299..HEAD --stat` 결과 이 브랜치의 **실제 고유 변경은 8개 파일**(CHANGELOG.md + ai-agent.memory.spec.ts + ai-memory-manager.ts + ai-turn-executor.ts + agent-memory-injection.{ts,spec.ts} + plan 2건)뿐이며, 이는 요구사항(§1.3 attribution 배선)과 정확히 부합한다. 그러나 origin/main 은 이 브랜치 분기 이후 `4086d84fa`(#881, EIA §R17 잔여 하드닝 — `deepRedactSecrets` terminal outputData 마스킹 + 캐시 + e2e)와 `01e68001c`(#882, 에러코드 카탈로그 §1.2.1/§1.8 도메인 등재)를 이미 머지했다. 리뷰 payload 의 diff 는 (원인 불명이나 stale-base 대비 방식으로 계산된 것으로 추정) 이 두 merged PR 의 변경분을 **"제거"로 표시**한다 — 즉 `getStatus` terminal `result`/`error` 의 `deepRedactSecrets` 마스킹이 사라지고 `execution.outputData` 가 다시 무가공 노출되며, secret 마스킹 unit/e2e 테스트·`deepRedactSecrets` WeakMap 캐시·완료된 plan 문서·spec §R17/§1.2.1/§1.8 서술이 모두 삭제된 것처럼 보인다. `review/consistency/2026/07/10/08_34_08/_retry_state.json` 의 절대경로가 `.../worktrees/conversation-thread-secret-hardening-6477bb/...` 를 가리키는 점도 이 파일들이 **다른 worktree/세션에서 이미 완료·리뷰까지 끝낸 별개 작업**임을 뒷받침한다. `scope` reviewer(같은 라운드, `review/code/2026/07/10/09_21_26/scope.md`) 도 동일 결론(CRITICAL)에 도달했다 — 원인은 `MEMORY: ensure-worktree stale base`(로컬 HEAD 기준으로 워크트리 생성 → 그 뒤 머지된 PR 미반영) 패턴과 일치.
  - 영향: 요구사항 충족 여부를 "이 PR 이 실제로 배포될 최종 상태"로 판단하면, secret 마스킹(보안 하드닝) regressed 여부가 핵심 변수가 된다. 일반적인 3-way `git merge`(non-fast-forward, non-squash)라면 이 브랜치 자체 커밋이 해당 파일들을 건드리지 않으므로 병합 시 자동 3-way 로 origin/main 상태가 보존될 가능성이 높지만, (a) squash-merge 나 강제 rebase-merge 전략, (b) 리뷰/PR diff 를 이 상태 그대로 신뢰해 승인하는 프로세스, 둘 다 이미 완료된 보안 마스킹 PR(#881)과 spec 카탈로그 PR(#882)을 잃어버릴 실질적 위험을 내포한다. 최소한 현재 diff 로는 "요구사항이 회귀 없이 안전하게 배포 가능한 상태"임을 검증할 수 없다.
  - 제안: PR 생성 전 `git fetch && git rebase origin/main` (또는 동등한 최신화)으로 base 를 갱신하고, `git diff origin/main...HEAD` (three-dot, merge-base 기준)로 diff 를 재확인해 무관 파일 전량(2·3·9·10·11·12·14·16-37·38·39)이 사라지고 8개 고유 파일 + `review/code/2026/07/10/09_21_26/**` 신규 산출물만 남는지 검증한다. 이 CRITICAL 이 해소되기 전에는 merge 하지 않는다.

- **[WARNING]** multi-turn resume 경로의 메모리 압축 chat attribution 배선이 회귀 테스트로 고정되지 않음 (single-turn 만 커버)
  - 위치: `codebase/backend/src/nodes/ai/ai-agent/ai-turn-executor.ts:2293-2296`(`applyMultiTurnTurnMemory` — `workflowId: state.workflowId`, `nodeExecutionId: state.nodeExecutionId`), 테스트 부재: `ai-agent.memory.spec.ts`(`describe('summary_buffer (multi-turn)'`류 기존 압축 트리거 테스트, 예: `callsBeforeTurn2` 인덱스 주변 1448-1471행)는 모델 선택(`summaryCall.model`)만 단언하고 `mock.calls[...][2]`(LlmCallContext)는 검증하지 않음
  - 상세: 이번 PR 의 핵심 수정(ai-review Critical#1 fix, `eaa11a466`)은 single-turn 경로의 config-파생 버그를 명시 파라미터(`context.*`/`state.*`)로 교정했고, `ai-agent.memory.spec.ts:513-520` 이 **single-turn** 경로만 회귀 테스트로 고정한다. multi-turn **resume** 경로(`applyMultiTurnTurnMemory` → `injectMemoryContext` → `buildSummaryBufferUpdate` 의 `state.workflowId`/`state.nodeExecutionId` 전달)는 코드상 대칭적으로 구현되어 있고 `state.workflowId`/`state.nodeExecutionId` 자체는 `buildRetryReentryState`(#877/#879)로 이미 검증된 값이라 실패 가능성은 낮지만, "요약 압축 chat + multi-turn resume" 조합을 실제로 실행하며 3번째 인자를 단언하는 테스트는 이 diff 에도, 기존 spec 에도 없다. 이전 라운드 리뷰(`review/code/2026/07/10/09_02_47/testing.md`)가 지적한 커버리지 갭 중 single-turn 은 해소됐으나 multi-turn resume 측 통합 단언은 plan 에도 명시적으로 포함되지 않았다(`plan/in-progress/ai-usage-attribution-hardening.md` "테스트" 섹션은 single-turn 만 언급).
  - 제안: 기존 "결합" multi-turn 압축 트리거 테스트(`callsBeforeTurn2` 부근)에 `expect(mockLlmService.chat.mock.calls[callsBeforeTurn2][2]).toMatchObject({ workflowId: ..., nodeExecutionId: ... })` 한 줄을 추가해 resume 경로도 명시적으로 고정 권장(저비용·고가치, single-turn 과 대칭).

- **[SPEC-DRIFT]** `spec/data-flow/7-llm-usage.md` §1.3 이 이번 코드 변경으로 stale 해짐 — 코드는 옳고 spec 반영만 누락
  - 위치: `spec/data-flow/7-llm-usage.md` §1.3 표 L107("AI Agent 자동 메모리 롤링 요약 압축 ... `context` 미전달 → ... 전부 NULL ... 잔여 갭"), 요약문 L113("노드 내부지만 미배선인 AI Agent 메모리 롤링 요약 압축뿐"), §4 표 L162(Agent Memory cross-ref: "롤링 요약 압축 chat (usage 적재, context NULL)"), L165-166(Dashboard/Alerts 잔여 갭 서술), Rationale L189-206("(b) `LlmCallContext` 가 아직 배선되지 않은 caller(... AI Agent 자동 메모리 롤링 요약 압축)")
  - 상세: 이번 diff(`ai-memory-manager.ts`/`agent-memory-injection.ts`/`ai-turn-executor.ts`)가 AI Agent 자동 메모리 롤링 요약 압축 chat 에 `workflowId`/`executionId`/`nodeExecutionId` 를 실제로 채우도록 배선했음을 코드·테스트로 확인했다(single-turn `context.*`, multi-turn resume `state.*`). 이는 §1.3 이 명시한 "잔여 attribution 갭 해소"라는 기존 spec 방향과 정확히 부합하는 **의도된 개선**이며, 코드가 틀린 것이 아니라 spec 본문의 "미배선/잔여 갭" 서술이 이제 낡았다. `plan/in-progress/ai-usage-attribution-hardening.md` 의 "SPEC-DRIFT (PR-2 로 이관)" 섹션이 이 정확한 위치(L107·L113·§4 표·Rationale)를 이미 cross-ref 로 추적하고 있어 처리 경로가 명확하다.
  - 제안: 코드는 그대로 유지. `project-planner` 가 후속 PR(plan 이 지칭하는 "PR-2")에서 §1.3 표 L107 행("전부 NULL... 잔여 갭"→"첫 턴/resume 모두 context.*/state.* 채움"으로 갱신), L113 요약문, §4 표 L162, Rationale L189-206 을 함께 갱신해야 한다. 이 PR(PR-1) 자체를 spec-drift 이유로 차단할 필요는 없다 — plan 이 이미 순서(PR-1 → PR-2 연속 병합)를 명시했다.

- **[INFO]** 기능 완전성·엣지케이스·반환값 — 8개 고유 파일 범위에서는 결함 없음
  - 위치: `ai-memory-manager.ts:99-124`(`injectMemoryContext` 시그니처에 `workflowId?`/`nodeExecutionId?` 명시 파라미터 추가), `ai-turn-executor.ts:1149-1167`(single-turn `context.workflowId`/`context.nodeExecutionId` 전달), `ai-turn-executor.ts:2274-2299`(multi-turn resume `state.workflowId`/`state.nodeExecutionId` 전달), `agent-memory-injection.ts:279-395`(`llmContext?: LlmCallContext` optional 파라미터 + `llmService.chat(..., llmContext)` 3번째 인자 forward)
  - 상세: `ExecutionContext.workflowId`(필수 string)·`ExecutionContext.nodeExecutionId`(엔진이 dispatch 직전 항상 주입, `node-handler.interface.ts:32-51`)를 single-turn 경로가 정확히 소비하고, multi-turn resume 은 `buildRetryReentryState`(선행 PR #877/#879)가 주입한 `state.workflowId`/`state.nodeExecutionId` 를 소비한다 — 두 경로 모두 "config 파생" 이 아니라 명시 파라미터로 바뀌어 초기 Critical#1 버그(단발 노드 config 에 해당 키가 없어 항상 NULL) 가 재발 불가능한 구조로 교정됐다. `buildSummaryBufferUpdate` 의 `llmContext` 는 optional 로 하위호환(`llmContext` 미전달 시 `chat(..., undefined)`, 회귀 테스트로 고정)이 보장되고, `executionId: args.executionId || undefined` falsy 방어도 적절하다. `ai-turn-executor.ts:2603` 의 `const llmContext: LlmCallContext = {...}` 명시 타입 주석(B1)은 TS excess-property check 를 활성화해 향후 필드 오탈자를 컴파일타임에 차단하는 방어적 강화로, 런타임 동작 변화 없음(순수 하드닝) 이 맞다. TODO/FIXME/HACK/XXX 신규 도입 없음(`git diff c417bd299..HEAD` grep 확인).

## 요약

이번 PR 의 **고유 의도(요구사항)** — AI Agent 자동 메모리(summary_buffer/persistent) 롤링 요약 압축 chat 이 `llm_usage_log` 의 `workflow_id`/`execution_id`/`node_execution_id` 를 채우도록 배선 — 는 8개 파일 범위에서 정확하고 완결적으로 구현됐다. single-turn 은 `ExecutionContext`, multi-turn resume 은 재구성 `state`(`buildRetryReentryState` 주입분)를 명시 파라미터로 소비하도록 설계되어 있고, 초기 구현의 config-파생 버그(single-turn 항상 NULL)는 선행 `/ai-review` Critical#1 이 발견해 `eaa11a466` 커밋으로 이미 교정됐으며 해당 회귀는 테스트로 고정됐다. 다만 multi-turn resume 쪽의 동일 배선은 대칭 회귀 테스트가 없어 WARNING 이며, spec 본문(`7-llm-usage.md` §1.3)은 이 개선을 아직 반영하지 못한 SPEC-DRIFT(코드는 옳고 spec 갱신만 후속 PR-2로 예정)로 확인된다. 그러나 이 요구사항 자체의 정합성과 무관하게, 리뷰에 제출된 **diff 전체는 요구사항 범위를 크게 벗어나** origin/main 에 이미 머지된 별개의 완료 작업(EIA §R17 secret 마스킹 하드닝 #881, 에러코드 카탈로그 완결 #882)을 되돌리는 변경을 포함한다 — merge-base 확인 결과 이는 이 브랜치 자체의 의도된 변경이 아니라 stale base(로컬 HEAD 기준 워크트리 생성 후 origin/main 이 앞서간) 로 인한 아티팩트로 강하게 추정되며, `scope` reviewer 도 동일 결론에 도달했다. 이 상태로 PR 이 생성·병합되면 완료된 보안 하드닝을 silent revert 할 실질적 위험이 있어, rebase 로 base 를 갱신하고 diff 를 재검증하기 전까지는 "요구사항이 안전하게 충족됐다"고 판단할 수 없다.

## 위험도

CRITICAL
