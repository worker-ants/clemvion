STATUS=success testing review complete — 5 files reviewed (state-machine.ts, execution-engine.service.ts, ai-turn-orchestrator.service.ts, engine-driver.interface.ts, retry-turn.service.ts), plus 4 associated *.spec.ts + plan/in-progress/retry-turn-terminal-guard.md cross-checked. 4 findings (all WARNING/INFO, no CRITICAL), overall risk LOW.
===REPORT_MARKDOWN_BELOW===
# 테스트(Testing) 리뷰 — retry-atomic-claim (execution.retry_last_turn 짝 전이 DB 가드)

## 사전 확인 사항

- 리뷰 대상 diff 는 `origin/main...HEAD` 범위이며(브랜치 `retry-atomic-claim-4d9e77`), 프롬프트가 준 5개 파일은 커밋 `b351731f0`~`3c306d593`(11개 커밋, "atomic claim" 전체 기능)의 누적 변경분이다.
- 이 브랜치는 이미 **10라운드**의 `/ai-review` + mutation testing 을 거쳤고(`plan/in-progress/retry-turn-terminal-guard.md` 참조), 그중 8R/9R/10R 세 라운드가 정확히 "테스트가 놓친 opts→DB가드 전파 결함"을 주제로 CRITICAL 을 냈다 — 매 라운드 mock 을 더 정직하게 고치고 회귀 테스트를 추가하는 식으로 수렴해왔다. 본 리뷰는 10R(`3c306d593`) 직후 시점의 상태를 검사한다.
- 발견한 갭 대부분이 이미 이 plan 문서에 P2/P3 로 추적·defer 된 항목과 실질적으로 겹친다 — 아래 각 항목에 교차 인용했다. 즉 **이번 diff 가 새로 만든 회귀는 발견되지 않았고**, 기존에 알려진 채로 남아있는 잔여 리스크를 재확인한 결과다.

## 발견사항

- **[WARNING]** DB 가드 opt-in(`allowRetryReentry`)이 COMPLETED/CANCELLED 를 여전히 배제하는지 검증하는 대조 테스트가 없음 — state-machine 계층과의 비대칭
  - 위치: `codebase/backend/src/modules/execution-engine/execution-engine.service.ts:534`(`NON_TERMINAL_OR_FAILED_STATUSES_SQL` 정의, JSDoc 은 8231-8232) / 테스트 `codebase/backend/src/modules/execution-engine/execution-engine.service.spec.ts:5115`,`5140`,`5164`,`5188`
  - 상세: `state-machine.spec.ts:114`("should keep failed terminal for every other target even with opt-in")는 `canTransition(FAILED, {COMPLETED,CANCELLED,PENDING}, {allowRetryReentry:true})` 가 전부 `false` 임을 명시적으로 고정한다. 그러나 이 PR 이 신설한 대응 DB 상수 `NON_TERMINAL_OR_FAILED_STATUSES_SQL`(및 이를 쓰는 `updateExecutionStatus`/`tryLockActiveExecutionAndSaveNodeExec`)에는 동형의 대조 테스트가 없다. 새로 추가된 4건의 opt-in 테스트(5115/5140/5164/5188행)는 전부 "opt-in+dbExecutionStatus=FAILED→매치" 또는 "no-opt-in→불일치" 방향만 확인하고, **"opt-in+dbExecutionStatus=CANCELLED(또는 COMPLETED)→여전히 불일치(persisted=false)"** 방향은 어느 테스트도 재현하지 않는다. `NON_TERMINAL_OR_FAILED_STATUSES_SQL` 의 필터 조건(`!TERMINAL_STATUSES.has(status) || status===FAILED`, 538-540행)이 향후 리팩터로 실수 확장되어 COMPLETED/CANCELLED 까지 포함해도 현재 스위트는 검출하지 못한다 — 이는 이 파일 자신이 8232행 JSDoc 에 명시한 안전 불변식("opt-in 시에도 COMPLETED/CANCELLED 는 여전히 배제되므로 진짜 동시 취소는 계속 막힌다")의 반대 방향 회귀다. `plan/in-progress/retry-turn-terminal-guard.md` 의 §코드 표 #21("상태 전이 허용 여부의 이중 진실 소스 — TS `canTransition` vs SQL 상수가 독립 존재·수동 동기화", P2, 10R 유래)이 구조적 원인을 이미 지목했다.
  - 제안: 구조적 해결(#21, SQL 을 상태머신에서 파생)은 별도 작업이지만, 그 전까지는 `updateExecutionStatus`/`tryLockActiveExecutionAndSaveNodeExec` 대상으로 `dbExecutionStatus = ExecutionStatus.CANCELLED`(또는 `COMPLETED`) + `{allowRetryReentry:true}` 조합 시 `persisted === false` 를 확인하는 대조 테스트 1~2건을 저비용으로 추가할 수 있다.

- **[WARNING]** `tryLockActiveExecutionAndSaveNodeExec` 자체 단위 테스트가 신규 `opts` 파라미터를 반영하지 않음 — 형제 메서드 대비 커버리지 비대칭
  - 위치: `codebase/backend/src/modules/execution-engine/execution-engine.service.spec.ts:5495-5573`(describe `tryLockActiveExecutionAndSaveNodeExec — RUNNING 유지 분기 전용 원자 관측+save`, 로컬 타입 선언은 5498-5501행), 대비 `codebase/backend/src/modules/execution-engine/ai-turn-orchestrator.service.ts:1504-1509`(`finalizeAiNode` isFailed 분기의 호출)
  - 상세: 이 describe 블록의 로컬 함수 타입(5498-5501행)은 `opts` 파라미터를 아예 선언하지 않고, 4개 테스트 전부 2-인자 호출만 한다 — 형제 메서드 `updateExecutionStatus` 가 같은 라운드에서 opt-in 전용 신규 단위 테스트 2건(5115, 5164행, `sql.toContain("'failed'")` 직접 단언)을 받은 것과 비대칭이다. `finalizeAiNode` 의 isFailed 분기(재시도 turn 이 다시 실패하는 시나리오 — 이 PR 헤드라인 버그의 "(a) turn 즉시 종료" 케이스 그 자체, `ai-turn-orchestrator.service.ts:1439` `allowRetryReentry = opts?.retryReentry === true` → 1508행에서 번역)가 `allowRetryReentry` 를 이 메서드까지 정확히 전달하는지는, 현재 **단 하나의 통합 스타일 테스트**(`execution-engine.service.spec.ts:16946` `re-failure (retryable again) → Execution FAILED + NODE_FAILED on spawned row` — 실제 DI 로 조립된 `RetryTurnService`→`AiTurnOrchestrator`→`ExecutionEngineService` 전체 경유, honest `mockTxManagerQuery` 의존)로만 간접 검증된다. 직접 코드를 추적해 이 테스트가 실제로 회귀를 잡는다는 것은 확인했다(opts 전파가 끊기면 `lockNonTerminalExecutionRow` 가 0행 반환 → `assertLinkedTransitionApplied` 가 `ExecutionCancelledError` 를 던져 기대하던 EXECUTION_FAILED/NODE_FAILED 대신 EXECUTION_CANCELLED 로 관측 가능하게 바뀜, `ai-turn-orchestrator.service.ts:392-413` `assertLinkedTransitionApplied` 참조). 다만 단일 통합 테스트에만 의존하는 구조는, 이 PR 이 다른 seam(`reparkAiResumeTurn`)에서 이미 겪은 "인자 shape 만 보존하는 뮤턴트에는 안 걸린다"는 교훈(10R, `ai-turn-orchestrator.service.spec.ts:140`)과 같은 종류의 취약점을 잠재적으로 남긴다 — focused 단위 테스트가 없으면 향후 리팩터가 실패 사유를 즉시 알려주지 못하고 더 먼 통합 레이어의 부수효과로만 드러난다. `plan/in-progress/retry-turn-terminal-guard.md` §코드 표 #24 가 형제 분기(RUNNING 유지, `:1600`)의 동일 클래스 갭을 이미 P3 로 등재했으나("현재 호출 그래프상 도달 불가능해 보이는 방어 코드"), isFailed 분기 쪽은 실제로 도달 가능하고 이 PR 의 실제 버그였다는 점에서 우선순위가 다르다.
  - 제안: `tryLockActiveExecutionAndSaveNodeExec` describe 블록에 3번째 `opts` 인자를 반영한 직접 단위 테스트(형제 `updateExecutionStatus` 테스트와 동일 패턴 — `dbExecutionStatus=FAILED` + `{allowRetryReentry:true}` → 잠금 SQL 에 `'failed'` 포함 확인, 및 위 항목의 CANCELLED/COMPLETED 대조)를 추가해 통합 테스트 1건에만 의존하는 구조를 보강.

- **[WARNING]** `execution.retry_last_turn` 원자 claim/짝 전이 가드에 대한 실 Postgres(e2e) 검증 부재 — 이미 P2 로 추적 중인 잔여 갭 재확인
  - 위치: 전체 기능 (`codebase/backend/src/modules/execution-engine/retry-turn.service.ts` `claimSpawnedRetryRow`/`applyRetryLastTurn`, `execution-engine.service.ts` `lockNonTerminalExecutionRow`/`tryLockActiveExecutionAndSaveNodeExec`/`updateExecutionStatus`)
  - 상세: `grep -rl "retry_last_turn" codebase/backend/test`(e2e 디렉터리, `*.e2e-spec.ts`) 결과 0건 — 이 브랜치가 8~10 라운드에 걸쳐 고친 결함 계열(짝 전이 SQL 가드가 opt-in 을 반영 못해 항상 0행 매칭)은 전부 Jest unit mock 레벨에서만 검증된다. `plan/in-progress/retry-turn-terminal-guard.md` 자신이 이미 이 갭을 "5R W6"/"§코드 표 #3"으로 P2 등재했다("atomic-consume SQL 이 어느 계층에서도 미검증... e2e(`grep -rl "retry_last_turn" test/` 0건) 모두에서 평가되지 않는다") — 본 리뷰 시점(10R 이후)까지도 미해소로 남아 있음을 재확인했다. 이 결함 계열(SQL 문자열/가드 로직이 실제 상태값과 미묘하게 어긋남)은 전형적으로 e2e/실 DB 가 잡는 유형이며, 이 PR 자체의 헤드라인 버그(8R CRITICAL)도 "mock 을 정직하게 다시 짜고 나서야" 처음 드러났다는 사실이 이를 방증한다. 동일 lock 패턴(`FOR UPDATE` + 조건부 status)을 쓰는 과거 refactor(06 C-2 atomic claim)에서도 "e2e 만 포착, unit 미검출"이 프로젝트 자체 기록에 남아 있다.
  - 제안: 신규 등재는 불필요(이미 P2 추적 중)하나, 유사 클래스 버그가 이미 3라운드(8R/9R/10R) 연속으로 unit mock 을 정교화하는 방식으로만 대응돼 온 이력을 고려하면, 다음 라운드에서는 `execution.retry_last_turn` WS 커맨드 → 실 Postgres FOR UPDATE 가드 → 짝 전이까지 검증하는 e2e 1건을 우선순위 상향 검토할 것을 권고.

- **[INFO]** `assertTransition` 의 opt-in 회귀 테스트가 신규 타깃(WAITING_FOR_INPUT)을 다루지 않음
  - 위치: `codebase/backend/src/modules/execution-engine/state/state-machine.spec.ts:178-192`(`assertTransition` describe), 대비 `canTransition` describe 의 93-127행
  - 상세: 이번 diff 가 `canTransition` 에 FAILED→WAITING_FOR_INPUT opt-in 을 추가했고 `canTransition` 레벨 테스트(93, 102, 114행)는 이를 양방향 다 다루지만, `assertTransition`(throw 래퍼) 레벨 테스트(180, 188행)는 여전히 FAILED→RUNNING 쌍만 다룬다. `assertTransition` 이 `canTransition` 결과에 단순 위임(`state-machine.ts:100`)하는 얇은 래퍼라 실질 위험은 낮지만, 완전성 관점에서 대칭 테스트가 비어 있다.
  - 제안: `assertTransition(FAILED, WAITING_FOR_INPUT, {allowRetryReentry:true})` not-throw / 미지정 시 throw 대조 테스트 1쌍 추가(저비용, 5분 내 작업).

## 요약

이번 diff(`execution.retry_last_turn` 짝 전이가 DB 가드에서 항상 0행이던 CRITICAL 을 포함한 atomic-claim 기능 전체)는 테스트 관점에서 이례적으로 높은 성숙도를 보인다. 특히 `mockTxManagerQuery` 를 "SQL·status 무관 항상 성공"에서 "SQL 의 `status IN (...)` 목록과 현재 DB status 를 실제로 대조"하는 honest mock 으로 교체한 것은 8라운드 동안 은폐됐던 버그를 즉시 드러낸 근본적인 테스트 품질 개선이었고, 이후 9R/10R 도 동일 정신(뮤턴트가 호출 shape 을 보존해야 로직 삭제를 잡는다)으로 `reparkAiResumeTurn` 세부 배선까지 잠갔다. `state-machine.spec.ts`/`retry-turn.service.spec.ts` 의 신규 테스트들도 opt-in 유무·claim 성공/실패·재배달 안전성 등 엣지 케이스를 광범위하게 커버하며, 각 발견에 대응하는 mutation 검증(N/N RED)까지 수행한 이력이 `plan/in-progress/retry-turn-terminal-guard.md` 에 상세히 기록돼 있다. 다만 정밀 추적 결과 두 가지 방향의 잔여 비대칭을 확인했다: (1) DB 가드의 "opt-in 시에도 COMPLETED/CANCELLED 는 배제한다"는 안전 불변식이 상태머신 계층과 달리 직접 대조 테스트가 없고, (2) `tryLockActiveExecutionAndSaveNodeExec` 자신의 단위 테스트가 신규 `opts` 파라미터를 반영하지 못해 이 PR 의 실제 버그 시나리오(isFailed 분기)가 단 하나의 통합 테스트에만 의존한다. 두 항목 모두 이 PR 이 진행해온 자체 백로그(`plan/in-progress/retry-turn-terminal-guard.md` #21/#24)와 실질적으로 겹치는 이미 알려진 잔여 리스크이며, 이번 diff 가 새로 도입한 회귀는 아니다. e2e 부재(#3, 기존 P2)도 재확인했다 — 이 결함 계열의 성격(SQL/가드 로직 오류)상 실 DB 검증이 최종적으로 필요하다는 점은 프로젝트 자신의 과거 사례로 이미 근거가 있다.

## 위험도
LOW
