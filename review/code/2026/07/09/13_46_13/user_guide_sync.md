### 발견사항

- **[WARNING]** `02-nodes/triggers.mdx` + `.en.mdx` 의 파라미터 이름 검증 Callout 이 이번 변경으로 도입된 "저장 시점(`POST /:id/save`)에도 즉시 거부" 동작을 반영하지 못해 stale
  - 변경 파일: `codebase/backend/src/modules/workflows/workflows.service.ts` (신규 `validateManualTrigger` 파라미터 스키마 게이트 — 빈 이름/식별자 위반/중복 이름을 저장 시점에 `400 INVALID_TRIGGER_PARAMETERS` 로 거부), `codebase/frontend/src/components/editor/settings-panel/node-configs/trigger-configs.tsx` (동일 규칙을 프론트 inline 에러로 미러링)
  - 매트릭스 항목: PROJECT.md §변경 유형 → 갱신 위치 매핑, "자주 누락되는 항목" 중 "노드 schema 변경 vs 가이드 본문 — dict 키만 갱신하고 `02-nodes/<cat>.mdx` 의 FieldTable 미갱신. 가이드 본문이 spec 과 어긋남" (`.claude/config/doc-sync-matrix.json` `node-schema-change` 행 target: "`codebase/frontend/src/content/docs/02-nodes/<cat>.mdx` 의 FieldTable"). 본 건은 FieldTable 자체가 아니라 같은 페이지의 검증 시점을 서술하는 `<Callout>` 이지만, "가이드 본문이 실제 동작과 어긋남" 이라는 동일한 실패 패턴 — trigger glob(`codebase/backend/src/nodes/**`) 은 문자 그대로 히트하지 않으나(변경이 `workflows.service.ts`/frontend config 컴포넌트에 위치) 사용자 가시 효과는 노드 필드 검증 시점 변경과 동일해 semantic 매칭으로 판단
  - 누락된 동반 갱신: `codebase/frontend/src/content/docs/02-nodes/triggers.mdx:61-63`, `codebase/frontend/src/content/docs/02-nodes/triggers.en.mdx:50-52`
  - 상세: 실측 결과 두 파일의 Callout 은 각각 "파라미터 이름이 중복되면 validate가 실패하고 **실행**도 거부돼요" / "Duplicate parameter names fail validation and **the run is rejected**" 로, 검증이 **실행 시점**에만 일어나는 것으로 읽힌다. 이번 diff 는 `WorkflowsService.validateManualTrigger` 에 동일 검증(`validateTriggerParameterSchema`)을 **저장 시점**(`saveCanvas`, `POST /:id/save`)에도 추가했고, 프론트 `ManualTriggerConfig` 는 키 입력마다 인라인 에러를 표시한다. 즉 사용자는 이제 저장을 누르는 순간(또는 타이핑 중) 거부당하는데, 가이드는 여전히 "실행 시 거부"만 안내해 오해를 유발한다. 정확히 이 gap 을 개발자 본인의 plan 문서(`plan/in-progress/spec-update-manual-trigger-save-time-error-code.md` 마지막 체크리스트 항목: "(user_guide_sync WARNING, re-review) 유저 가이드 `02-nodes/triggers.mdx` + `.en.mdx` 의 Callout 이 파라미터 이름 검증을 실행 시점에만 거부로 읽히게 stale")도 스스로 인지했지만, 같은 PR/turn 에서 고치지 않고 `project-planner` 위임 후속 plan 으로 미뤘다. 이는 PROJECT.md "사후 보정 PR 패턴 금지 — 같은 turn 원칙"("문서·번역 갱신은 코드 변경과 같은 PR·같은 turn·같은 단계 commit 안에서 끝낸다")을 정면으로 위반한다 — spec 문서(§6 에러코드 표) 반영은 project-planner 소관이 맞지만, **frontend user-guide MDX 본문 정정**은 developer 가 같은 turn 안에서 직접 할 수 있는 작업이라 위임 대상이 아니다. ko/en 두 파일이 동일하게 stale 해 언어 간 parity 불일치는 아니므로(i18n 가드는 통과) CRITICAL 아닌 WARNING.
  - 제안: 같은 PR 안에서 두 Callout 을 "파라미터 이름이 중복되거나 규칙을 어기면 **저장 시점**과 실행 시점 모두 거부돼요(캔버스 편집 중에는 즉시 인라인 에러로 표시)" 류 문구로 갱신. `plan/in-progress/spec-update-manual-trigger-save-time-error-code.md` 의 해당 체크리스트 항목은 spec(§6 표) 갱신 전용으로 남기고, user-guide MDX 항목은 별도로 지금 처리하거나 최소한 "developer 가 같은 turn 내 처리, project-planner 위임 대상 아님" 으로 재분류.

- **[INFO]** `INVALID_TRIGGER_PARAMETERS` 가 저장 경로(`workflows.service.ts`)에서도 재사용되지만 `backend-labels.ts` `ERROR_KO` 에 매핑이 없음 — 이번 diff 로 신규 발행된 코드는 아니고(기존에 `workflows.controller.ts` execute 경로가 이미 이 코드를 미매핑 상태로 썼음), 다만 이번 변경으로 노출 빈도가 "실행 시" → "**저장 시마다**" 로 크게 넓어짐
  - 변경 파일: `codebase/backend/src/modules/workflows/workflows.service.ts:610-625` (`validateManualTrigger` 신규 `BadRequestException({ code: 'INVALID_TRIGGER_PARAMETERS', ... })`)
  - 매트릭스 항목: `.claude/config/doc-sync-matrix.json` `new-error-code` 행 — trigger glob 이 `codebase/backend/src/nodes/core/error-codes.ts` 로, 이 코드는 `ErrorCode` enum 이 아니라 컨트롤러/서비스의 raw string literal 이라 **glob 상 리터럴 매칭은 아님**(strict 판정: N/A). 다만 PROJECT.md "자주 누락되는 항목" 의 "backend warning/error code → ko 매핑 — 백엔드가 새 warning/error 코드를 발행하면 ... 같은 commit 에 [WARNING_KO/ERROR_KO 매핑]. 누락 시 사용자에게 영문 그대로 노출" 취지와 맞닿아 있어 INFO 로 별도 기록
  - 누락된 동반 갱신: `codebase/frontend/src/lib/i18n/backend-labels.ts` 의 `ERROR_KO` (형제 코드인 같은 `saveCanvas` 경로의 `GRAPH_VALIDATION_FAILED` 는 이미 매핑돼 있어 비대칭)
  - 상세: 실측(`grep`) 결과 `ERROR_KO` 에 `INVALID_TRIGGER_PARAMETERS` 항목 없음 → 저장 시 malformed 파라미터를 넣으면 영문 fallback 메시지("Manual Trigger has an invalid parameter schema")가 그대로 노출된다. 이 gap 자체는 이번 PR 이전부터 존재(execute 경로)했고, 직전 리뷰 라운드(`review/code/2026/07/09/11_08_21/api_contract.md`)도 동일 사안을 INFO 로 이미 기록했다(중복 재확인). 다만 "얼마나 자주 사용자가 마주치는가" 가 execute(드묾) 에서 save(캔버스 편집마다 빈번) 로 늘어난 점은 이번 diff 가 만든 실질적 변화이므로 우선순위 재검토 가치가 있다.
  - 제안: `ERROR_KO` 에 `INVALID_TRIGGER_PARAMETERS` 한국어 매핑 추가 검토(비차단, 이미 프론트 inline 검증이 대부분의 경우를 사전 차단하므로 fallback 경로 빈도는 낮음).

- **[INFO]** i18n dict parity·백엔드 재진입 fix 는 사용자 가이드 sync 관점에서 문제 없음 (참고, 액션 불필요)
  - 위치: `codebase/frontend/src/lib/i18n/dict/{ko,en}/nodeConfigs.ts` (`errorNameRequired`/`errorNameInvalid`/`errorNameDuplicate` 3개 신규 키), `codebase/backend/src/modules/execution-engine/execution-engine.service.ts` / `retry-turn.service.ts` (재진입 durable input fix), `codebase/backend/src/modules/execution-engine/utils/load-trigger-parameter-schema.ts` (조회 방식 `category`→`type`)
  - 상세: 신규 UI 문자열 3건은 `ko`/`en` 양쪽에 동시 등록됐고(`i18n.test.ts` 포함 vitest `src/lib/i18n` 스위트 79 passed / 1 skipped / 0 failed 실측 확인), TSX 에 하드코딩된 한국어 리터럴 없이 전부 `t("nodeConfigs.trigger.errorName...")` 경유. 엔진 재진입 input·트리거 조회 방식 변경은 순수 내부 버그 수정으로, `codebase/frontend/src/content/docs/05-run-and-debug/` 어떤 페이지도 재진입/redrive/park-resume 메커니즘을 사용자 대상으로 노출하지 않아(grep 실측: 매치 0건) 해당 섹션 갱신 불필요. `version-history.mdx`(복원 흐름) 도 `restoreVersion` 이 검증을 건너뛰는 예외를 서술할 필요는 없음(사용자 입장에서 "복원은 항상 성공" 이라는 기존 문서 내용과 상충하지 않고 오히려 더 관대해지는 방향).
  - 제안: 없음.

### 요약

매트릭스 21개 행(`.claude/config/doc-sync-matrix.json`) 중 semantic 매칭 후보로 검토된 것은 `node-schema-change`(가장 근접, glob 은 미스지만 효과는 동일)·`new-error-code`(glob 미스)·`run-debug-flow-change`(N/A 확인)였고, glob 직접 매칭 트리거(새 노드 추가·통합 변경·신규 섹션 디렉토리·인증 흐름·표현식 언어)는 해당 파일 변경이 없어 전부 비매칭이다. i18n 신규 키 3건은 ko/en parity 완전 준수(vitest 실측 통과)로 CRITICAL 없음. 유일한 실질 갭은 `02-nodes/triggers.mdx`/`.en.mdx` 의 파라미터 이름 검증 Callout 이 신규 저장 시점 게이트를 반영하지 못한 stale 상태이며, 개발자 자신이 이를 인지하고도 같은 turn 에서 고치지 않고 project-planner 후속 plan(spec 전용이어야 할 문서)에 끼워 넣어 "동일 turn" 원칙을 위반했다 — WARNING 1건. 부수적으로 `INVALID_TRIGGER_PARAMETERS` 의 `ERROR_KO` 미매핑은 이번 PR 신규 발행이 아닌 기존 gap 의 노출 빈도 확대라 INFO 로 하향 기록(직전 리뷰 라운드와 중복 확인).

### 위험도

MEDIUM
