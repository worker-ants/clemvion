# RESOLUTION — §J-후속 (최종 라운드)

리뷰: `review/code/2026/07/24/01_46_34/SUMMARY.md` — RISK=**LOW**, **Critical 0**, Warning 2.
forced 7명 전원 확보. 직전 라운드(01_25_14) Warning 4건이 전부 반영됐음을 다수 리뷰어가
파일 열람·grep·테스트 실행으로 독립 재검증했다.

## Warning (2)

### W1 §L 캐너리가 회귀 수정과 같은 diff 에 포함 (scope) — 유지, 근거 기록

리뷰어 자신이 "기능 영향 없어 되돌릴 필요 없음, 비차단 권고" 로 판정했고 직전 라운드
INFO#3 에서도 검토·수용됐다. 유지한다. 다만 지적은 옳다 — 다음부터 "회귀 수정" 과
"새 갭 캐너리 등록" 은 커밋을 나눈다.

### W2 넛지 훅 superset 테스트가 "할당 개수" 축을 생성하지 않음 (testing) — 반영

진짜 갭이다. push 가드의 `GeneratedFloorTest` 는 두 축(값 SHAPE × 할당 개수)을 만드는데
넛지 훅 쪽은 값이 항상 유일 할당인 경우만 만들고 있었다. **이 PR 전체의 전제가 "두 훅이 같은
방식으로 두 번 회귀했다"** 이므로 비대칭 커버리지는 실질 구멍이다. 더구나 접두 그룹의 붕괴는
그룹이 **반복을 실패해야** 일어나므로, 단일 할당만으로는 엄밀히 더 쉬운 문제를 푼 것이다.
→ `_TEMPLATES` 도입(단일 + 다중 할당 3종). 생성 케이스 116 → 464건.

## INFO 반영

- **#3** plan 이 "넛지 훅도 §L 갭을 공유한다" 고 **산문으로만** 주장하고 테스트가 없었다.
  `test_shares_the_push_guard_s_known_gap` 추가 — 주장이 실행 가능한 단언이 됐고, §L 수정 시
  두 캐너리가 함께 뒤집힌다.
- **#20** 비-vacuity 하한 `10` 이 실측 대비 지나치게 느슨했다(넛지 336/464, push 147/203 참여인데
  하한은 3~5%). 절대 개수는 형태가 추가될수록 **조용히 느슨해진다** → 비율 하한 50% 로 전환.
  현재 양쪽 72% 로 여유가 있다.

## INFO 미반영(사유)

- **#7** plan 표의 "168건" 수치가 §L 형태 추가 후 그대로 재현되지 않는다 — 리뷰어도 핵심 주장
  ("손실 0, 엄밀한 상위집합")은 현재 목록으로 재계산해도 유지된다고 확인. 이미 처분된 저우선.
- **#17/#18/#19** 검사 로직 복제·상수명 불일치·대칭 복제 기준 — 전부 리뷰어가 "(선택)" 판정.
  #18 은 이번에 양쪽 다 `_MIN_PARTICIPATION` 으로 통일되며 자연 해소됐다.
- **#8/#9/#10/#11~#16** 조치 불요 판정.

## 리뷰가 놓친 것 — 같은 탈출구가 축에도 있었다

W2 를 반영한 뒤 뮤테이션을 돌리니 **다중 할당 템플릿을 지워도 아무 테스트도 실패하지 않았다**
(R2 GREEN). 값 목록에서 발견했던 것과 **정확히 같은 탈출구**다 — 축도 입력 생성이라, 실패하는
superset 테스트를 "축을 빼서" 통과시킬 수 있다.
→ `test_both_axes_are_actually_generated` 를 양쪽에 추가(다중 할당 ≥2종 + 단일 할당 기준선).
재측정: R2'·R3 둘 다 RED 로 뒤집혔다.

같은 클래스를 한 PR 에서 두 번 만났다는 건 **생성 기반 테스트의 일반 취약점**이라는 뜻이다:
입력 집합 자체가 커버리지이므로, 그것을 줄이는 편집은 언제나 조용히 통과한다. 이력을 이름으로
고정하는 것이 유일한 방어다.

## 검증

- 하네스 전체 **555건 OK**(552 → +3), plan-frontmatter 105건 OK.
- 뮤턴트: R1 넛지 훅 폴백 제거 → `test_no_classification_is_lost` RED /
  R2' 넛지 훅 축 제거 → `test_both_axes_are_actually_generated` RED /
  R3 push 가드 축 제거 → 동일 RED. 전부 포착, 원복 확인.

## 수렴 판단

궤적: 1R C0/W4 → **2R C0/W2(LOW)**. 남은 Warning 중 하나는 리뷰어가 비차단 권고로 명시했고,
다른 하나는 반영 완료다. 여기서 수렴으로 보고 PR 을 올린다.

## 한계

GitHub Actions 가 저장소 전체에서 비활성이라 위 수치는 **전부 로컬 실행** 결과다.
