해당 없음

변경된 코드 전체가 LLM 시스템 프롬프트 구성, 텍스트 채널 leak 탐지/복구, 스트리밍 이벤트 처리에 관한 것입니다. 데이터베이스 스키마 변경, SQL 쿼리, ORM 호출, 마이그레이션, 커넥션 관리 등 데이터베이스와 직접 관련된 코드가 포함되지 않습니다. `persistAssistantTurn` → `sessionService.appendMessage` 호출은 존재하나, 해당 구현은 이번 diff 범위 밖이며 테스트에서는 mock으로 대체되어 있습니다.

### 요약

이번 변경사항은 순수하게 LLM 프롬프트 엔지니어링(self-check 지시문 추가)과 서버 사이드 plan JSON leak 복구 로직(텍스트 파싱 → 합성 tool call 변환)에 국한됩니다. 데이터베이스 접근 패턴, 인덱스, 트랜잭션, 스키마 등 데이터베이스 관련 요소가 전혀 없어 데이터베이스 관점에서 검토할 항목이 없습니다.

### 위험도
NONE