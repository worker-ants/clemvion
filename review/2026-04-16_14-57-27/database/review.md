해당 없음

변경된 파일들은 프론트엔드의 MDX 문서 시스템(파일 기반 docs registry, MDX 컴포넌트, 테스트 픽스처, 스펙 문서)으로 구성되어 있으며, 데이터베이스 접근이 전혀 없습니다. `registry.ts`는 파일시스템(`fs`)에서 MDX 파일을 읽어 인메모리 Map으로 인덱싱하는 방식이며, DB 쿼리·ORM·마이그레이션·커넥션 관리 등 데이터베이스 관련 요소가 존재하지 않습니다.

### 요약
모든 변경사항은 순수 파일시스템 기반의 문서 레지스트리와 MDX 렌더링 설정에 해당하며, 데이터베이스와 무관합니다.

### 위험도
NONE