해당 없음

이번 변경사항은 Zod 스키마 정의(`keyValueSchema`에 `.passthrough()` 추가, `optionSchema`의 `value` 기본값 변경 및 export), 그리고 해당 스키마에 대한 단위 테스트 추가로만 구성됩니다. 공유 상태, 비동기 처리, 스레드/프로세스 간 자원 접근, 락, 이벤트 루프 조작 등 동시성과 관련된 코드는 전혀 포함되어 있지 않습니다.

### 요약
변경된 코드는 순수한 스키마 선언(Zod 객체 정의)과 동기 단위 테스트로 구성되며, 어떠한 동시성 관련 패턴(비동기 I/O, 공유 상태, 락, 병렬 실행 등)도 포함하지 않습니다. 동시성 관점의 검토 대상이 아닙니다.

### 위험도
NONE