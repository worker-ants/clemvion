해당 없음

변경된 파일 50개는 모두 프론트엔드 UI의 국제화(i18n) 작업입니다. 하드코딩된 문자열을 `useT()` / `translate()` 호출로 교체하고, 테스트에서 로케일 초기 상태를 설정하는 내용이 전부입니다. 백엔드 API 엔드포인트, 요청/응답 스키마, HTTP 상태 코드, 인증 로직, URL 설계 등 API 계약에 영향을 주는 요소는 변경되지 않았습니다.

### 요약
이번 변경은 순수한 클라이언트 사이드 텍스트 렌더링 레이어의 수정으로, API 계약과 무관합니다. 서버와의 통신 방식, 데이터 구조, 인증 흐름은 모두 그대로 유지됩니다.

### 위험도
NONE