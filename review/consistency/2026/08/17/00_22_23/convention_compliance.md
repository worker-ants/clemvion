### 발견사항

- **[WARNING]** `3-error-handling.md §2.2` 의 "실행 에러 형식" 예시가, 본 PR 이 방금 코드베이스 전수 실측으로 "죽은 필드"임을 증명한 `nodeName` 을 여전히 정본 예시로 싣고 있다
  - target 위치: `spec/5-system/3-error-handling.md` §2.2 "실행 에러 형식" (예시 JSON `"nodeName": "AI Agent"`)
  - 위반 규약: `spec/conventions/node-output.md` §3.2 "`output.error` 표준 형태" — 정식 규약 SoT 는 `{ code, message, details }` 세 필드만 정의하며 `nodeId`/`nodeName`/`nodeType`/`executionId`/`stack`/`requestId` 는 없음. 또한 실제 구현 DTO `codebase/backend/src/common/swagger/error-response.dto.ts` 의 `ErrorResponseBodyDto` 도 `code`/`message`/`requestId`/`details` 4필드뿐, `nodeName` 등은 없음.
  - 상세: 본 diff(6-websocket-protocol.md §4.1)는 `nodeName`→`nodeLabel` 4행을 정정하며 "실측(엔진 emit 전수가 `nodeLabel: node.label ?? node.type`, `nodeName` emit 0건)" 이라는 근거를 명문화했다(`git -C <worktree> grep -n "nodeName" codebase/backend/src codebase/frontend/src` 로 재확인해도 `.spec.ts`/docs-coverage 테스트 외 실사용 0건). 그런데 같은 `spec/5-system/` 영역의 `3-error-handling.md §2.2`(frontmatter `status: implemented`, 별도 Planned 캐비엇 없음)는 정확히 동일한 phantom 필드 `nodeName` 을 여전히 "구현됨" 예시로 제시하고, 게다가 `node-output.md §3.2` 가 정의한 표준 `output.error` shape 과도 필드 집합이 통째로 다르다(이 예시는 노드-레벨과 최상위 실행-레벨 에러 표현을 혼합한 것으로 보이나 어느 쪽 실제 DTO 와도 일치하지 않는다). 이 PR 은 이 파일을 건드리지 않았고 관련 plan(`plan/in-progress/eia-fanout-and-internal-data-masking.md`, `plan/in-progress/spec-draft-eia-fanout-masking.md`)의 체크리스트에도 `3-error-handling.md` 언급이 없어, 이번에 확보한 실측 근거가 이 파일에는 적용되지 않았다. (참고: `execution.paused`(§4.1, 계획·미구현) 행이 `nodeName` 을 그대로 둔 것은 diff 자체가 "미구현 이벤트라 emit 대상 아님" 이라 명시해 의도적 예외이고, `15-chat-channel.md` 의 `nodeName` 은 필드명이 아니라 기각된 placeholder 후보를 논하는 산문이라 이 지적 대상이 아니다 — `3-error-handling.md §2.2` 만 실제로 어긋난다.)
  - 제안: `3-error-handling.md §2.2` 예시의 `nodeName` 을 `nodeLabel` 로 정정하거나(§4.1 정정과 동일 근거 재사용), 이 예시가 `node-output.md §3.2` 의 `output.error` 와 다른 별도 표현(예: 상위 REST 에러 wrapper)이라면 그 사실과 실제 구현 근거(어느 코드가 이 정확한 shape 을 방출하는지)를 명시하거나, 구현이 없다면 §2.1 처럼 "계획(Planned)" 캐비엇을 붙인다.

- **[INFO]** 본 diff 가 신규로 추가한 `[EIA §R17](./14-external-interaction-api.md)` 교차참조 4곳이 앵커 프래그먼트를 생략해, 같은 파일·같은 절 안의 기존 관례(구체 앵커 포함)와 표기가 갈린다
  - target 위치: `spec/5-system/6-websocket-protocol.md:195`, `:198`, `:1092`(신규 추가), `spec/5-system/12-webhook.md:327`(신규 추가)
  - 위반 규약: 명문화된 `spec/conventions/**` 규칙은 없음 — 다만 같은 두 문서 안에서 다른 R-항목 인용(`[Spec EIA §R7](./14-external-interaction-api.md#r7-seq-동일-공유--sse-와-notification)`, `[EIA §R6](./14-external-interaction-api.md#r6-notification-실패-시-자동-비활성화-금지)`, `[EIA §R9]`, `[EIA §R5]`)과 `14-external-interaction-api.md` 자체 내부 R17 자기참조(`[§R17](#r17-getstatus-...)`)는 전부 구체 앵커를 포함하는 확립된 지역 관례가 있다.
  - 상세: `git -C <worktree> checkout` 없이 `build` 가드(`codebase/frontend/src/lib/docs/__tests__/spec-links.test.ts`, `vitest run` 12/12 통과 확인)는 앵커 **부재**를 오류로 잡지 않는다(존재하는 앵커만 검증) — 따라서 CRITICAL 은 아니다. 다만 R17 절 자체가 ~215줄에 달해, 앵커 없는 링크는 독자를 문서 최상단에 내려놓고 R17 을 다시 찾게 만든다 — 바로 같은 diff 가 도입한 "표면 번호 아라비아 숫자 통일"(글리프 충돌 회피, `23_49_05` naming W1 후속조치) 취지와 결이 같은 정밀성 문제다.
  - 제안: 4곳을 `[EIA §R17](./14-external-interaction-api.md#r17-getstatus-의-currentnodecontext-실값-노출-null-placeholder-부분-번복--sse-역할-분담--outputdata-표면-제약-결정-2026-06-25-conversationthread-reload-노출-재조정-2026-07-09)` 형태로 통일(다른 R-항목 인용과 동일 패턴). 규약 갱신이 아니라 기존 지역 관례에 맞추는 것으로 충분.

### 요약

이번 target(`spec/5-system/`, diff-base `origin/main`, 실질 변경은 `6-websocket-protocol.md`/`12-webhook.md`/`14-external-interaction-api.md` 세 문서·순수 spec-only)은 정식 규약 준수 관점에서 대체로 양호하다. 신규 도입된 마스킹 관련 식별자·상수(`redactStoredErrorForResponse`/`redactStoredDataForResponse`/`sanitizePayloadForWs`/`deepRedactSecretsPreserving`/`VALUE_MASK_MARKER`/`KEY_MASK_MARKER`/`DEPTH_MASK_MARKER`/`WIRE_PRESERVED_FIELDS`)는 전부 실제 워킹트리 코드와 1:1 대응하며 camelCase/SCREAMING_SNAKE_CASE 명명이 기존 관례와 일치하고, `output.error` 표준 형태(`spec/conventions/node-output.md §3.2`)의 `retryable`/`retryAfterSec` 등 non-string 필드도 마스킹 로직(`typeof value !== 'object'` 는 통과)이 훼손하지 않음을 코드로 확인했다. Overview/본문/Rationale 구조도 유지되며(신설 서술은 본문 §4.1/§5.3, 근거 확장은 `## Rationale`), 앞선 리뷰 라운드(`23_49_05` naming_collision W1, 원형숫자/아라비아숫자 글리프 충돌)도 이번 diff 에서 실제로 해소됐다. 유일하게 실질적인 지적은 이 PR 이 `6-websocket-protocol.md` 에서 실측으로 확정한 "`nodeName` 은 죽은 필드, 정본은 `nodeLabel`" 이라는 사실이 같은 `spec/5-system/` 영역의 `3-error-handling.md §2.2` 에는 적용되지 않아 남아 있다는 점(WARNING) 과, 신규 R17 교차참조 4곳의 앵커 생략(INFO, 빌드 비차단)이다.

### 위험도
LOW
