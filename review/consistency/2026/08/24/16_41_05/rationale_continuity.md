# Rationale 연속성 검토 — `plan/in-progress/planner-doc-batch.md`

## 발견사항

- **[CRITICAL] B3(`nodeOutput.nodeType` carve-out 각주)가 `node-output.md` Principle 1.1.4 의 명시적 금지·근거와 정면 충돌 — Principle 1.1.4 텍스트를 잘못 인용해 정당화**
  - target 위치: 실제 적용 커밋 `4af06d951`(`spec/5-system/6-websocket-protocol.md` §4.4, `buttonConfig.nodeOutput` 행 뒤 신규 각주 "`nodeOutput.nodeType` 과 `payload.nodeType` 은 이름이 같고 계층이 다르다"), plan 체크리스트 `B3` 행 및 "`## 작업`" 의 `[x] B3 ... nodeType carve-out 각주 ... (W4)"`.
  - 과거 결정 출처: `spec/conventions/node-output.md` `### 1.1.4. 예외 — output.view 타입 판별자 패턴은 사용하지 않는다`(line 112-114) — *"초안에서 제안했던 `output.view.type = 'form' | 'carousel' | ...` 판별자는 폐기합니다. 노드 종류는 `$node["X"]` 로 접근하는 시점에 이미 워크플로우 정의상 알 수 있으므로 판별자는 불필요한 중복입니다."* 및 동일 계약을 다시 진술한 `spec/5-system/6-websocket-protocol.md` 자신의 `## Rationale` `C3 — nodeOutput 판별자 폐지`(2026-06-03) — *"노드 종류는 상위 `payload.nodeType` 로 이미 식별되므로 `nodeOutput` 안의 `type` 판별자는 불필요·중복"*.
  - 상세: 신규 각주는 *"Principle 1.1.4 는 `nodeOutput` 에 `{type, data}` 같은 **래퍼**를 씌우지 마라 이지, `nodeOutput` 안에 `nodeType` 이라는 **이름**을 쓰지 마라 가 아니다"* 라는 텍스트를 근거로 `nodeOutput.nodeType`(chat-channel 렌더러가 읽는 wire-only 키)을 carve-out 으로 정당화한다. 그러나 이 주장은 두 SoT 문서 어디에도 근거가 없다 — 1.1.4 원문은 `{type,data}` **wrapper shape** 를 금지 대상으로 특정한 적이 없고, 금지 이유는 *"노드 종류가 이미 다른 경로로 식별 가능하므로 어떤 형태·이름의 판별자든 중복"* 이라는 **일반 원칙**이다. 실측(`codebase/backend/src/modules/chat-channel/providers/discord/discord-message.renderer.ts:306-330`)으로 확인하면 `buttonConfig.nodeOutput.nodeType` 은 **정확히 같은 `execution.waiting_for_input`(interactionType=buttons) 이벤트의, 정확히 같은 노드**의 타입(`chart`/`table`/`carousel`)을 담는다 — WS §4.4 표 자신이 *"노드 종류는 상위 `payload.nodeType` 로 식별"* 이라 말하는 **바로 그 노드**다. 즉 `nodeOutput.nodeType` 은 형태(wrapper 여부)와 무관하게 C3·Principle 1.1.4 가 명시적으로 기각한 그 대안 — *"노드 종류를 `nodeOutput` 안에 별도 판별자로 다시 싣는다"* — 그 자체다. 신규 각주 스스로도 *"값 공간이 겹쳐(`carousel` 은 양쪽에 나온다) 오독하기 쉽다"* 고 인정하는데, 이는 "다른 개념" 이 아니라 "같은 개념의 중복" 이라는 증거다. Principle 1.1.4 자신의 SoT 본문(`node-output.md`)은 이번 커밋에서 **전혀 갱신되지 않았다** — 예외가 정말 필요하다면 그 예외는 원칙의 SoT 에 있어야 하는데, 지금은 하위 소비 문서(WS)만 "1.1.4 는 이걸 안 막는다" 고 **원칙의 적용 범위를 재해석**해 버렸다. 참고로 `nodeOutput.nodeType` 자체는 이 plan 이 새로 만든 필드가 아니라 이미 코드에 존재하던(legacy chat-channel flat shape, `#1209` 가 wire-only 8키로 열거) 사실이라 **필드 자체의 폐기·제거를 요구하는 지적은 아니다** — 문제는 "왜 원칙 예외인가" 의 근거가 부정확하다는 점이다.
  - 제안: 각주의 정당화 논거를 교체한다. 실제로 성립 가능한 논거는 *"layer 분리"* 다 — Principle 1.1.4 는 **핸들러가 반환하는 `NodeHandlerOutput.output`** 범위를 규율하고, `nodeOutput.nodeType` 은 핸들러 산출물이 아니라 **wire 조립 레이어**가 legacy 렌더러 하위호환을 위해 얹은 값이다(이는 Principle 0 자신의 wire-only 8키 carve-out 논리와 동형이다). 다만 이 논거를 쓰려면 (a) "1.1.4 가 `{type,data}` wrapper 만 금지한다" 는 부정확한 문장을 삭제하고, (b) `node-output.md` §1.1.4 본문에도 이 legacy wire-only 예외를 교차 참조로 명시해 **원칙의 SoT 가 스스로 예외를 인정**하도록 갱신한다(§자기-반증형 소정정 대상은 아니므로 developer 가 아니라 이 planner 턴에서 직접 반영). 그래야 "적용 안 됨" 주장이 아니라 "적용되지만 legacy 호환을 위해 의도적으로 허용" 이라는, 근거가 맞는 새 Rationale 이 남는다.

- **[WARNING] 4개 conventions(`node-output.md`/`egress-masking.md`/`chat-channel-adapter.md`/`conversation-thread.md`) 의 `## Rationale` 본문이 이번 2회차 payload 에도 여전히 누락 — B3 CRITICAL 이 바로 그 갭에서 나왔다**
  - target 위치: `_prompts/rationale_continuity.md` 전체 — `#### \`spec/...\`` 헤더 76개 중 위 4개 conventions 파일은 이번 회차(`16_41_05`)에도 **헤더 자체가 없다**(다른 대형 문서처럼 "본문 생략됨" placeholder 조차 없음).
  - 과거 결정 출처: 동일 plan 의 `74186fd51`(`fix(plan): 내 진단이 반증됐다`) 커밋 — *"`node-output.md` 는 bundle 113개에도 절단 목록에도 없다 = `related_specs` 후보 선정이 도달하지 못한다"* 는 자기 진단, 그리고 `13_30_49` rationale_continuity 의 동일 CRITICAL(1회차에서도 같은 갭 지적).
  - 상세: plan 은 이 문제를 "쓴 뒤 2회차를 돌리면 적재된다" 는 최초 진단에서 "후보 선정 자체가 안 된다, 예산과 무관" 으로 정정했지만, 정정 이후에도 **번들 자체를 고치는 조치는 하지 않았다**(직접 `Read` 로 우회). 이번 검토(본 세션)도 동일한 갭을 확인했다 — 즉 이 checker 가 `_prompts` payload **만** 근거로 판정했다면 위 CRITICAL(B3-Principle 1.1.4 충돌)을 **놓쳤을 것**이다(실제로 `13_30_49` 1회차 rationale_continuity 는 이 갭 때문에 B3 을 INFO 수준으로도 다루지 못하고 통과시켰고, WARNING 4 는 다른 checker(`naming_collision`)가 "표면 충돌로 읽힌다" 는 형태로만 지적했다). 이 checker 는 실제 저장소 파일을 직접 열어(Bash/Read) 검증했기 때문에 발견했지만, payload 만 소비하는 후속 라운드·다른 checker 는 계속 이 사각지대에 놓인다.
  - 제안: orchestrator 번들러가 `spec_impact` 목록에 든 파일의 `## Rationale` 은 `related_specs` 랭킹·예산 절단과 **무관하게 항상 포함**하도록 개선(이미 `feedback_consistency_spec_mode_budget.md` 로 반복 재발 기록됨). 단기적으로는 이 plan 의 "검증 기준" 에 "spec_impact 문서는 매 회차 직접 Read 로 재확인" 을 명시적 상시 항목으로 못박는 편이 안전하다.

## 요약

target 이 해소한 B1·B2·B4·B5·B6·B7 은 실제 저장소 파일(egress-masking.md/conversation-thread.md/redis-keys.md/spec-impl-evidence.md 등)을 직접 대조한 결과 각각의 과거 Rationale·설계 원칙과 정합했다. 그러나 B3("동일 이름·다른 계층" carve-out 각주)는 `node-output.md` Principle 1.1.4 와 WS 자신의 C3 Rationale 이 명시적으로 기각한 "노드 종류를 `nodeOutput` 안에 판별자로 다시 싣는다" 는 패턴을 실질적으로 재도입하면서, 그 근거로 원칙 원문에 없는 "wrapper 형태만 금지" 라는 부정확한 해석을 내세운다 — 코드 실측(`discord-message.renderer.ts`)으로 확인하면 `nodeOutput.nodeType` 은 같은 이벤트의 같은 노드를 가리키는 진짜 중복 판별자다. 이 결함은 이 시리즈가 반복해 온 "conventions 4개 문서의 Rationale 이 checker payload 에 구조적으로 누락된다" 는 갭과 직접 연결돼 있다 — payload 만으로는 이번에도 잡히지 않았을 사각지대다.

## 위험도

HIGH
