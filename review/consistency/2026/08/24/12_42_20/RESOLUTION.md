# RESOLUTION — `12_42_20` (`--impl-done spec/5-system/` 재실행, BLOCK: YES)

CRITICAL 1 · WARNING 0 · INFO 7. 직전 두 라운드의 CRITICAL 은 해소 확인됐고, 이번 것은
**같은 주장의 네 번째 미러**다.

## CRITICAL 1 — `conversation-thread.md` §9.7. 네 번째다

`12_24_55` 에서 WS §4.1 의 `.failed` `error` shape 을 정정했는데, **같은 주장이
`conversation-thread.md` §9.7 에도 있었다.** 이번 세션에서 미러를 놓친 것이 **네 번째**이고,
매번 이유가 달랐다:

| # | 놓친 이유 | 축 |
|---|---|---|
| 1 | 문서 셋만 세고 코드 주석 둘을 안 셌다 | 세는 **대상의 종류** |
| 2 | 조사 한 글자(`은`/`이`)로 grep 이 비켰다 | **매칭 방식** |
| 3 | 같은 파일의 다른 절을 안 봤다 | 스윕 **단위** |
| 4 | **다른 파일의 다른 주제 절**을 안 봤다 (`§9.7 WS→store 변환 계약`) | 스윕 **범위** |

4번은 앞의 셋과 또 다르다 — `conversation-thread.md` 는 *"대화 스레드"* 문서라 *"`.failed`
의 error shape"* 을 찾을 때 **떠올릴 목록에 없었다**. 주제로 파일을 고르면 계약이 실린
자리를 놓친다.

**이번엔 한 건씩 고치지 않고 먼저 훑었다** — `spec` 전체에서 `node.failed` 를 언급하는 파일
6개를 뽑아 각각 열었다. 그 결과 §9.7 이 **두 행 다** 결함을 갖고 있음을 발견했다:

- `node.failed` 행 — wire `error` 를 구조화 객체로 서술 (checker 가 지적한 것)
- `node.completed` 행 — *"`payload.output.error` 에서 추출"* 로 **한 겹 얕다**
  (checker 가 지적하지 않았다. 한 건만 고쳤으면 이게 다섯 번째 미러가 됐을 것이다)

둘 다 정정했다. §1.2.1 은 **건드리지 않았다** — 거기 `output.error.code` 는 도메인 레벨
서술(`NodeHandlerOutput.output.error`)이라 맞다. wire 맥락에서만 한 겹이 더 붙는다.

### 인과를 문서에 남겼다

§9.7 위에 ⚠️ 블록을 넣어 *"이 두 행이 프런트 결함을 낳았고, 코드 수정은 별건으로 트래커에
있다"* 를 적었다. 그리고 트래커의 🔴 항목에는 **반대 방향 지침**을 넣었다 — *"착수 시 그
문구를 다시 고칠 게 아니라 코드를 문구에 맞추고 ⚠️ 블록을 지워라"*. 그 블록이 **"아직 안
고쳐진 코드" 를 가리키는 표지**로 남는다.

### 권한

checker 판정(*"`spec/conventions/` + wire 계약이라 예외 조건 1·2 둘 다 불충족 → planner"*)에
**동의한다**. `conversation-thread.md` 는 이 PR 의 `spec_impact` 에 이미 있지만 **자기-반증형
소정정 항목으로** 올라 있었다(§8.4). §9.7 은 성격이 달라 `spec_impact` 주석을 갈라
**같은 파일이 두 성격을 함께 갖는다**고 명시했다 — 예외의 범위를 파일 단위로 뭉뚱그리면
다음 사람이 §9.7 도 예외로 읽는다.

## WARNING — 없음

## INFO

- **#1** (provider spec 3곳) — 이미 등재. checker 도 *"반복 지적 불요"* 로 확인.
- **#2** (spec 정정과 코드 수정의 시차) — 의도한 분리다. 트래커 🔴 항목이 그 시차를 닫는다.
- **#3** (`node-output.md` 에 wire `output` 재사용 교차참조 없음) — 타당하지만 그 파일은 이
  PR 의 `spec_impact` 밖이다. B 묶음(planner doc 4건)에 이미 `node-output.md` Principle 0
  항목이 있으니 **그때 함께** 본다.
- **#4** (`/api` prefix 표기 불일치) — 사전 존재, 이번 PR 변경분 아님. 넘긴다.
- **#5** (절대 라인 번호 인용) — checker 가 직접 *"위반 아님"* 으로 확인. 조치 불요.
- **#6** (`complete` 이동 후에도 정정이 계속 커밋됨) — 관행과 일치하고 checker 도 그렇게
  판정. 다만 지적의 실질(*"다음 세션은 complete 라벨만 보지 말고 트래커도 훑어라"*)이
  옳아서, 트래커 🔴 항목에 착수 지침을 상세히 남긴 것이 그 대비다.
- **#7** (`node-output-` 접두 공유) — 직전 라운드와 동일, 조치 불요.
