### 발견사항

- **[WARNING]** `turnDebug` 필드명이 `execution.waiting_for_input` 페이로드 안에서 서로 다른 두 shape 을 가리키는데, 이번 보안 수정의 새 JSDoc/테스트가 그중 한쪽(WS spec 미문서화 경로)을 명시적으로 인용하기 시작했다
  - target 위치: `codebase/backend/src/modules/websocket/websocket.service.ts` (커밋 `81f2c60d6`) 신규 JSDoc — "`turnDebug.llmCalls.llmCalls[]`(`ai-turn-orchestrator.service.ts` turn1 스냅샷)와 `nodeOutput.meta.turnDebug[].llmCalls[]`(`ai-conversation-helpers.ts` — 턴 누적 전체)" 두 경로를 나란히 서술. 같은 파일의 `websocket.service.spec.ts` 신규 테스트("waiting_for_input 의 중첩 turnDebug.llmCalls 도 외부 fanout 에 남으면 안 된다")도 두 경로를 각각 fixture 로 재현
  - 충돌 대상: `spec/5-system/6-websocket-protocol.md` §4.4 필드 테이블 — `execution.waiting_for_input` payload shape 행(`{ executionId, nodeId, nodeExecutionId, nodeType, interactionType, formConfig?, buttonConfig?, conversationConfig?, conversationThread? }`, 두 위치 모두 실제 구현: `websocket-protocol.md` 라인 ~188)에는 `turnDebug` 필드가 아예 없고, 같은 문서의 `nodeOutput.meta.turnDebug` 행(라인 ~449)만 `{ turnIndex, ragSources[], ragDiagnostics?, llmCalls?, toolCalls?, totalDurationMs? }` **배열** shape 으로 `turnDebug` 를 정의한다. `spec/5-system/14-external-interaction-api.md` §6.2(`execution.waiting_for_input` outbound webhook 예시)도 `turnDebug` 를 전혀 언급하지 않는다
  - 상세: 실제 emit 코드(`ai-turn-orchestrator.service.ts:615-623`)는 AI turn1 waiting 이벤트에 **top-level 단일 객체** `turnDebug: { llmCalls: <TurnDebugEntry 하나>, metadata: {...} }` 를 싣는다. 이는 WS §4.4 가 문서화한 `nodeOutput.meta.turnDebug`(배열, 위치도 다름)와 **이름은 같고 위치·shape 은 다른 별개 필드**다. 지금까지는 코드에만 존재하던 비문서 필드였는데, 이번 보안 수정이 이 필드를 strip 대상 경로로 명시 인용하면서 spec 독자가 `6-websocket-protocol.md` §4.4 표만 보고는 이 top-level `turnDebug` 의 존재·shape 을 알 수 없는 상태가 코드 주석 차원에서 더 굳어졌다. 두 `turnDebug` 를 같은 이름으로 두면 향후 strip 목록·클라이언트 파서 어느 쪽이든 한쪽만 보고 다른 쪽을 놓치기 쉽다(실제로 이번 수정 전까지 정확히 그 사각지대에서 raw LLM payload 가 샜다)
  - 제안: `spec/5-system/6-websocket-protocol.md` §4.4 payload 필드 테이블에 top-level `turnDebug`(AI turn1 전용, `{ llmCalls, metadata }`)를 `nodeOutput.meta.turnDebug`(배열)와 구분해 명시하거나, 두 필드 중 하나를 리네임하는 결정을 planner 턴에서 내릴 것. 이미 `plan/in-progress/spec-draft-eia-62-waiting-payload.md` 가 "이름 충돌(`turnDebug` top-level vs `nodeOutput.meta.turnDebug`)은 이 처방과 함께 정리" 를 별건 TODO 로 등재해 두었으므로, 신규 발견이 아니라 **그 항목의 처리를 미루면 안 된다는 근거가 이번 diff 로 하나 더 늘었다**는 점을 반영해 우선순위를 올릴 것을 권고

- **[INFO]** (확인 결과 문제 없음, 참고) 이번 보안 수정의 JSDoc 이 인용하는 spec 좌표는 모두 실재하고 정합됨을 직접 대조로 확인했다 — `6-websocket-protocol.md` §4.4 라인 519(`llmCalls` 는 "모든 외부 fanout 수신자 — EIA SSE·notification webhook·chat-channel — 에서 strip" 을 이미 top-level 한정 없이 선언), `14-external-interaction-api.md` §6.5(`execution.ai_message` payload, `llmCalls` strip 언급), `15-chat-channel.md` CCH-MP-01(동일 strip 언급). 즉 스펙이 이미 "top-level 만" 이 아니라 **깊이 무관 strip** 을 보장으로 서술하고 있었고, 이번 코드 수정(depth-1 → 깊이무관)은 그 문서화된 보장을 코드가 뒤늦게 따라잡은 것이다 — spec 이 구현보다 넓게 약속하고 있던 기존 drift 를 해소하는 방향이며 새로운 모순을 만들지 않는다. `EXTERNAL_STRIPPED_FIELDS = ['llmCalls']` 목록과 이름이 겹치는 다른 "외부에 노출돼야 하는" 필드는 `spec/**` 전수 grep 상 없음(모든 `llmCalls` 용례가 debug-only 로 일관 서술됨)
  - `emitNodeEvent`(`execution.node.*`)에도 동일 strip 이 방어적으로 적용됐는데, 코드 주석은 "node 이벤트는 현재 llmCalls 를 포함하지 않는다" 고 전제한다. `execution.node.completed` 의 `output` 은 `NodeHandlerOutput.output` 전체를 실어 Text Classifier/Information Extractor 의 `meta.llmCalls` 를 원칙적으로 포함할 수 있는 경로라, 이 전제는 정확히는 "현재 관측된 emit 경로에서는" 정도로 좁혀 읽어야 한다 — 다만 strip 자체는 이미 깊이 무관이라 전제가 틀려도 실질적 위험은 없다(코드 review 영역, cross-spec CRITICAL 아님)

### 요약
이번 diff(`81f2c60d6`, `websocket.service.ts` `llmCalls` strip depth-1 → 깊이무관)는 spec 문서(§4.4 라인 519)가 이미 "top-level 한정" 언급 없이 선언해 둔 strip 보장을 코드가 뒤늦게 따라잡는 수정으로, 인용한 spec 좌표(WS §4.4·EIA §6.5·chat-channel CCH-MP-01)는 모두 실재·정합해 새 spec-vs-spec 모순을 만들지 않는다. 다만 이 수정이 명시적으로 근거로 삼은 두 leak 경로 중 하나(top-level `turnDebug: { llmCalls, metadata }`, AI turn1 waiting emit)는 WS §4.4 필드 테이블에도 EIA §6.2 에도 문서화돼 있지 않고, 같은 이름의 `nodeOutput.meta.turnDebug`(배열, WS §4.4 기존 문서화 대상)와 위치·shape 이 다르다 — 이 이름 충돌은 이미 `plan/in-progress/spec-draft-eia-62-waiting-payload.md` 가 별건으로 추적 중이던 항목이지만, 이번 diff 로 코드 주석·테스트가 그 미문서화 필드를 정본처럼 계속 참조하기 시작했으므로 방치 비용이 늘었다는 근거가 하나 추가됐다.

### 위험도
LOW
