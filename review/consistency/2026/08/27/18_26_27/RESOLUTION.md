# RESOLUTION — consistency `18_26_27` (`--impl-done spec/conventions/`)

**BLOCK: NO** · CRITICAL 0 · WARNING 3 → **전부 반영**. INFO 는 사유와 함께 처분.

## W1 — **내가 이 PR 에서 두 번 편집한 파일의 다른 자리**

`PROJECT.md` 안에 `spec-link-integrity.test.ts` 서술이 **두 곳**이다:

- §문서 링크 검증 — 내가 이 PR 에서 3-scope 로 갱신
- §자동 가드 표(`:275`) — *"`spec/**.md` 본문 …"* 만 남은 **구식 서술**

같은 파일 안에 신·구가 공존했다. 3-scope 요약으로 갱신했다.

**이 세션에서 반복된 형태다** — 한 파일을 고치면서 그 파일의 **다른 자리**를 안 본다.
스윕 축(구현 심볼 → 원칙명 → 범위)을 세 번 넓혔는데, 이번엔 **같은 파일 안**이었다.

## W2 — 새 스코프가 알려진 사각지대를 상속한다. **실측했다**

`extractLinks()` 는 줄 단위로 `LINK_RE` 를 돌려 `[` 와 `](` 가 다른 줄이면 **침묵으로
통과**한다(기존 등재 항목). scope 3 도 이를 그대로 상속한다.

**내 첫 프로브는 0건이라 오판했다** — *"`]` 로 끝나고 다음 줄이 `(` 로 시작"* 을 봤는데,
실제 형태는 링크 **텍스트**가 줄을 넘는 것이다. 정본 `LINK_RE` 로 per-line 매칭 수와
whole-text 매칭 수를 비교하니 **58파일 중 2건**:

```
.claude/skills/code-review-agents/SKILL.md      1건
.claude/skills/consistency-checker/SKILL.md     1건
  둘 다 [`report_paths.py`\n> `has_report()`](../../_shared/report_paths.py)
```

**지금은 무해**하다 — 타깃 `.claude/_shared/report_paths.py` 가 실재한다(확인). 그러나
그 두 링크는 아무도 안 본다.

**한 줄로 펴지 않았다.** 선행 트래커가 *"펴는 것은 증상 처리"* 라고 이미 판단했고, 오히려
근본 수정 PR 이 감당할 전수에 **거버넌스 스코프도 포함된다**는 증거로 남기는 편이 낫다.
탐지 방법(per-line vs whole-text 카운트)과 내 오탐지 사례를 함께 등재했다.

## W3 — **전제가 바뀌어 있었다**

선행 항목은 *"`plan/**` 문서 내부 링크 위생을 아무도 안 진다"* 로 적혔는데, 예고된
`#1123` 가드가 **실제로 존재한다** — 같은 표의 `plan-frontmatter.test.ts` (3)
*"top-level 살아있는 plan 의 상대링크 무결성"*.

즉 책임은 이제 **있고**, §4.2 만 없는 곳(`plan-coherence-checker`)을 가리키고 있었다.
그 checker 정의에 링크 언급이 **0건**임을 재확인하고(grep), 원문은 취소선으로 남긴 채
실재 가드로 귀속시켰다. 선행 트래커 항목은 종결 처리했다.

**왜 이번에 잡혔나**: 이 PR 이 scope 3 을 넣느라 **그 셀을 두 번 편집**했는데도 인접한
오기를 못 봤다. **편집하는 셀의 나머지 문장을 읽지 않는** 형태이고, W1 과 뿌리가 같다.

`spec/` 편집이라 planner 턴 — 이 PR 은 이미 W1(§4.2 scope 3) 으로 planner 턴을 담고 있어
같은 커밋에 얹었다.

## INFO — 처분

- **#1** R-9 요약 괄호가 scope 3 을 안 담는다 → **표 셀이 정본**이고 R-9 는 family 성격
  요약이다. 표를 고쳤으므로 정본은 정확하다. 다음 R-N 개정 때 함께 다듬을 대상.
- **#2** 신규 결정을 `R-11` 로 승격 제안 → 이 결정은 **가드 스코프 확장**이지 새 설계 원칙이
  아니다. `R-9`(family 신설)의 적용이고, 표 셀 서술로 충분하다. 승격하면 R-N 이 스코프
  변경마다 늘어난다.
- **#3·#4·#5** 명명 충돌 없음·용어 형태 겹침·pathspec 순수 추가 — 조치 불요.

TEST WORKFLOW 4단계 PASS — frontend 6,174 passed(가드 19/19) · e2e 285.
