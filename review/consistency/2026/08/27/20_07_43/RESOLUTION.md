# RESOLUTION — consistency `20_07_43` (`--impl-done spec/5-system/`)

**BLOCK: NO** · CRITICAL 0 · WARNING 0 · 5 checker 전원 위험도 **NONE**.

Critical/Warning 이 없으므로 이 문서는 필수가 아니다. 그럼에도 남기는 이유는 **INFO 5 가
내가 커밋 메시지에 쓴 주장을 반증**했기 때문이다 — 그건 조치가 필요하다.

## INFO 5 — **내가 쓴 근거가 틀렸다**

커밋 메시지와 트래커에 이렇게 적었다:

> *"`code:` frontmatter 는 `spec-code-paths.test.ts` 가 검사하므로 안 고치면 build 가
> 깨진다"*

**틀렸다.** 그 가드는 **any-match** 다:

```ts
const anyMatch = codes.some((c) => globMatchesAny(c, root));
```

목록의 **한 경로라도** 실존하면 통과한다. `egress-masking.md` 는 유효 경로를 8개,
`14-external-interaction-api.md` 도 여럿 갖고 있으므로 stale 경로 하나로는 **안 깨졌을
것이다**.

**고친 것 자체는 옳다** — stale 경로는 다음 사람을 헛걸음시키고, 링크 가드는 본문
`[..](path)` 만 보지 frontmatter 를 안 본다. 하지만 *"강제된다"* 는 것과 *"옳다"* 는 것은
다르고, 나는 전자를 실측 없이 주장했다.

**이 세션에서 반복된 형태다** — 가드의 동작을 읽지 않고 *"검사하니까 막힌다"* 로 단정했다.
`--impl-done` 이 없었으면 그 거짓 근거가 트래커에 남아, 다음 사람이 *"frontmatter 는
기계가 지킨다"* 고 믿었을 것이다. 트래커에 정정과 함께 **강제되지 않는다**는 사실을 적었다.

## INFO 6 — 완료 항목 옆 구 서술의 시제

`node-output-allowlist.ts` 재배치 항목에서, 2026-08-23 원 등재문의 *"국소화만 되고 완전히
회복되진 않았다"* 가 오늘 붙인 완료 주석과 나란히 있어 시제가 어긋나 보인다는 지적.

원문은 **이력이라 지우지 않는다**. 대신 그 서술이 **이동 *전* 상태**를 말한다는 표식을
완료 주석 쪽에 달았다.

## 나머지 INFO — 조치 불요

- **#1** `EIA-AU-09` 제거는 **dangling 참조의 해소**이지 신규 충돌이 아니라는 확인.
- **#2** 경로 이동에 따른 spec 참조 동반 갱신, 구 경로 잔존 **0건** 확인.
- **#3** *"shared = 도메인 비의존"* 원칙은 spec 상 formal SoT 가 없다는 관측 — 코드리뷰
  영역이라는 판정에 동의한다. 이번 이동은 그 원칙을 **복원하는 방향**이다.
- **#4** 신규 `swagger-probe.ts` 는 spec 이 디렉토리 목록을 박제하지 않으므로 문서화 대상 밖.
- **#7** 신규 식별자 4건 전 저장소 grep 충돌 **0건**, 구 이름/구 경로 잔존 **0건**.
