# Cross-Spec 일관성 검토 — brand-refresh 부분 롤백 draft

검토 대상: `plan/in-progress/spec-draft-brand-rollback.md`
검토 시각: 2026-05-15 23:45:11
검토 모드: --spec (spec draft 검토)

---

### 발견사항

- **[WARNING]** `spec/6-brand.md §8.4.6` 현행 본문 — 인증 화면 변종 기술 잔재
  - target 위치: draft §8.4.6 표 (인증 화면 행), Drop-in 대체 범위 표
  - 충돌 대상: `spec/6-brand.md` §8.4.6 현재 본문 (라인 230–234)
  - 상세: 현행 `spec/6-brand.md §8.4.6` 의 인증 화면 행은 `"Full logo (light)"` 변종을 명시하고 있으며 `"배경은 soil-50 단색 (그라데이션 금지, §8.4.4)"` 설명이 남아 있다. draft 의 신 §8.4.6 표에서는 인증 화면 행의 비고가 `"카드 자체의 배경·여백은 … 10-auth-flow.md §1 가 정의"` 로 바뀌고 변종도 `"Full logo"` (라이트 한정 없음) 로 정정된다. 이 자체는 의도된 정정이나, 동반 동기화 대상인 `spec/2-navigation/10-auth-flow.md §1` 의 현행 본문도 여전히 `"Full logo (light)"` 와 `"soil-50 단색, 그라데이션 금지"` 를 명시 중이다. draft 는 이 두 문서를 동시 갱신하는 것을 전제하므로 누락 없이 한 turn 에 모두 반영되어야 한다. 반영 순서를 놓치면 §8.4.6 (brand spec) 은 롤백 완료인데 10-auth-flow.md 는 옛 soil-50 + Full logo (light) 표현을 유지하는 일시 불일치가 발생한다.
  - 제안: draft "다음 액션" §2에 명시된 대로, 세 파일을 반드시 동일 commit 에 함께 갱신한다. 부분 반영(brand spec 만 먼저 push) 은 일시 불일치 문제를 되살린다.

- **[WARNING]** `spec/2-navigation/_layout.md §2.1` 현행 본문 — `"Full logo (light)"` 표현 잔재
  - target 위치: draft 동반 동기화 §2 (`_layout.md §2.1` 제안)
  - 충돌 대상: `spec/2-navigation/_layout.md` §2.1 (라인 51)
  - 상세: 현행 `_layout.md §2.1` 로고 행은 `"expanded 상태에서는 Full logo (light), collapsed 상태에서는 Icon mark"` 로 라이트 자산 한정 표현이 박혀 있다. §8.2 컬러 토큰 폐기에 따라 이 `(light)` 한정을 제거해야 하는데, draft 는 해당 정정안을 제시하고 있다. 위 §8.4.6 와 동일하게 반영 누락 시 brand spec 은 롤백 완료인데 _layout.md 는 Stage 1 표현 그대로인 불일치가 생긴다. 현행 spec 으로는 두 문서가 서로 다른 변종 명을 쓰는 직접 모순이 된다.
  - 제안: 세 파일 동시 갱신 시 함께 처리. draft 제안 표현 (`"Full logo"` + `"라이트/다크 자산 선택과 변종 매트릭스는 brand spec §8.4 참조"`) 으로 교체.

- **[INFO]** `spec/6-brand.md §8.4.4` 현행 — `"로고가 노출되는 배경에도 그라데이션을 사용하지 않는다"` 조항과 롤백 후 상태 간 독자 혼란 가능성
  - target 위치: draft §8.4.4 (롤백) 본문 끝 "여전히 금지" 항목
  - 충돌 대상: `spec/6-brand.md §8.4.4` 현행 본문 (라인 213), `spec/2-navigation/10-auth-flow.md §1` 배경 복원안
  - 상세: 현행 §8.4.4 의 "여전히 금지" 목록에는 `"로고가 노출되는 배경에도 그라데이션을 사용하지 않는다"` 가 포함되어 있다. 이 조항은 Stage 1 에서 인증 화면 배경을 `soil-50` 단색으로 굳히는 근거였다. draft 롤백 후 §8.4.4 에는 동일 문장이 그대로 유지되는데, 반면 `10-auth-flow.md §1` 배경은 `"제품 브랜드 색상 또는 그래디언트"` (그라데이션 허용) 로 복원된다. 결과적으로 §8.4.4 의 "배경 그라데이션 금지" 조항과 인증 화면 배경 스펙이 표면적으로 상충한다. (실제 코드는 Shadcn neutral 그라데이션을 사용 중이므로 §8.4.4 의 해당 구절이 *워드마크 배경* 을 가리키는지 *페이지 배경* 을 가리키는지 독자가 혼동할 수 있다.)
  - 제안: draft §8.4.4 에 `"여전히 금지"` 목록의 `"배경 그라데이션 금지"` 구절을 `"워드마크 자체의 배경에 그라데이션 금지"` 로 범위를 명확히 하거나, 인증 화면 배경과 무관함을 괄호 주석으로 추가한다. 필수 수정은 아니지만 Stage 1 이후 이 조항 때문에 혼동이 생겼으므로 정정을 권장한다.

- **[INFO]** `spec/6-brand.md §8.4.1` 자산 매트릭스 — `apple-icon.png` / `opengraph-image.png` 경로와 `§8.6` 임시 상태 기술 간 불일치
  - target 위치: draft §8.4.1 (`유지 — 변경 없음`), draft §8.6 (`갱신` 본문)
  - 충돌 대상: `spec/6-brand.md §8.4.1` (라인 181–182) — `apple-icon.png (180×180 PNG)`, `opengraph-image.png (1200×630 PNG)` 로 경로 명시
  - 상세: §8.4.1 자산 매트릭스는 `apple-icon.png` (PNG) 와 `opengraph-image.png` (PNG) 로 명시되어 있다. 그런데 draft §8.6 에서 현재 실제 파일은 `apple-icon.svg` (SVG 임시) 와 `opengraph-image.svg` (비활성화) 이며 PNG 는 follow-up 임을 기술한다. §8.4.1 본문을 "유지 — 변경 없음" 으로 두면 매트릭스에 `.png` 경로가 그대로 남아 현재 파일 상태와 불일치한다. 이는 §8.6 에서 이미 인정된 임시 상태이므로 CRITICAL 은 아니지만, 향후 개발자가 §8.4.1 만 보면 PNG 가 이미 존재한다고 오해할 수 있다.
  - 제안: §8.4.1 자산 매트릭스의 `apple-icon.png` 행과 `opengraph-image.png` 행에 `(임시 SVG 사용 중 — §8.6 참조)` 주석을 추가하거나, 경로를 현재 실파일(`apple-icon.svg`, `opengraph-image.svg`)로 수정하고 §8.6 에서 PNG 목표 경로를 follow-up 으로 명시한다. draft 가 §8.4.1 을 "변경 없음" 으로 두기로 했다면, §8.6 의 임시 상태 기술이 §8.4.1 과 연결되어 있음을 cross-reference 로 보강하면 충분하다.

- **[INFO]** `spec/6-brand.md §8.6` 제목 — `"임시 자산 마이그레이션"` 에서 `"자산 마이그레이션 (갱신)"` 으로 변경 시 §9 변경 이력의 내용과 용어 통일 필요
  - target 위치: draft §8.6 섹션 제목 및 §9 이력 행
  - 충돌 대상: `spec/6-brand.md §8.6` 현행 제목 `"임시 자산 마이그레이션"` (라인 244), spec/0-overview.md §8 문서 맵 (참조가 없으므로 직접 충돌 없음)
  - 상세: 직접 충돌은 아니지만 §8.6 제목이 바뀌면 내부에서 "(근거: R-1, R-7)" 인용이 §8.6 내용과 살짝 어색해진다 (R-1, R-7 은 모티프/자산 정식화 근거인데 롤백 §8.6 은 PNG follow-up 이 주안점). 이 역시 독자 혼란 수준이며 CRITICAL 은 아니다.
  - 제안: §8.6 본문에서 `(근거: R-1, R-7)` 인용을 `(근거: R-1, R-7, R-13)` 으로 갱신하거나, R-13 에서 §8.6 PNG follow-up 을 명시적으로 언급한다.

---

### 요약

draft 의 롤백 범위와 동반 동기화 대상(`_layout.md §2.1`, `10-auth-flow.md §1`)은 명확히 식별되어 있고, 세 파일을 동시에 갱신한다는 "다음 액션" 전제가 충족되는 한 직접 CRITICAL 모순은 없다. 주요 위험은 세 파일 중 일부만 먼저 반영될 경우 발생하는 일시 불일치다. 두 개의 WARNING 모두 "동시 반영" 전제를 지키면 해소되는 순서 의존 이슈다. INFO 두 건(§8.4.4 배경 그라데이션 문구 범위 모호, §8.4.1 자산 경로와 실파일 상태 불일치)은 독자 혼동 가능성에 해당하며 반영 차단 수준은 아니다. 단일 commit 에 세 spec 파일을 함께 기록하는 것이 본 draft 의 정합성 유지 조건이다.

### 위험도

LOW
