# Plan 정합성 Check — spec/4-nodes/3-ai/ (--impl-prep)

> 검토 대상: 현재 worktree(`funny-mahavira-50d003`, branch `claude/ai-agent-tool-payload-followups-6472a9`)의 uncommitted diff — `spec/4-nodes/3-ai/1-ai-agent.md`(§12.16 신설 + frontmatter `code:`), `spec/conventions/node-cancellation.md`, `codebase/backend/src/nodes/ai/ai-agent/{ai-turn-executor.ts,llm-call-timeout.ts,ai-turn-executor.spec.ts}`, `codebase/backend/src/nodes/core/node-handler.interface.ts`. 이는 `plan/in-progress/ai-agent-tool-payload-budget-followups.md` **항목 B**(resume 턴 LLM 호출 timeoutMs+signal 배선)를 구현 중인 상태다.

## 발견사항

- **[WARNING]** §12.16 신규 텍스트가 single-turn 의 미해결 error-라우팅 gap 을 반영하지 않고 일반화된 주장을 함
  - target 위치: `spec/4-nodes/3-ai/1-ai-agent.md` §12.16 (신설) — "`LlmService.chat` 의 `opts.timeoutMs>0` 이면 `withTimeout`(자체 `AbortController`)이 race 로 throw 하며, **이 throw 는 §10 `LLM_CALL_FAILED`(retryable, network timeout 계열)로 귀결된다** — 신규 에러 코드 없음."
  - 관련 plan: `plan/in-progress/node-output-redesign/ai-agent.md` §"에러 컨트랙트" — `- [ ] (impl) **CRITICAL (single-turn 잔여)** executeSingleTurn 의 llmService.chat 호출을 try/catch 로 감싸 output.error.{code:'LLM_CALL_FAILED', ...} + port:'error' ... 반환. 근거: ai-turn-executor.ts:1209(첫 chat)·:1439(tool-loop chat) — 현 throw 미캡처, spec §7.3.` (여전히 미체크). `plan/in-progress/ai-agent-tool-payload-budget-guardrail.md` (선행, 머지됨) 도 동일 gap 을 "`node-output-redesign/ai-agent.md` 의 single-turn error 라우팅 미해결 CRITICAL 과 선행 의존"으로 명시하고, 그 때문에 `TOOL_DEFINITION_PAYLOAD_EXCEEDED` **하나만** `buildSingleTurnToolsOrError` 에서 special-case try/catch 로 흡수시켰다(§4.2 본문 및 `AiTurnExecutor` 클래스 JSDoc 의 "의도적 비대칭 계약"이 이 에러 하나로 범위 한정됨을 명시).
  - 상세: 코드 확인 결과 `executeSingleTurn` 내부에서 `llmService.chat(...)` 호출(§12.16 diff 가 `timeoutMs` 를 새로 추가한 바로 그 두 지점, `ai-turn-executor.ts` ~1685/~1817)은 여전히 어떤 try/catch 로도 감싸여 있지 않다 — `buildSingleTurnToolsOrError` 의 catch 는 `ToolDefinitionPayloadExceededError` 만 잡고 나머지는 `throw err` 로 재던진다(:1500-1521). `AiAgentHandler.executeSingleTurn`(facade, `ai-agent.handler.ts:130`)도 `return await this.turnExecutor.executeSingleTurn(...)` 을 그대로 리턴할 뿐 catch 가 없다. 따라서 single-turn 모드에서 새 `AI_AGENT_LLM_CALL_TIMEOUT_MS` 타임아웃(혹은 그 외 어떤 chat() 예외)이 발생하면 `port:'error'`/`output.error` 로 귀결되지 않고 그대로 엔진까지 전파돼 노드가 `FAILED` 로 종료된다 — §12.16 의 "§10 LLM_CALL_FAILED 로 귀결" 서술과 실제 동작이 어긋난다(multi-turn 은 orchestrator `handleAiMessageTurn`(`ai-turn-orchestrator.service.ts:596-600`) 의 catch → `handleAiTurnError` 로 정상 귀결되므로 이 서술이 부분적으로만 참). 새로 추가된 단위테스트(`ai-turn-executor.spec.ts`)도 opts 에 `timeoutMs`/`signal` 이 전달되는지만 검증하고, 실제 throw 가 error 포트로 라우팅되는지는 검증하지 않는다.
  - 제안: (a) `node-output-redesign/ai-agent.md` 의 해당 CRITICAL 항목이 남아있는 한, §12.16 서술에 "multi-turn 한정 / single-turn 은 §7.3 error 포트 미구현(별도 CRITICAL 잔여, node-output-redesign 추적) — 현재는 엔진 `FAILED` 로 이탈" 캐비트를 명시하거나, (b) 이번 PR(항목 B) 범위를 넓혀 single-turn 의 일반 `llmService.chat` throw 도 `TOOL_DEFINITION_PAYLOAD_EXCEEDED` 와 동일하게 error 포트로 흡수하도록 구현한 뒤 §12.16 서술을 유지. 둘 중 하나로 spec 문구와 실제/계획된 동작을 정합시켜야 한다.

- **[WARNING]** `pending_plans` frontmatter 누락 — 항목 B 를 추적하는 현재 plan 이 미등재
  - target 위치: `spec/4-nodes/3-ai/1-ai-agent.md` frontmatter `pending_plans:` (현재 `plan/in-progress/ai-agent-tool-connection-rewrite.md` 1건만 등재, `status: partial`)
  - 관련 plan: `plan/in-progress/ai-agent-tool-payload-budget-followups.md` — 항목 B 체크리스트 중 `- [ ] resume 경로 abortSignal plumbing (orchestrator → state/options)` 등 미완 항목 존재. 현재 uncommitted diff 는 정확히 이 plan 의 항목 B(timeoutMs 부분)를 구현 중이다.
  - 상세: `git log --follow` 로 확인한 결과 `ai-agent-tool-payload-budget-followups.md` 는 한때 `pending_plans` 에 등재돼 있었으나, 항목 A 완료 커밋("spec 마감: ... 두 spec pending_plans 정리", PR #955)에서 통째로 제거됐다. 그러나 plan 자체는 항목 B 가 남아 in-progress 로 유지 중이며, `spec-impl-evidence.md` R-5 는 `status: partial` spec 이 "미구현 surface 를 책임지는 plan" 을 `pending_plans` 에 전부 등록할 것을 의무화한다(`spec-status-lifecycle.test.ts` 가드는 pending_plans 완전 부재만 잡고, 특정 plan 미등재는 자동으로 잡지 못함). §12.16 이 그대로 "미완결 surface"(resume abortSignal 실배선 부재)를 문서화하고 있으므로 이 plan 이 다시 등재돼야 한다.
  - 제안: `pending_plans` 에 `plan/in-progress/ai-agent-tool-payload-budget-followups.md` 재등록. 항목 B 완료(PR) 시점에 다시 정리.

- **[INFO]** §12.16/코드 신규 주석의 "후속" 귀속처가 이미 종료·이동된 plan을 가리킴
  - target 위치: `spec/4-nodes/3-ai/1-ai-agent.md` §12.16 Rationale 두 번째 항목 — "사용자 cancel signal 전파는 `node-cancellation-infrastructure` 후속으로 남지만"; 및 `codebase/backend/src/nodes/core/node-handler.interface.ts` 의 `ResumableMessageOptions.signal` JSDoc — "abort 컨텍스트 도입은 `node-cancellation-infrastructure` 후속."
  - 관련 plan: `plan/complete/node-cancellation-infrastructure.md` (2026-06-28 완료·이동됨. 해당 문서 §5 자체가 "resume 경로는 abort context 부재로 **by-design 미전파**"라고 **닫힌 결정**으로 기록). 실제 이 잔여(orchestrator→state abortSignal 실배선)를 살아있는 in-progress plan 으로 추적하는 것은 `plan/in-progress/ai-agent-tool-payload-budget-followups.md` 항목 B 의 미체크 항목("resume 경로 abortSignal plumbing (orchestrator → state/options)")뿐이다. `plan/in-progress/node-cancellation-inflight-followups.md`(현재 `spec/conventions/node-cancellation.md` 의 `pending_plans` 가 가리키는 실제 in-progress plan)도 DB/Email in-flight cancel·e2e 항목만 다룰 뿐 AI resume signal 항목은 포함하지 않는다.
  - 상세: 새로 작성된 주석·spec 문구가 완료·이동된 plan 이름을 계속 인용하면, 그 plan 을 열어봐도 후속 작업 흔적이 없어(이미 by-design 닫힘) 추적이 끊긴다. `spec/conventions/node-cancellation.md` §2.3 표의 기존(미변경) 문장도 동일한 stale 인용을 이미 갖고 있어 이번 diff 가 이를 답습했다.
  - 제안: 두 인용을 "`ai-agent-tool-payload-budget-followups.md`(항목 B, resume abortSignal plumbing 잔여)"로 정정하거나, 최소한 완료 plan 이 아니라 실제 in-progress 소유자를 병기.

## 요약

현재 uncommitted 작업은 `plan/in-progress/ai-agent-tool-payload-budget-followups.md` 항목 B(resume 턴 chat 호출 defense-in-depth timeout)를 구현 중이며, timeoutMs 배선 자체는 plan 체크리스트·선행 guardrail plan 의 결정과 잘 정합한다(signal 채널만 열어두고 실제 orchestrator 배선은 의도적으로 후속으로 미룬 것도 plan 서술과 일치). 다만 새로 추가된 spec §12.16 문구가 "이 throw 는 §10 LLM_CALL_FAILED 로 귀결된다"고 단정하면서, `plan/in-progress/node-output-redesign/ai-agent.md` 에 여전히 열려 있는 CRITICAL 항목(single-turn 의 `llmService.chat` throw 가 error 포트로 라우팅되지 않는 gap — 코드로 재확인함)을 반영하지 않아 target 이 아직 해소되지 않은 선행 plan 을 가정한 서술을 하고 있다. 아울러 frontmatter `pending_plans` 가 현재 작업을 규율하는 plan(`ai-agent-tool-payload-budget-followups.md`)을 누락하고 있고, 새 주석의 "후속" 귀속처가 이미 완료·이동된 plan(`node-cancellation-infrastructure.md`)을 가리켜 실제 추적선이 끊긴다. 모두 plan/spec 문구 갱신으로 해소 가능한 수준이며, 미해결 결정을 일방적으로 우회하는 CRITICAL 충돌은 발견되지 않았다.

## 위험도
MEDIUM
