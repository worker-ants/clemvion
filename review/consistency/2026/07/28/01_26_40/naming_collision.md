### 발견사항

- **[WARNING]** naming_collision 세션의 "target 문서" 번들이 이번 PR 의 실제 diff 범위와 무관하다 — 정작 관련 파일(`4-execution-engine.md`)은 컨텍스트 예산 초과로 빠짐
  - target 신규 식별자: 해당 없음. 번들에 전문이 실린 `spec/5-system/1-auth.md`·`10-graph-rag.md`·`11-mcp-client.md` 는 이 브랜치에서 **diff-base(`origin/main`) 대비 변경이 0줄**인 정적 스냅샷이다(`git diff origin/main...HEAD --stat -- spec/5-system/` 결과 공백). 즉 "target 이 새로 도입한 식별자" 자체가 이 세 파일에는 존재하지 않는다.
  - 기존 사용처: 이번 PR 의 실제 코드 diff(`codebase/backend/src/modules/execution-engine/retry-turn.service.ts`)가 `CHANGELOG.md`(diff `+`7번 항목)에서 명시하는 SoT 는 `spec/5-system/4-execution-engine.md` §1.1/§1.2 다. 그런데 이 파일은 `naming_collision.md` 프롬프트 2075~2096행 "⚠️ 컨텍스트 예산 초과로 생략된 파일 18개" 목록에 올라 있어 **번들 자체에 포함되지 않았다**(본문이 아예 없음).
  - 상세: 근본 원인은 두 단계가 겹친 것으로 보인다. (1) `meta.json` 의 `target_path` 가 `spec/5-system/`(디렉터리) 로 잡혀 있는데, 이는 `retry-turn.service.ts` 가 매칭되는 spec frontmatter(`spec/5-system/4-execution-engine.md` 의 `code: - codebase/backend/src/modules/execution-engine/**`)를 정확히 파일 단위가 아니라 **그 파일이 속한 디렉터리 전체**로 해석한 결과로 보인다(18개 파일, 과거 관측치 929KB — `feedback_impl_done_spec_bundle_bug` 메모의 "spec 영역이 크면 컨텍스트 한계로 truncate" 사례와 동일 계열). (2) 디렉터리 파일을 `ls`/`sorted()` 로 나열하면 문자열 사전순이 되어 `1-auth.md` → `10-graph-rag.md` → `11-mcp-client.md` → `12-…` → … → `2-api-convention.md` → `3-error-handling.md` → **`4-execution-engine.md`** 순서가 된다(`1"` < `"2"` 문자열 비교) — 번호가 두 자리인 파일들이 먼저 오고, 정작 필요한 한 자리 번호 파일은 뒤로 밀린다. 컨텍스트 예산이 앞쪽 3개 파일(1-auth·10-graph-rag·11-mcp-client, 이번 PR 과 무관한 인증/그래프RAG/MCP 도메인)만 싣고 소진되어, 사전순으로 밀린 `4-execution-engine.md`(이번 PR 의 실제 SoT)가 통째로 잘려나갔다. 부수적으로 같은 생략 목록(2087·2089·2090행)에 `_selectedPort`·`$trigger`·`$env` 같은 **파일 경로가 아닌 식별자 문자열이 파일 목록에 섞여 들어가 있는** 별도의 소소한 번들링 결함도 확인된다(무엇을 근거로 이 항목들이 "생략된 파일"로 분류됐는지 불명 — 별도 리스트 생성 로직의 버그로 추정).
    한편 이번 PR 은 `plan/in-progress/retry-turn-terminal-guard.md` frontmatter 에 `spec_impact: none` 을 명시하고 있고, CHANGELOG 문구도 "spec 갱신은 `plan/in-progress/spec-update-node-cancellation-shutdown-classification.md` #7 로 위임" 이라 밝혀, 애초에 이번 PR 로 spec/5-system/ 어떤 파일도 바뀌지 않는다(실측: `git diff origin/main...HEAD --stat` 전체 결과 = `CHANGELOG.md`, `retry-turn.service.ts`, `retry-turn.service.spec.ts`, `plan/in-progress/retry-turn-terminal-guard.md` 4개뿐, spec/ 파일 0개). 그럼에도 spec 영역 대상 5종 checker(cross_spec·rationale_continuity·convention_compliance·plan_coherence·naming_collision) 가 전부 호출된 것 자체는 "spec_impact: none 주장을 신뢰하지 않고 실제로 재검증한다" 는 관점에서 legitimate 한 belt-and-suspenders 절차일 수 있다 — 문제는 그 재검증에 쓰인 번들이 정작 검증해야 할 파일을 빠뜨렸다는 점이다. 이번 건은 결과적으로 "생략되지 않았어도 결론은 같았을 것"(스캔한 4-execution-engine.md §1.1 근방엔 `RetryTurnService`/`completeRetryExecution`/`failRetryExecution` 가 이미 문서화돼 있고 `finalizeGuarded` 는 그 두 public 메서드 내부의 private 헬퍼라 목록 갱신 의무 대상이 아님, 아래 두 번째 발견사항 참조)이지만, 실제로 충돌 소지가 있는 변경이었다면 이 checker 는 그 사실을 **놓치고도 "이상 없음" 으로 보고했을 것**이다.
  - 제안: harness 유지보수 관점 — ① `--impl-done` 스코프 해석 시 code diff 가 매칭하는 spec frontmatter 파일이 식별되면 **그 파일을 최우선으로(디렉터리 전체보다 앞서) 번들에 넣도록** 우선순위를 부여. ② 디렉터리 파일 나열은 문자열 사전순 대신 파일명 선행 숫자를 정수로 파싱한 **자연 정렬(natural sort)** 사용 — `1,2,3,4,…,10,11,…` 순서라면 이번처럼 4번 파일이 뒤로 밀리지 않는다. ③ "컨텍스트 예산 초과로 생략된 파일" 목록 생성 로직에서 파일 경로가 아닌 문자열(`_selectedPort`/`$trigger`/`$env`)이 섞여 들어가는 경로를 별도로 점검. 이번 PR 자체에 대한 조치는 불필요(스코프 결정권자인 developer/PR 작성자가 아니라 consistency-check 오케스트레이터 스크립트 쪽 결함).

- **[INFO]** 실제 diff 범위(코드)에서 독립 검증한 결과 — 신규 식별자 충돌 0건
  - target 신규 식별자: `finalizeGuarded` — `RetryTurnService` 에 새로 추가된 private 메서드(`codebase/backend/src/modules/execution-engine/retry-turn.service.ts:437`). 그 외 diff 는 로컬 변수(`finalStatus`·`live`·`persisted`)와 기존 export `canTransition`(`./state/state-machine.ts:58`, 커밋 `3213a4a55` 이래 기존 함수 — 이번 PR 신규 아님) re-import 뿐이다.
  - 기존 사용처: `git grep -n "finalizeGuarded" -- codebase/` 결과 정의·호출부(3곳) 외 다른 의미의 기존 사용처 없음. 이름이 유사한 "형제" 메서드 `finalizeCancelledExecution`/`finalizeFailedExecution` 은 **다른 클래스**(`execution-engine.service.ts`)에 정의된 별개 이름이며, 주석에서도 명시적으로 "형제(sibling)" 관계로 서술돼 의도된 병렬 설계다 — 이름 자체가 겹치지 않으므로 충돌 아님. `spec/5-system/4-execution-engine.md:1651` 은 `RetryTurnService` 의 public 표면(`retryLastTurn`·`applyRetryLastTurn`·`resumeGraphAfterRetry`·`completeRetryExecution`·`failRetryExecution`)만 열거하며 `finalizeGuarded` 는 그 두 public 메서드 내부에서만 쓰이는 private 헬퍼라 이 표면 목록에 없어도 결손이 아니다.
  - 상세: 테스트 파일(`retry-turn.service.spec.ts`) diff 가 도입하는 block-scope 타입 `FinalizeSubject`(694행)도 동일 이름이 `ai-turn-orchestrator.service.spec.ts` 에 3곳 이미 존재하지만, 각각 별도 `describe` 블록에 TS block-scope 로 격리돼 있고 "private 메서드 접근용 캐스팅 타입" 이라는 동일 관용구를 재사용한 것이므로 충돌이 아니라 기존 컨벤션과의 일관성이다. 신규 API endpoint·env var·webhook/큐/SSE 이벤트명·요구사항 ID·spec 파일 경로는 diff 어디에도 추가되지 않았다(신규 파일은 `plan/in-progress/retry-turn-terminal-guard.md` 1건뿐이며 `plan/`·`plan/complete/` 어디에도 동명 파일 없음 — 경로 충돌 없음).
  - 제안: 조치 불요.

### 요약

이번 세션에 전달된 "target 문서"(`spec/5-system/1-auth.md`·`10-graph-rag.md`·`11-mcp-client.md`)는 이 PR(`retry-turn-cancel-guard`)의 diff-base(`origin/main`) 대비 변경이 전혀 없는 정적 스냅샷이며, 정작 이 PR 의 CHANGELOG 가 SoT 로 지목한 `spec/5-system/4-execution-engine.md` 는 컨텍스트 예산 초과로 번들에서 빠졌다 — 디렉터리 단위 스코프 해석과 문자열 사전순 나열(`4-`가 `10-`/`11-` 보다 뒤로 밀림)이 겹친 결과로 진단된다. 이 PR 은 `spec_impact: none` 이 명시돼 있고 실측으로도 spec/ 어떤 파일도 바뀌지 않았으므로, "target 이 도입하는 새 식별자" 자체가 spec 쪽에는 없다. 대신 저장소에 직접 접근해 실제 코드 diff(`retry-turn.service.ts`)를 검증한 결과, 유일한 신규 식별자인 private 메서드 `finalizeGuarded` 는 기존 사용처와 충돌하지 않으며(형제 메서드와 이름이 다르고, 유일 정의), 테스트 파일의 로컬 타입 재사용도 기존 컨벤션과 일치한다. 따라서 이번 PR 자체의 신규 식별자 충돌 위험은 없다고 판단하지만, 이번에 확인된 "관련 spec 파일이 컨텍스트 예산에 밀려 통째로 빠지는" 번들링 결함은 이 PR 과 무관하게 harness 자체의 결함이며, 다음에 실제로 충돌이 있는 변경을 검토할 때는 이 checker 가 그 충돌을 놓치고도 "이상 없음"으로 보고할 수 있다는 점에서 별도로 고쳐져야 한다.

### 위험도
NONE
