# RESOLUTION — `review/consistency/2026/09/06/12_28_03`

**원 결과**: BLOCK: NO · Critical 0 · WARNING 2 · 위험도 MEDIUM
**처분**: WARNING 2건은 **5차 재확인된 planner 항목**(권한 밖) · INFO 1건 수정

## WARNING 1·2 — 5차 재확인, 코드 변경 없음

신규 검출 2축의 `code:` 미등재 · `User` 7컬럼 노출 금지 규범 부재. checker 가 명시적으로
*"별도 신규 plan 항목 생성은 불요 — 다음 planner 턴에서 그대로 집행"* 이라고 적었다.

등재문은 이미 checker 의 처방을 전부 담고 있다 — 대상 문장 두 개를 표로 명시했고,
*"새 개수를 적어 넣지 말고 나열하라"* 도 못 박아 뒀다. 이번 checker 도 독립적으로 같은
처방(*"개수 고정 문구는 나열형으로 교체"*)을 냈고, 그 근거로 *"이미 2회 개수-드리프트
반복 이력"* 을 들었는데 그 이력을 남긴 것이 바로 그 등재문이다.

**developer 가 할 수 있는 일은 끝났다.** 5회 반복은 우선순위 신호이지 미조치가 아니다.

## INFO 1 — 헤더의 spec 참조가 엉뚱한 절을 가리켰다 → **수정**

`workspace-rbac.e2e-spec.ts` 헤더가 *"`1-auth.md §1.3` 의 RBAC 계약"* 이라 적는데 실측하면:

| 절 | 실제 내용 |
|---|---|
| `§1.3` | 셀프 호스팅 추가 인증 **(미구현 · Planned)** |
| `§3` | **인가 (Authorization)** — `§3.1 RBAC 역할` |

**전혀 다른 절을 가리키고 있었다.** checker 는 *"이번 diff 밖, 다음에 편집할 때"* 로
분류했지만 **이 브랜치가 이미 그 파일을 편집한다**(`J.` 블록 이동). 그래서 지금 맞췄다 —
`review-citations.md §4` 의 *"그 자리를 다음에 건드릴 때 함께 맞춘다"* 가 정확히 이
상황을 말한다.

## 검증

이 정정은 주석 한 줄이다. 같은 커밋에 들어가는 코드 리뷰 처분과 함께 전 단계를 돌린다.
