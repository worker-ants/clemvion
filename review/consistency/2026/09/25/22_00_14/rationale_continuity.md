# Rationale 연속성 검토 — 워크플로우 어시스턴트의 통합 후보도 §8 을 따른다 (--spec)

## 발견사항

- **[INFO] ED-AI-39 계약 변경에 날짜 각주형 Rationale 갱신이 없다 — 인라인 링크로만 근거를 실었다**
  - target 위치: target 문서 "## 변경안 (3) Rationale ED-AI-39 «구현자가 기억해야 할 계약» 1번"
  - 과거 결정 출처: `spec/3-workflow-editor/4-ai-assistant.md` `## Rationale` → "Workflow Assistant — Candidate Picker 정책 결정 (ED-AI-39)" §"구현자가 기억해야 할 계약" 1번(라인 1414 부근, "워크스페이스 스코프로 쿼리해 `candidates` 를 채운다")
  - 상세: 직전 라운드 `review/consistency/2026/09/25/21_49_24/rationale_continuity.md` WARNING 2 는 "최소한 ED-AI-39 Rationale 에 '2026-09-25: 워크스페이스 스코프 쿼리는 이제 `4-integration.md §8` 의 소유자 판정을 얹는다' 각주를 추가한다" 를 제안했다. target 은 계약 문장 자체에 괄호로 "(Integration 은 거기에 더해 요청자에게 보이는 것만 — 남의 personal 제외, [통합 §8])" 를 끼워 넣어 실질적으로 같은 정보를 전달하지만, 같은 문서의 다른 항목들(예: "SMTP 연결 테스트를 `verify()` 로 구현" 절의 "(2026-09-19 갱신: …)" 인라인 날짜 주석, "자동 갱신 통합을 attention 술어에서 제외" 절의 별도 "**과거 결정과의 호환**" 단락)이 취하는 "날짜 + 이전 원칙과의 명시적 화해" 형식과는 결이 다르다 — 계약 문장에 조건절을 얹는 방식이라 "언제부터 왜 좁아졌는지"가 계약 자체에 섞여 별도로 추적하기 약간 어렵다.
  - 제안: 필수는 아니나, ED-AI-39 절 어딘가(또는 계약 항목 바로 아래)에 "2026-09-25: Integration 후보 조회는 이제 `4-integration.md §8` 의 personal 소유자 판정(«조회» 행)을 얹는다" 한 줄을 이 코드베이스의 기존 패턴과 맞춰 명시하면 향후 검색·추적이 쉬워진다. 차단 사유는 아니다.

## 요약

target 의 세 변경(§4.2 `list_integrations` 행, §4.3.1 `integration-selector`/`mcp-server-selector` 필터 행, ED-AI-39 "구현자가 기억해야 할 계약" 1번)은 모두 `spec/2-navigation/4-integration.md §8` 이 이미 명시한 "표에 없는 조회성 경로는 «조회» 행을 따른다 — … 워크플로우 어시스턴트의 통합 목록 도구와 노드 후보 제시" 문장(커밋 `f47069564` 로 이미 spec 에 반영됨, 실측 확인)을 그대로 투영한 것이다. §8 Rationale "Personal 통합 소유자 강제"의 기각된 대안(쓰기만 먼저 / §8 전부 한 번에 / 새 에러코드 / Organization Editor 유지) 중 어느 것도 재도입하지 않고, "«본인» = `created_by`, 역할 우위 없음" 원칙과 "남의 personal 은 없는 통합과 같다" 는 이미 확립된 문구를 그대로 재사용한다. 또한 §8 의 "아직 강제되지 않는 것"(워크플로우 노드에서의 런타임 사용 — 실행 엔진/저장 단의 시행은 후속)과 이번 변경(에디터 후보 *조회* 단의 시행) 은 서로 다른 표면이라 후속 defer 결정과 충돌하지 않는다. 세 자리(도구 표·후보 표·구현 계약)를 모두 고쳐 "한 곳만 고치면 나머지가 워크스페이스 전체를 계속 약속한다" 는 draft 자신의 우려도 해소한다. 유일한 흠은 계약 갱신에 이 코드베이스가 관례적으로 쓰는 날짜 각주 형식이 아니라 인라인 조건절로만 근거를 실었다는 점인데, 이는 INFO 수준의 스타일 제안이며 Rationale 연속성 위반은 아니다.

## 위험도

LOW
