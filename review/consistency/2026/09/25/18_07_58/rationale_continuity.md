# Rationale 연속성 검토 — `plan/in-progress/spec-draft-workspace-path-guard-oracle-census.md`

## 발견사항

- **[WARNING]** "HTTP 밖 호출자는 없다" 불변식을 재검증 없이 대상 집합에 3번째 메서드를 편입
  - target 위치: `plan/in-progress/spec-draft-workspace-path-guard-oracle-census.md` 라인 45-48 (**후 (2)** — "오라클이 있던 ~~두~~ 세 메서드도 인가를 앞으로 옮긴다 … 트랜잭션 안에서 락을 잡고 재검사하는 자리(`leaveWorkspace` · `transferOwnership`)는 …")
  - 과거 결정 출처: `spec/data-flow/12-workspace.md` §Rationale «경로 파라미터 워크스페이스도 가드가 본다» "**서비스 계층 검사는 남는다**" 단락의 바로 앞 문장 — "이 메서드들의 HTTP 밖 호출자는 없다(2026-09-25 실측: 내부 위임 `removeMember → leaveWorkspace` 하나 — 가드를 거친 HTTP 요청 안이다)."
  - 상세: 원문은 "이 메서드들"(오라클이 있던 `leaveWorkspace` · `addMemberByEmail` 2곳)에 대해 "HTTP 밖 호출자가 없다"는 전수 실측(내부 위임은 `removeMember → leaveWorkspace` 하나뿐)을 근거로, "서비스 검사가 막는 것은 가드 인식이 깨진 경우뿐이다"라는 결론을 세웠다. target 은 바로 다음 문장에 `transferOwnership` 을 편입시켜 같은 "두 번째 선" 집합을 2→3 으로 넓히지만, 그 직전 문장의 "내부 위임 하나 — 가드를 거친 HTTP 요청 안" 이라는 전수 실측 문장 자체는 target 의 편집 범위(«전 (1)/(2)» 두 문장)에 포함되지 않아 그대로 남는다. 이 문장은 원래 2-메서드 census 기준이므로 `transferOwnership` 에 HTTP 외부(다른 서비스 메서드의 내부 위임 등) 호출자가 없다는 사실이 확인됐는지 target 에 근거가 없다. 만약 그런 호출자가 있다면 "가드 인식이 깨질 때만 서비스 검사가 의미를 가진다"는 이 절의 핵심 불변식이 3번째 메서드에는 성립하지 않을 수 있다 — Rationale 에 기록된 암묵적 가정(모든 대상 메서드가 HTTP 경로로만 도달한다)을 재확인하지 않은 채 집합을 확장한 셈이다.
  - 제안: `transferOwnership` 에 대해서도 내부 위임/호출자 전수 확인을 수행하고, "이 메서드들의 HTTP 밖 호출자는 없다" 문장을 "세 메서드" 기준으로 갱신(또는 여전히 유효함을 실측과 함께 명시)한다. 최소한 그 문장이 아직 2-메서드 기준임을 각주로 밝혀 범위 혼동을 없앤다.

- **[INFO]** "같은 절의 각주 관행을 따른다"는 주장과 실제 편집 방식(인라인 취소선)의 형식 불일치
  - target 위치: `plan/in-progress/spec-draft-workspace-path-guard-oracle-census.md` 라인 33-39(후 (1)), 45-48(후 (2)), 52-53(Rationale 첫 불릿)
  - 과거 결정 출처: `spec/data-flow/12-workspace.md` §Rationale 안의 기존 정정 각주 두 건 — «(2026-09-25 정정 — 워크스페이스 `:id` 에 한해)»(§"`X-Workspace-Id` 헤더 vs `:id` 경로 파라미터" 절), «캐너리 지목 정정 (2026-08-09)»(같은 절)
  - 상세: target 은 "왜 원문을 지우지 않나: … 같은 절이 이미 쓰는 «(2026-09-25 정정)» 각주 관행을 따른다"고 명시하지만, 실제 두 선례는 모두 **원문 문단을 그대로 두고 그 아래 별도 blockquote 로 "(정정)" 주석을 덧붙이는 형식**이다(원문 문장 내부는 손대지 않음). target 이 실제로 적용한 방식은 원문 문장 **내부**에 단어를 인라인 취소선으로 표시하고 옆에 대체어를 병기하는, 이 문서에서 처음 쓰이는 제3의 편집 관행이다. 목적(원문 보존 + 정정 가시화)은 동일하게 달성하지만, "관행을 따른다"는 서술은 선례의 실제 형식과 정확히 일치하지 않는다.
  - 제안: 서술을 "같은 절의 정정 원칙(원문 보존)을 참고해 인라인 취소선으로 표기한다" 정도로 조정하거나, 기존 두 선례처럼 원문은 그대로 두고 아래에 별도 blockquote 정정 주석을 추가하는 형식으로 통일한다.

## 요약

target 은 `spec/data-flow/12-workspace.md` §Rationale «경로 파라미터 워크스페이스도 가드가 본다» 의 두 문장(«두 메서드» → «세 메서드»)만 정밀하게 겨냥해 수정하며, 그 근거(구현 후 `/ai-review` 4라운드가 찾은 `transferOwnership`, 실제 커밋 `1f616ef05`, 6케이스 RED→GREEN)를 실측으로 제시하고 결정 자체(가드 확장 + 거부 코드, "오라클 자리는 서비스도 인가 선행" 원칙)는 번복하지 않는다고 명시적으로 밝혀 연속성 관점의 핵심 위험(기각된 대안 재도입·무근거 번복)은 없다. 특히 "기각된 대안 (1) 오라클 2곳만 서비스에서 고친다"를 재검토하며 "셋이었어도 같은 이유로 기각됐을 선택지"라고 반사실적으로 짚은 부분은, 이미 전체 15개 경로를 덮는 `@WorkspaceParam` 구조적 가드가 1차 방어이고 서비스 계층은 '두 번째 선'일 뿐이라는 기존 구조를 정확히 이해하고 있음을 보여준다. 다만 두 번째 선(서비스 계층 검사)의 대상 집합을 2→3으로 넓히면서, 그 집합 전체에 대한 "HTTP 밖 호출자 없음"이라는 인접 불변식 문장은 갱신·재검증하지 않고 그대로 두어 암묵적 가정 충돌 가능성이 남았고(WARNING), "각주 관행을 따른다"는 자체 서술도 실제 선례 형식과는 다르다(INFO). 두 항목 모두 CRITICAL 수준은 아니며 문서 정합 보완 수준이다.

## 위험도
LOW
