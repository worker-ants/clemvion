# RESOLUTION — `--impl-done spec/conventions/` 라운드 6 (`review/consistency/2026/09/13/21_19_52`)

**BLOCK: NO** · Critical 0 · WARNING 4 · INFO 3 · 위험도 LOW.
5개 checker 전원 전문 제출. `[CRITICAL]` 마커 0건.

WARNING 4건 중 **둘은 6라운드 연속 planner 등재분**이고, **둘은 내 plan 위생**이다.

## WARNING#3 — 같은 문서가 두 번 자백한 실패를 **세 번째로** 밟았다 (고침)

라운드 1 에 등재한 항목이 spec **6파일**을 겨냥하는데, 그중 **5개가 이 문서의
`spec_impact` frontmatter 에서 빠져** 있었다. 목록에 있던 것은
`spec/5-system/4-execution-engine.md` 하나뿐이다.

**원인이 정확히 그 자백들과 같다** — 그 한 개가 이미 있어서 *"있다"* 로 읽었고, **항목의
대상 파일 전수를 목록과 대조하지 않았다.** frontmatter 주석 두 개가
*"내 검증 스크립트가 이걸 놓쳤다"*·*"넓히자 드러났다"* 로 같은 실패를 적어 두고 있다.

5경로를 추가하고 **절차를 주석으로 박았다** — *"항목을 등재할 때 그 항목의 «대상 파일
전수» 를 이 목록과 대조한다."* 그리고 처분 스크립트에 **전수 대조 assert** 를 넣었다
(신규 항목 대상 6파일 ∖ `spec_impact` = **0건** 확인).

> 방치하면 다음 `--spec`·`--impl-done` 라운드의 번들 스코프에서 **조용히 누락**된다 —
> 이 문서가 이미 그 비용을 두 번 치렀다.

## WARNING#4 — 같은 절을 겨냥하는 넷째 항목이 됐다 (고침)

이 문서 위쪽에 *"`3-error-handling.md §1` 을 겨냥하는 plan 이 셋이다 — 한 턴에 묶어라"*
합의가 있는데, 내가 등재한 §1.4 항목이 그것을 인용하지 않고 **독립 택일로** 들어갔다.

상호 링크하고 *"이 항목이 넷째"* 임을 명시했다. 인용이 없으면 planner 가 §1 하위구조를
**네 번 따로** 건드리게 된다.

## WARNING#1·#2 — planner 등재분 (조치 불요)

spec 6파일의 `CONTAINER_*` 서술 + §1.4 의 «메시지 접두 / 앵커 없음» 미구분.
**6라운드 연속 상태 불변**이고 checker 가 *"재등록 불요, 이 PR 비차단"* 으로 판정했다.

## INFO — 조치 불요

| # | 항목 | 사유 |
|---|---|---|
| 1 | `PROJECT.md:300` SoT 꼬리표 | 선재(`origin/main` 시점부터) · 등재분 |
| 2 | 2축 하네스의 `spec/conventions/` 문서 부재 | 선재 · 3라운드 연속 동일 처분 |
| 3 | `#1330` 원칙이 spec `## Rationale` 밖 | checker 가 *"코드 주석에 사유 명시 — 은폐형 번복 아님"* 으로 확인 |
