# RESOLUTION — `--impl-done` 라운드 5 (`review/consistency/2026/09/13/12_01_01`)

**BLOCK: NO · Critical 0 · WARNING 3 · INFO 3.** 라운드 4 의 CRITICAL 정정 확인
(`naming_collision`: *"직전 라운드 CRITICAL(오귀속)은 정정 확인"*).

## WARNING #1 — 내 처분 표가 거짓이었다 (실측으로 반증됨)

`plan_coherence` 가 `git show`+grep 으로 **§J 처분 표의 "등재" 가 실제로는 트래커에 없다**는
것을 잡았다. 같은 라운드의 `cross_spec` 은 *"이미 등재됨"* 으로 **잘못 판단**했고 summary 가
실측한 쪽을 채택했다 — 두 checker 가 갈렸고 갈린 쪽이 옳았다.

**직접 grep 해 확인: 0건. 체커가 맞다.** 실제로 등재했다.

### 이 PR 에서 **세 번째**다

| # | 라운드 | 어떻게 좁았나 |
|---|---|---|
| 1 | 2 | 등재 문구가 가드 **하나만** 적었다 |
| 2 | 3 | §2.1 **산문 표만** 겨냥하고 같은 파일 frontmatter 를 빠뜨렸다 |
| 3 | 4 | **아예 안 썼는데 "등재" 라고 적었다** |

셋 다 형태가 같다 — **처분 표를 「의도」로 쓰고 「결과」로 확인하지 않았다.** 이것은
`plan 체크박스 = 실제 상태` 규칙의 한 층 위 버전이다: 체크박스는 *내* 작업 상태를, 처분 표는
**다른 파일의 상태**를 주장한다. 후자가 더 쉽게 거짓이 된다(그 파일을 안 열어도 문장은 써진다).

### 구조적 처방 — 산문 규율이 세 번 실패했으니 코드로 옮긴다

1. 등재 스크립트가 쓴 **뒤에 그 파일을 다시 읽어** `assert` 로 검증한다. 라운드 5 등재가
   그 형태다(`5-makeshop.md §6`·`4-cafe24.md §6` 사후 grep 통과).
2. **이 PR 의 등재 주장 11건을 전수 감사**했다 — 각 항목을 트래커 본문의 고유 문구로 대조.
   **실재 11 · 부재 0.** §6 건이 유일한 거짓이었고 지금 닫혔다.

### 등재 내용 자체도 한 겹 더 팠다

*"§6 표에 한 줄 더하면 된다"* 가 아니다 — `#1330` 실측대로 그 이름은 **`.code` 로 방출되지
않으므로**, 그냥 등재하면 spec 이 거짓이 된다. (A) 메시지 접두임을 명시해 등재, 또는
(B) handler 를 `IntegrationError` 로 바꾼 뒤 등재. **자매 handler 항목과 한 턴에** 처리해야
spec 과 코드가 갈리지 않는다는 조건을 등재 문구에 적었다.

## WARNING #2·#3 — 조치 불요 (등재 확인됨)

| # | 내용 | 확인 |
|---|---|---|
| 2 | `testConnection` 실패 shape 이 `7-llm-client.md §8.3`·`6-config.md` 미문서 (§5.4 키-생략 사유 의무) | 전수 감사에서 **등재 확인** |
| 3 | `PROJECT.md` 가 신규 가드의 SoT 로 `user-guide-evidence.md §2` 를 지목했는데 그 절이 아직 미갱신 — dangling SoT | 전수 감사에서 **등재 확인**(§2 카운트 3→5 · §2.1 관계표 · frontmatter 3파일) |

> #3 의 "가드 3종" 과 내 등재의 "가드 2건" 은 **모순이 아니다** — 신규 *가드*는 2개
> (`*-existence` · `*-parity`), 신규 *파일*은 3개(순수 스캐너 `guide-error-code-scan.ts` 포함).
> 등재 문구가 그 둘을 갈라 적고 있다.

## INFO

| # | 처분 |
|---|---|
| 1 | 명명 원칙 Rationale 명문화 — 4라운드 연속 승계, 유실 없이 편입 확인 |
| 2 | `code`/`message` 가 에러 봉투와 이름은 같고 스코프가 다름 — 형제 선례 재사용, 신규 충돌 아님 |
| 3 | (§6 누락은 WARNING#1 로 통합) |

## 검증

`run-test-all.sh` 5회째 ALL PASS · ratchet 둘 baseline · 등재 전수 감사 11/11 실재.
