# RESOLUTION — `--impl-done spec/conventions/` 라운드 9 (`review/consistency/2026/09/13/22_38_43`)

**BLOCK: NO** · Critical 0 · WARNING 3 · INFO 3 · 위험도 LOW.
5개 checker 전원 전문 제출. `[CRITICAL]` 마커 실측 **0건** — 하향 없음.

`convention_compliance` 가 **NONE** 이고 라운드 8 의 SoT 인용 범위 지적이 **실제로
해소됐음**을 확인했다. `naming_collision` 도 NONE — 신규 식별자 2개(`SOURCE_ROOTS` ·
`skipBuildDirs`) 포함 누적 15개 전수 재검증, 충돌 0건.

## WARNING#3 — 같은 커밋이 자기 줄 인용을 깼다 (고침)

`guide-identifier-scan.ts` 가 트래커를 `:3407` 로 가리켰는데, **같은 커밋**이 그 트래커
위쪽(형제 항목의 실측 표)에 20여 줄을 넣어 그 항목이 **3421** 로 내려갔다. 쓸 때 참이었고
커밋될 때 거짓이 됐다.

> 이 저장소가 이미 적어 둔 규칙 그대로다 — ***"내가 편집하는 파일을 줄 번호로 인용하지
> 마라."*** 알고 있는 규칙을 **같은 커밋 안에서** 어겼다.

**checker 의 처방(`:3494`)은 틀렸다** — 3494 는 §1.4 «앵커 없는 코드» 를 다루는 **다른
항목**의 표제줄이고, 내가 가리킨 항목은 3421 이다. 게다가 번호로 고치면 같은 클래스를 한 번
더 밟는다. **앵커 문구**로 바꿨다: *"가이드 에러 코드 가드가 «존재» 만 보고 «방출» 을 안
본다"*.

**클래스로 전수 판정**했다 — `review/` 밖 브랜치 편집 파일 8개에서 «편집 대상을 줄 번호로
가리키는» 인용 5건:

| 인용 | 판정 | 처분 |
|---|---|---|
| `scan.ts` → 트래커 `:3407` | **내가 깼다**(같은 커밋) | 앵커 문구 |
| plan §L → `guide-identifier-scan.ts:77` | **내가 밀었다**(헤더 +21줄) | 번호 제거 |
| 트래커 → `PROJECT.md:40-41` | **선행 브랜치**(이 브랜치 시프트 0) | 앵커 문구 |
| 트래커 → `PROJECT.md:315` | **선행 브랜치**(실제 331) | 앵커 문구 |
| plan §A → `PROJECT.md:300` | **유효**(diff 는 300행 제자리 1줄) | 유지 |

> **선행 2건은 문장까지 이동했다.** `PROJECT.md:40-41` 이 인용하던 *"ratchet 은 `*.ts(x)`
> 변경 시 별도 필수"* 는 지금 그 파일에 **없다**(grep 0) — 2026-09-08 에 두 ratchet 이
> `build` 단계 **안으로** 들어갔기 때문이다. 그 항목은 `[x]` 이고 서술이 등재 시점의
> 기록이므로, 시제를 *"등재 시점의 …"* 로 명시하고 번호를 뺐다.

## WARNING#2 — 이미 등재돼 있다 (재등재 안 함)

checker 가 *"«허용목록 없음» 원칙의 2차 번복(`GUIDE_NON_EMITTED_VOCABULARY`)이 spec
`## Rationale` 에 착지한 적 없다"* 를 새 등재 대상으로 제안했다. **실측하니 트래커에 이미
있다** — `user-guide-evidence.md §2.1` 항목이

- 1차(`GUIDE_EXTERNAL_VOCABULARY`, **존재** 축 · *"기준집합에 없을 것"*)와
  2차(`GUIDE_NON_EMITTED_VOCABULARY`, **발행** 축 · *"기준집합에 있을 것"*)를 **이름으로 지목**하고,
- *"제약이 정반대라 합칠 수 없다"* 는 구조 요구까지 적고,
- *"그 근거가 지금 plan·코드 주석에만 있고 spec `## Rationale` 에는 없다 — 표·frontmatter·
  Rationale 을 **한 턴에**"* 로 닫는다.

checker 가 제안한 문장과 사실상 동일하다. 중복 등재하지 않는다.

## WARNING#1 — planner 등재분 (조치 불요)

spec 6파일의 `CONTAINER_*` 서술. **9라운드 연속 불변**이고 checker 스스로 *"신규 조치 불요,
이번 developer PR 은 조치 불요"* 로 판정했다.

## INFO 3건 — 조치 불요

| # | 항목 | 처분 |
|---|---|---|
| 1 | *"(라운드 9 정정)"* 라운드 라벨 오기 | **`/ai-review` W1 과 동일 사안** — 세 자리를 8 로 맞춤 |
| 2 | 인접 §1.4 항목이 W1 의 6파일 방향과 얽힘 | 트래커가 이미 *"함께 볼 것"* 명시 |
| 3 | *"카탈로그는 요구조건 아니라 탈출구"* 가 §1.4 note 와 정합 | **양성 확인** |
