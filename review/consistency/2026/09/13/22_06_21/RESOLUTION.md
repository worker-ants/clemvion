# RESOLUTION — `--impl-done spec/conventions/` 라운드 8 (`review/consistency/2026/09/13/22_06_21`)

**BLOCK: NO** · Critical 0 · WARNING 4 · INFO 5 · 위험도 LOW.
5개 checker 전원 전문 제출. `[CRITICAL]` 마커 **0건**(실측) — 하향 없음.

## WARNING#4 — SoT 인용 범위가 문서의 자기 선언보다 넓다 (고침)

`guide-identifier-scan.ts` 헤더가 `error-codes.md` 를 *"(코드 명명·**발행**)"* 으로
인용했는데, 그 문서 Overview 는 *"명명·안정성 규율**만** 정의한다"* 고 스스로 좁히고
유일 소유를 ①의미 기반 명명 ②rename 안정성 ③historical-artifact 예외 레지스트리 셋으로
열거한다. *"발행"* 은 §4(내부 전용 분류 코드)에서만 좁게 다룬다.

**`/ai-review` 의 documentation·scope WARNING 과 합쳐야 결론이 나온다.** 저쪽은 같은
헤더에 *"어디에도 안 붙는 조각이 남았다"* 를 지적했는데, 그 조각의 표기가
`(코드 명명·**은퇴 이력**)` — ①②③ 과 정확히 맞는다.

| 리뷰어 | 단독 결론 |
|---|---|
| documentation·scope | *"조각을 지워라"* |
| convention_compliance (여기) | *"범위를 좁혀라"* |
| **합치면** | **지운 쪽이 옳았다 — 되돌려라** |

라운드 7 이 옛 두 줄 중 첫 줄만 갈아 끼우면서 **맞는 표기를 덮고 틀린 표기를 새로 썼다**
(`git show HEAD~2` 로 원형 확인). 조각을 지우고 위 줄을
`(코드 명명·안정성·은퇴 이력)` 으로 되돌렸다.

## WARNING#3 — 형제 트래커 항목의 실측이 낡았다 (고침)

`spec-draft-nullable-notation-followups.md:3332` 가 리팩터 착수 조건을
*"361줄 = 주석 260 · 코드 84"* 위에 걸어 뒀는데, **그 숫자를 낡게 만든 것이 그 항목을
등재한 배치 자신**이다. 3시점 표로 갱신했다:

| 시점 | 총 | 주석 | 코드 | 빈줄 | 비율 |
|---|---|---|---|---|---|
| 등재 시(라운드 3) | 361 | 260 | 84 | 17 | 72% |
| 라운드 8 checker 실측 | 601 | 418 | 156 | 27 | 70% |
| 라운드 9 커밋 시점 | **622** | **439** | **156** | **27** | **71%** |

코드 +72 인데 주석 +179 — **비율은 거의 안 움직이고 절대량이 1.7배**다. 착수 조건을
«비율» 로 걸었으면 영영 발화하지 않았을 것이라 조건은 «문단마다 고정 테스트가 있는가»
로 두고, 표는 재개 시 대조용으로 남긴다.

> checker 가 *"이번 커밋 범위에 묶기 권장"* 이라 했고 `plan/**` 은 코드 시계를 늙히지
> 않으므로 이번 턴에 넣었다.

## WARNING#1·#2 — planner 등재분 (조치 불요)

spec 6파일의 `CONTAINER_*` 서술 + `3-error-handling.md §1.4` 앵커 축 미구분.
**8라운드 연속 불변**이고 checker 스스로 *"이미 정확히 등재·역참조됨, 재등록 불요,
이 PR 비차단"* 으로 판정했다.

## INFO 5건 — 조치 불요

| # | 항목 | 사유 |
|---|---|---|
| 1 | SoT 인용 정정은 사실 정정이지 결정 번복 아님 | **양성 확인** (grep 0건으로 미착지 입증) |
| 2 | 두 예외축 분리는 `user-guide-evidence.md R-2` 재적용 | **양성 확인** |
| 3 | `isMessagePrefixOnly` vs `R-CCA-9` 는 층이 다름 | 세 라운드 연속 같은 양성 판정. 헤더 한 줄 추가 제안은 **비필수**로 판단 — 주석이 이미 71% 다 |
| 4 | R7→R8 diff 에 `spec/**` 무변경 확인 | **양성 확인** |
| 5 | 신규 top-level 식별자 0개 · 누적 13개 충돌 0 | **양성 확인** |
