# RESOLUTION — `--impl-done spec/conventions/` 라운드 10 (`review/consistency/2026/09/13/23_04_09`)

**BLOCK: NO** · Critical 0 · WARNING 3 · INFO 2 · 위험도 LOW.
5개 checker 전원 전문 제출. `[CRITICAL]` 마커 실측 **0건** — 하향 없음.
`convention_compliance`·`naming_collision` **NONE**, 라운드 9 의 라운드-라벨 오기 해소 확인.

## WARNING#3 — 내 «전수» 가 형태를 추측하고 있었다 (고침)

라운드 9 가 *"«편집 대상을 줄 번호로 가리키는» 인용 전수 판정"* 이라고 적었는데, 술어는
`파일.md:NNN` **콜론 형태만** 훑는 정규식이었다. 맨 숫자(`트래커 3404` · `(3394)`)는
보이지 않았다. **주장이 측정보다 넓었다.**

**방법을 바꿨다** — 형태를 추측하지 않고 **숫자를 전수로** 훑는다. 문서의 «100 이상 정수»
38건을 전부 뽑아 **주어와 함께** 판정했다:

| 자리 | 쓴 값 | 가리킨 항목 | 커밋 시점 |
|---|---|---|---|
| §C ×2 | `3208` | *"`§1` 카탈로그가 통합·LLM 계열을 통째로 누락"* | 3220 |
| §D-2 | `3404` | *"`CONTAINER_*` 도 방출 코드가 아니다"* | 3459 |
| §I | `3394` | *"가드가 «존재» 만 보고 «방출» 을 안 본다"* | 3422 |
| §I | `3404` | 위 자매 | 3459 |
| 트래커 자기참조 ×2 | `3208` | 첫 행과 같은 항목 | 3220 |

> **checker 는 3건을 짚었고 `3208` 2건은 못 봤다.** 그리고 라운드 9 가 «드리프트를
> 설명하려고» 적은 `3421` 조차 **같은 커밋이 3422 로 밀었다** — 현상을 기록한 문장이 그
> 현상에 걸렸다.

전부 앵커 문구로 바꾸고, **§M·§N(=깨진 인용 자체를 기록하는 절) 밖의 살아있는 인용이
1건뿐이고 그것이 실측 유효**(`PROJECT.md:300` — 이 브랜치의 `PROJECT.md` diff 는 300행
제자리 1줄, 시프트 0)임을 사후 단언으로 고정했다. 트래커의 bare 3xxx 는 **0건**.

## WARNING#1·#2 — planner 등재분 (조치 불요)

spec 6파일의 `CONTAINER_*` 서술 + §1.4 카탈로그 포함/제외 기준 미정의. **10라운드 연속
불변**이고 checker 스스로 *"신규 아님, 재확인"* 으로 판정했다. 두 항목은 트래커에 *"한 턴에
묶어라"* 합의와 함께 등재돼 있다.

## INFO 2건

| # | 항목 | 처분 |
|---|---|---|
| 1 | 가드 패밀리가 `user-guide-evidence.md §2` 표에 미등재 | 등재분. 코드 쪽은 라운드 7 이 *"아직 그 표에 없다"* 를 정직하게 적는 것으로 닫았다 |
| 2 | «허용목록 없음» 1·2차 번복의 Rationale 착지가 미래 과제 | 등재분 — 라운드 9 에서 **이미 트래커에 있음을 실측**했고, 이번 checker 도 같은 항목을 가리킨다 |

## 라운드 10 이 마지막인 이유

이 라운드의 처분이 **전부 `plan/**`** 이다. `codebase/**` 수정이 0 이므로 코드 시계가
올라가지 않고, 선언한 완료 기준(*"마지막 라운드가 `codebase/**` 수정 0 으로 끝날 것"*)을
충족한다. `/ai-review` 의 유일한 `codebase/**` 요구(W1 `collectMatches` 캡처그룹)는
**결함이 아닌 개선**이고 두 라운드가 등급을 달리 매겨, 고치는 대신 트래커에 등재했다.
