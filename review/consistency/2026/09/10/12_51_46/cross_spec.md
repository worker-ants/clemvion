STATUS: success

# Cross-Spec 일관성 검토 — `spec-draft-api-convention-104-reconnect.md` (§10.4 재연결 교체)

## 검증 방법

target draft 가 인용하는 다섯 개 판단 각각을 SoT(`6-websocket-protocol.md`) 본문·Rationale·
`14-external-interaction-api.md` §R7 그리고 `2-api-convention.md` 자체 히스토리(`git log -S`)로
직접 재검증했다. draft 의 "SoT 독법"을 전제로 삼지 않고, 인용된 절을 전부 열어 원문과 대조했다.

## 발견사항

- **[INFO]** claim ① (백오프 수치가 SoT 가 부인한 초안값) — **정확함, git blame 으로 완전 확증**
  - target 위치: 착수 전 재판정 §① / E-1 교체안
  - 충돌 대상: `spec/5-system/6-websocket-protocol.md §6.1` (현재), 그리고 같은 파일의 커밋 이력
  - 상세: `git log -p --follow -S "1/2/4/8/16s" -- spec/5-system/6-websocket-protocol.md` 로 확인한
    결과, 2026-06-03 audit 커밋(`cfffc1355`) 이전 §6.1 표의 "백오프 전략" 셀은 정확히
    **"지수 백오프 (1s, 2s, 4s, 8s, 16s, 최대 30s)"** 였다. 그 커밋이 이 값을 Socket.IO 내장 알고리즘
    서술로 교체하면서, 옛 값을 "spec 초안의 '1/2/4/8/16s'"라는 각주로 남겼다. `2-api-convention.md`
    §10.4 의 `(1s, 2s, 4s, ... 최대 30s)` 는 이 옛 값을 "..." 로 가운데(8s, 16s)만 생략한 **동일
    시퀀스**다 — 부분 인용이 아니라 리터럴 일치. draft 의 claim ①은 추정이 아니라 실측 확인됨.
  - 제안: (반영 불필요 — 이미 correct) 다만 이 git 근거를 draft 의 재판정 섹션에 한 줄 인용해 두면
    다음 사람이 재검증 안 해도 되는 이득이 있다 (선택사항, 필수 아님).

- **[INFO]** claim ①의 대체안(숫자 삭제 + §6.1 앵커) — **문서 관례와 정합, 정보 손실 없음**
  - target 위치: E-1 첫 번째 불릿
  - 충돌 대상: 없음 — `2-api-convention.md` §10 자체의 기존 위임 패턴(§10.3 의 실행 제어 행이 이미
    §4.2 링크, §11 전체가 `12-webhook.md` 위임)과 일치
  - 상세: "구체 파라미터는 구현이 SoT" 라는 판단은 이 문서가 §10 전체에서 이미 쓰고 있는 전략이다.
    숫자를 지우고 "지수 백오프 + 지터로 무제한 재시도" 라는 정성적 특성만 남긴 뒤 앵커로 위임하는
    것은 독자가 잃는 정보가 없다 — 오히려 SoT 가 스스로 부인한 숫자를 요약 문서가 계속 옮겨 적는
    상태(현재)보다 안전하다.
  - 제안: 없음 — 현행 유지 권장.

- **[INFO]** claim ② (native WS 에 last-event-ID 재전송 없음) 및 대체 문구의 "같은 seq 공간만
  공유·버퍼는 공유 안 함" — **과장 아님, SoT Rationale 원문의 거의 축자적 인용**
  - target 위치: E-1 두 번째 불릿
  - 충돌 대상: `6-websocket-protocol.md §6.2` 본문, 같은 문서 `## Rationale` "재연결 복구 — native
    WS 는 snapshot, seq 버퍼-replay 는 SSE 전송" 항목, §4.7 핵심 규약, `14-external-interaction-api.md
    §R7`
  - 상세: §6.2 는 "native WebSocket 복구 모델 — `execution.snapshot`" 이라는 소제목으로 시작하고
    "seq 기반 정밀 재전송은 SSE 전송 표면의 메커니즘이다" 라고 명시한다. 그 Rationale 항목은
    "**두 전송은 같은 seq 공간만 공유하고 버퍼 인스턴스는 공유하지 않는다**" 라고 적고 있고, §4.7 도
    "두 전송이 같은 seq 공간을 공유할 뿐, 버퍼 자체는 공유하지 않는다" 라고 반복한다. draft 의
    "두 전송은 같은 `seq` 공간만 공유하고 버퍼는 공유하지 않는다" 는 이 SoT 문장들의 재진술이지
    over-statement 가 아니다. `§R7` 은 오히려 seq 공간이 WS 이벤트 envelope(§2.2)·SSE `id:`·Notification
    `seq` **세 곳 모두**에 동일 counter 로 배정된다고 규정해, "같은 seq 공간 공유" 주장을 더 강하게
    뒷받침한다(약화가 아니라 강화).
  - 제안: 없음 — 대체 문구를 그대로 채택 가능.

- **[WARNING]** claim ③ (구분 축 오류 — 복구방식 vs 재연결발화) 정확함이지만, **같은 파일의
  기존 Rationale 항목이 정확히 그 틀린 축을 이미 전제로 깔고 있고 draft 가 이를 건드리지 않는다**
  - target 위치: E-1 예외 blockquote / "무엇을 하지 않나" 절 (§10.1~§10.3, 6-websocket-protocol.md,
    §6.1 "spec 초안 값" 註 만 명시적으로 배제 대상으로 언급됨)
  - 충돌 대상: `spec/5-system/2-api-convention.md` **자기 자신의** `## Rationale` 절,
    "### §10.4 재연결 요약에 예외를 '복제' 하지 않고 '위임' 한 이유 (2026-09-02)" 항목
    (실제 파일 524번째 줄 부근)
  - 상세: 이 Rationale 은 다음과 같이 적혀 있다 — *"§10.4 는 재연결을 두 줄로 요약하는데, 그 둘 다
    서버가 스스로 끊은 경우에는 틀리다 — Socket.IO 는 서버발신 `disconnect()` 에 자동 재연결을
    발화하지 않고, **복구도 마지막 이벤트 ID 재전송이 아니라 스냅샷이다.**"* 이 문장은 "복구 방식이
    서버-disconnect 케이스에서만 다르다(= 그 외의 경우는 last-event-ID 재전송이 맞다)" 는 **바로 그
    틀린 축**을 전제로 서 있다. 그런데 §6.2 로 확인했듯 복구 방식(snapshot)은 두 경우 모두 동일하다
    — draft 의 claim ③ 이 정확히 지적한 그 오류가, §10.4 본문이 아니라 §10.4 **에 관한 Rationale**
    에도 독립적으로 박혀 있다. draft 의 E-1 은 §10.4 본문만 교체하고 이 Rationale 은 "무엇을 하지
    않나" 목록에 전혀 언급되지 않는다. 그 결과, 교체가 반영되면 **같은 문서 안에서 §10.4 본문
    ("복구 방식은 두 경우가 같다")과 §10.4 에 관한 Rationale ("복구도 … 스냅샷이다" 로 서버-disconnect
    케이스만 특별 취급하는 문장)이 서로 다른 축을 말하는 상태**가 남는다. 이 프로젝트가 이미 한 번
    (같은 트래커 도입부가 인용하는 `#1306`) 이 정확한 오독으로 다른 문서에 CRITICAL 오류를 만든
    전례가 있으므로, 이 Rationale 을 그대로 두면 다음 사람이 같은 길을 다시 밟을 위험이 실재한다.
  - 제안: E-1 범위에 이 Rationale 항목의 정정(또는 최소 "본 문단의 '복구도 다르다' 표현은 §10.4
    본문 교체로 갱신됨 — 복구 방식은 두 경우가 같다" 류의 addendum)을 포함시킨다. 문장 전체를
    새로 쓸 필요는 없다 — "복제 대신 위임한다" 는 구조적 결정 자체는 여전히 유효하므로, 그 결정을
    뒷받침하는 근거 문장 중 **"복구도 마지막 이벤트 ID 재전송이 아니라 스냅샷이다"** 부분만
    "복구는 두 경우 모두 스냅샷이다(다른 것은 재연결 발화 여부)" 로 고치면 충분하다. 이 정정을
    plan 의 체크리스트에도 새 항목으로 추가할 것을 권한다 (현재 체크리스트는 E-1·앵커 3건·자매
    트래커 플립만 언급하고 이 Rationale 은 없다).

- **[INFO]** claim ④ (대체안의 "지수 백오프 + 지터로 무제한 재시도") — **§6.1 표와 세 속성 모두 일치**
  - target 위치: E-1 첫 번째 불릿
  - 충돌 대상: `6-websocket-protocol.md §6.1` 구현 표
  - 상세: 표는 재연결시도=자동, 백오프 전략=Socket.IO 내장 지수 백오프, 지터=Socket.IO 내장
    randomizationFactor 적용, 최대 재시도=무제한(`reconnectionAttempts: Infinity`) 이라 규정한다.
    대체 문구의 "지수 백오프"·"지터"·"무제한 재시도" 세 속성이 정확히 대응한다. 과장·누락 없음.
  - 제안: 없음.

- **[INFO]** 역방향 검색 — last-event-ID 오류가 다른 spec 문서에 미러링되어 있는가
  - target 위치: "무엇을 하지 않나" 절 / 점검 관점 5
  - 충돌 대상: 번들 전체(`spec/**` 82개 문서, `spec/0-overview.md`·`1-data-model.md`·`2-navigation/**`·
    `3-workflow-editor/**`·`4-nodes/**`·`5-system/**`·`7-channel-web-chat/**`·`data-flow/**` 포함)
  - 상세: `재연결` 전문 검색 결과 이 오류(native WS 가 last-event-ID 로 재전송한다는 주장)를 반복하는
    자리는 `2-api-convention.md` 자기 자신(§10.4 본문 + 위에서 지적한 Rationale) 뿐이었다. 다른
    spec 영역(내비게이션·워크플로 에디터·노드·데이터플로우)은 이 개념 자체를 언급하지 않는다.
    즉 오류는 이미 스코프 안(같은 파일)에 갇혀 있고, "스코프가 한 칸 좁다" 는 의미의 위험은
    **다른 파일**이 아니라 위 WARNING 이 지적한 **같은 파일의 다른 절**에서 발생한다.
  - 제안: 위 WARNING 항목 반영으로 충분. 타 spec 문서로 검토 범위를 넓힐 필요는 없음.

- **[INFO]** §10.1/§10.2/§10.3 를 건드리지 않는 경계 — **타당함**
  - target 위치: "무엇을 하지 않나" 절 첫 항목
  - 충돌 대상: `6-websocket-protocol.md §1` 도입부 "전송 계층 (구현 현실)" 註
  - 상세: 이 註는 "`{type, id, payload}` JSON 프레임 표기는 **논리적 메시지 형태를 보이기 위한
    추상화**이며 실제 wire 는 Socket.IO 가 감싼다" 라고 §1~§9 전체에 대해 명시적으로 선언한다.
    `2-api-convention.md` §10.1(`ws(s)://...?token=`)·§10.2(`{type, payload}`)의 raw-WS 색은 이
    SoT 註가 이미 "틀림"이 아니라 "의도된 추상화"로 분류해 둔 표기와 같은 성격이다. §10.4 는
    다르다 — 존재하지 않는 매커니즘(last-event-ID 재전송)을 사실처럼 서술하므로 "추상화"로
    설명되지 않는다. draft 의 구분은 SoT 문언과 정확히 대응하며 경계가 유지된다.
  - 제안: 없음.

- **[INFO]** 신규 앵커 3건 실재 확인
  - target 위치: E-1 세 앵커 링크
  - 충돌 대상: `6-websocket-protocol.md` 헤더 렌더링
  - 상세: `#62-놓친-이벤트-복구` 와 `#47-외부-표면-매핑-external-interaction-api` 는 이미
    `14-external-interaction-api.md`(§EIA-NF-03 근거 각주, §11 도입부) 등 기존 문서가 같은 형태로
    인용 중이라 가드가 통과시켜 온 앵커임을 확인. `#61-클라이언트-재연결-socketio-내장` 은
    신규지만, 같은 문서의 다른 괄호 포함 헤더(예: `### 9.3 트랜잭션과 발송 순서 (EIA-RL-04)` →
    `#93-트랜잭션과-발송-순서-eia-rl-04`)가 만드는 슬러그 패턴과 정확히 같은 규칙(괄호 제거,
    소문자화, 공백→하이픈)을 따른다. 세 앵커 모두 유효.
  - 제안: 없음.

## 요약

target draft 가 제기하는 세 가지 사실 주장(백오프 수치가 SoT 가 스스로 부인한 초안값이라는 것,
native WS 에 last-event-ID 재전송이 없다는 것, 기존 註의 구분 축이 "복구 방식"이 아니라 "재연결
발화"여야 한다는 것)은 모두 SoT(`6-websocket-protocol.md` 본문·Rationale, `git log -S` 커밋 이력,
`14-external-interaction-api.md §R7`)로 독립 재검증한 결과 **사실**이었고, 그 중 claim ①은
git blame 으로 리터럴 일치까지 확인되는 가장 강한 증거를 갖는다. 대체 문구(E-1)도 SoT 문언을
과장하지 않고 오히려 SoT Rationale 의 표현을 거의 그대로 재사용하며, §10.1~§10.3 를 건드리지 않는
경계 판단도 SoT §1 의 명시적 "논리적 추상화" 註와 정합해 근거가 있다. 다만 한 가지 갭이 있다 —
`2-api-convention.md` **자기 자신의** 기존 `## Rationale` 항목("§10.4 재연결 요약에 예외를 '복제'
하지 않고 '위임' 한 이유")이 draft 가 claim ③에서 지적한 것과 **정확히 같은 틀린 축**("복구도
마지막 이벤트 ID 재전송이 아니라 스냅샷이다" — 서버-disconnect 케이스만 복구 방식이 다르다는 전제)을
이미 담고 있는데, E-1 은 §10.4 본문만 교체하고 이 Rationale 은 건드리지 않는다. 교체가 그대로
반영되면 같은 파일 안에서 본문과 Rationale 이 서로 다른 구분 축을 말하는 상태가 남아, 이 트래커가
발단이 된 바로 그 오독(다른 문서에 CRITICAL 을 유발했던 "seq 기반 복구" 오해)이 재발할 소지를
그 자리에 남겨 둔다. 이 한 곳을 제외하면 draft 의 판단과 교체안 모두 SoT 와 정합적이다.

## 위험도

MEDIUM — CRITICAL 은 없음(핵심 세 주장과 교체 문구는 SoT 와 정합). 다만 같은 파일 내 미수정
Rationale 이 교체 후 본문과 직접 모순되는 상태로 남는 WARNING 이 하나 있고, 그 오류 유형(축 오인)이
이 트래커의 발단이 된 실제 CRITICAL 사고와 같은 성격이라 방치 시 재발 위험이 있어 LOW 보다 한 단계
올렸다.
