# RESOLUTION — `review/consistency/2026/09/05/10_57_56` (`--impl-done`)

**BLOCK: NO** · Critical **0** · WARNING **1**. **조치 완료.**

## 조치 항목

| # | Checker | 지적 | 조치 |
|---|---|---|---|
| 1 | plan_coherence | README §5 신설 서술이 in-progress plan 의 미결 항목(*"V110 헤더 문장 정정 여부"*)을 **사실상 선점해 답했는데** 체크박스가 열려 있다 | 그 항목을 **(a) 로 종결**했다 — 아래 |

### 지적이 맞다

이 항목은 *"(a) 그대로 둔다 / (b) README·migrations.md 에 'V110 헤더는 이후 정정됐다' 한 줄"*
둘 중 하나를 정해야 한다며 등재해 뒀다. 그런데 같은 PR 이 README §5 를 **두 경로 표**로
정정하면서, 규약을 찾아 읽는 경로에서는 이미 정정된 문장을 먼저 만나게 됐다. 결정이 사실상
내려졌는데 트래커만 안 따라간 상태였다 — 이 저장소에서 반복된 *"체크와 상태가 어긋난다"*
그 형태다.

### (a) 를 택한 근거

- README §5 가 정정된 표를 담고 **바로 아래에서 `V110` 을 선례로** 가리킨다. SoT 는 모호하지 않다.
- `V110` 의 **SQL 은 지금도 옳다**(3문장 순서). 낡은 것은 헤더 **산문**뿐이다.
- 마이그레이션을 단독으로 열면 그 시점의 서술을 보게 되는 것은 **append-only 기록의 일반
  성질**이지 이 파일만의 문제가 아니다. (b) 는 그 일반 성질을 한 파일에만 예외로 다루는
  셈이라 택하지 않았다.

## INFO 처분

| # | 지적 | 처분 |
|---|---|---|
| 1~6 | 섹션 참조·상호 참조·인용 문구·`id` 유일성·직전 라운드 반영 — 전부 **정합 확인** | 확인 기록 |
| 7 | `migrations.md` 의 `## Overview` 가 다른 두 파일의 `## Overview (제품 정의)` 와 표기 혼재 | **미조치.** pre-existing 이고 이 PR 이 만든 것이 아니다. 고치면 `spec/` 편집이라 게이트를 한 바퀴 더 돌게 되는데, 얻는 것은 헤더 표기 통일뿐이다. checker 도 *"강제 사항 아님, 별도 정리 작업으로 분리"* 로 판단했다 |

## 이 조치가 게이트를 다시 세우지 않는 이유

변경은 `plan/in-progress/**` **한 파일뿐**이다 — `spec/` 도 `codebase/**` 도 건드리지 않아
코드 리뷰(`10_39_00`)와 이 `--impl-done`(`10_57_56`) 둘 다 유효하다. `git diff --name-only`
로 확인하고 게이트 정본(`review_guard.evaluate_review`)에도 물었다.

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint · build | **해당 없음** — 애플리케이션 코드 변경 0 |
| unit | **PASS** — frontend 스위트 **289 files / 6,328 tests** |
| e2e | **면제 (화이트리스트 부분집합)** — 변경 set 은 `.md` 와 `review/**` 아래 `.json` 뿐 |

## 보류·후속 항목

없음. 트래커의 열린 항목은 **3건**이고 전부 이 PR 범위 밖이다 —
§5.4 2단계(77곳), Flyway `mixed=true` 도입 여부, 해소 불가 bare 인용 8건.
