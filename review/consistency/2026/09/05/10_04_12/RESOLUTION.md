# RESOLUTION — `review/consistency/2026/09/05/10_04_12` (`--impl-done`)

**BLOCK: NO** · Critical **0** · WARNING **2** · INFO **2**. **전부 조치.**

## 조치 항목

| # | Checker | 지적 | 조치 |
|---|---|---|---|
| W1 | cross_spec | `code:` 를 "준수 예시" 로 재해석하면서 **SoT(`spec-impl-evidence.md`)를 갱신하지 않았다** — 한쪽만 재해석하면 SoT 가 그 사실을 모른다 | `spec-impl-evidence.md` §2.1 `code:` 정의에 **"시행 코드 없는 순수 문서형 convention" 예외 각주** + `review-citations.md` 선례 링크. 반대 방향 링크도 Rationale 에 추가 |
| W2 | cross_spec | `spec/data-flow/8-notifications.md:271` 이 **옛 2문장 순서를 절차처럼** 적는다 — 따라 하면 이번에 막은 위험이 그대로 생긴다 | 그 문단에 캐비엇 추가 — V056 시점의 순서이고 새로 쓰는 교체는 `README.md` §5 의 3문장을 따른다. V056 자신은 append-only 라 소급 대상 아님 |
| INFO#1 | cross_spec | §3 표가 `spec/**` 를 다루지 않는다 | **적용 대상 행 추가** — `codebase/**` 와 같은 논리(살아 있는 문서라 오래 읽힌다). 현재 위반 0건 |
| INFO#2 | convention_compliance | README 내부 참조 `§인덱스 교체` 가 숫자 전용 `§<번호>` 관례를 벗어나고 앵커도 없다 | `같은 절(§5) 아래 **인덱스 교체는 DROP-먼저**` 로 정정 |

### W2 를 "문서 하나 더" 로 넘기지 않은 이유

이 지적은 **내 변경이 만든 것이 아니라 내 변경이 드러낸 것**이다. `8-notifications.md` 는
원래부터 그 순서를 적고 있었고, 이번에 그 순서가 위험하다는 것을 규약화하면서 **두 문서가
서로 반대되는 절차를 말하는 상태**가 됐다. 새 규약을 세운 쪽이 그 모순을 남기고 가면
다음 사람은 어느 쪽을 따라야 할지 모른다.

전제는 직접 읽어 확인했다 — `:271` 이 실제로 *"`CREATE INDEX CONCURRENTLY ...` 후 옛 인덱스
`DROP INDEX CONCURRENTLY` 순서로 적용한다"* 라고 적는다.

## 수렴 판단

발견의 성격이 세 라운드에 걸쳐 이동했다:

| 라운드 | 성격 |
|---|---|
| `09_13_39` (`--spec`) | 규약 내용·근거 짝짓기 (실질) |
| `09_53_09` (`--impl-done`) | 두 규약의 **잠재 충돌**, 스코프 누락 (실질) |
| `10_04_12` (`--impl-done`) | **상호 링크·표기** (문서 정합) |

이번 조치도 전부 `spec/` 편집이라 이 리포트가 스스로 stale 해진다. 한 번 더 돌린다 —
그 라운드가 링크·표기 수준의 발견만 내면 수렴으로 본다.

## TEST 결과

| 단계 | 결과 |
|---|---|
| lint · build | **해당 없음** — 애플리케이션 코드 변경 0 |
| unit | **PASS** — frontend 스위트 **289 files / 6,328 tests** |
| e2e | **면제 (화이트리스트 부분집합)** — 변경 set 은 `.md` 와 `review/**` 아래 `.json` 뿐 |

## 보류·후속 항목

없음.
