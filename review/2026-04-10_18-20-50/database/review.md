해당 없음

이번 변경사항은 API 응답 래핑 구조를 `{ success: true }` 에서 `{ data: { success: true } }` 형태로 통일하는 작업으로, 데이터베이스 쿼리, 스키마, 트랜잭션, 인덱스, 커넥션 관리 등 데이터베이스 관련 코드와는 무관합니다.

### 요약
변경된 코드는 응답 DTO 래핑 레이어와 프론트엔드 클라이언트의 역직렬화 로직만 수정하며, 데이터베이스 접근 계층에는 아무런 영향이 없습니다.

### 위험도
NONE