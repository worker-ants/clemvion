해당 없음

이번 변경사항은 전적으로 프론트엔드 i18n 인프라 구성 작업입니다. `locale-sync.tsx`, `locale-store.ts`, `core.ts`, `index.ts`, 번역 딕셔너리, 관련 테스트 파일로 구성되며, 백엔드 API 엔드포인트·요청·응답 스키마·HTTP 상태 코드·인증 흐름에 직접적인 변경이 없습니다.

유일한 API 계약 관련 가정은 `GET /users/me` 응답에 `locale` 필드가 포함된다는 것이나, `isLocale()` 타입 가드로 방어적으로 처리되고 있어 미지원 값이 반환되어도 안전하게 폴백됩니다. 이 사항은 이전 리뷰(`2026-04-18_17-14-03/api_contract/review.md`)에서 이미 식별·문서화된 내용과 동일합니다.

### 위험도
NONE