해당 없음

이 변경사항들은 전적으로 인메모리 런타임 로직(Filter 노드 핸들러의 per-item 표현식 평가, 조건 필드 센티널 처리, 정규식 캐시 재설계)과 그에 대응하는 테스트 코드로 구성되어 있습니다. 데이터베이스 쿼리, 스키마 마이그레이션, ORM, 커넥션 풀, SQL 등 데이터베이스 관련 코드가 전혀 포함되어 있지 않습니다.

### 요약

변경된 코드는 워크플로우 엔진의 Filter 노드에서 배열 항목에 대한 표현식 바인딩(`$item`, `$itemIndex`)과 스칼라 배열 비교를 지원하기 위한 순수 비즈니스 로직 변경입니다. 인덱스, N+1, 트랜잭션, 마이그레이션, 스키마 설계, 커넥션 관리, SQL 인젝션, 대량 데이터 처리 등 데이터베이스 관련 사항이 하나도 없습니다.

### 위험도

NONE