# Spec↔Codebase 전수 상호 감사 — 위반·갭 보고서

- 실행: 2026-06-10 12:32 (worktree `spec-sync-audit-998544`)
- 방법: Workflow 3-phase 54 유닛 — 역방향 커버리지 21 (spec frontmatter 미커버 소스 378파일) / 순방향 drift 19 (2026-06-03 동기화 커밋 1161775f 이후 58커밋·코드 293파일 교차 spec 57개) / data-flow 13문서 전수 + 구조 갭 1
- 발견 총계: spec-violation 19 · spec-outdated/undocumented/frontmatter-gap 은 본 감사에서 spec 갱신으로 즉시 해소 (Wave 1: 116건 적용, Wave 2: data-flow 재작성 5·패치 8·신규 3 + 이월분)

> **본 문서의 위반(violation) 항목은 보고 전용** — 코드가 spec 본문의 명시 계약과 모순하는 케이스로, 코드 수정 또는 spec 하향은 별도 결정(developer/project-planner) 대상이다. 본 감사는 spec/코드 어느 쪽도 임의로 바꾸지 않았다.

## 1. Spec 위반 (코드가 spec 명시 계약과 모순)

### V-01 [severe] connected-expiry 0d 분기 — refresh_token 보유 makeshop 통합을 spec 금지에도 불구하고 expired 로 격하
- **spec**: spec/2-navigation/4-integration.md  (발견: rev:be-integrations)
- **코드**: codebase/backend/src/modules/integrations/integration-expiry-scanner.service.ts
- **증거**: spec/2-navigation/4-integration.md §11.1 MakeShop note(line 963): "`connected-expiry` 의 `remain ≤ 0d` 분기는 makeshop 을 cafe24 와 동일한 refresh-capable provider 로 취급한다 — `credentials.refresh_token` 이 유효한 makeshop 행은 `expired` 로 격하하지 않고 … refresh_token 을 보유한 통합(cafe24·makeshop)은 access_token 만 만료된 상태에서 `expired` 로 잘못 격하되지 않는다". spec/data-flow/5-integration.md §1.4(line 144) 동일 진술. 그러나 코드는 integration-expiry-scanner.service.ts:515-523 `isCafe24RefreshCapable()` 이 `if (integration.serviceType !== 'cafe24') return false;` 로 makeshop 을 제외하고, run() 의 0d 분기 :378-405 에서 capable=false 면 `integration.status = 'expired'; integration.statusReason = null;` 로 격하한다. makeshop access_token 은 1h 수명이고 OAuth callback 이 top-level `tokenExpiresAt` 를 채우므로(integration-oauth.service.ts:789) idle makeshop 통합은 다음 daily 스캔에서 곧바로 expired 로 잘못 격하된다. 해당 spec note 는 makeshop 도입 커밋 cc3032dd(#456, 2026-06-03 sync 커밋 1161775f 직후)에 추가됐으나 scanner 는 확장되지 않았다.
- **권고**: 코드 수정: `isCafe24RefreshCapable` 을 service-agnostic `isRefreshCapable` 류 술어로 일반화 — `credentials.refresh_token` 보유 시 (a) cafe24 는 기존대로 `cafe24-token-refresh` 큐 enqueue, (b) makeshop 은 enqueue 없이 expired 격하만 면제 (spec §11.1 note 그대로). 회귀 테스트 추가 (makeshop connected + refresh_token + token_expires_at 과거 → status 유지).

### V-02 [severe] AI 노드(text_classifier·information_extractor) override 설정 UI 가 spec 이 '노출'을 명시한 config 필드들을 렌더하지 않음
- **spec**: spec/4-nodes/3-ai/3-information-extractor.md  (발견: rev:fe-editor)
- **코드**: codebase/frontend/src/components/editor/settings-panel/node-configs/ai-configs.tsx:127, codebase/frontend/src/components/editor/settings-panel/node-configs/ai-configs.tsx:15, codebase/frontend/src/components/editor/settings-panel/node-configs/override-registry.ts:72, codebase/frontend/src/components/editor/settings-panel/node-configs/index.tsx:27, codebase/backend/src/nodes/ai/shared/agent-memory-schema.ts:136, codebase/backend/src/nodes/ai/information-extractor/information-extractor.schema.ts:105
- **증거**: spec/4-nodes/3-ai/3-information-extractor.md §1 설정 표는 memoryKey·memoryTopK·memoryThreshold·memoryTtlDays·embeddingModel·extractionModel 을 "`persistent` 일 때(만) 노출" 로 명시하고, §2 설정 UI mock 은 "── Examples (Few-shot) ── [+ Add Example]" 과 "Max Collection Retries: [3___]" 를, FieldDef 표(:61)는 enumValues 를 명시한다. spec/4-nodes/3-ai/0-common.md §10 은 "`gateOnManualMemoryStrategy` 로 자동 전략 시 contextScope 5필드를 숨긴다" 고 UI 노출 동작을 기술한다. spec/4-nodes/3-ai/2-text-classifier.md §1 도 contextScope 계열 5필드 + includeSystemContext/systemContextSections 를 설정 필드로, CategoryDef.examples 를 §2 UI mock 에 표시한다. 그러나 frontend 는 OVERRIDE_REGISTRY (override-registry.ts:72-73) 가 두 노드를 bespoke 컴포넌트로 대체하고, NodeConfigRenderer (index.tsx:27-28) 는 override 존재 시 auto-form 을 전혀 렌더하지 않는다. InformationExtractorConfig (ai-configs.tsx:127-262) 가 실제 렌더하는 필드는 mode·llmConfigId·model·inputField·instructions·outputSchema(name/type/description/required)·maxTurns 뿐 — memoryStrategy 계열 7필드, contextScope 계열 5필드, maxCollectionRetries, enumValues, examples(Few-shot) 전부 설정 패널에 부재. TextClassifierConfig (ai-configs.tsx:15-124) 도 contextScope 5필드·includeSystemContext·CategoryDef.examples 를 렌더하지 않는다. 백엔드 schema 는 ui.visibleWhen 힌트를 정상 방출하므로 (agent-memory-schema.ts:136, buildConversationContextSchemaFields) auto-form 이었다면 노출됐을 필드들이다. ai_agent 는 auto-form 으로 이행돼 동일 필드가 정상 노출되는 것과 대비된다. 현재 사용자는 Code 탭 JSON 편집으로만 이 필드들을 설정할 수 있다. #484 (cbfbfbb9, persistent memory 확장) 가 backend schema + spec 을 갱신했지만 ai-configs.tsx 는 갱신되지 않았다 (해당 파일은 2026-06-03 sync 이전부터 무변경).
- **권고**: 코드 수정 권장: (a) information_extractor·text_classifier 를 auto-form 으로 이행 (override-registry 에서 제거, schema ui 힌트는 이미 충분) 하거나, (b) override 컴포넌트에 누락 필드 섹션(Memory·Conversation Context·enumValues·examples·maxCollectionRetries)을 추가. 이행이 단기 불가하면 spec/4-nodes/3-ai/3-information-extractor.md·2-text-classifier.md 에 '설정 패널 미노출(Code 탭 JSON 으로만 설정 가능) — Planned' 단서를 달아 현황을 정직하게 반영해야 한다.

### V-03 [severe] GET /api/audit-logs — spec 이 명시한 Admin+ 권한 가드 부재 (+사용자 필터 미구현)
- **spec**: spec/5-system/1-auth.md  (발견: rev:be-observability)
- **코드**: codebase/backend/src/modules/audit-logs/audit-logs.controller.ts:16-35, codebase/backend/src/modules/audit-logs/dto/query-audit-log.dto.ts:5-43, codebase/backend/src/common/guards/roles.guard.ts:51-53, codebase/backend/src/common/decorators/workspace.decorator.ts:7-24
- **증거**: spec/5-system/1-auth.md §4.2: "관리자(Admin+)만 조회 가능 / 기간, 사용자, 액션 유형으로 필터링", §5 엔드포인트 표: "GET /api/audit-logs | 감사 로그 조회 (Admin+)". 그러나 AuditLogsController 에는 @Roles 데코레이터가 전혀 없다 (audit-logs.controller.ts:16-35). 전역 RolesGuard 는 @Roles 미지정 라우트를 무조건 통과시키고 (roles.guard.ts:51-53 "if (!requiredRoles ...) return true"), @WorkspaceId 데코레이터는 X-Workspace-Id 헤더를 멤버십 검증 없이 그대로 신뢰한다 (workspace.decorator.ts:14-15). 결과: 인증된 임의 사용자가 임의 워크스페이스 ID 헤더로 해당 워크스페이스의 전체 감사 로그(누가 무엇을 언제 변경했는지 + IP)를 열람 가능 — viewer 는 물론 비멤버까지. 부수: §4.2 가 약속한 '사용자' 필터도 미구현 (QueryAuditLogDto 는 action/resourceType/startDate/endDate 만, userId 필터 없음).
- **권고**: 코드 수정 권장: AuditLogsController.findAll 에 @Roles('admin') 부착 (이 경우 RolesGuard 가 WorkspacesService.getMemberRole 로 멤버십+역할을 함께 검증하므로 비멤버 열람도 차단됨). §4.2 의 '사용자' 필터는 QueryAuditLogDto 에 userId 추가 구현하거나, 의도적으로 미루는 것이면 spec/5-system/1-auth.md §4.2 에 Planned 표기. developer 작업 + plan 등재 대상.

### V-04 [major] 폴더 중첩 깊이 제한(최대 5단계)이 update(부모 이동) 경로에서 미검증 — cycle 검증도 부재
- **spec**: spec/1-data-model.md  (발견: rev:be-workflow-crud)
- **코드**: codebase/backend/src/modules/folders/folders.service.ts, codebase/backend/src/modules/folders/dto/update-folder.dto.ts
- **증거**: spec/1-data-model.md §2.5 Folder 제약 조건: '중첩 깊이 제한: 최대 5단계'. 코드는 create() 에서만 getDepth 검사를 수행하고 (folders.service.ts:43-51), update() 는 UpdateFolderDto.parentId (update-folder.dto.ts:26-35) 를 깊이·cycle·workspace 소속 검증 없이 Object.assign 으로 그대로 저장한다 (folders.service.ts:60-68 — `Object.assign(folder, data); return this.folderRepository.save(folder)`). 따라서 PATCH /folders/:id 로 (1) 깊이 5 초과 트리 구성 가능, (2) 자기 자신/자손을 parentId 로 지정해 cycle 생성 가능 — cycle 이 생기면 이후 create() 의 getDepth while 루프(folders.service.ts:75-91, visited 집합 없음)가 무한루프에 빠진다. 단 이 API 는 프론트엔드 미사용 dormant 상태라 현재 사용자 노출 경로는 없다.
- **권고**: 코드 수정 권고: update() 에서 parentId 변경 시 (a) 새 부모가 같은 workspace 소속인지, (b) 새 부모의 깊이 + 자기 서브트리 높이가 5 이하인지, (c) 새 부모가 자기 자신·자손이 아닌지(cycle) 검증 추가. getDepth 에 방문 집합 또는 깊이 상한 가드 추가. spec 쪽은 1-data-model §2.5 제약이 SoT 로 유지.

### V-05 [major] 실행 상세 페이지 노드 서브탭이 spec 약속(LLM Usage·Config·메시지 레벨 Response/Request) 미구현
- **spec**: spec/2-navigation/14-execution-history.md  (발견: rev:fe-app)
- **코드**: codebase/frontend/src/app/(main)/workflows/[id]/executions/[executionId]/page.tsx, codebase/frontend/src/components/editor/run-results/result-detail.tsx, codebase/frontend/src/components/editor/run-results/conversation-inspector.tsx
- **증거**: spec EH-DETAIL-03 및 §3.3 은 "서브 탭(노드 레벨): Preview / Input / Output / LLM Usage (AI 노드에서만) / Config / Error", "assistant 메시지를 선택하면 탭이 메시지 레벨로 전환: Preview / Response / Request / LLM Usage" 를 ✅ 구현 완료로 명시하고 §3.4.1·§3.4.2 가 상세 명세까지 둔다. 그러나 실행 상세 페이지의 detailTabs 는 preview/input/output/error 4개뿐이다 (page.tsx:668-673). Config 탭·LLM Usage 탭·메시지 레벨 Response/Request/LLM Usage 탭은 에디터 run-results 패널(result-detail.tsx:267 'config' 탭 등)에만 존재하고 본 페이지는 자체 탭 셋을 쓴다. 심지어 본 페이지가 렌더하는 ConversationInspector 의 안내 문구(conversation-inspector.tsx:433-434 "원문 요청 / 응답 / 사용량은 상단의 Request/Response/LLM Usage 탭에서 확인")가 이 surface 에 존재하지 않는 탭을 가리킨다.
- **권고**: 실행 상세 페이지의 NodeResultsTab 을 에디터 result-detail.tsx 의 탭 구성과 정렬(공용 컴포넌트 재사용으로 Config·LLM Usage·메시지 레벨 Response/Request 추가)하는 것이 spec 의도에 부합. 만약 실행 내역 surface 에서 의도적으로 축소한 것이면 project-planner 가 EH-DETAIL-03·§3.3·§3.4.1·§3.4.2 를 에디터 패널 한정으로 재서술해야 함 — 어느 쪽이든 결정 필요.

### V-06 [major] GET /api/integrations/services/:type/catalog — spec 은 makeshop operations 반환을 약속하나 코드는 cafe24 만 채움
- **spec**: spec/2-navigation/4-integration.md  (발견: rev:be-integrations)
- **코드**: codebase/backend/src/modules/integrations/integrations.controller.ts, codebase/backend/src/modules/integrations/integrations.service.ts
- **증거**: spec/2-navigation/4-integration.md §9.3(line 803): "**초기 응답 정책**: `:type='cafe24'` 및 `:type='makeshop'` 만 backend 메타데이터에서 추출한 `operations[]` 를 채워 반환하고, `:type ∈ {http, database, email, webhook, mcp, google, github}` 은 빈 배열". §4.6(line 366) 도 "makeshop 도 동일 (`makeshop.<resource>.<operation>` + `services/makeshop/catalog`)". 그러나 integrations.service.ts:1164-1178 `getServiceCatalog()` 은 `serviceType === 'cafe24'` 만 operations 를 채우고 그 외(makeshop 포함)는 `{ operations: [] }`. integrations.controller.ts:130-148 Swagger description 도 "초기엔 `cafe24` 만 채워 반환하고 그 외 서비스 타입은 빈 배열" 로 옛 정책을 그대로 노출한다. makeshop 활동 로그의 `apiLabel`(makeshop.<resource>.<operation>) 이 사람 친화 라벨로 변환되지 못한다.
- **권고**: 코드 수정: `getServiceCatalog('makeshop')` 이 makeshop 메타데이터(161 operation)에서 `operations[]` 를 채워 반환하도록 확장하고 controller 의 @ApiOperation/@ApiParam description 을 spec §9.3 표현(cafe24·makeshop 지원)으로 갱신. (또는 makeshop catalog 가 의도적으로 후속이라면 spec §9.3·§4.6 을 'cafe24 한정, makeshop 후속' 으로 격하하고 pending plan 연결.)

### V-07 [major] expired 격하 시 status_reason='token_expired' 미기록 + integration_expired 알림 발사 정책(§11.2) 미구현
- **spec**: spec/2-navigation/4-integration.md  (발견: rev:be-integrations)
- **코드**: codebase/backend/src/modules/integrations/integration-expiry-scanner.service.ts, codebase/backend/src/modules/integrations/integration-status-reason.ts, codebase/backend/src/modules/integrations/entities/integration-expiry-dispatch.entity.ts
- **증거**: (1) status_reason: spec/1-data-model.md §2.10(line 285) 와 spec/2-navigation/4-integration.md Rationale(line 1403)·data-flow/5-integration.md(line 262) 가 "`expired` → `token_expired` (refresh_token 없는 provider 의 token_expires_at 만료)" 를 명시하나, 코드는 격하 시 `integration.statusReason = null`(integration-expiry-scanner.service.ts:403) 이고 `INTEGRATION_STATUS_REASONS` union(integration-status-reason.ts:12-35) 에 `token_expired` 값 자체가 없다. (2) 발사 정책: §11.2(line 980) "**refresh_token 없는 provider 의 `token_expires_at` 만료 (`status_reason='token_expired'`) 에만 발사**" 인데, run()(:358-424) 은 service type 무관하게 모든 후보(자동갱신 cafe24·makeshop 포함)에 7d/3d/0d 알림을 push 한다. dedup 키가 `(integrationId, threshold, tokenExpiresAt)`(integration-expiry-dispatch.entity.ts:15-19) 라 토큰이 갱신될 때마다 키가 바뀌어 단수명 토큰(cafe24 2h)은 매 스캔마다 재발사 가능. 단, spec 자체도 §11.1 표(line 941)는 cafe24 0d 에 "enqueue … + 알림" 이라 §11.2 발사 정책과 내부 모순이 있다.
- **권고**: 결정 필요: (a) §11.2 정책 채택 시 — run() 에서 refresh-capable provider 를 passive `integration_expired` 알림 대상에서 제외하고, 격하 시 `statusReason='token_expired'` 기록 + union 에 `token_expired` 추가; §11.1 표의 cafe24 "+ 알림" 문구 정정. (b) §11.1 동작(알림 유지) 채택 시 — §11.2 발사 정책 문장과 1-data-model §2.10 의 token_expired 진술을 현 구현(null reason, 전 provider 알림)으로 갱신. 어느 쪽이든 spec 내부 모순(§11.1 vs §11.2) 해소가 선행.

### V-08 [major] MakeShop 활동 로그 catalog 라벨 경로 미구현 — `services/makeshop/catalog` 빈 배열 + Activity 탭 dict namespace cafe24 고정
- **spec**: spec/4-nodes/4-integration/5-makeshop.md  (발견: fwd:nodes-4-integration)
- **코드**: codebase/backend/src/modules/integrations/integrations.service.ts:1164, codebase/backend/src/modules/integrations/integrations.controller.ts:130, codebase/frontend/src/app/(main)/integrations/[id]/page.tsx:826, codebase/frontend/src/lib/i18n/dict/ko/makeshopCatalog.ts
- **증거**: spec 5-makeshop §4 step 11: "`api_label` = catalog key `makeshop.<resource>.<operation>` ... frontend 가 `GET /api/integrations/services/makeshop/catalog` 응답 `labelKey` + i18n dict 로 렌더". spec/2-navigation/4-integration.md §9.3(L803)도 "**초기 응답 정책**: `:type='cafe24'` 및 `:type='makeshop'` 만 ... `operations[]` 를 채워 반환" 이라 명시하고, plan/in-progress/makeshop-integration.md L67 에 해당 spec 변경이 [x] 처리됨. 그러나 코드는 (1) `IntegrationsService.getServiceCatalog` (integrations.service.ts:1164~1178) 가 `serviceType === 'cafe24'` 분기만 두고 makeshop 은 `{ operations: [] }` 반환 — controller doc(L134)도 "초기엔 `cafe24` 만" 이라고 적혀 있음. (2) 통합 상세 Activity 탭의 `tryTranslateLabel` (page.tsx:826~833) 이 lookup 키를 `cafe24Catalog.${catalogKey}` 로 하드코딩해, makeshop `apiLabel`(`makeshop.product.get-product` 등)은 dict miss → endpoint subtext fallback 으로 격하된다. 정작 `dict/{ko,en}/makeshopCatalog.ts` (177줄, 161 op 라벨) 는 채워져 있고 그 헤더 주석은 "(cafe24 와 달리) Activity 탭은 이 dict 로 사람 친화 라벨을 렌더한다" 고 spec 과 같은 약속을 한다. 즉 spec 의 명시 계약(두 문서)과 코드가 모순.
- **권고**: 코드 수정 권장: (1) `getServiceCatalog` 에 makeshop 분기 추가 — makeshop metadata(public-meta `toPublicMakeshopOperation` 활용)에서 `{key, method, path, labelKey, descriptionKey}` 생성, (2) Activity 탭 `tryTranslateLabel` 의 namespace 를 serviceType/catalog key prefix 기반(`cafe24Catalog.` / `makeshopCatalog.`)으로 일반화. 의도적 연기라면 5-makeshop §4 step 11 과 2-navigation §9.3 의 초기 응답 정책을 cafe24-only 로 되돌리고 pending plan 에 항목화해야 함.

### V-09 [major] 초대 수락 페이지가 spec 의 확인 단계([수락] 버튼)·이메일 불일치 UI 없이 마운트 즉시 자동 수락
- **spec**: spec/5-system/1-auth.md  (발견: rev:fe-app)
- **코드**: codebase/frontend/src/app/(main)/invitations/accept/accept-invitation-content.tsx, codebase/frontend/src/app/(main)/invitations/accept/page.tsx
- **증거**: spec §1.5.3 은 "1. 메일 링크 클릭 → 프론트엔드가 토큰 메타 조회 / 2. 로그인되어 있고 본인 이메일과 토큰 이메일이 일치 → 수락 페이지에 [수락] 버튼 노출" 그리고 "토큰 이메일과 로그인 사용자의 이메일이 다르면 수락 페이지에서 '이 초대는 {토큰.email} 에게 발송되었습니다. 해당 계정으로 로그인하세요' 안내 + 로그아웃 버튼만 노출한다" 를 명시. 그러나 accept-invitation-content.tsx:34-58 은 토큰 메타 조회(GET /api/invitations/:token) 없이 useEffect 마운트 시점에 workspacesApi.acceptInvitation(token) 을 즉시 호출(버튼 없음)하고, 실패 시 서버 에러 메시지 + [대시보드로] 버튼만 노출한다(로그아웃 affordance 없음). 서버측 이메일 일치 강제(400 invitation_email_mismatch)는 유효하나 프론트 흐름 계약과 모순. 또한 이 페이지는 어떤 spec frontmatter code: 에도 매핑돼 있지 않다.
- **권고**: spec §1.5.3 대로 토큰 메타 조회 → [수락] 버튼 → 이메일 불일치 시 계정 전환 안내+로그아웃 버튼 UI 를 구현하거나, 자동 수락이 의도된 진화라면 project-planner 가 §1.5.3 흐름과 spec/2-navigation/9-user-profile.md §4.1.1 을 갱신. 어느 쪽이든 frontmatter 매핑(예: 9-user-profile.md 또는 10-auth-flow.md code: 에 invitations/accept/** 추가) 필요.

### V-10 [minor] 트리거 목록(list)에 Schedule 트리거의 Cron 표현식·다음 실행 시각이 채워지지 않음 — spec §2.1 목록 항목 계약과 모순
- **spec**: spec/2-navigation/2-trigger-list.md  (발견: rev:be-triggers)
- **코드**: codebase/backend/src/modules/triggers/triggers.service.ts, codebase/backend/src/modules/triggers/dto/responses/trigger-response.dto.ts, codebase/frontend/src/app/(main)/triggers/page.tsx
- **증거**: spec/2-navigation/2-trigger-list.md §2.1 은 목록 행에 대해 "상세 정보 | Webhook: HTTP 메서드 + 경로, Schedule: Cron 표현식", "Schedule 태그 | Schedule 유형 트리거에 [Schedule] 태그 표시 + Cron 표현식 + 다음 실행 시각" 을 명시하고, §1 화면 구조 목업에도 목록 행에 `0 9 * * *  Next: 09:00` 이 그려져 있다. 그러나 backend `GET /api/triggers` 목록 경로(triggers.service.ts:75-112 `findAll`)는 schedule join/enrichment 없이 Trigger 행만 반환하고, cron/nextRun enrichment 는 단건 조회 `findOneDetail`(triggers.service.ts:128-145) 에만 존재한다. 응답 DTO 도 이를 명문화: trigger-response.dto.ts:45-55 "Cron 식 (schedule 타입 트리거 단건 조회 시에만 채워짐)". frontend 목록은 list 응답의 `t.cronExpression`/`t.nextRunAt` 을 그대로 매핑(page.tsx:230-231)하므로 항상 undefined — 목록 행에 cron/다음 실행 시각이 표시되지 않고, 목록에서 여는 삭제 다이얼로그도 cron 을 "—" 로 표시한다(trigger-delete-dialog.tsx:97).
- **권고**: 코드 수정이 자연스러움: `TriggersService.findAll` 에 type=schedule 행에 대한 schedule 테이블 일괄 join (triggerId IN (...)) 으로 cronExpression/nextRunAt enrichment 를 추가해 spec §2.1 약속을 충족. 대안으로 product 의도가 '목록은 미표시, drawer 단건 조회만' 이라면 project-planner 가 §1 목업·§2.1 두 행을 단건 조회/드로어 한정으로 하향 조정해야 함 — 현재는 spec 본문·응답 DTO 주석·frontend 기대가 3자 불일치 상태.

### V-11 [minor] 통합 삭제 차단 다이얼로그(사용처 목록 표시) 미구현 — 409 시 일반 토스트로 대체
- **spec**: spec/2-navigation/4-integration.md  (발견: rev:fe-app)
- **코드**: codebase/frontend/src/app/(main)/integrations/[id]/page.tsx
- **증거**: spec §4.7 은 "[Delete integration] 클릭 시 GET /api/integrations/:id/usages 를 확인하여 … 사용처 ≥ 1건: 삭제 차단 다이얼로그에 사용처 목록과 안내 메시지 표시 (§7)" 를, §7.2 는 워크플로·노드 목록 + [Open Workflow →] 버튼이 있는 차단 다이얼로그 와이어프레임을 명시. 그러나 DangerTab(page.tsx:903-935) 은 usages 사전 조회 없이 확인 버튼 → DELETE 를 보내고, 실패 시 deleteMutation onError(page.tsx:105-114)가 409 응답을 t("integrations.inUseError") 일반 토스트로만 처리한다. 사용처 목록은 별도 Usage 탭에서만 볼 수 있고 삭제 흐름과 연결돼 있지 않다. 서버측 차단(409 INTEGRATION_IN_USE)은 동작.
- **권고**: §4.7/§7.2 대로 삭제 클릭 시 usages 사전 조회 후 사용처 목록·Open Workflow 링크를 담은 차단 다이얼로그를 구현하거나, 현행 '409 → 토스트 + Usage 탭 참조' UX 를 의도로 확정한다면 spec §4.7·§7.2 를 다운스케일 — planner 결정 필요.

### V-12 [minor] Switch 노드 switchValue 의 requiredWhen asterisk — spec 은 UI asterisk 노출을 명시하나 override UI 가 미구현
- **spec**: spec/4-nodes/1-logic/2-switch.md  (발견: rev:fe-editor)
- **코드**: codebase/frontend/src/components/editor/settings-panel/node-configs/logic-configs.tsx:157, codebase/frontend/src/components/editor/settings-panel/node-configs/override-registry.ts:62, codebase/backend/src/nodes/logic/switch/switch.schema.ts:88
- **증거**: spec/4-nodes/1-logic/2-switch.md:21 — "UI asterisk 는 `ui.requiredWhen: { field: 'mode', equals: ['value'] }` 화이트리스트" + §8.1 이 이를 구현 사실로 기술. requiredWhen 힌트를 소비하는 것은 auto-form 의 isFieldRequired (auto-form/visibility.ts:69) 뿐인데, switch 는 OVERRIDE_REGISTRY (override-registry.ts:62) 의 bespoke SwitchConfig 가 렌더하며, 해당 컴포넌트의 switchValue ExpressionInput (logic-configs.tsx:157-163) 은 required 표시(asterisk)를 전혀 렌더하지 않는다. mode=value 일 때도 asterisk 미표시.
- **권고**: SwitchConfig 에 mode==='value' 시 switchValue 라벨에 required asterisk 를 추가하거나, switch 를 auto-form 으로 이행. 단기적으로는 spec 2-switch.md §8.1 에 'asterisk 는 schema 힌트 계약이며 현 override UI 는 미반영' 단서 추가도 가능.

### V-13 [minor] 캔버스 요약 미구현 — ai_agent·information_extractor 는 spec 이 약속한 캔버스 본문 요약(`{mode} · {model} · {N} KB · {N} MCP …`)을 렌더하지 않음 (summaryTemplate 부재, text_classifier 만 구현)
- **spec**: spec/4-nodes/3-ai/0-common.md  (발견: fwd:nodes-3-ai)
- **코드**: codebase/backend/src/nodes/ai/ai-agent/ai-agent.schema.ts, codebase/backend/src/nodes/ai/information-extractor/information-extractor.schema.ts, codebase/frontend/src/lib/utils/node-config-summary.ts
- **증거**: 0-common.md §8 캔버스 요약 표는 AI Agent 행에 "`{mode} · {model}` … `· {N} tools` … `· {N} KB` … `· {N} MCP` … `· {N} cond` 추가, multi_turn 이면 `Multi Turn` 표기" (예: `gpt-4o · 2 tools · 1 KB · 1 MCP · 3 cond`), Info Extractor 행에 "`{model} · {N} fields` … `Multi Turn` 접두어" 를 명시하고, 1-ai-agent.md §11·3-information-extractor.md §8 이 이를 인용한다 (1-ai-agent §11 은 `· 3 render` 표현 도구 수까지 약속). 그러나 frontend `getConfigSummary`(node-config-summary.ts:61~89)는 blocking warning 이 없으면 `def.summaryTemplate` 렌더 → 없으면 null 인데, `aiAgentNodeMetadata`(ai-agent.schema.ts:680~)와 `informationExtractorNodeMetadata`(information-extractor.schema.ts:297~)에는 summaryTemplate 이 존재하지 않는다 — 두 노드의 캔버스 본문 요약은 전혀 렌더되지 않는다. text_classifier 만 `summaryTemplate: '{{model}} · {{categories.length}} categories'`(text-classifier.schema.ts:234)로 구현·spec 정합. 부가로 AI Agent 행의 'Tool Area에 등록된 도구 수 `{N} tools`' 는 제거된 toolNodeIds/Tool Area(1-ai-agent §1 '재작성 예정 — 현재 제거됨')를 여전히 참조해 이중으로 낡았다. 동기화 시점(1161775f)에도 미구현이던 잔존 갭.
- **권고**: 둘 중 하나로 정리: (a) developer 가 ai_agent·information_extractor 에 summaryTemplate(또는 동적 요약 빌더)를 구현 — 단 mini-DSL 로는 'Multi Turn' 접두어·조건부 `· {N} KB` 조합이 안 되므로 템플릿 DSL 확장 또는 전용 빌더 필요, (b) project-planner 가 0-common §8 / 1-ai-agent §11 / 3-IE §8 의 두 행을 '미구현(Planned)' 으로 마킹하거나 현 동작(요약 없음, warning 배지만)으로 축소 기술. 최소한 `{N} tools` 세그먼트는 Tool Area 제거에 맞춰 삭제.

### V-14 [minor] Re-run 모달 — spec §10.2 의 '원본 ID 새 탭 링크'와 'manual_trigger 스키마 기반 typed 동적 폼' 미구현
- **spec**: spec/5-system/13-replay-rerun.md  (발견: rev:fe-components)
- **코드**: codebase/frontend/src/components/executions/rerun-modal.tsx
- **증거**: spec/5-system/13-replay-rerun.md §10.2 필드 동작 표: "원본 실행 헤더 — 원본 ID, 시작 시각, 최종 상태 표시. **ID 클릭 시 새 탭으로 원본 상세 페이지**", "입력 데이터 폼 — Manual Trigger parameters **스키마 기반 동적 폼. 필드 라벨/타입은 워크플로의 manual_trigger 노드 config 에서 도출**" (모달 ASCII 에 `extra.flag [☑ true]` 체크박스 예시 포함). 그러나 rerun-modal.tsx:237 은 원본 ID 를 링크 없는 `<span className="font-mono text-xs">{original.id}</span>` 로만 렌더하고, 입력 폼은 rerun-modal.tsx:189-192 + 266-281 에서 `Object.keys(original.inputData.parameters)` 만으로 전부 텍스트 `<Input>` 을 생성한다 — manual_trigger config 를 조회하지 않으며 타입별 위젯(checkbox 등)·라벨 도출이 없다. 나머지 계약(토글 기본 OFF, dry-run disabled+tooltip, 에러 코드 4종 매핑, 성공 시 라우팅, 외부 호출 노드 type 별 breakdown)은 spec 과 정확히 일치.
- **권고**: 코드 보강(원본 ID 를 `/workflows/:wid/executions/:id` 새 탭 링크로, 폼을 manual_trigger config 스키마 기반 typed 폼으로) 또는 의도된 단순화라면 spec §10.2 의 두 진술을 현 구현(플레인 텍스트 ID, 원본 입력 키 기반 텍스트 폼)으로 완화 갱신. 어느 쪽인지 project-planner 판단 필요.

### V-15 [minor] system-status 모니터링 큐 레지스트리에 makeshop-token-refresh 누락 (spec 은 모니터링 대상으로 명시)
- **spec**: spec/5-system/16-system-status-api.md  (발견: fwd:misc-4)
- **코드**: codebase/backend/src/modules/system-status/system-status.constants.ts:51-73, codebase/backend/src/modules/integrations/makeshop-token-refresh.constants.ts:20, spec/5-system/16-system-status-api.md:29, spec/data-flow/0-overview.md:93
- **증거**: spec/5-system/16-system-status-api.md §1 '대상 큐 레지스트리' 표는 `| makeshop-token-refresh | integration | 1 (기본) | MakeShop 토큰 갱신 (proactive + reactive_401) |` 행을 포함하고, SoT 인 spec/data-flow/0-overview.md §4 BullMQ 큐 카탈로그도 `makeshop-token-refresh` 를 등재한다. 그러나 코드의 `MONITORED_QUEUES` (system-status.constants.ts L51-73) 는 EXECUTION_RUN_QUEUE·BACKGROUND_EXECUTION_QUEUE·CONTINUATION·document-embedding·graph-extraction·notification-webhook·CAFE24_REFRESH_QUEUE·schedule 외 system 그룹 6종만 포함하며 `MAKESHOP_REFRESH_QUEUE` import/엔트리가 없다. 커밋 cc3032dd (#456, 2026-06-03 sync 이후) 가 'W-9: makeshop-token-refresh 큐를 system-status-api §1 + data-flow §4 카탈로그에 등록' 으로 spec 두 곳만 갱신하고 코드 레지스트리는 미동기화 — 이후 #469 에서 execution-run 만 추가됐다. 결과적으로 MakeShop 토큰 갱신 큐는 시스템 상태 API/화면(utilization·failed·health 파생)에서 보이지 않아 spec 의 명시 계약(모니터링 대상)과 모순.
- **권고**: 코드 수정: system-status.constants.ts 에 `MAKESHOP_REFRESH_QUEUE` 를 import 하고 `MONITORED_QUEUES` 에 `{ name: MAKESHOP_REFRESH_QUEUE, group: 'integration', concurrency: 1 }` 엔트리 추가 (processor 는 기본 concurrency 1 — spec 표 '1 (기본)' 과 일치). 회귀 테스트로 MONITORED_QUEUES ↔ data-flow §4 카탈로그 동기 검증 보강 권장.

### V-16 [minor] KB 생성 DTO 의 Swagger 설명이 "cross_encoder_llm 은 후속 구현" 으로 남아 spec 의 "두 모드 모두 구현됨" 과 모순 (코드측 문서 문자열 수정 대상)
- **spec**: spec/5-system/9-rag-search.md  (발견: rev:be-kb-rerank)
- **코드**: codebase/backend/src/modules/knowledge-base/dto/create-knowledge-base.dto.ts, codebase/backend/src/modules/knowledge-base/dto/rag-search.dto.ts
- **증거**: spec/5-system/9-rag-search.md §3.3.2 "`cross_encoder` 와 `cross_encoder_llm` 두 모드 모두 구현됨" / spec/1-data-model.md §2.16.1 "두 모드가 모두 구현됨". 그러나 사용자에게 노출되는 Swagger 문서인 create-knowledge-base.dto.ts:153-155 (`rerankMode` 설명: "`cross_encoder_llm` 은 추가 LLM grading(후속 구현)") 와 198-200 (`rerankLlmConfigId`: "(cross_encoder_llm 은 후속 구현)") 이 stale 로 남아 API 소비자에게 미구현으로 오인시킨다. 동작 자체는 spec 과 일치(구현됨). 부수: rag-search.dto.ts:39 의 `topK` Swagger `default: 5` 도 실제로는 고정 default 미적용(미지정 시 §3.4 동적 컷) — D1 변경 후 stale.
- **권고**: 코드 수정 (developer): create-knowledge-base.dto.ts 의 두 ApiProperty 설명에서 "(후속 구현)" 제거 + conditional escalate 의미로 갱신, rag-search.dto.ts 의 `default: 5` 표기를 "미지정 시 동적 점수 컷 적용" 으로 교체. spec 변경 불요.

### V-17 [minor] web-chat-sdk README·BYO-UI 예제가 폐기된 firstMessage 패턴을 여전히 안내 (§R6 폐기 결정과 모순 — 메시지 증발 패턴)
- **spec**: spec/7-channel-web-chat/1-widget-app.md  (발견: fwd:webchat)
- **코드**: codebase/packages/web-chat-sdk/README.md, codebase/packages/web-chat-sdk/examples/byo-ui-headless.ts
- **증거**: spec 1-widget-app §3 "**`firstMessage` 미사용**: webhook payload 는 `profile` 만 싣는다" + §R6 "`firstMessage` 메커니즘은 폐기한다", 그리고 §R6-2 가 명시하듯 webhook 에 실린 firstMessage 는 multi_turn 이 첫 턴으로 소비하지 않아 "어느 노드에도 닿지 못해 증발"한다. 그런데 web-chat-sdk README.md:64 `client.triggerWebhook(endpointPath, { firstMessage })` 와 examples/byo-ui-headless.ts:34,44 가 M2 BYO-UI 개발자에게 이 깨진 패턴을 그대로 안내 중. plan/in-progress/webchat-eager-start.md 비차단 backlog(impl-done W1)에 "submit_message 패턴 예제로 교체 후속" 으로 추적되고 있음.
- **권고**: 코드(문서) 쪽 수정: web-chat-sdk README·byo-ui-headless 예제를 firstMessage 동봉 대신 webhook(profile만) → 첫 waiting_for_input 후 submit_message 패턴으로 교체. spec 변경 불필요.

### V-18 [minor] 3-auth-session §3.1 재로드 복원 시퀀스(GET 상태 확인 → 401 낙관적 refresh 1회 → 410 storage 정리) 미구현 — 위젯은 저장 세션으로 SSE 직행
- **spec**: spec/7-channel-web-chat/3-auth-session.md  (발견: fwd:webchat)
- **코드**: codebase/channel-web-chat/src/widget/use-widget.ts, codebase/channel-web-chat/src/lib/eia-client.ts
- **증거**: spec 3-auth-session §3.1 은 복원 시 "2. `GET /api/external/executions/:id` 로 상태 확인: 200→SSE 재연결 / 410→storage 정리 후 [ended] / 401→낙관적으로 `POST .../refresh-token` 1회 시도" 를 규정하고 §R4 가 그 근거를 상술한다. 그러나 use-widget.ts:398-406 복원 경로는 `loadSession` 성공 시 상태 확인 없이 곧바로 `dispatch(RESTORED)` + `openStream(saved)` 한다 — `EiaClient.getStatus`(eia-client.ts:94) 는 어디서도 호출되지 않고, 401 낙관적 refresh·410 시 storage 즉시 정리(복원 시점)도 없다. SSE 가 토큰 만료/blacklist 로 죽으면 console.warn(use-widget.ts:165-169) 만 남고 [ended] 전이가 일어나지 않는다. 1-widget-app §3.1 의 Last-Event-Id 재연결·5분 버퍼 만료 시 snapshot 폴백도 동일하게 미구현(openStream 의 lastEventId 인자는 호출처가 없는 dead param, eia-client.ts:127·131). 단 이는 2026-06-03 동기화 시점 이전부터의 상태(forward drift 아님)로 frontmatter `status: partial` 이 표시하지만, pending_plans 의 channel-web-chat-followups.md 가 "종결(parked, 2026-06-03) — 활성 TODO 0건" 이라 이 §3.1 갭을 추적하는 활성 항목이 없다.
- **권고**: 둘 중 하나: (a) §3.1 복원 시퀀스·SSE snapshot 폴백을 구현하는 후속 plan 항목을 만들거나(followups 재개), (b) v1 범위에서 보류임을 spec 본문(§3.1)에 명시(예: "v1 은 SSE 직행 복원, 상태 확인/낙관적 refresh 는 후속")해 partial 범위를 본문에서 식별 가능하게.

### V-19 [info — **해소됨**] 9-rag-search §2.2/§3.1/§4.2/§6 의 검색 불가 KB 명시 신호(not_searchable / kb_unsearchable)가 미구현 — 코드는 여전히 silent skip (단, pending plan 으로 추적 중인 의도적 spec-ahead)
- **spec**: spec/5-system/9-rag-search.md  (발견: fwd:misc-5)
- **코드**: codebase/backend/src/modules/knowledge-base/search/rag-search.service.ts, codebase/backend/src/nodes/ai/ai-agent/ai-agent.handler.ts, codebase/backend/src/nodes/ai/ai-agent/tool-providers/kb-tool-provider.ts, plan/in-progress/kb-unsearchable-warning.md
- **증거**: spec §2.2 는 embedding_dimension NULL KB 호출 시 tool_result 에 `status:"not_searchable"` + `reason`(reembedding_required/in_progress) + `note` 봉투를, §4.2 는 `ragDiagnostics.skipReason="kb_unsearchable"` 를 현재 동작처럼 기술한다 (§3.1 "명시적 사전 차단이며 ... 호출부에 노출한다"). 그러나 코드는 rag-search.service.ts:352 (groupVectorKbs `if (kb.embeddingDimension == null) continue;`)·:271(rerank 경로)·:379(isGraphKbSearchable) 에서 조용히 스킵해 빈 results 만 반환하고, ai-agent.handler.ts:139 의 skipReason 타입은 `'empty_kb_list' | 'no_results'` 뿐이며, kb-tool-provider.ts 에는 not_searchable 봉투 생성 코드가 없다. 이는 마지막 커밋 37ebd640 (docs-only, 2026-06 spec 신호화) 이 spec 을 먼저 갱신한 케이스로, frontmatter `status: partial` + `pending_plans: plan/in-progress/kb-unsearchable-warning.md` (실존, 설계·사용자 confirm 2026-06-06 완료) 로 정확히 추적되고 있다.
- **권고**: 별도 spec 조치 불요 — pending plan kb-unsearchable-warning.md 의 구현 완료로 해소될 추적 중 항목. 감사 관점에서는 9-rag-search.md frontmatter(status: partial + pending_plans)가 정확함을 확인함. 구현 전까지 본 spec 구간을 '현재 동작'으로 인용하지 않도록만 주의.
- **2026-06-10 추기**: rebase 중 유입된 main 커밋 b4a551a5(#511) 가 `not_searchable` 신호화·목록 경고를 구현 완료 — 본 항목은 더 이상 갭이 아님 (pending plan 정상 추적의 사례).

## 2. data-flow 검증 중 발견된 코드 측 갭 (audit 도메인)

### G-01 [major] §Overview·§1.1·§4: audit_log 의 호출자는 '각 도메인의 service (Workflows / Triggers / Integrations / Workspace
- **실제**: AuditLogsService.record 의 실제 호출자는 4개 모듈 9개 call site 뿐: integrations.service.ts(585,630,667,983,1096,1125 — 6곳), workspaces.service.ts:535(1곳), executions.service.ts:421(1곳), auth-configs.service.ts:191(1곳). workflows / triggers / alerts / schedules / llm-config / rerank-config 모듈은 audit 기록을 전혀 하지 않는다 (grep 'auditLogsService.record' 전수 + 각 모듈 grep 'audit' 0건으로 확인). spec/5-system/1-auth.md §4.1 이 기록 대상으로 약속한 workflow.*, trigger.*, member.*, schedule.*, llm_config.*, rerank_config.* 액션도 미구현 — 코드 쪽 audit coverage 갭(잠재 spec-violation)이기도 하다.
- **증거**: codebase/backend/src/modules/integrations/integrations.service.ts:585,630,667,983,1096,1125 · workspaces/workspaces.service.ts:535 · executions/executions.service.ts:421 · auth-configs/auth-configs.service.ts:191. 반례: codebase/backend/src/modules/workflows, triggers, alerts 어디에도 AuditLogsService import 없음.

### G-02 [major] §1.1: Action naming 은 `<resource>.<verb>` 이며 예시는 workflow.create, workflow.update, trigger.delete, i
- **실제**: 문서의 예시 6개 중 코드에 존재하는 것은 0개. 실제 기록되는 action 은 integration.created / integration.updated / integration.deleted / integration.rotated / integration.scope_changed / integration.reauthorized (과거분사형), workspace.transfer_ownership, auth_config.reveal, re_run_initiated 의 9종이다. 특히 executions 의 `re_run_initiated` (executions.service.ts:421) 는 문서가 명시한 `<resource>.<verb>` 규약 자체를 위반한다 (resourceType 'execution' 인데 dot-prefix 없음) — 코드를 `execution.re_run` 류로 고치거나 spec 의 규약 진술을 완화해야 하는 케이스.
- **증거**: codebase/backend/src/modules/executions/executions.service.ts:421 `action: 're_run_initiated'` · integrations.service.ts:585 `action: 'integration.created'` 등. grep 'workflow\.create|integration\.connect|alert_rule\.|member_role_change' → 코드 본문 0건 (audit-log-response.dto.ts 의 @ApiProperty example 만 존재).
